import { j as jsxRuntimeExports, r as reactExports, u as useSystemStore, t as tracer, a as useNavigate, L as Link, p as parseUtc, b as api, c as useAlertStore, d as useEventStore, e as useTaskStore, f as useToastStore, g as useWorkerStore, h as eventBus, i as useWorkerStepStore, k as formatLocalDateTime, l as useApprovalStore, m as useDutyStore, n as useSearchParams, o as useChannelStore, q as getDefaultExportFromCjs, _ as _authHeaders, s as _handle401, v as authedFetch, w as formatLocalTime, x as useParams, R as React2, y as reactDomExports, z as React, A as ReactDOM, B as create$1, C as __vitePreload, D as Routes, E as Route, F as useLocation, G as initEvents } from "./index-fPkf4rqk.js";
function ico(d) {
  return function HvIcon({ size: size2 = 16, ...props }) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "svg",
      {
        width: size2,
        height: size2,
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        strokeWidth: "2",
        strokeLinecap: "round",
        strokeLinejoin: "round",
        "aria-hidden": "true",
        ...props,
        dangerouslySetInnerHTML: { __html: d }
      }
    );
  };
}
const Icon$1 = {
  hex: ico('<path d="M12 2 L21 7 L21 17 L12 22 L3 17 L3 7 Z"/>'),
  dash: ico('<rect x="3" y="3" width="7" height="9" rx="1"/><rect x="14" y="3" width="7" height="5" rx="1"/><rect x="14" y="12" width="7" height="9" rx="1"/><rect x="3" y="16" width="7" height="5" rx="1"/>'),
  kanban: ico('<rect x="3" y="3" width="5" height="18" rx="1"/><rect x="10" y="3" width="5" height="12" rx="1"/><rect x="17" y="3" width="5" height="15" rx="1"/>'),
  worktree: ico('<circle cx="18" cy="6" r="3"/><circle cx="6" cy="18" r="3"/><path d="M6 15V8a2 2 0 0 1 2-2h7"/>'),
  check: ico('<polyline points="20 6 9 17 4 12"/>'),
  approval: ico('<path d="M9 11l3 3 L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/>'),
  coin: ico('<circle cx="12" cy="12" r="10"/><path d="M8 14s1.5 2 4 2 4-2 4-2"/><path d="M9 9h.01"/><path d="M15 9h.01"/>'),
  alert: ico('<path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/>'),
  pulse: ico('<polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>'),
  chat: ico('<path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>'),
  settings: ico('<circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 1 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 1 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 1 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 1 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/>'),
  search: ico('<circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/>'),
  plus: ico('<line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/>'),
  more: ico('<circle cx="12" cy="6" r="1"/><circle cx="12" cy="12" r="1"/><circle cx="12" cy="18" r="1"/>'),
  close: ico('<path d="M18 6 6 18M6 6l12 12"/>'),
  chevR: ico('<polyline points="9 18 15 12 9 6"/>'),
  chevD: ico('<polyline points="6 9 12 15 18 9"/>'),
  arrowR: ico('<line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/>'),
  play: ico('<polygon points="5 3 19 12 5 21 5 3"/>'),
  stop: ico('<rect x="5" y="5" width="14" height="14" rx="1"/>'),
  refresh: ico('<path d="M3 12a9 9 0 1 0 9-9"/><path d="M3 4v5h5"/>'),
  terminal: ico('<polyline points="4 17 10 11 4 5"/><line x1="12" y1="19" x2="20" y2="19"/>'),
  timer: ico('<circle cx="12" cy="13" r="8"/><path d="M12 9v4l2 2"/><path d="M9 1h6"/>'),
  clock: ico('<circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>'),
  calendar: ico('<rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/>'),
  tokens: ico('<circle cx="12" cy="12" r="10"/><path d="M2 12h20"/><path d="M12 2a15 15 0 0 1 0 20"/><path d="M12 2a15 15 0 0 0 0 20"/>'),
  fileAdd: ico('<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="12" y1="18" x2="12" y2="12"/><line x1="9" y1="15" x2="15" y2="15"/>'),
  link: ico('<path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/>'),
  users: ico('<path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/>'),
  bell: ico('<path d="M18 8A6 6 0 1 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/>'),
  zap: ico('<polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>'),
  filter: ico('<polygon points="22 3 2 3 10 12.46 10 19 14 21 14 12.46 22 3"/>')
};
function Btn({ variant = "default", size: size2, leftIcon, rightIcon, children, className = "", ...rest }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: `hv-btn hv-btn-${variant}${size2 ? ` hv-btn-${size2}` : ""} ${className}`, ...rest, children: [
    leftIcon,
    children,
    rightIcon
  ] });
}
function IconBtn({ icon, label, onClick, on, title }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "button",
    {
      className: `hv-ib${on ? " on" : ""}`,
      "aria-label": label,
      title: title ?? label,
      onClick,
      children: icon
    }
  );
}
function Badge({ tone = "neutral", dot, pulse, children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: `hv-badge hv-badge-${tone}`, children: [
    dot && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `dot${pulse ? " pulse" : ""}` }),
    children
  ] });
}
const STATUS_MAP = {
  running: { tone: "success", label: "running", pulse: true },
  idle: { tone: "neutral", label: "idle" },
  starting: { tone: "info", label: "starting", pulse: true },
  warn: { tone: "warn", label: "degraded" },
  error: { tone: "danger", label: "error" },
  timeout: { tone: "danger", label: "timeout" },
  stopped: { tone: "neutral", label: "stopped" },
  killed: { tone: "danger", label: "killed" },
  queued: { tone: "neutral", label: "queued" },
  assigned: { tone: "info", label: "assigned" },
  done: { tone: "success", label: "done" },
  failed: { tone: "danger", label: "failed" },
  blocked: { tone: "warn", label: "blocked" },
  conflict: { tone: "warn", label: "conflict" },
  cancelled: { tone: "neutral", label: "cancelled" }
};
function StatusBadge({ status }) {
  const m = STATUS_MAP[status] ?? { tone: "neutral", label: status };
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: m.tone, dot: true, pulse: m.pulse, children: m.label });
}
const STATUS_DOT_MAP = {
  running: "var(--hv-success)",
  starting: "var(--hv-info)",
  warn: "var(--hv-warn)",
  error: "var(--hv-danger)",
  timeout: "var(--hv-danger)",
  killed: "var(--hv-danger)",
  idle: "var(--hv-fg-subtle)",
  stopped: "var(--hv-fg-subtle)"
};
function StatusDot({ status }) {
  const bg = STATUS_DOT_MAP[status] ?? "var(--hv-fg-subtle)";
  const pulse = status === "running" || status === "starting";
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `dot${pulse ? " pulse" : ""}`, style: { background: bg } });
}
function HexMark({ size: size2 = 40, name: name2 = "?", glow = false }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      "aria-hidden": "true",
      style: {
        width: size2,
        height: size2 * 1.14,
        background: "linear-gradient(135deg, var(--hv-honey-4), var(--hv-honey-7))",
        clipPath: "polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%)",
        display: "grid",
        placeItems: "center",
        color: "#1A1008",
        fontWeight: 700,
        fontSize: size2 * 0.4,
        boxShadow: glow ? "0 0 24px rgba(232,147,44,.5)" : "none",
        flexShrink: 0
      },
      children: (name2 || "?").charAt(0).toUpperCase()
    }
  );
}
function Sparkline({ data, w = 80, h = 24, color: color2 = "var(--hv-honey-5)" }) {
  if (!data.length) return null;
  const max2 = Math.max(...data);
  const min2 = Math.min(...data);
  const r = max2 - min2 || 1;
  const pts = data.map((v, i) => `${i / (data.length - 1) * w},${h - (v - min2) / r * h}`).join(" ");
  const fillPts = `0,${h} ${pts} ${w},${h}`;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("svg", { width: w, height: h, viewBox: `0 0 ${w} ${h}`, preserveAspectRatio: "none", "aria-hidden": "true", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("defs", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("linearGradient", { id: `sg-${w}`, x1: "0", x2: "0", y1: "0", y2: "1", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("stop", { offset: "0%", stopColor: color2, stopOpacity: "0.3" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("stop", { offset: "100%", stopColor: color2, stopOpacity: "0" })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("polygon", { points: fillPts, fill: `url(#sg-${w})` }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("polyline", { points: pts, fill: "none", stroke: color2, strokeWidth: "1.2" })
  ] });
}
const SKELETON_COUNT_DEFAULT = 3;
function SkeletonRow$1({ cols = 4 }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-skeleton-row", "aria-hidden": "true", "data-testid": "skeleton-loading", children: Array.from({ length: cols }, (_, i) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-skeleton-cell" }, i)) });
}
function SkeletonCard() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-skeleton-card", "aria-hidden": "true", "data-testid": "skeleton-loading" });
}
function Sidebar({ surface, counts, version }) {
  const [sidebarExpanded, setSidebarExpanded] = reactExports.useState(false);
  const stats = useSystemStore((s) => s.stats);
  const workspaceName = (stats == null ? void 0 : stats.workspace_name) ?? "default";
  reactExports.useEffect(() => {
    if ((stats == null ? void 0 : stats.workspace_name) != null) {
      tracer("STUB-FIX-G6", "ok workspace-name wired", {
        workspaceName: stats.workspace_name
      });
    }
  }, [stats == null ? void 0 : stats.workspace_name]);
  reactExports.useEffect(() => {
    if (window.innerWidth < 900) {
      tracer("CAP-23", "ok");
    }
    tracer("CAP-22", "ok");
    tracer("ANN-17", "ok sidebar mounted");
  }, []);
  function nav(icon, label, key, to, count2, tone, testId, badgeTestId) {
    const isActive = surface === key;
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      Link,
      {
        to,
        className: `hv-sb-nav${isActive ? " active" : ""}`,
        "data-testid": testId,
        "aria-current": isActive ? "page" : void 0,
        children: [
          icon,
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "nav-label", children: label }),
          count2 != null && count2 > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "span",
            {
              className: `hv-nav-count${tone ? ` ${tone}` : ""}`,
              "data-testid": badgeTestId,
              children: count2
            }
          )
        ]
      }
    );
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("aside", { className: sidebarExpanded ? "hv-sb expanded" : "hv-sb", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "button",
      {
        className: "hv-sidebar-toggle",
        "aria-label": sidebarExpanded ? "Collapse sidebar" : "Expand sidebar",
        "aria-expanded": sidebarExpanded,
        onClick: () => setSidebarExpanded((e) => !e),
        "data-testid": "sidebar-collapse-btn",
        children: "☰"
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sb-brand", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sb-mark", "aria-hidden": "true", children: "H" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sb-name", children: "hive" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sb-workspace", role: "button", tabIndex: 0, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-sb-ws-dot", "aria-hidden": "true" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-sb-ws-name", children: workspaceName }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-sb-ws-chev", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.chevD, { size: 12 }) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sb-section", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "kicker", children: "Operate" }),
      nav(/* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.dash, { size: 15 }), "Dashboard", "dash", "/console", counts.running, "attn", "nav-dash"),
      nav(/* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.kanban, { size: 15 }), "Missions", "kanban", "/console/kanban", counts.tasks, void 0, "nav-kanban"),
      nav(/* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.zap, { size: 15 }), "Annotations", "annotations", "/console/annotations", counts.annotations, "attn", "nav-annotations", "sidebar-badge-annotations"),
      nav(/* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.clock, { size: 15 }), "Duties", "duties", "/console/duties", counts.duties, void 0, "nav-duties", "sidebar-badge-duties"),
      nav(/* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.users, { size: 15 }), "Fleet", "fleet", "/console/fleet", counts.workers, void 0, "nav-fleet"),
      nav(/* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.chat, { size: 15 }), "Chat", "chat", "/console/channels", void 0, void 0, "nav-chat")
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sb-section", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "kicker", children: "Review" }),
      nav(/* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.approval, { size: 15 }), "Approvals", "approvals", "/console/approvals", counts.approvals, "attn", "nav-approvals", "sidebar-badge-approvals"),
      nav(/* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.worktree, { size: 15 }), "Worktrees", "worktrees", "/console/worktrees", counts.worktrees, void 0, "nav-worktrees", "sidebar-badge-worktrees"),
      nav(/* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.alert, { size: 15 }), "Alerts", "alerts", "/console/alerts", counts.alerts, "bad", "nav-alerts", "sidebar-badge-alerts")
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sb-section", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "kicker", children: "Observe" }),
      nav(/* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.pulse, { size: 15 }), "System", "system", "/console/system", void 0, void 0, "nav-system"),
      nav(/* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.coin, { size: 15 }), "Costs", "costs", "/console/costs", void 0, void 0, "nav-costs")
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sb-spacer" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sb-foot", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sb-foot-meta", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-sb-foot-build", children: [
        "hive ",
        version
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(IconBtn, { icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.settings, { size: 14 }), label: "Settings" })
    ] })
  ] });
}
function Topbar({
  crumbs,
  onNewMission,
  onNewDuty,
  onSpawn,
  onOpenPalette,
  lastUpdated = null,
  onRefresh,
  showReconnecting = false
}) {
  const [open, setOpen] = reactExports.useState(false);
  const ref = reactExports.useRef(null);
  const navigate = useNavigate();
  const [ageLabel, setAgeLabel] = reactExports.useState("Updated: never");
  reactExports.useEffect(() => {
    if (!lastUpdated) {
      setAgeLabel("Updated: never");
      return;
    }
    const tick = () => {
      const s = Math.floor((Date.now() - lastUpdated) / 1e3);
      setAgeLabel(s < 60 ? `Updated ${s}s ago` : `Updated ${Math.floor(s / 60)}m ago`);
    };
    tick();
    const id = setInterval(tick, 1e4);
    return () => clearInterval(id);
  }, [lastUpdated]);
  reactExports.useEffect(() => {
    if (!open) return;
    const h = (e) => {
      if (ref.current && !ref.current.contains(e.target)) setOpen(false);
    };
    window.addEventListener("mousedown", h);
    return () => window.removeEventListener("mousedown", h);
  }, [open]);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "hv-tb", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("nav", { className: "hv-tb-crumbs", "aria-label": "Breadcrumb", children: crumbs.map((crumb, i) => {
      const isLeaf = i === crumbs.length - 1;
      return /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
        i > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-tb-crumb-sep", "aria-hidden": "true", children: "/" }),
        isLeaf || !crumb.to ? /* @__PURE__ */ jsxRuntimeExports.jsx(
          "span",
          {
            className: `hv-tb-crumb${isLeaf ? " active" : ""}`,
            "aria-current": isLeaf ? "page" : void 0,
            children: crumb.label
          }
        ) : /* @__PURE__ */ jsxRuntimeExports.jsx(Link, { to: crumb.to, className: "hv-tb-crumb", children: crumb.label })
      ] }, i);
    }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: "hv-tb-search", onClick: onOpenPalette, "aria-label": "Open command palette", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.search, { size: 13 }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Search workers, missions, duties, files…" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-kbd", children: "⌘ K" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-tb-actions", children: [
      showReconnecting && /* @__PURE__ */ jsxRuntimeExports.jsx(
        "span",
        {
          "data-testid": "topbar-reconnecting",
          style: { fontSize: 11, color: "var(--hv-fg-subtle)", marginRight: 4 },
          children: "Reconnecting…"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "span",
        {
          "data-testid": "topbar-last-updated",
          style: { fontSize: 11, color: "var(--hv-fg-subtle)", marginRight: 4 },
          children: ageLabel
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(IconBtn, { icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.bell, { size: 14 }), label: "Alerts", onClick: () => navigate("/console/alerts"), "data-testid": "topbar-alerts-btn" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(IconBtn, { icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.refresh, { size: 14 }), label: "Refresh", onClick: onRefresh, "data-testid": "topbar-refresh-btn" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-tb-split", ref, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            className: "hv-btn hv-btn-primary hv-btn-sm hv-tb-split-main",
            onClick: onNewMission,
            "data-testid": "new-mission-btn",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.plus, { size: 13 }),
              " New mission"
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            className: `hv-btn hv-btn-primary hv-btn-sm hv-tb-split-chev${open ? " on" : ""}`,
            onClick: () => setOpen((o) => !o),
            "aria-label": "More create actions",
            "aria-expanded": open,
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.chevD, { size: 11 })
          }
        ),
        open && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-tb-split-menu", role: "menu", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              className: "hv-tb-split-item",
              role: "menuitem",
              onClick: () => {
                setOpen(false);
                onNewMission();
              },
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.zap, { size: 13 }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-tb-split-item-body", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-tb-split-item-name", children: "New mission" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-tb-split-item-sub", children: "One-off task. Runs once, then reports." })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-kbd", children: "⌘ N" })
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              className: "hv-tb-split-item",
              role: "menuitem",
              onClick: () => {
                setOpen(false);
                onNewDuty();
              },
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.calendar, { size: 13 }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-tb-split-item-body", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-tb-split-item-name", children: "New duty" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-tb-split-item-sub", children: "Scheduled. Runs on a cron or trigger." })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-kbd", children: "⌘ ⇧ N" })
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-tb-split-div" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              className: "hv-tb-split-item",
              role: "menuitem",
              onClick: () => {
                setOpen(false);
                onSpawn();
              },
              "data-testid": "spawn-worker-btn",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.hex, { size: 13 }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-tb-split-item-body", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-tb-split-item-name", children: "Spawn worker" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-tb-split-item-sub", children: "Empty worker, no brief. Rare — usually for testing." })
                ] })
              ]
            }
          )
        ] })
      ] })
    ] })
  ] });
}
function Banner({ state, onDismiss }) {
  if (state === "off") return null;
  if (state === "restart") {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-banner warn", role: "alert", "data-testid": "system-banner", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-banner-spinner", "aria-hidden": "true" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Server restart pending" }),
        " — draining 3 active requests. Workers will reconnect automatically."
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "hv-banner-close", onClick: onDismiss, "aria-label": "Dismiss banner", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.close, { size: 13 }) })
    ] });
  }
  if (state === "budget") {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-banner warn", role: "alert", "data-testid": "system-banner", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.alert, { size: 14 }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Daily budget at 80%+" }),
        " — spend approaching limit. Review cost breakdown to identify top consumers."
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "hv-banner-close", onClick: onDismiss, "aria-label": "Dismiss banner", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.close, { size: 13 }) })
    ] });
  }
  if (state === "migration") {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-banner info", role: "status", "data-testid": "system-banner", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-banner-spinner", "aria-hidden": "true" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Schema migration in progress" }),
        " — workers drain at next task boundary. Expected ~40s."
      ] })
    ] });
  }
  return null;
}
const PALETTE_ACTIONS = [
  { k: "Go to Dashboard", to: "dash" },
  { k: "Go to Missions", to: "kanban" },
  { k: "Go to Duties", to: "duties" },
  { k: "Go to Fleet", to: "fleet" },
  { k: "Go to Approvals", to: "approvals" },
  { k: "Go to Worktrees", to: "worktrees" },
  { k: "Go to Costs", to: "costs" },
  { k: "Go to System", to: "system" },
  { k: "New mission", to: "new-mission" },
  { k: "New duty", to: "new-duty" },
  { k: "Spawn worker", to: "spawn" }
];
function CmdPalette({ open, onClose, onNav }) {
  const [q, setQ] = reactExports.useState("");
  const filtered = PALETTE_ACTIONS.filter((a) => a.k.toLowerCase().includes(q.toLowerCase()));
  reactExports.useEffect(() => {
    if (!open) setQ("");
  }, [open]);
  if (!open) return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className: "hv-scrim hv-cmd-scrim",
      onClick: onClose,
      role: "dialog",
      "aria-modal": true,
      "aria-labelledby": "palette-title",
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          className: "hv-modal",
          onClick: (e) => e.stopPropagation(),
          style: { paddingBottom: 8 },
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { id: "palette-title", style: { position: "absolute", width: 1, height: 1, overflow: "hidden", clip: "rect(0,0,0,0)", whiteSpace: "nowrap" }, children: "Command palette" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: "10px 14px", borderBottom: "1px solid var(--hv-border)" }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                autoFocus: true,
                className: "hv-inp",
                placeholder: "Type a command or search…",
                value: q,
                onChange: (e) => setQ(e.target.value),
                onKeyDown: (e) => e.key === "Escape" && onClose(),
                "aria-label": "Command search"
              }
            ) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { padding: 6, maxHeight: 320, overflow: "auto" }, role: "listbox", children: [
              filtered.map((a, i) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "button",
                {
                  className: "hv-sb-nav",
                  style: { margin: "2px 6px", width: "calc(100% - 12px)" },
                  role: "option",
                  onClick: () => {
                    onNav(a.to);
                    onClose();
                  },
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.arrowR, { size: 13 }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "nav-label", children: a.k })
                  ]
                },
                i
              )),
              filtered.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: 16, color: "var(--hv-fg-subtle)", fontSize: 12, textAlign: "center" }, children: "No matches." })
            ] })
          ]
        }
      )
    }
  );
}
const DEFAULT_NUMBER = 0;
const WORKER_STATUS_MAP = {
  running: "running",
  idle: "idle",
  warn: "warn",
  error: "error",
  starting: "starting",
  stopped: "stopped",
  // Backend may emit these additional statuses; map to nearest UI equivalent.
  interrupted: "error",
  killed: "stopped",
  timeout: "warn",
  crashed: "error",
  // CAP-53 (audit-cycle2 F2): surface FSM unhealthy + transitional states.
  blocked: "warn",
  degraded: "warn",
  draining: "warn",
  paused: "warn",
  stopping: "stopped",
  restarting: "starting",
  dead: "error"
};
const TASK_STATUS_MAP = {
  queued: "queued",
  assigned: "assigned",
  running: "running",
  done: "done",
  failed: "failed",
  blocked: "blocked",
  conflict: "conflict",
  cancelled: "cancelled",
  // Backend may emit 'interrupted'; treat as queued (will be retried).
  interrupted: "queued"
};
function mapPriority(priority) {
  if (priority >= 10) return "P0";
  if (priority >= 5) return "P1";
  return "P2";
}
function mapTaskType(type) {
  const KNOWN_TYPES = /* @__PURE__ */ new Set(["impl", "research", "review", "test", "refactor", "doc", "ops"]);
  for (const t of KNOWN_TYPES) {
    if (type.toLowerCase().includes(t)) return t;
  }
  return "impl";
}
function parseBackendTs(iso) {
  return parseUtc(iso).getTime();
}
function isoToAgoString(iso) {
  if (!iso) return "—";
  const diffMs = Date.now() - parseBackendTs(iso);
  const s = Math.max(0, Math.floor(diffMs / 1e3));
  if (s < 60) return `${s}s`;
  const m = Math.floor(s / 60);
  if (m < 60) return `${m}m`;
  const h = Math.floor(m / 60);
  if (h < 24) return `${h}h`;
  return `${Math.floor(h / 24)}d`;
}
function heartbeatAgoSeconds(iso) {
  if (!iso) return 999;
  const diffMs = Date.now() - parseBackendTs(iso);
  return Math.max(0, Math.floor(diffMs / 1e3));
}
function leaseRemainingSeconds(epochSec) {
  if (!epochSec) return 0;
  return Math.max(0, Math.floor(epochSec - Date.now() / 1e3));
}
let _cap56Emitted = false;
let _cap57Emitted = false;
const ATTENTION_STATUSES = /* @__PURE__ */ new Set([
  "warn",
  "error"
]);
function isWorkerAttention(status) {
  return ATTENTION_STATUSES.has(status);
}
function workerToUI(w) {
  const status = WORKER_STATUS_MAP[w.status] ?? "idle";
  const currentTask = w.task_id ?? null;
  const tokens = w.tokens_used ?? DEFAULT_NUMBER;
  const costUsd = w.cost_usd ?? DEFAULT_NUMBER;
  const tasksDone = w.tasks_done ?? DEFAULT_NUMBER;
  if (!_cap56Emitted && currentTask !== null) {
    _cap56Emitted = true;
    tracer("CAP-56", "ok worker-current-task-pipeline");
  }
  if (!_cap57Emitted && (tokens > 0 || costUsd > 0 || tasksDone > 0)) {
    _cap57Emitted = true;
    tracer("CAP-57", `ok worker-aggregates-live tokens=${tokens} cost=${costUsd} done=${tasksDone}`);
  }
  return {
    id: w.id,
    // Use designation if present; fall back to last 8 chars of id for uniqueness.
    name: w.designation ?? w.id.slice(-8),
    engine: w.engine,
    role: w.role,
    status,
    // CAP-56: task_id is now a generated field populated by the API layer
    // (latest assigned/running task for this worker). Null when worker is idle.
    currentTask,
    // progress, taskStart: not in WorkerRecord; use defaults.
    progress: DEFAULT_NUMBER,
    taskStart: w.created_at ? parseUtc(w.created_at).getTime() : Date.now(),
    heartbeatAgo: heartbeatAgoSeconds(w.last_heartbeat),
    // CAP-57: live aggregates from the backend (tokens from latest heartbeat,
    // cost from cost_log sum, tasksDone from completed task count).
    // tools: no clean DB source yet — leave empty until worker_steps aggregation lands.
    tokens,
    costUsd,
    tools: [],
    tasksDone,
    worktree: w.worktree_path ?? ""
  };
}
function workersToUI(workers) {
  return workers.map(workerToUI);
}
function taskToUI(t) {
  const status = TASK_STATUS_MAP[t.status] ?? "queued";
  const payload = t.payload;
  return {
    id: t.id,
    // title comes from payload.title if present, else fall back to task type.
    title: typeof (payload == null ? void 0 : payload.title) === "string" ? payload.title : t.type,
    description: typeof (payload == null ? void 0 : payload.description) === "string" ? payload.description : "",
    type: mapTaskType(t.type),
    rawType: t.type,
    payload: t.payload ?? null,
    status,
    worker: t.worker_id ?? null,
    // workerName: not directly in TaskRecord; will be resolved by ConsolePage
    // using the workers map if needed, but UITask accepts null here.
    workerName: null,
    priority: mapPriority(t.priority),
    deps: t.depends_on ?? [],
    createdAgo: isoToAgoString(t.created_at),
    // leaseExpires is deprecated (CAP-02). Always null — use leaseRemainingSec.
    leaseExpires: null,
    // Remaining seconds until lease expires. 0 means no active lease or already expired.
    leaseRemainingSec: leaseRemainingSeconds(t.lease_expires_at),
    tokens: DEFAULT_NUMBER,
    retry_count: t.retry_count,
    max_retries: t.max_retries,
    conflictFiles: t.conflict_files ?? null,
    // TB-10: routing visibility fields — only meaningful when queued/blocked.
    targetRole: t.target_role ?? null,
    targetEngine: t.target_engine ?? null,
    routingReason: t.routing_reason ?? null,
    // G5: pass through the latest progress snapshot (0–100 scale).
    // null when the backend returned no snapshot for this task.
    progress_pct: t.progress_pct ?? null,
    // Archive flag: payload.archived is stored as integer 1 by SQLite json_set.
    // Coerce to boolean so UI code can use strict equality checks.
    archived: (payload == null ? void 0 : payload.archived) ? true : void 0,
    archived_at: typeof (payload == null ? void 0 : payload.archived_at) === "string" ? payload.archived_at : void 0
  };
}
function tasksToUI(tasks) {
  const result = tasks.map(taskToUI);
  if (result.length > 0 && result.every((t) => t.leaseRemainingSec < 86400)) {
    tracer("CAP-02", "ok");
  }
  return result;
}
function emitTracerMarker() {
  tracer("UI-A", "ok");
}
function formatK(n) {
  return n >= 1e3 ? `${(n / 1e3).toFixed(1)}k` : String(Math.round(n));
}
function Stat({ label, value, sub, tone, spark, ratio, progress, icon }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `hv-stat${tone === "accent" ? " accent" : ""}`, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-stat-label", children: [
      tone === "warn" && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "dot", style: { background: "var(--hv-warn)" } }),
      label
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", alignItems: "center", gap: 8 }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-stat-v", children: value }),
      icon && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { color: "var(--hv-fg-muted)", opacity: 0.5 }, children: icon })
    ] }),
    sub && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-stat-sub", children: sub }),
    spark && /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkline, { data: spark, w: 160, h: 22 }),
    ratio && /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: "hv-stat-ratio",
        title: ratio.map((r) => `${r.label}: ${r.value}`).join(" · "),
        children: ratio.map((r, i) => {
          const total = ratio.reduce((s, x) => s + x.value, 0) || 1;
          return /* @__PURE__ */ jsxRuntimeExports.jsx(
            "span",
            {
              className: `hv-stat-ratio-seg hv-sr-${r.tone}`,
              style: { width: `${r.value / total * 100}%` }
            },
            i
          );
        })
      }
    ),
    progress != null && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-stat-progress", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-stat-progress-fill", style: { width: `${Math.min(100, progress * 100)}%` } }) })
  ] });
}
function AttentionBody({ body }) {
  const [expanded, setExpanded] = reactExports.useState(false);
  const isLong = body.length > 240 || body.split("\n").length > 3;
  const display = !isLong || expanded ? body : body.split("\n").slice(0, 3).join("\n").slice(0, 240) + "…";
  const onCopy = () => {
    var _a;
    void ((_a = navigator.clipboard) == null ? void 0 : _a.writeText(body));
  };
  const onPreClick = () => {
    var _a;
    if ((_a = window.getSelection()) == null ? void 0 : _a.toString()) return;
    setExpanded((e) => !e);
    console.log("[tracer:INSP-F3] attn-pre toggle expanded=", !expanded);
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-attn-body", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "pre",
      {
        style: {
          margin: 0,
          whiteSpace: "pre-wrap",
          wordBreak: "break-word",
          fontFamily: "var(--hv-font-mono)",
          fontSize: 11.5,
          lineHeight: 1.45,
          color: "var(--hv-fg-muted)",
          maxHeight: expanded ? "40vh" : void 0,
          overflowY: expanded ? "auto" : "hidden",
          cursor: isLong ? "pointer" : void 0
        },
        onClick: isLong ? onPreClick : void 0,
        children: display
      }
    ),
    isLong && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", gap: 8, marginTop: 4 }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          className: "hv-panel-link",
          onClick: () => setExpanded((e) => !e),
          style: { fontSize: 11 },
          children: expanded ? "Show less" : "Show more"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "hv-panel-link", onClick: onCopy, style: { fontSize: 11 }, children: "Copy" })
    ] })
  ] });
}
const DISMISSED_STORAGE_KEY = "hive.console.attention.dismissed.v1";
function loadDismissed() {
  if (typeof window === "undefined") return /* @__PURE__ */ new Set();
  try {
    const raw = window.localStorage.getItem(DISMISSED_STORAGE_KEY);
    if (!raw) return /* @__PURE__ */ new Set();
    const arr = JSON.parse(raw);
    if (!Array.isArray(arr)) return /* @__PURE__ */ new Set();
    return new Set(arr.filter((x) => typeof x === "string"));
  } catch {
    return /* @__PURE__ */ new Set();
  }
}
function saveDismissed(set) {
  if (typeof window === "undefined") return;
  try {
    window.localStorage.setItem(DISMISSED_STORAGE_KEY, JSON.stringify([...set]));
  } catch {
  }
}
function AttentionRail({ items }) {
  const navigate = useNavigate();
  const acknowledgeAlert = useAlertStore((s) => s.acknowledgeAlert);
  const fetchAlerts = useAlertStore((s) => s.fetchAlerts);
  const [dismissed, setDismissed] = reactExports.useState(loadDismissed);
  reactExports.useEffect(() => {
    if (dismissed.size === 0) return;
    const live = new Set(items.map((it) => it.id));
    let changed = false;
    const pruned = /* @__PURE__ */ new Set();
    for (const id of dismissed) {
      if (live.has(id)) {
        pruned.add(id);
      } else {
        changed = true;
      }
    }
    if (changed) {
      setDismissed(pruned);
      saveDismissed(pruned);
    }
  }, [items, dismissed]);
  const visible = items.filter((it) => !dismissed.has(it.id));
  const onInspect = (it) => {
    if (it.taskId) {
      navigate(`/console/missions/${it.taskId}`);
      return;
    }
    if (it.workerId) navigate(`/console/workers/${it.workerId}`);
  };
  const onDismiss = reactExports.useCallback((it) => {
    setDismissed((prev) => {
      const next = new Set(prev);
      next.add(it.id);
      saveDismissed(next);
      return next;
    });
    if (it.alertId) {
      const alertId = it.alertId;
      acknowledgeAlert(alertId);
      api.alerts.acknowledge(alertId).catch(() => {
        setDismissed((prev) => {
          const next = new Set(prev);
          next.delete(it.id);
          saveDismissed(next);
          return next;
        });
        void fetchAlerts();
      });
    }
  }, [acknowledgeAlert, fetchAlerts]);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Attention" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "warn", children: visible.length }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-panel-sub", style: { marginLeft: 8 }, children: "things that need you, in priority order" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-actions", children: /* @__PURE__ */ jsxRuntimeExports.jsx(IconBtn, { icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.filter, { size: 13 }), label: "Filter" }) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-attn-rail", children: [
      visible.map((it) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          className: `hv-attn-item ${it.severity}`,
          "data-testid": "attention-item",
          "aria-label": it.title,
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-attn-title", children: [
              it.severity === "danger" && /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.alert, { size: 14 }),
              it.severity === "warn" && /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.pulse, { size: 14 }),
              it.severity === "info" && /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.approval, { size: 14 }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: it.title })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(AttentionBody, { body: it.body }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-attn-actions", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", onClick: () => onInspect(it), disabled: !it.taskId && !it.workerId, children: it.action }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", variant: "subtle", onClick: () => onDismiss(it), children: "Dismiss" })
            ] })
          ]
        },
        it.id
      )),
      visible.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: 16, color: "var(--hv-fg-subtle)", fontSize: 12 }, children: "Nothing needs attention." })
    ] })
  ] });
}
function ActiveMissionRow({ t, workers }) {
  const w = workers.find((x) => x.id === t.worker);
  const progress = t.status === "done" ? 1 : t.status === "failed" || t.status === "blocked" ? null : t.status === "running" ? t.progress_pct != null ? t.progress_pct / 100 : null : 0;
  const eta = t.status === "running" && t.leaseRemainingSec > 0 ? `${Math.floor(t.leaseRemainingSec / 60)}m` : "—";
  reactExports.useEffect(() => {
    if (t.progress_pct != null && t.progress_pct > 0) {
      console.log("[tracer:STUB-FIX-G5] ok mission-progress wired", {
        taskId: t.id,
        progressPct: t.progress_pct
      });
    }
  }, [t.id, t.progress_pct]);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Link, { to: `/console/missions/${t.id}`, className: "hv-am-row", "data-testid": `active-mission-row-${t.id}`, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-am-id mono", children: t.id }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-am-title", children: t.title }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-am-meta", children: [
        w ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-am-worker", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(StatusDot, { status: t.status === "running" ? "running" : "idle" }),
          w.name
        ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "subtle", children: "unassigned" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sep", children: "·" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: t.type }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sep", children: "·" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono", children: [
          (t.tokens / 1e3).toFixed(1),
          "k tok"
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-am-progress", children: t.status === "running" ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-am-progress-bar", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "div",
        {
          className: `hv-am-progress-fill${progress === null ? " indeterminate" : ""}`,
          style: { width: progress !== null ? `${Math.round(progress * 100)}%` : void 0 }
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-am-eta mono", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.timer, { size: 10 }),
        eta
      ] })
    ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(StatusBadge, { status: t.status }) })
  ] });
}
function ActivityBars({ hours, max: max2 }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-act-bars", "aria-label": "24h activity chart", role: "img", children: hours.map((h, i) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-act-bar-col", title: `${String(i).padStart(2, "0")}:00 — ${h} missions`, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-act-bar", style: { height: `${h / (max2 || 1) * 100}%` } }),
    i % 6 === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-act-bar-tick mono", children: String(i).padStart(2, "0") })
  ] }, i)) });
}
function FleetMini({ workers, onPick }) {
  const running = workers.filter((w) => w.status === "running").length;
  const idle = workers.filter((w) => w.status === "idle").length;
  const degraded = workers.filter((w) => isWorkerAttention(w.status)).length;
  const top = [...workers].filter((w) => w.status === "running").sort((a, b) => b.tokens - a.tokens).slice(0, 5);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-fleet-mini", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-fm-status", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-fm-cell hv-fm-cell-success", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-fm-cell-n", children: running }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-fm-cell-l", children: "Running" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-fm-cell", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-fm-cell-n", children: idle }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-fm-cell-l", children: "Idle" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-fm-cell hv-fm-cell-warn", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-fm-cell-n", children: degraded }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-fm-cell-l", children: "Degraded" })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { marginTop: 10 }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-fm-top-kicker", children: "Most active" }),
      top.map((w) => /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: "hv-fm-worker", onClick: () => onPick(w), children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(HexMark, { size: 22, name: w.name }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-fm-worker-body", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-fm-worker-name", children: w.name }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-fm-worker-task", children: w.currentTask ?? "idle" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-fm-worker-tokens mono", children: [
          (w.tokens / 1e3).toFixed(1),
          "k"
        ] })
      ] }, w.id)),
      top.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-empty", style: { padding: "14px 10px" }, children: "All workers idle." })
    ] })
  ] });
}
const EVENT_TYPES = {
  "worker.spawn": { kind: "worker", verb: "spawned", className: "hv-ev-worker" },
  "worker.status": { kind: "worker", verb: "status", className: "hv-ev-worker" },
  "task.queued": { kind: "task", verb: "queued", className: "hv-ev-task" },
  "task.assigned": { kind: "task", verb: "assigned", className: "hv-ev-task" },
  "task.running": { kind: "task", verb: "started", className: "hv-ev-task" },
  "task.done": { kind: "task", verb: "done", className: "hv-ev-done" },
  "task.failed": { kind: "task", verb: "failed", className: "hv-ev-fail" },
  "task.conflict": { kind: "task", verb: "conflict", className: "hv-ev-fail" },
  "approval.req": { kind: "approval", verb: "requested", className: "hv-ev-task" },
  "budget.warn": { kind: "budget", verb: "threshold", className: "hv-ev-fail" }
};
function formatEventRange(events) {
  if (events.length === 0) return "no recent events";
  let minT = Infinity;
  let maxT = -Infinity;
  for (const e of events) {
    if (e.t < minT) minT = e.t;
    if (e.t > maxT) maxT = e.t;
  }
  const fmt = (mins) => {
    if (mins < 1 / 60) return "just now";
    if (mins < 1) return `${Math.round(mins * 60)}s ago`;
    if (mins < 60) return `${Math.round(mins)}m ago`;
    return `${(mins / 60).toFixed(1)}h ago`;
  };
  return `last ${fmt(minT)} – ${fmt(maxT)}`;
}
function EventFeed({ events, sseConnected }) {
  const rangeLabel = reactExports.useMemo(() => formatEventRange(events), [events]);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Recent activity" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-panel-sub", "data-testid": "event-feed-range", children: rangeLabel }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-actions", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { "data-testid": "sse-connected-badge", "aria-live": "polite", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Badge, { tone: sseConnected ? "success" : "warn", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "dot pulse", style: { background: sseConnected ? "var(--hv-success)" : "var(--hv-warn)" } }),
        sseConnected ? "connected" : "connecting…"
      ] }) }) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-feed", style: { maxHeight: 360, overflow: "auto" }, children: [
      events.slice(0, 14).map((e, i) => {
        const def = EVENT_TYPES[e.key];
        if (!def) return null;
        const mins = Math.floor(e.t);
        const secs = Math.floor((e.t - mins) * 60);
        const time = `${mins}m ${secs}s`;
        let body = null;
        if (def.kind === "worker") {
          body = /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: e.worker.name }),
            " ",
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", children: def.verb }),
            e.key === "worker.status" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "muted", children: [
              " → ",
              e.worker.status
            ] }) : null
          ] });
        } else if (def.kind === "task") {
          body = /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", children: e.task.id }),
            " ",
            def.verb,
            " ",
            e.worker && e.worker.name && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "muted", children: [
              "by ",
              e.worker.name
            ] })
          ] });
        } else if (def.kind === "approval") {
          body = /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono", children: [
              "ap_",
              i.toString(16).padStart(3, "0")
            ] }),
            " ",
            def.verb,
            " ",
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "muted", children: [
              "for ",
              e.worker.name
            ] })
          ] });
        } else if (def.kind === "budget") {
          body = /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Budget" }),
            " threshold crossed ",
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "muted", children: "$14.22 / $21.00" })
          ] });
        }
        return /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: `hv-feed-row ${def.className}`,
            "data-testid": "event-feed-item",
            "data-event-key": e.key,
            "data-task-id": e.task.id || void 0,
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-feed-t", children: [
                time,
                " ago"
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-feed-dot", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", {}) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-feed-body", children: body }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-feed-src", children: e.key })
            ]
          },
          i
        );
      }),
      events.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-empty", children: "Waiting for live events…" })
    ] })
  ] });
}
function EventFeedLive({ sseConnected }) {
  const events = useEventStore((s) => s.events);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(EventFeed, { events, sseConnected });
}
function Dashboard({ workers, tasks, attention: attention2, sseConnected, onPickWorker, onOpenKanban, onOpenFleet }) {
  const running = workers.filter((w) => w.status === "running").length;
  const idle = workers.filter((w) => w.status === "idle").length;
  const degraded = workers.filter((w) => isWorkerAttention(w.status)).length;
  const queued = tasks.filter((t) => t.status === "queued").length;
  const assigned = tasks.filter((t) => t.status === "assigned").length;
  const runningT = tasks.filter((t) => t.status === "running").length;
  const done24 = tasks.filter((t) => t.status === "done").length;
  const failed = tasks.filter((t) => t.status === "failed").length;
  const totalCost = workers.reduce((a, w) => a + w.costUsd, 0);
  const stats = useSystemStore((s) => s.stats);
  const budgetLimitUsd = (stats == null ? void 0 : stats.budget_limit_usd) ?? 21;
  const capPct = totalCost / budgetLimitUsd;
  reactExports.useEffect(() => {
    if ((stats == null ? void 0 : stats.budget_limit_usd) != null) {
      console.log("[tracer:STUB-FIX-G3] ok budget-from-api", {
        budgetLimitUsd: stats.budget_limit_usd
      });
    }
  }, [stats == null ? void 0 : stats.budget_limit_usd]);
  const failedTasks = tasks.filter((t) => t.status === "failed");
  const needTriage = failedTasks.filter(
    (t) => t.retry_count === 0 || t.retry_count >= t.max_retries
  ).length;
  const retrying = failedTasks.filter(
    (t) => t.retry_count > 0 && t.retry_count < t.max_retries
  ).length;
  reactExports.useEffect(() => {
    if (failedTasks.length > 0) {
      console.log("[tracer:STUB-FIX-G4] ok failed-subline wired", {
        failed: failedTasks.length,
        needTriage,
        retrying
      });
    }
  }, [failedTasks.length, needTriage, retrying]);
  const [hourlyData, setHourlyData] = reactExports.useState([]);
  const [hourlySummary, setHourlySummary] = reactExports.useState(null);
  reactExports.useEffect(() => {
    const fetchHourly = () => {
      api.costs.hourly().then((data) => {
        setHourlyData(data.hours);
        setHourlySummary(data.summary);
        if (data.hours.some((h) => h.cost_usd > 0 || h.tasks_done > 0)) {
          console.log("[tracer:STUB-FIX-G1] ok sparklines cost/tokens/activity wired", {
            nonZeroSlots: data.hours.filter((h) => h.cost_usd > 0).length,
            totalTasks: data.hours.reduce((s, h) => s + h.tasks_done, 0)
          });
        }
        console.log("[tracer:STUB-FIX-G1] fetch-ok", { slots: data.hours.length });
        if (data.summary.tokens_last_5min > 0) {
          console.log("[tracer:STUB-FIX-G2] ok tokens-headline wired", {
            tokensLast5min: data.summary.tokens_last_5min,
            peakHour: data.summary.peak_hour
          });
        }
      }).catch(() => {
      });
    };
    fetchHourly();
    const id = setInterval(fetchHourly, 6e4);
    return () => clearInterval(id);
  }, []);
  const spark = hourlyData.length > 0 ? hourlyData.map((h) => h.cost_usd) : void 0;
  const tokensSpark = hourlyData.length > 0 ? hourlyData.map((h) => h.input_tokens + h.output_tokens) : void 0;
  const hours = hourlyData.length > 0 ? hourlyData.map((h) => h.tasks_done) : [];
  const hoursMax = Math.max(...hours, 1);
  const tracedF1 = reactExports.useRef(false);
  const tracedF2 = reactExports.useRef(false);
  if (!tracedF1.current && hourlyData.length === 0) {
    tracedF1.current = true;
    console.log("[tracer:INSP-F1] tokens-spark hidden: no hourlyData");
  }
  if (!tracedF2.current && hourlyData.length === 0) {
    tracedF2.current = true;
    console.log("[tracer:INSP-F2] cost-spark hidden: no hourlyData");
  }
  const blocked = tasks.filter((t) => t.status === "blocked").length;
  const conflict = tasks.filter((t) => t.status === "conflict").length;
  const activeMissionCount = runningT + assigned + queued + blocked + conflict;
  const tracedF7 = reactExports.useRef(false);
  if (!tracedF7.current) {
    tracedF7.current = true;
    console.log("[tracer:INSP-F7] fleet-active include-idle=", running + idle, "total=", workers.length);
  }
  const tracedF4 = reactExports.useRef(false);
  if (!tracedF4.current) {
    tracedF4.current = true;
    console.log("[tracer:INSP-F4] header-missions active=", activeMissionCount, "total=", tasks.length);
  }
  const [lastDataRefresh, setLastDataRefresh] = reactExports.useState(() => /* @__PURE__ */ new Date());
  const [tick, setTick] = reactExports.useState(0);
  const workersSignature = workers.map((w) => w.id).join(",");
  const tasksSignature = tasks.map((t) => t.id).join(",");
  reactExports.useEffect(() => {
    setLastDataRefresh(/* @__PURE__ */ new Date());
  }, [workersSignature, tasksSignature]);
  reactExports.useEffect(() => {
    const id = setInterval(() => setTick((t) => t + 1), 5e3);
    return () => clearInterval(id);
  }, []);
  function formatRelativeTime(ms) {
    const s = ms / 1e3;
    if (s < 5) return "just now";
    if (s < 60) return `${Math.floor(s)}s ago`;
    const m = s / 60;
    if (m < 60) return `${Math.floor(m)}m ago`;
    return `${Math.floor(m / 60)}h ago`;
  }
  const relativeUpdated = formatRelativeTime(Date.now() - lastDataRefresh.getTime());
  if (tick >= 0) {
    console.log("[tracer:INSP-C5] header-updated relative=", relativeUpdated);
  }
  const activeMissions = tasks.filter((t) => ["running", "assigned"].includes(t.status)).slice(0, 8);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-summary", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: workers.length }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "workers" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-sep" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item info", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-dot" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: activeMissionCount }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "missions" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-sep" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-item", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-lbl", children: [
          "updated ",
          relativeUpdated
        ] }) })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.refresh, { size: 13 }), children: "Refresh" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.plus, { size: 13 }), onClick: onOpenKanban, children: "New mission" })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-stats", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Stat,
        {
          label: "Workers",
          value: workers.length,
          sub: (() => {
            const statusMap = /* @__PURE__ */ new Map();
            for (const w of workers) {
              statusMap.set(w.status, (statusMap.get(w.status) ?? 0) + 1);
            }
            const PRIORITY = [
              { key: "running", label: "running" },
              { key: "idle", label: "idle" }
            ];
            const degradedCount = workers.filter((w) => isWorkerAttention(w.status)).length;
            const priorityKeys = new Set(PRIORITY.map((p) => p.key));
            const otherStatuses = [...statusMap.keys()].filter((s) => !priorityKeys.has(s) && !isWorkerAttention(s)).sort();
            const parts = [
              `${running} running`,
              `${idle} idle`,
              `${degradedCount} degraded`
            ];
            for (const s of otherStatuses) {
              const n = statusMap.get(s) ?? 0;
              if (n > 0) parts.push(`${n} ${s}`);
            }
            const tracedC1Ref = { current: false };
            if (!tracedC1Ref.current && otherStatuses.length > 0) {
              tracedC1Ref.current = true;
              const payload = [...statusMap.entries()].map(([status, n]) => ({ status, n }));
              console.log("[tracer:INSP-C1] workers-sub statuses=", JSON.stringify(payload));
            }
            return parts.join(" · ");
          })(),
          ratio: [
            { label: "running", value: running, tone: "success" },
            { label: "idle", value: idle, tone: "neutral" },
            { label: "degraded", value: degraded, tone: "warn" },
            { label: "other", value: Math.max(0, workers.length - running - idle - degraded), tone: "muted" }
          ]
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Stat,
        {
          label: "Missions",
          value: activeMissionCount,
          sub: (() => {
            console.log("[tracer:INSP-F6] missions value=active=", activeMissionCount, "done=", done24);
            return `${runningT} running · ${queued} queued · ${assigned} assigned${done24 > 0 ? ` · ${done24} done` : ""}`;
          })(),
          ratio: [
            { label: "running", value: runningT, tone: "success" },
            { label: "assigned", value: assigned, tone: "info" },
            { label: "queued", value: queued, tone: "neutral" },
            { label: "done", value: done24, tone: "muted" }
          ]
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Stat,
        {
          label: "Failed 24h",
          tone: failed > 0 ? "warn" : void 0,
          value: failed,
          sub: `${needTriage} need triage · ${retrying} retrying`,
          icon: failed > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.alert, { size: 24 }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.check, { size: 24 })
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Stat,
        {
          label: "Tokens/5 min",
          value: hourlySummary ? formatK(hourlySummary.tokens_last_5min) : "…",
          sub: hourlySummary && hourlySummary.peak_tokens_hour > 0 && hourlySummary.peak_hour !== null ? `peak ${formatK(hourlySummary.peak_tokens_hour)} tokens/hr · ${String(hourlySummary.peak_hour).padStart(2, "0")}:00 UTC` : "—",
          spark: tokensSpark
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Stat,
        {
          label: "Cost today",
          tone: "accent",
          value: `$${totalCost.toFixed(2)}`,
          sub: `${Math.min(100, Math.round(capPct * 100))}% of $${budgetLimitUsd.toFixed(0)} daily cap`,
          progress: capPct,
          spark
        }
      )
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-dash-grid", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-dash-col", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(AttentionRail, { items: attention2 }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Active missions" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-panel-sub", children: [
              runningT,
              " running · ",
              assigned,
              " just assigned · ",
              queued,
              " queued behind"
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-actions", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: "hv-panel-link", onClick: onOpenKanban, children: [
              "Open Missions",
              /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.arrowR, { size: 11 })
            ] }) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            activeMissions.map((t) => /* @__PURE__ */ jsxRuntimeExports.jsx(ActiveMissionRow, { t, workers }, t.id)),
            activeMissions.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-empty", children: "No missions running." })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "24h activity" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-panel-sub", children: "missions completed per hour, UTC" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-actions", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono hv-panel-sub", children: hours.length > 0 ? `${hours.reduce((s, h) => s + h, 0)} total` : "—" }) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-body", children: hours.length > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(ActivityBars, { hours, max: hoursMax }) : (() => {
            console.log("[tracer:INSP-C2] activity-bars hidden: no hourlyData");
            return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-empty", children: "No activity data yet." });
          })() })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-dash-col", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Fleet" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-panel-sub", title: "Active = workers that are running or idle (alive process). Excludes degraded/starting.", children: [
              running + idle,
              "/",
              workers.length,
              " active"
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-actions", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: "hv-panel-link", onClick: onOpenFleet, children: [
              "Open Fleet",
              /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.arrowR, { size: 11 })
            ] }) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-body", children: /* @__PURE__ */ jsxRuntimeExports.jsx(FleetMini, { workers, onPick: onPickWorker }) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(EventFeedLive, { sseConnected })
      ] })
    ] })
  ] });
}
const VALID_TASK_STATUSES_SET = /* @__PURE__ */ new Set([
  "queued",
  "assigned",
  "running",
  "done",
  "failed",
  "cancelled"
]);
const READONLY_STATUSES = /* @__PURE__ */ new Set(["conflict", "blocked"]);
const ALL_COLS = [
  { id: "queued", label: "Queued", color: "var(--hv-fg-subtle)" },
  { id: "assigned", label: "Assigned", color: "var(--hv-info)" },
  { id: "running", label: "Running", color: "var(--hv-honey-5)" },
  { id: "done", label: "Done", color: "var(--hv-success)" },
  { id: "failed", label: "Failed", color: "var(--hv-danger)" },
  { id: "cancelled", label: "Cancelled", color: "var(--hv-fg-subtle)" }
];
const KB_COLS = ALL_COLS.filter((c) => VALID_TASK_STATUSES_SET.has(c.id));
const READONLY_COLS = [
  { id: "conflict", label: "Conflict", color: "var(--hv-danger)" },
  { id: "blocked", label: "Blocked", color: "var(--hv-warn, #f59e0b)" }
];
let _cap32TracerFired = false;
let _cap38TracerFired = false;
function RoutingBadge({ targetRole, targetEngine, routingReason }) {
  if (!_cap32TracerFired) {
    _cap32TracerFired = true;
    tracer("CAP-32", "ok");
  }
  if (targetEngine !== null && !_cap38TracerFired) {
    _cap38TracerFired = true;
    tracer("CAP-38", "ok");
  }
  const label = targetEngine ? `Needs ${targetRole} (${targetEngine})` : `Needs ${targetRole} worker`;
  const title = routingReason ?? `Waiting for a ${targetRole} worker`;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      "data-routing-role": targetRole,
      "data-routing-engine": targetEngine ?? void 0,
      title,
      style: {
        marginTop: 4,
        display: "inline-flex",
        alignItems: "center",
        gap: 4,
        fontSize: 10,
        fontWeight: 600,
        letterSpacing: "0.03em",
        color: "var(--hv-info, #3b82f6)",
        background: "var(--hv-info-bg, rgba(59,130,246,0.12))",
        border: "1px solid var(--hv-info, #3b82f6)",
        borderRadius: 3,
        padding: "1px 5px"
      },
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.link, { size: 9 }),
        label
      ]
    }
  );
}
function TaskCard({ t, onOpen, onDragStart, onDragEnd, isDragging, readOnly, onArchive }) {
  const isArchived = Boolean(t.archived);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: `hv-tcard${isDragging ? " drag" : ""}${readOnly ? " readonly" : ""}`,
      draggable: !readOnly,
      "data-task-id": t.id,
      "data-task-type": t.rawType ?? t.type,
      style: isArchived ? { opacity: 0.5 } : void 0,
      onDragStart: readOnly ? void 0 : (e) => {
        e.dataTransfer.effectAllowed = "move";
        onDragStart(t);
      },
      onDragEnd: readOnly ? void 0 : onDragEnd,
      onClick: () => onOpen(t),
      onKeyDown: (e) => {
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          onOpen(t);
        }
      },
      role: "button",
      tabIndex: 0,
      "aria-label": `Task ${t.id}: ${t.title}`,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-tcard-title", children: [
          t.title,
          isArchived && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "span",
            {
              "data-testid": "kanban-archived-badge",
              style: {
                marginLeft: 8,
                fontSize: 10,
                padding: "1px 5px",
                borderRadius: 3,
                border: "1px solid var(--hv-fg-subtle)",
                color: "var(--hv-fg-subtle)",
                letterSpacing: "0.04em",
                fontWeight: 600
              },
              children: "ARCHIVED"
            }
          ),
          (() => {
            const count2 = t.payload && typeof t.payload.snapshot_count === "number" ? Number(t.payload.snapshot_count) : null;
            if (count2 == null || count2 <= 0) return null;
            return /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "span",
              {
                "data-testid": "kanban-annotation-chip",
                style: {
                  marginLeft: 8,
                  fontSize: 11,
                  padding: "1px 6px",
                  borderRadius: 10,
                  border: "1px solid var(--hv-border)",
                  color: "var(--hv-fg-muted)"
                },
                children: [
                  "📌 ",
                  count2
                ]
              }
            );
          })()
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-tcard-meta", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: t.id }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sep", children: "·" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: t.type }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sep", children: "·" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: t.priority }),
          t.deps.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sep", children: "·" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { display: "inline-flex", alignItems: "center", gap: 3 }, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.link, { size: 10 }),
              t.deps[0]
            ] })
          ] })
        ] }),
        t.conflictFiles && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-tcard-conflict", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.alert, { size: 10 }),
            " ",
            t.conflictFiles.length,
            " conflicting files"
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "muted", children: t.conflictFiles[0] })
        ] }),
        (t.status === "queued" || t.status === "blocked") && t.targetRole && /* @__PURE__ */ jsxRuntimeExports.jsx(
          RoutingBadge,
          {
            targetRole: t.targetRole,
            targetEngine: t.targetEngine,
            routingReason: t.routingReason
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-tcard-foot", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-tcard-meta", style: { gap: 6 }, children: t.workerName ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { display: "inline-flex", alignItems: "center", gap: 4 }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(HexMark, { size: 14, name: t.workerName }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: t.workerName })
          ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "subtle", children: "unassigned" }) }),
          t.leaseRemainingSec > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-tcard-ct", "data-testid": "task-lease-mins", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.timer, { size: 10 }),
            Math.floor(t.leaseRemainingSec / 60),
            "m"
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              "data-testid": isArchived ? "kanban-unarchive-btn" : "kanban-archive-btn",
              title: isArchived ? "Unarchive mission" : "Archive mission",
              onClick: (e) => {
                e.stopPropagation();
                onArchive(t);
              },
              style: {
                marginLeft: "auto",
                background: "none",
                border: "none",
                cursor: "pointer",
                padding: "2px 4px",
                color: "var(--hv-fg-subtle)",
                fontSize: 11,
                lineHeight: 1,
                opacity: 0.7
              },
              "aria-label": isArchived ? `Unarchive ${t.id}` : `Archive ${t.id}`,
              children: isArchived ? "↑" : "↓"
            }
          )
        ] })
      ]
    }
  );
}
function Kanban({ tasks, onMoveTask, onOpen, onNewMission, onArchive, showArchived, onToggleArchived }) {
  const loading = useTaskStore((s) => s.loading);
  const [drag, setDrag] = reactExports.useState(null);
  const [dropTarget, setDropTarget] = reactExports.useState(null);
  reactExports.useEffect(() => {
    if (!loading) tracer("CAP-20", "ok");
  }, [loading]);
  const pendingTasks = reactExports.useRef(/* @__PURE__ */ new Set());
  const tracerFired = reactExports.useRef(false);
  const handleDrop = async (colId) => {
    if (!drag || drag.status === colId) {
      setDropTarget(null);
      setDrag(null);
      return;
    }
    const taskId = drag.id;
    setDropTarget(null);
    setDrag(null);
    if (pendingTasks.current.has(taskId)) return;
    pendingTasks.current.add(taskId);
    try {
      await onMoveTask(taskId, colId);
      tracer("CAP-06", "ok", taskId);
    } catch (err) {
      useToastStore.getState().push({
        type: "error",
        title: "Move failed",
        message: err instanceof Error ? err.message : "Unknown error"
      });
      tracer("CAP-06b", "ok", taskId);
    } finally {
      pendingTasks.current.delete(taskId);
    }
  };
  const by = Object.fromEntries([
    ...KB_COLS.map((c) => [c.id, []]),
    ...READONLY_COLS.map((c) => [c.id, []])
  ]);
  tasks.forEach((t) => {
    if (!showArchived && t.archived) return;
    if (VALID_TASK_STATUSES_SET.has(t.status)) {
      by[t.status].push(t);
    } else if (READONLY_STATUSES.has(t.status)) {
      by[t.status].push(t);
      if (!tracerFired.current) {
        tracerFired.current = true;
        tracer("CAP-07", "ok", t.id);
      }
    }
  });
  const blockedCount = READONLY_COLS.reduce((n, c) => n + by[c.id].length, 0);
  if (loading) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-view nopad", style: { display: "flex", flexDirection: "column" }, children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { "data-testid": "skeleton-kanban", style: { display: "flex", gap: 12, padding: 16, flex: 1 }, children: Array.from({ length: SKELETON_COUNT_DEFAULT }, (_, i) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { flex: 1, display: "flex", flexDirection: "column", gap: 8 }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonCard, {}),
      /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonCard, {})
    ] }, i)) }) });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view nopad", style: { display: "flex", flexDirection: "column" }, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-summary", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: tasks.length }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "missions" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-sep" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-item", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "drag between columns to change state" }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-sep" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item warn", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-dot" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "conflicts shown inline" })
        ] })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.filter, { size: 13 }), children: "Filter" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "label",
          {
            "data-testid": "kanban-show-archived-toggle",
            style: {
              display: "inline-flex",
              alignItems: "center",
              gap: 5,
              fontSize: 12,
              color: showArchived ? "var(--hv-fg)" : "var(--hv-fg-subtle)",
              cursor: "pointer",
              userSelect: "none"
            },
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "input",
                {
                  type: "checkbox",
                  checked: showArchived,
                  onChange: onToggleArchived,
                  style: { cursor: "pointer" }
                }
              ),
              "Show archived"
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", variant: "primary", leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.plus, { size: 13 }), onClick: onNewMission, children: "New mission" })
      ] })
    ] }),
    blockedCount > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        "data-testid": "kanban-blocked-banner",
        style: {
          display: "flex",
          alignItems: "center",
          gap: 8,
          padding: "6px 16px",
          background: "var(--hv-danger-bg, rgba(239,68,68,0.1))",
          borderBottom: "1px solid var(--hv-danger, #ef4444)",
          fontSize: 12,
          color: "var(--hv-danger, #ef4444)"
        },
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.alert, { size: 13 }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: blockedCount }),
            " task",
            blockedCount !== 1 ? "s" : "",
            " in conflict or blocked state — read-only columns below"
          ] })
        ]
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-kb", role: "list", "aria-label": "Mission kanban board", children: [
      KB_COLS.map((col) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-kb-col", role: "listitem", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-kb-col-head", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-kb-col-dot", style: { background: col.color }, "aria-hidden": "true" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-kb-col-name", children: col.label }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-kb-col-count", children: by[col.id].length }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-kb-col-actions", children: /* @__PURE__ */ jsxRuntimeExports.jsx(IconBtn, { icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.more, { size: 13 }), label: "More" }) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: `hv-kb-col-body${dropTarget === col.id ? " drop" : ""}`,
            "data-testid": `kanban-col-${col.id}`,
            onDragOver: (e) => {
              e.preventDefault();
              setDropTarget(col.id);
            },
            onDragLeave: () => setDropTarget(null),
            onDrop: () => handleDrop(col.id),
            "aria-label": `${col.label} column`,
            children: [
              by[col.id].map((t) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                TaskCard,
                {
                  t,
                  onOpen,
                  onDragStart: setDrag,
                  onDragEnd: () => setDrag(null),
                  isDragging: (drag == null ? void 0 : drag.id) === t.id,
                  onArchive: (t2) => {
                    void onArchive(t2.id, Boolean(t2.archived));
                  }
                },
                t.id
              )),
              by[col.id].length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: "20px 10px", fontSize: 11.5, color: "var(--hv-fg-subtle)", textAlign: "center" }, children: "—" })
            ]
          }
        )
      ] }, col.id)),
      READONLY_COLS.map((col) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-kb-col", role: "listitem", style: { opacity: 0.85 }, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-kb-col-head", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-kb-col-dot", style: { background: col.color }, "aria-hidden": "true" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-kb-col-name", children: col.label }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-kb-col-count", children: by[col.id].length }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "span",
            {
              title: "Read-only — these statuses cannot be set via drag-drop",
              style: { marginLeft: "auto", fontSize: 10, color: "var(--hv-fg-subtle)", letterSpacing: "0.04em" },
              "aria-label": "read-only column",
              children: "read-only"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: "hv-kb-col-body",
            "data-testid": `kanban-col-${col.id}`,
            style: { background: "var(--hv-surface-subtle, rgba(0,0,0,0.04))" },
            "aria-label": `${col.label} column (read-only)`,
            children: [
              by[col.id].map((t) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                TaskCard,
                {
                  t,
                  onOpen,
                  onDragStart: setDrag,
                  onDragEnd: () => setDrag(null),
                  isDragging: false,
                  readOnly: true,
                  onArchive: (t2) => {
                    void onArchive(t2.id, Boolean(t2.archived));
                  }
                },
                t.id
              )),
              by[col.id].length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: "20px 10px", fontSize: 11.5, color: "var(--hv-fg-subtle)", textAlign: "center" }, children: "—" })
            ]
          }
        )
      ] }, col.id))
    ] })
  ] });
}
const STAGE_GLYPH = {
  setup: "⌬",
  // ⌬
  plan: "◇",
  // ◇
  work: "·",
  // ·
  gate: "◷",
  // ◷
  teardown: "⌬"
  // ⌬
};
const STATUS_TONE = {
  done: "success",
  running: "brand",
  failed: "danger",
  warn: "warn",
  pending: "neutral",
  skipped: "neutral",
  "awaiting-approval": "warn",
  unknown: "neutral"
};
function fmtDur$1(ms) {
  if (ms == null) return "—";
  if (ms < 1e3) return `${ms}ms`;
  if (ms < 6e4) return `${(ms / 1e3).toFixed(1)}s`;
  const m = Math.floor(ms / 6e4);
  const s = Math.floor(ms % 6e4 / 1e3);
  return `${m}m ${String(s).padStart(2, "0")}s`;
}
function stageLabel(name2) {
  if (!name2) return "Stage pending";
  const labels = {
    triage: "Triage",
    plan_tracers: "Plan + tracers",
    review_plan: "Review plan",
    implement: "Implement",
    review_impl: "Review implementation",
    commit: "Commit"
  };
  if (labels[name2]) return labels[name2];
  return name2.replace(/[_-]+/g, " ").replace(/\b\w/g, (c) => c.toUpperCase());
}
function MiniPipelineSpark({
  pipeline,
  onClick,
  max: max2 = 16,
  className,
  style
}) {
  var _a;
  if (!pipeline || !((_a = pipeline.stages) == null ? void 0 : _a.length)) return null;
  const stages = pipeline.stages.slice(0, max2);
  const overflow = pipeline.stages.length - max2;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "button",
    {
      type: "button",
      className: `sp-mini-spark ${className ?? ""}`,
      style,
      onClick,
      title: `${pipeline.title ?? pipeline.task_id} · ${pipeline.stages.length} stages`,
      "data-testid": "mini-pipeline-spark",
      "data-task-id": pipeline.task_id,
      children: [
        stages.map((s) => {
          const tone = STATUS_TONE[s.status] ?? "neutral";
          return /* @__PURE__ */ jsxRuntimeExports.jsx(
            "span",
            {
              className: `sp-mini-dot sp-${tone}${s.status === "running" ? " run" : ""}`,
              "data-stage-status": s.status
            },
            s.id
          );
        }),
        overflow > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-mini-trunc mono", children: [
          "+",
          overflow
        ] })
      ]
    }
  );
}
function useFleetPipelines(workers) {
  const [pipelines, setPipelines] = reactExports.useState({});
  const requestedRef = reactExports.useRef(/* @__PURE__ */ new Set());
  reactExports.useEffect(() => {
    for (const w of workers) {
      const tid = w.currentTask;
      if (!tid) continue;
      if (requestedRef.current.has(tid)) continue;
      requestedRef.current.add(tid);
      api.tasks.pipeline(tid).then((p) => {
        setPipelines((prev) => ({ ...prev, [tid]: p }));
      }).catch(() => {
      });
    }
  }, [workers]);
  reactExports.useEffect(() => {
    const unsub = eventBus.subscribe("worker.step", (data) => {
      const wid = data.worker_id;
      if (!wid) return;
      const w = workers.find((x) => x.id === wid);
      const tid = w == null ? void 0 : w.currentTask;
      if (!tid) return;
      api.tasks.pipeline(tid).then((p) => {
        setPipelines((prev) => ({ ...prev, [tid]: p }));
      }).catch(() => {
      });
    });
    return () => {
      unsub();
    };
  }, [workers]);
  return pipelines;
}
const CANVAS_W = 800;
const CANVAS_H = 420;
const HEX_SIZE = 30;
const SQRT3 = Math.sqrt(3);
const HEX_COL_STEP = HEX_SIZE * SQRT3;
const HEX_ROW_STEP = HEX_SIZE * 1.5;
function idxToAxial(idx) {
  if (idx <= 0) return { q: 0, r: 0 };
  let ring = 1;
  let cum = 1;
  while (cum + 6 * ring <= idx) {
    cum += 6 * ring;
    ring += 1;
  }
  const within = idx - cum;
  const dirs = [
    [1, -1],
    [1, 0],
    [0, 1],
    [-1, 1],
    [-1, 0],
    [0, -1]
  ];
  let q = 0;
  let r = -ring;
  let pos = 0;
  for (let s = 0; s < 6; s++) {
    for (let i = 0; i < ring; i++) {
      if (pos === within) return { q, r };
      q += dirs[s][0];
      r += dirs[s][1];
      pos += 1;
    }
  }
  return { q, r };
}
function axialToPx(q, r) {
  return {
    x: HEX_COL_STEP * (q + r / 2),
    y: HEX_ROW_STEP * r
  };
}
function hexPts(cx, cy, radius) {
  const pts = [];
  for (let i = 0; i < 6; i++) {
    const a = Math.PI / 3 * i + Math.PI / 6;
    pts.push(`${(cx + radius * Math.cos(a)).toFixed(2)},${(cy + radius * Math.sin(a)).toFixed(2)}`);
  }
  return pts.join(" ");
}
function FleetHive({ workers, onPick, selected }) {
  const tipRef = reactExports.useRef(null);
  const wrapRef = reactExports.useRef(null);
  const [hoverAgent, setHoverAgent] = reactExports.useState(null);
  reactExports.useEffect(() => {
    const wrap2 = wrapRef.current;
    if (!wrap2) return;
    const handler = (e) => {
      const tip = tipRef.current;
      if (!tip || !hoverAgent) return;
      const rect = wrap2.getBoundingClientRect();
      let tx = e.clientX - rect.left + 12;
      let ty = e.clientY - rect.top + 12;
      const tw = tip.offsetWidth || 220;
      const th = tip.offsetHeight || 70;
      if (tx + tw > rect.width - 8) tx = e.clientX - rect.left - tw - 12;
      if (ty + th > rect.height - 8) ty = e.clientY - rect.top - th - 12;
      tip.style.transform = `translate(${tx}px,${ty}px)`;
    };
    wrap2.addEventListener("mousemove", handler);
    return () => wrap2.removeEventListener("mousemove", handler);
  }, [hoverAgent]);
  const layout = reactExports.useMemo(() => {
    const groups = /* @__PURE__ */ new Map();
    let solo = 0;
    for (const w of workers) {
      const key = w.currentTask ? `t:${w.currentTask}` : `s:${solo++}`;
      const arr = groups.get(key) ?? [];
      arr.push(w);
      groups.set(key, arr);
    }
    const clusters = Array.from(groups.values());
    const n = clusters.length;
    const cols = n <= 1 ? 1 : n <= 2 ? 2 : n <= 4 ? 2 : n <= 6 ? 3 : n <= 9 ? 3 : 4;
    const rows = Math.max(1, Math.ceil(n / cols));
    const insetX = 70;
    const insetY = 70;
    const cellW = (CANVAS_W - insetX * 2) / Math.max(1, cols);
    const cellH = (CANVAS_H - insetY * 2) / Math.max(1, rows);
    const positions = [];
    const flatWorkers = [];
    clusters.forEach((cluster, ci) => {
      const col = ci % cols;
      const row = Math.floor(ci / cols);
      const ox = insetX + cellW * (col + 0.5);
      const oy = insetY + cellH * (row + 0.5);
      cluster.forEach((w, wi) => {
        const { q, r } = idxToAxial(wi);
        const p = axialToPx(q, r);
        positions.push({ x: ox + p.x, y: oy + p.y });
        flatWorkers.push(w);
      });
    });
    return { positions, flatWorkers };
  }, [workers]);
  const svgHeight = CANVAS_H;
  const svgWidth = CANVAS_W;
  const liveCount = reactExports.useMemo(() => workers.filter((w) => w.status === "running").length, [workers]);
  const warnCount = reactExports.useMemo(
    () => workers.filter((w) => isWorkerAttention(w.status)).length,
    [workers]
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { ref: wrapRef, className: "hv-canvas", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-jobstrip", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-job", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "span",
        {
          className: `hv-job-dot status-${warnCount > 0 ? "warn" : liveCount > 0 ? "live" : "idle"}`
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-job-body", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-job-name", children: "fleet" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-job-prog", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          "span",
          {
            style: {
              width: `${Math.max(6, liveCount / Math.max(1, workers.length) * 100)}%`
            }
          }
        ) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-job-count", children: [
        liveCount,
        "/",
        workers.length
      ] })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "svg",
      {
        viewBox: `0 0 ${svgWidth} ${svgHeight}`,
        preserveAspectRatio: "xMidYMid meet",
        className: "hv-svg",
        role: "img",
        "aria-label": "Fleet hex visualization",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("defs", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "pattern",
              {
                id: "hv-hexpattern",
                x: "0",
                y: "0",
                width: "36",
                height: "31.18",
                patternUnits: "userSpaceOnUse",
                children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "path",
                  {
                    d: "M 9 0 L 27 0 L 36 15.59 L 27 31.18 L 9 31.18 L 0 15.59 Z",
                    fill: "none",
                    stroke: "#E8932C",
                    strokeWidth: "0.5",
                    strokeOpacity: "0.05"
                  }
                )
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("linearGradient", { id: "hv-grad-live", x1: "0", y1: "0", x2: "0", y2: "1", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("stop", { offset: "0%", stopColor: "#E8932C" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("stop", { offset: "100%", stopColor: "#9C5A11" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("linearGradient", { id: "hv-grad-warn", x1: "0", y1: "0", x2: "0", y2: "1", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("stop", { offset: "0%", stopColor: "#4A2E0F" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("stop", { offset: "100%", stopColor: "#2E1D0A" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("linearGradient", { id: "hv-grad-failed", x1: "0", y1: "0", x2: "0", y2: "1", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("stop", { offset: "0%", stopColor: "#4A1818" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("stop", { offset: "100%", stopColor: "#2A0E0E" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("linearGradient", { id: "hv-grad-idle", x1: "0", y1: "0", x2: "0", y2: "1", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("stop", { offset: "0%", stopColor: "#2A2620" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("stop", { offset: "100%", stopColor: "#1C1914" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "rect",
            {
              x: "0",
              y: "0",
              width: svgWidth,
              height: svgHeight,
              fill: "url(#hv-hexpattern)"
            }
          ),
          layout.flatWorkers.map((w, idx) => {
            const p = layout.positions[idx];
            const cx = p.x;
            const cy = p.y;
            const statusKey = w.status === "running" ? "live" : w.status === "warn" ? "warn" : ["error", "killed", "timeout"].includes(w.status) ? "failed" : "idle";
            const fill = `url(#hv-grad-${statusKey})`;
            const strokeColor = statusKey === "live" ? "rgba(255,200,120,0.5)" : statusKey === "warn" ? "rgba(255,177,74,0.4)" : statusKey === "failed" ? "rgba(255,107,107,0.4)" : "rgba(255,255,255,0.08)";
            const dotColor = statusKey === "live" ? "#5BE38A" : statusKey === "warn" ? "#FFB14A" : statusKey === "failed" ? "#FF6B6B" : "rgba(255,255,255,0.2)";
            const isSel = (selected == null ? void 0 : selected.id) === w.id;
            const main = hexPts(0, 0, HEX_SIZE - 2.5);
            const inner = hexPts(0, 0, HEX_SIZE - 5);
            const elapsedS = w.taskStart != null ? Math.floor((Date.now() - w.taskStart) / 1e3) : 0;
            const elapsedLabel = elapsedS <= 0 ? "—" : elapsedS < 60 ? `${elapsedS}s` : elapsedS < 3600 ? `${Math.floor(elapsedS / 60)}m` : `${Math.floor(elapsedS / 3600)}h`;
            return /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "g",
              {
                className: `hv-cell${isSel ? " is-sel" : ""}`,
                transform: `translate(${cx} ${cy})`,
                "data-worker-id": w.id,
                role: "button",
                tabIndex: 0,
                "aria-label": `Worker ${w.name}: ${w.status}`,
                onMouseEnter: () => setHoverAgent(w),
                onMouseLeave: () => setHoverAgent(null),
                onClick: () => onPick(w),
                onKeyDown: (e) => {
                  if (e.key === "Enter" || e.key === " ") {
                    e.preventDefault();
                    onPick(w);
                  }
                },
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "polygon",
                    {
                      points: main,
                      fill: "rgba(0,0,0,0.4)",
                      transform: "translate(0 2)",
                      style: { filter: "blur(2px)", opacity: 0.7 }
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "polygon",
                    {
                      className: "hv-cell-main",
                      points: main,
                      fill,
                      stroke: isSel ? "#FFD08A" : strokeColor,
                      strokeWidth: isSel ? 2 : 1,
                      style: isSel ? { filter: "drop-shadow(0 0 12px rgba(255,177,74,0.65))" } : void 0
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "polygon",
                    {
                      points: inner,
                      fill: "none",
                      stroke: statusKey === "live" ? "rgba(255,255,255,0.22)" : "rgba(255,255,255,0.06)",
                      strokeWidth: "1"
                    }
                  ),
                  elapsedS > 0 && statusKey !== "idle" && /* @__PURE__ */ jsxRuntimeExports.jsx("text", { y: "-12", textAnchor: "middle", className: "hv-cell-age", children: elapsedLabel }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "text",
                    {
                      y: "2",
                      textAnchor: "middle",
                      dominantBaseline: "middle",
                      className: "hv-cell-name",
                      children: w.name
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs(
                    "text",
                    {
                      y: "17",
                      textAnchor: "middle",
                      dominantBaseline: "middle",
                      className: "hv-cell-tok",
                      children: [
                        (w.tokens / 1e3).toFixed(1),
                        "k"
                      ]
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "circle",
                    {
                      cx: "14",
                      cy: "-22",
                      r: "3",
                      fill: dotColor,
                      style: statusKey !== "idle" ? { filter: `drop-shadow(0 0 4px ${dotColor})` } : void 0
                    }
                  ),
                  statusKey === "live" && /* @__PURE__ */ jsxRuntimeExports.jsxs("circle", { cx: "14", cy: "-22", r: "3", fill: "none", stroke: dotColor, strokeWidth: "1", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("animate", { attributeName: "r", values: "3;7", dur: "1.6s", repeatCount: "indefinite" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "animate",
                      {
                        attributeName: "opacity",
                        values: "0.8;0",
                        dur: "1.6s",
                        repeatCount: "indefinite"
                      }
                    )
                  ] })
                ]
              },
              w.id
            );
          })
        ]
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        ref: tipRef,
        className: "hv-tip",
        style: { opacity: hoverAgent ? 1 : 0 },
        "aria-hidden": !hoverAgent,
        children: hoverAgent && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-tip-head", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "span",
              {
                className: `hv-tip-dot hv-tip-${hoverAgent.status === "running" ? "live" : hoverAgent.status}`
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: hoverAgent.name }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-tip-role", children: hoverAgent.role })
          ] }),
          hoverAgent.currentTask && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-tip-task", children: hoverAgent.currentTask }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-tip-meta", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: hoverAgent.status }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "·" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: hoverAgent.engine }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "·" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
              (hoverAgent.tokens / 1e3).toFixed(1),
              "k"
            ] })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx(HiveSummary, { workers })
  ] });
}
function HiveSummary({ workers }) {
  const total = workers.length;
  const live = workers.filter((w) => w.status === "running").length;
  const warn = workers.filter((w) => isWorkerAttention(w.status)).length;
  const tokens = workers.reduce((a, w) => a + (w.tokens || 0), 0);
  const cost = workers.reduce((a, w) => a + (w.costUsd || 0), 0);
  const pct = total > 0 ? Math.round(live / total * 100) : 0;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-summary", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sm-item", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sm-k", children: "Live" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sm-v", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-sm-num", children: live }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-sm-sub", children: [
          "/ ",
          total
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sm-bar", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { width: `${pct}%` } }) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sm-sep" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sm-item", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sm-k", children: "Tokens/min" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sm-v", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-sm-num", children: [
        (tokens / 6e4).toFixed(1),
        "k"
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sm-foot", children: [
        (tokens / 1e3).toFixed(0),
        "k total"
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sm-sep" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sm-item", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sm-k", children: "Spend" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sm-v", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-sm-num", children: [
        "$",
        cost.toFixed(2)
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sm-foot", children: "of $21.00 cap" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sm-sep" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sm-item", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sm-k", children: "Degraded" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-sm-v", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-sm-num", children: warn }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sm-foot", children: [
        live,
        " active"
      ] })
    ] })
  ] });
}
function WorkerCard({ w, selected, onClick }) {
  const elapsedS = w.taskStart != null ? Math.floor((Date.now() - w.taskStart) / 1e3) : 0;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: `hv-wcard status-${w.status}${selected ? " sel" : ""}`,
      onClick: () => onClick(w),
      role: "button",
      tabIndex: 0,
      onKeyDown: (e) => {
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          onClick(w);
        }
      },
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-wcard-head", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(StatusDot, { status: w.status }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-wcard-name", children: w.name }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-wcard-eng", children: w.role })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-wcard-task", children: w.currentTask ? w.currentTask : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "subtle", children: "awaiting assignment" }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-wcard-meta", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono", children: [
            Math.floor(elapsedS / 60),
            "m"
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono", children: [
            (w.tokens / 1e3).toFixed(1),
            "k"
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono", children: [
            "$",
            w.costUsd.toFixed(2)
          ] })
        ] }),
        w.status === "running" && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-wcard-bar", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { width: `${Math.round(w.progress * 100)}%` } }) })
      ]
    }
  );
}
function FleetTable({
  workers,
  onPick,
  selected
}) {
  const pipelines = useFleetPipelines(workers);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("table", { className: "hv-cost-row-table", style: { fontSize: 12.5 }, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("thead", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("th", {}),
      /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Worker" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Role" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Engine" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Task" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Pipeline" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("th", { className: "num", children: "Tokens" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("th", { className: "num", children: "Cost" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "HB" })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("tbody", { children: workers.map((w) => {
      const pipeline = w.currentTask ? pipelines[w.currentTask] : null;
      return /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "tr",
        {
          onClick: () => onPick(w),
          "data-testid": "fleet-row",
          "data-worker-id": w.id,
          style: {
            cursor: "pointer",
            background: (selected == null ? void 0 : selected.id) === w.id ? "var(--hv-surface-hover)" : void 0
          },
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("td", { style: { width: 24, padding: "8px 0 8px 14px" }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(StatusDot, { status: w.status }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("td", { style: { fontWeight: 600 }, children: w.name }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("td", { className: "muted", children: w.role }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("td", { className: "muted", children: w.engine }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "td",
              {
                style: {
                  maxWidth: 220,
                  overflow: "hidden",
                  textOverflow: "ellipsis",
                  whiteSpace: "nowrap"
                },
                children: w.currentTask ?? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "subtle", children: "—" })
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx("td", { onClick: (e) => e.stopPropagation(), children: pipeline && pipeline.stages.length > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(
              MiniPipelineSpark,
              {
                pipeline,
                onClick: () => onPick(w)
              }
            ) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "subtle", style: { fontSize: 11 }, children: "—" }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("td", { className: "num", children: [
              (w.tokens / 1e3).toFixed(1),
              "k"
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("td", { className: "num", children: [
              "$",
              w.costUsd.toFixed(2)
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "td",
              {
                style: {
                  color: w.heartbeatAgo > 20 ? "var(--hv-warn)" : "var(--hv-fg-muted)",
                  fontFamily: "var(--hv-font-mono)",
                  fontSize: 11
                },
                children: [
                  w.heartbeatAgo,
                  "s"
                ]
              }
            )
          ]
        },
        w.id
      );
    }) })
  ] });
}
function FleetBreakdown({ workers }) {
  const byRole = {};
  for (const w of workers) byRole[w.role] = (byRole[w.role] || 0) + 1;
  const roles = Object.entries(byRole).sort((a, b) => b[1] - a[1]);
  const total = workers.length || 1;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "By role" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-panel-sub", children: [
        roles.length,
        " roles"
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-body", children: roles.map(([role, n]) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-br-row", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-br-role", children: role }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-br-bar", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-br-fill", style: { width: `${n / total * 100}%` } }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-br-n mono", children: n })
    ] }, role)) })
  ] });
}
function FleetEngines({ workers }) {
  const byEngine = {};
  for (const w of workers) {
    if (!byEngine[w.engine]) byEngine[w.engine] = { n: 0, tokens: 0, cost: 0 };
    byEngine[w.engine].n += 1;
    byEngine[w.engine].tokens += w.tokens;
    byEngine[w.engine].cost += w.costUsd;
  }
  const engines = Object.entries(byEngine).sort((a, b) => b[1].tokens - a[1].tokens);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "By engine" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-panel-sub", children: [
        engines.length,
        " engines"
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: engines.map(([eng, d]) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-eng-row", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-eng-name", children: eng }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-eng-stats", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono", children: [
          d.n,
          " ",
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "subtle", children: "workers" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono", children: [
          (d.tokens / 1e3).toFixed(1),
          "k ",
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "subtle", children: "tok" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono", children: [
          "$",
          d.cost.toFixed(2)
        ] })
      ] })
    ] }, eng)) })
  ] });
}
function FleetView({ workers, onPickWorker, selected }) {
  const loading = useWorkerStore((s) => s.loading);
  const [filter, setFilter] = reactExports.useState("all");
  const [query, setQuery] = reactExports.useState("");
  const [viz, setViz] = reactExports.useState("hex");
  const running = workers.filter((w) => w.status === "running").length;
  const idle = workers.filter((w) => w.status === "idle").length;
  const degraded = workers.filter((w) => isWorkerAttention(w.status)).length;
  const filtered = workers.filter((w) => {
    if (filter === "running" && w.status !== "running") return false;
    if (filter === "idle" && w.status !== "idle") return false;
    if (filter === "degraded" && !isWorkerAttention(w.status)) return false;
    if (query && !`${w.name} ${w.role} ${w.engine} ${w.currentTask ?? ""}`.toLowerCase().includes(query.toLowerCase()))
      return false;
    return true;
  });
  reactExports.useEffect(() => {
    if (workers.length >= 3) {
      tracer("CAP-24", "ok");
    }
  }, [workers.length]);
  if (loading) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-view", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { "data-testid": "skeleton-fleet", style: { display: "flex", flexWrap: "wrap", gap: 12, padding: 24 }, children: Array.from({ length: 8 }, (_, i) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { width: 80 }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonCard, {}) }, i)) }) });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-summary", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: workers.length }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "workers" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-sep" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item running", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-dot" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: running }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "running" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item idle", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-dot" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: idle }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "idle" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item degraded", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-dot" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: degraded }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "degraded" })
        ] })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-fleet-search", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.search, { size: 12 }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "input",
            {
              placeholder: "filter workers…",
              value: query,
              onChange: (e) => setQuery(e.target.value),
              "aria-label": "Filter workers"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-seg", children: ["all", "running", "idle", "degraded"].map((k) => /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            className: `hv-seg-btn${filter === k ? " on" : ""}`,
            onClick: () => setFilter(k),
            children: k
          },
          k
        )) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-seg", style: { marginLeft: 6 }, children: ["hex", "grid", "table"].map((k) => /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            className: `hv-seg-btn${viz === k ? " on" : ""}`,
            onClick: () => setViz(k),
            children: k
          },
          k
        )) })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-fleet-grid", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel hv-fleet-main", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: viz === "hex" ? "Hive" : viz === "grid" ? "Grid" : "Table" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-panel-sub", children: [
            filtered.length,
            " shown"
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: "hv-panel-body",
            style: { padding: viz === "table" ? 0 : 14 },
            children: [
              viz === "hex" && /* @__PURE__ */ jsxRuntimeExports.jsx(FleetHive, { workers: filtered, onPick: onPickWorker, selected }),
              viz === "grid" && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-wg", children: filtered.map((w) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                WorkerCard,
                {
                  w,
                  selected: (selected == null ? void 0 : selected.id) === w.id,
                  onClick: onPickWorker
                },
                w.id
              )) }),
              viz === "table" && /* @__PURE__ */ jsxRuntimeExports.jsx(FleetTable, { workers: filtered, onPick: onPickWorker, selected })
            ]
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-fleet-side", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(FleetBreakdown, { workers }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(FleetEngines, { workers })
      ] })
    ] })
  ] });
}
const MIN_LOOP = 3;
function collapseStages(stages) {
  const out = [];
  let i = 0;
  while (i < stages.length) {
    const cur = stages[i];
    let j = i + 1;
    while (j < stages.length) {
      const next = stages[j];
      const sameLoopId = cur.parentLoopId && next.parentLoopId === cur.parentLoopId;
      const sameNameWork = next.name === cur.name && next.kind === "work";
      if (sameLoopId || sameNameWork) {
        j++;
      } else {
        break;
      }
    }
    const run = stages.slice(i, j);
    if (run.length >= MIN_LOOP) {
      const ticks = run.map((s, idx) => ({
        iter: s.iteration ?? (idx === 0 ? 1 : null),
        status: s.status,
        durationMs: s.durationMs ?? null,
        id: s.id
      }));
      const failedCount = ticks.filter((t) => t.status === "failed").length;
      const warnCount = ticks.filter((t) => t.status === "warn").length;
      const doneCount = ticks.filter((t) => t.status === "done").length;
      const runningStage = run.find((s) => s.status === "running");
      const status = runningStage ? "running" : failedCount > 0 ? "failed" : "done";
      out.push({
        type: "loop",
        id: cur.parentLoopId || `loop-${cur.id}`,
        name: cur.name,
        kind: cur.kind,
        iterations: run,
        ticks,
        doneCount,
        warnCount,
        failedCount,
        running: !!runningStage,
        status
      });
    } else {
      for (const s of run) out.push({ type: "stage", stage: s });
    }
    i = j;
  }
  return out;
}
function statusOf(it) {
  return it.type === "stage" ? it.stage.status : it.status;
}
function MiniPipeline({
  pipeline,
  max: max2 = 6,
  onClick,
  className,
  style
}) {
  var _a;
  if (!pipeline || !((_a = pipeline.stages) == null ? void 0 : _a.length)) return null;
  const items = collapseStages(pipeline.stages);
  let pivotIdx = items.findIndex((it) => statusOf(it) === "running");
  if (pivotIdx < 0) pivotIdx = items.findIndex((it) => statusOf(it) === "awaiting-approval");
  if (pivotIdx < 0) pivotIdx = items.findIndex((it) => statusOf(it) === "failed");
  if (pivotIdx < 0) {
    for (let j = items.length - 1; j >= 0; j--) {
      if (statusOf(items[j]) === "done") {
        pivotIdx = j;
        break;
      }
    }
  }
  if (pivotIdx < 0) pivotIdx = 0;
  let start = Math.max(0, pivotIdx - Math.floor(max2 / 2));
  const end = Math.min(items.length, start + max2);
  if (end - start < max2) start = Math.max(0, end - max2);
  const truncL = start;
  const truncR = items.length - end;
  const totals = {
    total: pipeline.stages.length,
    done: pipeline.stages.filter((s) => s.status === "done").length,
    failed: pipeline.stages.filter((s) => s.status === "failed").length
  };
  const slice = items.slice(start, end);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "button",
    {
      type: "button",
      className: `sp-mini ${className ?? ""}`,
      style,
      onClick,
      title: "Open pipeline",
      "data-testid": "mini-pipeline",
      "data-task-id": pipeline.task_id,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-mini-counts mono", children: [
          totals.done,
          "/",
          totals.total
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-mini-track", children: [
          truncL > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-mini-trunc mono", children: [
            "+",
            truncL
          ] }),
          slice.map((item, i) => {
            const isLoop = item.type === "loop";
            const status = statusOf(item);
            const tone = STATUS_TONE[status] ?? "neutral";
            const name2 = isLoop ? item.name : item.stage.name;
            const isPivot = start + i === pivotIdx;
            const glyph = isLoop ? "⟳" : STAGE_GLYPH[item.stage.kind] ?? "·";
            const key = isLoop ? item.id : item.stage.id;
            return /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { display: "inline-flex", alignItems: "center" }, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "span",
                {
                  className: `sp-mini-node sp-${tone}${isPivot ? " pivot" : ""}${status === "running" ? " run" : ""}`,
                  "data-mini-node-status": status,
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-mini-glyph", children: glyph }),
                    isPivot && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-mini-name", children: [
                      name2,
                      isLoop ? ` ×${item.iterations.length}` : ""
                    ] }),
                    status === "running" && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-pulse" })
                  ]
                }
              ),
              i < slice.length - 1 && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `sp-mini-conn${status === "pending" ? " pending" : ""}` })
            ] }, key);
          }),
          truncR > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-mini-trunc mono", children: [
            "+",
            truncR
          ] })
        ] }),
        totals.failed > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-mini-badge fail mono", children: [
          totals.failed,
          "✗"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-mini-cta", children: [
          "view",
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-mini-cta-arrow", children: "→" })
        ] })
      ]
    }
  );
}
const LOG_RING_BUFFER_SIZE = 500;
const LOG_POLL_INTERVAL_MS = 3e3;
function normalizeLevel(raw) {
  if (!raw) return "info";
  const lower = raw.toLowerCase();
  switch (lower) {
    case "tool":
      return "tool";
    case "warn":
    case "warning":
      return "warn";
    case "error":
    case "critical":
      return "error";
    case "debug":
      return "debug";
    default:
      return "info";
  }
}
function formatTimestamp(ts) {
  if (ts === void 0 || ts === null) return "--:--:--";
  const d = typeof ts === "number" ? new Date(ts * 1e3) : parseUtc(ts);
  if (isNaN(d.getTime())) return "--:--:--";
  return d.toLocaleTimeString("en-US", { hour12: false });
}
function tailLineToLogLine(raw) {
  return {
    t: formatTimestamp(raw.timestamp),
    lvl: normalizeLevel(raw.level),
    msg: raw.message ?? ""
  };
}
function appendToRing(existing, incoming) {
  const merged = [...existing, ...incoming];
  return merged.length > LOG_RING_BUFFER_SIZE ? merged.slice(merged.length - LOG_RING_BUFFER_SIZE) : merged;
}
async function fetchLogTail(workerId) {
  try {
    const body = await api.logs.tail(workerId, LOG_RING_BUFFER_SIZE);
    return (body.lines ?? []).map(tailLineToLogLine);
  } catch {
    return [];
  }
}
function WorkerDetail({ worker, onBack: _onBack, onKill, onRestart, onStop }) {
  const [tab2, setTab] = reactExports.useState("overview");
  const [logs, setLogs] = reactExports.useState([]);
  const [live, setLive] = reactExports.useState(true);
  const [killing, setKilling] = reactExports.useState(false);
  const [stopping, setStopping] = reactExports.useState(false);
  const [restarting, setRestarting] = reactExports.useState(false);
  const [actionError, setActionError] = reactExports.useState(null);
  const logsRef = reactExports.useRef(null);
  const tracerEmitted = reactExports.useRef(false);
  const [workerTasks, setWorkerTasks] = reactExports.useState([]);
  const [tasksLoading, setTasksLoading] = reactExports.useState(false);
  const [tasksError, setTasksError] = reactExports.useState(null);
  const [workerMessages, setWorkerMessages] = reactExports.useState([]);
  const [messagesLoading, setMessagesLoading] = reactExports.useState(false);
  const [messagesError, setMessagesError] = reactExports.useState(null);
  const [placement, setPlacement] = reactExports.useState(null);
  const cap33TasksDone = reactExports.useRef(false);
  const cap33MsgsDone = reactExports.useRef(false);
  const cap33TracerEmitted = reactExports.useRef(false);
  const maybeEmitCap33 = reactExports.useCallback(() => {
    if (!cap33TracerEmitted.current && cap33TasksDone.current && cap33MsgsDone.current) {
      cap33TracerEmitted.current = true;
      tracer("CAP-33", "ok");
    }
  }, []);
  const emitTracerOnce = reactExports.useCallback((_lines) => {
    if (!tracerEmitted.current) {
      tracerEmitted.current = true;
      tracer("UI-C", "ok");
    }
  }, []);
  reactExports.useEffect(() => {
    tracer("CAP-33", "mount");
  }, [worker.id]);
  reactExports.useEffect(() => {
    let cancelled = false;
    cap33TasksDone.current = false;
    setTasksLoading(true);
    setTasksError(null);
    api.workers.tasks(worker.id).then((result) => {
      if (cancelled) return;
      setWorkerTasks(result.tasks);
      setTasksLoading(false);
      cap33TasksDone.current = true;
      maybeEmitCap33();
    }).catch((err) => {
      if (cancelled) return;
      setTasksError(err instanceof Error ? err.message : "Failed to load tasks");
      setTasksLoading(false);
      cap33TasksDone.current = true;
      maybeEmitCap33();
    });
    return () => {
      cancelled = true;
    };
  }, [worker.id, maybeEmitCap33]);
  reactExports.useEffect(() => {
    let cancelled = false;
    cap33MsgsDone.current = false;
    setMessagesLoading(true);
    setMessagesError(null);
    api.workers.messages(worker.id).then((result) => {
      if (cancelled) return;
      setWorkerMessages(result.messages);
      setMessagesLoading(false);
      cap33MsgsDone.current = true;
      maybeEmitCap33();
    }).catch((err) => {
      if (cancelled) return;
      setMessagesError(err instanceof Error ? err.message : "Failed to load messages");
      setMessagesLoading(false);
      cap33MsgsDone.current = true;
      maybeEmitCap33();
    });
    return () => {
      cancelled = true;
    };
  }, [worker.id, maybeEmitCap33]);
  reactExports.useEffect(() => {
    let cancelled = false;
    api.workers.get(worker.id).then((body) => {
      if (cancelled) return;
      if (body && typeof body === "object" && "placement" in body) {
        const p = body.placement;
        setPlacement(p ?? null);
        if (p) {
          tracer("CAP-37", "ok");
        }
      }
    }).catch(() => {
    });
    return () => {
      cancelled = true;
    };
  }, [worker.id]);
  reactExports.useEffect(() => {
    let cancelled = false;
    async function load() {
      const lines = await fetchLogTail(worker.id);
      if (cancelled) return;
      setLogs((prev) => appendToRing(prev, lines.filter((l) => !prev.some((p) => p.t === l.t && p.msg === l.msg))));
      emitTracerOnce(lines);
    }
    void load();
    const timer = setInterval(() => {
      if (!cancelled) void load();
    }, LOG_POLL_INTERVAL_MS);
    return () => {
      cancelled = true;
      clearInterval(timer);
    };
  }, [worker.id, emitTracerOnce]);
  reactExports.useEffect(() => {
    if (!live) return;
    const unsub = eventBus.subscribe("log", (data) => {
      const payload = data;
      if (payload.worker_id !== worker.id) return;
      const line = {
        t: formatTimestamp(payload.timestamp),
        lvl: normalizeLevel(payload.level),
        msg: typeof payload.message === "string" ? payload.message : ""
      };
      setLogs((prev) => appendToRing(prev, [line]));
    });
    return () => {
      unsub();
    };
  }, [worker.id, live]);
  reactExports.useEffect(() => {
    useWorkerStepStore.getState().registerWorker(worker.id);
    void useWorkerStepStore.getState().hydrateSinceSeq(worker.id);
  }, [worker.id]);
  const [taskPipeline, setTaskPipeline] = reactExports.useState(null);
  const storeLastSeq = useWorkerStepStore((s) => {
    var _a;
    return ((_a = s.entries[worker.id]) == null ? void 0 : _a.lastSeq) ?? 0;
  });
  reactExports.useEffect(() => {
    const taskId = worker.currentTask;
    if (!taskId) {
      setTaskPipeline(null);
      return;
    }
    let cancelled = false;
    api.tasks.pipeline(taskId).then((p) => {
      if (!cancelled) setTaskPipeline(p);
    }).catch(() => {
      if (!cancelled) setTaskPipeline(null);
    });
    return () => {
      cancelled = true;
    };
  }, [worker.id, worker.currentTask, storeLastSeq]);
  reactExports.useEffect(() => {
    if (logsRef.current && live) {
      logsRef.current.scrollTop = logsRef.current.scrollHeight;
    }
  }, [logs, live]);
  const tabs = ["overview", "tasks", "logs", "messages", "settings"];
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "hv-view nopad",
      "data-testid": "worker-detail",
      "data-worker-id": worker.id,
      style: { display: "flex", flexDirection: "column" },
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-wd-header", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(HexMark, { size: 44, name: worker.name, glow: worker.status === "running" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { flex: 1, minWidth: 0 }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-wd-title-row", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-wd-name", children: worker.name }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(StatusBadge, { status: worker.status }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-wd-id mono", children: worker.id })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-wd-stats", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "s", children: "Engine" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "v", children: worker.engine })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "s", children: "Role" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "v", children: worker.role })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "s", children: "Tasks done" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "v", children: worker.tasksDone })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "s", children: "Tokens" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "v", children: [
                  (worker.tokens / 1e3).toFixed(1),
                  "k"
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "s", children: "Cost" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "v", children: [
                  "$",
                  worker.costUsd.toFixed(2)
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "s", children: "Heartbeat" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "v", style: { color: worker.heartbeatAgo > 20 ? "var(--hv-warn)" : void 0 }, children: [
                  worker.heartbeatAgo,
                  "s ago"
                ] })
              ] })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-wd-actions", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Btn,
              {
                size: "sm",
                leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.play, { size: 12 }),
                disabled: !onStop || stopping || killing || restarting,
                "data-testid": "worker-stop-btn",
                title: onStop ? "Drain in-flight tasks, then SIGTERM" : "Graceful Stop (handler not wired)",
                onClick: async () => {
                  if (!onStop) return;
                  setActionError(null);
                  setStopping(true);
                  try {
                    await onStop(worker.id);
                  } catch (e) {
                    setActionError(e instanceof Error ? e.message : "Graceful stop failed");
                  } finally {
                    setStopping(false);
                  }
                },
                children: stopping ? "Stopping…" : "Graceful stop"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Btn,
              {
                size: "sm",
                leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.refresh, { size: 12 }),
                disabled: restarting || killing,
                "data-testid": "worker-restart-btn",
                onClick: async () => {
                  setActionError(null);
                  setRestarting(true);
                  try {
                    await onRestart(worker.id);
                  } catch (e) {
                    setActionError(e instanceof Error ? e.message : "Restart failed");
                  } finally {
                    setRestarting(false);
                  }
                },
                children: restarting ? "Restarting…" : "Restart"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Btn,
              {
                size: "sm",
                variant: "ghost-danger",
                leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.stop, { size: 12 }),
                disabled: killing || restarting,
                "data-testid": "worker-kill-btn",
                onClick: async () => {
                  if (!confirm("Kill this worker? Cannot be undone.")) return;
                  setActionError(null);
                  setKilling(true);
                  try {
                    await onKill(worker.id);
                  } catch (e) {
                    setActionError(e instanceof Error ? e.message : "Kill failed");
                    setKilling(false);
                  }
                },
                children: killing ? "Killing…" : "Kill"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(IconBtn, { icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.more, { size: 14 }), label: "More" })
          ] }),
          actionError && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              "data-testid": "worker-action-error",
              style: { padding: "4px 16px", fontSize: 11.5, color: "var(--hv-danger, #e05)" },
              children: actionError
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-tabs", role: "tablist", "aria-label": "Worker detail tabs", children: tabs.map((k) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            className: `hv-tab${tab2 === k ? " active" : ""}`,
            role: "tab",
            "aria-selected": tab2 === k,
            id: `tab-worker-${k}`,
            "aria-controls": `panel-worker-${k}`,
            onClick: () => setTab(k),
            children: [
              k.charAt(0).toUpperCase() + k.slice(1),
              k === "logs" && live && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "dot pulse", style: { background: "var(--hv-success)" } })
            ]
          },
          k
        )) }),
        tab2 === "overview" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-wd-panel", style: { padding: "20px 24px", display: "grid", gridTemplateColumns: "1fr 320px", gap: 20, overflow: "auto" }, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", flexDirection: "column", gap: 16 }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-head", children: /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Current task" }) }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-body", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { fontSize: 14, fontWeight: 500 }, children: worker.currentTask ?? "Idle — awaiting task" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", gap: 12, marginTop: 10, fontSize: 11.5, color: "var(--hv-fg-muted)", fontFamily: "var(--hv-font-mono)" }, children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.tokens, { size: 10 }),
                    " ",
                    (worker.tokens / 1e3).toFixed(1),
                    "k tokens"
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.coin, { size: 10 }),
                    " $",
                    worker.costUsd.toFixed(2)
                  ] })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-wcard-bar", style: { marginTop: 10 }, children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { width: `${Math.round(worker.progress * 100)}%` } }) }),
                taskPipeline && taskPipeline.stages.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { marginTop: 12 }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                  MiniPipeline,
                  {
                    pipeline: taskPipeline,
                    onClick: () => {
                      if (typeof window !== "undefined" && worker.currentTask) {
                        window.location.hash = `#/missions/${worker.currentTask}`;
                      }
                    }
                  }
                ) })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-head", children: /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Tool usage" }) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-body muted", style: { fontSize: 12, fontStyle: "italic" }, children: "Per-worker tool usage aggregation is pending backend work (TB-12)." })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", flexDirection: "column", gap: 16 }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-head", children: /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Identity" }) }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-body", style: { display: "flex", flexDirection: "column", gap: 8, fontSize: 12, fontFamily: "var(--hv-font-mono)" }, children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "muted", children: "session" }),
                  "   tmux://ops/",
                  worker.name.toLowerCase()
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "muted", children: "worktree" }),
                  "   ",
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { wordBreak: "break-all" }, children: worker.worktree })
                ] }),
                placement ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { "data-testid": "worker-placement", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "muted", children: "fleet" }),
                  "  ",
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { wordBreak: "break-all" }, children: [
                    placement.instance_id,
                    placement.host ? ` (${placement.host})` : "",
                    placement.region ? ` · ${placement.region}` : ""
                  ] })
                ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { color: "var(--hv-fg-muted)", fontStyle: "italic" }, children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "muted", children: "fleet" }),
                  "   not assigned"
                ] })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-head", children: /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Tools" }) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-body", style: { display: "flex", flexDirection: "column", gap: 4 }, children: worker.tools.map((t) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: "5px 9px", background: "var(--hv-bg)", border: "1px solid var(--hv-border)", borderRadius: 4, fontFamily: "var(--hv-font-mono)", fontSize: 11.5 }, children: t }, t)) })
            ] })
          ] })
        ] }),
        tab2 === "logs" && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-logs-toolbar", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-live-ind", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "dot" }),
              "live tail"
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "muted mono", style: { fontSize: 11 }, children: [
              logs.length,
              " lines · /api/logs/tail?worker_id=",
              worker.id
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { marginLeft: "auto", display: "flex", gap: 6 }, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-chips", children: ["ALL", "INFO", "TOOL", "WARN", "ERROR"].map((l) => /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "hv-chip", style: { padding: "3px 9px", fontSize: 11 }, children: l }, l)) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.fileAdd, { size: 12 }), children: "Export" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", variant: live ? "default" : "primary", onClick: () => setLive((v) => !v), children: live ? "Pause" : "Resume" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              className: "hv-logs",
              ref: logsRef,
              role: "log",
              "aria-live": "polite",
              "aria-label": "Worker logs",
              id: "panel-worker-logs",
              children: [
                logs.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                  "div",
                  {
                    className: "hv-log-line info",
                    "data-testid": "log-line",
                    style: { color: "var(--hv-fg-muted)", fontStyle: "italic" },
                    children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-log-t", children: "--:--:--" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-log-lvl", children: "INFO" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-log-msg", children: [
                        "No log lines yet for worker ",
                        worker.id
                      ] })
                    ]
                  }
                ),
                logs.map((l, i) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `hv-log-line ${l.lvl}`, "data-testid": "log-line", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-log-t", children: l.t }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-log-lvl", children: l.lvl.toUpperCase() }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-log-msg", children: l.msg })
                ] }, i))
              ]
            }
          )
        ] }),
        tab2 === "messages" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-tl", style: { padding: "24px" }, children: [
          messagesLoading && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              "data-testid": "messages-loading",
              style: { color: "var(--hv-fg-muted)", fontSize: 13, fontStyle: "italic" },
              children: "Loading messages…"
            }
          ),
          !messagesLoading && messagesError && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              "data-testid": "messages-error",
              style: { fontSize: 13, color: "var(--hv-danger, #e05)" },
              children: messagesError
            }
          ),
          !messagesLoading && !messagesError && workerMessages.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              "data-testid": "messages-empty",
              style: { color: "var(--hv-fg-muted)", fontSize: 13, fontStyle: "italic" },
              children: "No messages in this worker's DM channel yet."
            }
          ),
          !messagesLoading && !messagesError && workerMessages.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { "data-testid": "messages-list", children: workerMessages.map((m) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              className: "hv-feed-row",
              "data-testid": "message-row",
              "data-message-id": m.id,
              style: { gridTemplateColumns: "1fr auto", padding: "10px 0", borderBottom: "1px solid var(--hv-border)" },
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { fontSize: 12.5 }, children: m.content }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs(
                    "div",
                    {
                      style: { fontSize: 10.5, color: "var(--hv-fg-subtle)", fontFamily: "var(--hv-font-mono)", marginTop: 2 },
                      children: [
                        m.sender_type,
                        m.created_at ? ` · ${formatLocalDateTime(m.created_at)}` : ""
                      ]
                    }
                  )
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "div",
                  {
                    style: { fontSize: 10.5, color: "var(--hv-fg-subtle)", fontFamily: "var(--hv-font-mono)", alignSelf: "center" },
                    children: m.message_class
                  }
                )
              ]
            },
            m.id
          )) })
        ] }),
        tab2 === "tasks" && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: 20, overflow: "auto" }, children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
          tasksLoading && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              className: "hv-panel-body muted",
              "data-testid": "tasks-loading",
              style: { padding: "16px 14px", fontSize: 12.5, fontStyle: "italic" },
              children: "Loading task history…"
            }
          ),
          !tasksLoading && tasksError && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              className: "hv-panel-body",
              "data-testid": "tasks-error",
              style: { padding: "16px 14px", fontSize: 12.5, color: "var(--hv-danger, #e05)" },
              children: tasksError
            }
          ),
          !tasksLoading && !tasksError && workerTasks.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              className: "hv-panel-body muted",
              "data-testid": "tasks-empty",
              style: { padding: "16px 14px", fontSize: 12.5, fontStyle: "italic" },
              children: "No tasks recorded for this worker yet."
            }
          ),
          !tasksLoading && !tasksError && workerTasks.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { "data-testid": "tasks-list", children: workerTasks.map((t) => {
            const payload = t.payload;
            const title = typeof (payload == null ? void 0 : payload.title) === "string" ? payload.title : t.type;
            return /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "div",
              {
                className: "hv-feed-row",
                "data-testid": "task-row",
                "data-task-id": t.id,
                style: { gridTemplateColumns: "1fr auto auto", padding: "10px 14px" },
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { fontSize: 12.5 }, children: title }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "div",
                      {
                        className: "mono",
                        style: { fontSize: 10.5, color: "var(--hv-fg-subtle)", fontFamily: "var(--hv-font-mono)", marginTop: 2 },
                        children: t.id
                      }
                    )
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mono subtle", style: { fontSize: 11, alignSelf: "center" }, children: t.created_at ? formatLocalDateTime(t.created_at) : "—" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { alignSelf: "center" }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(StatusBadge, { status: t.status }) })
                ]
              },
              t.id
            );
          }) })
        ] }) }),
        tab2 === "settings" && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: 20 }, children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-body muted", children: "Configuration editor is out of scope for this prototype." }) }) })
      ]
    }
  );
}
const POLL_INTERVAL_MS$3 = 1e4;
const MODIFY_MAX_LENGTH = 5e3;
const COUNTDOWN_TICK_MS = 1e3;
function workerName(a) {
  const md = a.metadata;
  return md.duty_name || md.duty_id || "unknown";
}
function actionLabel(a) {
  return a.metadata.action_type || "action";
}
function displayStatus(status) {
  if (status === "auto_decided") return "auto";
  if (status === "approved" || status === "rejected" || status === "pending") return status;
  return "pending";
}
function useCountdown(a) {
  const timeoutMin = a.metadata.timeout_minutes;
  const createdAt = a.metadata.created_at ?? a.created_at;
  const [secsLeft, setSecsLeft] = reactExports.useState(() => {
    if (timeoutMin == null || !createdAt) return null;
    const expiresMs = parseUtc(createdAt).getTime() + timeoutMin * 60 * 1e3;
    return Math.max(0, Math.round((expiresMs - Date.now()) / 1e3));
  });
  reactExports.useEffect(() => {
    if (timeoutMin == null || !createdAt) return;
    const expiresMs = parseUtc(createdAt).getTime() + timeoutMin * 60 * 1e3;
    const timer = setInterval(() => {
      const remaining = Math.max(0, Math.round((expiresMs - Date.now()) / 1e3));
      setSecsLeft(remaining);
      if (remaining <= 0) clearInterval(timer);
    }, COUNTDOWN_TICK_MS);
    return () => clearInterval(timer);
  }, [timeoutMin, createdAt]);
  return secsLeft;
}
function ApprovalRow({ a, error, onApprove, onSkip, onModify, onClearError }) {
  const ds = displayStatus(a.metadata.status);
  const worker = workerName(a);
  const options = a.metadata.options ?? [];
  const secsLeft = useCountdown(a);
  const [modifyOpen, setModifyOpen] = reactExports.useState(false);
  const [instructions, setInstructions] = reactExports.useState("");
  const textareaRef = reactExports.useRef(null);
  function handleModifyOpen() {
    setModifyOpen(true);
    setTimeout(() => {
      var _a;
      return (_a = textareaRef.current) == null ? void 0 : _a.focus();
    }, 0);
  }
  function handleModifySubmit() {
    if (!instructions.trim()) return;
    onModify(a.id, instructions.trim());
    setModifyOpen(false);
    setInstructions("");
  }
  function formatCountdown(secs) {
    if (secs <= 0) return "expired";
    const m = Math.floor(secs / 60);
    const s = secs % 60;
    return m > 0 ? `${m}m ${s}s` : `${s}s`;
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: `hv-appr ${ds}`,
      "data-testid": `approval-item-${a.id}`,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(HexMark, { size: 40, name: worker, glow: ds === "pending" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-appr-body", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-appr-title", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: worker }),
            " ",
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "muted", style: { fontWeight: 400 }, children: [
              "wants to ",
              actionLabel(a)
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-appr-sub", children: a.content }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-appr-action", children: options.map((opt) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "key", children: "option" }),
            " ",
            opt.label,
            opt.recommended && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { marginLeft: 6, color: "var(--hv-success)", fontSize: 11 }, children: "recommended" })
          ] }, opt.id)) }),
          error && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              role: "alert",
              style: {
                marginTop: 6,
                padding: "4px 8px",
                background: "var(--hv-error-bg, rgba(220,50,50,0.12))",
                color: "var(--hv-error, #f87171)",
                borderRadius: 4,
                fontSize: 12,
                display: "flex",
                gap: 8,
                alignItems: "center"
              },
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: error }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    onClick: () => onClearError(a.id),
                    style: { background: "none", border: "none", cursor: "pointer", color: "inherit", fontSize: 11 },
                    children: "dismiss"
                  }
                )
              ]
            }
          ),
          modifyOpen && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { marginTop: 8 }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "textarea",
              {
                ref: textareaRef,
                value: instructions,
                onChange: (e) => setInstructions(e.target.value),
                maxLength: MODIFY_MAX_LENGTH,
                placeholder: "Instructions for modification…",
                rows: 3,
                style: {
                  width: "100%",
                  boxSizing: "border-box",
                  background: "var(--hv-surface2)",
                  color: "var(--hv-fg)",
                  border: "1px solid var(--hv-border)",
                  borderRadius: 4,
                  padding: "6px 8px",
                  fontSize: 12,
                  resize: "vertical"
                }
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", gap: 6, marginTop: 4 }, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", variant: "primary", onClick: handleModifySubmit, children: "Submit" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", onClick: () => {
                setModifyOpen(false);
                setInstructions("");
              }, children: "Cancel" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-appr-meta", children: [
            ds === "pending" && secsLeft != null && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
              "auto-expire in",
              " ",
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "strong",
                {
                  "data-testid": `approval-countdown-${a.id}`,
                  style: { color: "var(--hv-warn)" },
                  children: formatCountdown(secsLeft)
                }
              )
            ] }),
            a.metadata.resolved_at && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
              "decided",
              " ",
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: a.metadata.selected_option ?? "n/a" })
            ] })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-appr-act", children: ds === "pending" ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Btn,
            {
              variant: "primary",
              onClick: () => onApprove(a),
              leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.check, { size: 12 }),
              children: "Approve"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Btn,
            {
              onClick: () => onSkip(a.id),
              leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.close, { size: 12 }),
              children: "Skip"
            }
          ),
          !modifyOpen && /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", onClick: handleModifyOpen, children: "Modify" })
        ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(StatusBadge, { status: ds === "auto" ? "done" : ds }) })
      ]
    },
    a.id
  );
}
function Approvals() {
  const { approvals, errors, hydrate, approve, skip, modify, clearError } = useApprovalStore();
  reactExports.useEffect(() => {
    void hydrate();
  }, [hydrate]);
  reactExports.useEffect(() => {
    const timer = setInterval(() => {
      void hydrate();
    }, POLL_INTERVAL_MS$3);
    return () => clearInterval(timer);
  }, [hydrate]);
  const handleRefresh = reactExports.useCallback(() => {
    void hydrate();
  }, [hydrate]);
  const list2 = Object.values(approvals);
  const pending = list2.filter((a) => a.metadata.status === "pending").length;
  const approved = list2.filter((a) => a.metadata.status === "approved").length;
  const auto = list2.filter((a) => a.metadata.status === "auto_decided").length;
  function handleApprove(a) {
    const options = a.metadata.options ?? [];
    const recommended = options.find((o) => o.recommended) ?? options[0];
    if (recommended) {
      void approve(a.id, recommended.id);
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-summary", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item warn", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-dot" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: pending }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "pending" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item running", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-dot" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: approved }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "approved" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item info", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-dot" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: auto }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "auto-decided" })
        ] })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            size: "sm",
            "data-testid": "approvals-refresh-btn",
            onClick: handleRefresh,
            leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.refresh, { size: 13 }),
            children: "Refresh"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", children: "Policy" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.filter, { size: 13 }), children: "Filter" })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-appr-list", children: [
      list2.map((a) => /* @__PURE__ */ jsxRuntimeExports.jsx(
        ApprovalRow,
        {
          a,
          error: errors[a.id],
          onApprove: handleApprove,
          onSkip: (id) => {
            void skip(id);
          },
          onModify: (id, instructions) => {
            void modify(id, instructions);
          },
          onClearError: clearError
        },
        a.id
      )),
      list2.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: 40, textAlign: "center", color: "var(--hv-fg-subtle)", fontSize: 13 }, children: "No pending approvals." })
    ] })
  ] });
}
const POLL_INTERVAL_MS$2 = 1e4;
const SEVERITY_WEIGHT = {
  critical: 0,
  warning: 1,
  info: 2
};
function AlertRow({ a, busy, onAcknowledge, error, onClearError }) {
  const navigate = useNavigate();
  function severityColor2(sev) {
    if (sev === "critical") return "var(--hv-danger, #e55)";
    if (sev === "warning") return "var(--hv-warn, #e8a)";
    return "var(--hv-info, #5ae)";
  }
  function handleSourceTaskClick(taskId) {
    tracer("ALR-05", `ok nav kind=task task=${taskId}`);
    navigate(`/console/missions/${taskId}`);
  }
  function handleSourceWorkerClick(workerId) {
    tracer("ALR-05", `ok nav kind=worker worker=${workerId}`);
    navigate(`/console/fleet/${workerId}`);
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      "data-testid": `alert-row-${a.alert_id}`,
      style: {
        display: "flex",
        flexDirection: "column",
        gap: 4,
        padding: "10px 14px",
        borderBottom: "1px solid var(--hv-border)",
        opacity: a.acknowledged ? 0.55 : 1
      },
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", alignItems: "center", gap: 8 }, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "span",
            {
              style: {
                width: 8,
                height: 8,
                borderRadius: "50%",
                flexShrink: 0,
                background: severityColor2(a.severity)
              }
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "span",
            {
              style: {
                fontSize: 10,
                fontWeight: 700,
                letterSpacing: 0.5,
                textTransform: "uppercase",
                color: severityColor2(a.severity),
                minWidth: 52
              },
              children: a.severity
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { fontWeight: 600, fontSize: 13, flex: 1 }, children: a.title }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { fontSize: 11, color: "var(--hv-muted)", whiteSpace: "nowrap" }, children: formatLocalDateTime(a.created_at) })
        ] }),
        a.body && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { fontSize: 12, color: "var(--hv-fg-subtle)", paddingLeft: 16, wordBreak: "break-word" }, children: a.body }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", gap: 6, paddingLeft: 16, flexWrap: "wrap" }, children: [
          a.source_task_id != null && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              onClick: () => handleSourceTaskClick(a.source_task_id),
              style: {
                background: "var(--hv-surface2)",
                border: "1px solid var(--hv-border)",
                borderRadius: 4,
                padding: "1px 7px",
                fontSize: 11,
                cursor: "pointer",
                color: "var(--hv-fg)"
              },
              children: [
                "task: ",
                a.source_task_id
              ]
            }
          ),
          a.source_worker_id != null && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              onClick: () => handleSourceWorkerClick(a.source_worker_id),
              style: {
                background: "var(--hv-surface2)",
                border: "1px solid var(--hv-border)",
                borderRadius: 4,
                padding: "1px 7px",
                fontSize: 11,
                cursor: "pointer",
                color: "var(--hv-fg)"
              },
              children: [
                "worker: ",
                a.source_worker_id
              ]
            }
          )
        ] }),
        error && /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            role: "alert",
            style: {
              marginTop: 4,
              padding: "4px 8px",
              background: "var(--hv-error-bg, rgba(220,50,50,0.12))",
              color: "var(--hv-error, #f87171)",
              borderRadius: 4,
              fontSize: 12,
              display: "flex",
              gap: 8,
              alignItems: "center"
            },
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { flex: 1 }, children: error }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "button",
                {
                  onClick: () => onClearError(a.alert_id),
                  style: { background: "none", border: "none", cursor: "pointer", color: "inherit", fontSize: 11 },
                  children: "×"
                }
              )
            ]
          }
        ),
        !a.acknowledged && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { paddingLeft: 16, marginTop: 2 }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            size: "sm",
            onClick: () => onAcknowledge(a.alert_id),
            disabled: busy,
            "data-testid": `alert-ack-btn-${a.alert_id}`,
            children: busy ? "Acknowledging…" : "Acknowledge"
          }
        ) })
      ]
    }
  );
}
function AlertsToolbar({
  counts,
  hiddenCount,
  showHidden,
  severityFilter,
  onSetSeverity,
  onToggleHidden,
  onRefresh,
  onDismissAll,
  dismissBusy
}) {
  const FILTERS = ["all", "critical", "warning", "info"];
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-head", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-summary", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item", style: { color: "var(--hv-danger, #e55)" }, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-dot" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: counts.critical }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "critical" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item", style: { color: "var(--hv-warn, #e8a)" }, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-dot" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: counts.warning }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "warning" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item info", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-dot" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: counts.info }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "info" })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-chips", style: { marginTop: 8 }, children: FILTERS.map((f) => /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          className: `hv-chip${severityFilter === f ? " on" : ""}`,
          onClick: () => onSetSeverity(f),
          "data-testid": `alerts-filter-${f}`,
          children: f
        },
        f
      )) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-actions", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Btn,
        {
          size: "sm",
          variant: showHidden ? "subtle" : "default",
          onClick: onToggleHidden,
          "data-testid": "alerts-toggle-hidden-btn",
          "aria-pressed": showHidden,
          children: showHidden ? "Hide hidden" : `Show hidden${hiddenCount > 0 ? ` (${hiddenCount})` : ""}`
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Btn,
        {
          size: "sm",
          onClick: onRefresh,
          leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.refresh, { size: 13 }),
          "data-testid": "alerts-refresh-btn",
          children: "Refresh"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Btn,
        {
          size: "sm",
          variant: "ghost-danger",
          onClick: onDismissAll,
          disabled: dismissBusy,
          "data-testid": "alerts-dismiss-all-btn",
          children: dismissBusy ? "Dismissing…" : "Dismiss all"
        }
      )
    ] })
  ] });
}
function Alerts() {
  const alerts = useAlertStore((s) => s.alerts);
  const counts = useAlertStore((s) => s.counts);
  const acknowledgeAlert = useAlertStore((s) => s.acknowledgeAlert);
  const setAlerts = useAlertStore((s) => s.setAlerts);
  const fetchAlerts = useAlertStore((s) => s.fetchAlerts);
  const pushToast = useToastStore((s) => s.push);
  const [severityFilter, setSeverityFilter] = reactExports.useState("all");
  const [busyIds, setBusyIds] = reactExports.useState(/* @__PURE__ */ new Set());
  const [errors, setErrors] = reactExports.useState({});
  const [dismissBusy, setDismissBusy] = reactExports.useState(false);
  const [showHidden, setShowHidden] = reactExports.useState(false);
  const alr01Ref = reactExports.useRef(false);
  const doFetch = reactExports.useCallback((filter) => {
    const sev = (filter ?? severityFilter) === "all" ? void 0 : filter ?? severityFilter;
    void fetchAlerts(sev);
  }, [fetchAlerts, severityFilter]);
  reactExports.useEffect(() => {
    const sev = severityFilter === "all" ? void 0 : severityFilter;
    void fetchAlerts(sev).then(() => {
      if (!alr01Ref.current) {
        alr01Ref.current = true;
        const n = useAlertStore.getState().alerts.length;
        tracer("ALR-01", `ok mounted n=${n}`);
      }
    });
    const timer = setInterval(() => {
      const cur = severityFilter === "all" ? void 0 : severityFilter;
      void fetchAlerts(cur);
    }, POLL_INTERVAL_MS$2);
    return () => clearInterval(timer);
  }, [severityFilter]);
  const handleSetSeverity = reactExports.useCallback((s) => {
    setSeverityFilter(s);
    const sev = s === "all" ? void 0 : s;
    void fetchAlerts(sev);
    tracer("ALR-04", `ok filter=${s}`);
  }, [fetchAlerts]);
  const handleAcknowledge = reactExports.useCallback(async (alertId) => {
    setBusyIds((prev) => /* @__PURE__ */ new Set([...prev, alertId]));
    acknowledgeAlert(alertId);
    try {
      await api.alerts.acknowledge(alertId);
      tracer("ALR-02", `ok ack id=${alertId}`);
    } catch (err) {
      setAlerts(
        useAlertStore.getState().alerts.map(
          (x) => x.alert_id === alertId ? { ...x, acknowledged: false } : x
        )
      );
      const msg = err instanceof Error ? err.message : "Acknowledge failed";
      setErrors((prev) => ({ ...prev, [alertId]: msg }));
    } finally {
      setBusyIds((prev) => {
        const next = new Set(prev);
        next.delete(alertId);
        return next;
      });
    }
  }, [acknowledgeAlert, setAlerts]);
  const handleDismissAll = reactExports.useCallback(async () => {
    if (!window.confirm("Dismiss all active alerts?")) return;
    setDismissBusy(true);
    const dismissedIds = new Set(
      useAlertStore.getState().alerts.filter((a) => !a.acknowledged).map((a) => a.alert_id)
    );
    setAlerts(
      useAlertStore.getState().alerts.map(
        (x) => dismissedIds.has(x.alert_id) ? { ...x, acknowledged: true } : x
      )
    );
    try {
      await api.alerts.dismissAll();
      tracer("ALR-03", "ok dismiss-all");
      const sev = severityFilter === "all" ? void 0 : severityFilter;
      void fetchAlerts(sev);
    } catch (err) {
      setAlerts(
        useAlertStore.getState().alerts.map(
          (x) => dismissedIds.has(x.alert_id) ? { ...x, acknowledged: false } : x
        )
      );
      const msg = err instanceof Error ? err.message : "Dismiss all failed";
      pushToast({ type: "error", title: "dismiss-all failed", message: msg });
    } finally {
      setDismissBusy(false);
    }
  }, [fetchAlerts, pushToast, setAlerts, severityFilter]);
  const handleRefresh = reactExports.useCallback(() => {
    doFetch();
  }, [doFetch]);
  const handleClearError = reactExports.useCallback((alertId) => {
    setErrors((prev) => {
      const next = { ...prev };
      delete next[alertId];
      return next;
    });
  }, []);
  const handleToggleHidden = reactExports.useCallback(() => {
    setShowHidden((prev) => !prev);
  }, []);
  const visibleAlerts = showHidden ? alerts : alerts.filter((a) => !a.acknowledged);
  const hiddenCount = alerts.filter((a) => a.acknowledged).length;
  const sortedAlerts = [...visibleAlerts].sort((a, b) => {
    const wA = SEVERITY_WEIGHT[a.severity] ?? 99;
    const wB = SEVERITY_WEIGHT[b.severity] ?? 99;
    if (wA !== wB) return wA - wB;
    return parseUtc(b.created_at).getTime() - parseUtc(a.created_at).getTime();
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      AlertsToolbar,
      {
        counts,
        hiddenCount,
        showHidden,
        severityFilter,
        onSetSeverity: handleSetSeverity,
        onToggleHidden: handleToggleHidden,
        onRefresh: handleRefresh,
        onDismissAll: () => {
          void handleDismissAll();
        },
        dismissBusy
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { "data-testid": "alerts-list", children: [
      sortedAlerts.map((a) => /* @__PURE__ */ jsxRuntimeExports.jsx(
        AlertRow,
        {
          a,
          busy: busyIds.has(a.alert_id),
          onAcknowledge: (id) => {
            void handleAcknowledge(id);
          },
          error: errors[a.alert_id],
          onClearError: handleClearError
        },
        a.alert_id
      )),
      sortedAlerts.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(
        "div",
        {
          "data-testid": "alerts-empty-state",
          style: { padding: 40, textAlign: "center", color: "var(--hv-fg-subtle)", fontSize: 13 },
          children: "No alerts. The fleet is quiet."
        }
      )
    ] })
  ] });
}
const DAILY_BUDGET_USD = 21;
const TOP_WORKERS_LIMIT = 5;
function rollupToChartPoints(rollup) {
  return rollup.slice(-30).map((r) => ({ date: r.date, cost_usd: r.cost_usd }));
}
function Costs({ workers }) {
  const [chartPoints, setChartPoints] = reactExports.useState([]);
  const [totalToday, setTotalToday] = reactExports.useState(0);
  const [breakdown, setBreakdown] = reactExports.useState([]);
  const [period, setPeriod] = reactExports.useState("—");
  const [loadingChart, setLoadingChart] = reactExports.useState(true);
  const [loadingBreakdown, setLoadingBreakdown] = reactExports.useState(true);
  reactExports.useEffect(() => {
    api.costs.daily().then((data) => {
      const points = rollupToChartPoints(data.rollup ?? []);
      setChartPoints(points);
      if ((data.rollup ?? []).some((r) => r.cost_usd > 0)) {
        tracer("CAP-10", "ok");
      }
    }).catch(() => {
    }).finally(() => setLoadingChart(false));
  }, []);
  reactExports.useEffect(() => {
    api.costs.byWorker().then((data) => {
      const sorted = (data.breakdown ?? []).slice().sort((a, b) => b.amount_usd - a.amount_usd).slice(0, TOP_WORKERS_LIMIT);
      setBreakdown(sorted);
      setTotalToday(data.total_usd ?? 0);
      setPeriod(data.period ?? "—");
    }).catch(() => {
    }).finally(() => setLoadingBreakdown(false));
  }, []);
  const stats = useSystemStore((s) => s.stats);
  const budget = (stats == null ? void 0 : stats.budget_limit_usd) ?? DAILY_BUDGET_USD;
  reactExports.useEffect(() => {
    if ((stats == null ? void 0 : stats.budget_limit_usd) != null) {
      tracer("STUB-FIX-G3-cost-view", "ok budget-from-api", {
        budget: stats.budget_limit_usd
      });
    }
  }, [stats == null ? void 0 : stats.budget_limit_usd]);
  const pct = Math.min(100, totalToday / budget * 100);
  const maxV = chartPoints.length > 0 ? Math.max(...chartPoints.map((p) => p.cost_usd), 1e-3) : 1e-3;
  function resolveWorkerName(workerId) {
    const match = workers.find((w) => w.id === workerId);
    return (match == null ? void 0 : match.name) ?? workerId;
  }
  const isLoading = loadingChart || loadingBreakdown;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-view-summary", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-item", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-lbl", children: [
        "today · UTC · ",
        isLoading ? "loading…" : "live"
      ] }) }) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", children: "Export CSV" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", children: "Budget limits" })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-cost-grid", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", flexDirection: "column", gap: 16 }, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Spend · daily" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-panel-sub", children: "last 30 days" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-actions", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "honey", children: "live" }) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-body", children: /* @__PURE__ */ jsxRuntimeExports.jsx(CostChart, { chartPoints, maxV }) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", "data-testid": "cost-top-contributors", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-head", children: /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Top contributors" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("table", { className: "hv-cost-row-table", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("thead", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Worker" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("th", { className: "num", children: "Cost" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("th", { className: "num", children: "%" })
            ] }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("tbody", { children: [
              breakdown.map((entry) => /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("td", { style: { fontWeight: 600 }, children: resolveWorkerName(entry.group_key) }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("td", { className: "num", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { style: { color: "var(--hv-fg)" }, children: [
                  "$",
                  entry.amount_usd.toFixed(4)
                ] }) }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("td", { className: "num", style: { color: "var(--hv-honey-4)" }, children: [
                  totalToday > 0 ? (entry.amount_usd / totalToday * 100).toFixed(0) : "0",
                  "%"
                ] })
              ] }, entry.group_key)),
              breakdown.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("tr", { children: /* @__PURE__ */ jsxRuntimeExports.jsx("td", { colSpan: 3, style: { color: "var(--hv-fg-subtle)", textAlign: "center", padding: "12px 0" }, children: "No cost entries yet." }) })
            ] })
          ] }) })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", flexDirection: "column", gap: 16 }, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-head", children: /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Today" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-body", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-cost-total", children: [
              "$",
              totalToday.toFixed(4)
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "muted", style: { fontSize: 12, marginTop: 2 }, children: [
              "of $",
              budget.toFixed(2),
              " daily budget · ",
              pct.toFixed(0),
              "%"
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "div",
              {
                "data-testid": "cost-budget-bar",
                className: `hv-budget-bar${pct > 100 ? " over" : ""}`,
                role: "progressbar",
                "aria-valuenow": Math.round(pct),
                "aria-valuemin": 0,
                "aria-valuemax": 100,
                "aria-label": "Budget usage",
                children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { width: `${Math.min(100, pct)}%` } })
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { marginTop: 14, display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 }, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "kicker", children: "Period" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mono", style: { fontSize: 14 }, children: period })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "kicker", children: "Entries" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mono", style: { fontSize: 16 }, children: breakdown.length })
              ] })
            ] })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-head", children: /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Budget alerts" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-body", style: { display: "flex", flexDirection: "column", gap: 8 }, children: pct >= 80 ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-attn-item warn", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-attn-title", children: [
              pct.toFixed(0),
              "% of daily cap"
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-attn-body", children: "Approaching daily budget limit." })
          ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-attn-item info", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-attn-title", children: "Within budget" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-attn-body", children: [
              "$",
              totalToday.toFixed(4),
              " of $",
              budget.toFixed(2),
              " used today."
            ] })
          ] }) })
        ] })
      ] })
    ] })
  ] });
}
function CostChart({ chartPoints, maxV }) {
  if (chartPoints.length === 0) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        "data-testid": "cost-chart",
        style: {
          height: 220,
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          color: "var(--hv-fg-subtle)",
          fontSize: 13
        },
        children: "No cost data yet."
      }
    );
  }
  const W = 800;
  const H = 220;
  const PAD_L = 55;
  const PAD_R = 20;
  const PAD_T = 20;
  const PAD_B = 30;
  const chartW = W - PAD_L - PAD_R;
  const chartH = H - PAD_T - PAD_B;
  const barW = Math.max(4, Math.floor(chartW / chartPoints.length) - 2);
  const gridFracs = [0, 0.25, 0.5, 0.75, 1];
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "svg",
    {
      "data-testid": "cost-chart",
      width: "100%",
      height: H,
      viewBox: `0 0 ${W} ${H}`,
      preserveAspectRatio: "none",
      "aria-label": "Daily cost chart",
      role: "img",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("defs", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("linearGradient", { id: "bar-grad", x1: "0", x2: "0", y1: "0", y2: "1", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("stop", { offset: "0%", stopColor: "#EE961F", stopOpacity: "0.85" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("stop", { offset: "100%", stopColor: "#EE961F", stopOpacity: "0.4" })
        ] }) }),
        gridFracs.map((f) => {
          const y = PAD_T + f * chartH;
          return /* @__PURE__ */ jsxRuntimeExports.jsxs("g", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "line",
              {
                x1: PAD_L,
                x2: W - PAD_R,
                y1: y,
                y2: y,
                stroke: "var(--hv-border)",
                strokeDasharray: "3 4"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "text",
              {
                x: PAD_L - 4,
                y: y + 4,
                textAnchor: "end",
                fontSize: "10",
                fill: "var(--hv-fg-subtle)",
                fontFamily: "var(--hv-font-mono)",
                children: [
                  "$",
                  ((1 - f) * maxV).toFixed(3)
                ]
              }
            )
          ] }, f);
        }),
        chartPoints.map((pt, i) => {
          const x = PAD_L + i / chartPoints.length * chartW + 1;
          const barH = Math.max(1, pt.cost_usd / maxV * chartH);
          const y = PAD_T + chartH - barH;
          return /* @__PURE__ */ jsxRuntimeExports.jsx(
            "rect",
            {
              x,
              y,
              width: barW,
              height: barH,
              fill: "url(#bar-grad)",
              rx: "2"
            },
            pt.date
          );
        }),
        [0, Math.floor(chartPoints.length / 2), chartPoints.length - 1].filter((idx, pos, arr) => arr.indexOf(idx) === pos && chartPoints[idx]).map((i) => {
          const x = PAD_L + i / chartPoints.length * chartW + barW / 2;
          return /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "text",
            {
              x,
              y: H - 6,
              textAnchor: "middle",
              fontSize: "9",
              fill: "var(--hv-fg-subtle)",
              fontFamily: "var(--hv-font-mono)",
              children: [
                chartPoints[i].date.slice(5),
                " "
              ]
            },
            i
          );
        })
      ]
    }
  );
}
let _cap49DutiesEmitted = false;
function isEnabled(d) {
  return d.status === "active";
}
function triggerType(d) {
  if (d.event_triggers && d.event_triggers.length > 0) return "event";
  return "cron";
}
function cronDisplay(d) {
  const s = d.cron_interval_s;
  if (s < 120) return `every ${s}s`;
  if (s < 7200) return `every ${Math.round(s / 60)}m`;
  if (s < 172800) return `every ${Math.round(s / 3600)}h`;
  return `every ${Math.round(s / 86400)}d`;
}
function relativeTime(iso) {
  if (!iso) return "never";
  try {
    const diff = Date.now() - parseUtc(iso).getTime();
    const s = Math.floor(diff / 1e3);
    if (s < 60) return `${s}s ago`;
    if (s < 3600) return `${Math.floor(s / 60)}m ago`;
    if (s < 86400) return `${Math.floor(s / 3600)}h ago`;
    return `${Math.floor(s / 86400)}d ago`;
  } catch {
    return "unknown";
  }
}
function EditForm({
  d,
  onSave,
  onCancel
}) {
  const [name2, setName] = reactExports.useState(d.name);
  const [interval, setInterval2] = reactExports.useState(String(d.cron_interval_s));
  const [dutyType, setDutyType] = reactExports.useState(d.duty_type);
  const [saving, setSaving] = reactExports.useState(false);
  const [error, setError] = reactExports.useState(null);
  reactExports.useEffect(() => {
    const handler = (e) => {
      if (e.key === "Escape") onCancel();
    };
    document.addEventListener("keydown", handler);
    return () => document.removeEventListener("keydown", handler);
  }, [onCancel]);
  async function handleSave() {
    const intervalNum = Number(interval);
    if (!name2.trim()) {
      setError("Name is required");
      return;
    }
    if (isNaN(intervalNum) || intervalNum < 1) {
      setError("Interval must be a positive number");
      return;
    }
    setSaving(true);
    setError(null);
    try {
      await onSave({ name: name2.trim(), cron_interval_s: intervalNum, duty_type: dutyType });
      onCancel();
    } catch (err) {
      setError(err instanceof Error ? err.message : "Save failed");
    } finally {
      setSaving(false);
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-expand", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-expand-grid", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "hv-duty-exp-kicker", htmlFor: `edit-name-${d.id}`, children: "Name" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "input",
          {
            id: `edit-name-${d.id}`,
            value: name2,
            onChange: (e) => setName(e.target.value),
            style: { width: "100%" }
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "hv-duty-exp-kicker", htmlFor: `edit-interval-${d.id}`, children: "Interval (s)" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "input",
          {
            id: `edit-interval-${d.id}`,
            type: "number",
            min: 1,
            value: interval,
            onChange: (e) => setInterval2(e.target.value),
            style: { width: "100%" }
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "hv-duty-exp-kicker", htmlFor: `edit-type-${d.id}`, children: "Type" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "select",
          {
            id: `edit-type-${d.id}`,
            value: dutyType,
            onChange: (e) => setDutyType(e.target.value),
            children: ["recon", "command", "engineering", "sentinel", "custom"].map((t) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: t, children: t }, t))
          }
        )
      ] })
    ] }),
    error && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { role: "alert", style: { color: "var(--hv-danger)", fontSize: 12, padding: "4px 0" }, children: error }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-expand-foot", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", variant: "primary", onClick: handleSave, disabled: saving, children: saving ? "Saving…" : "Save" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", variant: "subtle", onClick: onCancel, disabled: saving, children: "Cancel" })
    ] })
  ] });
}
function NewDutyForm({
  onCreate,
  onCancel
}) {
  const [name2, setName] = reactExports.useState("");
  const [interval, setInterval2] = reactExports.useState("3600");
  const [dutyType, setDutyType] = reactExports.useState("custom");
  const [triggerMode, setTriggerMode] = reactExports.useState("cron");
  const [eventTriggers, setEventTriggers] = reactExports.useState("");
  const [skillPath, setSkillPath] = reactExports.useState("");
  const [timeoutMinutes, setTimeoutMinutes] = reactExports.useState("");
  const [configText, setConfigText] = reactExports.useState("");
  const [saving, setSaving] = reactExports.useState(false);
  const [error, setError] = reactExports.useState(null);
  reactExports.useEffect(() => {
    const handler = (e) => {
      if (e.key === "Escape") onCancel();
    };
    document.addEventListener("keydown", handler);
    return () => document.removeEventListener("keydown", handler);
  }, [onCancel]);
  async function handleCreate() {
    if (!name2.trim()) {
      setError("Name is required");
      return;
    }
    const req = {
      name: name2.trim(),
      duty_type: dutyType
    };
    if (triggerMode === "cron") {
      const intervalNum = Number(interval);
      if (isNaN(intervalNum) || intervalNum < 1) {
        setError("Interval must be a positive number");
        return;
      }
      req.cron_interval_s = intervalNum;
    } else {
      const triggers = eventTriggers.split(",").map((s) => s.trim()).filter(Boolean);
      if (triggers.length === 0) {
        setError("At least one event trigger is required");
        return;
      }
      req.event_triggers = triggers;
    }
    if (skillPath.trim()) req.skill_path = skillPath.trim();
    if (timeoutMinutes.trim()) {
      const t = Number(timeoutMinutes);
      if (isNaN(t) || t < 1 || t > 1440) {
        setError("Timeout must be 1–1440 minutes");
        return;
      }
      req.timeout_minutes = t;
    }
    if (configText.trim()) {
      try {
        const parsed = JSON.parse(configText);
        if (parsed && typeof parsed === "object" && !Array.isArray(parsed)) {
          req.config = parsed;
        } else {
          setError("Config must be a JSON object");
          return;
        }
      } catch {
        setError("Config is not valid JSON");
        return;
      }
    }
    setSaving(true);
    setError(null);
    try {
      await onCreate(req);
      tracer(
        "CAP-54",
        "ok global-new-duty-real-config trigger=" + triggerMode + " skill=" + (skillPath ? "1" : "0") + " timeout=" + (timeoutMinutes ? "1" : "0") + " config=" + (configText ? "1" : "0")
      );
      onCancel();
    } catch (err) {
      setError(err instanceof Error ? err.message : "Create failed");
    } finally {
      setSaving(false);
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-expand", style: { marginBottom: 8 }, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-expand-grid", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "hv-duty-exp-kicker", htmlFor: "new-duty-name", children: "Name" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "input",
          {
            id: "new-duty-name",
            value: name2,
            onChange: (e) => setName(e.target.value),
            placeholder: "e.g. daily-health-check",
            style: { width: "100%" }
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "hv-duty-exp-kicker", htmlFor: "new-duty-trigger-mode", children: "Trigger" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "select",
          {
            id: "new-duty-trigger-mode",
            value: triggerMode,
            onChange: (e) => setTriggerMode(e.target.value),
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "cron", children: "cron — interval" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "event", children: "event — backend events" })
            ]
          }
        )
      ] }),
      triggerMode === "cron" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "hv-duty-exp-kicker", htmlFor: "new-duty-interval", children: "Interval (s)" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "input",
          {
            id: "new-duty-interval",
            type: "number",
            min: 1,
            value: interval,
            onChange: (e) => setInterval2(e.target.value),
            style: { width: "100%" }
          }
        )
      ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "hv-duty-exp-kicker", htmlFor: "new-duty-event-triggers", children: "Event triggers (comma-separated)" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "input",
          {
            id: "new-duty-event-triggers",
            value: eventTriggers,
            onChange: (e) => setEventTriggers(e.target.value),
            placeholder: "e.g. task_failed, worker_auto_killed",
            style: { width: "100%" }
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "hv-duty-exp-kicker", htmlFor: "new-duty-type", children: "Type" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "select",
          {
            id: "new-duty-type",
            value: dutyType,
            onChange: (e) => setDutyType(e.target.value),
            children: ["recon", "command", "engineering", "sentinel", "custom"].map((t) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: t, children: t }, t))
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "hv-duty-exp-kicker", htmlFor: "new-duty-skill-path", children: "Skill path (optional)" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "input",
          {
            id: "new-duty-skill-path",
            value: skillPath,
            onChange: (e) => setSkillPath(e.target.value),
            placeholder: "e.g. templates/duties/test-ui.md",
            style: { width: "100%" }
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "hv-duty-exp-kicker", htmlFor: "new-duty-timeout", children: "Timeout (min, optional)" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "input",
          {
            id: "new-duty-timeout",
            type: "number",
            min: 1,
            max: 1440,
            value: timeoutMinutes,
            onChange: (e) => setTimeoutMinutes(e.target.value),
            placeholder: "1–1440",
            style: { width: "100%" }
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { gridColumn: "1 / -1" }, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "hv-duty-exp-kicker", htmlFor: "new-duty-config", children: "Config JSON (optional)" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "textarea",
          {
            id: "new-duty-config",
            rows: 3,
            value: configText,
            onChange: (e) => setConfigText(e.target.value),
            placeholder: '{"key": "value"}',
            style: { width: "100%", fontFamily: "monospace", fontSize: 12 }
          }
        )
      ] })
    ] }),
    error && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { role: "alert", style: { color: "var(--hv-danger)", fontSize: 12, padding: "4px 0" }, children: error }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-expand-foot", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", variant: "primary", onClick: handleCreate, disabled: saving, children: saving ? "Creating…" : "Create duty" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", variant: "subtle", onClick: onCancel, disabled: saving, children: "Cancel" })
    ] })
  ] });
}
function outcomeTone(outcome) {
  if (outcome === "success" || outcome === "ok") return "success";
  if (outcome === "warn") return "warn";
  return "danger";
}
function DutyRow({ d, onPause, onResume, onTrigger, onDelete, onUpdate }) {
  const [open, setOpen] = reactExports.useState(false);
  const [editing, setEditing] = reactExports.useState(false);
  const [busy, setBusy] = reactExports.useState(null);
  const [rowError, setRowError] = reactExports.useState(null);
  const [runs, setRuns] = reactExports.useState(null);
  const enabled = isEnabled(d);
  const trigger = triggerType(d);
  const cron = trigger === "cron" ? cronDisplay(d) : (d.event_triggers ?? []).join(", ");
  const lastRun = relativeTime(d.last_triggered_at);
  const sevenDaysAgo = Date.now() - 7 * 86400 * 1e3;
  const runs7d = runs ? runs.filter((r) => r.started_at && parseUtc(r.started_at).getTime() > sevenDaysAgo).length : 0;
  const fails7d = runs ? runs.filter((r) => r.started_at && parseUtc(r.started_at).getTime() > sevenDaysAgo && (r.status === "failed" || r.status === "timeout")).length : 0;
  const lastStatus = (() => {
    if (!runs || runs.length === 0) return d.status === "error" ? "fail" : "ok";
    const sorted = [...runs].sort((a, b) => {
      const ta = a.started_at ? parseUtc(a.started_at).getTime() : 0;
      const tb = b.started_at ? parseUtc(b.started_at).getTime() : 0;
      return tb - ta;
    });
    const latest = sorted[0];
    return latest.status === "success" ? "ok" : latest.status;
  })();
  const tone = outcomeTone(lastStatus);
  async function doAction(key, action) {
    setBusy(key);
    setRowError(null);
    try {
      await action();
    } catch (err) {
      setRowError(err instanceof Error ? err.message : "Action failed");
    } finally {
      setBusy(null);
    }
  }
  function handleToggle(e) {
    e.stopPropagation();
    if (enabled) {
      void doAction("pause", () => onPause(d.id));
    } else {
      void doAction("resume", () => onResume(d.id));
    }
  }
  function handleExpand() {
    const next = !open;
    setOpen(next);
    if (next && runs === null) {
      void fetchRuns();
    }
  }
  async function fetchRuns() {
    try {
      const res = await fetch(`/api/duties/${d.id}/runs?limit=50`);
      if (!res.ok) return;
      const data = await res.json();
      setRuns(data.runs ?? []);
    } catch {
      setRuns([]);
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: `hv-duty-row${!enabled ? " paused" : ""}`,
        "data-testid": `duty-row-${d.id}`,
        onClick: handleExpand,
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-name-cell", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                className: `hv-duty-toggle${enabled ? " on" : ""}`,
                onClick: handleToggle,
                "aria-label": `${enabled ? "Disable" : "Enable"} ${d.name}`,
                "aria-pressed": enabled,
                disabled: busy === "pause" || busy === "resume",
                children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-duty-toggle-knob" })
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-duty-name", children: d.name }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-duty-id", children: d.id })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-duty-sched", children: trigger === "cron" ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.clock, { size: 11 }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", children: cron })
          ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.zap, { size: 11 }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: cron })
          ] }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-duty-role", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: d.duty_type }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-last", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `dot dot-${tone}`, style: { background: `var(--hv-${tone})` } }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: lastRun }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-duty-outcome", children: lastStatus })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-duty-next mono", children: enabled ? "—" : "paused" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-stats mono", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: runs7d }),
            fails7d > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-duty-fails", children: [
              " · ",
              fails7d,
              " fail"
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-actions", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                className: "hv-ib",
                "aria-label": "Run now",
                title: "Run now",
                "data-testid": `duty-trigger-btn-${d.id}`,
                disabled: busy === "trigger",
                onClick: (e) => {
                  e.stopPropagation();
                  void doAction("trigger", () => onTrigger(d.id));
                },
                children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.play, { size: 12 })
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(IconBtn, { icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.more, { size: 13 }), label: "More", onClick: () => void 0 }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `hv-duty-chev${open ? " open" : ""}`, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.chevD, { size: 12 }) })
          ] })
        ]
      }
    ),
    rowError && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { role: "alert", style: { color: "var(--hv-danger)", fontSize: 12, padding: "4px 12px" }, children: rowError }),
    open && !editing && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-expand", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-expand-grid", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-duty-exp-kicker", children: "Type" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-duty-exp-body", children: d.duty_type })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-duty-exp-kicker", children: "Settings" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-duty-settings", children: [
            ["Interval", cronDisplay(d)],
            ["Status", d.status],
            ["Created", relativeTime(d.created_at)]
          ].map(([k, v]) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-setting", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: k }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", children: v })
          ] }, k)) })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-expand-foot", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            size: "sm",
            variant: "subtle",
            leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.play, { size: 11 }),
            onClick: (e) => {
              e.stopPropagation();
              void doAction("trigger", () => onTrigger(d.id));
            },
            disabled: busy === "trigger",
            children: busy === "trigger" ? "Triggering…" : "Run now"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            size: "sm",
            variant: "subtle",
            leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.settings, { size: 11 }),
            onClick: (e) => {
              e.stopPropagation();
              setEditing(true);
            },
            children: "Edit"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", variant: "subtle", leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.pulse, { size: 11 }), children: "View runs" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { flex: 1 } }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            size: "sm",
            variant: "subtle",
            "data-testid": `duty-pause-btn-${d.id}`,
            onClick: (e) => {
              e.stopPropagation();
              if (enabled) {
                void doAction("pause", () => onPause(d.id));
              } else {
                void doAction("resume", () => onResume(d.id));
              }
            },
            disabled: busy === "pause" || busy === "resume",
            children: busy === "pause" ? "Pausing…" : busy === "resume" ? "Resuming…" : enabled ? "Pause" : "Resume"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            size: "sm",
            variant: "subtle",
            onClick: (e) => {
              e.stopPropagation();
              if (!confirm("Delete this duty?")) return;
              void doAction("delete", () => onDelete(d.id));
            },
            disabled: busy === "delete",
            children: busy === "delete" ? "Deleting…" : "Delete"
          }
        )
      ] })
    ] }),
    open && editing && /* @__PURE__ */ jsxRuntimeExports.jsx(
      EditForm,
      {
        d,
        onSave: (req) => onUpdate(d.id, req),
        onCancel: () => setEditing(false)
      }
    )
  ] });
}
function DutiesView() {
  const { duties, loading, hydrate, pauseDuty, resumeDuty, triggerDuty, deleteDuty, updateDuty, createDuty } = useDutyStore();
  const [filter, setFilter] = reactExports.useState("all");
  const [q, setQ] = reactExports.useState("");
  const [showNewForm, setShowNewForm] = reactExports.useState(false);
  const [searchParams, setSearchParams] = useSearchParams();
  reactExports.useEffect(() => {
    void hydrate().then(() => {
      tracer("UI-D-duties", "ok");
    });
  }, [hydrate]);
  reactExports.useEffect(() => {
    if (searchParams.get("new") === "1") {
      setShowNewForm(true);
      const next = new URLSearchParams(searchParams);
      next.delete("new");
      setSearchParams(next, { replace: true });
      if (!_cap49DutiesEmitted) {
        _cap49DutiesEmitted = true;
        tracer("CAP-49", "ok global-new-duty source=duties-view");
      }
    }
  }, [searchParams, setSearchParams]);
  const dutyList = Object.values(duties);
  const filtered = dutyList.filter((d) => {
    if (filter === "cron" && triggerType(d) !== "cron") return false;
    if (filter === "event" && triggerType(d) !== "event") return false;
    if (filter === "paused" && isEnabled(d)) return false;
    if (q && !d.name.toLowerCase().includes(q.toLowerCase())) return false;
    return true;
  });
  const totals = {
    total: dutyList.length,
    enabled: dutyList.filter((d) => isEnabled(d)).length,
    // runs7d at view level uses run_stats totals (lazy — exact counts in row)
    runs7d: dutyList.reduce((s, d) => {
      const r = d.run_stats ?? {};
      return s + Object.values(r).reduce((a, b) => a + b, 0);
    }, 0),
    fails7d: dutyList.reduce((s, d) => {
      const r = d.run_stats ?? {};
      return s + (r["failed"] ?? 0) + (r["timeout"] ?? 0);
    }, 0)
  };
  const filterTabs = [
    { k: "all", label: "All", n: dutyList.length },
    { k: "cron", label: "Cron", n: dutyList.filter((d) => triggerType(d) === "cron").length },
    { k: "event", label: "Event", n: dutyList.filter((d) => triggerType(d) === "event").length },
    { k: "paused", label: "Paused", n: dutyList.filter((d) => !isEnabled(d)).length }
  ];
  if (loading) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-view", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { "data-testid": "skeleton-duties", style: { padding: 16 }, children: Array.from({ length: SKELETON_COUNT_DEFAULT }, (_, i) => /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonRow$1, { cols: 4 }, i)) }) });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-summary", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: totals.total }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "total" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-sep" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item running", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-dot" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: totals.enabled }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "enabled" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: totals.runs7d.toLocaleString() }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "runs / 7d" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item warn", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-dot" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: totals.fails7d }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "failed" })
        ] })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-fleet-search", style: { width: 220 }, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.search, { size: 12 }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "input",
            {
              placeholder: "Filter by name…",
              value: q,
              onChange: (e) => setQ(e.target.value),
              "aria-label": "Filter duties"
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.filter, { size: 13 }), children: "Filter" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            size: "sm",
            variant: "primary",
            leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.plus, { size: 13 }),
            onClick: () => setShowNewForm((f) => !f),
            children: "New duty"
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-duty-filters", children: filterTabs.map((f) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "button",
      {
        className: `hv-duty-filter${filter === f.k ? " on" : ""}`,
        onClick: () => setFilter(f.k),
        children: [
          f.label,
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-duty-filter-n", children: f.n })
        ]
      },
      f.k
    )) }),
    showNewForm && /* @__PURE__ */ jsxRuntimeExports.jsx(
      NewDutyForm,
      {
        onCreate: (req) => createDuty(req).then(() => void 0),
        onCancel: () => setShowNewForm(false)
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-table", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-duty-thead", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "Duty" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "Schedule" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "Type" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "Last run" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "Next" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mono", children: "7d" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", {})
      ] }),
      filtered.map((d) => /* @__PURE__ */ jsxRuntimeExports.jsx(
        DutyRow,
        {
          d,
          onPause: pauseDuty,
          onResume: resumeDuty,
          onTrigger: triggerDuty,
          onDelete: deleteDuty,
          onUpdate: updateDuty
        },
        d.id
      )),
      filtered.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: 40, textAlign: "center", color: "var(--hv-fg-subtle)", fontSize: 13 }, children: "No duties match this filter." })
    ] })
  ] });
}
function ChannelList({ channels, activeId, onSelect, unreadCounts }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-chat-sidebar", children: [
    channels.map((ch) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "button",
      {
        className: `hv-chat-channel${ch.id === activeId ? " active" : ""}`,
        "data-testid": `channel-item-${ch.id}`,
        onClick: () => onSelect(ch.id),
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-chat-channel-name", children: [
            "# ",
            ch.name
          ] }),
          (unreadCounts[ch.id] ?? 0) > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "honey", children: unreadCounts[ch.id] })
        ]
      },
      ch.id
    )),
    channels.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: "16px 12px", color: "var(--hv-fg-subtle)", fontSize: 12 }, children: "No channels yet." })
  ] });
}
function MessageBubble({ message }) {
  const isOperator = message.sender_type === "operator";
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: `hv-chat-msg${isOperator ? " user" : ""}`,
      "data-testid": "chat-message",
      "data-message-id": message.id,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "who mono", children: message.sender_id ?? message.sender_type }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "body", children: message.content })
      ]
    }
  );
}
function sortChannels(channels) {
  return [...channels].sort((a, b) => {
    const na = (a.name ?? a.id).toLowerCase();
    const nb = (b.name ?? b.id).toLowerCase();
    if (na < nb) return -1;
    if (na > nb) return 1;
    return a.id.localeCompare(b.id);
  });
}
function Chat() {
  const {
    channels,
    messages,
    unreadCounts,
    activeChannelId,
    hydrate,
    setMessages,
    setActiveChannel,
    addMessage,
    markRead
  } = useChannelStore();
  const [drafts, setDrafts] = reactExports.useState({});
  const [sending, setSending] = reactExports.useState(false);
  const bottomRef = reactExports.useRef(null);
  const [windowH, setWindowH] = reactExports.useState(1);
  function parseTs(iso) {
    if (!iso) return 0;
    return parseUtc(iso).getTime();
  }
  const cutoffMs = windowH === null ? 0 : Date.now() - windowH * 3600 * 1e3;
  reactExports.useEffect(() => {
    void hydrate();
  }, [hydrate]);
  reactExports.useEffect(() => {
    const sorted = sortChannels(Object.values(channels));
    if (sorted.length > 0 && activeChannelId === null) {
      setActiveChannel(sorted[0].id);
    }
  }, [channels, activeChannelId, setActiveChannel]);
  reactExports.useEffect(() => {
    if (!activeChannelId) return;
    api.channels.messages(activeChannelId).then((msgs) => {
      setMessages(activeChannelId, msgs.slice(-50));
      markRead(activeChannelId);
      tracer("UI-D-chat", "ok");
    }).catch(() => {
    });
  }, [activeChannelId, setMessages, markRead]);
  reactExports.useEffect(() => {
    var _a;
    (_a = bottomRef.current) == null ? void 0 : _a.scrollIntoView({ behavior: "smooth" });
  }, [messages, activeChannelId]);
  const allChannels = Object.values(channels);
  const filteredChannels = windowH === null ? allChannels : allChannels.filter((ch) => {
    const msgs = messages[ch.id] ?? [];
    if (msgs.length === 0) return false;
    const newest = msgs.reduce((m, x) => Math.max(m, parseTs(x.created_at)), 0);
    return newest >= cutoffMs;
  });
  const sortedChannels = sortChannels(filteredChannels);
  const activeChannel = activeChannelId ? channels[activeChannelId] : void 0;
  const allMessages = activeChannelId ? messages[activeChannelId] ?? [] : [];
  const activeMessages = windowH === null ? allMessages : allMessages.filter((m) => parseTs(m.created_at) >= cutoffMs);
  const draft = activeChannelId ? drafts[activeChannelId] ?? "" : "";
  function setDraft(value) {
    if (!activeChannelId) return;
    setDrafts((prev) => ({ ...prev, [activeChannelId]: value }));
  }
  function handleSelectChannel(id) {
    setActiveChannel(id);
  }
  async function handleSend() {
    if (!activeChannelId || !draft.trim()) return;
    setSending(true);
    try {
      const sent = await api.channels.send(activeChannelId, {
        content: draft.trim(),
        sender_type: "operator"
      });
      addMessage(activeChannelId, sent);
      setDraft("");
    } catch (e) {
      useToastStore.getState().push({
        type: "error",
        title: "Send failed",
        message: String(e)
      });
    } finally {
      setSending(false);
    }
  }
  function handleKeyDown(e) {
    if ((e.metaKey || e.ctrlKey) && e.key === "Enter") {
      e.preventDefault();
      void handleSend();
      return;
    }
    if (e.key === "Enter" && !e.shiftKey) {
      e.preventDefault();
      void handleSend();
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view", style: { padding: 0, display: "flex", flexDirection: "column" }, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-head", style: { padding: "12px 20px" }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { fontSize: 16, fontWeight: 600 }, children: [
          "Chat",
          activeChannel ? ` · #${activeChannel.name}` : ""
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { fontSize: 12.5, color: "var(--hv-fg-muted)", fontFamily: "var(--hv-font-mono)" }, children: [
          activeMessages.length,
          " messages"
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-actions", style: { display: "flex", gap: 8, alignItems: "center" }, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { style: { fontSize: 12, color: "var(--hv-fg-muted)" }, children: "Window" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "select",
          {
            "data-testid": "chat-window-filter",
            value: windowH === null ? "all" : String(windowH),
            onChange: (e) => setWindowH(e.target.value === "all" ? null : Number(e.target.value)),
            className: "hv-inp",
            style: { fontSize: 12, padding: "4px 8px" },
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "1", children: "Last 1h" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "2", children: "Last 2h" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "6", children: "Last 6h" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "24", children: "Last 24h" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "all", children: "All" })
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.filter, { size: 13 }), children: "Jump" })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-chat-panel", style: { display: "flex", flex: 1, overflow: "hidden" }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        ChannelList,
        {
          channels: sortedChannels,
          activeId: activeChannelId,
          onSelect: handleSelectChannel,
          unreadCounts
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", flexDirection: "column", flex: 1, overflow: "hidden" }, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: "hv-chat",
            style: { flex: 1, overflowY: "auto" },
            role: "log",
            "aria-live": "polite",
            "aria-label": "Chat messages",
            "aria-relevant": "additions",
            children: [
              activeMessages.map((m) => /* @__PURE__ */ jsxRuntimeExports.jsx(MessageBubble, { message: m }, m.id)),
              activeMessages.length === 0 && activeChannelId && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: 40, textAlign: "center", color: "var(--hv-fg-subtle)", fontSize: 13 }, children: "No messages yet." }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref: bottomRef })
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-chat-composer", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "input",
            {
              className: "hv-inp",
              placeholder: activeChannel ? `Message #${activeChannel.name}…` : "Select a channel…",
              style: { flex: 1 },
              "aria-label": "Chat message",
              "data-testid": "chat-message-input",
              value: draft,
              onChange: (e) => setDraft(e.target.value),
              onKeyDown: handleKeyDown,
              disabled: !activeChannelId || sending
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Btn,
            {
              variant: "primary",
              leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.arrowR, { size: 13 }),
              "data-testid": "chat-send-btn",
              onClick: () => void handleSend(),
              children: "Send"
            }
          )
        ] })
      ] })
    ] })
  ] });
}
function HumanReviewBanner(p) {
  const {
    taskId,
    blockedOn,
    warnings,
    reviewOutcomes,
    currentStage,
    resumeBusy,
    cancelBusy,
    onResume,
    onCancel,
    onShowFeedback
  } = p;
  const hrv01FiredRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    if (blockedOn === "human_review" && hrv01FiredRef.current !== taskId) {
      hrv01FiredRef.current = taskId;
      tracer("HRV-01", `ok banner_shown task=${taskId} warnings=${warnings.join(",")}`);
    }
  }, [blockedOn, taskId, warnings]);
  if (blockedOn !== "human_review") return null;
  const anyBusy = resumeBusy || cancelBusy;
  const reviseCount = (reviewOutcomes ?? []).filter((o) => o === "reviewer_revise").length;
  const stageLabel2 = currentStage ? currentStage.replace(/_/g, " ") : "plan";
  const handleBannerClick = (e) => {
    if (!onShowFeedback) return;
    const tgt = e.target;
    if (tgt.closest('[data-testid="human-review-resume-btn"]')) return;
    if (tgt.closest('[data-testid="human-review-cancel-btn"]')) return;
    onShowFeedback();
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      "data-testid": "human-review-banner",
      "data-compact": "true",
      role: onShowFeedback ? "button" : "alert",
      tabIndex: onShowFeedback ? 0 : void 0,
      "aria-label": onShowFeedback ? "Open reviewer feedback" : void 0,
      onClick: onShowFeedback ? handleBannerClick : void 0,
      onKeyDown: onShowFeedback ? (e) => {
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          onShowFeedback();
        }
      } : void 0,
      style: {
        margin: "6px 12px",
        padding: "4px 12px",
        background: "var(--hv-warn-bg, rgba(220,160,30,0.12))",
        border: "1px solid var(--hv-warn-border, rgba(220,160,30,0.4))",
        borderRadius: 4,
        display: "flex",
        alignItems: "center",
        gap: 10,
        fontSize: 12,
        flexWrap: "wrap",
        cursor: onShowFeedback ? "pointer" : "default"
      },
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { color: "var(--hv-warn, #facc15)", fontWeight: 700 }, children: "⚠" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { flex: 1, minWidth: 200 }, children: [
          "Paused at ",
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", children: stageLabel2 }),
          reviseCount >= 2 && ` — reviewer rejected ${reviseCount}×`,
          onShowFeedback && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "span",
            {
              "data-testid": "human-review-jump-feedback",
              "aria-hidden": "true",
              style: { display: "none" }
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            variant: "primary",
            size: "sm",
            onClick: onResume,
            disabled: anyBusy,
            "data-testid": "human-review-resume-btn",
            children: resumeBusy ? "…" : "Resume"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            variant: "ghost-danger",
            size: "sm",
            onClick: onCancel,
            disabled: anyBusy,
            "data-testid": "human-review-cancel-btn",
            children: cancelBusy ? "…" : "Cancel"
          }
        )
      ]
    }
  );
}
function ok$1() {
}
function unreachable() {
}
function stringify$1(values, options) {
  const settings = {};
  const input = values[values.length - 1] === "" ? [...values, ""] : values;
  return input.join(
    (settings.padRight ? " " : "") + "," + (settings.padLeft === false ? "" : " ")
  ).trim();
}
const nameRe = /^[$_\p{ID_Start}][$_\u{200C}\u{200D}\p{ID_Continue}]*$/u;
const nameReJsx = /^[$_\p{ID_Start}][-$_\u{200C}\u{200D}\p{ID_Continue}]*$/u;
const emptyOptions$3 = {};
function name(name2, options) {
  const settings = emptyOptions$3;
  const re2 = settings.jsx ? nameReJsx : nameRe;
  return re2.test(name2);
}
const re = /[ \t\n\f\r]/g;
function whitespace(thing) {
  return typeof thing === "object" ? thing.type === "text" ? empty$1(thing.value) : false : empty$1(thing);
}
function empty$1(value) {
  return value.replace(re, "") === "";
}
class Schema {
  /**
   * @param {SchemaType['property']} property
   *   Property.
   * @param {SchemaType['normal']} normal
   *   Normal.
   * @param {Space | undefined} [space]
   *   Space.
   * @returns
   *   Schema.
   */
  constructor(property, normal, space2) {
    this.normal = normal;
    this.property = property;
    if (space2) {
      this.space = space2;
    }
  }
}
Schema.prototype.normal = {};
Schema.prototype.property = {};
Schema.prototype.space = void 0;
function merge(definitions, space2) {
  const property = {};
  const normal = {};
  for (const definition2 of definitions) {
    Object.assign(property, definition2.property);
    Object.assign(normal, definition2.normal);
  }
  return new Schema(property, normal, space2);
}
function normalize$1(value) {
  return value.toLowerCase();
}
class Info {
  /**
   * @param {string} property
   *   Property.
   * @param {string} attribute
   *   Attribute.
   * @returns
   *   Info.
   */
  constructor(property, attribute) {
    this.attribute = attribute;
    this.property = property;
  }
}
Info.prototype.attribute = "";
Info.prototype.booleanish = false;
Info.prototype.boolean = false;
Info.prototype.commaOrSpaceSeparated = false;
Info.prototype.commaSeparated = false;
Info.prototype.defined = false;
Info.prototype.mustUseProperty = false;
Info.prototype.number = false;
Info.prototype.overloadedBoolean = false;
Info.prototype.property = "";
Info.prototype.spaceSeparated = false;
Info.prototype.space = void 0;
let powers = 0;
const boolean = increment();
const booleanish = increment();
const overloadedBoolean = increment();
const number = increment();
const spaceSeparated = increment();
const commaSeparated = increment();
const commaOrSpaceSeparated = increment();
function increment() {
  return 2 ** ++powers;
}
const types = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  boolean,
  booleanish,
  commaOrSpaceSeparated,
  commaSeparated,
  number,
  overloadedBoolean,
  spaceSeparated
}, Symbol.toStringTag, { value: "Module" }));
const checks = (
  /** @type {ReadonlyArray<keyof typeof types>} */
  Object.keys(types)
);
class DefinedInfo extends Info {
  /**
   * @constructor
   * @param {string} property
   *   Property.
   * @param {string} attribute
   *   Attribute.
   * @param {number | null | undefined} [mask]
   *   Mask.
   * @param {Space | undefined} [space]
   *   Space.
   * @returns
   *   Info.
   */
  constructor(property, attribute, mask, space2) {
    let index2 = -1;
    super(property, attribute);
    mark(this, "space", space2);
    if (typeof mask === "number") {
      while (++index2 < checks.length) {
        const check = checks[index2];
        mark(this, checks[index2], (mask & types[check]) === types[check]);
      }
    }
  }
}
DefinedInfo.prototype.defined = true;
function mark(values, key, value) {
  if (value) {
    values[key] = value;
  }
}
function create(definition2) {
  const properties = {};
  const normals = {};
  for (const [property, value] of Object.entries(definition2.properties)) {
    const info = new DefinedInfo(
      property,
      definition2.transform(definition2.attributes || {}, property),
      value,
      definition2.space
    );
    if (definition2.mustUseProperty && definition2.mustUseProperty.includes(property)) {
      info.mustUseProperty = true;
    }
    properties[property] = info;
    normals[normalize$1(property)] = property;
    normals[normalize$1(info.attribute)] = property;
  }
  return new Schema(properties, normals, definition2.space);
}
const aria = create({
  properties: {
    ariaActiveDescendant: null,
    ariaAtomic: booleanish,
    ariaAutoComplete: null,
    ariaBusy: booleanish,
    ariaChecked: booleanish,
    ariaColCount: number,
    ariaColIndex: number,
    ariaColSpan: number,
    ariaControls: spaceSeparated,
    ariaCurrent: null,
    ariaDescribedBy: spaceSeparated,
    ariaDetails: null,
    ariaDisabled: booleanish,
    ariaDropEffect: spaceSeparated,
    ariaErrorMessage: null,
    ariaExpanded: booleanish,
    ariaFlowTo: spaceSeparated,
    ariaGrabbed: booleanish,
    ariaHasPopup: null,
    ariaHidden: booleanish,
    ariaInvalid: null,
    ariaKeyShortcuts: null,
    ariaLabel: null,
    ariaLabelledBy: spaceSeparated,
    ariaLevel: number,
    ariaLive: null,
    ariaModal: booleanish,
    ariaMultiLine: booleanish,
    ariaMultiSelectable: booleanish,
    ariaOrientation: null,
    ariaOwns: spaceSeparated,
    ariaPlaceholder: null,
    ariaPosInSet: number,
    ariaPressed: booleanish,
    ariaReadOnly: booleanish,
    ariaRelevant: null,
    ariaRequired: booleanish,
    ariaRoleDescription: spaceSeparated,
    ariaRowCount: number,
    ariaRowIndex: number,
    ariaRowSpan: number,
    ariaSelected: booleanish,
    ariaSetSize: number,
    ariaSort: null,
    ariaValueMax: number,
    ariaValueMin: number,
    ariaValueNow: number,
    ariaValueText: null,
    role: null
  },
  transform(_, property) {
    return property === "role" ? property : "aria-" + property.slice(4).toLowerCase();
  }
});
function caseSensitiveTransform(attributes, attribute) {
  return attribute in attributes ? attributes[attribute] : attribute;
}
function caseInsensitiveTransform(attributes, property) {
  return caseSensitiveTransform(attributes, property.toLowerCase());
}
const html$3 = create({
  attributes: {
    acceptcharset: "accept-charset",
    classname: "class",
    htmlfor: "for",
    httpequiv: "http-equiv"
  },
  mustUseProperty: ["checked", "multiple", "muted", "selected"],
  properties: {
    // Standard Properties.
    abbr: null,
    accept: commaSeparated,
    acceptCharset: spaceSeparated,
    accessKey: spaceSeparated,
    action: null,
    allow: null,
    allowFullScreen: boolean,
    allowPaymentRequest: boolean,
    allowUserMedia: boolean,
    alt: null,
    as: null,
    async: boolean,
    autoCapitalize: null,
    autoComplete: spaceSeparated,
    autoFocus: boolean,
    autoPlay: boolean,
    blocking: spaceSeparated,
    capture: null,
    charSet: null,
    checked: boolean,
    cite: null,
    className: spaceSeparated,
    cols: number,
    colSpan: null,
    content: null,
    contentEditable: booleanish,
    controls: boolean,
    controlsList: spaceSeparated,
    coords: number | commaSeparated,
    crossOrigin: null,
    data: null,
    dateTime: null,
    decoding: null,
    default: boolean,
    defer: boolean,
    dir: null,
    dirName: null,
    disabled: boolean,
    download: overloadedBoolean,
    draggable: booleanish,
    encType: null,
    enterKeyHint: null,
    fetchPriority: null,
    form: null,
    formAction: null,
    formEncType: null,
    formMethod: null,
    formNoValidate: boolean,
    formTarget: null,
    headers: spaceSeparated,
    height: number,
    hidden: overloadedBoolean,
    high: number,
    href: null,
    hrefLang: null,
    htmlFor: spaceSeparated,
    httpEquiv: spaceSeparated,
    id: null,
    imageSizes: null,
    imageSrcSet: null,
    inert: boolean,
    inputMode: null,
    integrity: null,
    is: null,
    isMap: boolean,
    itemId: null,
    itemProp: spaceSeparated,
    itemRef: spaceSeparated,
    itemScope: boolean,
    itemType: spaceSeparated,
    kind: null,
    label: null,
    lang: null,
    language: null,
    list: null,
    loading: null,
    loop: boolean,
    low: number,
    manifest: null,
    max: null,
    maxLength: number,
    media: null,
    method: null,
    min: null,
    minLength: number,
    multiple: boolean,
    muted: boolean,
    name: null,
    nonce: null,
    noModule: boolean,
    noValidate: boolean,
    onAbort: null,
    onAfterPrint: null,
    onAuxClick: null,
    onBeforeMatch: null,
    onBeforePrint: null,
    onBeforeToggle: null,
    onBeforeUnload: null,
    onBlur: null,
    onCancel: null,
    onCanPlay: null,
    onCanPlayThrough: null,
    onChange: null,
    onClick: null,
    onClose: null,
    onContextLost: null,
    onContextMenu: null,
    onContextRestored: null,
    onCopy: null,
    onCueChange: null,
    onCut: null,
    onDblClick: null,
    onDrag: null,
    onDragEnd: null,
    onDragEnter: null,
    onDragExit: null,
    onDragLeave: null,
    onDragOver: null,
    onDragStart: null,
    onDrop: null,
    onDurationChange: null,
    onEmptied: null,
    onEnded: null,
    onError: null,
    onFocus: null,
    onFormData: null,
    onHashChange: null,
    onInput: null,
    onInvalid: null,
    onKeyDown: null,
    onKeyPress: null,
    onKeyUp: null,
    onLanguageChange: null,
    onLoad: null,
    onLoadedData: null,
    onLoadedMetadata: null,
    onLoadEnd: null,
    onLoadStart: null,
    onMessage: null,
    onMessageError: null,
    onMouseDown: null,
    onMouseEnter: null,
    onMouseLeave: null,
    onMouseMove: null,
    onMouseOut: null,
    onMouseOver: null,
    onMouseUp: null,
    onOffline: null,
    onOnline: null,
    onPageHide: null,
    onPageShow: null,
    onPaste: null,
    onPause: null,
    onPlay: null,
    onPlaying: null,
    onPopState: null,
    onProgress: null,
    onRateChange: null,
    onRejectionHandled: null,
    onReset: null,
    onResize: null,
    onScroll: null,
    onScrollEnd: null,
    onSecurityPolicyViolation: null,
    onSeeked: null,
    onSeeking: null,
    onSelect: null,
    onSlotChange: null,
    onStalled: null,
    onStorage: null,
    onSubmit: null,
    onSuspend: null,
    onTimeUpdate: null,
    onToggle: null,
    onUnhandledRejection: null,
    onUnload: null,
    onVolumeChange: null,
    onWaiting: null,
    onWheel: null,
    open: boolean,
    optimum: number,
    pattern: null,
    ping: spaceSeparated,
    placeholder: null,
    playsInline: boolean,
    popover: null,
    popoverTarget: null,
    popoverTargetAction: null,
    poster: null,
    preload: null,
    readOnly: boolean,
    referrerPolicy: null,
    rel: spaceSeparated,
    required: boolean,
    reversed: boolean,
    rows: number,
    rowSpan: number,
    sandbox: spaceSeparated,
    scope: null,
    scoped: boolean,
    seamless: boolean,
    selected: boolean,
    shadowRootClonable: boolean,
    shadowRootDelegatesFocus: boolean,
    shadowRootMode: null,
    shape: null,
    size: number,
    sizes: null,
    slot: null,
    span: number,
    spellCheck: booleanish,
    src: null,
    srcDoc: null,
    srcLang: null,
    srcSet: null,
    start: number,
    step: null,
    style: null,
    tabIndex: number,
    target: null,
    title: null,
    translate: null,
    type: null,
    typeMustMatch: boolean,
    useMap: null,
    value: booleanish,
    width: number,
    wrap: null,
    writingSuggestions: null,
    // Legacy.
    // See: https://html.spec.whatwg.org/#other-elements,-attributes-and-apis
    align: null,
    // Several. Use CSS `text-align` instead,
    aLink: null,
    // `<body>`. Use CSS `a:active {color}` instead
    archive: spaceSeparated,
    // `<object>`. List of URIs to archives
    axis: null,
    // `<td>` and `<th>`. Use `scope` on `<th>`
    background: null,
    // `<body>`. Use CSS `background-image` instead
    bgColor: null,
    // `<body>` and table elements. Use CSS `background-color` instead
    border: number,
    // `<table>`. Use CSS `border-width` instead,
    borderColor: null,
    // `<table>`. Use CSS `border-color` instead,
    bottomMargin: number,
    // `<body>`
    cellPadding: null,
    // `<table>`
    cellSpacing: null,
    // `<table>`
    char: null,
    // Several table elements. When `align=char`, sets the character to align on
    charOff: null,
    // Several table elements. When `char`, offsets the alignment
    classId: null,
    // `<object>`
    clear: null,
    // `<br>`. Use CSS `clear` instead
    code: null,
    // `<object>`
    codeBase: null,
    // `<object>`
    codeType: null,
    // `<object>`
    color: null,
    // `<font>` and `<hr>`. Use CSS instead
    compact: boolean,
    // Lists. Use CSS to reduce space between items instead
    declare: boolean,
    // `<object>`
    event: null,
    // `<script>`
    face: null,
    // `<font>`. Use CSS instead
    frame: null,
    // `<table>`
    frameBorder: null,
    // `<iframe>`. Use CSS `border` instead
    hSpace: number,
    // `<img>` and `<object>`
    leftMargin: number,
    // `<body>`
    link: null,
    // `<body>`. Use CSS `a:link {color: *}` instead
    longDesc: null,
    // `<frame>`, `<iframe>`, and `<img>`. Use an `<a>`
    lowSrc: null,
    // `<img>`. Use a `<picture>`
    marginHeight: number,
    // `<body>`
    marginWidth: number,
    // `<body>`
    noResize: boolean,
    // `<frame>`
    noHref: boolean,
    // `<area>`. Use no href instead of an explicit `nohref`
    noShade: boolean,
    // `<hr>`. Use background-color and height instead of borders
    noWrap: boolean,
    // `<td>` and `<th>`
    object: null,
    // `<applet>`
    profile: null,
    // `<head>`
    prompt: null,
    // `<isindex>`
    rev: null,
    // `<link>`
    rightMargin: number,
    // `<body>`
    rules: null,
    // `<table>`
    scheme: null,
    // `<meta>`
    scrolling: booleanish,
    // `<frame>`. Use overflow in the child context
    standby: null,
    // `<object>`
    summary: null,
    // `<table>`
    text: null,
    // `<body>`. Use CSS `color` instead
    topMargin: number,
    // `<body>`
    valueType: null,
    // `<param>`
    version: null,
    // `<html>`. Use a doctype.
    vAlign: null,
    // Several. Use CSS `vertical-align` instead
    vLink: null,
    // `<body>`. Use CSS `a:visited {color}` instead
    vSpace: number,
    // `<img>` and `<object>`
    // Non-standard Properties.
    allowTransparency: null,
    autoCorrect: null,
    autoSave: null,
    disablePictureInPicture: boolean,
    disableRemotePlayback: boolean,
    prefix: null,
    property: null,
    results: number,
    security: null,
    unselectable: null
  },
  space: "html",
  transform: caseInsensitiveTransform
});
const svg$1 = create({
  attributes: {
    accentHeight: "accent-height",
    alignmentBaseline: "alignment-baseline",
    arabicForm: "arabic-form",
    baselineShift: "baseline-shift",
    capHeight: "cap-height",
    className: "class",
    clipPath: "clip-path",
    clipRule: "clip-rule",
    colorInterpolation: "color-interpolation",
    colorInterpolationFilters: "color-interpolation-filters",
    colorProfile: "color-profile",
    colorRendering: "color-rendering",
    crossOrigin: "crossorigin",
    dataType: "datatype",
    dominantBaseline: "dominant-baseline",
    enableBackground: "enable-background",
    fillOpacity: "fill-opacity",
    fillRule: "fill-rule",
    floodColor: "flood-color",
    floodOpacity: "flood-opacity",
    fontFamily: "font-family",
    fontSize: "font-size",
    fontSizeAdjust: "font-size-adjust",
    fontStretch: "font-stretch",
    fontStyle: "font-style",
    fontVariant: "font-variant",
    fontWeight: "font-weight",
    glyphName: "glyph-name",
    glyphOrientationHorizontal: "glyph-orientation-horizontal",
    glyphOrientationVertical: "glyph-orientation-vertical",
    hrefLang: "hreflang",
    horizAdvX: "horiz-adv-x",
    horizOriginX: "horiz-origin-x",
    horizOriginY: "horiz-origin-y",
    imageRendering: "image-rendering",
    letterSpacing: "letter-spacing",
    lightingColor: "lighting-color",
    markerEnd: "marker-end",
    markerMid: "marker-mid",
    markerStart: "marker-start",
    navDown: "nav-down",
    navDownLeft: "nav-down-left",
    navDownRight: "nav-down-right",
    navLeft: "nav-left",
    navNext: "nav-next",
    navPrev: "nav-prev",
    navRight: "nav-right",
    navUp: "nav-up",
    navUpLeft: "nav-up-left",
    navUpRight: "nav-up-right",
    onAbort: "onabort",
    onActivate: "onactivate",
    onAfterPrint: "onafterprint",
    onBeforePrint: "onbeforeprint",
    onBegin: "onbegin",
    onCancel: "oncancel",
    onCanPlay: "oncanplay",
    onCanPlayThrough: "oncanplaythrough",
    onChange: "onchange",
    onClick: "onclick",
    onClose: "onclose",
    onCopy: "oncopy",
    onCueChange: "oncuechange",
    onCut: "oncut",
    onDblClick: "ondblclick",
    onDrag: "ondrag",
    onDragEnd: "ondragend",
    onDragEnter: "ondragenter",
    onDragExit: "ondragexit",
    onDragLeave: "ondragleave",
    onDragOver: "ondragover",
    onDragStart: "ondragstart",
    onDrop: "ondrop",
    onDurationChange: "ondurationchange",
    onEmptied: "onemptied",
    onEnd: "onend",
    onEnded: "onended",
    onError: "onerror",
    onFocus: "onfocus",
    onFocusIn: "onfocusin",
    onFocusOut: "onfocusout",
    onHashChange: "onhashchange",
    onInput: "oninput",
    onInvalid: "oninvalid",
    onKeyDown: "onkeydown",
    onKeyPress: "onkeypress",
    onKeyUp: "onkeyup",
    onLoad: "onload",
    onLoadedData: "onloadeddata",
    onLoadedMetadata: "onloadedmetadata",
    onLoadStart: "onloadstart",
    onMessage: "onmessage",
    onMouseDown: "onmousedown",
    onMouseEnter: "onmouseenter",
    onMouseLeave: "onmouseleave",
    onMouseMove: "onmousemove",
    onMouseOut: "onmouseout",
    onMouseOver: "onmouseover",
    onMouseUp: "onmouseup",
    onMouseWheel: "onmousewheel",
    onOffline: "onoffline",
    onOnline: "ononline",
    onPageHide: "onpagehide",
    onPageShow: "onpageshow",
    onPaste: "onpaste",
    onPause: "onpause",
    onPlay: "onplay",
    onPlaying: "onplaying",
    onPopState: "onpopstate",
    onProgress: "onprogress",
    onRateChange: "onratechange",
    onRepeat: "onrepeat",
    onReset: "onreset",
    onResize: "onresize",
    onScroll: "onscroll",
    onSeeked: "onseeked",
    onSeeking: "onseeking",
    onSelect: "onselect",
    onShow: "onshow",
    onStalled: "onstalled",
    onStorage: "onstorage",
    onSubmit: "onsubmit",
    onSuspend: "onsuspend",
    onTimeUpdate: "ontimeupdate",
    onToggle: "ontoggle",
    onUnload: "onunload",
    onVolumeChange: "onvolumechange",
    onWaiting: "onwaiting",
    onZoom: "onzoom",
    overlinePosition: "overline-position",
    overlineThickness: "overline-thickness",
    paintOrder: "paint-order",
    panose1: "panose-1",
    pointerEvents: "pointer-events",
    referrerPolicy: "referrerpolicy",
    renderingIntent: "rendering-intent",
    shapeRendering: "shape-rendering",
    stopColor: "stop-color",
    stopOpacity: "stop-opacity",
    strikethroughPosition: "strikethrough-position",
    strikethroughThickness: "strikethrough-thickness",
    strokeDashArray: "stroke-dasharray",
    strokeDashOffset: "stroke-dashoffset",
    strokeLineCap: "stroke-linecap",
    strokeLineJoin: "stroke-linejoin",
    strokeMiterLimit: "stroke-miterlimit",
    strokeOpacity: "stroke-opacity",
    strokeWidth: "stroke-width",
    tabIndex: "tabindex",
    textAnchor: "text-anchor",
    textDecoration: "text-decoration",
    textRendering: "text-rendering",
    transformOrigin: "transform-origin",
    typeOf: "typeof",
    underlinePosition: "underline-position",
    underlineThickness: "underline-thickness",
    unicodeBidi: "unicode-bidi",
    unicodeRange: "unicode-range",
    unitsPerEm: "units-per-em",
    vAlphabetic: "v-alphabetic",
    vHanging: "v-hanging",
    vIdeographic: "v-ideographic",
    vMathematical: "v-mathematical",
    vectorEffect: "vector-effect",
    vertAdvY: "vert-adv-y",
    vertOriginX: "vert-origin-x",
    vertOriginY: "vert-origin-y",
    wordSpacing: "word-spacing",
    writingMode: "writing-mode",
    xHeight: "x-height",
    // These were camelcased in Tiny. Now lowercased in SVG 2
    playbackOrder: "playbackorder",
    timelineBegin: "timelinebegin"
  },
  properties: {
    about: commaOrSpaceSeparated,
    accentHeight: number,
    accumulate: null,
    additive: null,
    alignmentBaseline: null,
    alphabetic: number,
    amplitude: number,
    arabicForm: null,
    ascent: number,
    attributeName: null,
    attributeType: null,
    azimuth: number,
    bandwidth: null,
    baselineShift: null,
    baseFrequency: null,
    baseProfile: null,
    bbox: null,
    begin: null,
    bias: number,
    by: null,
    calcMode: null,
    capHeight: number,
    className: spaceSeparated,
    clip: null,
    clipPath: null,
    clipPathUnits: null,
    clipRule: null,
    color: null,
    colorInterpolation: null,
    colorInterpolationFilters: null,
    colorProfile: null,
    colorRendering: null,
    content: null,
    contentScriptType: null,
    contentStyleType: null,
    crossOrigin: null,
    cursor: null,
    cx: null,
    cy: null,
    d: null,
    dataType: null,
    defaultAction: null,
    descent: number,
    diffuseConstant: number,
    direction: null,
    display: null,
    dur: null,
    divisor: number,
    dominantBaseline: null,
    download: boolean,
    dx: null,
    dy: null,
    edgeMode: null,
    editable: null,
    elevation: number,
    enableBackground: null,
    end: null,
    event: null,
    exponent: number,
    externalResourcesRequired: null,
    fill: null,
    fillOpacity: number,
    fillRule: null,
    filter: null,
    filterRes: null,
    filterUnits: null,
    floodColor: null,
    floodOpacity: null,
    focusable: null,
    focusHighlight: null,
    fontFamily: null,
    fontSize: null,
    fontSizeAdjust: null,
    fontStretch: null,
    fontStyle: null,
    fontVariant: null,
    fontWeight: null,
    format: null,
    fr: null,
    from: null,
    fx: null,
    fy: null,
    g1: commaSeparated,
    g2: commaSeparated,
    glyphName: commaSeparated,
    glyphOrientationHorizontal: null,
    glyphOrientationVertical: null,
    glyphRef: null,
    gradientTransform: null,
    gradientUnits: null,
    handler: null,
    hanging: number,
    hatchContentUnits: null,
    hatchUnits: null,
    height: null,
    href: null,
    hrefLang: null,
    horizAdvX: number,
    horizOriginX: number,
    horizOriginY: number,
    id: null,
    ideographic: number,
    imageRendering: null,
    initialVisibility: null,
    in: null,
    in2: null,
    intercept: number,
    k: number,
    k1: number,
    k2: number,
    k3: number,
    k4: number,
    kernelMatrix: commaOrSpaceSeparated,
    kernelUnitLength: null,
    keyPoints: null,
    // SEMI_COLON_SEPARATED
    keySplines: null,
    // SEMI_COLON_SEPARATED
    keyTimes: null,
    // SEMI_COLON_SEPARATED
    kerning: null,
    lang: null,
    lengthAdjust: null,
    letterSpacing: null,
    lightingColor: null,
    limitingConeAngle: number,
    local: null,
    markerEnd: null,
    markerMid: null,
    markerStart: null,
    markerHeight: null,
    markerUnits: null,
    markerWidth: null,
    mask: null,
    maskContentUnits: null,
    maskUnits: null,
    mathematical: null,
    max: null,
    media: null,
    mediaCharacterEncoding: null,
    mediaContentEncodings: null,
    mediaSize: number,
    mediaTime: null,
    method: null,
    min: null,
    mode: null,
    name: null,
    navDown: null,
    navDownLeft: null,
    navDownRight: null,
    navLeft: null,
    navNext: null,
    navPrev: null,
    navRight: null,
    navUp: null,
    navUpLeft: null,
    navUpRight: null,
    numOctaves: null,
    observer: null,
    offset: null,
    onAbort: null,
    onActivate: null,
    onAfterPrint: null,
    onBeforePrint: null,
    onBegin: null,
    onCancel: null,
    onCanPlay: null,
    onCanPlayThrough: null,
    onChange: null,
    onClick: null,
    onClose: null,
    onCopy: null,
    onCueChange: null,
    onCut: null,
    onDblClick: null,
    onDrag: null,
    onDragEnd: null,
    onDragEnter: null,
    onDragExit: null,
    onDragLeave: null,
    onDragOver: null,
    onDragStart: null,
    onDrop: null,
    onDurationChange: null,
    onEmptied: null,
    onEnd: null,
    onEnded: null,
    onError: null,
    onFocus: null,
    onFocusIn: null,
    onFocusOut: null,
    onHashChange: null,
    onInput: null,
    onInvalid: null,
    onKeyDown: null,
    onKeyPress: null,
    onKeyUp: null,
    onLoad: null,
    onLoadedData: null,
    onLoadedMetadata: null,
    onLoadStart: null,
    onMessage: null,
    onMouseDown: null,
    onMouseEnter: null,
    onMouseLeave: null,
    onMouseMove: null,
    onMouseOut: null,
    onMouseOver: null,
    onMouseUp: null,
    onMouseWheel: null,
    onOffline: null,
    onOnline: null,
    onPageHide: null,
    onPageShow: null,
    onPaste: null,
    onPause: null,
    onPlay: null,
    onPlaying: null,
    onPopState: null,
    onProgress: null,
    onRateChange: null,
    onRepeat: null,
    onReset: null,
    onResize: null,
    onScroll: null,
    onSeeked: null,
    onSeeking: null,
    onSelect: null,
    onShow: null,
    onStalled: null,
    onStorage: null,
    onSubmit: null,
    onSuspend: null,
    onTimeUpdate: null,
    onToggle: null,
    onUnload: null,
    onVolumeChange: null,
    onWaiting: null,
    onZoom: null,
    opacity: null,
    operator: null,
    order: null,
    orient: null,
    orientation: null,
    origin: null,
    overflow: null,
    overlay: null,
    overlinePosition: number,
    overlineThickness: number,
    paintOrder: null,
    panose1: null,
    path: null,
    pathLength: number,
    patternContentUnits: null,
    patternTransform: null,
    patternUnits: null,
    phase: null,
    ping: spaceSeparated,
    pitch: null,
    playbackOrder: null,
    pointerEvents: null,
    points: null,
    pointsAtX: number,
    pointsAtY: number,
    pointsAtZ: number,
    preserveAlpha: null,
    preserveAspectRatio: null,
    primitiveUnits: null,
    propagate: null,
    property: commaOrSpaceSeparated,
    r: null,
    radius: null,
    referrerPolicy: null,
    refX: null,
    refY: null,
    rel: commaOrSpaceSeparated,
    rev: commaOrSpaceSeparated,
    renderingIntent: null,
    repeatCount: null,
    repeatDur: null,
    requiredExtensions: commaOrSpaceSeparated,
    requiredFeatures: commaOrSpaceSeparated,
    requiredFonts: commaOrSpaceSeparated,
    requiredFormats: commaOrSpaceSeparated,
    resource: null,
    restart: null,
    result: null,
    rotate: null,
    rx: null,
    ry: null,
    scale: null,
    seed: null,
    shapeRendering: null,
    side: null,
    slope: null,
    snapshotTime: null,
    specularConstant: number,
    specularExponent: number,
    spreadMethod: null,
    spacing: null,
    startOffset: null,
    stdDeviation: null,
    stemh: null,
    stemv: null,
    stitchTiles: null,
    stopColor: null,
    stopOpacity: null,
    strikethroughPosition: number,
    strikethroughThickness: number,
    string: null,
    stroke: null,
    strokeDashArray: commaOrSpaceSeparated,
    strokeDashOffset: null,
    strokeLineCap: null,
    strokeLineJoin: null,
    strokeMiterLimit: number,
    strokeOpacity: number,
    strokeWidth: null,
    style: null,
    surfaceScale: number,
    syncBehavior: null,
    syncBehaviorDefault: null,
    syncMaster: null,
    syncTolerance: null,
    syncToleranceDefault: null,
    systemLanguage: commaOrSpaceSeparated,
    tabIndex: number,
    tableValues: null,
    target: null,
    targetX: number,
    targetY: number,
    textAnchor: null,
    textDecoration: null,
    textRendering: null,
    textLength: null,
    timelineBegin: null,
    title: null,
    transformBehavior: null,
    type: null,
    typeOf: commaOrSpaceSeparated,
    to: null,
    transform: null,
    transformOrigin: null,
    u1: null,
    u2: null,
    underlinePosition: number,
    underlineThickness: number,
    unicode: null,
    unicodeBidi: null,
    unicodeRange: null,
    unitsPerEm: number,
    values: null,
    vAlphabetic: number,
    vMathematical: number,
    vectorEffect: null,
    vHanging: number,
    vIdeographic: number,
    version: null,
    vertAdvY: number,
    vertOriginX: number,
    vertOriginY: number,
    viewBox: null,
    viewTarget: null,
    visibility: null,
    width: null,
    widths: null,
    wordSpacing: null,
    writingMode: null,
    x: null,
    x1: null,
    x2: null,
    xChannelSelector: null,
    xHeight: number,
    y: null,
    y1: null,
    y2: null,
    yChannelSelector: null,
    z: null,
    zoomAndPan: null
  },
  space: "svg",
  transform: caseSensitiveTransform
});
const xlink = create({
  properties: {
    xLinkActuate: null,
    xLinkArcRole: null,
    xLinkHref: null,
    xLinkRole: null,
    xLinkShow: null,
    xLinkTitle: null,
    xLinkType: null
  },
  space: "xlink",
  transform(_, property) {
    return "xlink:" + property.slice(5).toLowerCase();
  }
});
const xmlns = create({
  attributes: { xmlnsxlink: "xmlns:xlink" },
  properties: { xmlnsXLink: null, xmlns: null },
  space: "xmlns",
  transform: caseInsensitiveTransform
});
const xml = create({
  properties: { xmlBase: null, xmlLang: null, xmlSpace: null },
  space: "xml",
  transform(_, property) {
    return "xml:" + property.slice(3).toLowerCase();
  }
});
const hastToReact = {
  classId: "classID",
  dataType: "datatype",
  itemId: "itemID",
  strokeDashArray: "strokeDasharray",
  strokeDashOffset: "strokeDashoffset",
  strokeLineCap: "strokeLinecap",
  strokeLineJoin: "strokeLinejoin",
  strokeMiterLimit: "strokeMiterlimit",
  typeOf: "typeof",
  xLinkActuate: "xlinkActuate",
  xLinkArcRole: "xlinkArcrole",
  xLinkHref: "xlinkHref",
  xLinkRole: "xlinkRole",
  xLinkShow: "xlinkShow",
  xLinkTitle: "xlinkTitle",
  xLinkType: "xlinkType",
  xmlnsXLink: "xmlnsXlink"
};
const cap$1 = /[A-Z]/g;
const dash = /-[a-z]/g;
const valid = /^data[-\w.:]+$/i;
function find(schema, value) {
  const normal = normalize$1(value);
  let property = value;
  let Type = Info;
  if (normal in schema.normal) {
    return schema.property[schema.normal[normal]];
  }
  if (normal.length > 4 && normal.slice(0, 4) === "data" && valid.test(value)) {
    if (value.charAt(4) === "-") {
      const rest = value.slice(5).replace(dash, camelcase);
      property = "data" + rest.charAt(0).toUpperCase() + rest.slice(1);
    } else {
      const rest = value.slice(4);
      if (!dash.test(rest)) {
        let dashes = rest.replace(cap$1, kebab);
        if (dashes.charAt(0) !== "-") {
          dashes = "-" + dashes;
        }
        value = "data" + dashes;
      }
    }
    Type = DefinedInfo;
  }
  return new Type(property, value);
}
function kebab($0) {
  return "-" + $0.toLowerCase();
}
function camelcase($0) {
  return $0.charAt(1).toUpperCase();
}
const html$2 = merge([aria, html$3, xlink, xmlns, xml], "html");
const svg = merge([aria, svg$1, xlink, xmlns, xml], "svg");
function stringify(values) {
  return values.join(" ").trim();
}
var cjs$2 = {};
var cjs$1;
var hasRequiredCjs$2;
function requireCjs$2() {
  if (hasRequiredCjs$2) return cjs$1;
  hasRequiredCjs$2 = 1;
  var COMMENT_REGEX = /\/\*[^*]*\*+([^/*][^*]*\*+)*\//g;
  var NEWLINE_REGEX = /\n/g;
  var WHITESPACE_REGEX = /^\s*/;
  var PROPERTY_REGEX = /^(\*?[-#/*\\\w]+(\[[0-9a-z_-]+\])?)\s*/;
  var COLON_REGEX = /^:\s*/;
  var VALUE_REGEX = /^((?:'(?:\\'|.)*?'|"(?:\\"|.)*?"|\([^)]*?\)|[^};])+)/;
  var SEMICOLON_REGEX = /^[;\s]*/;
  var TRIM_REGEX = /^\s+|\s+$/g;
  var NEWLINE = "\n";
  var FORWARD_SLASH = "/";
  var ASTERISK = "*";
  var EMPTY_STRING = "";
  var TYPE_COMMENT = "comment";
  var TYPE_DECLARATION = "declaration";
  function index2(style, options) {
    if (typeof style !== "string") {
      throw new TypeError("First argument must be a string");
    }
    if (!style) return [];
    options = options || {};
    var lineno = 1;
    var column = 1;
    function updatePosition(str) {
      var lines = str.match(NEWLINE_REGEX);
      if (lines) lineno += lines.length;
      var i = str.lastIndexOf(NEWLINE);
      column = ~i ? str.length - i : column + str.length;
    }
    function position2() {
      var start = { line: lineno, column };
      return function(node2) {
        node2.position = new Position(start);
        whitespace2();
        return node2;
      };
    }
    function Position(start) {
      this.start = start;
      this.end = { line: lineno, column };
      this.source = options.source;
    }
    Position.prototype.content = style;
    function error(msg) {
      var err = new Error(
        options.source + ":" + lineno + ":" + column + ": " + msg
      );
      err.reason = msg;
      err.filename = options.source;
      err.line = lineno;
      err.column = column;
      err.source = style;
      if (options.silent) ;
      else {
        throw err;
      }
    }
    function match(re2) {
      var m = re2.exec(style);
      if (!m) return;
      var str = m[0];
      updatePosition(str);
      style = style.slice(str.length);
      return m;
    }
    function whitespace2() {
      match(WHITESPACE_REGEX);
    }
    function comments(rules) {
      var c;
      rules = rules || [];
      while (c = comment()) {
        if (c !== false) {
          rules.push(c);
        }
      }
      return rules;
    }
    function comment() {
      var pos = position2();
      if (FORWARD_SLASH != style.charAt(0) || ASTERISK != style.charAt(1)) return;
      var i = 2;
      while (EMPTY_STRING != style.charAt(i) && (ASTERISK != style.charAt(i) || FORWARD_SLASH != style.charAt(i + 1))) {
        ++i;
      }
      i += 2;
      if (EMPTY_STRING === style.charAt(i - 1)) {
        return error("End of comment missing");
      }
      var str = style.slice(2, i - 2);
      column += 2;
      updatePosition(str);
      style = style.slice(i);
      column += 2;
      return pos({
        type: TYPE_COMMENT,
        comment: str
      });
    }
    function declaration() {
      var pos = position2();
      var prop = match(PROPERTY_REGEX);
      if (!prop) return;
      comment();
      if (!match(COLON_REGEX)) return error("property missing ':'");
      var val = match(VALUE_REGEX);
      var ret = pos({
        type: TYPE_DECLARATION,
        property: trim(prop[0].replace(COMMENT_REGEX, EMPTY_STRING)),
        value: val ? trim(val[0].replace(COMMENT_REGEX, EMPTY_STRING)) : EMPTY_STRING
      });
      match(SEMICOLON_REGEX);
      return ret;
    }
    function declarations() {
      var decls = [];
      comments(decls);
      var decl;
      while (decl = declaration()) {
        if (decl !== false) {
          decls.push(decl);
          comments(decls);
        }
      }
      return decls;
    }
    whitespace2();
    return declarations();
  }
  function trim(str) {
    return str ? str.replace(TRIM_REGEX, EMPTY_STRING) : EMPTY_STRING;
  }
  cjs$1 = index2;
  return cjs$1;
}
var hasRequiredCjs$1;
function requireCjs$1() {
  if (hasRequiredCjs$1) return cjs$2;
  hasRequiredCjs$1 = 1;
  var __importDefault = cjs$2 && cjs$2.__importDefault || function(mod) {
    return mod && mod.__esModule ? mod : { "default": mod };
  };
  Object.defineProperty(cjs$2, "__esModule", { value: true });
  cjs$2.default = StyleToObject;
  const inline_style_parser_1 = __importDefault(requireCjs$2());
  function StyleToObject(style, iterator) {
    let styleObject = null;
    if (!style || typeof style !== "string") {
      return styleObject;
    }
    const declarations = (0, inline_style_parser_1.default)(style);
    const hasIterator = typeof iterator === "function";
    declarations.forEach((declaration) => {
      if (declaration.type !== "declaration") {
        return;
      }
      const { property, value } = declaration;
      if (hasIterator) {
        iterator(property, value, declaration);
      } else if (value) {
        styleObject = styleObject || {};
        styleObject[property] = value;
      }
    });
    return styleObject;
  }
  return cjs$2;
}
var utilities = {};
var hasRequiredUtilities;
function requireUtilities() {
  if (hasRequiredUtilities) return utilities;
  hasRequiredUtilities = 1;
  Object.defineProperty(utilities, "__esModule", { value: true });
  utilities.camelCase = void 0;
  var CUSTOM_PROPERTY_REGEX = /^--[a-zA-Z0-9_-]+$/;
  var HYPHEN_REGEX = /-([a-z])/g;
  var NO_HYPHEN_REGEX = /^[^-]+$/;
  var VENDOR_PREFIX_REGEX = /^-(webkit|moz|ms|o|khtml)-/;
  var MS_VENDOR_PREFIX_REGEX = /^-(ms)-/;
  var skipCamelCase = function(property) {
    return !property || NO_HYPHEN_REGEX.test(property) || CUSTOM_PROPERTY_REGEX.test(property);
  };
  var capitalize = function(match, character) {
    return character.toUpperCase();
  };
  var trimHyphen = function(match, prefix) {
    return "".concat(prefix, "-");
  };
  var camelCase = function(property, options) {
    if (options === void 0) {
      options = {};
    }
    if (skipCamelCase(property)) {
      return property;
    }
    property = property.toLowerCase();
    if (options.reactCompat) {
      property = property.replace(MS_VENDOR_PREFIX_REGEX, trimHyphen);
    } else {
      property = property.replace(VENDOR_PREFIX_REGEX, trimHyphen);
    }
    return property.replace(HYPHEN_REGEX, capitalize);
  };
  utilities.camelCase = camelCase;
  return utilities;
}
var cjs;
var hasRequiredCjs;
function requireCjs() {
  if (hasRequiredCjs) return cjs;
  hasRequiredCjs = 1;
  var __importDefault = cjs && cjs.__importDefault || function(mod) {
    return mod && mod.__esModule ? mod : { "default": mod };
  };
  var style_to_object_1 = __importDefault(requireCjs$1());
  var utilities_1 = requireUtilities();
  function StyleToJS(style, options) {
    var output = {};
    if (!style || typeof style !== "string") {
      return output;
    }
    (0, style_to_object_1.default)(style, function(property, value) {
      if (property && value) {
        output[(0, utilities_1.camelCase)(property, options)] = value;
      }
    });
    return output;
  }
  StyleToJS.default = StyleToJS;
  cjs = StyleToJS;
  return cjs;
}
var cjsExports = requireCjs();
const styleToJs = /* @__PURE__ */ getDefaultExportFromCjs(cjsExports);
const pointEnd = point$2("end");
const pointStart = point$2("start");
function point$2(type) {
  return point2;
  function point2(node2) {
    const point3 = node2 && node2.position && node2.position[type] || {};
    if (typeof point3.line === "number" && point3.line > 0 && typeof point3.column === "number" && point3.column > 0) {
      return {
        line: point3.line,
        column: point3.column,
        offset: typeof point3.offset === "number" && point3.offset > -1 ? point3.offset : void 0
      };
    }
  }
}
function position$1(node2) {
  const start = pointStart(node2);
  const end = pointEnd(node2);
  if (start && end) {
    return { start, end };
  }
}
function stringifyPosition(value) {
  if (!value || typeof value !== "object") {
    return "";
  }
  if ("position" in value || "type" in value) {
    return position(value.position);
  }
  if ("start" in value || "end" in value) {
    return position(value);
  }
  if ("line" in value || "column" in value) {
    return point$1(value);
  }
  return "";
}
function point$1(point2) {
  return index$1(point2 && point2.line) + ":" + index$1(point2 && point2.column);
}
function position(pos) {
  return point$1(pos && pos.start) + "-" + point$1(pos && pos.end);
}
function index$1(value) {
  return value && typeof value === "number" ? value : 1;
}
class VFileMessage extends Error {
  /**
   * Create a message for `reason`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {Options | null | undefined} [options]
   * @returns
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | Options | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns
   *   Instance of `VFileMessage`.
   */
  // eslint-disable-next-line complexity
  constructor(causeOrReason, optionsOrParentOrPlace, origin) {
    super();
    if (typeof optionsOrParentOrPlace === "string") {
      origin = optionsOrParentOrPlace;
      optionsOrParentOrPlace = void 0;
    }
    let reason = "";
    let options = {};
    let legacyCause = false;
    if (optionsOrParentOrPlace) {
      if ("line" in optionsOrParentOrPlace && "column" in optionsOrParentOrPlace) {
        options = { place: optionsOrParentOrPlace };
      } else if ("start" in optionsOrParentOrPlace && "end" in optionsOrParentOrPlace) {
        options = { place: optionsOrParentOrPlace };
      } else if ("type" in optionsOrParentOrPlace) {
        options = {
          ancestors: [optionsOrParentOrPlace],
          place: optionsOrParentOrPlace.position
        };
      } else {
        options = { ...optionsOrParentOrPlace };
      }
    }
    if (typeof causeOrReason === "string") {
      reason = causeOrReason;
    } else if (!options.cause && causeOrReason) {
      legacyCause = true;
      reason = causeOrReason.message;
      options.cause = causeOrReason;
    }
    if (!options.ruleId && !options.source && typeof origin === "string") {
      const index2 = origin.indexOf(":");
      if (index2 === -1) {
        options.ruleId = origin;
      } else {
        options.source = origin.slice(0, index2);
        options.ruleId = origin.slice(index2 + 1);
      }
    }
    if (!options.place && options.ancestors && options.ancestors) {
      const parent = options.ancestors[options.ancestors.length - 1];
      if (parent) {
        options.place = parent.position;
      }
    }
    const start = options.place && "start" in options.place ? options.place.start : options.place;
    this.ancestors = options.ancestors || void 0;
    this.cause = options.cause || void 0;
    this.column = start ? start.column : void 0;
    this.fatal = void 0;
    this.file = "";
    this.message = reason;
    this.line = start ? start.line : void 0;
    this.name = stringifyPosition(options.place) || "1:1";
    this.place = options.place || void 0;
    this.reason = this.message;
    this.ruleId = options.ruleId || void 0;
    this.source = options.source || void 0;
    this.stack = legacyCause && options.cause && typeof options.cause.stack === "string" ? options.cause.stack : "";
    this.actual = void 0;
    this.expected = void 0;
    this.note = void 0;
    this.url = void 0;
  }
}
VFileMessage.prototype.file = "";
VFileMessage.prototype.name = "";
VFileMessage.prototype.reason = "";
VFileMessage.prototype.message = "";
VFileMessage.prototype.stack = "";
VFileMessage.prototype.column = void 0;
VFileMessage.prototype.line = void 0;
VFileMessage.prototype.ancestors = void 0;
VFileMessage.prototype.cause = void 0;
VFileMessage.prototype.fatal = void 0;
VFileMessage.prototype.place = void 0;
VFileMessage.prototype.ruleId = void 0;
VFileMessage.prototype.source = void 0;
const own$3 = {}.hasOwnProperty;
const emptyMap = /* @__PURE__ */ new Map();
const cap = /[A-Z]/g;
const tableElements = /* @__PURE__ */ new Set(["table", "tbody", "thead", "tfoot", "tr"]);
const tableCellElement = /* @__PURE__ */ new Set(["td", "th"]);
const docs = "https://github.com/syntax-tree/hast-util-to-jsx-runtime";
function toJsxRuntime(tree, options) {
  if (!options || options.Fragment === void 0) {
    throw new TypeError("Expected `Fragment` in options");
  }
  const filePath = options.filePath || void 0;
  let create2;
  if (options.development) {
    if (typeof options.jsxDEV !== "function") {
      throw new TypeError(
        "Expected `jsxDEV` in options when `development: true`"
      );
    }
    create2 = developmentCreate(filePath, options.jsxDEV);
  } else {
    if (typeof options.jsx !== "function") {
      throw new TypeError("Expected `jsx` in production options");
    }
    if (typeof options.jsxs !== "function") {
      throw new TypeError("Expected `jsxs` in production options");
    }
    create2 = productionCreate(filePath, options.jsx, options.jsxs);
  }
  const state = {
    Fragment: options.Fragment,
    ancestors: [],
    components: options.components || {},
    create: create2,
    elementAttributeNameCase: options.elementAttributeNameCase || "react",
    evaluater: options.createEvaluater ? options.createEvaluater() : void 0,
    filePath,
    ignoreInvalidStyle: options.ignoreInvalidStyle || false,
    passKeys: options.passKeys !== false,
    passNode: options.passNode || false,
    schema: options.space === "svg" ? svg : html$2,
    stylePropertyNameCase: options.stylePropertyNameCase || "dom",
    tableCellAlignToStyle: options.tableCellAlignToStyle !== false
  };
  const result = one$1(state, tree, void 0);
  if (result && typeof result !== "string") {
    return result;
  }
  return state.create(
    tree,
    state.Fragment,
    { children: result || void 0 },
    void 0
  );
}
function one$1(state, node2, key) {
  if (node2.type === "element") {
    return element$1(state, node2, key);
  }
  if (node2.type === "mdxFlowExpression" || node2.type === "mdxTextExpression") {
    return mdxExpression(state, node2);
  }
  if (node2.type === "mdxJsxFlowElement" || node2.type === "mdxJsxTextElement") {
    return mdxJsxElement(state, node2, key);
  }
  if (node2.type === "mdxjsEsm") {
    return mdxEsm(state, node2);
  }
  if (node2.type === "root") {
    return root$2(state, node2, key);
  }
  if (node2.type === "text") {
    return text$5(state, node2);
  }
}
function element$1(state, node2, key) {
  const parentSchema = state.schema;
  let schema = parentSchema;
  if (node2.tagName.toLowerCase() === "svg" && parentSchema.space === "html") {
    schema = svg;
    state.schema = schema;
  }
  state.ancestors.push(node2);
  const type = findComponentFromName(state, node2.tagName, false);
  const props = createElementProps(state, node2);
  let children = createChildren(state, node2);
  if (tableElements.has(node2.tagName)) {
    children = children.filter(function(child) {
      return typeof child === "string" ? !whitespace(child) : true;
    });
  }
  addNode(state, props, type, node2);
  addChildren(props, children);
  state.ancestors.pop();
  state.schema = parentSchema;
  return state.create(node2, type, props, key);
}
function mdxExpression(state, node2) {
  if (node2.data && node2.data.estree && state.evaluater) {
    const program = node2.data.estree;
    const expression = program.body[0];
    ok$1(expression.type === "ExpressionStatement");
    return (
      /** @type {Child | undefined} */
      state.evaluater.evaluateExpression(expression.expression)
    );
  }
  crashEstree(state, node2.position);
}
function mdxEsm(state, node2) {
  if (node2.data && node2.data.estree && state.evaluater) {
    return (
      /** @type {Child | undefined} */
      state.evaluater.evaluateProgram(node2.data.estree)
    );
  }
  crashEstree(state, node2.position);
}
function mdxJsxElement(state, node2, key) {
  const parentSchema = state.schema;
  let schema = parentSchema;
  if (node2.name === "svg" && parentSchema.space === "html") {
    schema = svg;
    state.schema = schema;
  }
  state.ancestors.push(node2);
  const type = node2.name === null ? state.Fragment : findComponentFromName(state, node2.name, true);
  const props = createJsxElementProps(state, node2);
  const children = createChildren(state, node2);
  addNode(state, props, type, node2);
  addChildren(props, children);
  state.ancestors.pop();
  state.schema = parentSchema;
  return state.create(node2, type, props, key);
}
function root$2(state, node2, key) {
  const props = {};
  addChildren(props, createChildren(state, node2));
  return state.create(node2, state.Fragment, props, key);
}
function text$5(_, node2) {
  return node2.value;
}
function addNode(state, props, type, node2) {
  if (typeof type !== "string" && type !== state.Fragment && state.passNode) {
    props.node = node2;
  }
}
function addChildren(props, children) {
  if (children.length > 0) {
    const value = children.length > 1 ? children : children[0];
    if (value) {
      props.children = value;
    }
  }
}
function productionCreate(_, jsx, jsxs) {
  return create2;
  function create2(_2, type, props, key) {
    const isStaticChildren = Array.isArray(props.children);
    const fn = isStaticChildren ? jsxs : jsx;
    return key ? fn(type, props, key) : fn(type, props);
  }
}
function developmentCreate(filePath, jsxDEV) {
  return create2;
  function create2(node2, type, props, key) {
    const isStaticChildren = Array.isArray(props.children);
    const point2 = pointStart(node2);
    return jsxDEV(
      type,
      props,
      key,
      isStaticChildren,
      {
        columnNumber: point2 ? point2.column - 1 : void 0,
        fileName: filePath,
        lineNumber: point2 ? point2.line : void 0
      },
      void 0
    );
  }
}
function createElementProps(state, node2) {
  const props = {};
  let alignValue;
  let prop;
  for (prop in node2.properties) {
    if (prop !== "children" && own$3.call(node2.properties, prop)) {
      const result = createProperty(state, prop, node2.properties[prop]);
      if (result) {
        const [key, value] = result;
        if (state.tableCellAlignToStyle && key === "align" && typeof value === "string" && tableCellElement.has(node2.tagName)) {
          alignValue = value;
        } else {
          props[key] = value;
        }
      }
    }
  }
  if (alignValue) {
    const style = (
      /** @type {Style} */
      props.style || (props.style = {})
    );
    style[state.stylePropertyNameCase === "css" ? "text-align" : "textAlign"] = alignValue;
  }
  return props;
}
function createJsxElementProps(state, node2) {
  const props = {};
  for (const attribute of node2.attributes) {
    if (attribute.type === "mdxJsxExpressionAttribute") {
      if (attribute.data && attribute.data.estree && state.evaluater) {
        const program = attribute.data.estree;
        const expression = program.body[0];
        ok$1(expression.type === "ExpressionStatement");
        const objectExpression = expression.expression;
        ok$1(objectExpression.type === "ObjectExpression");
        const property = objectExpression.properties[0];
        ok$1(property.type === "SpreadElement");
        Object.assign(
          props,
          state.evaluater.evaluateExpression(property.argument)
        );
      } else {
        crashEstree(state, node2.position);
      }
    } else {
      const name2 = attribute.name;
      let value;
      if (attribute.value && typeof attribute.value === "object") {
        if (attribute.value.data && attribute.value.data.estree && state.evaluater) {
          const program = attribute.value.data.estree;
          const expression = program.body[0];
          ok$1(expression.type === "ExpressionStatement");
          value = state.evaluater.evaluateExpression(expression.expression);
        } else {
          crashEstree(state, node2.position);
        }
      } else {
        value = attribute.value === null ? true : attribute.value;
      }
      props[name2] = /** @type {Props[keyof Props]} */
      value;
    }
  }
  return props;
}
function createChildren(state, node2) {
  const children = [];
  let index2 = -1;
  const countsByName = state.passKeys ? /* @__PURE__ */ new Map() : emptyMap;
  while (++index2 < node2.children.length) {
    const child = node2.children[index2];
    let key;
    if (state.passKeys) {
      const name2 = child.type === "element" ? child.tagName : child.type === "mdxJsxFlowElement" || child.type === "mdxJsxTextElement" ? child.name : void 0;
      if (name2) {
        const count2 = countsByName.get(name2) || 0;
        key = name2 + "-" + count2;
        countsByName.set(name2, count2 + 1);
      }
    }
    const result = one$1(state, child, key);
    if (result !== void 0) children.push(result);
  }
  return children;
}
function createProperty(state, prop, value) {
  const info = find(state.schema, prop);
  if (value === null || value === void 0 || typeof value === "number" && Number.isNaN(value)) {
    return;
  }
  if (Array.isArray(value)) {
    value = info.commaSeparated ? stringify$1(value) : stringify(value);
  }
  if (info.property === "style") {
    let styleObject = typeof value === "object" ? value : parseStyle(state, String(value));
    if (state.stylePropertyNameCase === "css") {
      styleObject = transformStylesToCssCasing(styleObject);
    }
    return ["style", styleObject];
  }
  return [
    state.elementAttributeNameCase === "react" && info.space ? hastToReact[info.property] || info.property : info.attribute,
    value
  ];
}
function parseStyle(state, value) {
  try {
    return styleToJs(value, { reactCompat: true });
  } catch (error) {
    if (state.ignoreInvalidStyle) {
      return {};
    }
    const cause = (
      /** @type {Error} */
      error
    );
    const message = new VFileMessage("Cannot parse `style` attribute", {
      ancestors: state.ancestors,
      cause,
      ruleId: "style",
      source: "hast-util-to-jsx-runtime"
    });
    message.file = state.filePath || void 0;
    message.url = docs + "#cannot-parse-style-attribute";
    throw message;
  }
}
function findComponentFromName(state, name$1, allowExpression) {
  let result;
  if (!allowExpression) {
    result = { type: "Literal", value: name$1 };
  } else if (name$1.includes(".")) {
    const identifiers = name$1.split(".");
    let index2 = -1;
    let node2;
    while (++index2 < identifiers.length) {
      const prop = name(identifiers[index2]) ? { type: "Identifier", name: identifiers[index2] } : { type: "Literal", value: identifiers[index2] };
      node2 = node2 ? {
        type: "MemberExpression",
        object: node2,
        property: prop,
        computed: Boolean(index2 && prop.type === "Literal"),
        optional: false
      } : prop;
    }
    result = node2;
  } else {
    result = name(name$1) && !/^[a-z]/.test(name$1) ? { type: "Identifier", name: name$1 } : { type: "Literal", value: name$1 };
  }
  if (result.type === "Literal") {
    const name2 = (
      /** @type {string | number} */
      result.value
    );
    return own$3.call(state.components, name2) ? state.components[name2] : name2;
  }
  if (state.evaluater) {
    return state.evaluater.evaluateExpression(result);
  }
  crashEstree(state);
}
function crashEstree(state, place) {
  const message = new VFileMessage(
    "Cannot handle MDX estrees without `createEvaluater`",
    {
      ancestors: state.ancestors,
      place,
      ruleId: "mdx-estree",
      source: "hast-util-to-jsx-runtime"
    }
  );
  message.file = state.filePath || void 0;
  message.url = docs + "#cannot-handle-mdx-estrees-without-createevaluater";
  throw message;
}
function transformStylesToCssCasing(domCasing) {
  const cssCasing = {};
  let from;
  for (from in domCasing) {
    if (own$3.call(domCasing, from)) {
      cssCasing[transformStyleToCssCasing(from)] = domCasing[from];
    }
  }
  return cssCasing;
}
function transformStyleToCssCasing(from) {
  let to = from.replace(cap, toDash);
  if (to.slice(0, 3) === "ms-") to = "-" + to;
  return to;
}
function toDash($0) {
  return "-" + $0.toLowerCase();
}
const urlAttributes = {
  action: ["form"],
  cite: ["blockquote", "del", "ins", "q"],
  data: ["object"],
  formAction: ["button", "input"],
  href: ["a", "area", "base", "link"],
  icon: ["menuitem"],
  itemId: null,
  manifest: ["html"],
  ping: ["a", "area"],
  poster: ["video"],
  src: [
    "audio",
    "embed",
    "iframe",
    "img",
    "input",
    "script",
    "source",
    "track",
    "video"
  ]
};
const emptyOptions$2 = {};
function toString$1(value, options) {
  const settings = emptyOptions$2;
  const includeImageAlt = typeof settings.includeImageAlt === "boolean" ? settings.includeImageAlt : true;
  const includeHtml = typeof settings.includeHtml === "boolean" ? settings.includeHtml : true;
  return one(value, includeImageAlt, includeHtml);
}
function one(value, includeImageAlt, includeHtml) {
  if (node(value)) {
    if ("value" in value) {
      return value.type === "html" && !includeHtml ? "" : value.value;
    }
    if (includeImageAlt && "alt" in value && value.alt) {
      return value.alt;
    }
    if ("children" in value) {
      return all(value.children, includeImageAlt, includeHtml);
    }
  }
  if (Array.isArray(value)) {
    return all(value, includeImageAlt, includeHtml);
  }
  return "";
}
function all(values, includeImageAlt, includeHtml) {
  const result = [];
  let index2 = -1;
  while (++index2 < values.length) {
    result[index2] = one(values[index2], includeImageAlt, includeHtml);
  }
  return result.join("");
}
function node(value) {
  return Boolean(value && typeof value === "object");
}
const element = document.createElement("i");
function decodeNamedCharacterReference(value) {
  const characterReference2 = "&" + value + ";";
  element.innerHTML = characterReference2;
  const character = element.textContent;
  if (character.charCodeAt(character.length - 1) === 59 && value !== "semi") {
    return false;
  }
  return character === characterReference2 ? false : character;
}
function splice(list2, start, remove, items) {
  const end = list2.length;
  let chunkStart = 0;
  let parameters;
  if (start < 0) {
    start = -start > end ? 0 : end + start;
  } else {
    start = start > end ? end : start;
  }
  remove = remove > 0 ? remove : 0;
  if (items.length < 1e4) {
    parameters = Array.from(items);
    parameters.unshift(start, remove);
    list2.splice(...parameters);
  } else {
    if (remove) list2.splice(start, remove);
    while (chunkStart < items.length) {
      parameters = items.slice(chunkStart, chunkStart + 1e4);
      parameters.unshift(start, 0);
      list2.splice(...parameters);
      chunkStart += 1e4;
      start += 1e4;
    }
  }
}
function push(list2, items) {
  if (list2.length > 0) {
    splice(list2, list2.length, 0, items);
    return list2;
  }
  return items;
}
const hasOwnProperty = {}.hasOwnProperty;
function combineExtensions(extensions) {
  const all2 = {};
  let index2 = -1;
  while (++index2 < extensions.length) {
    syntaxExtension(all2, extensions[index2]);
  }
  return all2;
}
function syntaxExtension(all2, extension2) {
  let hook;
  for (hook in extension2) {
    const maybe = hasOwnProperty.call(all2, hook) ? all2[hook] : void 0;
    const left = maybe || (all2[hook] = {});
    const right = extension2[hook];
    let code2;
    if (right) {
      for (code2 in right) {
        if (!hasOwnProperty.call(left, code2)) left[code2] = [];
        const value = right[code2];
        constructs(
          // @ts-expect-error Looks like a list.
          left[code2],
          Array.isArray(value) ? value : value ? [value] : []
        );
      }
    }
  }
}
function constructs(existing, list2) {
  let index2 = -1;
  const before = [];
  while (++index2 < list2.length) {
    (list2[index2].add === "after" ? existing : before).push(list2[index2]);
  }
  splice(existing, 0, 0, before);
}
function decodeNumericCharacterReference(value, base) {
  const code2 = Number.parseInt(value, base);
  if (
    // C0 except for HT, LF, FF, CR, space.
    code2 < 9 || code2 === 11 || code2 > 13 && code2 < 32 || // Control character (DEL) of C0, and C1 controls.
    code2 > 126 && code2 < 160 || // Lone high surrogates and low surrogates.
    code2 > 55295 && code2 < 57344 || // Noncharacters.
    code2 > 64975 && code2 < 65008 || /* eslint-disable no-bitwise */
    (code2 & 65535) === 65535 || (code2 & 65535) === 65534 || /* eslint-enable no-bitwise */
    // Out of range
    code2 > 1114111
  ) {
    return "�";
  }
  return String.fromCodePoint(code2);
}
function normalizeIdentifier(value) {
  return value.replace(/[\t\n\r ]+/g, " ").replace(/^ | $/g, "").toLowerCase().toUpperCase();
}
const asciiAlpha = regexCheck(/[A-Za-z]/);
const asciiAlphanumeric = regexCheck(/[\dA-Za-z]/);
const asciiAtext = regexCheck(/[#-'*+\--9=?A-Z^-~]/);
function asciiControl(code2) {
  return (
    // Special whitespace codes (which have negative values), C0 and Control
    // character DEL
    code2 !== null && (code2 < 32 || code2 === 127)
  );
}
const asciiDigit = regexCheck(/\d/);
const asciiHexDigit = regexCheck(/[\dA-Fa-f]/);
const asciiPunctuation = regexCheck(/[!-/:-@[-`{-~]/);
function markdownLineEnding(code2) {
  return code2 !== null && code2 < -2;
}
function markdownLineEndingOrSpace(code2) {
  return code2 !== null && (code2 < 0 || code2 === 32);
}
function markdownSpace(code2) {
  return code2 === -2 || code2 === -1 || code2 === 32;
}
const unicodePunctuation = regexCheck(new RegExp("\\p{P}|\\p{S}", "u"));
const unicodeWhitespace = regexCheck(/\s/);
function regexCheck(regex) {
  return check;
  function check(code2) {
    return code2 !== null && code2 > -1 && regex.test(String.fromCharCode(code2));
  }
}
function normalizeUri(value) {
  const result = [];
  let index2 = -1;
  let start = 0;
  let skip = 0;
  while (++index2 < value.length) {
    const code2 = value.charCodeAt(index2);
    let replace2 = "";
    if (code2 === 37 && asciiAlphanumeric(value.charCodeAt(index2 + 1)) && asciiAlphanumeric(value.charCodeAt(index2 + 2))) {
      skip = 2;
    } else if (code2 < 128) {
      if (!/[!#$&-;=?-Z_a-z~]/.test(String.fromCharCode(code2))) {
        replace2 = String.fromCharCode(code2);
      }
    } else if (code2 > 55295 && code2 < 57344) {
      const next = value.charCodeAt(index2 + 1);
      if (code2 < 56320 && next > 56319 && next < 57344) {
        replace2 = String.fromCharCode(code2, next);
        skip = 1;
      } else {
        replace2 = "�";
      }
    } else {
      replace2 = String.fromCharCode(code2);
    }
    if (replace2) {
      result.push(value.slice(start, index2), encodeURIComponent(replace2));
      start = index2 + skip + 1;
      replace2 = "";
    }
    if (skip) {
      index2 += skip;
      skip = 0;
    }
  }
  return result.join("") + value.slice(start);
}
function factorySpace(effects, ok2, type, max2) {
  const limit = max2 ? max2 - 1 : Number.POSITIVE_INFINITY;
  let size2 = 0;
  return start;
  function start(code2) {
    if (markdownSpace(code2)) {
      effects.enter(type);
      return prefix(code2);
    }
    return ok2(code2);
  }
  function prefix(code2) {
    if (markdownSpace(code2) && size2++ < limit) {
      effects.consume(code2);
      return prefix;
    }
    effects.exit(type);
    return ok2(code2);
  }
}
const content$1 = {
  tokenize: initializeContent
};
function initializeContent(effects) {
  const contentStart = effects.attempt(this.parser.constructs.contentInitial, afterContentStartConstruct, paragraphInitial);
  let previous2;
  return contentStart;
  function afterContentStartConstruct(code2) {
    if (code2 === null) {
      effects.consume(code2);
      return;
    }
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return factorySpace(effects, contentStart, "linePrefix");
  }
  function paragraphInitial(code2) {
    effects.enter("paragraph");
    return lineStart(code2);
  }
  function lineStart(code2) {
    const token = effects.enter("chunkText", {
      contentType: "text",
      previous: previous2
    });
    if (previous2) {
      previous2.next = token;
    }
    previous2 = token;
    return data(code2);
  }
  function data(code2) {
    if (code2 === null) {
      effects.exit("chunkText");
      effects.exit("paragraph");
      effects.consume(code2);
      return;
    }
    if (markdownLineEnding(code2)) {
      effects.consume(code2);
      effects.exit("chunkText");
      return lineStart;
    }
    effects.consume(code2);
    return data;
  }
}
const document$2 = {
  tokenize: initializeDocument
};
const containerConstruct = {
  tokenize: tokenizeContainer
};
function initializeDocument(effects) {
  const self2 = this;
  const stack = [];
  let continued = 0;
  let childFlow;
  let childToken;
  let lineStartOffset;
  return start;
  function start(code2) {
    if (continued < stack.length) {
      const item = stack[continued];
      self2.containerState = item[1];
      return effects.attempt(item[0].continuation, documentContinue, checkNewContainers)(code2);
    }
    return checkNewContainers(code2);
  }
  function documentContinue(code2) {
    continued++;
    if (self2.containerState._closeFlow) {
      self2.containerState._closeFlow = void 0;
      if (childFlow) {
        closeFlow();
      }
      const indexBeforeExits = self2.events.length;
      let indexBeforeFlow = indexBeforeExits;
      let point2;
      while (indexBeforeFlow--) {
        if (self2.events[indexBeforeFlow][0] === "exit" && self2.events[indexBeforeFlow][1].type === "chunkFlow") {
          point2 = self2.events[indexBeforeFlow][1].end;
          break;
        }
      }
      exitContainers(continued);
      let index2 = indexBeforeExits;
      while (index2 < self2.events.length) {
        self2.events[index2][1].end = {
          ...point2
        };
        index2++;
      }
      splice(self2.events, indexBeforeFlow + 1, 0, self2.events.slice(indexBeforeExits));
      self2.events.length = index2;
      return checkNewContainers(code2);
    }
    return start(code2);
  }
  function checkNewContainers(code2) {
    if (continued === stack.length) {
      if (!childFlow) {
        return documentContinued(code2);
      }
      if (childFlow.currentConstruct && childFlow.currentConstruct.concrete) {
        return flowStart(code2);
      }
      self2.interrupt = Boolean(childFlow.currentConstruct && !childFlow._gfmTableDynamicInterruptHack);
    }
    self2.containerState = {};
    return effects.check(containerConstruct, thereIsANewContainer, thereIsNoNewContainer)(code2);
  }
  function thereIsANewContainer(code2) {
    if (childFlow) closeFlow();
    exitContainers(continued);
    return documentContinued(code2);
  }
  function thereIsNoNewContainer(code2) {
    self2.parser.lazy[self2.now().line] = continued !== stack.length;
    lineStartOffset = self2.now().offset;
    return flowStart(code2);
  }
  function documentContinued(code2) {
    self2.containerState = {};
    return effects.attempt(containerConstruct, containerContinue, flowStart)(code2);
  }
  function containerContinue(code2) {
    continued++;
    stack.push([self2.currentConstruct, self2.containerState]);
    return documentContinued(code2);
  }
  function flowStart(code2) {
    if (code2 === null) {
      if (childFlow) closeFlow();
      exitContainers(0);
      effects.consume(code2);
      return;
    }
    childFlow = childFlow || self2.parser.flow(self2.now());
    effects.enter("chunkFlow", {
      _tokenizer: childFlow,
      contentType: "flow",
      previous: childToken
    });
    return flowContinue(code2);
  }
  function flowContinue(code2) {
    if (code2 === null) {
      writeToChild(effects.exit("chunkFlow"), true);
      exitContainers(0);
      effects.consume(code2);
      return;
    }
    if (markdownLineEnding(code2)) {
      effects.consume(code2);
      writeToChild(effects.exit("chunkFlow"));
      continued = 0;
      self2.interrupt = void 0;
      return start;
    }
    effects.consume(code2);
    return flowContinue;
  }
  function writeToChild(token, endOfFile) {
    const stream = self2.sliceStream(token);
    if (endOfFile) stream.push(null);
    token.previous = childToken;
    if (childToken) childToken.next = token;
    childToken = token;
    childFlow.defineSkip(token.start);
    childFlow.write(stream);
    if (self2.parser.lazy[token.start.line]) {
      let index2 = childFlow.events.length;
      while (index2--) {
        if (
          // The token starts before the line ending…
          childFlow.events[index2][1].start.offset < lineStartOffset && // …and either is not ended yet…
          (!childFlow.events[index2][1].end || // …or ends after it.
          childFlow.events[index2][1].end.offset > lineStartOffset)
        ) {
          return;
        }
      }
      const indexBeforeExits = self2.events.length;
      let indexBeforeFlow = indexBeforeExits;
      let seen;
      let point2;
      while (indexBeforeFlow--) {
        if (self2.events[indexBeforeFlow][0] === "exit" && self2.events[indexBeforeFlow][1].type === "chunkFlow") {
          if (seen) {
            point2 = self2.events[indexBeforeFlow][1].end;
            break;
          }
          seen = true;
        }
      }
      exitContainers(continued);
      index2 = indexBeforeExits;
      while (index2 < self2.events.length) {
        self2.events[index2][1].end = {
          ...point2
        };
        index2++;
      }
      splice(self2.events, indexBeforeFlow + 1, 0, self2.events.slice(indexBeforeExits));
      self2.events.length = index2;
    }
  }
  function exitContainers(size2) {
    let index2 = stack.length;
    while (index2-- > size2) {
      const entry = stack[index2];
      self2.containerState = entry[1];
      entry[0].exit.call(self2, effects);
    }
    stack.length = size2;
  }
  function closeFlow() {
    childFlow.write([null]);
    childToken = void 0;
    childFlow = void 0;
    self2.containerState._closeFlow = void 0;
  }
}
function tokenizeContainer(effects, ok2, nok) {
  return factorySpace(effects, effects.attempt(this.parser.constructs.document, ok2, nok), "linePrefix", this.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4);
}
function classifyCharacter(code2) {
  if (code2 === null || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2)) {
    return 1;
  }
  if (unicodePunctuation(code2)) {
    return 2;
  }
}
function resolveAll(constructs2, events, context) {
  const called = [];
  let index2 = -1;
  while (++index2 < constructs2.length) {
    const resolve = constructs2[index2].resolveAll;
    if (resolve && !called.includes(resolve)) {
      events = resolve(events, context);
      called.push(resolve);
    }
  }
  return events;
}
const attention = {
  name: "attention",
  resolveAll: resolveAllAttention,
  tokenize: tokenizeAttention
};
function resolveAllAttention(events, context) {
  let index2 = -1;
  let open;
  let group;
  let text2;
  let openingSequence;
  let closingSequence;
  let use;
  let nextEvents;
  let offset2;
  while (++index2 < events.length) {
    if (events[index2][0] === "enter" && events[index2][1].type === "attentionSequence" && events[index2][1]._close) {
      open = index2;
      while (open--) {
        if (events[open][0] === "exit" && events[open][1].type === "attentionSequence" && events[open][1]._open && // If the markers are the same:
        context.sliceSerialize(events[open][1]).charCodeAt(0) === context.sliceSerialize(events[index2][1]).charCodeAt(0)) {
          if ((events[open][1]._close || events[index2][1]._open) && (events[index2][1].end.offset - events[index2][1].start.offset) % 3 && !((events[open][1].end.offset - events[open][1].start.offset + events[index2][1].end.offset - events[index2][1].start.offset) % 3)) {
            continue;
          }
          use = events[open][1].end.offset - events[open][1].start.offset > 1 && events[index2][1].end.offset - events[index2][1].start.offset > 1 ? 2 : 1;
          const start = {
            ...events[open][1].end
          };
          const end = {
            ...events[index2][1].start
          };
          movePoint(start, -use);
          movePoint(end, use);
          openingSequence = {
            type: use > 1 ? "strongSequence" : "emphasisSequence",
            start,
            end: {
              ...events[open][1].end
            }
          };
          closingSequence = {
            type: use > 1 ? "strongSequence" : "emphasisSequence",
            start: {
              ...events[index2][1].start
            },
            end
          };
          text2 = {
            type: use > 1 ? "strongText" : "emphasisText",
            start: {
              ...events[open][1].end
            },
            end: {
              ...events[index2][1].start
            }
          };
          group = {
            type: use > 1 ? "strong" : "emphasis",
            start: {
              ...openingSequence.start
            },
            end: {
              ...closingSequence.end
            }
          };
          events[open][1].end = {
            ...openingSequence.start
          };
          events[index2][1].start = {
            ...closingSequence.end
          };
          nextEvents = [];
          if (events[open][1].end.offset - events[open][1].start.offset) {
            nextEvents = push(nextEvents, [["enter", events[open][1], context], ["exit", events[open][1], context]]);
          }
          nextEvents = push(nextEvents, [["enter", group, context], ["enter", openingSequence, context], ["exit", openingSequence, context], ["enter", text2, context]]);
          nextEvents = push(nextEvents, resolveAll(context.parser.constructs.insideSpan.null, events.slice(open + 1, index2), context));
          nextEvents = push(nextEvents, [["exit", text2, context], ["enter", closingSequence, context], ["exit", closingSequence, context], ["exit", group, context]]);
          if (events[index2][1].end.offset - events[index2][1].start.offset) {
            offset2 = 2;
            nextEvents = push(nextEvents, [["enter", events[index2][1], context], ["exit", events[index2][1], context]]);
          } else {
            offset2 = 0;
          }
          splice(events, open - 1, index2 - open + 3, nextEvents);
          index2 = open + nextEvents.length - offset2 - 2;
          break;
        }
      }
    }
  }
  index2 = -1;
  while (++index2 < events.length) {
    if (events[index2][1].type === "attentionSequence") {
      events[index2][1].type = "data";
    }
  }
  return events;
}
function tokenizeAttention(effects, ok2) {
  const attentionMarkers2 = this.parser.constructs.attentionMarkers.null;
  const previous2 = this.previous;
  const before = classifyCharacter(previous2);
  let marker;
  return start;
  function start(code2) {
    marker = code2;
    effects.enter("attentionSequence");
    return inside(code2);
  }
  function inside(code2) {
    if (code2 === marker) {
      effects.consume(code2);
      return inside;
    }
    const token = effects.exit("attentionSequence");
    const after = classifyCharacter(code2);
    const open = !after || after === 2 && before || attentionMarkers2.includes(code2);
    const close = !before || before === 2 && after || attentionMarkers2.includes(previous2);
    token._open = Boolean(marker === 42 ? open : open && (before || !close));
    token._close = Boolean(marker === 42 ? close : close && (after || !open));
    return ok2(code2);
  }
}
function movePoint(point2, offset2) {
  point2.column += offset2;
  point2.offset += offset2;
  point2._bufferIndex += offset2;
}
const autolink = {
  name: "autolink",
  tokenize: tokenizeAutolink
};
function tokenizeAutolink(effects, ok2, nok) {
  let size2 = 0;
  return start;
  function start(code2) {
    effects.enter("autolink");
    effects.enter("autolinkMarker");
    effects.consume(code2);
    effects.exit("autolinkMarker");
    effects.enter("autolinkProtocol");
    return open;
  }
  function open(code2) {
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return schemeOrEmailAtext;
    }
    if (code2 === 64) {
      return nok(code2);
    }
    return emailAtext(code2);
  }
  function schemeOrEmailAtext(code2) {
    if (code2 === 43 || code2 === 45 || code2 === 46 || asciiAlphanumeric(code2)) {
      size2 = 1;
      return schemeInsideOrEmailAtext(code2);
    }
    return emailAtext(code2);
  }
  function schemeInsideOrEmailAtext(code2) {
    if (code2 === 58) {
      effects.consume(code2);
      size2 = 0;
      return urlInside;
    }
    if ((code2 === 43 || code2 === 45 || code2 === 46 || asciiAlphanumeric(code2)) && size2++ < 32) {
      effects.consume(code2);
      return schemeInsideOrEmailAtext;
    }
    size2 = 0;
    return emailAtext(code2);
  }
  function urlInside(code2) {
    if (code2 === 62) {
      effects.exit("autolinkProtocol");
      effects.enter("autolinkMarker");
      effects.consume(code2);
      effects.exit("autolinkMarker");
      effects.exit("autolink");
      return ok2;
    }
    if (code2 === null || code2 === 32 || code2 === 60 || asciiControl(code2)) {
      return nok(code2);
    }
    effects.consume(code2);
    return urlInside;
  }
  function emailAtext(code2) {
    if (code2 === 64) {
      effects.consume(code2);
      return emailAtSignOrDot;
    }
    if (asciiAtext(code2)) {
      effects.consume(code2);
      return emailAtext;
    }
    return nok(code2);
  }
  function emailAtSignOrDot(code2) {
    return asciiAlphanumeric(code2) ? emailLabel(code2) : nok(code2);
  }
  function emailLabel(code2) {
    if (code2 === 46) {
      effects.consume(code2);
      size2 = 0;
      return emailAtSignOrDot;
    }
    if (code2 === 62) {
      effects.exit("autolinkProtocol").type = "autolinkEmail";
      effects.enter("autolinkMarker");
      effects.consume(code2);
      effects.exit("autolinkMarker");
      effects.exit("autolink");
      return ok2;
    }
    return emailValue(code2);
  }
  function emailValue(code2) {
    if ((code2 === 45 || asciiAlphanumeric(code2)) && size2++ < 63) {
      const next = code2 === 45 ? emailValue : emailLabel;
      effects.consume(code2);
      return next;
    }
    return nok(code2);
  }
}
const blankLine = {
  partial: true,
  tokenize: tokenizeBlankLine
};
function tokenizeBlankLine(effects, ok2, nok) {
  return start;
  function start(code2) {
    return markdownSpace(code2) ? factorySpace(effects, after, "linePrefix")(code2) : after(code2);
  }
  function after(code2) {
    return code2 === null || markdownLineEnding(code2) ? ok2(code2) : nok(code2);
  }
}
const blockQuote = {
  continuation: {
    tokenize: tokenizeBlockQuoteContinuation
  },
  exit: exit$1,
  name: "blockQuote",
  tokenize: tokenizeBlockQuoteStart
};
function tokenizeBlockQuoteStart(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    if (code2 === 62) {
      const state = self2.containerState;
      if (!state.open) {
        effects.enter("blockQuote", {
          _container: true
        });
        state.open = true;
      }
      effects.enter("blockQuotePrefix");
      effects.enter("blockQuoteMarker");
      effects.consume(code2);
      effects.exit("blockQuoteMarker");
      return after;
    }
    return nok(code2);
  }
  function after(code2) {
    if (markdownSpace(code2)) {
      effects.enter("blockQuotePrefixWhitespace");
      effects.consume(code2);
      effects.exit("blockQuotePrefixWhitespace");
      effects.exit("blockQuotePrefix");
      return ok2;
    }
    effects.exit("blockQuotePrefix");
    return ok2(code2);
  }
}
function tokenizeBlockQuoteContinuation(effects, ok2, nok) {
  const self2 = this;
  return contStart;
  function contStart(code2) {
    if (markdownSpace(code2)) {
      return factorySpace(effects, contBefore, "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2);
    }
    return contBefore(code2);
  }
  function contBefore(code2) {
    return effects.attempt(blockQuote, ok2, nok)(code2);
  }
}
function exit$1(effects) {
  effects.exit("blockQuote");
}
const characterEscape = {
  name: "characterEscape",
  tokenize: tokenizeCharacterEscape
};
function tokenizeCharacterEscape(effects, ok2, nok) {
  return start;
  function start(code2) {
    effects.enter("characterEscape");
    effects.enter("escapeMarker");
    effects.consume(code2);
    effects.exit("escapeMarker");
    return inside;
  }
  function inside(code2) {
    if (asciiPunctuation(code2)) {
      effects.enter("characterEscapeValue");
      effects.consume(code2);
      effects.exit("characterEscapeValue");
      effects.exit("characterEscape");
      return ok2;
    }
    return nok(code2);
  }
}
const characterReference = {
  name: "characterReference",
  tokenize: tokenizeCharacterReference
};
function tokenizeCharacterReference(effects, ok2, nok) {
  const self2 = this;
  let size2 = 0;
  let max2;
  let test;
  return start;
  function start(code2) {
    effects.enter("characterReference");
    effects.enter("characterReferenceMarker");
    effects.consume(code2);
    effects.exit("characterReferenceMarker");
    return open;
  }
  function open(code2) {
    if (code2 === 35) {
      effects.enter("characterReferenceMarkerNumeric");
      effects.consume(code2);
      effects.exit("characterReferenceMarkerNumeric");
      return numeric;
    }
    effects.enter("characterReferenceValue");
    max2 = 31;
    test = asciiAlphanumeric;
    return value(code2);
  }
  function numeric(code2) {
    if (code2 === 88 || code2 === 120) {
      effects.enter("characterReferenceMarkerHexadecimal");
      effects.consume(code2);
      effects.exit("characterReferenceMarkerHexadecimal");
      effects.enter("characterReferenceValue");
      max2 = 6;
      test = asciiHexDigit;
      return value;
    }
    effects.enter("characterReferenceValue");
    max2 = 7;
    test = asciiDigit;
    return value(code2);
  }
  function value(code2) {
    if (code2 === 59 && size2) {
      const token = effects.exit("characterReferenceValue");
      if (test === asciiAlphanumeric && !decodeNamedCharacterReference(self2.sliceSerialize(token))) {
        return nok(code2);
      }
      effects.enter("characterReferenceMarker");
      effects.consume(code2);
      effects.exit("characterReferenceMarker");
      effects.exit("characterReference");
      return ok2;
    }
    if (test(code2) && size2++ < max2) {
      effects.consume(code2);
      return value;
    }
    return nok(code2);
  }
}
const nonLazyContinuation = {
  partial: true,
  tokenize: tokenizeNonLazyContinuation
};
const codeFenced = {
  concrete: true,
  name: "codeFenced",
  tokenize: tokenizeCodeFenced
};
function tokenizeCodeFenced(effects, ok2, nok) {
  const self2 = this;
  const closeStart = {
    partial: true,
    tokenize: tokenizeCloseStart
  };
  let initialPrefix = 0;
  let sizeOpen = 0;
  let marker;
  return start;
  function start(code2) {
    return beforeSequenceOpen(code2);
  }
  function beforeSequenceOpen(code2) {
    const tail = self2.events[self2.events.length - 1];
    initialPrefix = tail && tail[1].type === "linePrefix" ? tail[2].sliceSerialize(tail[1], true).length : 0;
    marker = code2;
    effects.enter("codeFenced");
    effects.enter("codeFencedFence");
    effects.enter("codeFencedFenceSequence");
    return sequenceOpen(code2);
  }
  function sequenceOpen(code2) {
    if (code2 === marker) {
      sizeOpen++;
      effects.consume(code2);
      return sequenceOpen;
    }
    if (sizeOpen < 3) {
      return nok(code2);
    }
    effects.exit("codeFencedFenceSequence");
    return markdownSpace(code2) ? factorySpace(effects, infoBefore, "whitespace")(code2) : infoBefore(code2);
  }
  function infoBefore(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("codeFencedFence");
      return self2.interrupt ? ok2(code2) : effects.check(nonLazyContinuation, atNonLazyBreak, after)(code2);
    }
    effects.enter("codeFencedFenceInfo");
    effects.enter("chunkString", {
      contentType: "string"
    });
    return info(code2);
  }
  function info(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("chunkString");
      effects.exit("codeFencedFenceInfo");
      return infoBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.exit("chunkString");
      effects.exit("codeFencedFenceInfo");
      return factorySpace(effects, metaBefore, "whitespace")(code2);
    }
    if (code2 === 96 && code2 === marker) {
      return nok(code2);
    }
    effects.consume(code2);
    return info;
  }
  function metaBefore(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return infoBefore(code2);
    }
    effects.enter("codeFencedFenceMeta");
    effects.enter("chunkString", {
      contentType: "string"
    });
    return meta(code2);
  }
  function meta(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("chunkString");
      effects.exit("codeFencedFenceMeta");
      return infoBefore(code2);
    }
    if (code2 === 96 && code2 === marker) {
      return nok(code2);
    }
    effects.consume(code2);
    return meta;
  }
  function atNonLazyBreak(code2) {
    return effects.attempt(closeStart, after, contentBefore)(code2);
  }
  function contentBefore(code2) {
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return contentStart;
  }
  function contentStart(code2) {
    return initialPrefix > 0 && markdownSpace(code2) ? factorySpace(effects, beforeContentChunk, "linePrefix", initialPrefix + 1)(code2) : beforeContentChunk(code2);
  }
  function beforeContentChunk(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return effects.check(nonLazyContinuation, atNonLazyBreak, after)(code2);
    }
    effects.enter("codeFlowValue");
    return contentChunk(code2);
  }
  function contentChunk(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("codeFlowValue");
      return beforeContentChunk(code2);
    }
    effects.consume(code2);
    return contentChunk;
  }
  function after(code2) {
    effects.exit("codeFenced");
    return ok2(code2);
  }
  function tokenizeCloseStart(effects2, ok3, nok2) {
    let size2 = 0;
    return startBefore;
    function startBefore(code2) {
      effects2.enter("lineEnding");
      effects2.consume(code2);
      effects2.exit("lineEnding");
      return start2;
    }
    function start2(code2) {
      effects2.enter("codeFencedFence");
      return markdownSpace(code2) ? factorySpace(effects2, beforeSequenceClose, "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2) : beforeSequenceClose(code2);
    }
    function beforeSequenceClose(code2) {
      if (code2 === marker) {
        effects2.enter("codeFencedFenceSequence");
        return sequenceClose(code2);
      }
      return nok2(code2);
    }
    function sequenceClose(code2) {
      if (code2 === marker) {
        size2++;
        effects2.consume(code2);
        return sequenceClose;
      }
      if (size2 >= sizeOpen) {
        effects2.exit("codeFencedFenceSequence");
        return markdownSpace(code2) ? factorySpace(effects2, sequenceCloseAfter, "whitespace")(code2) : sequenceCloseAfter(code2);
      }
      return nok2(code2);
    }
    function sequenceCloseAfter(code2) {
      if (code2 === null || markdownLineEnding(code2)) {
        effects2.exit("codeFencedFence");
        return ok3(code2);
      }
      return nok2(code2);
    }
  }
}
function tokenizeNonLazyContinuation(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return lineStart;
  }
  function lineStart(code2) {
    return self2.parser.lazy[self2.now().line] ? nok(code2) : ok2(code2);
  }
}
const codeIndented = {
  name: "codeIndented",
  tokenize: tokenizeCodeIndented
};
const furtherStart = {
  partial: true,
  tokenize: tokenizeFurtherStart
};
function tokenizeCodeIndented(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    effects.enter("codeIndented");
    return factorySpace(effects, afterPrefix, "linePrefix", 4 + 1)(code2);
  }
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === "linePrefix" && tail[2].sliceSerialize(tail[1], true).length >= 4 ? atBreak(code2) : nok(code2);
  }
  function atBreak(code2) {
    if (code2 === null) {
      return after(code2);
    }
    if (markdownLineEnding(code2)) {
      return effects.attempt(furtherStart, atBreak, after)(code2);
    }
    effects.enter("codeFlowValue");
    return inside(code2);
  }
  function inside(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("codeFlowValue");
      return atBreak(code2);
    }
    effects.consume(code2);
    return inside;
  }
  function after(code2) {
    effects.exit("codeIndented");
    return ok2(code2);
  }
}
function tokenizeFurtherStart(effects, ok2, nok) {
  const self2 = this;
  return furtherStart2;
  function furtherStart2(code2) {
    if (self2.parser.lazy[self2.now().line]) {
      return nok(code2);
    }
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return furtherStart2;
    }
    return factorySpace(effects, afterPrefix, "linePrefix", 4 + 1)(code2);
  }
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === "linePrefix" && tail[2].sliceSerialize(tail[1], true).length >= 4 ? ok2(code2) : markdownLineEnding(code2) ? furtherStart2(code2) : nok(code2);
  }
}
const codeText = {
  name: "codeText",
  previous: previous$1,
  resolve: resolveCodeText,
  tokenize: tokenizeCodeText
};
function resolveCodeText(events) {
  let tailExitIndex = events.length - 4;
  let headEnterIndex = 3;
  let index2;
  let enter;
  if ((events[headEnterIndex][1].type === "lineEnding" || events[headEnterIndex][1].type === "space") && (events[tailExitIndex][1].type === "lineEnding" || events[tailExitIndex][1].type === "space")) {
    index2 = headEnterIndex;
    while (++index2 < tailExitIndex) {
      if (events[index2][1].type === "codeTextData") {
        events[headEnterIndex][1].type = "codeTextPadding";
        events[tailExitIndex][1].type = "codeTextPadding";
        headEnterIndex += 2;
        tailExitIndex -= 2;
        break;
      }
    }
  }
  index2 = headEnterIndex - 1;
  tailExitIndex++;
  while (++index2 <= tailExitIndex) {
    if (enter === void 0) {
      if (index2 !== tailExitIndex && events[index2][1].type !== "lineEnding") {
        enter = index2;
      }
    } else if (index2 === tailExitIndex || events[index2][1].type === "lineEnding") {
      events[enter][1].type = "codeTextData";
      if (index2 !== enter + 2) {
        events[enter][1].end = events[index2 - 1][1].end;
        events.splice(enter + 2, index2 - enter - 2);
        tailExitIndex -= index2 - enter - 2;
        index2 = enter + 2;
      }
      enter = void 0;
    }
  }
  return events;
}
function previous$1(code2) {
  return code2 !== 96 || this.events[this.events.length - 1][1].type === "characterEscape";
}
function tokenizeCodeText(effects, ok2, nok) {
  let sizeOpen = 0;
  let size2;
  let token;
  return start;
  function start(code2) {
    effects.enter("codeText");
    effects.enter("codeTextSequence");
    return sequenceOpen(code2);
  }
  function sequenceOpen(code2) {
    if (code2 === 96) {
      effects.consume(code2);
      sizeOpen++;
      return sequenceOpen;
    }
    effects.exit("codeTextSequence");
    return between(code2);
  }
  function between(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (code2 === 32) {
      effects.enter("space");
      effects.consume(code2);
      effects.exit("space");
      return between;
    }
    if (code2 === 96) {
      token = effects.enter("codeTextSequence");
      size2 = 0;
      return sequenceClose(code2);
    }
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return between;
    }
    effects.enter("codeTextData");
    return data(code2);
  }
  function data(code2) {
    if (code2 === null || code2 === 32 || code2 === 96 || markdownLineEnding(code2)) {
      effects.exit("codeTextData");
      return between(code2);
    }
    effects.consume(code2);
    return data;
  }
  function sequenceClose(code2) {
    if (code2 === 96) {
      effects.consume(code2);
      size2++;
      return sequenceClose;
    }
    if (size2 === sizeOpen) {
      effects.exit("codeTextSequence");
      effects.exit("codeText");
      return ok2(code2);
    }
    token.type = "codeTextData";
    return data(code2);
  }
}
class SpliceBuffer {
  /**
   * @param {ReadonlyArray<T> | null | undefined} [initial]
   *   Initial items (optional).
   * @returns
   *   Splice buffer.
   */
  constructor(initial) {
    this.left = initial ? [...initial] : [];
    this.right = [];
  }
  /**
   * Array access;
   * does not move the cursor.
   *
   * @param {number} index
   *   Index.
   * @return {T}
   *   Item.
   */
  get(index2) {
    if (index2 < 0 || index2 >= this.left.length + this.right.length) {
      throw new RangeError("Cannot access index `" + index2 + "` in a splice buffer of size `" + (this.left.length + this.right.length) + "`");
    }
    if (index2 < this.left.length) return this.left[index2];
    return this.right[this.right.length - index2 + this.left.length - 1];
  }
  /**
   * The length of the splice buffer, one greater than the largest index in the
   * array.
   */
  get length() {
    return this.left.length + this.right.length;
  }
  /**
   * Remove and return `list[0]`;
   * moves the cursor to `0`.
   *
   * @returns {T | undefined}
   *   Item, optional.
   */
  shift() {
    this.setCursor(0);
    return this.right.pop();
  }
  /**
   * Slice the buffer to get an array;
   * does not move the cursor.
   *
   * @param {number} start
   *   Start.
   * @param {number | null | undefined} [end]
   *   End (optional).
   * @returns {Array<T>}
   *   Array of items.
   */
  slice(start, end) {
    const stop = end === null || end === void 0 ? Number.POSITIVE_INFINITY : end;
    if (stop < this.left.length) {
      return this.left.slice(start, stop);
    }
    if (start > this.left.length) {
      return this.right.slice(this.right.length - stop + this.left.length, this.right.length - start + this.left.length).reverse();
    }
    return this.left.slice(start).concat(this.right.slice(this.right.length - stop + this.left.length).reverse());
  }
  /**
   * Mimics the behavior of Array.prototype.splice() except for the change of
   * interface necessary to avoid segfaults when patching in very large arrays.
   *
   * This operation moves cursor is moved to `start` and results in the cursor
   * placed after any inserted items.
   *
   * @param {number} start
   *   Start;
   *   zero-based index at which to start changing the array;
   *   negative numbers count backwards from the end of the array and values
   *   that are out-of bounds are clamped to the appropriate end of the array.
   * @param {number | null | undefined} [deleteCount=0]
   *   Delete count (default: `0`);
   *   maximum number of elements to delete, starting from start.
   * @param {Array<T> | null | undefined} [items=[]]
   *   Items to include in place of the deleted items (default: `[]`).
   * @return {Array<T>}
   *   Any removed items.
   */
  splice(start, deleteCount, items) {
    const count2 = deleteCount || 0;
    this.setCursor(Math.trunc(start));
    const removed = this.right.splice(this.right.length - count2, Number.POSITIVE_INFINITY);
    if (items) chunkedPush(this.left, items);
    return removed.reverse();
  }
  /**
   * Remove and return the highest-numbered item in the array, so
   * `list[list.length - 1]`;
   * Moves the cursor to `length`.
   *
   * @returns {T | undefined}
   *   Item, optional.
   */
  pop() {
    this.setCursor(Number.POSITIVE_INFINITY);
    return this.left.pop();
  }
  /**
   * Inserts a single item to the high-numbered side of the array;
   * moves the cursor to `length`.
   *
   * @param {T} item
   *   Item.
   * @returns {undefined}
   *   Nothing.
   */
  push(item) {
    this.setCursor(Number.POSITIVE_INFINITY);
    this.left.push(item);
  }
  /**
   * Inserts many items to the high-numbered side of the array.
   * Moves the cursor to `length`.
   *
   * @param {Array<T>} items
   *   Items.
   * @returns {undefined}
   *   Nothing.
   */
  pushMany(items) {
    this.setCursor(Number.POSITIVE_INFINITY);
    chunkedPush(this.left, items);
  }
  /**
   * Inserts a single item to the low-numbered side of the array;
   * Moves the cursor to `0`.
   *
   * @param {T} item
   *   Item.
   * @returns {undefined}
   *   Nothing.
   */
  unshift(item) {
    this.setCursor(0);
    this.right.push(item);
  }
  /**
   * Inserts many items to the low-numbered side of the array;
   * moves the cursor to `0`.
   *
   * @param {Array<T>} items
   *   Items.
   * @returns {undefined}
   *   Nothing.
   */
  unshiftMany(items) {
    this.setCursor(0);
    chunkedPush(this.right, items.reverse());
  }
  /**
   * Move the cursor to a specific position in the array. Requires
   * time proportional to the distance moved.
   *
   * If `n < 0`, the cursor will end up at the beginning.
   * If `n > length`, the cursor will end up at the end.
   *
   * @param {number} n
   *   Position.
   * @return {undefined}
   *   Nothing.
   */
  setCursor(n) {
    if (n === this.left.length || n > this.left.length && this.right.length === 0 || n < 0 && this.left.length === 0) return;
    if (n < this.left.length) {
      const removed = this.left.splice(n, Number.POSITIVE_INFINITY);
      chunkedPush(this.right, removed.reverse());
    } else {
      const removed = this.right.splice(this.left.length + this.right.length - n, Number.POSITIVE_INFINITY);
      chunkedPush(this.left, removed.reverse());
    }
  }
}
function chunkedPush(list2, right) {
  let chunkStart = 0;
  if (right.length < 1e4) {
    list2.push(...right);
  } else {
    while (chunkStart < right.length) {
      list2.push(...right.slice(chunkStart, chunkStart + 1e4));
      chunkStart += 1e4;
    }
  }
}
function subtokenize(eventsArray) {
  const jumps = {};
  let index2 = -1;
  let event;
  let lineIndex;
  let otherIndex;
  let otherEvent;
  let parameters;
  let subevents;
  let more;
  const events = new SpliceBuffer(eventsArray);
  while (++index2 < events.length) {
    while (index2 in jumps) {
      index2 = jumps[index2];
    }
    event = events.get(index2);
    if (index2 && event[1].type === "chunkFlow" && events.get(index2 - 1)[1].type === "listItemPrefix") {
      subevents = event[1]._tokenizer.events;
      otherIndex = 0;
      if (otherIndex < subevents.length && subevents[otherIndex][1].type === "lineEndingBlank") {
        otherIndex += 2;
      }
      if (otherIndex < subevents.length && subevents[otherIndex][1].type === "content") {
        while (++otherIndex < subevents.length) {
          if (subevents[otherIndex][1].type === "content") {
            break;
          }
          if (subevents[otherIndex][1].type === "chunkText") {
            subevents[otherIndex][1]._isInFirstContentOfListItem = true;
            otherIndex++;
          }
        }
      }
    }
    if (event[0] === "enter") {
      if (event[1].contentType) {
        Object.assign(jumps, subcontent(events, index2));
        index2 = jumps[index2];
        more = true;
      }
    } else if (event[1]._container) {
      otherIndex = index2;
      lineIndex = void 0;
      while (otherIndex--) {
        otherEvent = events.get(otherIndex);
        if (otherEvent[1].type === "lineEnding" || otherEvent[1].type === "lineEndingBlank") {
          if (otherEvent[0] === "enter") {
            if (lineIndex) {
              events.get(lineIndex)[1].type = "lineEndingBlank";
            }
            otherEvent[1].type = "lineEnding";
            lineIndex = otherIndex;
          }
        } else if (otherEvent[1].type === "linePrefix" || otherEvent[1].type === "listItemIndent") ;
        else {
          break;
        }
      }
      if (lineIndex) {
        event[1].end = {
          ...events.get(lineIndex)[1].start
        };
        parameters = events.slice(lineIndex, index2);
        parameters.unshift(event);
        events.splice(lineIndex, index2 - lineIndex + 1, parameters);
      }
    }
  }
  splice(eventsArray, 0, Number.POSITIVE_INFINITY, events.slice(0));
  return !more;
}
function subcontent(events, eventIndex) {
  const token = events.get(eventIndex)[1];
  const context = events.get(eventIndex)[2];
  let startPosition = eventIndex - 1;
  const startPositions = [];
  let tokenizer = token._tokenizer;
  if (!tokenizer) {
    tokenizer = context.parser[token.contentType](token.start);
    if (token._contentTypeTextTrailing) {
      tokenizer._contentTypeTextTrailing = true;
    }
  }
  const childEvents = tokenizer.events;
  const jumps = [];
  const gaps = {};
  let stream;
  let previous2;
  let index2 = -1;
  let current = token;
  let adjust = 0;
  let start = 0;
  const breaks = [start];
  while (current) {
    while (events.get(++startPosition)[1] !== current) {
    }
    startPositions.push(startPosition);
    if (!current._tokenizer) {
      stream = context.sliceStream(current);
      if (!current.next) {
        stream.push(null);
      }
      if (previous2) {
        tokenizer.defineSkip(current.start);
      }
      if (current._isInFirstContentOfListItem) {
        tokenizer._gfmTasklistFirstContentOfListItem = true;
      }
      tokenizer.write(stream);
      if (current._isInFirstContentOfListItem) {
        tokenizer._gfmTasklistFirstContentOfListItem = void 0;
      }
    }
    previous2 = current;
    current = current.next;
  }
  current = token;
  while (++index2 < childEvents.length) {
    if (
      // Find a void token that includes a break.
      childEvents[index2][0] === "exit" && childEvents[index2 - 1][0] === "enter" && childEvents[index2][1].type === childEvents[index2 - 1][1].type && childEvents[index2][1].start.line !== childEvents[index2][1].end.line
    ) {
      start = index2 + 1;
      breaks.push(start);
      current._tokenizer = void 0;
      current.previous = void 0;
      current = current.next;
    }
  }
  tokenizer.events = [];
  if (current) {
    current._tokenizer = void 0;
    current.previous = void 0;
  } else {
    breaks.pop();
  }
  index2 = breaks.length;
  while (index2--) {
    const slice = childEvents.slice(breaks[index2], breaks[index2 + 1]);
    const start2 = startPositions.pop();
    jumps.push([start2, start2 + slice.length - 1]);
    events.splice(start2, 2, slice);
  }
  jumps.reverse();
  index2 = -1;
  while (++index2 < jumps.length) {
    gaps[adjust + jumps[index2][0]] = adjust + jumps[index2][1];
    adjust += jumps[index2][1] - jumps[index2][0] - 1;
  }
  return gaps;
}
const content = {
  resolve: resolveContent,
  tokenize: tokenizeContent
};
const continuationConstruct = {
  partial: true,
  tokenize: tokenizeContinuation
};
function resolveContent(events) {
  subtokenize(events);
  return events;
}
function tokenizeContent(effects, ok2) {
  let previous2;
  return chunkStart;
  function chunkStart(code2) {
    effects.enter("content");
    previous2 = effects.enter("chunkContent", {
      contentType: "content"
    });
    return chunkInside(code2);
  }
  function chunkInside(code2) {
    if (code2 === null) {
      return contentEnd(code2);
    }
    if (markdownLineEnding(code2)) {
      return effects.check(continuationConstruct, contentContinue, contentEnd)(code2);
    }
    effects.consume(code2);
    return chunkInside;
  }
  function contentEnd(code2) {
    effects.exit("chunkContent");
    effects.exit("content");
    return ok2(code2);
  }
  function contentContinue(code2) {
    effects.consume(code2);
    effects.exit("chunkContent");
    previous2.next = effects.enter("chunkContent", {
      contentType: "content",
      previous: previous2
    });
    previous2 = previous2.next;
    return chunkInside;
  }
}
function tokenizeContinuation(effects, ok2, nok) {
  const self2 = this;
  return startLookahead;
  function startLookahead(code2) {
    effects.exit("chunkContent");
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return factorySpace(effects, prefixed, "linePrefix");
  }
  function prefixed(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return nok(code2);
    }
    const tail = self2.events[self2.events.length - 1];
    if (!self2.parser.constructs.disable.null.includes("codeIndented") && tail && tail[1].type === "linePrefix" && tail[2].sliceSerialize(tail[1], true).length >= 4) {
      return ok2(code2);
    }
    return effects.interrupt(self2.parser.constructs.flow, nok, ok2)(code2);
  }
}
function factoryDestination(effects, ok2, nok, type, literalType, literalMarkerType, rawType, stringType, max2) {
  const limit = max2 || Number.POSITIVE_INFINITY;
  let balance = 0;
  return start;
  function start(code2) {
    if (code2 === 60) {
      effects.enter(type);
      effects.enter(literalType);
      effects.enter(literalMarkerType);
      effects.consume(code2);
      effects.exit(literalMarkerType);
      return enclosedBefore;
    }
    if (code2 === null || code2 === 32 || code2 === 41 || asciiControl(code2)) {
      return nok(code2);
    }
    effects.enter(type);
    effects.enter(rawType);
    effects.enter(stringType);
    effects.enter("chunkString", {
      contentType: "string"
    });
    return raw(code2);
  }
  function enclosedBefore(code2) {
    if (code2 === 62) {
      effects.enter(literalMarkerType);
      effects.consume(code2);
      effects.exit(literalMarkerType);
      effects.exit(literalType);
      effects.exit(type);
      return ok2;
    }
    effects.enter(stringType);
    effects.enter("chunkString", {
      contentType: "string"
    });
    return enclosed(code2);
  }
  function enclosed(code2) {
    if (code2 === 62) {
      effects.exit("chunkString");
      effects.exit(stringType);
      return enclosedBefore(code2);
    }
    if (code2 === null || code2 === 60 || markdownLineEnding(code2)) {
      return nok(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? enclosedEscape : enclosed;
  }
  function enclosedEscape(code2) {
    if (code2 === 60 || code2 === 62 || code2 === 92) {
      effects.consume(code2);
      return enclosed;
    }
    return enclosed(code2);
  }
  function raw(code2) {
    if (!balance && (code2 === null || code2 === 41 || markdownLineEndingOrSpace(code2))) {
      effects.exit("chunkString");
      effects.exit(stringType);
      effects.exit(rawType);
      effects.exit(type);
      return ok2(code2);
    }
    if (balance < limit && code2 === 40) {
      effects.consume(code2);
      balance++;
      return raw;
    }
    if (code2 === 41) {
      effects.consume(code2);
      balance--;
      return raw;
    }
    if (code2 === null || code2 === 32 || code2 === 40 || asciiControl(code2)) {
      return nok(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? rawEscape : raw;
  }
  function rawEscape(code2) {
    if (code2 === 40 || code2 === 41 || code2 === 92) {
      effects.consume(code2);
      return raw;
    }
    return raw(code2);
  }
}
function factoryLabel(effects, ok2, nok, type, markerType, stringType) {
  const self2 = this;
  let size2 = 0;
  let seen;
  return start;
  function start(code2) {
    effects.enter(type);
    effects.enter(markerType);
    effects.consume(code2);
    effects.exit(markerType);
    effects.enter(stringType);
    return atBreak;
  }
  function atBreak(code2) {
    if (size2 > 999 || code2 === null || code2 === 91 || code2 === 93 && !seen || // To do: remove in the future once we’ve switched from
    // `micromark-extension-footnote` to `micromark-extension-gfm-footnote`,
    // which doesn’t need this.
    // Hidden footnotes hook.
    /* c8 ignore next 3 */
    code2 === 94 && !size2 && "_hiddenFootnoteSupport" in self2.parser.constructs) {
      return nok(code2);
    }
    if (code2 === 93) {
      effects.exit(stringType);
      effects.enter(markerType);
      effects.consume(code2);
      effects.exit(markerType);
      effects.exit(type);
      return ok2;
    }
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return atBreak;
    }
    effects.enter("chunkString", {
      contentType: "string"
    });
    return labelInside(code2);
  }
  function labelInside(code2) {
    if (code2 === null || code2 === 91 || code2 === 93 || markdownLineEnding(code2) || size2++ > 999) {
      effects.exit("chunkString");
      return atBreak(code2);
    }
    effects.consume(code2);
    if (!seen) seen = !markdownSpace(code2);
    return code2 === 92 ? labelEscape : labelInside;
  }
  function labelEscape(code2) {
    if (code2 === 91 || code2 === 92 || code2 === 93) {
      effects.consume(code2);
      size2++;
      return labelInside;
    }
    return labelInside(code2);
  }
}
function factoryTitle(effects, ok2, nok, type, markerType, stringType) {
  let marker;
  return start;
  function start(code2) {
    if (code2 === 34 || code2 === 39 || code2 === 40) {
      effects.enter(type);
      effects.enter(markerType);
      effects.consume(code2);
      effects.exit(markerType);
      marker = code2 === 40 ? 41 : code2;
      return begin;
    }
    return nok(code2);
  }
  function begin(code2) {
    if (code2 === marker) {
      effects.enter(markerType);
      effects.consume(code2);
      effects.exit(markerType);
      effects.exit(type);
      return ok2;
    }
    effects.enter(stringType);
    return atBreak(code2);
  }
  function atBreak(code2) {
    if (code2 === marker) {
      effects.exit(stringType);
      return begin(marker);
    }
    if (code2 === null) {
      return nok(code2);
    }
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return factorySpace(effects, atBreak, "linePrefix");
    }
    effects.enter("chunkString", {
      contentType: "string"
    });
    return inside(code2);
  }
  function inside(code2) {
    if (code2 === marker || code2 === null || markdownLineEnding(code2)) {
      effects.exit("chunkString");
      return atBreak(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? escape : inside;
  }
  function escape(code2) {
    if (code2 === marker || code2 === 92) {
      effects.consume(code2);
      return inside;
    }
    return inside(code2);
  }
}
function factoryWhitespace(effects, ok2) {
  let seen;
  return start;
  function start(code2) {
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      seen = true;
      return start;
    }
    if (markdownSpace(code2)) {
      return factorySpace(effects, start, seen ? "linePrefix" : "lineSuffix")(code2);
    }
    return ok2(code2);
  }
}
const definition$1 = {
  name: "definition",
  tokenize: tokenizeDefinition
};
const titleBefore = {
  partial: true,
  tokenize: tokenizeTitleBefore
};
function tokenizeDefinition(effects, ok2, nok) {
  const self2 = this;
  let identifier;
  return start;
  function start(code2) {
    effects.enter("definition");
    return before(code2);
  }
  function before(code2) {
    return factoryLabel.call(
      self2,
      effects,
      labelAfter,
      // Note: we don’t need to reset the way `markdown-rs` does.
      nok,
      "definitionLabel",
      "definitionLabelMarker",
      "definitionLabelString"
    )(code2);
  }
  function labelAfter(code2) {
    identifier = normalizeIdentifier(self2.sliceSerialize(self2.events[self2.events.length - 1][1]).slice(1, -1));
    if (code2 === 58) {
      effects.enter("definitionMarker");
      effects.consume(code2);
      effects.exit("definitionMarker");
      return markerAfter;
    }
    return nok(code2);
  }
  function markerAfter(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, destinationBefore)(code2) : destinationBefore(code2);
  }
  function destinationBefore(code2) {
    return factoryDestination(
      effects,
      destinationAfter,
      // Note: we don’t need to reset the way `markdown-rs` does.
      nok,
      "definitionDestination",
      "definitionDestinationLiteral",
      "definitionDestinationLiteralMarker",
      "definitionDestinationRaw",
      "definitionDestinationString"
    )(code2);
  }
  function destinationAfter(code2) {
    return effects.attempt(titleBefore, after, after)(code2);
  }
  function after(code2) {
    return markdownSpace(code2) ? factorySpace(effects, afterWhitespace, "whitespace")(code2) : afterWhitespace(code2);
  }
  function afterWhitespace(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("definition");
      self2.parser.defined.push(identifier);
      return ok2(code2);
    }
    return nok(code2);
  }
}
function tokenizeTitleBefore(effects, ok2, nok) {
  return titleBefore2;
  function titleBefore2(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, beforeMarker)(code2) : nok(code2);
  }
  function beforeMarker(code2) {
    return factoryTitle(effects, titleAfter, nok, "definitionTitle", "definitionTitleMarker", "definitionTitleString")(code2);
  }
  function titleAfter(code2) {
    return markdownSpace(code2) ? factorySpace(effects, titleAfterOptionalWhitespace, "whitespace")(code2) : titleAfterOptionalWhitespace(code2);
  }
  function titleAfterOptionalWhitespace(code2) {
    return code2 === null || markdownLineEnding(code2) ? ok2(code2) : nok(code2);
  }
}
const hardBreakEscape = {
  name: "hardBreakEscape",
  tokenize: tokenizeHardBreakEscape
};
function tokenizeHardBreakEscape(effects, ok2, nok) {
  return start;
  function start(code2) {
    effects.enter("hardBreakEscape");
    effects.consume(code2);
    return after;
  }
  function after(code2) {
    if (markdownLineEnding(code2)) {
      effects.exit("hardBreakEscape");
      return ok2(code2);
    }
    return nok(code2);
  }
}
const headingAtx = {
  name: "headingAtx",
  resolve: resolveHeadingAtx,
  tokenize: tokenizeHeadingAtx
};
function resolveHeadingAtx(events, context) {
  let contentEnd = events.length - 2;
  let contentStart = 3;
  let content2;
  let text2;
  if (events[contentStart][1].type === "whitespace") {
    contentStart += 2;
  }
  if (contentEnd - 2 > contentStart && events[contentEnd][1].type === "whitespace") {
    contentEnd -= 2;
  }
  if (events[contentEnd][1].type === "atxHeadingSequence" && (contentStart === contentEnd - 1 || contentEnd - 4 > contentStart && events[contentEnd - 2][1].type === "whitespace")) {
    contentEnd -= contentStart + 1 === contentEnd ? 2 : 4;
  }
  if (contentEnd > contentStart) {
    content2 = {
      type: "atxHeadingText",
      start: events[contentStart][1].start,
      end: events[contentEnd][1].end
    };
    text2 = {
      type: "chunkText",
      start: events[contentStart][1].start,
      end: events[contentEnd][1].end,
      contentType: "text"
    };
    splice(events, contentStart, contentEnd - contentStart + 1, [["enter", content2, context], ["enter", text2, context], ["exit", text2, context], ["exit", content2, context]]);
  }
  return events;
}
function tokenizeHeadingAtx(effects, ok2, nok) {
  let size2 = 0;
  return start;
  function start(code2) {
    effects.enter("atxHeading");
    return before(code2);
  }
  function before(code2) {
    effects.enter("atxHeadingSequence");
    return sequenceOpen(code2);
  }
  function sequenceOpen(code2) {
    if (code2 === 35 && size2++ < 6) {
      effects.consume(code2);
      return sequenceOpen;
    }
    if (code2 === null || markdownLineEndingOrSpace(code2)) {
      effects.exit("atxHeadingSequence");
      return atBreak(code2);
    }
    return nok(code2);
  }
  function atBreak(code2) {
    if (code2 === 35) {
      effects.enter("atxHeadingSequence");
      return sequenceFurther(code2);
    }
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("atxHeading");
      return ok2(code2);
    }
    if (markdownSpace(code2)) {
      return factorySpace(effects, atBreak, "whitespace")(code2);
    }
    effects.enter("atxHeadingText");
    return data(code2);
  }
  function sequenceFurther(code2) {
    if (code2 === 35) {
      effects.consume(code2);
      return sequenceFurther;
    }
    effects.exit("atxHeadingSequence");
    return atBreak(code2);
  }
  function data(code2) {
    if (code2 === null || code2 === 35 || markdownLineEndingOrSpace(code2)) {
      effects.exit("atxHeadingText");
      return atBreak(code2);
    }
    effects.consume(code2);
    return data;
  }
}
const htmlBlockNames = [
  "address",
  "article",
  "aside",
  "base",
  "basefont",
  "blockquote",
  "body",
  "caption",
  "center",
  "col",
  "colgroup",
  "dd",
  "details",
  "dialog",
  "dir",
  "div",
  "dl",
  "dt",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "frame",
  "frameset",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "head",
  "header",
  "hr",
  "html",
  "iframe",
  "legend",
  "li",
  "link",
  "main",
  "menu",
  "menuitem",
  "nav",
  "noframes",
  "ol",
  "optgroup",
  "option",
  "p",
  "param",
  "search",
  "section",
  "summary",
  "table",
  "tbody",
  "td",
  "tfoot",
  "th",
  "thead",
  "title",
  "tr",
  "track",
  "ul"
];
const htmlRawNames = ["pre", "script", "style", "textarea"];
const htmlFlow = {
  concrete: true,
  name: "htmlFlow",
  resolveTo: resolveToHtmlFlow,
  tokenize: tokenizeHtmlFlow
};
const blankLineBefore = {
  partial: true,
  tokenize: tokenizeBlankLineBefore
};
const nonLazyContinuationStart = {
  partial: true,
  tokenize: tokenizeNonLazyContinuationStart
};
function resolveToHtmlFlow(events) {
  let index2 = events.length;
  while (index2--) {
    if (events[index2][0] === "enter" && events[index2][1].type === "htmlFlow") {
      break;
    }
  }
  if (index2 > 1 && events[index2 - 2][1].type === "linePrefix") {
    events[index2][1].start = events[index2 - 2][1].start;
    events[index2 + 1][1].start = events[index2 - 2][1].start;
    events.splice(index2 - 2, 2);
  }
  return events;
}
function tokenizeHtmlFlow(effects, ok2, nok) {
  const self2 = this;
  let marker;
  let closingTag;
  let buffer;
  let index2;
  let markerB;
  return start;
  function start(code2) {
    return before(code2);
  }
  function before(code2) {
    effects.enter("htmlFlow");
    effects.enter("htmlFlowData");
    effects.consume(code2);
    return open;
  }
  function open(code2) {
    if (code2 === 33) {
      effects.consume(code2);
      return declarationOpen;
    }
    if (code2 === 47) {
      effects.consume(code2);
      closingTag = true;
      return tagCloseStart;
    }
    if (code2 === 63) {
      effects.consume(code2);
      marker = 3;
      return self2.interrupt ? ok2 : continuationDeclarationInside;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      buffer = String.fromCharCode(code2);
      return tagName;
    }
    return nok(code2);
  }
  function declarationOpen(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      marker = 2;
      return commentOpenInside;
    }
    if (code2 === 91) {
      effects.consume(code2);
      marker = 5;
      index2 = 0;
      return cdataOpenInside;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      marker = 4;
      return self2.interrupt ? ok2 : continuationDeclarationInside;
    }
    return nok(code2);
  }
  function commentOpenInside(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return self2.interrupt ? ok2 : continuationDeclarationInside;
    }
    return nok(code2);
  }
  function cdataOpenInside(code2) {
    const value = "CDATA[";
    if (code2 === value.charCodeAt(index2++)) {
      effects.consume(code2);
      if (index2 === value.length) {
        return self2.interrupt ? ok2 : continuation;
      }
      return cdataOpenInside;
    }
    return nok(code2);
  }
  function tagCloseStart(code2) {
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      buffer = String.fromCharCode(code2);
      return tagName;
    }
    return nok(code2);
  }
  function tagName(code2) {
    if (code2 === null || code2 === 47 || code2 === 62 || markdownLineEndingOrSpace(code2)) {
      const slash = code2 === 47;
      const name2 = buffer.toLowerCase();
      if (!slash && !closingTag && htmlRawNames.includes(name2)) {
        marker = 1;
        return self2.interrupt ? ok2(code2) : continuation(code2);
      }
      if (htmlBlockNames.includes(buffer.toLowerCase())) {
        marker = 6;
        if (slash) {
          effects.consume(code2);
          return basicSelfClosing;
        }
        return self2.interrupt ? ok2(code2) : continuation(code2);
      }
      marker = 7;
      return self2.interrupt && !self2.parser.lazy[self2.now().line] ? nok(code2) : closingTag ? completeClosingTagAfter(code2) : completeAttributeNameBefore(code2);
    }
    if (code2 === 45 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      buffer += String.fromCharCode(code2);
      return tagName;
    }
    return nok(code2);
  }
  function basicSelfClosing(code2) {
    if (code2 === 62) {
      effects.consume(code2);
      return self2.interrupt ? ok2 : continuation;
    }
    return nok(code2);
  }
  function completeClosingTagAfter(code2) {
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeClosingTagAfter;
    }
    return completeEnd(code2);
  }
  function completeAttributeNameBefore(code2) {
    if (code2 === 47) {
      effects.consume(code2);
      return completeEnd;
    }
    if (code2 === 58 || code2 === 95 || asciiAlpha(code2)) {
      effects.consume(code2);
      return completeAttributeName;
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeAttributeNameBefore;
    }
    return completeEnd(code2);
  }
  function completeAttributeName(code2) {
    if (code2 === 45 || code2 === 46 || code2 === 58 || code2 === 95 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      return completeAttributeName;
    }
    return completeAttributeNameAfter(code2);
  }
  function completeAttributeNameAfter(code2) {
    if (code2 === 61) {
      effects.consume(code2);
      return completeAttributeValueBefore;
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeAttributeNameAfter;
    }
    return completeAttributeNameBefore(code2);
  }
  function completeAttributeValueBefore(code2) {
    if (code2 === null || code2 === 60 || code2 === 61 || code2 === 62 || code2 === 96) {
      return nok(code2);
    }
    if (code2 === 34 || code2 === 39) {
      effects.consume(code2);
      markerB = code2;
      return completeAttributeValueQuoted;
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeAttributeValueBefore;
    }
    return completeAttributeValueUnquoted(code2);
  }
  function completeAttributeValueQuoted(code2) {
    if (code2 === markerB) {
      effects.consume(code2);
      markerB = null;
      return completeAttributeValueQuotedAfter;
    }
    if (code2 === null || markdownLineEnding(code2)) {
      return nok(code2);
    }
    effects.consume(code2);
    return completeAttributeValueQuoted;
  }
  function completeAttributeValueUnquoted(code2) {
    if (code2 === null || code2 === 34 || code2 === 39 || code2 === 47 || code2 === 60 || code2 === 61 || code2 === 62 || code2 === 96 || markdownLineEndingOrSpace(code2)) {
      return completeAttributeNameAfter(code2);
    }
    effects.consume(code2);
    return completeAttributeValueUnquoted;
  }
  function completeAttributeValueQuotedAfter(code2) {
    if (code2 === 47 || code2 === 62 || markdownSpace(code2)) {
      return completeAttributeNameBefore(code2);
    }
    return nok(code2);
  }
  function completeEnd(code2) {
    if (code2 === 62) {
      effects.consume(code2);
      return completeAfter;
    }
    return nok(code2);
  }
  function completeAfter(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return continuation(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeAfter;
    }
    return nok(code2);
  }
  function continuation(code2) {
    if (code2 === 45 && marker === 2) {
      effects.consume(code2);
      return continuationCommentInside;
    }
    if (code2 === 60 && marker === 1) {
      effects.consume(code2);
      return continuationRawTagOpen;
    }
    if (code2 === 62 && marker === 4) {
      effects.consume(code2);
      return continuationClose;
    }
    if (code2 === 63 && marker === 3) {
      effects.consume(code2);
      return continuationDeclarationInside;
    }
    if (code2 === 93 && marker === 5) {
      effects.consume(code2);
      return continuationCdataInside;
    }
    if (markdownLineEnding(code2) && (marker === 6 || marker === 7)) {
      effects.exit("htmlFlowData");
      return effects.check(blankLineBefore, continuationAfter, continuationStart)(code2);
    }
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("htmlFlowData");
      return continuationStart(code2);
    }
    effects.consume(code2);
    return continuation;
  }
  function continuationStart(code2) {
    return effects.check(nonLazyContinuationStart, continuationStartNonLazy, continuationAfter)(code2);
  }
  function continuationStartNonLazy(code2) {
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return continuationBefore;
  }
  function continuationBefore(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return continuationStart(code2);
    }
    effects.enter("htmlFlowData");
    return continuation(code2);
  }
  function continuationCommentInside(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return continuationDeclarationInside;
    }
    return continuation(code2);
  }
  function continuationRawTagOpen(code2) {
    if (code2 === 47) {
      effects.consume(code2);
      buffer = "";
      return continuationRawEndTag;
    }
    return continuation(code2);
  }
  function continuationRawEndTag(code2) {
    if (code2 === 62) {
      const name2 = buffer.toLowerCase();
      if (htmlRawNames.includes(name2)) {
        effects.consume(code2);
        return continuationClose;
      }
      return continuation(code2);
    }
    if (asciiAlpha(code2) && buffer.length < 8) {
      effects.consume(code2);
      buffer += String.fromCharCode(code2);
      return continuationRawEndTag;
    }
    return continuation(code2);
  }
  function continuationCdataInside(code2) {
    if (code2 === 93) {
      effects.consume(code2);
      return continuationDeclarationInside;
    }
    return continuation(code2);
  }
  function continuationDeclarationInside(code2) {
    if (code2 === 62) {
      effects.consume(code2);
      return continuationClose;
    }
    if (code2 === 45 && marker === 2) {
      effects.consume(code2);
      return continuationDeclarationInside;
    }
    return continuation(code2);
  }
  function continuationClose(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("htmlFlowData");
      return continuationAfter(code2);
    }
    effects.consume(code2);
    return continuationClose;
  }
  function continuationAfter(code2) {
    effects.exit("htmlFlow");
    return ok2(code2);
  }
}
function tokenizeNonLazyContinuationStart(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return after;
    }
    return nok(code2);
  }
  function after(code2) {
    return self2.parser.lazy[self2.now().line] ? nok(code2) : ok2(code2);
  }
}
function tokenizeBlankLineBefore(effects, ok2, nok) {
  return start;
  function start(code2) {
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return effects.attempt(blankLine, ok2, nok);
  }
}
const htmlText = {
  name: "htmlText",
  tokenize: tokenizeHtmlText
};
function tokenizeHtmlText(effects, ok2, nok) {
  const self2 = this;
  let marker;
  let index2;
  let returnState;
  return start;
  function start(code2) {
    effects.enter("htmlText");
    effects.enter("htmlTextData");
    effects.consume(code2);
    return open;
  }
  function open(code2) {
    if (code2 === 33) {
      effects.consume(code2);
      return declarationOpen;
    }
    if (code2 === 47) {
      effects.consume(code2);
      return tagCloseStart;
    }
    if (code2 === 63) {
      effects.consume(code2);
      return instruction;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return tagOpen;
    }
    return nok(code2);
  }
  function declarationOpen(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return commentOpenInside;
    }
    if (code2 === 91) {
      effects.consume(code2);
      index2 = 0;
      return cdataOpenInside;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return declaration;
    }
    return nok(code2);
  }
  function commentOpenInside(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return commentEnd;
    }
    return nok(code2);
  }
  function comment(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (code2 === 45) {
      effects.consume(code2);
      return commentClose;
    }
    if (markdownLineEnding(code2)) {
      returnState = comment;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return comment;
  }
  function commentClose(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return commentEnd;
    }
    return comment(code2);
  }
  function commentEnd(code2) {
    return code2 === 62 ? end(code2) : code2 === 45 ? commentClose(code2) : comment(code2);
  }
  function cdataOpenInside(code2) {
    const value = "CDATA[";
    if (code2 === value.charCodeAt(index2++)) {
      effects.consume(code2);
      return index2 === value.length ? cdata : cdataOpenInside;
    }
    return nok(code2);
  }
  function cdata(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (code2 === 93) {
      effects.consume(code2);
      return cdataClose;
    }
    if (markdownLineEnding(code2)) {
      returnState = cdata;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return cdata;
  }
  function cdataClose(code2) {
    if (code2 === 93) {
      effects.consume(code2);
      return cdataEnd;
    }
    return cdata(code2);
  }
  function cdataEnd(code2) {
    if (code2 === 62) {
      return end(code2);
    }
    if (code2 === 93) {
      effects.consume(code2);
      return cdataEnd;
    }
    return cdata(code2);
  }
  function declaration(code2) {
    if (code2 === null || code2 === 62) {
      return end(code2);
    }
    if (markdownLineEnding(code2)) {
      returnState = declaration;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return declaration;
  }
  function instruction(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (code2 === 63) {
      effects.consume(code2);
      return instructionClose;
    }
    if (markdownLineEnding(code2)) {
      returnState = instruction;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return instruction;
  }
  function instructionClose(code2) {
    return code2 === 62 ? end(code2) : instruction(code2);
  }
  function tagCloseStart(code2) {
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return tagClose;
    }
    return nok(code2);
  }
  function tagClose(code2) {
    if (code2 === 45 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      return tagClose;
    }
    return tagCloseBetween(code2);
  }
  function tagCloseBetween(code2) {
    if (markdownLineEnding(code2)) {
      returnState = tagCloseBetween;
      return lineEndingBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return tagCloseBetween;
    }
    return end(code2);
  }
  function tagOpen(code2) {
    if (code2 === 45 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      return tagOpen;
    }
    if (code2 === 47 || code2 === 62 || markdownLineEndingOrSpace(code2)) {
      return tagOpenBetween(code2);
    }
    return nok(code2);
  }
  function tagOpenBetween(code2) {
    if (code2 === 47) {
      effects.consume(code2);
      return end;
    }
    if (code2 === 58 || code2 === 95 || asciiAlpha(code2)) {
      effects.consume(code2);
      return tagOpenAttributeName;
    }
    if (markdownLineEnding(code2)) {
      returnState = tagOpenBetween;
      return lineEndingBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return tagOpenBetween;
    }
    return end(code2);
  }
  function tagOpenAttributeName(code2) {
    if (code2 === 45 || code2 === 46 || code2 === 58 || code2 === 95 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      return tagOpenAttributeName;
    }
    return tagOpenAttributeNameAfter(code2);
  }
  function tagOpenAttributeNameAfter(code2) {
    if (code2 === 61) {
      effects.consume(code2);
      return tagOpenAttributeValueBefore;
    }
    if (markdownLineEnding(code2)) {
      returnState = tagOpenAttributeNameAfter;
      return lineEndingBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return tagOpenAttributeNameAfter;
    }
    return tagOpenBetween(code2);
  }
  function tagOpenAttributeValueBefore(code2) {
    if (code2 === null || code2 === 60 || code2 === 61 || code2 === 62 || code2 === 96) {
      return nok(code2);
    }
    if (code2 === 34 || code2 === 39) {
      effects.consume(code2);
      marker = code2;
      return tagOpenAttributeValueQuoted;
    }
    if (markdownLineEnding(code2)) {
      returnState = tagOpenAttributeValueBefore;
      return lineEndingBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return tagOpenAttributeValueBefore;
    }
    effects.consume(code2);
    return tagOpenAttributeValueUnquoted;
  }
  function tagOpenAttributeValueQuoted(code2) {
    if (code2 === marker) {
      effects.consume(code2);
      marker = void 0;
      return tagOpenAttributeValueQuotedAfter;
    }
    if (code2 === null) {
      return nok(code2);
    }
    if (markdownLineEnding(code2)) {
      returnState = tagOpenAttributeValueQuoted;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return tagOpenAttributeValueQuoted;
  }
  function tagOpenAttributeValueUnquoted(code2) {
    if (code2 === null || code2 === 34 || code2 === 39 || code2 === 60 || code2 === 61 || code2 === 96) {
      return nok(code2);
    }
    if (code2 === 47 || code2 === 62 || markdownLineEndingOrSpace(code2)) {
      return tagOpenBetween(code2);
    }
    effects.consume(code2);
    return tagOpenAttributeValueUnquoted;
  }
  function tagOpenAttributeValueQuotedAfter(code2) {
    if (code2 === 47 || code2 === 62 || markdownLineEndingOrSpace(code2)) {
      return tagOpenBetween(code2);
    }
    return nok(code2);
  }
  function end(code2) {
    if (code2 === 62) {
      effects.consume(code2);
      effects.exit("htmlTextData");
      effects.exit("htmlText");
      return ok2;
    }
    return nok(code2);
  }
  function lineEndingBefore(code2) {
    effects.exit("htmlTextData");
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return lineEndingAfter;
  }
  function lineEndingAfter(code2) {
    return markdownSpace(code2) ? factorySpace(effects, lineEndingAfterPrefix, "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2) : lineEndingAfterPrefix(code2);
  }
  function lineEndingAfterPrefix(code2) {
    effects.enter("htmlTextData");
    return returnState(code2);
  }
}
const labelEnd = {
  name: "labelEnd",
  resolveAll: resolveAllLabelEnd,
  resolveTo: resolveToLabelEnd,
  tokenize: tokenizeLabelEnd
};
const resourceConstruct = {
  tokenize: tokenizeResource
};
const referenceFullConstruct = {
  tokenize: tokenizeReferenceFull
};
const referenceCollapsedConstruct = {
  tokenize: tokenizeReferenceCollapsed
};
function resolveAllLabelEnd(events) {
  let index2 = -1;
  const newEvents = [];
  while (++index2 < events.length) {
    const token = events[index2][1];
    newEvents.push(events[index2]);
    if (token.type === "labelImage" || token.type === "labelLink" || token.type === "labelEnd") {
      const offset2 = token.type === "labelImage" ? 4 : 2;
      token.type = "data";
      index2 += offset2;
    }
  }
  if (events.length !== newEvents.length) {
    splice(events, 0, events.length, newEvents);
  }
  return events;
}
function resolveToLabelEnd(events, context) {
  let index2 = events.length;
  let offset2 = 0;
  let token;
  let open;
  let close;
  let media;
  while (index2--) {
    token = events[index2][1];
    if (open) {
      if (token.type === "link" || token.type === "labelLink" && token._inactive) {
        break;
      }
      if (events[index2][0] === "enter" && token.type === "labelLink") {
        token._inactive = true;
      }
    } else if (close) {
      if (events[index2][0] === "enter" && (token.type === "labelImage" || token.type === "labelLink") && !token._balanced) {
        open = index2;
        if (token.type !== "labelLink") {
          offset2 = 2;
          break;
        }
      }
    } else if (token.type === "labelEnd") {
      close = index2;
    }
  }
  const group = {
    type: events[open][1].type === "labelLink" ? "link" : "image",
    start: {
      ...events[open][1].start
    },
    end: {
      ...events[events.length - 1][1].end
    }
  };
  const label = {
    type: "label",
    start: {
      ...events[open][1].start
    },
    end: {
      ...events[close][1].end
    }
  };
  const text2 = {
    type: "labelText",
    start: {
      ...events[open + offset2 + 2][1].end
    },
    end: {
      ...events[close - 2][1].start
    }
  };
  media = [["enter", group, context], ["enter", label, context]];
  media = push(media, events.slice(open + 1, open + offset2 + 3));
  media = push(media, [["enter", text2, context]]);
  media = push(media, resolveAll(context.parser.constructs.insideSpan.null, events.slice(open + offset2 + 4, close - 3), context));
  media = push(media, [["exit", text2, context], events[close - 2], events[close - 1], ["exit", label, context]]);
  media = push(media, events.slice(close + 1));
  media = push(media, [["exit", group, context]]);
  splice(events, open, events.length, media);
  return events;
}
function tokenizeLabelEnd(effects, ok2, nok) {
  const self2 = this;
  let index2 = self2.events.length;
  let labelStart;
  let defined;
  while (index2--) {
    if ((self2.events[index2][1].type === "labelImage" || self2.events[index2][1].type === "labelLink") && !self2.events[index2][1]._balanced) {
      labelStart = self2.events[index2][1];
      break;
    }
  }
  return start;
  function start(code2) {
    if (!labelStart) {
      return nok(code2);
    }
    if (labelStart._inactive) {
      return labelEndNok(code2);
    }
    defined = self2.parser.defined.includes(normalizeIdentifier(self2.sliceSerialize({
      start: labelStart.end,
      end: self2.now()
    })));
    effects.enter("labelEnd");
    effects.enter("labelMarker");
    effects.consume(code2);
    effects.exit("labelMarker");
    effects.exit("labelEnd");
    return after;
  }
  function after(code2) {
    if (code2 === 40) {
      return effects.attempt(resourceConstruct, labelEndOk, defined ? labelEndOk : labelEndNok)(code2);
    }
    if (code2 === 91) {
      return effects.attempt(referenceFullConstruct, labelEndOk, defined ? referenceNotFull : labelEndNok)(code2);
    }
    return defined ? labelEndOk(code2) : labelEndNok(code2);
  }
  function referenceNotFull(code2) {
    return effects.attempt(referenceCollapsedConstruct, labelEndOk, labelEndNok)(code2);
  }
  function labelEndOk(code2) {
    return ok2(code2);
  }
  function labelEndNok(code2) {
    labelStart._balanced = true;
    return nok(code2);
  }
}
function tokenizeResource(effects, ok2, nok) {
  return resourceStart;
  function resourceStart(code2) {
    effects.enter("resource");
    effects.enter("resourceMarker");
    effects.consume(code2);
    effects.exit("resourceMarker");
    return resourceBefore;
  }
  function resourceBefore(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, resourceOpen)(code2) : resourceOpen(code2);
  }
  function resourceOpen(code2) {
    if (code2 === 41) {
      return resourceEnd(code2);
    }
    return factoryDestination(effects, resourceDestinationAfter, resourceDestinationMissing, "resourceDestination", "resourceDestinationLiteral", "resourceDestinationLiteralMarker", "resourceDestinationRaw", "resourceDestinationString", 32)(code2);
  }
  function resourceDestinationAfter(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, resourceBetween)(code2) : resourceEnd(code2);
  }
  function resourceDestinationMissing(code2) {
    return nok(code2);
  }
  function resourceBetween(code2) {
    if (code2 === 34 || code2 === 39 || code2 === 40) {
      return factoryTitle(effects, resourceTitleAfter, nok, "resourceTitle", "resourceTitleMarker", "resourceTitleString")(code2);
    }
    return resourceEnd(code2);
  }
  function resourceTitleAfter(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, resourceEnd)(code2) : resourceEnd(code2);
  }
  function resourceEnd(code2) {
    if (code2 === 41) {
      effects.enter("resourceMarker");
      effects.consume(code2);
      effects.exit("resourceMarker");
      effects.exit("resource");
      return ok2;
    }
    return nok(code2);
  }
}
function tokenizeReferenceFull(effects, ok2, nok) {
  const self2 = this;
  return referenceFull;
  function referenceFull(code2) {
    return factoryLabel.call(self2, effects, referenceFullAfter, referenceFullMissing, "reference", "referenceMarker", "referenceString")(code2);
  }
  function referenceFullAfter(code2) {
    return self2.parser.defined.includes(normalizeIdentifier(self2.sliceSerialize(self2.events[self2.events.length - 1][1]).slice(1, -1))) ? ok2(code2) : nok(code2);
  }
  function referenceFullMissing(code2) {
    return nok(code2);
  }
}
function tokenizeReferenceCollapsed(effects, ok2, nok) {
  return referenceCollapsedStart;
  function referenceCollapsedStart(code2) {
    effects.enter("reference");
    effects.enter("referenceMarker");
    effects.consume(code2);
    effects.exit("referenceMarker");
    return referenceCollapsedOpen;
  }
  function referenceCollapsedOpen(code2) {
    if (code2 === 93) {
      effects.enter("referenceMarker");
      effects.consume(code2);
      effects.exit("referenceMarker");
      effects.exit("reference");
      return ok2;
    }
    return nok(code2);
  }
}
const labelStartImage = {
  name: "labelStartImage",
  resolveAll: labelEnd.resolveAll,
  tokenize: tokenizeLabelStartImage
};
function tokenizeLabelStartImage(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    effects.enter("labelImage");
    effects.enter("labelImageMarker");
    effects.consume(code2);
    effects.exit("labelImageMarker");
    return open;
  }
  function open(code2) {
    if (code2 === 91) {
      effects.enter("labelMarker");
      effects.consume(code2);
      effects.exit("labelMarker");
      effects.exit("labelImage");
      return after;
    }
    return nok(code2);
  }
  function after(code2) {
    return code2 === 94 && "_hiddenFootnoteSupport" in self2.parser.constructs ? nok(code2) : ok2(code2);
  }
}
const labelStartLink = {
  name: "labelStartLink",
  resolveAll: labelEnd.resolveAll,
  tokenize: tokenizeLabelStartLink
};
function tokenizeLabelStartLink(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    effects.enter("labelLink");
    effects.enter("labelMarker");
    effects.consume(code2);
    effects.exit("labelMarker");
    effects.exit("labelLink");
    return after;
  }
  function after(code2) {
    return code2 === 94 && "_hiddenFootnoteSupport" in self2.parser.constructs ? nok(code2) : ok2(code2);
  }
}
const lineEnding = {
  name: "lineEnding",
  tokenize: tokenizeLineEnding
};
function tokenizeLineEnding(effects, ok2) {
  return start;
  function start(code2) {
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return factorySpace(effects, ok2, "linePrefix");
  }
}
const thematicBreak$2 = {
  name: "thematicBreak",
  tokenize: tokenizeThematicBreak
};
function tokenizeThematicBreak(effects, ok2, nok) {
  let size2 = 0;
  let marker;
  return start;
  function start(code2) {
    effects.enter("thematicBreak");
    return before(code2);
  }
  function before(code2) {
    marker = code2;
    return atBreak(code2);
  }
  function atBreak(code2) {
    if (code2 === marker) {
      effects.enter("thematicBreakSequence");
      return sequence(code2);
    }
    if (size2 >= 3 && (code2 === null || markdownLineEnding(code2))) {
      effects.exit("thematicBreak");
      return ok2(code2);
    }
    return nok(code2);
  }
  function sequence(code2) {
    if (code2 === marker) {
      effects.consume(code2);
      size2++;
      return sequence;
    }
    effects.exit("thematicBreakSequence");
    return markdownSpace(code2) ? factorySpace(effects, atBreak, "whitespace")(code2) : atBreak(code2);
  }
}
const list$2 = {
  continuation: {
    tokenize: tokenizeListContinuation
  },
  exit: tokenizeListEnd,
  name: "list",
  tokenize: tokenizeListStart
};
const listItemPrefixWhitespaceConstruct = {
  partial: true,
  tokenize: tokenizeListItemPrefixWhitespace
};
const indentConstruct = {
  partial: true,
  tokenize: tokenizeIndent$1
};
function tokenizeListStart(effects, ok2, nok) {
  const self2 = this;
  const tail = self2.events[self2.events.length - 1];
  let initialSize = tail && tail[1].type === "linePrefix" ? tail[2].sliceSerialize(tail[1], true).length : 0;
  let size2 = 0;
  return start;
  function start(code2) {
    const kind = self2.containerState.type || (code2 === 42 || code2 === 43 || code2 === 45 ? "listUnordered" : "listOrdered");
    if (kind === "listUnordered" ? !self2.containerState.marker || code2 === self2.containerState.marker : asciiDigit(code2)) {
      if (!self2.containerState.type) {
        self2.containerState.type = kind;
        effects.enter(kind, {
          _container: true
        });
      }
      if (kind === "listUnordered") {
        effects.enter("listItemPrefix");
        return code2 === 42 || code2 === 45 ? effects.check(thematicBreak$2, nok, atMarker)(code2) : atMarker(code2);
      }
      if (!self2.interrupt || code2 === 49) {
        effects.enter("listItemPrefix");
        effects.enter("listItemValue");
        return inside(code2);
      }
    }
    return nok(code2);
  }
  function inside(code2) {
    if (asciiDigit(code2) && ++size2 < 10) {
      effects.consume(code2);
      return inside;
    }
    if ((!self2.interrupt || size2 < 2) && (self2.containerState.marker ? code2 === self2.containerState.marker : code2 === 41 || code2 === 46)) {
      effects.exit("listItemValue");
      return atMarker(code2);
    }
    return nok(code2);
  }
  function atMarker(code2) {
    effects.enter("listItemMarker");
    effects.consume(code2);
    effects.exit("listItemMarker");
    self2.containerState.marker = self2.containerState.marker || code2;
    return effects.check(
      blankLine,
      // Can’t be empty when interrupting.
      self2.interrupt ? nok : onBlank,
      effects.attempt(listItemPrefixWhitespaceConstruct, endOfPrefix, otherPrefix)
    );
  }
  function onBlank(code2) {
    self2.containerState.initialBlankLine = true;
    initialSize++;
    return endOfPrefix(code2);
  }
  function otherPrefix(code2) {
    if (markdownSpace(code2)) {
      effects.enter("listItemPrefixWhitespace");
      effects.consume(code2);
      effects.exit("listItemPrefixWhitespace");
      return endOfPrefix;
    }
    return nok(code2);
  }
  function endOfPrefix(code2) {
    self2.containerState.size = initialSize + self2.sliceSerialize(effects.exit("listItemPrefix"), true).length;
    return ok2(code2);
  }
}
function tokenizeListContinuation(effects, ok2, nok) {
  const self2 = this;
  self2.containerState._closeFlow = void 0;
  return effects.check(blankLine, onBlank, notBlank);
  function onBlank(code2) {
    self2.containerState.furtherBlankLines = self2.containerState.furtherBlankLines || self2.containerState.initialBlankLine;
    return factorySpace(effects, ok2, "listItemIndent", self2.containerState.size + 1)(code2);
  }
  function notBlank(code2) {
    if (self2.containerState.furtherBlankLines || !markdownSpace(code2)) {
      self2.containerState.furtherBlankLines = void 0;
      self2.containerState.initialBlankLine = void 0;
      return notInCurrentItem(code2);
    }
    self2.containerState.furtherBlankLines = void 0;
    self2.containerState.initialBlankLine = void 0;
    return effects.attempt(indentConstruct, ok2, notInCurrentItem)(code2);
  }
  function notInCurrentItem(code2) {
    self2.containerState._closeFlow = true;
    self2.interrupt = void 0;
    return factorySpace(effects, effects.attempt(list$2, ok2, nok), "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2);
  }
}
function tokenizeIndent$1(effects, ok2, nok) {
  const self2 = this;
  return factorySpace(effects, afterPrefix, "listItemIndent", self2.containerState.size + 1);
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === "listItemIndent" && tail[2].sliceSerialize(tail[1], true).length === self2.containerState.size ? ok2(code2) : nok(code2);
  }
}
function tokenizeListEnd(effects) {
  effects.exit(this.containerState.type);
}
function tokenizeListItemPrefixWhitespace(effects, ok2, nok) {
  const self2 = this;
  return factorySpace(effects, afterPrefix, "listItemPrefixWhitespace", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4 + 1);
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return !markdownSpace(code2) && tail && tail[1].type === "listItemPrefixWhitespace" ? ok2(code2) : nok(code2);
  }
}
const setextUnderline = {
  name: "setextUnderline",
  resolveTo: resolveToSetextUnderline,
  tokenize: tokenizeSetextUnderline
};
function resolveToSetextUnderline(events, context) {
  let index2 = events.length;
  let content2;
  let text2;
  let definition2;
  while (index2--) {
    if (events[index2][0] === "enter") {
      if (events[index2][1].type === "content") {
        content2 = index2;
        break;
      }
      if (events[index2][1].type === "paragraph") {
        text2 = index2;
      }
    } else {
      if (events[index2][1].type === "content") {
        events.splice(index2, 1);
      }
      if (!definition2 && events[index2][1].type === "definition") {
        definition2 = index2;
      }
    }
  }
  const heading2 = {
    type: "setextHeading",
    start: {
      ...events[content2][1].start
    },
    end: {
      ...events[events.length - 1][1].end
    }
  };
  events[text2][1].type = "setextHeadingText";
  if (definition2) {
    events.splice(text2, 0, ["enter", heading2, context]);
    events.splice(definition2 + 1, 0, ["exit", events[content2][1], context]);
    events[content2][1].end = {
      ...events[definition2][1].end
    };
  } else {
    events[content2][1] = heading2;
  }
  events.push(["exit", heading2, context]);
  return events;
}
function tokenizeSetextUnderline(effects, ok2, nok) {
  const self2 = this;
  let marker;
  return start;
  function start(code2) {
    let index2 = self2.events.length;
    let paragraph2;
    while (index2--) {
      if (self2.events[index2][1].type !== "lineEnding" && self2.events[index2][1].type !== "linePrefix" && self2.events[index2][1].type !== "content") {
        paragraph2 = self2.events[index2][1].type === "paragraph";
        break;
      }
    }
    if (!self2.parser.lazy[self2.now().line] && (self2.interrupt || paragraph2)) {
      effects.enter("setextHeadingLine");
      marker = code2;
      return before(code2);
    }
    return nok(code2);
  }
  function before(code2) {
    effects.enter("setextHeadingLineSequence");
    return inside(code2);
  }
  function inside(code2) {
    if (code2 === marker) {
      effects.consume(code2);
      return inside;
    }
    effects.exit("setextHeadingLineSequence");
    return markdownSpace(code2) ? factorySpace(effects, after, "lineSuffix")(code2) : after(code2);
  }
  function after(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("setextHeadingLine");
      return ok2(code2);
    }
    return nok(code2);
  }
}
const flow$1 = {
  tokenize: initializeFlow
};
function initializeFlow(effects) {
  const self2 = this;
  const initial = effects.attempt(
    // Try to parse a blank line.
    blankLine,
    atBlankEnding,
    // Try to parse initial flow (essentially, only code).
    effects.attempt(this.parser.constructs.flowInitial, afterConstruct, factorySpace(effects, effects.attempt(this.parser.constructs.flow, afterConstruct, effects.attempt(content, afterConstruct)), "linePrefix"))
  );
  return initial;
  function atBlankEnding(code2) {
    if (code2 === null) {
      effects.consume(code2);
      return;
    }
    effects.enter("lineEndingBlank");
    effects.consume(code2);
    effects.exit("lineEndingBlank");
    self2.currentConstruct = void 0;
    return initial;
  }
  function afterConstruct(code2) {
    if (code2 === null) {
      effects.consume(code2);
      return;
    }
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    self2.currentConstruct = void 0;
    return initial;
  }
}
const resolver = {
  resolveAll: createResolver()
};
const string$1 = initializeFactory("string");
const text$4 = initializeFactory("text");
function initializeFactory(field) {
  return {
    resolveAll: createResolver(field === "text" ? resolveAllLineSuffixes : void 0),
    tokenize: initializeText
  };
  function initializeText(effects) {
    const self2 = this;
    const constructs2 = this.parser.constructs[field];
    const text2 = effects.attempt(constructs2, start, notText);
    return start;
    function start(code2) {
      return atBreak(code2) ? text2(code2) : notText(code2);
    }
    function notText(code2) {
      if (code2 === null) {
        effects.consume(code2);
        return;
      }
      effects.enter("data");
      effects.consume(code2);
      return data;
    }
    function data(code2) {
      if (atBreak(code2)) {
        effects.exit("data");
        return text2(code2);
      }
      effects.consume(code2);
      return data;
    }
    function atBreak(code2) {
      if (code2 === null) {
        return true;
      }
      const list2 = constructs2[code2];
      let index2 = -1;
      if (list2) {
        while (++index2 < list2.length) {
          const item = list2[index2];
          if (!item.previous || item.previous.call(self2, self2.previous)) {
            return true;
          }
        }
      }
      return false;
    }
  }
}
function createResolver(extraResolver) {
  return resolveAllText;
  function resolveAllText(events, context) {
    let index2 = -1;
    let enter;
    while (++index2 <= events.length) {
      if (enter === void 0) {
        if (events[index2] && events[index2][1].type === "data") {
          enter = index2;
          index2++;
        }
      } else if (!events[index2] || events[index2][1].type !== "data") {
        if (index2 !== enter + 2) {
          events[enter][1].end = events[index2 - 1][1].end;
          events.splice(enter + 2, index2 - enter - 2);
          index2 = enter + 2;
        }
        enter = void 0;
      }
    }
    return extraResolver ? extraResolver(events, context) : events;
  }
}
function resolveAllLineSuffixes(events, context) {
  let eventIndex = 0;
  while (++eventIndex <= events.length) {
    if ((eventIndex === events.length || events[eventIndex][1].type === "lineEnding") && events[eventIndex - 1][1].type === "data") {
      const data = events[eventIndex - 1][1];
      const chunks = context.sliceStream(data);
      let index2 = chunks.length;
      let bufferIndex = -1;
      let size2 = 0;
      let tabs;
      while (index2--) {
        const chunk = chunks[index2];
        if (typeof chunk === "string") {
          bufferIndex = chunk.length;
          while (chunk.charCodeAt(bufferIndex - 1) === 32) {
            size2++;
            bufferIndex--;
          }
          if (bufferIndex) break;
          bufferIndex = -1;
        } else if (chunk === -2) {
          tabs = true;
          size2++;
        } else if (chunk === -1) ;
        else {
          index2++;
          break;
        }
      }
      if (context._contentTypeTextTrailing && eventIndex === events.length) {
        size2 = 0;
      }
      if (size2) {
        const token = {
          type: eventIndex === events.length || tabs || size2 < 2 ? "lineSuffix" : "hardBreakTrailing",
          start: {
            _bufferIndex: index2 ? bufferIndex : data.start._bufferIndex + bufferIndex,
            _index: data.start._index + index2,
            line: data.end.line,
            column: data.end.column - size2,
            offset: data.end.offset - size2
          },
          end: {
            ...data.end
          }
        };
        data.end = {
          ...token.start
        };
        if (data.start.offset === data.end.offset) {
          Object.assign(data, token);
        } else {
          events.splice(eventIndex, 0, ["enter", token, context], ["exit", token, context]);
          eventIndex += 2;
        }
      }
      eventIndex++;
    }
  }
  return events;
}
const document$1 = {
  [42]: list$2,
  [43]: list$2,
  [45]: list$2,
  [48]: list$2,
  [49]: list$2,
  [50]: list$2,
  [51]: list$2,
  [52]: list$2,
  [53]: list$2,
  [54]: list$2,
  [55]: list$2,
  [56]: list$2,
  [57]: list$2,
  [62]: blockQuote
};
const contentInitial = {
  [91]: definition$1
};
const flowInitial = {
  [-2]: codeIndented,
  [-1]: codeIndented,
  [32]: codeIndented
};
const flow = {
  [35]: headingAtx,
  [42]: thematicBreak$2,
  [45]: [setextUnderline, thematicBreak$2],
  [60]: htmlFlow,
  [61]: setextUnderline,
  [95]: thematicBreak$2,
  [96]: codeFenced,
  [126]: codeFenced
};
const string = {
  [38]: characterReference,
  [92]: characterEscape
};
const text$3 = {
  [-5]: lineEnding,
  [-4]: lineEnding,
  [-3]: lineEnding,
  [33]: labelStartImage,
  [38]: characterReference,
  [42]: attention,
  [60]: [autolink, htmlText],
  [91]: labelStartLink,
  [92]: [hardBreakEscape, characterEscape],
  [93]: labelEnd,
  [95]: attention,
  [96]: codeText
};
const insideSpan = {
  null: [attention, resolver]
};
const attentionMarkers = {
  null: [42, 95]
};
const disable = {
  null: []
};
const defaultConstructs = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  attentionMarkers,
  contentInitial,
  disable,
  document: document$1,
  flow,
  flowInitial,
  insideSpan,
  string,
  text: text$3
}, Symbol.toStringTag, { value: "Module" }));
function createTokenizer(parser, initialize, from) {
  let point2 = {
    _bufferIndex: -1,
    _index: 0,
    line: from && from.line || 1,
    column: from && from.column || 1,
    offset: from && from.offset || 0
  };
  const columnStart = {};
  const resolveAllConstructs = [];
  let chunks = [];
  let stack = [];
  const effects = {
    attempt: constructFactory(onsuccessfulconstruct),
    check: constructFactory(onsuccessfulcheck),
    consume,
    enter,
    exit: exit2,
    interrupt: constructFactory(onsuccessfulcheck, {
      interrupt: true
    })
  };
  const context = {
    code: null,
    containerState: {},
    defineSkip,
    events: [],
    now,
    parser,
    previous: null,
    sliceSerialize,
    sliceStream,
    write
  };
  let state = initialize.tokenize.call(context, effects);
  if (initialize.resolveAll) {
    resolveAllConstructs.push(initialize);
  }
  return context;
  function write(slice) {
    chunks = push(chunks, slice);
    main();
    if (chunks[chunks.length - 1] !== null) {
      return [];
    }
    addResult(initialize, 0);
    context.events = resolveAll(resolveAllConstructs, context.events, context);
    return context.events;
  }
  function sliceSerialize(token, expandTabs) {
    return serializeChunks(sliceStream(token), expandTabs);
  }
  function sliceStream(token) {
    return sliceChunks(chunks, token);
  }
  function now() {
    const {
      _bufferIndex,
      _index,
      line,
      column,
      offset: offset2
    } = point2;
    return {
      _bufferIndex,
      _index,
      line,
      column,
      offset: offset2
    };
  }
  function defineSkip(value) {
    columnStart[value.line] = value.column;
    accountForPotentialSkip();
  }
  function main() {
    let chunkIndex;
    while (point2._index < chunks.length) {
      const chunk = chunks[point2._index];
      if (typeof chunk === "string") {
        chunkIndex = point2._index;
        if (point2._bufferIndex < 0) {
          point2._bufferIndex = 0;
        }
        while (point2._index === chunkIndex && point2._bufferIndex < chunk.length) {
          go(chunk.charCodeAt(point2._bufferIndex));
        }
      } else {
        go(chunk);
      }
    }
  }
  function go(code2) {
    state = state(code2);
  }
  function consume(code2) {
    if (markdownLineEnding(code2)) {
      point2.line++;
      point2.column = 1;
      point2.offset += code2 === -3 ? 2 : 1;
      accountForPotentialSkip();
    } else if (code2 !== -1) {
      point2.column++;
      point2.offset++;
    }
    if (point2._bufferIndex < 0) {
      point2._index++;
    } else {
      point2._bufferIndex++;
      if (point2._bufferIndex === // Points w/ non-negative `_bufferIndex` reference
      // strings.
      /** @type {string} */
      chunks[point2._index].length) {
        point2._bufferIndex = -1;
        point2._index++;
      }
    }
    context.previous = code2;
  }
  function enter(type, fields) {
    const token = fields || {};
    token.type = type;
    token.start = now();
    context.events.push(["enter", token, context]);
    stack.push(token);
    return token;
  }
  function exit2(type) {
    const token = stack.pop();
    token.end = now();
    context.events.push(["exit", token, context]);
    return token;
  }
  function onsuccessfulconstruct(construct, info) {
    addResult(construct, info.from);
  }
  function onsuccessfulcheck(_, info) {
    info.restore();
  }
  function constructFactory(onreturn, fields) {
    return hook;
    function hook(constructs2, returnState, bogusState) {
      let listOfConstructs;
      let constructIndex;
      let currentConstruct;
      let info;
      return Array.isArray(constructs2) ? (
        /* c8 ignore next 1 */
        handleListOfConstructs(constructs2)
      ) : "tokenize" in constructs2 ? (
        // Looks like a construct.
        handleListOfConstructs([
          /** @type {Construct} */
          constructs2
        ])
      ) : handleMapOfConstructs(constructs2);
      function handleMapOfConstructs(map2) {
        return start;
        function start(code2) {
          const left = code2 !== null && map2[code2];
          const all2 = code2 !== null && map2.null;
          const list2 = [
            // To do: add more extension tests.
            /* c8 ignore next 2 */
            ...Array.isArray(left) ? left : left ? [left] : [],
            ...Array.isArray(all2) ? all2 : all2 ? [all2] : []
          ];
          return handleListOfConstructs(list2)(code2);
        }
      }
      function handleListOfConstructs(list2) {
        listOfConstructs = list2;
        constructIndex = 0;
        if (list2.length === 0) {
          return bogusState;
        }
        return handleConstruct(list2[constructIndex]);
      }
      function handleConstruct(construct) {
        return start;
        function start(code2) {
          info = store();
          currentConstruct = construct;
          if (!construct.partial) {
            context.currentConstruct = construct;
          }
          if (construct.name && context.parser.constructs.disable.null.includes(construct.name)) {
            return nok();
          }
          return construct.tokenize.call(
            // If we do have fields, create an object w/ `context` as its
            // prototype.
            // This allows a “live binding”, which is needed for `interrupt`.
            fields ? Object.assign(Object.create(context), fields) : context,
            effects,
            ok2,
            nok
          )(code2);
        }
      }
      function ok2(code2) {
        onreturn(currentConstruct, info);
        return returnState;
      }
      function nok(code2) {
        info.restore();
        if (++constructIndex < listOfConstructs.length) {
          return handleConstruct(listOfConstructs[constructIndex]);
        }
        return bogusState;
      }
    }
  }
  function addResult(construct, from2) {
    if (construct.resolveAll && !resolveAllConstructs.includes(construct)) {
      resolveAllConstructs.push(construct);
    }
    if (construct.resolve) {
      splice(context.events, from2, context.events.length - from2, construct.resolve(context.events.slice(from2), context));
    }
    if (construct.resolveTo) {
      context.events = construct.resolveTo(context.events, context);
    }
  }
  function store() {
    const startPoint = now();
    const startPrevious = context.previous;
    const startCurrentConstruct = context.currentConstruct;
    const startEventsIndex = context.events.length;
    const startStack = Array.from(stack);
    return {
      from: startEventsIndex,
      restore
    };
    function restore() {
      point2 = startPoint;
      context.previous = startPrevious;
      context.currentConstruct = startCurrentConstruct;
      context.events.length = startEventsIndex;
      stack = startStack;
      accountForPotentialSkip();
    }
  }
  function accountForPotentialSkip() {
    if (point2.line in columnStart && point2.column < 2) {
      point2.column = columnStart[point2.line];
      point2.offset += columnStart[point2.line] - 1;
    }
  }
}
function sliceChunks(chunks, token) {
  const startIndex = token.start._index;
  const startBufferIndex = token.start._bufferIndex;
  const endIndex = token.end._index;
  const endBufferIndex = token.end._bufferIndex;
  let view;
  if (startIndex === endIndex) {
    view = [chunks[startIndex].slice(startBufferIndex, endBufferIndex)];
  } else {
    view = chunks.slice(startIndex, endIndex);
    if (startBufferIndex > -1) {
      const head = view[0];
      if (typeof head === "string") {
        view[0] = head.slice(startBufferIndex);
      } else {
        view.shift();
      }
    }
    if (endBufferIndex > 0) {
      view.push(chunks[endIndex].slice(0, endBufferIndex));
    }
  }
  return view;
}
function serializeChunks(chunks, expandTabs) {
  let index2 = -1;
  const result = [];
  let atTab;
  while (++index2 < chunks.length) {
    const chunk = chunks[index2];
    let value;
    if (typeof chunk === "string") {
      value = chunk;
    } else switch (chunk) {
      case -5: {
        value = "\r";
        break;
      }
      case -4: {
        value = "\n";
        break;
      }
      case -3: {
        value = "\r\n";
        break;
      }
      case -2: {
        value = expandTabs ? " " : "	";
        break;
      }
      case -1: {
        if (!expandTabs && atTab) continue;
        value = " ";
        break;
      }
      default: {
        value = String.fromCharCode(chunk);
      }
    }
    atTab = chunk === -2;
    result.push(value);
  }
  return result.join("");
}
function parse$1(options) {
  const settings = options || {};
  const constructs2 = (
    /** @type {FullNormalizedExtension} */
    combineExtensions([defaultConstructs, ...settings.extensions || []])
  );
  const parser = {
    constructs: constructs2,
    content: create2(content$1),
    defined: [],
    document: create2(document$2),
    flow: create2(flow$1),
    lazy: {},
    string: create2(string$1),
    text: create2(text$4)
  };
  return parser;
  function create2(initial) {
    return creator;
    function creator(from) {
      return createTokenizer(parser, initial, from);
    }
  }
}
function postprocess(events) {
  while (!subtokenize(events)) {
  }
  return events;
}
const search = /[\0\t\n\r]/g;
function preprocess() {
  let column = 1;
  let buffer = "";
  let start = true;
  let atCarriageReturn;
  return preprocessor;
  function preprocessor(value, encoding, end) {
    const chunks = [];
    let match;
    let next;
    let startPosition;
    let endPosition;
    let code2;
    value = buffer + (typeof value === "string" ? value.toString() : new TextDecoder(encoding || void 0).decode(value));
    startPosition = 0;
    buffer = "";
    if (start) {
      if (value.charCodeAt(0) === 65279) {
        startPosition++;
      }
      start = void 0;
    }
    while (startPosition < value.length) {
      search.lastIndex = startPosition;
      match = search.exec(value);
      endPosition = match && match.index !== void 0 ? match.index : value.length;
      code2 = value.charCodeAt(endPosition);
      if (!match) {
        buffer = value.slice(startPosition);
        break;
      }
      if (code2 === 10 && startPosition === endPosition && atCarriageReturn) {
        chunks.push(-3);
        atCarriageReturn = void 0;
      } else {
        if (atCarriageReturn) {
          chunks.push(-5);
          atCarriageReturn = void 0;
        }
        if (startPosition < endPosition) {
          chunks.push(value.slice(startPosition, endPosition));
          column += endPosition - startPosition;
        }
        switch (code2) {
          case 0: {
            chunks.push(65533);
            column++;
            break;
          }
          case 9: {
            next = Math.ceil(column / 4) * 4;
            chunks.push(-2);
            while (column++ < next) chunks.push(-1);
            break;
          }
          case 10: {
            chunks.push(-4);
            column = 1;
            break;
          }
          default: {
            atCarriageReturn = true;
            column = 1;
          }
        }
      }
      startPosition = endPosition + 1;
    }
    if (end) {
      if (atCarriageReturn) chunks.push(-5);
      if (buffer) chunks.push(buffer);
      chunks.push(null);
    }
    return chunks;
  }
}
const characterEscapeOrReference = /\\([!-/:-@[-`{-~])|&(#(?:\d{1,7}|x[\da-f]{1,6})|[\da-z]{1,31});/gi;
function decodeString(value) {
  return value.replace(characterEscapeOrReference, decode);
}
function decode($0, $1, $2) {
  if ($1) {
    return $1;
  }
  const head = $2.charCodeAt(0);
  if (head === 35) {
    const head2 = $2.charCodeAt(1);
    const hex = head2 === 120 || head2 === 88;
    return decodeNumericCharacterReference($2.slice(hex ? 2 : 1), hex ? 16 : 10);
  }
  return decodeNamedCharacterReference($2) || $0;
}
const own$2 = {}.hasOwnProperty;
function fromMarkdown(value, encoding, options) {
  if (encoding && typeof encoding === "object") {
    options = encoding;
    encoding = void 0;
  }
  return compiler(options)(postprocess(parse$1(options).document().write(preprocess()(value, encoding, true))));
}
function compiler(options) {
  const config = {
    transforms: [],
    canContainEols: ["emphasis", "fragment", "heading", "paragraph", "strong"],
    enter: {
      autolink: opener(link2),
      autolinkProtocol: onenterdata,
      autolinkEmail: onenterdata,
      atxHeading: opener(heading2),
      blockQuote: opener(blockQuote2),
      characterEscape: onenterdata,
      characterReference: onenterdata,
      codeFenced: opener(codeFlow),
      codeFencedFenceInfo: buffer,
      codeFencedFenceMeta: buffer,
      codeIndented: opener(codeFlow, buffer),
      codeText: opener(codeText2, buffer),
      codeTextData: onenterdata,
      data: onenterdata,
      codeFlowValue: onenterdata,
      definition: opener(definition2),
      definitionDestinationString: buffer,
      definitionLabelString: buffer,
      definitionTitleString: buffer,
      emphasis: opener(emphasis2),
      hardBreakEscape: opener(hardBreak2),
      hardBreakTrailing: opener(hardBreak2),
      htmlFlow: opener(html2, buffer),
      htmlFlowData: onenterdata,
      htmlText: opener(html2, buffer),
      htmlTextData: onenterdata,
      image: opener(image2),
      label: buffer,
      link: opener(link2),
      listItem: opener(listItem2),
      listItemValue: onenterlistitemvalue,
      listOrdered: opener(list2, onenterlistordered),
      listUnordered: opener(list2),
      paragraph: opener(paragraph2),
      reference: onenterreference,
      referenceString: buffer,
      resourceDestinationString: buffer,
      resourceTitleString: buffer,
      setextHeading: opener(heading2),
      strong: opener(strong2),
      thematicBreak: opener(thematicBreak2)
    },
    exit: {
      atxHeading: closer(),
      atxHeadingSequence: onexitatxheadingsequence,
      autolink: closer(),
      autolinkEmail: onexitautolinkemail,
      autolinkProtocol: onexitautolinkprotocol,
      blockQuote: closer(),
      characterEscapeValue: onexitdata,
      characterReferenceMarkerHexadecimal: onexitcharacterreferencemarker,
      characterReferenceMarkerNumeric: onexitcharacterreferencemarker,
      characterReferenceValue: onexitcharacterreferencevalue,
      characterReference: onexitcharacterreference,
      codeFenced: closer(onexitcodefenced),
      codeFencedFence: onexitcodefencedfence,
      codeFencedFenceInfo: onexitcodefencedfenceinfo,
      codeFencedFenceMeta: onexitcodefencedfencemeta,
      codeFlowValue: onexitdata,
      codeIndented: closer(onexitcodeindented),
      codeText: closer(onexitcodetext),
      codeTextData: onexitdata,
      data: onexitdata,
      definition: closer(),
      definitionDestinationString: onexitdefinitiondestinationstring,
      definitionLabelString: onexitdefinitionlabelstring,
      definitionTitleString: onexitdefinitiontitlestring,
      emphasis: closer(),
      hardBreakEscape: closer(onexithardbreak),
      hardBreakTrailing: closer(onexithardbreak),
      htmlFlow: closer(onexithtmlflow),
      htmlFlowData: onexitdata,
      htmlText: closer(onexithtmltext),
      htmlTextData: onexitdata,
      image: closer(onexitimage),
      label: onexitlabel,
      labelText: onexitlabeltext,
      lineEnding: onexitlineending,
      link: closer(onexitlink),
      listItem: closer(),
      listOrdered: closer(),
      listUnordered: closer(),
      paragraph: closer(),
      referenceString: onexitreferencestring,
      resourceDestinationString: onexitresourcedestinationstring,
      resourceTitleString: onexitresourcetitlestring,
      resource: onexitresource,
      setextHeading: closer(onexitsetextheading),
      setextHeadingLineSequence: onexitsetextheadinglinesequence,
      setextHeadingText: onexitsetextheadingtext,
      strong: closer(),
      thematicBreak: closer()
    }
  };
  configure(config, (options || {}).mdastExtensions || []);
  const data = {};
  return compile;
  function compile(events) {
    let tree = {
      type: "root",
      children: []
    };
    const context = {
      stack: [tree],
      tokenStack: [],
      config,
      enter,
      exit: exit2,
      buffer,
      resume,
      data
    };
    const listStack = [];
    let index2 = -1;
    while (++index2 < events.length) {
      if (events[index2][1].type === "listOrdered" || events[index2][1].type === "listUnordered") {
        if (events[index2][0] === "enter") {
          listStack.push(index2);
        } else {
          const tail = listStack.pop();
          index2 = prepareList(events, tail, index2);
        }
      }
    }
    index2 = -1;
    while (++index2 < events.length) {
      const handler = config[events[index2][0]];
      if (own$2.call(handler, events[index2][1].type)) {
        handler[events[index2][1].type].call(Object.assign({
          sliceSerialize: events[index2][2].sliceSerialize
        }, context), events[index2][1]);
      }
    }
    if (context.tokenStack.length > 0) {
      const tail = context.tokenStack[context.tokenStack.length - 1];
      const handler = tail[1] || defaultOnError;
      handler.call(context, void 0, tail[0]);
    }
    tree.position = {
      start: point(events.length > 0 ? events[0][1].start : {
        line: 1,
        column: 1,
        offset: 0
      }),
      end: point(events.length > 0 ? events[events.length - 2][1].end : {
        line: 1,
        column: 1,
        offset: 0
      })
    };
    index2 = -1;
    while (++index2 < config.transforms.length) {
      tree = config.transforms[index2](tree) || tree;
    }
    return tree;
  }
  function prepareList(events, start, length) {
    let index2 = start - 1;
    let containerBalance = -1;
    let listSpread = false;
    let listItem3;
    let lineIndex;
    let firstBlankLineIndex;
    let atMarker;
    while (++index2 <= length) {
      const event = events[index2];
      switch (event[1].type) {
        case "listUnordered":
        case "listOrdered":
        case "blockQuote": {
          if (event[0] === "enter") {
            containerBalance++;
          } else {
            containerBalance--;
          }
          atMarker = void 0;
          break;
        }
        case "lineEndingBlank": {
          if (event[0] === "enter") {
            if (listItem3 && !atMarker && !containerBalance && !firstBlankLineIndex) {
              firstBlankLineIndex = index2;
            }
            atMarker = void 0;
          }
          break;
        }
        case "linePrefix":
        case "listItemValue":
        case "listItemMarker":
        case "listItemPrefix":
        case "listItemPrefixWhitespace": {
          break;
        }
        default: {
          atMarker = void 0;
        }
      }
      if (!containerBalance && event[0] === "enter" && event[1].type === "listItemPrefix" || containerBalance === -1 && event[0] === "exit" && (event[1].type === "listUnordered" || event[1].type === "listOrdered")) {
        if (listItem3) {
          let tailIndex = index2;
          lineIndex = void 0;
          while (tailIndex--) {
            const tailEvent = events[tailIndex];
            if (tailEvent[1].type === "lineEnding" || tailEvent[1].type === "lineEndingBlank") {
              if (tailEvent[0] === "exit") continue;
              if (lineIndex) {
                events[lineIndex][1].type = "lineEndingBlank";
                listSpread = true;
              }
              tailEvent[1].type = "lineEnding";
              lineIndex = tailIndex;
            } else if (tailEvent[1].type === "linePrefix" || tailEvent[1].type === "blockQuotePrefix" || tailEvent[1].type === "blockQuotePrefixWhitespace" || tailEvent[1].type === "blockQuoteMarker" || tailEvent[1].type === "listItemIndent") ;
            else {
              break;
            }
          }
          if (firstBlankLineIndex && (!lineIndex || firstBlankLineIndex < lineIndex)) {
            listItem3._spread = true;
          }
          listItem3.end = Object.assign({}, lineIndex ? events[lineIndex][1].start : event[1].end);
          events.splice(lineIndex || index2, 0, ["exit", listItem3, event[2]]);
          index2++;
          length++;
        }
        if (event[1].type === "listItemPrefix") {
          const item = {
            type: "listItem",
            _spread: false,
            start: Object.assign({}, event[1].start),
            // @ts-expect-error: we’ll add `end` in a second.
            end: void 0
          };
          listItem3 = item;
          events.splice(index2, 0, ["enter", item, event[2]]);
          index2++;
          length++;
          firstBlankLineIndex = void 0;
          atMarker = true;
        }
      }
    }
    events[start][1]._spread = listSpread;
    return length;
  }
  function opener(create2, and) {
    return open;
    function open(token) {
      enter.call(this, create2(token), token);
      if (and) and.call(this, token);
    }
  }
  function buffer() {
    this.stack.push({
      type: "fragment",
      children: []
    });
  }
  function enter(node2, token, errorHandler) {
    const parent = this.stack[this.stack.length - 1];
    const siblings = parent.children;
    siblings.push(node2);
    this.stack.push(node2);
    this.tokenStack.push([token, errorHandler || void 0]);
    node2.position = {
      start: point(token.start),
      // @ts-expect-error: `end` will be patched later.
      end: void 0
    };
  }
  function closer(and) {
    return close;
    function close(token) {
      if (and) and.call(this, token);
      exit2.call(this, token);
    }
  }
  function exit2(token, onExitError) {
    const node2 = this.stack.pop();
    const open = this.tokenStack.pop();
    if (!open) {
      throw new Error("Cannot close `" + token.type + "` (" + stringifyPosition({
        start: token.start,
        end: token.end
      }) + "): it’s not open");
    } else if (open[0].type !== token.type) {
      if (onExitError) {
        onExitError.call(this, token, open[0]);
      } else {
        const handler = open[1] || defaultOnError;
        handler.call(this, token, open[0]);
      }
    }
    node2.position.end = point(token.end);
  }
  function resume() {
    return toString$1(this.stack.pop());
  }
  function onenterlistordered() {
    this.data.expectingFirstListItemValue = true;
  }
  function onenterlistitemvalue(token) {
    if (this.data.expectingFirstListItemValue) {
      const ancestor = this.stack[this.stack.length - 2];
      ancestor.start = Number.parseInt(this.sliceSerialize(token), 10);
      this.data.expectingFirstListItemValue = void 0;
    }
  }
  function onexitcodefencedfenceinfo() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.lang = data2;
  }
  function onexitcodefencedfencemeta() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.meta = data2;
  }
  function onexitcodefencedfence() {
    if (this.data.flowCodeInside) return;
    this.buffer();
    this.data.flowCodeInside = true;
  }
  function onexitcodefenced() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data2.replace(/^(\r?\n|\r)|(\r?\n|\r)$/g, "");
    this.data.flowCodeInside = void 0;
  }
  function onexitcodeindented() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data2.replace(/(\r?\n|\r)$/g, "");
  }
  function onexitdefinitionlabelstring(token) {
    const label = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.label = label;
    node2.identifier = normalizeIdentifier(this.sliceSerialize(token)).toLowerCase();
  }
  function onexitdefinitiontitlestring() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.title = data2;
  }
  function onexitdefinitiondestinationstring() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.url = data2;
  }
  function onexitatxheadingsequence(token) {
    const node2 = this.stack[this.stack.length - 1];
    if (!node2.depth) {
      const depth = this.sliceSerialize(token).length;
      node2.depth = depth;
    }
  }
  function onexitsetextheadingtext() {
    this.data.setextHeadingSlurpLineEnding = true;
  }
  function onexitsetextheadinglinesequence(token) {
    const node2 = this.stack[this.stack.length - 1];
    node2.depth = this.sliceSerialize(token).codePointAt(0) === 61 ? 1 : 2;
  }
  function onexitsetextheading() {
    this.data.setextHeadingSlurpLineEnding = void 0;
  }
  function onenterdata(token) {
    const node2 = this.stack[this.stack.length - 1];
    const siblings = node2.children;
    let tail = siblings[siblings.length - 1];
    if (!tail || tail.type !== "text") {
      tail = text2();
      tail.position = {
        start: point(token.start),
        // @ts-expect-error: we’ll add `end` later.
        end: void 0
      };
      siblings.push(tail);
    }
    this.stack.push(tail);
  }
  function onexitdata(token) {
    const tail = this.stack.pop();
    tail.value += this.sliceSerialize(token);
    tail.position.end = point(token.end);
  }
  function onexitlineending(token) {
    const context = this.stack[this.stack.length - 1];
    if (this.data.atHardBreak) {
      const tail = context.children[context.children.length - 1];
      tail.position.end = point(token.end);
      this.data.atHardBreak = void 0;
      return;
    }
    if (!this.data.setextHeadingSlurpLineEnding && config.canContainEols.includes(context.type)) {
      onenterdata.call(this, token);
      onexitdata.call(this, token);
    }
  }
  function onexithardbreak() {
    this.data.atHardBreak = true;
  }
  function onexithtmlflow() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data2;
  }
  function onexithtmltext() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data2;
  }
  function onexitcodetext() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data2;
  }
  function onexitlink() {
    const node2 = this.stack[this.stack.length - 1];
    if (this.data.inReference) {
      const referenceType = this.data.referenceType || "shortcut";
      node2.type += "Reference";
      node2.referenceType = referenceType;
      delete node2.url;
      delete node2.title;
    } else {
      delete node2.identifier;
      delete node2.label;
    }
    this.data.referenceType = void 0;
  }
  function onexitimage() {
    const node2 = this.stack[this.stack.length - 1];
    if (this.data.inReference) {
      const referenceType = this.data.referenceType || "shortcut";
      node2.type += "Reference";
      node2.referenceType = referenceType;
      delete node2.url;
      delete node2.title;
    } else {
      delete node2.identifier;
      delete node2.label;
    }
    this.data.referenceType = void 0;
  }
  function onexitlabeltext(token) {
    const string2 = this.sliceSerialize(token);
    const ancestor = this.stack[this.stack.length - 2];
    ancestor.label = decodeString(string2);
    ancestor.identifier = normalizeIdentifier(string2).toLowerCase();
  }
  function onexitlabel() {
    const fragment = this.stack[this.stack.length - 1];
    const value = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    this.data.inReference = true;
    if (node2.type === "link") {
      const children = fragment.children;
      node2.children = children;
    } else {
      node2.alt = value;
    }
  }
  function onexitresourcedestinationstring() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.url = data2;
  }
  function onexitresourcetitlestring() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.title = data2;
  }
  function onexitresource() {
    this.data.inReference = void 0;
  }
  function onenterreference() {
    this.data.referenceType = "collapsed";
  }
  function onexitreferencestring(token) {
    const label = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.label = label;
    node2.identifier = normalizeIdentifier(this.sliceSerialize(token)).toLowerCase();
    this.data.referenceType = "full";
  }
  function onexitcharacterreferencemarker(token) {
    this.data.characterReferenceType = token.type;
  }
  function onexitcharacterreferencevalue(token) {
    const data2 = this.sliceSerialize(token);
    const type = this.data.characterReferenceType;
    let value;
    if (type) {
      value = decodeNumericCharacterReference(data2, type === "characterReferenceMarkerNumeric" ? 10 : 16);
      this.data.characterReferenceType = void 0;
    } else {
      const result = decodeNamedCharacterReference(data2);
      value = result;
    }
    const tail = this.stack[this.stack.length - 1];
    tail.value += value;
  }
  function onexitcharacterreference(token) {
    const tail = this.stack.pop();
    tail.position.end = point(token.end);
  }
  function onexitautolinkprotocol(token) {
    onexitdata.call(this, token);
    const node2 = this.stack[this.stack.length - 1];
    node2.url = this.sliceSerialize(token);
  }
  function onexitautolinkemail(token) {
    onexitdata.call(this, token);
    const node2 = this.stack[this.stack.length - 1];
    node2.url = "mailto:" + this.sliceSerialize(token);
  }
  function blockQuote2() {
    return {
      type: "blockquote",
      children: []
    };
  }
  function codeFlow() {
    return {
      type: "code",
      lang: null,
      meta: null,
      value: ""
    };
  }
  function codeText2() {
    return {
      type: "inlineCode",
      value: ""
    };
  }
  function definition2() {
    return {
      type: "definition",
      identifier: "",
      label: null,
      title: null,
      url: ""
    };
  }
  function emphasis2() {
    return {
      type: "emphasis",
      children: []
    };
  }
  function heading2() {
    return {
      type: "heading",
      // @ts-expect-error `depth` will be set later.
      depth: 0,
      children: []
    };
  }
  function hardBreak2() {
    return {
      type: "break"
    };
  }
  function html2() {
    return {
      type: "html",
      value: ""
    };
  }
  function image2() {
    return {
      type: "image",
      title: null,
      url: "",
      alt: null
    };
  }
  function link2() {
    return {
      type: "link",
      title: null,
      url: "",
      children: []
    };
  }
  function list2(token) {
    return {
      type: "list",
      ordered: token.type === "listOrdered",
      start: null,
      spread: token._spread,
      children: []
    };
  }
  function listItem2(token) {
    return {
      type: "listItem",
      spread: token._spread,
      checked: null,
      children: []
    };
  }
  function paragraph2() {
    return {
      type: "paragraph",
      children: []
    };
  }
  function strong2() {
    return {
      type: "strong",
      children: []
    };
  }
  function text2() {
    return {
      type: "text",
      value: ""
    };
  }
  function thematicBreak2() {
    return {
      type: "thematicBreak"
    };
  }
}
function point(d) {
  return {
    line: d.line,
    column: d.column,
    offset: d.offset
  };
}
function configure(combined, extensions) {
  let index2 = -1;
  while (++index2 < extensions.length) {
    const value = extensions[index2];
    if (Array.isArray(value)) {
      configure(combined, value);
    } else {
      extension(combined, value);
    }
  }
}
function extension(combined, extension2) {
  let key;
  for (key in extension2) {
    if (own$2.call(extension2, key)) {
      switch (key) {
        case "canContainEols": {
          const right = extension2[key];
          if (right) {
            combined[key].push(...right);
          }
          break;
        }
        case "transforms": {
          const right = extension2[key];
          if (right) {
            combined[key].push(...right);
          }
          break;
        }
        case "enter":
        case "exit": {
          const right = extension2[key];
          if (right) {
            Object.assign(combined[key], right);
          }
          break;
        }
      }
    }
  }
}
function defaultOnError(left, right) {
  if (left) {
    throw new Error("Cannot close `" + left.type + "` (" + stringifyPosition({
      start: left.start,
      end: left.end
    }) + "): a different token (`" + right.type + "`, " + stringifyPosition({
      start: right.start,
      end: right.end
    }) + ") is open");
  } else {
    throw new Error("Cannot close document, a token (`" + right.type + "`, " + stringifyPosition({
      start: right.start,
      end: right.end
    }) + ") is still open");
  }
}
function remarkParse(options) {
  const self2 = this;
  self2.parser = parser;
  function parser(doc) {
    return fromMarkdown(doc, {
      ...self2.data("settings"),
      ...options,
      // Note: these options are not in the readme.
      // The goal is for them to be set by plugins on `data` instead of being
      // passed by users.
      extensions: self2.data("micromarkExtensions") || [],
      mdastExtensions: self2.data("fromMarkdownExtensions") || []
    });
  }
}
function blockquote$1(state, node2) {
  const result = {
    type: "element",
    tagName: "blockquote",
    properties: {},
    children: state.wrap(state.all(node2), true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function hardBreak$1(state, node2) {
  const result = { type: "element", tagName: "br", properties: {}, children: [] };
  state.patch(node2, result);
  return [state.applyData(node2, result), { type: "text", value: "\n" }];
}
function code$2(state, node2) {
  const value = node2.value ? node2.value + "\n" : "";
  const properties = {};
  const language = node2.lang ? node2.lang.split(/\s+/) : [];
  if (language.length > 0) {
    properties.className = ["language-" + language[0]];
  }
  let result = {
    type: "element",
    tagName: "code",
    properties,
    children: [{ type: "text", value }]
  };
  if (node2.meta) {
    result.data = { meta: node2.meta };
  }
  state.patch(node2, result);
  result = state.applyData(node2, result);
  result = { type: "element", tagName: "pre", properties: {}, children: [result] };
  state.patch(node2, result);
  return result;
}
function strikethrough(state, node2) {
  const result = {
    type: "element",
    tagName: "del",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function emphasis$1(state, node2) {
  const result = {
    type: "element",
    tagName: "em",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function footnoteReference$1(state, node2) {
  const clobberPrefix = typeof state.options.clobberPrefix === "string" ? state.options.clobberPrefix : "user-content-";
  const id = String(node2.identifier).toUpperCase();
  const safeId = normalizeUri(id.toLowerCase());
  const index2 = state.footnoteOrder.indexOf(id);
  let counter;
  let reuseCounter = state.footnoteCounts.get(id);
  if (reuseCounter === void 0) {
    reuseCounter = 0;
    state.footnoteOrder.push(id);
    counter = state.footnoteOrder.length;
  } else {
    counter = index2 + 1;
  }
  reuseCounter += 1;
  state.footnoteCounts.set(id, reuseCounter);
  const link2 = {
    type: "element",
    tagName: "a",
    properties: {
      href: "#" + clobberPrefix + "fn-" + safeId,
      id: clobberPrefix + "fnref-" + safeId + (reuseCounter > 1 ? "-" + reuseCounter : ""),
      dataFootnoteRef: true,
      ariaDescribedBy: ["footnote-label"]
    },
    children: [{ type: "text", value: String(counter) }]
  };
  state.patch(node2, link2);
  const sup = {
    type: "element",
    tagName: "sup",
    properties: {},
    children: [link2]
  };
  state.patch(node2, sup);
  return state.applyData(node2, sup);
}
function heading$1(state, node2) {
  const result = {
    type: "element",
    tagName: "h" + node2.depth,
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function html$1(state, node2) {
  if (state.options.allowDangerousHtml) {
    const result = { type: "raw", value: node2.value };
    state.patch(node2, result);
    return state.applyData(node2, result);
  }
  return void 0;
}
function revert(state, node2) {
  const subtype = node2.referenceType;
  let suffix = "]";
  if (subtype === "collapsed") {
    suffix += "[]";
  } else if (subtype === "full") {
    suffix += "[" + (node2.label || node2.identifier) + "]";
  }
  if (node2.type === "imageReference") {
    return [{ type: "text", value: "![" + node2.alt + suffix }];
  }
  const contents = state.all(node2);
  const head = contents[0];
  if (head && head.type === "text") {
    head.value = "[" + head.value;
  } else {
    contents.unshift({ type: "text", value: "[" });
  }
  const tail = contents[contents.length - 1];
  if (tail && tail.type === "text") {
    tail.value += suffix;
  } else {
    contents.push({ type: "text", value: suffix });
  }
  return contents;
}
function imageReference$1(state, node2) {
  const id = String(node2.identifier).toUpperCase();
  const definition2 = state.definitionById.get(id);
  if (!definition2) {
    return revert(state, node2);
  }
  const properties = { src: normalizeUri(definition2.url || ""), alt: node2.alt };
  if (definition2.title !== null && definition2.title !== void 0) {
    properties.title = definition2.title;
  }
  const result = { type: "element", tagName: "img", properties, children: [] };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function image$1(state, node2) {
  const properties = { src: normalizeUri(node2.url) };
  if (node2.alt !== null && node2.alt !== void 0) {
    properties.alt = node2.alt;
  }
  if (node2.title !== null && node2.title !== void 0) {
    properties.title = node2.title;
  }
  const result = { type: "element", tagName: "img", properties, children: [] };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function inlineCode$1(state, node2) {
  const text2 = { type: "text", value: node2.value.replace(/\r?\n|\r/g, " ") };
  state.patch(node2, text2);
  const result = {
    type: "element",
    tagName: "code",
    properties: {},
    children: [text2]
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function linkReference$1(state, node2) {
  const id = String(node2.identifier).toUpperCase();
  const definition2 = state.definitionById.get(id);
  if (!definition2) {
    return revert(state, node2);
  }
  const properties = { href: normalizeUri(definition2.url || "") };
  if (definition2.title !== null && definition2.title !== void 0) {
    properties.title = definition2.title;
  }
  const result = {
    type: "element",
    tagName: "a",
    properties,
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function link$1(state, node2) {
  const properties = { href: normalizeUri(node2.url) };
  if (node2.title !== null && node2.title !== void 0) {
    properties.title = node2.title;
  }
  const result = {
    type: "element",
    tagName: "a",
    properties,
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function listItem$1(state, node2, parent) {
  const results = state.all(node2);
  const loose = parent ? listLoose(parent) : listItemLoose(node2);
  const properties = {};
  const children = [];
  if (typeof node2.checked === "boolean") {
    const head = results[0];
    let paragraph2;
    if (head && head.type === "element" && head.tagName === "p") {
      paragraph2 = head;
    } else {
      paragraph2 = { type: "element", tagName: "p", properties: {}, children: [] };
      results.unshift(paragraph2);
    }
    if (paragraph2.children.length > 0) {
      paragraph2.children.unshift({ type: "text", value: " " });
    }
    paragraph2.children.unshift({
      type: "element",
      tagName: "input",
      properties: { type: "checkbox", checked: node2.checked, disabled: true },
      children: []
    });
    properties.className = ["task-list-item"];
  }
  let index2 = -1;
  while (++index2 < results.length) {
    const child = results[index2];
    if (loose || index2 !== 0 || child.type !== "element" || child.tagName !== "p") {
      children.push({ type: "text", value: "\n" });
    }
    if (child.type === "element" && child.tagName === "p" && !loose) {
      children.push(...child.children);
    } else {
      children.push(child);
    }
  }
  const tail = results[results.length - 1];
  if (tail && (loose || tail.type !== "element" || tail.tagName !== "p")) {
    children.push({ type: "text", value: "\n" });
  }
  const result = { type: "element", tagName: "li", properties, children };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function listLoose(node2) {
  let loose = false;
  if (node2.type === "list") {
    loose = node2.spread || false;
    const children = node2.children;
    let index2 = -1;
    while (!loose && ++index2 < children.length) {
      loose = listItemLoose(children[index2]);
    }
  }
  return loose;
}
function listItemLoose(node2) {
  const spread = node2.spread;
  return spread === null || spread === void 0 ? node2.children.length > 1 : spread;
}
function list$1(state, node2) {
  const properties = {};
  const results = state.all(node2);
  let index2 = -1;
  if (typeof node2.start === "number" && node2.start !== 1) {
    properties.start = node2.start;
  }
  while (++index2 < results.length) {
    const child = results[index2];
    if (child.type === "element" && child.tagName === "li" && child.properties && Array.isArray(child.properties.className) && child.properties.className.includes("task-list-item")) {
      properties.className = ["contains-task-list"];
      break;
    }
  }
  const result = {
    type: "element",
    tagName: node2.ordered ? "ol" : "ul",
    properties,
    children: state.wrap(results, true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function paragraph$1(state, node2) {
  const result = {
    type: "element",
    tagName: "p",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function root$1(state, node2) {
  const result = { type: "root", children: state.wrap(state.all(node2)) };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function strong$1(state, node2) {
  const result = {
    type: "element",
    tagName: "strong",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function table(state, node2) {
  const rows = state.all(node2);
  const firstRow = rows.shift();
  const tableContent = [];
  if (firstRow) {
    const head = {
      type: "element",
      tagName: "thead",
      properties: {},
      children: state.wrap([firstRow], true)
    };
    state.patch(node2.children[0], head);
    tableContent.push(head);
  }
  if (rows.length > 0) {
    const body = {
      type: "element",
      tagName: "tbody",
      properties: {},
      children: state.wrap(rows, true)
    };
    const start = pointStart(node2.children[1]);
    const end = pointEnd(node2.children[node2.children.length - 1]);
    if (start && end) body.position = { start, end };
    tableContent.push(body);
  }
  const result = {
    type: "element",
    tagName: "table",
    properties: {},
    children: state.wrap(tableContent, true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function tableRow(state, node2, parent) {
  const siblings = parent ? parent.children : void 0;
  const rowIndex = siblings ? siblings.indexOf(node2) : 1;
  const tagName = rowIndex === 0 ? "th" : "td";
  const align = parent && parent.type === "table" ? parent.align : void 0;
  const length = align ? align.length : node2.children.length;
  let cellIndex = -1;
  const cells = [];
  while (++cellIndex < length) {
    const cell = node2.children[cellIndex];
    const properties = {};
    const alignValue = align ? align[cellIndex] : void 0;
    if (alignValue) {
      properties.align = alignValue;
    }
    let result2 = { type: "element", tagName, properties, children: [] };
    if (cell) {
      result2.children = state.all(cell);
      state.patch(cell, result2);
      result2 = state.applyData(cell, result2);
    }
    cells.push(result2);
  }
  const result = {
    type: "element",
    tagName: "tr",
    properties: {},
    children: state.wrap(cells, true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function tableCell(state, node2) {
  const result = {
    type: "element",
    tagName: "td",
    // Assume body cell.
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
const tab = 9;
const space = 32;
function trimLines(value) {
  const source = String(value);
  const search2 = /\r?\n|\r/g;
  let match = search2.exec(source);
  let last = 0;
  const lines = [];
  while (match) {
    lines.push(
      trimLine(source.slice(last, match.index), last > 0, true),
      match[0]
    );
    last = match.index + match[0].length;
    match = search2.exec(source);
  }
  lines.push(trimLine(source.slice(last), last > 0, false));
  return lines.join("");
}
function trimLine(value, start, end) {
  let startIndex = 0;
  let endIndex = value.length;
  if (start) {
    let code2 = value.codePointAt(startIndex);
    while (code2 === tab || code2 === space) {
      startIndex++;
      code2 = value.codePointAt(startIndex);
    }
  }
  if (end) {
    let code2 = value.codePointAt(endIndex - 1);
    while (code2 === tab || code2 === space) {
      endIndex--;
      code2 = value.codePointAt(endIndex - 1);
    }
  }
  return endIndex > startIndex ? value.slice(startIndex, endIndex) : "";
}
function text$2(state, node2) {
  const result = { type: "text", value: trimLines(String(node2.value)) };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function thematicBreak$1(state, node2) {
  const result = {
    type: "element",
    tagName: "hr",
    properties: {},
    children: []
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
const handlers = {
  blockquote: blockquote$1,
  break: hardBreak$1,
  code: code$2,
  delete: strikethrough,
  emphasis: emphasis$1,
  footnoteReference: footnoteReference$1,
  heading: heading$1,
  html: html$1,
  imageReference: imageReference$1,
  image: image$1,
  inlineCode: inlineCode$1,
  linkReference: linkReference$1,
  link: link$1,
  listItem: listItem$1,
  list: list$1,
  paragraph: paragraph$1,
  // @ts-expect-error: root is different, but hard to type.
  root: root$1,
  strong: strong$1,
  table,
  tableCell,
  tableRow,
  text: text$2,
  thematicBreak: thematicBreak$1,
  toml: ignore,
  yaml: ignore,
  definition: ignore,
  footnoteDefinition: ignore
};
function ignore() {
  return void 0;
}
const VOID = -1;
const PRIMITIVE = 0;
const ARRAY = 1;
const OBJECT = 2;
const DATE = 3;
const REGEXP = 4;
const MAP = 5;
const SET = 6;
const ERROR = 7;
const BIGINT = 8;
const env = typeof self === "object" ? self : globalThis;
const deserializer = ($, _) => {
  const as = (out, index2) => {
    $.set(index2, out);
    return out;
  };
  const unpair = (index2) => {
    if ($.has(index2))
      return $.get(index2);
    const [type, value] = _[index2];
    switch (type) {
      case PRIMITIVE:
      case VOID:
        return as(value, index2);
      case ARRAY: {
        const arr = as([], index2);
        for (const index3 of value)
          arr.push(unpair(index3));
        return arr;
      }
      case OBJECT: {
        const object = as({}, index2);
        for (const [key, index3] of value)
          object[unpair(key)] = unpair(index3);
        return object;
      }
      case DATE:
        return as(new Date(value), index2);
      case REGEXP: {
        const { source, flags } = value;
        return as(new RegExp(source, flags), index2);
      }
      case MAP: {
        const map2 = as(/* @__PURE__ */ new Map(), index2);
        for (const [key, index3] of value)
          map2.set(unpair(key), unpair(index3));
        return map2;
      }
      case SET: {
        const set = as(/* @__PURE__ */ new Set(), index2);
        for (const index3 of value)
          set.add(unpair(index3));
        return set;
      }
      case ERROR: {
        const { name: name2, message } = value;
        return as(new env[name2](message), index2);
      }
      case BIGINT:
        return as(BigInt(value), index2);
      case "BigInt":
        return as(Object(BigInt(value)), index2);
      case "ArrayBuffer":
        return as(new Uint8Array(value).buffer, value);
      case "DataView": {
        const { buffer } = new Uint8Array(value);
        return as(new DataView(buffer), value);
      }
    }
    return as(new env[type](value), index2);
  };
  return unpair;
};
const deserialize = (serialized) => deserializer(/* @__PURE__ */ new Map(), serialized)(0);
const EMPTY = "";
const { toString } = {};
const { keys } = Object;
const typeOf = (value) => {
  const type = typeof value;
  if (type !== "object" || !value)
    return [PRIMITIVE, type];
  const asString = toString.call(value).slice(8, -1);
  switch (asString) {
    case "Array":
      return [ARRAY, EMPTY];
    case "Object":
      return [OBJECT, EMPTY];
    case "Date":
      return [DATE, EMPTY];
    case "RegExp":
      return [REGEXP, EMPTY];
    case "Map":
      return [MAP, EMPTY];
    case "Set":
      return [SET, EMPTY];
    case "DataView":
      return [ARRAY, asString];
  }
  if (asString.includes("Array"))
    return [ARRAY, asString];
  if (asString.includes("Error"))
    return [ERROR, asString];
  return [OBJECT, asString];
};
const shouldSkip = ([TYPE, type]) => TYPE === PRIMITIVE && (type === "function" || type === "symbol");
const serializer = (strict, json, $, _) => {
  const as = (out, value) => {
    const index2 = _.push(out) - 1;
    $.set(value, index2);
    return index2;
  };
  const pair = (value) => {
    if ($.has(value))
      return $.get(value);
    let [TYPE, type] = typeOf(value);
    switch (TYPE) {
      case PRIMITIVE: {
        let entry = value;
        switch (type) {
          case "bigint":
            TYPE = BIGINT;
            entry = value.toString();
            break;
          case "function":
          case "symbol":
            if (strict)
              throw new TypeError("unable to serialize " + type);
            entry = null;
            break;
          case "undefined":
            return as([VOID], value);
        }
        return as([TYPE, entry], value);
      }
      case ARRAY: {
        if (type) {
          let spread = value;
          if (type === "DataView") {
            spread = new Uint8Array(value.buffer);
          } else if (type === "ArrayBuffer") {
            spread = new Uint8Array(value);
          }
          return as([type, [...spread]], value);
        }
        const arr = [];
        const index2 = as([TYPE, arr], value);
        for (const entry of value)
          arr.push(pair(entry));
        return index2;
      }
      case OBJECT: {
        if (type) {
          switch (type) {
            case "BigInt":
              return as([type, value.toString()], value);
            case "Boolean":
            case "Number":
            case "String":
              return as([type, value.valueOf()], value);
          }
        }
        if (json && "toJSON" in value)
          return pair(value.toJSON());
        const entries = [];
        const index2 = as([TYPE, entries], value);
        for (const key of keys(value)) {
          if (strict || !shouldSkip(typeOf(value[key])))
            entries.push([pair(key), pair(value[key])]);
        }
        return index2;
      }
      case DATE:
        return as([TYPE, value.toISOString()], value);
      case REGEXP: {
        const { source, flags } = value;
        return as([TYPE, { source, flags }], value);
      }
      case MAP: {
        const entries = [];
        const index2 = as([TYPE, entries], value);
        for (const [key, entry] of value) {
          if (strict || !(shouldSkip(typeOf(key)) || shouldSkip(typeOf(entry))))
            entries.push([pair(key), pair(entry)]);
        }
        return index2;
      }
      case SET: {
        const entries = [];
        const index2 = as([TYPE, entries], value);
        for (const entry of value) {
          if (strict || !shouldSkip(typeOf(entry)))
            entries.push(pair(entry));
        }
        return index2;
      }
    }
    const { message } = value;
    return as([TYPE, { name: type, message }], value);
  };
  return pair;
};
const serialize$1 = (value, { json, lossy } = {}) => {
  const _ = [];
  return serializer(!(json || lossy), !!json, /* @__PURE__ */ new Map(), _)(value), _;
};
const structuredClone$1 = typeof structuredClone === "function" ? (
  /* c8 ignore start */
  (any, options) => options && ("json" in options || "lossy" in options) ? deserialize(serialize$1(any, options)) : structuredClone(any)
) : (any, options) => deserialize(serialize$1(any, options));
function defaultFootnoteBackContent(_, rereferenceIndex) {
  const result = [{ type: "text", value: "↩" }];
  if (rereferenceIndex > 1) {
    result.push({
      type: "element",
      tagName: "sup",
      properties: {},
      children: [{ type: "text", value: String(rereferenceIndex) }]
    });
  }
  return result;
}
function defaultFootnoteBackLabel(referenceIndex, rereferenceIndex) {
  return "Back to reference " + (referenceIndex + 1) + (rereferenceIndex > 1 ? "-" + rereferenceIndex : "");
}
function footer(state) {
  const clobberPrefix = typeof state.options.clobberPrefix === "string" ? state.options.clobberPrefix : "user-content-";
  const footnoteBackContent = state.options.footnoteBackContent || defaultFootnoteBackContent;
  const footnoteBackLabel = state.options.footnoteBackLabel || defaultFootnoteBackLabel;
  const footnoteLabel = state.options.footnoteLabel || "Footnotes";
  const footnoteLabelTagName = state.options.footnoteLabelTagName || "h2";
  const footnoteLabelProperties = state.options.footnoteLabelProperties || {
    className: ["sr-only"]
  };
  const listItems = [];
  let referenceIndex = -1;
  while (++referenceIndex < state.footnoteOrder.length) {
    const definition2 = state.footnoteById.get(
      state.footnoteOrder[referenceIndex]
    );
    if (!definition2) {
      continue;
    }
    const content2 = state.all(definition2);
    const id = String(definition2.identifier).toUpperCase();
    const safeId = normalizeUri(id.toLowerCase());
    let rereferenceIndex = 0;
    const backReferences = [];
    const counts = state.footnoteCounts.get(id);
    while (counts !== void 0 && ++rereferenceIndex <= counts) {
      if (backReferences.length > 0) {
        backReferences.push({ type: "text", value: " " });
      }
      let children = typeof footnoteBackContent === "string" ? footnoteBackContent : footnoteBackContent(referenceIndex, rereferenceIndex);
      if (typeof children === "string") {
        children = { type: "text", value: children };
      }
      backReferences.push({
        type: "element",
        tagName: "a",
        properties: {
          href: "#" + clobberPrefix + "fnref-" + safeId + (rereferenceIndex > 1 ? "-" + rereferenceIndex : ""),
          dataFootnoteBackref: "",
          ariaLabel: typeof footnoteBackLabel === "string" ? footnoteBackLabel : footnoteBackLabel(referenceIndex, rereferenceIndex),
          className: ["data-footnote-backref"]
        },
        children: Array.isArray(children) ? children : [children]
      });
    }
    const tail = content2[content2.length - 1];
    if (tail && tail.type === "element" && tail.tagName === "p") {
      const tailTail = tail.children[tail.children.length - 1];
      if (tailTail && tailTail.type === "text") {
        tailTail.value += " ";
      } else {
        tail.children.push({ type: "text", value: " " });
      }
      tail.children.push(...backReferences);
    } else {
      content2.push(...backReferences);
    }
    const listItem2 = {
      type: "element",
      tagName: "li",
      properties: { id: clobberPrefix + "fn-" + safeId },
      children: state.wrap(content2, true)
    };
    state.patch(definition2, listItem2);
    listItems.push(listItem2);
  }
  if (listItems.length === 0) {
    return;
  }
  return {
    type: "element",
    tagName: "section",
    properties: { dataFootnotes: true, className: ["footnotes"] },
    children: [
      {
        type: "element",
        tagName: footnoteLabelTagName,
        properties: {
          ...structuredClone$1(footnoteLabelProperties),
          id: "footnote-label"
        },
        children: [{ type: "text", value: footnoteLabel }]
      },
      { type: "text", value: "\n" },
      {
        type: "element",
        tagName: "ol",
        properties: {},
        children: state.wrap(listItems, true)
      },
      { type: "text", value: "\n" }
    ]
  };
}
const convert = (
  // Note: overloads in JSDoc can’t yet use different `@template`s.
  /**
   * @type {(
   *   (<Condition extends string>(test: Condition) => (node: unknown, index?: number | null | undefined, parent?: Parent | null | undefined, context?: unknown) => node is Node & {type: Condition}) &
   *   (<Condition extends Props>(test: Condition) => (node: unknown, index?: number | null | undefined, parent?: Parent | null | undefined, context?: unknown) => node is Node & Condition) &
   *   (<Condition extends TestFunction>(test: Condition) => (node: unknown, index?: number | null | undefined, parent?: Parent | null | undefined, context?: unknown) => node is Node & Predicate<Condition, Node>) &
   *   ((test?: null | undefined) => (node?: unknown, index?: number | null | undefined, parent?: Parent | null | undefined, context?: unknown) => node is Node) &
   *   ((test?: Test) => Check)
   * )}
   */
  /**
   * @param {Test} [test]
   * @returns {Check}
   */
  (function(test) {
    if (test === null || test === void 0) {
      return ok;
    }
    if (typeof test === "function") {
      return castFactory(test);
    }
    if (typeof test === "object") {
      return Array.isArray(test) ? anyFactory(test) : (
        // Cast because `ReadonlyArray` goes into the above but `isArray`
        // narrows to `Array`.
        propertiesFactory(
          /** @type {Props} */
          test
        )
      );
    }
    if (typeof test === "string") {
      return typeFactory(test);
    }
    throw new Error("Expected function, string, or object as test");
  })
);
function anyFactory(tests) {
  const checks2 = [];
  let index2 = -1;
  while (++index2 < tests.length) {
    checks2[index2] = convert(tests[index2]);
  }
  return castFactory(any);
  function any(...parameters) {
    let index3 = -1;
    while (++index3 < checks2.length) {
      if (checks2[index3].apply(this, parameters)) return true;
    }
    return false;
  }
}
function propertiesFactory(check) {
  const checkAsRecord = (
    /** @type {Record<string, unknown>} */
    check
  );
  return castFactory(all2);
  function all2(node2) {
    const nodeAsRecord = (
      /** @type {Record<string, unknown>} */
      /** @type {unknown} */
      node2
    );
    let key;
    for (key in check) {
      if (nodeAsRecord[key] !== checkAsRecord[key]) return false;
    }
    return true;
  }
}
function typeFactory(check) {
  return castFactory(type);
  function type(node2) {
    return node2 && node2.type === check;
  }
}
function castFactory(testFunction) {
  return check;
  function check(value, index2, parent) {
    return Boolean(
      looksLikeANode(value) && testFunction.call(
        this,
        value,
        typeof index2 === "number" ? index2 : void 0,
        parent || void 0
      )
    );
  }
}
function ok() {
  return true;
}
function looksLikeANode(value) {
  return value !== null && typeof value === "object" && "type" in value;
}
function color(d) {
  return d;
}
const empty = [];
const CONTINUE = true;
const EXIT = false;
const SKIP = "skip";
function visitParents(tree, test, visitor, reverse) {
  let check;
  if (typeof test === "function" && typeof visitor !== "function") {
    reverse = visitor;
    visitor = test;
  } else {
    check = test;
  }
  const is = convert(check);
  const step = reverse ? -1 : 1;
  factory(tree, void 0, [])();
  function factory(node2, index2, parents) {
    const value = (
      /** @type {Record<string, unknown>} */
      node2 && typeof node2 === "object" ? node2 : {}
    );
    if (typeof value.type === "string") {
      const name2 = (
        // `hast`
        typeof value.tagName === "string" ? value.tagName : (
          // `xast`
          typeof value.name === "string" ? value.name : void 0
        )
      );
      Object.defineProperty(visit2, "name", {
        value: "node (" + color(node2.type + (name2 ? "<" + name2 + ">" : "")) + ")"
      });
    }
    return visit2;
    function visit2() {
      let result = empty;
      let subresult;
      let offset2;
      let grandparents;
      if (!test || is(node2, index2, parents[parents.length - 1] || void 0)) {
        result = toResult(visitor(node2, parents));
        if (result[0] === EXIT) {
          return result;
        }
      }
      if ("children" in node2 && node2.children) {
        const nodeAsParent = (
          /** @type {UnistParent} */
          node2
        );
        if (nodeAsParent.children && result[0] !== SKIP) {
          offset2 = (reverse ? nodeAsParent.children.length : -1) + step;
          grandparents = parents.concat(nodeAsParent);
          while (offset2 > -1 && offset2 < nodeAsParent.children.length) {
            const child = nodeAsParent.children[offset2];
            subresult = factory(child, offset2, grandparents)();
            if (subresult[0] === EXIT) {
              return subresult;
            }
            offset2 = typeof subresult[1] === "number" ? subresult[1] : offset2 + step;
          }
        }
      }
      return result;
    }
  }
}
function toResult(value) {
  if (Array.isArray(value)) {
    return value;
  }
  if (typeof value === "number") {
    return [CONTINUE, value];
  }
  return value === null || value === void 0 ? empty : [value];
}
function visit(tree, testOrVisitor, visitorOrReverse, maybeReverse) {
  let reverse;
  let test;
  let visitor;
  if (typeof testOrVisitor === "function" && typeof visitorOrReverse !== "function") {
    test = void 0;
    visitor = testOrVisitor;
    reverse = visitorOrReverse;
  } else {
    test = testOrVisitor;
    visitor = visitorOrReverse;
    reverse = maybeReverse;
  }
  visitParents(tree, test, overload, reverse);
  function overload(node2, parents) {
    const parent = parents[parents.length - 1];
    const index2 = parent ? parent.children.indexOf(node2) : void 0;
    return visitor(node2, index2, parent);
  }
}
const own$1 = {}.hasOwnProperty;
const emptyOptions$1 = {};
function createState(tree, options) {
  const settings = options || emptyOptions$1;
  const definitionById = /* @__PURE__ */ new Map();
  const footnoteById = /* @__PURE__ */ new Map();
  const footnoteCounts = /* @__PURE__ */ new Map();
  const handlers$1 = { ...handlers, ...settings.handlers };
  const state = {
    all: all2,
    applyData,
    definitionById,
    footnoteById,
    footnoteCounts,
    footnoteOrder: [],
    handlers: handlers$1,
    one: one2,
    options: settings,
    patch,
    wrap: wrap$1
  };
  visit(tree, function(node2) {
    if (node2.type === "definition" || node2.type === "footnoteDefinition") {
      const map2 = node2.type === "definition" ? definitionById : footnoteById;
      const id = String(node2.identifier).toUpperCase();
      if (!map2.has(id)) {
        map2.set(id, node2);
      }
    }
  });
  return state;
  function one2(node2, parent) {
    const type = node2.type;
    const handle2 = state.handlers[type];
    if (own$1.call(state.handlers, type) && handle2) {
      return handle2(state, node2, parent);
    }
    if (state.options.passThrough && state.options.passThrough.includes(type)) {
      if ("children" in node2) {
        const { children, ...shallow } = node2;
        const result = structuredClone$1(shallow);
        result.children = state.all(node2);
        return result;
      }
      return structuredClone$1(node2);
    }
    const unknown = state.options.unknownHandler || defaultUnknownHandler;
    return unknown(state, node2, parent);
  }
  function all2(parent) {
    const values = [];
    if ("children" in parent) {
      const nodes = parent.children;
      let index2 = -1;
      while (++index2 < nodes.length) {
        const result = state.one(nodes[index2], parent);
        if (result) {
          if (index2 && nodes[index2 - 1].type === "break") {
            if (!Array.isArray(result) && result.type === "text") {
              result.value = trimMarkdownSpaceStart(result.value);
            }
            if (!Array.isArray(result) && result.type === "element") {
              const head = result.children[0];
              if (head && head.type === "text") {
                head.value = trimMarkdownSpaceStart(head.value);
              }
            }
          }
          if (Array.isArray(result)) {
            values.push(...result);
          } else {
            values.push(result);
          }
        }
      }
    }
    return values;
  }
}
function patch(from, to) {
  if (from.position) to.position = position$1(from);
}
function applyData(from, to) {
  let result = to;
  if (from && from.data) {
    const hName = from.data.hName;
    const hChildren = from.data.hChildren;
    const hProperties = from.data.hProperties;
    if (typeof hName === "string") {
      if (result.type === "element") {
        result.tagName = hName;
      } else {
        const children = "children" in result ? result.children : [result];
        result = { type: "element", tagName: hName, properties: {}, children };
      }
    }
    if (result.type === "element" && hProperties) {
      Object.assign(result.properties, structuredClone$1(hProperties));
    }
    if ("children" in result && result.children && hChildren !== null && hChildren !== void 0) {
      result.children = hChildren;
    }
  }
  return result;
}
function defaultUnknownHandler(state, node2) {
  const data = node2.data || {};
  const result = "value" in node2 && !(own$1.call(data, "hProperties") || own$1.call(data, "hChildren")) ? { type: "text", value: node2.value } : {
    type: "element",
    tagName: "div",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function wrap$1(nodes, loose) {
  const result = [];
  let index2 = -1;
  if (loose) {
    result.push({ type: "text", value: "\n" });
  }
  while (++index2 < nodes.length) {
    if (index2) result.push({ type: "text", value: "\n" });
    result.push(nodes[index2]);
  }
  if (loose && nodes.length > 0) {
    result.push({ type: "text", value: "\n" });
  }
  return result;
}
function trimMarkdownSpaceStart(value) {
  let index2 = 0;
  let code2 = value.charCodeAt(index2);
  while (code2 === 9 || code2 === 32) {
    index2++;
    code2 = value.charCodeAt(index2);
  }
  return value.slice(index2);
}
function toHast(tree, options) {
  const state = createState(tree, options);
  const node2 = state.one(tree, void 0);
  const foot = footer(state);
  const result = Array.isArray(node2) ? { type: "root", children: node2 } : node2 || { type: "root", children: [] };
  if (foot) {
    result.children.push({ type: "text", value: "\n" }, foot);
  }
  return result;
}
function remarkRehype(destination, options) {
  if (destination && "run" in destination) {
    return async function(tree, file) {
      const hastTree = (
        /** @type {HastRoot} */
        toHast(tree, { file, ...options })
      );
      await destination.run(hastTree, file);
    };
  }
  return function(tree, file) {
    return (
      /** @type {HastRoot} */
      toHast(tree, { file, ...destination || options })
    );
  };
}
function bail(error) {
  if (error) {
    throw error;
  }
}
var extend$1;
var hasRequiredExtend;
function requireExtend() {
  if (hasRequiredExtend) return extend$1;
  hasRequiredExtend = 1;
  var hasOwn = Object.prototype.hasOwnProperty;
  var toStr = Object.prototype.toString;
  var defineProperty = Object.defineProperty;
  var gOPD = Object.getOwnPropertyDescriptor;
  var isArray = function isArray2(arr) {
    if (typeof Array.isArray === "function") {
      return Array.isArray(arr);
    }
    return toStr.call(arr) === "[object Array]";
  };
  var isPlainObject2 = function isPlainObject3(obj) {
    if (!obj || toStr.call(obj) !== "[object Object]") {
      return false;
    }
    var hasOwnConstructor = hasOwn.call(obj, "constructor");
    var hasIsPrototypeOf = obj.constructor && obj.constructor.prototype && hasOwn.call(obj.constructor.prototype, "isPrototypeOf");
    if (obj.constructor && !hasOwnConstructor && !hasIsPrototypeOf) {
      return false;
    }
    var key;
    for (key in obj) {
    }
    return typeof key === "undefined" || hasOwn.call(obj, key);
  };
  var setProperty = function setProperty2(target, options) {
    if (defineProperty && options.name === "__proto__") {
      defineProperty(target, options.name, {
        enumerable: true,
        configurable: true,
        value: options.newValue,
        writable: true
      });
    } else {
      target[options.name] = options.newValue;
    }
  };
  var getProperty = function getProperty2(obj, name2) {
    if (name2 === "__proto__") {
      if (!hasOwn.call(obj, name2)) {
        return void 0;
      } else if (gOPD) {
        return gOPD(obj, name2).value;
      }
    }
    return obj[name2];
  };
  extend$1 = function extend2() {
    var options, name2, src, copy, copyIsArray, clone;
    var target = arguments[0];
    var i = 1;
    var length = arguments.length;
    var deep = false;
    if (typeof target === "boolean") {
      deep = target;
      target = arguments[1] || {};
      i = 2;
    }
    if (target == null || typeof target !== "object" && typeof target !== "function") {
      target = {};
    }
    for (; i < length; ++i) {
      options = arguments[i];
      if (options != null) {
        for (name2 in options) {
          src = getProperty(target, name2);
          copy = getProperty(options, name2);
          if (target !== copy) {
            if (deep && copy && (isPlainObject2(copy) || (copyIsArray = isArray(copy)))) {
              if (copyIsArray) {
                copyIsArray = false;
                clone = src && isArray(src) ? src : [];
              } else {
                clone = src && isPlainObject2(src) ? src : {};
              }
              setProperty(target, { name: name2, newValue: extend2(deep, clone, copy) });
            } else if (typeof copy !== "undefined") {
              setProperty(target, { name: name2, newValue: copy });
            }
          }
        }
      }
    }
    return target;
  };
  return extend$1;
}
var extendExports = requireExtend();
const extend = /* @__PURE__ */ getDefaultExportFromCjs(extendExports);
function isPlainObject(value) {
  if (typeof value !== "object" || value === null) {
    return false;
  }
  const prototype = Object.getPrototypeOf(value);
  return (prototype === null || prototype === Object.prototype || Object.getPrototypeOf(prototype) === null) && !(Symbol.toStringTag in value) && !(Symbol.iterator in value);
}
function trough() {
  const fns = [];
  const pipeline = { run, use };
  return pipeline;
  function run(...values) {
    let middlewareIndex = -1;
    const callback = values.pop();
    if (typeof callback !== "function") {
      throw new TypeError("Expected function as last argument, not " + callback);
    }
    next(null, ...values);
    function next(error, ...output) {
      const fn = fns[++middlewareIndex];
      let index2 = -1;
      if (error) {
        callback(error);
        return;
      }
      while (++index2 < values.length) {
        if (output[index2] === null || output[index2] === void 0) {
          output[index2] = values[index2];
        }
      }
      values = output;
      if (fn) {
        wrap(fn, next)(...output);
      } else {
        callback(null, ...output);
      }
    }
  }
  function use(middelware) {
    if (typeof middelware !== "function") {
      throw new TypeError(
        "Expected `middelware` to be a function, not " + middelware
      );
    }
    fns.push(middelware);
    return pipeline;
  }
}
function wrap(middleware, callback) {
  let called;
  return wrapped;
  function wrapped(...parameters) {
    const fnExpectsCallback = middleware.length > parameters.length;
    let result;
    if (fnExpectsCallback) {
      parameters.push(done);
    }
    try {
      result = middleware.apply(this, parameters);
    } catch (error) {
      const exception = (
        /** @type {Error} */
        error
      );
      if (fnExpectsCallback && called) {
        throw exception;
      }
      return done(exception);
    }
    if (!fnExpectsCallback) {
      if (result && result.then && typeof result.then === "function") {
        result.then(then, done);
      } else if (result instanceof Error) {
        done(result);
      } else {
        then(result);
      }
    }
  }
  function done(error, ...output) {
    if (!called) {
      called = true;
      callback(error, ...output);
    }
  }
  function then(value) {
    done(null, value);
  }
}
const minpath = { basename, dirname, extname, join, sep: "/" };
function basename(path2, extname2) {
  if (extname2 !== void 0 && typeof extname2 !== "string") {
    throw new TypeError('"ext" argument must be a string');
  }
  assertPath$1(path2);
  let start = 0;
  let end = -1;
  let index2 = path2.length;
  let seenNonSlash;
  if (extname2 === void 0 || extname2.length === 0 || extname2.length > path2.length) {
    while (index2--) {
      if (path2.codePointAt(index2) === 47) {
        if (seenNonSlash) {
          start = index2 + 1;
          break;
        }
      } else if (end < 0) {
        seenNonSlash = true;
        end = index2 + 1;
      }
    }
    return end < 0 ? "" : path2.slice(start, end);
  }
  if (extname2 === path2) {
    return "";
  }
  let firstNonSlashEnd = -1;
  let extnameIndex = extname2.length - 1;
  while (index2--) {
    if (path2.codePointAt(index2) === 47) {
      if (seenNonSlash) {
        start = index2 + 1;
        break;
      }
    } else {
      if (firstNonSlashEnd < 0) {
        seenNonSlash = true;
        firstNonSlashEnd = index2 + 1;
      }
      if (extnameIndex > -1) {
        if (path2.codePointAt(index2) === extname2.codePointAt(extnameIndex--)) {
          if (extnameIndex < 0) {
            end = index2;
          }
        } else {
          extnameIndex = -1;
          end = firstNonSlashEnd;
        }
      }
    }
  }
  if (start === end) {
    end = firstNonSlashEnd;
  } else if (end < 0) {
    end = path2.length;
  }
  return path2.slice(start, end);
}
function dirname(path2) {
  assertPath$1(path2);
  if (path2.length === 0) {
    return ".";
  }
  let end = -1;
  let index2 = path2.length;
  let unmatchedSlash;
  while (--index2) {
    if (path2.codePointAt(index2) === 47) {
      if (unmatchedSlash) {
        end = index2;
        break;
      }
    } else if (!unmatchedSlash) {
      unmatchedSlash = true;
    }
  }
  return end < 0 ? path2.codePointAt(0) === 47 ? "/" : "." : end === 1 && path2.codePointAt(0) === 47 ? "//" : path2.slice(0, end);
}
function extname(path2) {
  assertPath$1(path2);
  let index2 = path2.length;
  let end = -1;
  let startPart = 0;
  let startDot = -1;
  let preDotState = 0;
  let unmatchedSlash;
  while (index2--) {
    const code2 = path2.codePointAt(index2);
    if (code2 === 47) {
      if (unmatchedSlash) {
        startPart = index2 + 1;
        break;
      }
      continue;
    }
    if (end < 0) {
      unmatchedSlash = true;
      end = index2 + 1;
    }
    if (code2 === 46) {
      if (startDot < 0) {
        startDot = index2;
      } else if (preDotState !== 1) {
        preDotState = 1;
      }
    } else if (startDot > -1) {
      preDotState = -1;
    }
  }
  if (startDot < 0 || end < 0 || // We saw a non-dot character immediately before the dot.
  preDotState === 0 || // The (right-most) trimmed path component is exactly `..`.
  preDotState === 1 && startDot === end - 1 && startDot === startPart + 1) {
    return "";
  }
  return path2.slice(startDot, end);
}
function join(...segments) {
  let index2 = -1;
  let joined;
  while (++index2 < segments.length) {
    assertPath$1(segments[index2]);
    if (segments[index2]) {
      joined = joined === void 0 ? segments[index2] : joined + "/" + segments[index2];
    }
  }
  return joined === void 0 ? "." : normalize(joined);
}
function normalize(path2) {
  assertPath$1(path2);
  const absolute = path2.codePointAt(0) === 47;
  let value = normalizeString(path2, !absolute);
  if (value.length === 0 && !absolute) {
    value = ".";
  }
  if (value.length > 0 && path2.codePointAt(path2.length - 1) === 47) {
    value += "/";
  }
  return absolute ? "/" + value : value;
}
function normalizeString(path2, allowAboveRoot) {
  let result = "";
  let lastSegmentLength = 0;
  let lastSlash = -1;
  let dots = 0;
  let index2 = -1;
  let code2;
  let lastSlashIndex;
  while (++index2 <= path2.length) {
    if (index2 < path2.length) {
      code2 = path2.codePointAt(index2);
    } else if (code2 === 47) {
      break;
    } else {
      code2 = 47;
    }
    if (code2 === 47) {
      if (lastSlash === index2 - 1 || dots === 1) ;
      else if (lastSlash !== index2 - 1 && dots === 2) {
        if (result.length < 2 || lastSegmentLength !== 2 || result.codePointAt(result.length - 1) !== 46 || result.codePointAt(result.length - 2) !== 46) {
          if (result.length > 2) {
            lastSlashIndex = result.lastIndexOf("/");
            if (lastSlashIndex !== result.length - 1) {
              if (lastSlashIndex < 0) {
                result = "";
                lastSegmentLength = 0;
              } else {
                result = result.slice(0, lastSlashIndex);
                lastSegmentLength = result.length - 1 - result.lastIndexOf("/");
              }
              lastSlash = index2;
              dots = 0;
              continue;
            }
          } else if (result.length > 0) {
            result = "";
            lastSegmentLength = 0;
            lastSlash = index2;
            dots = 0;
            continue;
          }
        }
        if (allowAboveRoot) {
          result = result.length > 0 ? result + "/.." : "..";
          lastSegmentLength = 2;
        }
      } else {
        if (result.length > 0) {
          result += "/" + path2.slice(lastSlash + 1, index2);
        } else {
          result = path2.slice(lastSlash + 1, index2);
        }
        lastSegmentLength = index2 - lastSlash - 1;
      }
      lastSlash = index2;
      dots = 0;
    } else if (code2 === 46 && dots > -1) {
      dots++;
    } else {
      dots = -1;
    }
  }
  return result;
}
function assertPath$1(path2) {
  if (typeof path2 !== "string") {
    throw new TypeError(
      "Path must be a string. Received " + JSON.stringify(path2)
    );
  }
}
const minproc = { cwd };
function cwd() {
  return "/";
}
function isUrl(fileUrlOrPath) {
  return Boolean(
    fileUrlOrPath !== null && typeof fileUrlOrPath === "object" && "href" in fileUrlOrPath && fileUrlOrPath.href && "protocol" in fileUrlOrPath && fileUrlOrPath.protocol && // @ts-expect-error: indexing is fine.
    fileUrlOrPath.auth === void 0
  );
}
function urlToPath(path2) {
  if (typeof path2 === "string") {
    path2 = new URL(path2);
  } else if (!isUrl(path2)) {
    const error = new TypeError(
      'The "path" argument must be of type string or an instance of URL. Received `' + path2 + "`"
    );
    error.code = "ERR_INVALID_ARG_TYPE";
    throw error;
  }
  if (path2.protocol !== "file:") {
    const error = new TypeError("The URL must be of scheme file");
    error.code = "ERR_INVALID_URL_SCHEME";
    throw error;
  }
  return getPathFromURLPosix(path2);
}
function getPathFromURLPosix(url) {
  if (url.hostname !== "") {
    const error = new TypeError(
      'File URL host must be "localhost" or empty on darwin'
    );
    error.code = "ERR_INVALID_FILE_URL_HOST";
    throw error;
  }
  const pathname = url.pathname;
  let index2 = -1;
  while (++index2 < pathname.length) {
    if (pathname.codePointAt(index2) === 37 && pathname.codePointAt(index2 + 1) === 50) {
      const third = pathname.codePointAt(index2 + 2);
      if (third === 70 || third === 102) {
        const error = new TypeError(
          "File URL path must not include encoded / characters"
        );
        error.code = "ERR_INVALID_FILE_URL_PATH";
        throw error;
      }
    }
  }
  return decodeURIComponent(pathname);
}
const order = (
  /** @type {const} */
  [
    "history",
    "path",
    "basename",
    "stem",
    "extname",
    "dirname"
  ]
);
class VFile {
  /**
   * Create a new virtual file.
   *
   * `options` is treated as:
   *
   * *   `string` or `Uint8Array` — `{value: options}`
   * *   `URL` — `{path: options}`
   * *   `VFile` — shallow copies its data over to the new file
   * *   `object` — all fields are shallow copied over to the new file
   *
   * Path related fields are set in the following order (least specific to
   * most specific): `history`, `path`, `basename`, `stem`, `extname`,
   * `dirname`.
   *
   * You cannot set `dirname` or `extname` without setting either `history`,
   * `path`, `basename`, or `stem` too.
   *
   * @param {Compatible | null | undefined} [value]
   *   File value.
   * @returns
   *   New instance.
   */
  constructor(value) {
    let options;
    if (!value) {
      options = {};
    } else if (isUrl(value)) {
      options = { path: value };
    } else if (typeof value === "string" || isUint8Array$1(value)) {
      options = { value };
    } else {
      options = value;
    }
    this.cwd = "cwd" in options ? "" : minproc.cwd();
    this.data = {};
    this.history = [];
    this.messages = [];
    this.value;
    this.map;
    this.result;
    this.stored;
    let index2 = -1;
    while (++index2 < order.length) {
      const field2 = order[index2];
      if (field2 in options && options[field2] !== void 0 && options[field2] !== null) {
        this[field2] = field2 === "history" ? [...options[field2]] : options[field2];
      }
    }
    let field;
    for (field in options) {
      if (!order.includes(field)) {
        this[field] = options[field];
      }
    }
  }
  /**
   * Get the basename (including extname) (example: `'index.min.js'`).
   *
   * @returns {string | undefined}
   *   Basename.
   */
  get basename() {
    return typeof this.path === "string" ? minpath.basename(this.path) : void 0;
  }
  /**
   * Set basename (including extname) (`'index.min.js'`).
   *
   * Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
   * on windows).
   * Cannot be nullified (use `file.path = file.dirname` instead).
   *
   * @param {string} basename
   *   Basename.
   * @returns {undefined}
   *   Nothing.
   */
  set basename(basename2) {
    assertNonEmpty(basename2, "basename");
    assertPart(basename2, "basename");
    this.path = minpath.join(this.dirname || "", basename2);
  }
  /**
   * Get the parent path (example: `'~'`).
   *
   * @returns {string | undefined}
   *   Dirname.
   */
  get dirname() {
    return typeof this.path === "string" ? minpath.dirname(this.path) : void 0;
  }
  /**
   * Set the parent path (example: `'~'`).
   *
   * Cannot be set if there’s no `path` yet.
   *
   * @param {string | undefined} dirname
   *   Dirname.
   * @returns {undefined}
   *   Nothing.
   */
  set dirname(dirname2) {
    assertPath(this.basename, "dirname");
    this.path = minpath.join(dirname2 || "", this.basename);
  }
  /**
   * Get the extname (including dot) (example: `'.js'`).
   *
   * @returns {string | undefined}
   *   Extname.
   */
  get extname() {
    return typeof this.path === "string" ? minpath.extname(this.path) : void 0;
  }
  /**
   * Set the extname (including dot) (example: `'.js'`).
   *
   * Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
   * on windows).
   * Cannot be set if there’s no `path` yet.
   *
   * @param {string | undefined} extname
   *   Extname.
   * @returns {undefined}
   *   Nothing.
   */
  set extname(extname2) {
    assertPart(extname2, "extname");
    assertPath(this.dirname, "extname");
    if (extname2) {
      if (extname2.codePointAt(0) !== 46) {
        throw new Error("`extname` must start with `.`");
      }
      if (extname2.includes(".", 1)) {
        throw new Error("`extname` cannot contain multiple dots");
      }
    }
    this.path = minpath.join(this.dirname, this.stem + (extname2 || ""));
  }
  /**
   * Get the full path (example: `'~/index.min.js'`).
   *
   * @returns {string}
   *   Path.
   */
  get path() {
    return this.history[this.history.length - 1];
  }
  /**
   * Set the full path (example: `'~/index.min.js'`).
   *
   * Cannot be nullified.
   * You can set a file URL (a `URL` object with a `file:` protocol) which will
   * be turned into a path with `url.fileURLToPath`.
   *
   * @param {URL | string} path
   *   Path.
   * @returns {undefined}
   *   Nothing.
   */
  set path(path2) {
    if (isUrl(path2)) {
      path2 = urlToPath(path2);
    }
    assertNonEmpty(path2, "path");
    if (this.path !== path2) {
      this.history.push(path2);
    }
  }
  /**
   * Get the stem (basename w/o extname) (example: `'index.min'`).
   *
   * @returns {string | undefined}
   *   Stem.
   */
  get stem() {
    return typeof this.path === "string" ? minpath.basename(this.path, this.extname) : void 0;
  }
  /**
   * Set the stem (basename w/o extname) (example: `'index.min'`).
   *
   * Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
   * on windows).
   * Cannot be nullified (use `file.path = file.dirname` instead).
   *
   * @param {string} stem
   *   Stem.
   * @returns {undefined}
   *   Nothing.
   */
  set stem(stem) {
    assertNonEmpty(stem, "stem");
    assertPart(stem, "stem");
    this.path = minpath.join(this.dirname || "", stem + (this.extname || ""));
  }
  // Normal prototypal methods.
  /**
   * Create a fatal message for `reason` associated with the file.
   *
   * The `fatal` field of the message is set to `true` (error; file not usable)
   * and the `file` field is set to the current file path.
   * The message is added to the `messages` field on `file`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {MessageOptions | null | undefined} [options]
   * @returns {never}
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | MessageOptions | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns {never}
   *   Never.
   * @throws {VFileMessage}
   *   Message.
   */
  fail(causeOrReason, optionsOrParentOrPlace, origin) {
    const message = this.message(causeOrReason, optionsOrParentOrPlace, origin);
    message.fatal = true;
    throw message;
  }
  /**
   * Create an info message for `reason` associated with the file.
   *
   * The `fatal` field of the message is set to `undefined` (info; change
   * likely not needed) and the `file` field is set to the current file path.
   * The message is added to the `messages` field on `file`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {MessageOptions | null | undefined} [options]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | MessageOptions | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns {VFileMessage}
   *   Message.
   */
  info(causeOrReason, optionsOrParentOrPlace, origin) {
    const message = this.message(causeOrReason, optionsOrParentOrPlace, origin);
    message.fatal = void 0;
    return message;
  }
  /**
   * Create a message for `reason` associated with the file.
   *
   * The `fatal` field of the message is set to `false` (warning; change may be
   * needed) and the `file` field is set to the current file path.
   * The message is added to the `messages` field on `file`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {MessageOptions | null | undefined} [options]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | MessageOptions | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns {VFileMessage}
   *   Message.
   */
  message(causeOrReason, optionsOrParentOrPlace, origin) {
    const message = new VFileMessage(
      // @ts-expect-error: the overloads are fine.
      causeOrReason,
      optionsOrParentOrPlace,
      origin
    );
    if (this.path) {
      message.name = this.path + ":" + message.name;
      message.file = this.path;
    }
    message.fatal = false;
    this.messages.push(message);
    return message;
  }
  /**
   * Serialize the file.
   *
   * > **Note**: which encodings are supported depends on the engine.
   * > For info on Node.js, see:
   * > <https://nodejs.org/api/util.html#whatwg-supported-encodings>.
   *
   * @param {string | null | undefined} [encoding='utf8']
   *   Character encoding to understand `value` as when it’s a `Uint8Array`
   *   (default: `'utf-8'`).
   * @returns {string}
   *   Serialized file.
   */
  toString(encoding) {
    if (this.value === void 0) {
      return "";
    }
    if (typeof this.value === "string") {
      return this.value;
    }
    const decoder = new TextDecoder(encoding || void 0);
    return decoder.decode(this.value);
  }
}
function assertPart(part, name2) {
  if (part && part.includes(minpath.sep)) {
    throw new Error(
      "`" + name2 + "` cannot be a path: did not expect `" + minpath.sep + "`"
    );
  }
}
function assertNonEmpty(part, name2) {
  if (!part) {
    throw new Error("`" + name2 + "` cannot be empty");
  }
}
function assertPath(path2, name2) {
  if (!path2) {
    throw new Error("Setting `" + name2 + "` requires `path` to be set too");
  }
}
function isUint8Array$1(value) {
  return Boolean(
    value && typeof value === "object" && "byteLength" in value && "byteOffset" in value
  );
}
const CallableInstance = (
  /**
   * @type {new <Parameters extends Array<unknown>, Result>(property: string | symbol) => (...parameters: Parameters) => Result}
   */
  /** @type {unknown} */
  /**
   * @this {Function}
   * @param {string | symbol} property
   * @returns {(...parameters: Array<unknown>) => unknown}
   */
  (function(property) {
    const self2 = this;
    const constr = self2.constructor;
    const proto = (
      /** @type {Record<string | symbol, Function>} */
      // Prototypes do exist.
      // type-coverage:ignore-next-line
      constr.prototype
    );
    const value = proto[property];
    const apply = function() {
      return value.apply(apply, arguments);
    };
    Object.setPrototypeOf(apply, proto);
    return apply;
  })
);
const own = {}.hasOwnProperty;
class Processor extends CallableInstance {
  /**
   * Create a processor.
   */
  constructor() {
    super("copy");
    this.Compiler = void 0;
    this.Parser = void 0;
    this.attachers = [];
    this.compiler = void 0;
    this.freezeIndex = -1;
    this.frozen = void 0;
    this.namespace = {};
    this.parser = void 0;
    this.transformers = trough();
  }
  /**
   * Copy a processor.
   *
   * @deprecated
   *   This is a private internal method and should not be used.
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *   New *unfrozen* processor ({@linkcode Processor}) that is
   *   configured to work the same as its ancestor.
   *   When the descendant processor is configured in the future it does not
   *   affect the ancestral processor.
   */
  copy() {
    const destination = (
      /** @type {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>} */
      new Processor()
    );
    let index2 = -1;
    while (++index2 < this.attachers.length) {
      const attacher = this.attachers[index2];
      destination.use(...attacher);
    }
    destination.data(extend(true, {}, this.namespace));
    return destination;
  }
  /**
   * Configure the processor with info available to all plugins.
   * Information is stored in an object.
   *
   * Typically, options can be given to a specific plugin, but sometimes it
   * makes sense to have information shared with several plugins.
   * For example, a list of HTML elements that are self-closing, which is
   * needed during all phases.
   *
   * > **Note**: setting information cannot occur on *frozen* processors.
   * > Call the processor first to create a new unfrozen processor.
   *
   * > **Note**: to register custom data in TypeScript, augment the
   * > {@linkcode Data} interface.
   *
   * @example
   *   This example show how to get and set info:
   *
   *   ```js
   *   import {unified} from 'unified'
   *
   *   const processor = unified().data('alpha', 'bravo')
   *
   *   processor.data('alpha') // => 'bravo'
   *
   *   processor.data() // => {alpha: 'bravo'}
   *
   *   processor.data({charlie: 'delta'})
   *
   *   processor.data() // => {charlie: 'delta'}
   *   ```
   *
   * @template {keyof Data} Key
   *
   * @overload
   * @returns {Data}
   *
   * @overload
   * @param {Data} dataset
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @overload
   * @param {Key} key
   * @returns {Data[Key]}
   *
   * @overload
   * @param {Key} key
   * @param {Data[Key]} value
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @param {Data | Key} [key]
   *   Key to get or set, or entire dataset to set, or nothing to get the
   *   entire dataset (optional).
   * @param {Data[Key]} [value]
   *   Value to set (optional).
   * @returns {unknown}
   *   The current processor when setting, the value at `key` when getting, or
   *   the entire dataset when getting without key.
   */
  data(key, value) {
    if (typeof key === "string") {
      if (arguments.length === 2) {
        assertUnfrozen("data", this.frozen);
        this.namespace[key] = value;
        return this;
      }
      return own.call(this.namespace, key) && this.namespace[key] || void 0;
    }
    if (key) {
      assertUnfrozen("data", this.frozen);
      this.namespace = key;
      return this;
    }
    return this.namespace;
  }
  /**
   * Freeze a processor.
   *
   * Frozen processors are meant to be extended and not to be configured
   * directly.
   *
   * When a processor is frozen it cannot be unfrozen.
   * New processors working the same way can be created by calling the
   * processor.
   *
   * It’s possible to freeze processors explicitly by calling `.freeze()`.
   * Processors freeze automatically when `.parse()`, `.run()`, `.runSync()`,
   * `.stringify()`, `.process()`, or `.processSync()` are called.
   *
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *   The current processor.
   */
  freeze() {
    if (this.frozen) {
      return this;
    }
    const self2 = (
      /** @type {Processor} */
      /** @type {unknown} */
      this
    );
    while (++this.freezeIndex < this.attachers.length) {
      const [attacher, ...options] = this.attachers[this.freezeIndex];
      if (options[0] === false) {
        continue;
      }
      if (options[0] === true) {
        options[0] = void 0;
      }
      const transformer = attacher.call(self2, ...options);
      if (typeof transformer === "function") {
        this.transformers.use(transformer);
      }
    }
    this.frozen = true;
    this.freezeIndex = Number.POSITIVE_INFINITY;
    return this;
  }
  /**
   * Parse text to a syntax tree.
   *
   * > **Note**: `parse` freezes the processor if not already *frozen*.
   *
   * > **Note**: `parse` performs the parse phase, not the run phase or other
   * > phases.
   *
   * @param {Compatible | undefined} [file]
   *   file to parse (optional); typically `string` or `VFile`; any value
   *   accepted as `x` in `new VFile(x)`.
   * @returns {ParseTree extends undefined ? Node : ParseTree}
   *   Syntax tree representing `file`.
   */
  parse(file) {
    this.freeze();
    const realFile = vfile(file);
    const parser = this.parser || this.Parser;
    assertParser("parse", parser);
    return parser(String(realFile), realFile);
  }
  /**
   * Process the given file as configured on the processor.
   *
   * > **Note**: `process` freezes the processor if not already *frozen*.
   *
   * > **Note**: `process` performs the parse, run, and stringify phases.
   *
   * @overload
   * @param {Compatible | undefined} file
   * @param {ProcessCallback<VFileWithOutput<CompileResult>>} done
   * @returns {undefined}
   *
   * @overload
   * @param {Compatible | undefined} [file]
   * @returns {Promise<VFileWithOutput<CompileResult>>}
   *
   * @param {Compatible | undefined} [file]
   *   File (optional); typically `string` or `VFile`]; any value accepted as
   *   `x` in `new VFile(x)`.
   * @param {ProcessCallback<VFileWithOutput<CompileResult>> | undefined} [done]
   *   Callback (optional).
   * @returns {Promise<VFile> | undefined}
   *   Nothing if `done` is given.
   *   Otherwise a promise, rejected with a fatal error or resolved with the
   *   processed file.
   *
   *   The parsed, transformed, and compiled value is available at
   *   `file.value` (see note).
   *
   *   > **Note**: unified typically compiles by serializing: most
   *   > compilers return `string` (or `Uint8Array`).
   *   > Some compilers, such as the one configured with
   *   > [`rehype-react`][rehype-react], return other values (in this case, a
   *   > React tree).
   *   > If you’re using a compiler that doesn’t serialize, expect different
   *   > result values.
   *   >
   *   > To register custom results in TypeScript, add them to
   *   > {@linkcode CompileResultMap}.
   *
   *   [rehype-react]: https://github.com/rehypejs/rehype-react
   */
  process(file, done) {
    const self2 = this;
    this.freeze();
    assertParser("process", this.parser || this.Parser);
    assertCompiler("process", this.compiler || this.Compiler);
    return done ? executor(void 0, done) : new Promise(executor);
    function executor(resolve, reject) {
      const realFile = vfile(file);
      const parseTree = (
        /** @type {HeadTree extends undefined ? Node : HeadTree} */
        /** @type {unknown} */
        self2.parse(realFile)
      );
      self2.run(parseTree, realFile, function(error, tree, file2) {
        if (error || !tree || !file2) {
          return realDone(error);
        }
        const compileTree = (
          /** @type {CompileTree extends undefined ? Node : CompileTree} */
          /** @type {unknown} */
          tree
        );
        const compileResult = self2.stringify(compileTree, file2);
        if (looksLikeAValue(compileResult)) {
          file2.value = compileResult;
        } else {
          file2.result = compileResult;
        }
        realDone(
          error,
          /** @type {VFileWithOutput<CompileResult>} */
          file2
        );
      });
      function realDone(error, file2) {
        if (error || !file2) {
          reject(error);
        } else if (resolve) {
          resolve(file2);
        } else {
          done(void 0, file2);
        }
      }
    }
  }
  /**
   * Process the given file as configured on the processor.
   *
   * An error is thrown if asynchronous transforms are configured.
   *
   * > **Note**: `processSync` freezes the processor if not already *frozen*.
   *
   * > **Note**: `processSync` performs the parse, run, and stringify phases.
   *
   * @param {Compatible | undefined} [file]
   *   File (optional); typically `string` or `VFile`; any value accepted as
   *   `x` in `new VFile(x)`.
   * @returns {VFileWithOutput<CompileResult>}
   *   The processed file.
   *
   *   The parsed, transformed, and compiled value is available at
   *   `file.value` (see note).
   *
   *   > **Note**: unified typically compiles by serializing: most
   *   > compilers return `string` (or `Uint8Array`).
   *   > Some compilers, such as the one configured with
   *   > [`rehype-react`][rehype-react], return other values (in this case, a
   *   > React tree).
   *   > If you’re using a compiler that doesn’t serialize, expect different
   *   > result values.
   *   >
   *   > To register custom results in TypeScript, add them to
   *   > {@linkcode CompileResultMap}.
   *
   *   [rehype-react]: https://github.com/rehypejs/rehype-react
   */
  processSync(file) {
    let complete = false;
    let result;
    this.freeze();
    assertParser("processSync", this.parser || this.Parser);
    assertCompiler("processSync", this.compiler || this.Compiler);
    this.process(file, realDone);
    assertDone("processSync", "process", complete);
    return result;
    function realDone(error, file2) {
      complete = true;
      bail(error);
      result = file2;
    }
  }
  /**
   * Run *transformers* on a syntax tree.
   *
   * > **Note**: `run` freezes the processor if not already *frozen*.
   *
   * > **Note**: `run` performs the run phase, not other phases.
   *
   * @overload
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   * @param {RunCallback<TailTree extends undefined ? Node : TailTree>} done
   * @returns {undefined}
   *
   * @overload
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   * @param {Compatible | undefined} file
   * @param {RunCallback<TailTree extends undefined ? Node : TailTree>} done
   * @returns {undefined}
   *
   * @overload
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   * @param {Compatible | undefined} [file]
   * @returns {Promise<TailTree extends undefined ? Node : TailTree>}
   *
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   *   Tree to transform and inspect.
   * @param {(
   *   RunCallback<TailTree extends undefined ? Node : TailTree> |
   *   Compatible
   * )} [file]
   *   File associated with `node` (optional); any value accepted as `x` in
   *   `new VFile(x)`.
   * @param {RunCallback<TailTree extends undefined ? Node : TailTree>} [done]
   *   Callback (optional).
   * @returns {Promise<TailTree extends undefined ? Node : TailTree> | undefined}
   *   Nothing if `done` is given.
   *   Otherwise, a promise rejected with a fatal error or resolved with the
   *   transformed tree.
   */
  run(tree, file, done) {
    assertNode(tree);
    this.freeze();
    const transformers = this.transformers;
    if (!done && typeof file === "function") {
      done = file;
      file = void 0;
    }
    return done ? executor(void 0, done) : new Promise(executor);
    function executor(resolve, reject) {
      const realFile = vfile(file);
      transformers.run(tree, realFile, realDone);
      function realDone(error, outputTree, file2) {
        const resultingTree = (
          /** @type {TailTree extends undefined ? Node : TailTree} */
          outputTree || tree
        );
        if (error) {
          reject(error);
        } else if (resolve) {
          resolve(resultingTree);
        } else {
          done(void 0, resultingTree, file2);
        }
      }
    }
  }
  /**
   * Run *transformers* on a syntax tree.
   *
   * An error is thrown if asynchronous transforms are configured.
   *
   * > **Note**: `runSync` freezes the processor if not already *frozen*.
   *
   * > **Note**: `runSync` performs the run phase, not other phases.
   *
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   *   Tree to transform and inspect.
   * @param {Compatible | undefined} [file]
   *   File associated with `node` (optional); any value accepted as `x` in
   *   `new VFile(x)`.
   * @returns {TailTree extends undefined ? Node : TailTree}
   *   Transformed tree.
   */
  runSync(tree, file) {
    let complete = false;
    let result;
    this.run(tree, file, realDone);
    assertDone("runSync", "run", complete);
    return result;
    function realDone(error, tree2) {
      bail(error);
      result = tree2;
      complete = true;
    }
  }
  /**
   * Compile a syntax tree.
   *
   * > **Note**: `stringify` freezes the processor if not already *frozen*.
   *
   * > **Note**: `stringify` performs the stringify phase, not the run phase
   * > or other phases.
   *
   * @param {CompileTree extends undefined ? Node : CompileTree} tree
   *   Tree to compile.
   * @param {Compatible | undefined} [file]
   *   File associated with `node` (optional); any value accepted as `x` in
   *   `new VFile(x)`.
   * @returns {CompileResult extends undefined ? Value : CompileResult}
   *   Textual representation of the tree (see note).
   *
   *   > **Note**: unified typically compiles by serializing: most compilers
   *   > return `string` (or `Uint8Array`).
   *   > Some compilers, such as the one configured with
   *   > [`rehype-react`][rehype-react], return other values (in this case, a
   *   > React tree).
   *   > If you’re using a compiler that doesn’t serialize, expect different
   *   > result values.
   *   >
   *   > To register custom results in TypeScript, add them to
   *   > {@linkcode CompileResultMap}.
   *
   *   [rehype-react]: https://github.com/rehypejs/rehype-react
   */
  stringify(tree, file) {
    this.freeze();
    const realFile = vfile(file);
    const compiler2 = this.compiler || this.Compiler;
    assertCompiler("stringify", compiler2);
    assertNode(tree);
    return compiler2(tree, realFile);
  }
  /**
   * Configure the processor to use a plugin, a list of usable values, or a
   * preset.
   *
   * If the processor is already using a plugin, the previous plugin
   * configuration is changed based on the options that are passed in.
   * In other words, the plugin is not added a second time.
   *
   * > **Note**: `use` cannot be called on *frozen* processors.
   * > Call the processor first to create a new unfrozen processor.
   *
   * @example
   *   There are many ways to pass plugins to `.use()`.
   *   This example gives an overview:
   *
   *   ```js
   *   import {unified} from 'unified'
   *
   *   unified()
   *     // Plugin with options:
   *     .use(pluginA, {x: true, y: true})
   *     // Passing the same plugin again merges configuration (to `{x: true, y: false, z: true}`):
   *     .use(pluginA, {y: false, z: true})
   *     // Plugins:
   *     .use([pluginB, pluginC])
   *     // Two plugins, the second with options:
   *     .use([pluginD, [pluginE, {}]])
   *     // Preset with plugins and settings:
   *     .use({plugins: [pluginF, [pluginG, {}]], settings: {position: false}})
   *     // Settings only:
   *     .use({settings: {position: false}})
   *   ```
   *
   * @template {Array<unknown>} [Parameters=[]]
   * @template {Node | string | undefined} [Input=undefined]
   * @template [Output=Input]
   *
   * @overload
   * @param {Preset | null | undefined} [preset]
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @overload
   * @param {PluggableList} list
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @overload
   * @param {Plugin<Parameters, Input, Output>} plugin
   * @param {...(Parameters | [boolean])} parameters
   * @returns {UsePlugin<ParseTree, HeadTree, TailTree, CompileTree, CompileResult, Input, Output>}
   *
   * @param {PluggableList | Plugin | Preset | null | undefined} value
   *   Usable value.
   * @param {...unknown} parameters
   *   Parameters, when a plugin is given as a usable value.
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *   Current processor.
   */
  use(value, ...parameters) {
    const attachers = this.attachers;
    const namespace = this.namespace;
    assertUnfrozen("use", this.frozen);
    if (value === null || value === void 0) ;
    else if (typeof value === "function") {
      addPlugin(value, parameters);
    } else if (typeof value === "object") {
      if (Array.isArray(value)) {
        addList(value);
      } else {
        addPreset(value);
      }
    } else {
      throw new TypeError("Expected usable value, not `" + value + "`");
    }
    return this;
    function add(value2) {
      if (typeof value2 === "function") {
        addPlugin(value2, []);
      } else if (typeof value2 === "object") {
        if (Array.isArray(value2)) {
          const [plugin, ...parameters2] = (
            /** @type {PluginTuple<Array<unknown>>} */
            value2
          );
          addPlugin(plugin, parameters2);
        } else {
          addPreset(value2);
        }
      } else {
        throw new TypeError("Expected usable value, not `" + value2 + "`");
      }
    }
    function addPreset(result) {
      if (!("plugins" in result) && !("settings" in result)) {
        throw new Error(
          "Expected usable value but received an empty preset, which is probably a mistake: presets typically come with `plugins` and sometimes with `settings`, but this has neither"
        );
      }
      addList(result.plugins);
      if (result.settings) {
        namespace.settings = extend(true, namespace.settings, result.settings);
      }
    }
    function addList(plugins) {
      let index2 = -1;
      if (plugins === null || plugins === void 0) ;
      else if (Array.isArray(plugins)) {
        while (++index2 < plugins.length) {
          const thing = plugins[index2];
          add(thing);
        }
      } else {
        throw new TypeError("Expected a list of plugins, not `" + plugins + "`");
      }
    }
    function addPlugin(plugin, parameters2) {
      let index2 = -1;
      let entryIndex = -1;
      while (++index2 < attachers.length) {
        if (attachers[index2][0] === plugin) {
          entryIndex = index2;
          break;
        }
      }
      if (entryIndex === -1) {
        attachers.push([plugin, ...parameters2]);
      } else if (parameters2.length > 0) {
        let [primary, ...rest] = parameters2;
        const currentPrimary = attachers[entryIndex][1];
        if (isPlainObject(currentPrimary) && isPlainObject(primary)) {
          primary = extend(true, currentPrimary, primary);
        }
        attachers[entryIndex] = [plugin, primary, ...rest];
      }
    }
  }
}
const unified = new Processor().freeze();
function assertParser(name2, value) {
  if (typeof value !== "function") {
    throw new TypeError("Cannot `" + name2 + "` without `parser`");
  }
}
function assertCompiler(name2, value) {
  if (typeof value !== "function") {
    throw new TypeError("Cannot `" + name2 + "` without `compiler`");
  }
}
function assertUnfrozen(name2, frozen) {
  if (frozen) {
    throw new Error(
      "Cannot call `" + name2 + "` on a frozen processor.\nCreate a new processor first, by calling it: use `processor()` instead of `processor`."
    );
  }
}
function assertNode(node2) {
  if (!isPlainObject(node2) || typeof node2.type !== "string") {
    throw new TypeError("Expected node, got `" + node2 + "`");
  }
}
function assertDone(name2, asyncName, complete) {
  if (!complete) {
    throw new Error(
      "`" + name2 + "` finished async. Use `" + asyncName + "` instead"
    );
  }
}
function vfile(value) {
  return looksLikeAVFile(value) ? value : new VFile(value);
}
function looksLikeAVFile(value) {
  return Boolean(
    value && typeof value === "object" && "message" in value && "messages" in value
  );
}
function looksLikeAValue(value) {
  return typeof value === "string" || isUint8Array(value);
}
function isUint8Array(value) {
  return Boolean(
    value && typeof value === "object" && "byteLength" in value && "byteOffset" in value
  );
}
const changelog = "https://github.com/remarkjs/react-markdown/blob/main/changelog.md";
const emptyPlugins = [];
const emptyRemarkRehypeOptions = { allowDangerousHtml: true };
const safeProtocol = /^(https?|ircs?|mailto|xmpp)$/i;
const deprecations = [
  { from: "astPlugins", id: "remove-buggy-html-in-markdown-parser" },
  { from: "allowDangerousHtml", id: "remove-buggy-html-in-markdown-parser" },
  {
    from: "allowNode",
    id: "replace-allownode-allowedtypes-and-disallowedtypes",
    to: "allowElement"
  },
  {
    from: "allowedTypes",
    id: "replace-allownode-allowedtypes-and-disallowedtypes",
    to: "allowedElements"
  },
  { from: "className", id: "remove-classname" },
  {
    from: "disallowedTypes",
    id: "replace-allownode-allowedtypes-and-disallowedtypes",
    to: "disallowedElements"
  },
  { from: "escapeHtml", id: "remove-buggy-html-in-markdown-parser" },
  { from: "includeElementIndex", id: "#remove-includeelementindex" },
  {
    from: "includeNodeIndex",
    id: "change-includenodeindex-to-includeelementindex"
  },
  { from: "linkTarget", id: "remove-linktarget" },
  { from: "plugins", id: "change-plugins-to-remarkplugins", to: "remarkPlugins" },
  { from: "rawSourcePos", id: "#remove-rawsourcepos" },
  { from: "renderers", id: "change-renderers-to-components", to: "components" },
  { from: "source", id: "change-source-to-children", to: "children" },
  { from: "sourcePos", id: "#remove-sourcepos" },
  { from: "transformImageUri", id: "#add-urltransform", to: "urlTransform" },
  { from: "transformLinkUri", id: "#add-urltransform", to: "urlTransform" }
];
function Markdown$1(options) {
  const processor = createProcessor(options);
  const file = createFile(options);
  return post(processor.runSync(processor.parse(file), file), options);
}
function createProcessor(options) {
  const rehypePlugins = options.rehypePlugins || emptyPlugins;
  const remarkPlugins = options.remarkPlugins || emptyPlugins;
  const remarkRehypeOptions = options.remarkRehypeOptions ? { ...options.remarkRehypeOptions, ...emptyRemarkRehypeOptions } : emptyRemarkRehypeOptions;
  const processor = unified().use(remarkParse).use(remarkPlugins).use(remarkRehype, remarkRehypeOptions).use(rehypePlugins);
  return processor;
}
function createFile(options) {
  const children = options.children || "";
  const file = new VFile();
  if (typeof children === "string") {
    file.value = children;
  }
  return file;
}
function post(tree, options) {
  const allowedElements = options.allowedElements;
  const allowElement = options.allowElement;
  const components = options.components;
  const disallowedElements = options.disallowedElements;
  const skipHtml = options.skipHtml;
  const unwrapDisallowed = options.unwrapDisallowed;
  const urlTransform = options.urlTransform || defaultUrlTransform;
  for (const deprecation of deprecations) {
    if (Object.hasOwn(options, deprecation.from)) {
      unreachable(
        "Unexpected `" + deprecation.from + "` prop, " + (deprecation.to ? "use `" + deprecation.to + "` instead" : "remove it") + " (see <" + changelog + "#" + deprecation.id + "> for more info)"
      );
    }
  }
  visit(tree, transform);
  return toJsxRuntime(tree, {
    Fragment: jsxRuntimeExports.Fragment,
    components,
    ignoreInvalidStyle: true,
    jsx: jsxRuntimeExports.jsx,
    jsxs: jsxRuntimeExports.jsxs,
    passKeys: true,
    passNode: true
  });
  function transform(node2, index2, parent) {
    if (node2.type === "raw" && parent && typeof index2 === "number") {
      if (skipHtml) {
        parent.children.splice(index2, 1);
      } else {
        parent.children[index2] = { type: "text", value: node2.value };
      }
      return index2;
    }
    if (node2.type === "element") {
      let key;
      for (key in urlAttributes) {
        if (Object.hasOwn(urlAttributes, key) && Object.hasOwn(node2.properties, key)) {
          const value = node2.properties[key];
          const test = urlAttributes[key];
          if (test === null || test.includes(node2.tagName)) {
            node2.properties[key] = urlTransform(String(value || ""), key, node2);
          }
        }
      }
    }
    if (node2.type === "element") {
      let remove = allowedElements ? !allowedElements.includes(node2.tagName) : disallowedElements ? disallowedElements.includes(node2.tagName) : false;
      if (!remove && allowElement && typeof index2 === "number") {
        remove = !allowElement(node2, index2, parent);
      }
      if (remove && parent && typeof index2 === "number") {
        if (unwrapDisallowed && node2.children) {
          parent.children.splice(index2, 1, ...node2.children);
        } else {
          parent.children.splice(index2, 1);
        }
        return index2;
      }
    }
  }
}
function defaultUrlTransform(value) {
  const colon = value.indexOf(":");
  const questionMark = value.indexOf("?");
  const numberSign = value.indexOf("#");
  const slash = value.indexOf("/");
  if (
    // If there is no protocol, it’s relative.
    colon === -1 || // If the first colon is after a `?`, `#`, or `/`, it’s not a protocol.
    slash !== -1 && colon > slash || questionMark !== -1 && colon > questionMark || numberSign !== -1 && colon > numberSign || // It is a protocol, it should be allowed.
    safeProtocol.test(value.slice(0, colon))
  ) {
    return value;
  }
  return "";
}
function ccount(value, character) {
  const source = String(value);
  if (typeof character !== "string") {
    throw new TypeError("Expected character");
  }
  let count2 = 0;
  let index2 = source.indexOf(character);
  while (index2 !== -1) {
    count2++;
    index2 = source.indexOf(character, index2 + character.length);
  }
  return count2;
}
function escapeStringRegexp(string2) {
  if (typeof string2 !== "string") {
    throw new TypeError("Expected a string");
  }
  return string2.replace(/[|\\{}()[\]^$+*?.]/g, "\\$&").replace(/-/g, "\\x2d");
}
function findAndReplace(tree, list2, options) {
  const settings = options || {};
  const ignored = convert(settings.ignore || []);
  const pairs = toPairs(list2);
  let pairIndex = -1;
  while (++pairIndex < pairs.length) {
    visitParents(tree, "text", visitor);
  }
  function visitor(node2, parents) {
    let index2 = -1;
    let grandparent;
    while (++index2 < parents.length) {
      const parent = parents[index2];
      const siblings = grandparent ? grandparent.children : void 0;
      if (ignored(
        parent,
        siblings ? siblings.indexOf(parent) : void 0,
        grandparent
      )) {
        return;
      }
      grandparent = parent;
    }
    if (grandparent) {
      return handler(node2, parents);
    }
  }
  function handler(node2, parents) {
    const parent = parents[parents.length - 1];
    const find2 = pairs[pairIndex][0];
    const replace2 = pairs[pairIndex][1];
    let start = 0;
    const siblings = parent.children;
    const index2 = siblings.indexOf(node2);
    let change = false;
    let nodes = [];
    find2.lastIndex = 0;
    let match = find2.exec(node2.value);
    while (match) {
      const position2 = match.index;
      const matchObject = {
        index: match.index,
        input: match.input,
        stack: [...parents, node2]
      };
      let value = replace2(...match, matchObject);
      if (typeof value === "string") {
        value = value.length > 0 ? { type: "text", value } : void 0;
      }
      if (value === false) {
        find2.lastIndex = position2 + 1;
      } else {
        if (start !== position2) {
          nodes.push({
            type: "text",
            value: node2.value.slice(start, position2)
          });
        }
        if (Array.isArray(value)) {
          nodes.push(...value);
        } else if (value) {
          nodes.push(value);
        }
        start = position2 + match[0].length;
        change = true;
      }
      if (!find2.global) {
        break;
      }
      match = find2.exec(node2.value);
    }
    if (change) {
      if (start < node2.value.length) {
        nodes.push({ type: "text", value: node2.value.slice(start) });
      }
      parent.children.splice(index2, 1, ...nodes);
    } else {
      nodes = [node2];
    }
    return index2 + nodes.length;
  }
}
function toPairs(tupleOrList) {
  const result = [];
  if (!Array.isArray(tupleOrList)) {
    throw new TypeError("Expected find and replace tuple or list of tuples");
  }
  const list2 = !tupleOrList[0] || Array.isArray(tupleOrList[0]) ? tupleOrList : [tupleOrList];
  let index2 = -1;
  while (++index2 < list2.length) {
    const tuple = list2[index2];
    result.push([toExpression(tuple[0]), toFunction(tuple[1])]);
  }
  return result;
}
function toExpression(find2) {
  return typeof find2 === "string" ? new RegExp(escapeStringRegexp(find2), "g") : find2;
}
function toFunction(replace2) {
  return typeof replace2 === "function" ? replace2 : function() {
    return replace2;
  };
}
const inConstruct = "phrasing";
const notInConstruct = ["autolink", "link", "image", "label"];
function gfmAutolinkLiteralFromMarkdown() {
  return {
    transforms: [transformGfmAutolinkLiterals],
    enter: {
      literalAutolink: enterLiteralAutolink,
      literalAutolinkEmail: enterLiteralAutolinkValue,
      literalAutolinkHttp: enterLiteralAutolinkValue,
      literalAutolinkWww: enterLiteralAutolinkValue
    },
    exit: {
      literalAutolink: exitLiteralAutolink,
      literalAutolinkEmail: exitLiteralAutolinkEmail,
      literalAutolinkHttp: exitLiteralAutolinkHttp,
      literalAutolinkWww: exitLiteralAutolinkWww
    }
  };
}
function gfmAutolinkLiteralToMarkdown() {
  return {
    unsafe: [
      {
        character: "@",
        before: "[+\\-.\\w]",
        after: "[\\-.\\w]",
        inConstruct,
        notInConstruct
      },
      {
        character: ".",
        before: "[Ww]",
        after: "[\\-.\\w]",
        inConstruct,
        notInConstruct
      },
      {
        character: ":",
        before: "[ps]",
        after: "\\/",
        inConstruct,
        notInConstruct
      }
    ]
  };
}
function enterLiteralAutolink(token) {
  this.enter({ type: "link", title: null, url: "", children: [] }, token);
}
function enterLiteralAutolinkValue(token) {
  this.config.enter.autolinkProtocol.call(this, token);
}
function exitLiteralAutolinkHttp(token) {
  this.config.exit.autolinkProtocol.call(this, token);
}
function exitLiteralAutolinkWww(token) {
  this.config.exit.data.call(this, token);
  const node2 = this.stack[this.stack.length - 1];
  ok$1(node2.type === "link");
  node2.url = "http://" + this.sliceSerialize(token);
}
function exitLiteralAutolinkEmail(token) {
  this.config.exit.autolinkEmail.call(this, token);
}
function exitLiteralAutolink(token) {
  this.exit(token);
}
function transformGfmAutolinkLiterals(tree) {
  findAndReplace(
    tree,
    [
      [/(https?:\/\/|www(?=\.))([-.\w]+)([^ \t\r\n]*)/gi, findUrl],
      [new RegExp("(?<=^|\\s|\\p{P}|\\p{S})([-.\\w+]+)@([-\\w]+(?:\\.[-\\w]+)+)", "gu"), findEmail]
    ],
    { ignore: ["link", "linkReference"] }
  );
}
function findUrl(_, protocol, domain2, path2, match) {
  let prefix = "";
  if (!previous(match)) {
    return false;
  }
  if (/^w/i.test(protocol)) {
    domain2 = protocol + domain2;
    protocol = "";
    prefix = "http://";
  }
  if (!isCorrectDomain(domain2)) {
    return false;
  }
  const parts = splitUrl(domain2 + path2);
  if (!parts[0]) return false;
  const result = {
    type: "link",
    title: null,
    url: prefix + protocol + parts[0],
    children: [{ type: "text", value: protocol + parts[0] }]
  };
  if (parts[1]) {
    return [result, { type: "text", value: parts[1] }];
  }
  return result;
}
function findEmail(_, atext, label, match) {
  if (
    // Not an expected previous character.
    !previous(match, true) || // Label ends in not allowed character.
    /[-\d_]$/.test(label)
  ) {
    return false;
  }
  return {
    type: "link",
    title: null,
    url: "mailto:" + atext + "@" + label,
    children: [{ type: "text", value: atext + "@" + label }]
  };
}
function isCorrectDomain(domain2) {
  const parts = domain2.split(".");
  if (parts.length < 2 || parts[parts.length - 1] && (/_/.test(parts[parts.length - 1]) || !/[a-zA-Z\d]/.test(parts[parts.length - 1])) || parts[parts.length - 2] && (/_/.test(parts[parts.length - 2]) || !/[a-zA-Z\d]/.test(parts[parts.length - 2]))) {
    return false;
  }
  return true;
}
function splitUrl(url) {
  const trailExec = /[!"&'),.:;<>?\]}]+$/.exec(url);
  if (!trailExec) {
    return [url, void 0];
  }
  url = url.slice(0, trailExec.index);
  let trail2 = trailExec[0];
  let closingParenIndex = trail2.indexOf(")");
  const openingParens = ccount(url, "(");
  let closingParens = ccount(url, ")");
  while (closingParenIndex !== -1 && openingParens > closingParens) {
    url += trail2.slice(0, closingParenIndex + 1);
    trail2 = trail2.slice(closingParenIndex + 1);
    closingParenIndex = trail2.indexOf(")");
    closingParens++;
  }
  return [url, trail2];
}
function previous(match, email) {
  const code2 = match.input.charCodeAt(match.index - 1);
  return (match.index === 0 || unicodeWhitespace(code2) || unicodePunctuation(code2)) && // If it’s an email, the previous character should not be a slash.
  (!email || code2 !== 47);
}
footnoteReference.peek = footnoteReferencePeek;
function enterFootnoteCallString() {
  this.buffer();
}
function enterFootnoteCall(token) {
  this.enter({ type: "footnoteReference", identifier: "", label: "" }, token);
}
function enterFootnoteDefinitionLabelString() {
  this.buffer();
}
function enterFootnoteDefinition(token) {
  this.enter(
    { type: "footnoteDefinition", identifier: "", label: "", children: [] },
    token
  );
}
function exitFootnoteCallString(token) {
  const label = this.resume();
  const node2 = this.stack[this.stack.length - 1];
  ok$1(node2.type === "footnoteReference");
  node2.identifier = normalizeIdentifier(
    this.sliceSerialize(token)
  ).toLowerCase();
  node2.label = label;
}
function exitFootnoteCall(token) {
  this.exit(token);
}
function exitFootnoteDefinitionLabelString(token) {
  const label = this.resume();
  const node2 = this.stack[this.stack.length - 1];
  ok$1(node2.type === "footnoteDefinition");
  node2.identifier = normalizeIdentifier(
    this.sliceSerialize(token)
  ).toLowerCase();
  node2.label = label;
}
function exitFootnoteDefinition(token) {
  this.exit(token);
}
function footnoteReferencePeek() {
  return "[";
}
function footnoteReference(node2, _, state, info) {
  const tracker = state.createTracker(info);
  let value = tracker.move("[^");
  const exit2 = state.enter("footnoteReference");
  const subexit = state.enter("reference");
  value += tracker.move(
    state.safe(state.associationId(node2), { after: "]", before: value })
  );
  subexit();
  exit2();
  value += tracker.move("]");
  return value;
}
function gfmFootnoteFromMarkdown() {
  return {
    enter: {
      gfmFootnoteCallString: enterFootnoteCallString,
      gfmFootnoteCall: enterFootnoteCall,
      gfmFootnoteDefinitionLabelString: enterFootnoteDefinitionLabelString,
      gfmFootnoteDefinition: enterFootnoteDefinition
    },
    exit: {
      gfmFootnoteCallString: exitFootnoteCallString,
      gfmFootnoteCall: exitFootnoteCall,
      gfmFootnoteDefinitionLabelString: exitFootnoteDefinitionLabelString,
      gfmFootnoteDefinition: exitFootnoteDefinition
    }
  };
}
function gfmFootnoteToMarkdown(options) {
  let firstLineBlank = false;
  if (options && options.firstLineBlank) {
    firstLineBlank = true;
  }
  return {
    handlers: { footnoteDefinition, footnoteReference },
    // This is on by default already.
    unsafe: [{ character: "[", inConstruct: ["label", "phrasing", "reference"] }]
  };
  function footnoteDefinition(node2, _, state, info) {
    const tracker = state.createTracker(info);
    let value = tracker.move("[^");
    const exit2 = state.enter("footnoteDefinition");
    const subexit = state.enter("label");
    value += tracker.move(
      state.safe(state.associationId(node2), { before: value, after: "]" })
    );
    subexit();
    value += tracker.move("]:");
    if (node2.children && node2.children.length > 0) {
      tracker.shift(4);
      value += tracker.move(
        (firstLineBlank ? "\n" : " ") + state.indentLines(
          state.containerFlow(node2, tracker.current()),
          firstLineBlank ? mapAll : mapExceptFirst
        )
      );
    }
    exit2();
    return value;
  }
}
function mapExceptFirst(line, index2, blank) {
  return index2 === 0 ? line : mapAll(line, index2, blank);
}
function mapAll(line, index2, blank) {
  return (blank ? "" : "    ") + line;
}
const constructsWithoutStrikethrough = [
  "autolink",
  "destinationLiteral",
  "destinationRaw",
  "reference",
  "titleQuote",
  "titleApostrophe"
];
handleDelete.peek = peekDelete;
function gfmStrikethroughFromMarkdown() {
  return {
    canContainEols: ["delete"],
    enter: { strikethrough: enterStrikethrough },
    exit: { strikethrough: exitStrikethrough }
  };
}
function gfmStrikethroughToMarkdown() {
  return {
    unsafe: [
      {
        character: "~",
        inConstruct: "phrasing",
        notInConstruct: constructsWithoutStrikethrough
      }
    ],
    handlers: { delete: handleDelete }
  };
}
function enterStrikethrough(token) {
  this.enter({ type: "delete", children: [] }, token);
}
function exitStrikethrough(token) {
  this.exit(token);
}
function handleDelete(node2, _, state, info) {
  const tracker = state.createTracker(info);
  const exit2 = state.enter("strikethrough");
  let value = tracker.move("~~");
  value += state.containerPhrasing(node2, {
    ...tracker.current(),
    before: value,
    after: "~"
  });
  value += tracker.move("~~");
  exit2();
  return value;
}
function peekDelete() {
  return "~";
}
function defaultStringLength(value) {
  return value.length;
}
function markdownTable(table2, options) {
  const settings = options || {};
  const align = (settings.align || []).concat();
  const stringLength = settings.stringLength || defaultStringLength;
  const alignments = [];
  const cellMatrix = [];
  const sizeMatrix = [];
  const longestCellByColumn = [];
  let mostCellsPerRow = 0;
  let rowIndex = -1;
  while (++rowIndex < table2.length) {
    const row2 = [];
    const sizes2 = [];
    let columnIndex2 = -1;
    if (table2[rowIndex].length > mostCellsPerRow) {
      mostCellsPerRow = table2[rowIndex].length;
    }
    while (++columnIndex2 < table2[rowIndex].length) {
      const cell = serialize(table2[rowIndex][columnIndex2]);
      if (settings.alignDelimiters !== false) {
        const size2 = stringLength(cell);
        sizes2[columnIndex2] = size2;
        if (longestCellByColumn[columnIndex2] === void 0 || size2 > longestCellByColumn[columnIndex2]) {
          longestCellByColumn[columnIndex2] = size2;
        }
      }
      row2.push(cell);
    }
    cellMatrix[rowIndex] = row2;
    sizeMatrix[rowIndex] = sizes2;
  }
  let columnIndex = -1;
  if (typeof align === "object" && "length" in align) {
    while (++columnIndex < mostCellsPerRow) {
      alignments[columnIndex] = toAlignment(align[columnIndex]);
    }
  } else {
    const code2 = toAlignment(align);
    while (++columnIndex < mostCellsPerRow) {
      alignments[columnIndex] = code2;
    }
  }
  columnIndex = -1;
  const row = [];
  const sizes = [];
  while (++columnIndex < mostCellsPerRow) {
    const code2 = alignments[columnIndex];
    let before = "";
    let after = "";
    if (code2 === 99) {
      before = ":";
      after = ":";
    } else if (code2 === 108) {
      before = ":";
    } else if (code2 === 114) {
      after = ":";
    }
    let size2 = settings.alignDelimiters === false ? 1 : Math.max(
      1,
      longestCellByColumn[columnIndex] - before.length - after.length
    );
    const cell = before + "-".repeat(size2) + after;
    if (settings.alignDelimiters !== false) {
      size2 = before.length + size2 + after.length;
      if (size2 > longestCellByColumn[columnIndex]) {
        longestCellByColumn[columnIndex] = size2;
      }
      sizes[columnIndex] = size2;
    }
    row[columnIndex] = cell;
  }
  cellMatrix.splice(1, 0, row);
  sizeMatrix.splice(1, 0, sizes);
  rowIndex = -1;
  const lines = [];
  while (++rowIndex < cellMatrix.length) {
    const row2 = cellMatrix[rowIndex];
    const sizes2 = sizeMatrix[rowIndex];
    columnIndex = -1;
    const line = [];
    while (++columnIndex < mostCellsPerRow) {
      const cell = row2[columnIndex] || "";
      let before = "";
      let after = "";
      if (settings.alignDelimiters !== false) {
        const size2 = longestCellByColumn[columnIndex] - (sizes2[columnIndex] || 0);
        const code2 = alignments[columnIndex];
        if (code2 === 114) {
          before = " ".repeat(size2);
        } else if (code2 === 99) {
          if (size2 % 2) {
            before = " ".repeat(size2 / 2 + 0.5);
            after = " ".repeat(size2 / 2 - 0.5);
          } else {
            before = " ".repeat(size2 / 2);
            after = before;
          }
        } else {
          after = " ".repeat(size2);
        }
      }
      if (settings.delimiterStart !== false && !columnIndex) {
        line.push("|");
      }
      if (settings.padding !== false && // Don’t add the opening space if we’re not aligning and the cell is
      // empty: there will be a closing space.
      !(settings.alignDelimiters === false && cell === "") && (settings.delimiterStart !== false || columnIndex)) {
        line.push(" ");
      }
      if (settings.alignDelimiters !== false) {
        line.push(before);
      }
      line.push(cell);
      if (settings.alignDelimiters !== false) {
        line.push(after);
      }
      if (settings.padding !== false) {
        line.push(" ");
      }
      if (settings.delimiterEnd !== false || columnIndex !== mostCellsPerRow - 1) {
        line.push("|");
      }
    }
    lines.push(
      settings.delimiterEnd === false ? line.join("").replace(/ +$/, "") : line.join("")
    );
  }
  return lines.join("\n");
}
function serialize(value) {
  return value === null || value === void 0 ? "" : String(value);
}
function toAlignment(value) {
  const code2 = typeof value === "string" ? value.codePointAt(0) : 0;
  return code2 === 67 || code2 === 99 ? 99 : code2 === 76 || code2 === 108 ? 108 : code2 === 82 || code2 === 114 ? 114 : 0;
}
function blockquote(node2, _, state, info) {
  const exit2 = state.enter("blockquote");
  const tracker = state.createTracker(info);
  tracker.move("> ");
  tracker.shift(2);
  const value = state.indentLines(
    state.containerFlow(node2, tracker.current()),
    map$1
  );
  exit2();
  return value;
}
function map$1(line, _, blank) {
  return ">" + (blank ? "" : " ") + line;
}
function patternInScope(stack, pattern) {
  return listInScope(stack, pattern.inConstruct, true) && !listInScope(stack, pattern.notInConstruct, false);
}
function listInScope(stack, list2, none) {
  if (typeof list2 === "string") {
    list2 = [list2];
  }
  if (!list2 || list2.length === 0) {
    return none;
  }
  let index2 = -1;
  while (++index2 < list2.length) {
    if (stack.includes(list2[index2])) {
      return true;
    }
  }
  return false;
}
function hardBreak(_, _1, state, info) {
  let index2 = -1;
  while (++index2 < state.unsafe.length) {
    if (state.unsafe[index2].character === "\n" && patternInScope(state.stack, state.unsafe[index2])) {
      return /[ \t]/.test(info.before) ? "" : " ";
    }
  }
  return "\\\n";
}
function longestStreak(value, substring) {
  const source = String(value);
  let index2 = source.indexOf(substring);
  let expected = index2;
  let count2 = 0;
  let max2 = 0;
  if (typeof substring !== "string") {
    throw new TypeError("Expected substring");
  }
  while (index2 !== -1) {
    if (index2 === expected) {
      if (++count2 > max2) {
        max2 = count2;
      }
    } else {
      count2 = 1;
    }
    expected = index2 + substring.length;
    index2 = source.indexOf(substring, expected);
  }
  return max2;
}
function formatCodeAsIndented(node2, state) {
  return Boolean(
    state.options.fences === false && node2.value && // If there’s no info…
    !node2.lang && // And there’s a non-whitespace character…
    /[^ \r\n]/.test(node2.value) && // And the value doesn’t start or end in a blank…
    !/^[\t ]*(?:[\r\n]|$)|(?:^|[\r\n])[\t ]*$/.test(node2.value)
  );
}
function checkFence(state) {
  const marker = state.options.fence || "`";
  if (marker !== "`" && marker !== "~") {
    throw new Error(
      "Cannot serialize code with `" + marker + "` for `options.fence`, expected `` ` `` or `~`"
    );
  }
  return marker;
}
function code$1(node2, _, state, info) {
  const marker = checkFence(state);
  const raw = node2.value || "";
  const suffix = marker === "`" ? "GraveAccent" : "Tilde";
  if (formatCodeAsIndented(node2, state)) {
    const exit3 = state.enter("codeIndented");
    const value2 = state.indentLines(raw, map);
    exit3();
    return value2;
  }
  const tracker = state.createTracker(info);
  const sequence = marker.repeat(Math.max(longestStreak(raw, marker) + 1, 3));
  const exit2 = state.enter("codeFenced");
  let value = tracker.move(sequence);
  if (node2.lang) {
    const subexit = state.enter(`codeFencedLang${suffix}`);
    value += tracker.move(
      state.safe(node2.lang, {
        before: value,
        after: " ",
        encode: ["`"],
        ...tracker.current()
      })
    );
    subexit();
  }
  if (node2.lang && node2.meta) {
    const subexit = state.enter(`codeFencedMeta${suffix}`);
    value += tracker.move(" ");
    value += tracker.move(
      state.safe(node2.meta, {
        before: value,
        after: "\n",
        encode: ["`"],
        ...tracker.current()
      })
    );
    subexit();
  }
  value += tracker.move("\n");
  if (raw) {
    value += tracker.move(raw + "\n");
  }
  value += tracker.move(sequence);
  exit2();
  return value;
}
function map(line, _, blank) {
  return (blank ? "" : "    ") + line;
}
function checkQuote(state) {
  const marker = state.options.quote || '"';
  if (marker !== '"' && marker !== "'") {
    throw new Error(
      "Cannot serialize title with `" + marker + "` for `options.quote`, expected `\"`, or `'`"
    );
  }
  return marker;
}
function definition(node2, _, state, info) {
  const quote = checkQuote(state);
  const suffix = quote === '"' ? "Quote" : "Apostrophe";
  const exit2 = state.enter("definition");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("[");
  value += tracker.move(
    state.safe(state.associationId(node2), {
      before: value,
      after: "]",
      ...tracker.current()
    })
  );
  value += tracker.move("]: ");
  subexit();
  if (
    // If there’s no url, or…
    !node2.url || // If there are control characters or whitespace.
    /[\0- \u007F]/.test(node2.url)
  ) {
    subexit = state.enter("destinationLiteral");
    value += tracker.move("<");
    value += tracker.move(
      state.safe(node2.url, { before: value, after: ">", ...tracker.current() })
    );
    value += tracker.move(">");
  } else {
    subexit = state.enter("destinationRaw");
    value += tracker.move(
      state.safe(node2.url, {
        before: value,
        after: node2.title ? " " : "\n",
        ...tracker.current()
      })
    );
  }
  subexit();
  if (node2.title) {
    subexit = state.enter(`title${suffix}`);
    value += tracker.move(" " + quote);
    value += tracker.move(
      state.safe(node2.title, {
        before: value,
        after: quote,
        ...tracker.current()
      })
    );
    value += tracker.move(quote);
    subexit();
  }
  exit2();
  return value;
}
function checkEmphasis(state) {
  const marker = state.options.emphasis || "*";
  if (marker !== "*" && marker !== "_") {
    throw new Error(
      "Cannot serialize emphasis with `" + marker + "` for `options.emphasis`, expected `*`, or `_`"
    );
  }
  return marker;
}
function encodeCharacterReference(code2) {
  return "&#x" + code2.toString(16).toUpperCase() + ";";
}
function encodeInfo(outside, inside, marker) {
  const outsideKind = classifyCharacter(outside);
  const insideKind = classifyCharacter(inside);
  if (outsideKind === void 0) {
    return insideKind === void 0 ? (
      // Letter inside:
      // we have to encode *both* letters for `_` as it is looser.
      // it already forms for `*` (and GFMs `~`).
      marker === "_" ? { inside: true, outside: true } : { inside: false, outside: false }
    ) : insideKind === 1 ? (
      // Whitespace inside: encode both (letter, whitespace).
      { inside: true, outside: true }
    ) : (
      // Punctuation inside: encode outer (letter)
      { inside: false, outside: true }
    );
  }
  if (outsideKind === 1) {
    return insideKind === void 0 ? (
      // Letter inside: already forms.
      { inside: false, outside: false }
    ) : insideKind === 1 ? (
      // Whitespace inside: encode both (whitespace).
      { inside: true, outside: true }
    ) : (
      // Punctuation inside: already forms.
      { inside: false, outside: false }
    );
  }
  return insideKind === void 0 ? (
    // Letter inside: already forms.
    { inside: false, outside: false }
  ) : insideKind === 1 ? (
    // Whitespace inside: encode inner (whitespace).
    { inside: true, outside: false }
  ) : (
    // Punctuation inside: already forms.
    { inside: false, outside: false }
  );
}
emphasis.peek = emphasisPeek;
function emphasis(node2, _, state, info) {
  const marker = checkEmphasis(state);
  const exit2 = state.enter("emphasis");
  const tracker = state.createTracker(info);
  const before = tracker.move(marker);
  let between = tracker.move(
    state.containerPhrasing(node2, {
      after: marker,
      before,
      ...tracker.current()
    })
  );
  const betweenHead = between.charCodeAt(0);
  const open = encodeInfo(
    info.before.charCodeAt(info.before.length - 1),
    betweenHead,
    marker
  );
  if (open.inside) {
    between = encodeCharacterReference(betweenHead) + between.slice(1);
  }
  const betweenTail = between.charCodeAt(between.length - 1);
  const close = encodeInfo(info.after.charCodeAt(0), betweenTail, marker);
  if (close.inside) {
    between = between.slice(0, -1) + encodeCharacterReference(betweenTail);
  }
  const after = tracker.move(marker);
  exit2();
  state.attentionEncodeSurroundingInfo = {
    after: close.outside,
    before: open.outside
  };
  return before + between + after;
}
function emphasisPeek(_, _1, state) {
  return state.options.emphasis || "*";
}
function formatHeadingAsSetext(node2, state) {
  let literalWithBreak = false;
  visit(node2, function(node3) {
    if ("value" in node3 && /\r?\n|\r/.test(node3.value) || node3.type === "break") {
      literalWithBreak = true;
      return EXIT;
    }
  });
  return Boolean(
    (!node2.depth || node2.depth < 3) && toString$1(node2) && (state.options.setext || literalWithBreak)
  );
}
function heading(node2, _, state, info) {
  const rank = Math.max(Math.min(6, node2.depth || 1), 1);
  const tracker = state.createTracker(info);
  if (formatHeadingAsSetext(node2, state)) {
    const exit3 = state.enter("headingSetext");
    const subexit2 = state.enter("phrasing");
    const value2 = state.containerPhrasing(node2, {
      ...tracker.current(),
      before: "\n",
      after: "\n"
    });
    subexit2();
    exit3();
    return value2 + "\n" + (rank === 1 ? "=" : "-").repeat(
      // The whole size…
      value2.length - // Minus the position of the character after the last EOL (or
      // 0 if there is none)…
      (Math.max(value2.lastIndexOf("\r"), value2.lastIndexOf("\n")) + 1)
    );
  }
  const sequence = "#".repeat(rank);
  const exit2 = state.enter("headingAtx");
  const subexit = state.enter("phrasing");
  tracker.move(sequence + " ");
  let value = state.containerPhrasing(node2, {
    before: "# ",
    after: "\n",
    ...tracker.current()
  });
  if (/^[\t ]/.test(value)) {
    value = encodeCharacterReference(value.charCodeAt(0)) + value.slice(1);
  }
  value = value ? sequence + " " + value : sequence;
  if (state.options.closeAtx) {
    value += " " + sequence;
  }
  subexit();
  exit2();
  return value;
}
html.peek = htmlPeek;
function html(node2) {
  return node2.value || "";
}
function htmlPeek() {
  return "<";
}
image.peek = imagePeek;
function image(node2, _, state, info) {
  const quote = checkQuote(state);
  const suffix = quote === '"' ? "Quote" : "Apostrophe";
  const exit2 = state.enter("image");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("![");
  value += tracker.move(
    state.safe(node2.alt, { before: value, after: "]", ...tracker.current() })
  );
  value += tracker.move("](");
  subexit();
  if (
    // If there’s no url but there is a title…
    !node2.url && node2.title || // If there are control characters or whitespace.
    /[\0- \u007F]/.test(node2.url)
  ) {
    subexit = state.enter("destinationLiteral");
    value += tracker.move("<");
    value += tracker.move(
      state.safe(node2.url, { before: value, after: ">", ...tracker.current() })
    );
    value += tracker.move(">");
  } else {
    subexit = state.enter("destinationRaw");
    value += tracker.move(
      state.safe(node2.url, {
        before: value,
        after: node2.title ? " " : ")",
        ...tracker.current()
      })
    );
  }
  subexit();
  if (node2.title) {
    subexit = state.enter(`title${suffix}`);
    value += tracker.move(" " + quote);
    value += tracker.move(
      state.safe(node2.title, {
        before: value,
        after: quote,
        ...tracker.current()
      })
    );
    value += tracker.move(quote);
    subexit();
  }
  value += tracker.move(")");
  exit2();
  return value;
}
function imagePeek() {
  return "!";
}
imageReference.peek = imageReferencePeek;
function imageReference(node2, _, state, info) {
  const type = node2.referenceType;
  const exit2 = state.enter("imageReference");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("![");
  const alt = state.safe(node2.alt, {
    before: value,
    after: "]",
    ...tracker.current()
  });
  value += tracker.move(alt + "][");
  subexit();
  const stack = state.stack;
  state.stack = [];
  subexit = state.enter("reference");
  const reference = state.safe(state.associationId(node2), {
    before: value,
    after: "]",
    ...tracker.current()
  });
  subexit();
  state.stack = stack;
  exit2();
  if (type === "full" || !alt || alt !== reference) {
    value += tracker.move(reference + "]");
  } else if (type === "shortcut") {
    value = value.slice(0, -1);
  } else {
    value += tracker.move("]");
  }
  return value;
}
function imageReferencePeek() {
  return "!";
}
inlineCode.peek = inlineCodePeek;
function inlineCode(node2, _, state) {
  let value = node2.value || "";
  let sequence = "`";
  let index2 = -1;
  while (new RegExp("(^|[^`])" + sequence + "([^`]|$)").test(value)) {
    sequence += "`";
  }
  if (/[^ \r\n]/.test(value) && (/^[ \r\n]/.test(value) && /[ \r\n]$/.test(value) || /^`|`$/.test(value))) {
    value = " " + value + " ";
  }
  while (++index2 < state.unsafe.length) {
    const pattern = state.unsafe[index2];
    const expression = state.compilePattern(pattern);
    let match;
    if (!pattern.atBreak) continue;
    while (match = expression.exec(value)) {
      let position2 = match.index;
      if (value.charCodeAt(position2) === 10 && value.charCodeAt(position2 - 1) === 13) {
        position2--;
      }
      value = value.slice(0, position2) + " " + value.slice(match.index + 1);
    }
  }
  return sequence + value + sequence;
}
function inlineCodePeek() {
  return "`";
}
function formatLinkAsAutolink(node2, state) {
  const raw = toString$1(node2);
  return Boolean(
    !state.options.resourceLink && // If there’s a url…
    node2.url && // And there’s a no title…
    !node2.title && // And the content of `node` is a single text node…
    node2.children && node2.children.length === 1 && node2.children[0].type === "text" && // And if the url is the same as the content…
    (raw === node2.url || "mailto:" + raw === node2.url) && // And that starts w/ a protocol…
    /^[a-z][a-z+.-]+:/i.test(node2.url) && // And that doesn’t contain ASCII control codes (character escapes and
    // references don’t work), space, or angle brackets…
    !/[\0- <>\u007F]/.test(node2.url)
  );
}
link.peek = linkPeek;
function link(node2, _, state, info) {
  const quote = checkQuote(state);
  const suffix = quote === '"' ? "Quote" : "Apostrophe";
  const tracker = state.createTracker(info);
  let exit2;
  let subexit;
  if (formatLinkAsAutolink(node2, state)) {
    const stack = state.stack;
    state.stack = [];
    exit2 = state.enter("autolink");
    let value2 = tracker.move("<");
    value2 += tracker.move(
      state.containerPhrasing(node2, {
        before: value2,
        after: ">",
        ...tracker.current()
      })
    );
    value2 += tracker.move(">");
    exit2();
    state.stack = stack;
    return value2;
  }
  exit2 = state.enter("link");
  subexit = state.enter("label");
  let value = tracker.move("[");
  value += tracker.move(
    state.containerPhrasing(node2, {
      before: value,
      after: "](",
      ...tracker.current()
    })
  );
  value += tracker.move("](");
  subexit();
  if (
    // If there’s no url but there is a title…
    !node2.url && node2.title || // If there are control characters or whitespace.
    /[\0- \u007F]/.test(node2.url)
  ) {
    subexit = state.enter("destinationLiteral");
    value += tracker.move("<");
    value += tracker.move(
      state.safe(node2.url, { before: value, after: ">", ...tracker.current() })
    );
    value += tracker.move(">");
  } else {
    subexit = state.enter("destinationRaw");
    value += tracker.move(
      state.safe(node2.url, {
        before: value,
        after: node2.title ? " " : ")",
        ...tracker.current()
      })
    );
  }
  subexit();
  if (node2.title) {
    subexit = state.enter(`title${suffix}`);
    value += tracker.move(" " + quote);
    value += tracker.move(
      state.safe(node2.title, {
        before: value,
        after: quote,
        ...tracker.current()
      })
    );
    value += tracker.move(quote);
    subexit();
  }
  value += tracker.move(")");
  exit2();
  return value;
}
function linkPeek(node2, _, state) {
  return formatLinkAsAutolink(node2, state) ? "<" : "[";
}
linkReference.peek = linkReferencePeek;
function linkReference(node2, _, state, info) {
  const type = node2.referenceType;
  const exit2 = state.enter("linkReference");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("[");
  const text2 = state.containerPhrasing(node2, {
    before: value,
    after: "]",
    ...tracker.current()
  });
  value += tracker.move(text2 + "][");
  subexit();
  const stack = state.stack;
  state.stack = [];
  subexit = state.enter("reference");
  const reference = state.safe(state.associationId(node2), {
    before: value,
    after: "]",
    ...tracker.current()
  });
  subexit();
  state.stack = stack;
  exit2();
  if (type === "full" || !text2 || text2 !== reference) {
    value += tracker.move(reference + "]");
  } else if (type === "shortcut") {
    value = value.slice(0, -1);
  } else {
    value += tracker.move("]");
  }
  return value;
}
function linkReferencePeek() {
  return "[";
}
function checkBullet(state) {
  const marker = state.options.bullet || "*";
  if (marker !== "*" && marker !== "+" && marker !== "-") {
    throw new Error(
      "Cannot serialize items with `" + marker + "` for `options.bullet`, expected `*`, `+`, or `-`"
    );
  }
  return marker;
}
function checkBulletOther(state) {
  const bullet = checkBullet(state);
  const bulletOther = state.options.bulletOther;
  if (!bulletOther) {
    return bullet === "*" ? "-" : "*";
  }
  if (bulletOther !== "*" && bulletOther !== "+" && bulletOther !== "-") {
    throw new Error(
      "Cannot serialize items with `" + bulletOther + "` for `options.bulletOther`, expected `*`, `+`, or `-`"
    );
  }
  if (bulletOther === bullet) {
    throw new Error(
      "Expected `bullet` (`" + bullet + "`) and `bulletOther` (`" + bulletOther + "`) to be different"
    );
  }
  return bulletOther;
}
function checkBulletOrdered(state) {
  const marker = state.options.bulletOrdered || ".";
  if (marker !== "." && marker !== ")") {
    throw new Error(
      "Cannot serialize items with `" + marker + "` for `options.bulletOrdered`, expected `.` or `)`"
    );
  }
  return marker;
}
function checkRule(state) {
  const marker = state.options.rule || "*";
  if (marker !== "*" && marker !== "-" && marker !== "_") {
    throw new Error(
      "Cannot serialize rules with `" + marker + "` for `options.rule`, expected `*`, `-`, or `_`"
    );
  }
  return marker;
}
function list(node2, parent, state, info) {
  const exit2 = state.enter("list");
  const bulletCurrent = state.bulletCurrent;
  let bullet = node2.ordered ? checkBulletOrdered(state) : checkBullet(state);
  const bulletOther = node2.ordered ? bullet === "." ? ")" : "." : checkBulletOther(state);
  let useDifferentMarker = parent && state.bulletLastUsed ? bullet === state.bulletLastUsed : false;
  if (!node2.ordered) {
    const firstListItem = node2.children ? node2.children[0] : void 0;
    if (
      // Bullet could be used as a thematic break marker:
      (bullet === "*" || bullet === "-") && // Empty first list item:
      firstListItem && (!firstListItem.children || !firstListItem.children[0]) && // Directly in two other list items:
      state.stack[state.stack.length - 1] === "list" && state.stack[state.stack.length - 2] === "listItem" && state.stack[state.stack.length - 3] === "list" && state.stack[state.stack.length - 4] === "listItem" && // That are each the first child.
      state.indexStack[state.indexStack.length - 1] === 0 && state.indexStack[state.indexStack.length - 2] === 0 && state.indexStack[state.indexStack.length - 3] === 0
    ) {
      useDifferentMarker = true;
    }
    if (checkRule(state) === bullet && firstListItem) {
      let index2 = -1;
      while (++index2 < node2.children.length) {
        const item = node2.children[index2];
        if (item && item.type === "listItem" && item.children && item.children[0] && item.children[0].type === "thematicBreak") {
          useDifferentMarker = true;
          break;
        }
      }
    }
  }
  if (useDifferentMarker) {
    bullet = bulletOther;
  }
  state.bulletCurrent = bullet;
  const value = state.containerFlow(node2, info);
  state.bulletLastUsed = bullet;
  state.bulletCurrent = bulletCurrent;
  exit2();
  return value;
}
function checkListItemIndent(state) {
  const style = state.options.listItemIndent || "one";
  if (style !== "tab" && style !== "one" && style !== "mixed") {
    throw new Error(
      "Cannot serialize items with `" + style + "` for `options.listItemIndent`, expected `tab`, `one`, or `mixed`"
    );
  }
  return style;
}
function listItem(node2, parent, state, info) {
  const listItemIndent = checkListItemIndent(state);
  let bullet = state.bulletCurrent || checkBullet(state);
  if (parent && parent.type === "list" && parent.ordered) {
    bullet = (typeof parent.start === "number" && parent.start > -1 ? parent.start : 1) + (state.options.incrementListMarker === false ? 0 : parent.children.indexOf(node2)) + bullet;
  }
  let size2 = bullet.length + 1;
  if (listItemIndent === "tab" || listItemIndent === "mixed" && (parent && parent.type === "list" && parent.spread || node2.spread)) {
    size2 = Math.ceil(size2 / 4) * 4;
  }
  const tracker = state.createTracker(info);
  tracker.move(bullet + " ".repeat(size2 - bullet.length));
  tracker.shift(size2);
  const exit2 = state.enter("listItem");
  const value = state.indentLines(
    state.containerFlow(node2, tracker.current()),
    map2
  );
  exit2();
  return value;
  function map2(line, index2, blank) {
    if (index2) {
      return (blank ? "" : " ".repeat(size2)) + line;
    }
    return (blank ? bullet : bullet + " ".repeat(size2 - bullet.length)) + line;
  }
}
function paragraph(node2, _, state, info) {
  const exit2 = state.enter("paragraph");
  const subexit = state.enter("phrasing");
  const value = state.containerPhrasing(node2, info);
  subexit();
  exit2();
  return value;
}
const phrasing = (
  /** @type {(node?: unknown) => node is Exclude<PhrasingContent, Html>} */
  convert([
    "break",
    "delete",
    "emphasis",
    // To do: next major: removed since footnotes were added to GFM.
    "footnote",
    "footnoteReference",
    "image",
    "imageReference",
    "inlineCode",
    // Enabled by `mdast-util-math`:
    "inlineMath",
    "link",
    "linkReference",
    // Enabled by `mdast-util-mdx`:
    "mdxJsxTextElement",
    // Enabled by `mdast-util-mdx`:
    "mdxTextExpression",
    "strong",
    "text",
    // Enabled by `mdast-util-directive`:
    "textDirective"
  ])
);
function root(node2, _, state, info) {
  const hasPhrasing = node2.children.some(function(d) {
    return phrasing(d);
  });
  const container = hasPhrasing ? state.containerPhrasing : state.containerFlow;
  return container.call(state, node2, info);
}
function checkStrong(state) {
  const marker = state.options.strong || "*";
  if (marker !== "*" && marker !== "_") {
    throw new Error(
      "Cannot serialize strong with `" + marker + "` for `options.strong`, expected `*`, or `_`"
    );
  }
  return marker;
}
strong.peek = strongPeek;
function strong(node2, _, state, info) {
  const marker = checkStrong(state);
  const exit2 = state.enter("strong");
  const tracker = state.createTracker(info);
  const before = tracker.move(marker + marker);
  let between = tracker.move(
    state.containerPhrasing(node2, {
      after: marker,
      before,
      ...tracker.current()
    })
  );
  const betweenHead = between.charCodeAt(0);
  const open = encodeInfo(
    info.before.charCodeAt(info.before.length - 1),
    betweenHead,
    marker
  );
  if (open.inside) {
    between = encodeCharacterReference(betweenHead) + between.slice(1);
  }
  const betweenTail = between.charCodeAt(between.length - 1);
  const close = encodeInfo(info.after.charCodeAt(0), betweenTail, marker);
  if (close.inside) {
    between = between.slice(0, -1) + encodeCharacterReference(betweenTail);
  }
  const after = tracker.move(marker + marker);
  exit2();
  state.attentionEncodeSurroundingInfo = {
    after: close.outside,
    before: open.outside
  };
  return before + between + after;
}
function strongPeek(_, _1, state) {
  return state.options.strong || "*";
}
function text$1(node2, _, state, info) {
  return state.safe(node2.value, info);
}
function checkRuleRepetition(state) {
  const repetition = state.options.ruleRepetition || 3;
  if (repetition < 3) {
    throw new Error(
      "Cannot serialize rules with repetition `" + repetition + "` for `options.ruleRepetition`, expected `3` or more"
    );
  }
  return repetition;
}
function thematicBreak(_, _1, state) {
  const value = (checkRule(state) + (state.options.ruleSpaces ? " " : "")).repeat(checkRuleRepetition(state));
  return state.options.ruleSpaces ? value.slice(0, -1) : value;
}
const handle = {
  blockquote,
  break: hardBreak,
  code: code$1,
  definition,
  emphasis,
  hardBreak,
  heading,
  html,
  image,
  imageReference,
  inlineCode,
  link,
  linkReference,
  list,
  listItem,
  paragraph,
  root,
  strong,
  text: text$1,
  thematicBreak
};
function gfmTableFromMarkdown() {
  return {
    enter: {
      table: enterTable,
      tableData: enterCell,
      tableHeader: enterCell,
      tableRow: enterRow
    },
    exit: {
      codeText: exitCodeText,
      table: exitTable,
      tableData: exit,
      tableHeader: exit,
      tableRow: exit
    }
  };
}
function enterTable(token) {
  const align = token._align;
  this.enter(
    {
      type: "table",
      align: align.map(function(d) {
        return d === "none" ? null : d;
      }),
      children: []
    },
    token
  );
  this.data.inTable = true;
}
function exitTable(token) {
  this.exit(token);
  this.data.inTable = void 0;
}
function enterRow(token) {
  this.enter({ type: "tableRow", children: [] }, token);
}
function exit(token) {
  this.exit(token);
}
function enterCell(token) {
  this.enter({ type: "tableCell", children: [] }, token);
}
function exitCodeText(token) {
  let value = this.resume();
  if (this.data.inTable) {
    value = value.replace(/\\([\\|])/g, replace);
  }
  const node2 = this.stack[this.stack.length - 1];
  ok$1(node2.type === "inlineCode");
  node2.value = value;
  this.exit(token);
}
function replace($0, $1) {
  return $1 === "|" ? $1 : $0;
}
function gfmTableToMarkdown(options) {
  const settings = options || {};
  const padding = settings.tableCellPadding;
  const alignDelimiters = settings.tablePipeAlign;
  const stringLength = settings.stringLength;
  const around = padding ? " " : "|";
  return {
    unsafe: [
      { character: "\r", inConstruct: "tableCell" },
      { character: "\n", inConstruct: "tableCell" },
      // A pipe, when followed by a tab or space (padding), or a dash or colon
      // (unpadded delimiter row), could result in a table.
      { atBreak: true, character: "|", after: "[	 :-]" },
      // A pipe in a cell must be encoded.
      { character: "|", inConstruct: "tableCell" },
      // A colon must be followed by a dash, in which case it could start a
      // delimiter row.
      { atBreak: true, character: ":", after: "-" },
      // A delimiter row can also start with a dash, when followed by more
      // dashes, a colon, or a pipe.
      // This is a stricter version than the built in check for lists, thematic
      // breaks, and setex heading underlines though:
      // <https://github.com/syntax-tree/mdast-util-to-markdown/blob/51a2038/lib/unsafe.js#L57>
      { atBreak: true, character: "-", after: "[:|-]" }
    ],
    handlers: {
      inlineCode: inlineCodeWithTable,
      table: handleTable,
      tableCell: handleTableCell,
      tableRow: handleTableRow
    }
  };
  function handleTable(node2, _, state, info) {
    return serializeData(handleTableAsData(node2, state, info), node2.align);
  }
  function handleTableRow(node2, _, state, info) {
    const row = handleTableRowAsData(node2, state, info);
    const value = serializeData([row]);
    return value.slice(0, value.indexOf("\n"));
  }
  function handleTableCell(node2, _, state, info) {
    const exit2 = state.enter("tableCell");
    const subexit = state.enter("phrasing");
    const value = state.containerPhrasing(node2, {
      ...info,
      before: around,
      after: around
    });
    subexit();
    exit2();
    return value;
  }
  function serializeData(matrix, align) {
    return markdownTable(matrix, {
      align,
      // @ts-expect-error: `markdown-table` types should support `null`.
      alignDelimiters,
      // @ts-expect-error: `markdown-table` types should support `null`.
      padding,
      // @ts-expect-error: `markdown-table` types should support `null`.
      stringLength
    });
  }
  function handleTableAsData(node2, state, info) {
    const children = node2.children;
    let index2 = -1;
    const result = [];
    const subexit = state.enter("table");
    while (++index2 < children.length) {
      result[index2] = handleTableRowAsData(children[index2], state, info);
    }
    subexit();
    return result;
  }
  function handleTableRowAsData(node2, state, info) {
    const children = node2.children;
    let index2 = -1;
    const result = [];
    const subexit = state.enter("tableRow");
    while (++index2 < children.length) {
      result[index2] = handleTableCell(children[index2], node2, state, info);
    }
    subexit();
    return result;
  }
  function inlineCodeWithTable(node2, parent, state) {
    let value = handle.inlineCode(node2, parent, state);
    if (state.stack.includes("tableCell")) {
      value = value.replace(/\|/g, "\\$&");
    }
    return value;
  }
}
function gfmTaskListItemFromMarkdown() {
  return {
    exit: {
      taskListCheckValueChecked: exitCheck,
      taskListCheckValueUnchecked: exitCheck,
      paragraph: exitParagraphWithTaskListItem
    }
  };
}
function gfmTaskListItemToMarkdown() {
  return {
    unsafe: [{ atBreak: true, character: "-", after: "[:|-]" }],
    handlers: { listItem: listItemWithTaskListItem }
  };
}
function exitCheck(token) {
  const node2 = this.stack[this.stack.length - 2];
  ok$1(node2.type === "listItem");
  node2.checked = token.type === "taskListCheckValueChecked";
}
function exitParagraphWithTaskListItem(token) {
  const parent = this.stack[this.stack.length - 2];
  if (parent && parent.type === "listItem" && typeof parent.checked === "boolean") {
    const node2 = this.stack[this.stack.length - 1];
    ok$1(node2.type === "paragraph");
    const head = node2.children[0];
    if (head && head.type === "text") {
      const siblings = parent.children;
      let index2 = -1;
      let firstParaghraph;
      while (++index2 < siblings.length) {
        const sibling = siblings[index2];
        if (sibling.type === "paragraph") {
          firstParaghraph = sibling;
          break;
        }
      }
      if (firstParaghraph === node2) {
        head.value = head.value.slice(1);
        if (head.value.length === 0) {
          node2.children.shift();
        } else if (node2.position && head.position && typeof head.position.start.offset === "number") {
          head.position.start.column++;
          head.position.start.offset++;
          node2.position.start = Object.assign({}, head.position.start);
        }
      }
    }
  }
  this.exit(token);
}
function listItemWithTaskListItem(node2, parent, state, info) {
  const head = node2.children[0];
  const checkable = typeof node2.checked === "boolean" && head && head.type === "paragraph";
  const checkbox = "[" + (node2.checked ? "x" : " ") + "] ";
  const tracker = state.createTracker(info);
  if (checkable) {
    tracker.move(checkbox);
  }
  let value = handle.listItem(node2, parent, state, {
    ...info,
    ...tracker.current()
  });
  if (checkable) {
    value = value.replace(/^(?:[*+-]|\d+\.)([\r\n]| {1,3})/, check);
  }
  return value;
  function check($0) {
    return $0 + checkbox;
  }
}
function gfmFromMarkdown() {
  return [
    gfmAutolinkLiteralFromMarkdown(),
    gfmFootnoteFromMarkdown(),
    gfmStrikethroughFromMarkdown(),
    gfmTableFromMarkdown(),
    gfmTaskListItemFromMarkdown()
  ];
}
function gfmToMarkdown(options) {
  return {
    extensions: [
      gfmAutolinkLiteralToMarkdown(),
      gfmFootnoteToMarkdown(options),
      gfmStrikethroughToMarkdown(),
      gfmTableToMarkdown(options),
      gfmTaskListItemToMarkdown()
    ]
  };
}
const wwwPrefix = {
  tokenize: tokenizeWwwPrefix,
  partial: true
};
const domain = {
  tokenize: tokenizeDomain,
  partial: true
};
const path = {
  tokenize: tokenizePath,
  partial: true
};
const trail = {
  tokenize: tokenizeTrail,
  partial: true
};
const emailDomainDotTrail = {
  tokenize: tokenizeEmailDomainDotTrail,
  partial: true
};
const wwwAutolink = {
  name: "wwwAutolink",
  tokenize: tokenizeWwwAutolink,
  previous: previousWww
};
const protocolAutolink = {
  name: "protocolAutolink",
  tokenize: tokenizeProtocolAutolink,
  previous: previousProtocol
};
const emailAutolink = {
  name: "emailAutolink",
  tokenize: tokenizeEmailAutolink,
  previous: previousEmail
};
const text = {};
function gfmAutolinkLiteral() {
  return {
    text
  };
}
let code = 48;
while (code < 123) {
  text[code] = emailAutolink;
  code++;
  if (code === 58) code = 65;
  else if (code === 91) code = 97;
}
text[43] = emailAutolink;
text[45] = emailAutolink;
text[46] = emailAutolink;
text[95] = emailAutolink;
text[72] = [emailAutolink, protocolAutolink];
text[104] = [emailAutolink, protocolAutolink];
text[87] = [emailAutolink, wwwAutolink];
text[119] = [emailAutolink, wwwAutolink];
function tokenizeEmailAutolink(effects, ok2, nok) {
  const self2 = this;
  let dot;
  let data;
  return start;
  function start(code2) {
    if (!gfmAtext(code2) || !previousEmail.call(self2, self2.previous) || previousUnbalanced(self2.events)) {
      return nok(code2);
    }
    effects.enter("literalAutolink");
    effects.enter("literalAutolinkEmail");
    return atext(code2);
  }
  function atext(code2) {
    if (gfmAtext(code2)) {
      effects.consume(code2);
      return atext;
    }
    if (code2 === 64) {
      effects.consume(code2);
      return emailDomain;
    }
    return nok(code2);
  }
  function emailDomain(code2) {
    if (code2 === 46) {
      return effects.check(emailDomainDotTrail, emailDomainAfter, emailDomainDot)(code2);
    }
    if (code2 === 45 || code2 === 95 || asciiAlphanumeric(code2)) {
      data = true;
      effects.consume(code2);
      return emailDomain;
    }
    return emailDomainAfter(code2);
  }
  function emailDomainDot(code2) {
    effects.consume(code2);
    dot = true;
    return emailDomain;
  }
  function emailDomainAfter(code2) {
    if (data && dot && asciiAlpha(self2.previous)) {
      effects.exit("literalAutolinkEmail");
      effects.exit("literalAutolink");
      return ok2(code2);
    }
    return nok(code2);
  }
}
function tokenizeWwwAutolink(effects, ok2, nok) {
  const self2 = this;
  return wwwStart;
  function wwwStart(code2) {
    if (code2 !== 87 && code2 !== 119 || !previousWww.call(self2, self2.previous) || previousUnbalanced(self2.events)) {
      return nok(code2);
    }
    effects.enter("literalAutolink");
    effects.enter("literalAutolinkWww");
    return effects.check(wwwPrefix, effects.attempt(domain, effects.attempt(path, wwwAfter), nok), nok)(code2);
  }
  function wwwAfter(code2) {
    effects.exit("literalAutolinkWww");
    effects.exit("literalAutolink");
    return ok2(code2);
  }
}
function tokenizeProtocolAutolink(effects, ok2, nok) {
  const self2 = this;
  let buffer = "";
  let seen = false;
  return protocolStart;
  function protocolStart(code2) {
    if ((code2 === 72 || code2 === 104) && previousProtocol.call(self2, self2.previous) && !previousUnbalanced(self2.events)) {
      effects.enter("literalAutolink");
      effects.enter("literalAutolinkHttp");
      buffer += String.fromCodePoint(code2);
      effects.consume(code2);
      return protocolPrefixInside;
    }
    return nok(code2);
  }
  function protocolPrefixInside(code2) {
    if (asciiAlpha(code2) && buffer.length < 5) {
      buffer += String.fromCodePoint(code2);
      effects.consume(code2);
      return protocolPrefixInside;
    }
    if (code2 === 58) {
      const protocol = buffer.toLowerCase();
      if (protocol === "http" || protocol === "https") {
        effects.consume(code2);
        return protocolSlashesInside;
      }
    }
    return nok(code2);
  }
  function protocolSlashesInside(code2) {
    if (code2 === 47) {
      effects.consume(code2);
      if (seen) {
        return afterProtocol;
      }
      seen = true;
      return protocolSlashesInside;
    }
    return nok(code2);
  }
  function afterProtocol(code2) {
    return code2 === null || asciiControl(code2) || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2) || unicodePunctuation(code2) ? nok(code2) : effects.attempt(domain, effects.attempt(path, protocolAfter), nok)(code2);
  }
  function protocolAfter(code2) {
    effects.exit("literalAutolinkHttp");
    effects.exit("literalAutolink");
    return ok2(code2);
  }
}
function tokenizeWwwPrefix(effects, ok2, nok) {
  let size2 = 0;
  return wwwPrefixInside;
  function wwwPrefixInside(code2) {
    if ((code2 === 87 || code2 === 119) && size2 < 3) {
      size2++;
      effects.consume(code2);
      return wwwPrefixInside;
    }
    if (code2 === 46 && size2 === 3) {
      effects.consume(code2);
      return wwwPrefixAfter;
    }
    return nok(code2);
  }
  function wwwPrefixAfter(code2) {
    return code2 === null ? nok(code2) : ok2(code2);
  }
}
function tokenizeDomain(effects, ok2, nok) {
  let underscoreInLastSegment;
  let underscoreInLastLastSegment;
  let seen;
  return domainInside;
  function domainInside(code2) {
    if (code2 === 46 || code2 === 95) {
      return effects.check(trail, domainAfter, domainAtPunctuation)(code2);
    }
    if (code2 === null || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2) || code2 !== 45 && unicodePunctuation(code2)) {
      return domainAfter(code2);
    }
    seen = true;
    effects.consume(code2);
    return domainInside;
  }
  function domainAtPunctuation(code2) {
    if (code2 === 95) {
      underscoreInLastSegment = true;
    } else {
      underscoreInLastLastSegment = underscoreInLastSegment;
      underscoreInLastSegment = void 0;
    }
    effects.consume(code2);
    return domainInside;
  }
  function domainAfter(code2) {
    if (underscoreInLastLastSegment || underscoreInLastSegment || !seen) {
      return nok(code2);
    }
    return ok2(code2);
  }
}
function tokenizePath(effects, ok2) {
  let sizeOpen = 0;
  let sizeClose = 0;
  return pathInside;
  function pathInside(code2) {
    if (code2 === 40) {
      sizeOpen++;
      effects.consume(code2);
      return pathInside;
    }
    if (code2 === 41 && sizeClose < sizeOpen) {
      return pathAtPunctuation(code2);
    }
    if (code2 === 33 || code2 === 34 || code2 === 38 || code2 === 39 || code2 === 41 || code2 === 42 || code2 === 44 || code2 === 46 || code2 === 58 || code2 === 59 || code2 === 60 || code2 === 63 || code2 === 93 || code2 === 95 || code2 === 126) {
      return effects.check(trail, ok2, pathAtPunctuation)(code2);
    }
    if (code2 === null || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2)) {
      return ok2(code2);
    }
    effects.consume(code2);
    return pathInside;
  }
  function pathAtPunctuation(code2) {
    if (code2 === 41) {
      sizeClose++;
    }
    effects.consume(code2);
    return pathInside;
  }
}
function tokenizeTrail(effects, ok2, nok) {
  return trail2;
  function trail2(code2) {
    if (code2 === 33 || code2 === 34 || code2 === 39 || code2 === 41 || code2 === 42 || code2 === 44 || code2 === 46 || code2 === 58 || code2 === 59 || code2 === 63 || code2 === 95 || code2 === 126) {
      effects.consume(code2);
      return trail2;
    }
    if (code2 === 38) {
      effects.consume(code2);
      return trailCharacterReferenceStart;
    }
    if (code2 === 93) {
      effects.consume(code2);
      return trailBracketAfter;
    }
    if (
      // `<` is an end.
      code2 === 60 || // So is whitespace.
      code2 === null || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2)
    ) {
      return ok2(code2);
    }
    return nok(code2);
  }
  function trailBracketAfter(code2) {
    if (code2 === null || code2 === 40 || code2 === 91 || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2)) {
      return ok2(code2);
    }
    return trail2(code2);
  }
  function trailCharacterReferenceStart(code2) {
    return asciiAlpha(code2) ? trailCharacterReferenceInside(code2) : nok(code2);
  }
  function trailCharacterReferenceInside(code2) {
    if (code2 === 59) {
      effects.consume(code2);
      return trail2;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return trailCharacterReferenceInside;
    }
    return nok(code2);
  }
}
function tokenizeEmailDomainDotTrail(effects, ok2, nok) {
  return start;
  function start(code2) {
    effects.consume(code2);
    return after;
  }
  function after(code2) {
    return asciiAlphanumeric(code2) ? nok(code2) : ok2(code2);
  }
}
function previousWww(code2) {
  return code2 === null || code2 === 40 || code2 === 42 || code2 === 95 || code2 === 91 || code2 === 93 || code2 === 126 || markdownLineEndingOrSpace(code2);
}
function previousProtocol(code2) {
  return !asciiAlpha(code2);
}
function previousEmail(code2) {
  return !(code2 === 47 || gfmAtext(code2));
}
function gfmAtext(code2) {
  return code2 === 43 || code2 === 45 || code2 === 46 || code2 === 95 || asciiAlphanumeric(code2);
}
function previousUnbalanced(events) {
  let index2 = events.length;
  let result = false;
  while (index2--) {
    const token = events[index2][1];
    if ((token.type === "labelLink" || token.type === "labelImage") && !token._balanced) {
      result = true;
      break;
    }
    if (token._gfmAutolinkLiteralWalkedInto) {
      result = false;
      break;
    }
  }
  if (events.length > 0 && !result) {
    events[events.length - 1][1]._gfmAutolinkLiteralWalkedInto = true;
  }
  return result;
}
const indent = {
  tokenize: tokenizeIndent,
  partial: true
};
function gfmFootnote() {
  return {
    document: {
      [91]: {
        name: "gfmFootnoteDefinition",
        tokenize: tokenizeDefinitionStart,
        continuation: {
          tokenize: tokenizeDefinitionContinuation
        },
        exit: gfmFootnoteDefinitionEnd
      }
    },
    text: {
      [91]: {
        name: "gfmFootnoteCall",
        tokenize: tokenizeGfmFootnoteCall
      },
      [93]: {
        name: "gfmPotentialFootnoteCall",
        add: "after",
        tokenize: tokenizePotentialGfmFootnoteCall,
        resolveTo: resolveToPotentialGfmFootnoteCall
      }
    }
  };
}
function tokenizePotentialGfmFootnoteCall(effects, ok2, nok) {
  const self2 = this;
  let index2 = self2.events.length;
  const defined = self2.parser.gfmFootnotes || (self2.parser.gfmFootnotes = []);
  let labelStart;
  while (index2--) {
    const token = self2.events[index2][1];
    if (token.type === "labelImage") {
      labelStart = token;
      break;
    }
    if (token.type === "gfmFootnoteCall" || token.type === "labelLink" || token.type === "label" || token.type === "image" || token.type === "link") {
      break;
    }
  }
  return start;
  function start(code2) {
    if (!labelStart || !labelStart._balanced) {
      return nok(code2);
    }
    const id = normalizeIdentifier(self2.sliceSerialize({
      start: labelStart.end,
      end: self2.now()
    }));
    if (id.codePointAt(0) !== 94 || !defined.includes(id.slice(1))) {
      return nok(code2);
    }
    effects.enter("gfmFootnoteCallLabelMarker");
    effects.consume(code2);
    effects.exit("gfmFootnoteCallLabelMarker");
    return ok2(code2);
  }
}
function resolveToPotentialGfmFootnoteCall(events, context) {
  let index2 = events.length;
  while (index2--) {
    if (events[index2][1].type === "labelImage" && events[index2][0] === "enter") {
      events[index2][1];
      break;
    }
  }
  events[index2 + 1][1].type = "data";
  events[index2 + 3][1].type = "gfmFootnoteCallLabelMarker";
  const call = {
    type: "gfmFootnoteCall",
    start: Object.assign({}, events[index2 + 3][1].start),
    end: Object.assign({}, events[events.length - 1][1].end)
  };
  const marker = {
    type: "gfmFootnoteCallMarker",
    start: Object.assign({}, events[index2 + 3][1].end),
    end: Object.assign({}, events[index2 + 3][1].end)
  };
  marker.end.column++;
  marker.end.offset++;
  marker.end._bufferIndex++;
  const string2 = {
    type: "gfmFootnoteCallString",
    start: Object.assign({}, marker.end),
    end: Object.assign({}, events[events.length - 1][1].start)
  };
  const chunk = {
    type: "chunkString",
    contentType: "string",
    start: Object.assign({}, string2.start),
    end: Object.assign({}, string2.end)
  };
  const replacement = [
    // Take the `labelImageMarker` (now `data`, the `!`)
    events[index2 + 1],
    events[index2 + 2],
    ["enter", call, context],
    // The `[`
    events[index2 + 3],
    events[index2 + 4],
    // The `^`.
    ["enter", marker, context],
    ["exit", marker, context],
    // Everything in between.
    ["enter", string2, context],
    ["enter", chunk, context],
    ["exit", chunk, context],
    ["exit", string2, context],
    // The ending (`]`, properly parsed and labelled).
    events[events.length - 2],
    events[events.length - 1],
    ["exit", call, context]
  ];
  events.splice(index2, events.length - index2 + 1, ...replacement);
  return events;
}
function tokenizeGfmFootnoteCall(effects, ok2, nok) {
  const self2 = this;
  const defined = self2.parser.gfmFootnotes || (self2.parser.gfmFootnotes = []);
  let size2 = 0;
  let data;
  return start;
  function start(code2) {
    effects.enter("gfmFootnoteCall");
    effects.enter("gfmFootnoteCallLabelMarker");
    effects.consume(code2);
    effects.exit("gfmFootnoteCallLabelMarker");
    return callStart;
  }
  function callStart(code2) {
    if (code2 !== 94) return nok(code2);
    effects.enter("gfmFootnoteCallMarker");
    effects.consume(code2);
    effects.exit("gfmFootnoteCallMarker");
    effects.enter("gfmFootnoteCallString");
    effects.enter("chunkString").contentType = "string";
    return callData;
  }
  function callData(code2) {
    if (
      // Too long.
      size2 > 999 || // Closing brace with nothing.
      code2 === 93 && !data || // Space or tab is not supported by GFM for some reason.
      // `\n` and `[` not being supported makes sense.
      code2 === null || code2 === 91 || markdownLineEndingOrSpace(code2)
    ) {
      return nok(code2);
    }
    if (code2 === 93) {
      effects.exit("chunkString");
      const token = effects.exit("gfmFootnoteCallString");
      if (!defined.includes(normalizeIdentifier(self2.sliceSerialize(token)))) {
        return nok(code2);
      }
      effects.enter("gfmFootnoteCallLabelMarker");
      effects.consume(code2);
      effects.exit("gfmFootnoteCallLabelMarker");
      effects.exit("gfmFootnoteCall");
      return ok2;
    }
    if (!markdownLineEndingOrSpace(code2)) {
      data = true;
    }
    size2++;
    effects.consume(code2);
    return code2 === 92 ? callEscape : callData;
  }
  function callEscape(code2) {
    if (code2 === 91 || code2 === 92 || code2 === 93) {
      effects.consume(code2);
      size2++;
      return callData;
    }
    return callData(code2);
  }
}
function tokenizeDefinitionStart(effects, ok2, nok) {
  const self2 = this;
  const defined = self2.parser.gfmFootnotes || (self2.parser.gfmFootnotes = []);
  let identifier;
  let size2 = 0;
  let data;
  return start;
  function start(code2) {
    effects.enter("gfmFootnoteDefinition")._container = true;
    effects.enter("gfmFootnoteDefinitionLabel");
    effects.enter("gfmFootnoteDefinitionLabelMarker");
    effects.consume(code2);
    effects.exit("gfmFootnoteDefinitionLabelMarker");
    return labelAtMarker;
  }
  function labelAtMarker(code2) {
    if (code2 === 94) {
      effects.enter("gfmFootnoteDefinitionMarker");
      effects.consume(code2);
      effects.exit("gfmFootnoteDefinitionMarker");
      effects.enter("gfmFootnoteDefinitionLabelString");
      effects.enter("chunkString").contentType = "string";
      return labelInside;
    }
    return nok(code2);
  }
  function labelInside(code2) {
    if (
      // Too long.
      size2 > 999 || // Closing brace with nothing.
      code2 === 93 && !data || // Space or tab is not supported by GFM for some reason.
      // `\n` and `[` not being supported makes sense.
      code2 === null || code2 === 91 || markdownLineEndingOrSpace(code2)
    ) {
      return nok(code2);
    }
    if (code2 === 93) {
      effects.exit("chunkString");
      const token = effects.exit("gfmFootnoteDefinitionLabelString");
      identifier = normalizeIdentifier(self2.sliceSerialize(token));
      effects.enter("gfmFootnoteDefinitionLabelMarker");
      effects.consume(code2);
      effects.exit("gfmFootnoteDefinitionLabelMarker");
      effects.exit("gfmFootnoteDefinitionLabel");
      return labelAfter;
    }
    if (!markdownLineEndingOrSpace(code2)) {
      data = true;
    }
    size2++;
    effects.consume(code2);
    return code2 === 92 ? labelEscape : labelInside;
  }
  function labelEscape(code2) {
    if (code2 === 91 || code2 === 92 || code2 === 93) {
      effects.consume(code2);
      size2++;
      return labelInside;
    }
    return labelInside(code2);
  }
  function labelAfter(code2) {
    if (code2 === 58) {
      effects.enter("definitionMarker");
      effects.consume(code2);
      effects.exit("definitionMarker");
      if (!defined.includes(identifier)) {
        defined.push(identifier);
      }
      return factorySpace(effects, whitespaceAfter, "gfmFootnoteDefinitionWhitespace");
    }
    return nok(code2);
  }
  function whitespaceAfter(code2) {
    return ok2(code2);
  }
}
function tokenizeDefinitionContinuation(effects, ok2, nok) {
  return effects.check(blankLine, ok2, effects.attempt(indent, ok2, nok));
}
function gfmFootnoteDefinitionEnd(effects) {
  effects.exit("gfmFootnoteDefinition");
}
function tokenizeIndent(effects, ok2, nok) {
  const self2 = this;
  return factorySpace(effects, afterPrefix, "gfmFootnoteDefinitionIndent", 4 + 1);
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === "gfmFootnoteDefinitionIndent" && tail[2].sliceSerialize(tail[1], true).length === 4 ? ok2(code2) : nok(code2);
  }
}
function gfmStrikethrough(options) {
  const options_ = options || {};
  let single = options_.singleTilde;
  const tokenizer = {
    name: "strikethrough",
    tokenize: tokenizeStrikethrough,
    resolveAll: resolveAllStrikethrough
  };
  if (single === null || single === void 0) {
    single = true;
  }
  return {
    text: {
      [126]: tokenizer
    },
    insideSpan: {
      null: [tokenizer]
    },
    attentionMarkers: {
      null: [126]
    }
  };
  function resolveAllStrikethrough(events, context) {
    let index2 = -1;
    while (++index2 < events.length) {
      if (events[index2][0] === "enter" && events[index2][1].type === "strikethroughSequenceTemporary" && events[index2][1]._close) {
        let open = index2;
        while (open--) {
          if (events[open][0] === "exit" && events[open][1].type === "strikethroughSequenceTemporary" && events[open][1]._open && // If the sizes are the same:
          events[index2][1].end.offset - events[index2][1].start.offset === events[open][1].end.offset - events[open][1].start.offset) {
            events[index2][1].type = "strikethroughSequence";
            events[open][1].type = "strikethroughSequence";
            const strikethrough2 = {
              type: "strikethrough",
              start: Object.assign({}, events[open][1].start),
              end: Object.assign({}, events[index2][1].end)
            };
            const text2 = {
              type: "strikethroughText",
              start: Object.assign({}, events[open][1].end),
              end: Object.assign({}, events[index2][1].start)
            };
            const nextEvents = [["enter", strikethrough2, context], ["enter", events[open][1], context], ["exit", events[open][1], context], ["enter", text2, context]];
            const insideSpan2 = context.parser.constructs.insideSpan.null;
            if (insideSpan2) {
              splice(nextEvents, nextEvents.length, 0, resolveAll(insideSpan2, events.slice(open + 1, index2), context));
            }
            splice(nextEvents, nextEvents.length, 0, [["exit", text2, context], ["enter", events[index2][1], context], ["exit", events[index2][1], context], ["exit", strikethrough2, context]]);
            splice(events, open - 1, index2 - open + 3, nextEvents);
            index2 = open + nextEvents.length - 2;
            break;
          }
        }
      }
    }
    index2 = -1;
    while (++index2 < events.length) {
      if (events[index2][1].type === "strikethroughSequenceTemporary") {
        events[index2][1].type = "data";
      }
    }
    return events;
  }
  function tokenizeStrikethrough(effects, ok2, nok) {
    const previous2 = this.previous;
    const events = this.events;
    let size2 = 0;
    return start;
    function start(code2) {
      if (previous2 === 126 && events[events.length - 1][1].type !== "characterEscape") {
        return nok(code2);
      }
      effects.enter("strikethroughSequenceTemporary");
      return more(code2);
    }
    function more(code2) {
      const before = classifyCharacter(previous2);
      if (code2 === 126) {
        if (size2 > 1) return nok(code2);
        effects.consume(code2);
        size2++;
        return more;
      }
      if (size2 < 2 && !single) return nok(code2);
      const token = effects.exit("strikethroughSequenceTemporary");
      const after = classifyCharacter(code2);
      token._open = !after || after === 2 && Boolean(before);
      token._close = !before || before === 2 && Boolean(after);
      return ok2(code2);
    }
  }
}
class EditMap {
  /**
   * Create a new edit map.
   */
  constructor() {
    this.map = [];
  }
  /**
   * Create an edit: a remove and/or add at a certain place.
   *
   * @param {number} index
   * @param {number} remove
   * @param {Array<Event>} add
   * @returns {undefined}
   */
  add(index2, remove, add) {
    addImplementation(this, index2, remove, add);
  }
  // To do: add this when moving to `micromark`.
  // /**
  //  * Create an edit: but insert `add` before existing additions.
  //  *
  //  * @param {number} index
  //  * @param {number} remove
  //  * @param {Array<Event>} add
  //  * @returns {undefined}
  //  */
  // addBefore(index, remove, add) {
  //   addImplementation(this, index, remove, add, true)
  // }
  /**
   * Done, change the events.
   *
   * @param {Array<Event>} events
   * @returns {undefined}
   */
  consume(events) {
    this.map.sort(function(a, b) {
      return a[0] - b[0];
    });
    if (this.map.length === 0) {
      return;
    }
    let index2 = this.map.length;
    const vecs = [];
    while (index2 > 0) {
      index2 -= 1;
      vecs.push(events.slice(this.map[index2][0] + this.map[index2][1]), this.map[index2][2]);
      events.length = this.map[index2][0];
    }
    vecs.push(events.slice());
    events.length = 0;
    let slice = vecs.pop();
    while (slice) {
      for (const element2 of slice) {
        events.push(element2);
      }
      slice = vecs.pop();
    }
    this.map.length = 0;
  }
}
function addImplementation(editMap, at, remove, add) {
  let index2 = 0;
  if (remove === 0 && add.length === 0) {
    return;
  }
  while (index2 < editMap.map.length) {
    if (editMap.map[index2][0] === at) {
      editMap.map[index2][1] += remove;
      editMap.map[index2][2].push(...add);
      return;
    }
    index2 += 1;
  }
  editMap.map.push([at, remove, add]);
}
function gfmTableAlign(events, index2) {
  let inDelimiterRow = false;
  const align = [];
  while (index2 < events.length) {
    const event = events[index2];
    if (inDelimiterRow) {
      if (event[0] === "enter") {
        if (event[1].type === "tableContent") {
          align.push(events[index2 + 1][1].type === "tableDelimiterMarker" ? "left" : "none");
        }
      } else if (event[1].type === "tableContent") {
        if (events[index2 - 1][1].type === "tableDelimiterMarker") {
          const alignIndex = align.length - 1;
          align[alignIndex] = align[alignIndex] === "left" ? "center" : "right";
        }
      } else if (event[1].type === "tableDelimiterRow") {
        break;
      }
    } else if (event[0] === "enter" && event[1].type === "tableDelimiterRow") {
      inDelimiterRow = true;
    }
    index2 += 1;
  }
  return align;
}
function gfmTable() {
  return {
    flow: {
      null: {
        name: "table",
        tokenize: tokenizeTable,
        resolveAll: resolveTable
      }
    }
  };
}
function tokenizeTable(effects, ok2, nok) {
  const self2 = this;
  let size2 = 0;
  let sizeB = 0;
  let seen;
  return start;
  function start(code2) {
    let index2 = self2.events.length - 1;
    while (index2 > -1) {
      const type = self2.events[index2][1].type;
      if (type === "lineEnding" || // Note: markdown-rs uses `whitespace` instead of `linePrefix`
      type === "linePrefix") index2--;
      else break;
    }
    const tail = index2 > -1 ? self2.events[index2][1].type : null;
    const next = tail === "tableHead" || tail === "tableRow" ? bodyRowStart : headRowBefore;
    if (next === bodyRowStart && self2.parser.lazy[self2.now().line]) {
      return nok(code2);
    }
    return next(code2);
  }
  function headRowBefore(code2) {
    effects.enter("tableHead");
    effects.enter("tableRow");
    return headRowStart(code2);
  }
  function headRowStart(code2) {
    if (code2 === 124) {
      return headRowBreak(code2);
    }
    seen = true;
    sizeB += 1;
    return headRowBreak(code2);
  }
  function headRowBreak(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (markdownLineEnding(code2)) {
      if (sizeB > 1) {
        sizeB = 0;
        self2.interrupt = true;
        effects.exit("tableRow");
        effects.enter("lineEnding");
        effects.consume(code2);
        effects.exit("lineEnding");
        return headDelimiterStart;
      }
      return nok(code2);
    }
    if (markdownSpace(code2)) {
      return factorySpace(effects, headRowBreak, "whitespace")(code2);
    }
    sizeB += 1;
    if (seen) {
      seen = false;
      size2 += 1;
    }
    if (code2 === 124) {
      effects.enter("tableCellDivider");
      effects.consume(code2);
      effects.exit("tableCellDivider");
      seen = true;
      return headRowBreak;
    }
    effects.enter("data");
    return headRowData(code2);
  }
  function headRowData(code2) {
    if (code2 === null || code2 === 124 || markdownLineEndingOrSpace(code2)) {
      effects.exit("data");
      return headRowBreak(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? headRowEscape : headRowData;
  }
  function headRowEscape(code2) {
    if (code2 === 92 || code2 === 124) {
      effects.consume(code2);
      return headRowData;
    }
    return headRowData(code2);
  }
  function headDelimiterStart(code2) {
    self2.interrupt = false;
    if (self2.parser.lazy[self2.now().line]) {
      return nok(code2);
    }
    effects.enter("tableDelimiterRow");
    seen = false;
    if (markdownSpace(code2)) {
      return factorySpace(effects, headDelimiterBefore, "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2);
    }
    return headDelimiterBefore(code2);
  }
  function headDelimiterBefore(code2) {
    if (code2 === 45 || code2 === 58) {
      return headDelimiterValueBefore(code2);
    }
    if (code2 === 124) {
      seen = true;
      effects.enter("tableCellDivider");
      effects.consume(code2);
      effects.exit("tableCellDivider");
      return headDelimiterCellBefore;
    }
    return headDelimiterNok(code2);
  }
  function headDelimiterCellBefore(code2) {
    if (markdownSpace(code2)) {
      return factorySpace(effects, headDelimiterValueBefore, "whitespace")(code2);
    }
    return headDelimiterValueBefore(code2);
  }
  function headDelimiterValueBefore(code2) {
    if (code2 === 58) {
      sizeB += 1;
      seen = true;
      effects.enter("tableDelimiterMarker");
      effects.consume(code2);
      effects.exit("tableDelimiterMarker");
      return headDelimiterLeftAlignmentAfter;
    }
    if (code2 === 45) {
      sizeB += 1;
      return headDelimiterLeftAlignmentAfter(code2);
    }
    if (code2 === null || markdownLineEnding(code2)) {
      return headDelimiterCellAfter(code2);
    }
    return headDelimiterNok(code2);
  }
  function headDelimiterLeftAlignmentAfter(code2) {
    if (code2 === 45) {
      effects.enter("tableDelimiterFiller");
      return headDelimiterFiller(code2);
    }
    return headDelimiterNok(code2);
  }
  function headDelimiterFiller(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return headDelimiterFiller;
    }
    if (code2 === 58) {
      seen = true;
      effects.exit("tableDelimiterFiller");
      effects.enter("tableDelimiterMarker");
      effects.consume(code2);
      effects.exit("tableDelimiterMarker");
      return headDelimiterRightAlignmentAfter;
    }
    effects.exit("tableDelimiterFiller");
    return headDelimiterRightAlignmentAfter(code2);
  }
  function headDelimiterRightAlignmentAfter(code2) {
    if (markdownSpace(code2)) {
      return factorySpace(effects, headDelimiterCellAfter, "whitespace")(code2);
    }
    return headDelimiterCellAfter(code2);
  }
  function headDelimiterCellAfter(code2) {
    if (code2 === 124) {
      return headDelimiterBefore(code2);
    }
    if (code2 === null || markdownLineEnding(code2)) {
      if (!seen || size2 !== sizeB) {
        return headDelimiterNok(code2);
      }
      effects.exit("tableDelimiterRow");
      effects.exit("tableHead");
      return ok2(code2);
    }
    return headDelimiterNok(code2);
  }
  function headDelimiterNok(code2) {
    return nok(code2);
  }
  function bodyRowStart(code2) {
    effects.enter("tableRow");
    return bodyRowBreak(code2);
  }
  function bodyRowBreak(code2) {
    if (code2 === 124) {
      effects.enter("tableCellDivider");
      effects.consume(code2);
      effects.exit("tableCellDivider");
      return bodyRowBreak;
    }
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("tableRow");
      return ok2(code2);
    }
    if (markdownSpace(code2)) {
      return factorySpace(effects, bodyRowBreak, "whitespace")(code2);
    }
    effects.enter("data");
    return bodyRowData(code2);
  }
  function bodyRowData(code2) {
    if (code2 === null || code2 === 124 || markdownLineEndingOrSpace(code2)) {
      effects.exit("data");
      return bodyRowBreak(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? bodyRowEscape : bodyRowData;
  }
  function bodyRowEscape(code2) {
    if (code2 === 92 || code2 === 124) {
      effects.consume(code2);
      return bodyRowData;
    }
    return bodyRowData(code2);
  }
}
function resolveTable(events, context) {
  let index2 = -1;
  let inFirstCellAwaitingPipe = true;
  let rowKind = 0;
  let lastCell = [0, 0, 0, 0];
  let cell = [0, 0, 0, 0];
  let afterHeadAwaitingFirstBodyRow = false;
  let lastTableEnd = 0;
  let currentTable;
  let currentBody;
  let currentCell;
  const map2 = new EditMap();
  while (++index2 < events.length) {
    const event = events[index2];
    const token = event[1];
    if (event[0] === "enter") {
      if (token.type === "tableHead") {
        afterHeadAwaitingFirstBodyRow = false;
        if (lastTableEnd !== 0) {
          flushTableEnd(map2, context, lastTableEnd, currentTable, currentBody);
          currentBody = void 0;
          lastTableEnd = 0;
        }
        currentTable = {
          type: "table",
          start: Object.assign({}, token.start),
          // Note: correct end is set later.
          end: Object.assign({}, token.end)
        };
        map2.add(index2, 0, [["enter", currentTable, context]]);
      } else if (token.type === "tableRow" || token.type === "tableDelimiterRow") {
        inFirstCellAwaitingPipe = true;
        currentCell = void 0;
        lastCell = [0, 0, 0, 0];
        cell = [0, index2 + 1, 0, 0];
        if (afterHeadAwaitingFirstBodyRow) {
          afterHeadAwaitingFirstBodyRow = false;
          currentBody = {
            type: "tableBody",
            start: Object.assign({}, token.start),
            // Note: correct end is set later.
            end: Object.assign({}, token.end)
          };
          map2.add(index2, 0, [["enter", currentBody, context]]);
        }
        rowKind = token.type === "tableDelimiterRow" ? 2 : currentBody ? 3 : 1;
      } else if (rowKind && (token.type === "data" || token.type === "tableDelimiterMarker" || token.type === "tableDelimiterFiller")) {
        inFirstCellAwaitingPipe = false;
        if (cell[2] === 0) {
          if (lastCell[1] !== 0) {
            cell[0] = cell[1];
            currentCell = flushCell(map2, context, lastCell, rowKind, void 0, currentCell);
            lastCell = [0, 0, 0, 0];
          }
          cell[2] = index2;
        }
      } else if (token.type === "tableCellDivider") {
        if (inFirstCellAwaitingPipe) {
          inFirstCellAwaitingPipe = false;
        } else {
          if (lastCell[1] !== 0) {
            cell[0] = cell[1];
            currentCell = flushCell(map2, context, lastCell, rowKind, void 0, currentCell);
          }
          lastCell = cell;
          cell = [lastCell[1], index2, 0, 0];
        }
      }
    } else if (token.type === "tableHead") {
      afterHeadAwaitingFirstBodyRow = true;
      lastTableEnd = index2;
    } else if (token.type === "tableRow" || token.type === "tableDelimiterRow") {
      lastTableEnd = index2;
      if (lastCell[1] !== 0) {
        cell[0] = cell[1];
        currentCell = flushCell(map2, context, lastCell, rowKind, index2, currentCell);
      } else if (cell[1] !== 0) {
        currentCell = flushCell(map2, context, cell, rowKind, index2, currentCell);
      }
      rowKind = 0;
    } else if (rowKind && (token.type === "data" || token.type === "tableDelimiterMarker" || token.type === "tableDelimiterFiller")) {
      cell[3] = index2;
    }
  }
  if (lastTableEnd !== 0) {
    flushTableEnd(map2, context, lastTableEnd, currentTable, currentBody);
  }
  map2.consume(context.events);
  index2 = -1;
  while (++index2 < context.events.length) {
    const event = context.events[index2];
    if (event[0] === "enter" && event[1].type === "table") {
      event[1]._align = gfmTableAlign(context.events, index2);
    }
  }
  return events;
}
function flushCell(map2, context, range, rowKind, rowEnd, previousCell) {
  const groupName = rowKind === 1 ? "tableHeader" : rowKind === 2 ? "tableDelimiter" : "tableData";
  const valueName = "tableContent";
  if (range[0] !== 0) {
    previousCell.end = Object.assign({}, getPoint(context.events, range[0]));
    map2.add(range[0], 0, [["exit", previousCell, context]]);
  }
  const now = getPoint(context.events, range[1]);
  previousCell = {
    type: groupName,
    start: Object.assign({}, now),
    // Note: correct end is set later.
    end: Object.assign({}, now)
  };
  map2.add(range[1], 0, [["enter", previousCell, context]]);
  if (range[2] !== 0) {
    const relatedStart = getPoint(context.events, range[2]);
    const relatedEnd = getPoint(context.events, range[3]);
    const valueToken = {
      type: valueName,
      start: Object.assign({}, relatedStart),
      end: Object.assign({}, relatedEnd)
    };
    map2.add(range[2], 0, [["enter", valueToken, context]]);
    if (rowKind !== 2) {
      const start = context.events[range[2]];
      const end = context.events[range[3]];
      start[1].end = Object.assign({}, end[1].end);
      start[1].type = "chunkText";
      start[1].contentType = "text";
      if (range[3] > range[2] + 1) {
        const a = range[2] + 1;
        const b = range[3] - range[2] - 1;
        map2.add(a, b, []);
      }
    }
    map2.add(range[3] + 1, 0, [["exit", valueToken, context]]);
  }
  if (rowEnd !== void 0) {
    previousCell.end = Object.assign({}, getPoint(context.events, rowEnd));
    map2.add(rowEnd, 0, [["exit", previousCell, context]]);
    previousCell = void 0;
  }
  return previousCell;
}
function flushTableEnd(map2, context, index2, table2, tableBody) {
  const exits = [];
  const related = getPoint(context.events, index2);
  if (tableBody) {
    tableBody.end = Object.assign({}, related);
    exits.push(["exit", tableBody, context]);
  }
  table2.end = Object.assign({}, related);
  exits.push(["exit", table2, context]);
  map2.add(index2 + 1, 0, exits);
}
function getPoint(events, index2) {
  const event = events[index2];
  const side = event[0] === "enter" ? "start" : "end";
  return event[1][side];
}
const tasklistCheck = {
  name: "tasklistCheck",
  tokenize: tokenizeTasklistCheck
};
function gfmTaskListItem() {
  return {
    text: {
      [91]: tasklistCheck
    }
  };
}
function tokenizeTasklistCheck(effects, ok2, nok) {
  const self2 = this;
  return open;
  function open(code2) {
    if (
      // Exit if there’s stuff before.
      self2.previous !== null || // Exit if not in the first content that is the first child of a list
      // item.
      !self2._gfmTasklistFirstContentOfListItem
    ) {
      return nok(code2);
    }
    effects.enter("taskListCheck");
    effects.enter("taskListCheckMarker");
    effects.consume(code2);
    effects.exit("taskListCheckMarker");
    return inside;
  }
  function inside(code2) {
    if (markdownLineEndingOrSpace(code2)) {
      effects.enter("taskListCheckValueUnchecked");
      effects.consume(code2);
      effects.exit("taskListCheckValueUnchecked");
      return close;
    }
    if (code2 === 88 || code2 === 120) {
      effects.enter("taskListCheckValueChecked");
      effects.consume(code2);
      effects.exit("taskListCheckValueChecked");
      return close;
    }
    return nok(code2);
  }
  function close(code2) {
    if (code2 === 93) {
      effects.enter("taskListCheckMarker");
      effects.consume(code2);
      effects.exit("taskListCheckMarker");
      effects.exit("taskListCheck");
      return after;
    }
    return nok(code2);
  }
  function after(code2) {
    if (markdownLineEnding(code2)) {
      return ok2(code2);
    }
    if (markdownSpace(code2)) {
      return effects.check({
        tokenize: spaceThenNonSpace
      }, ok2, nok)(code2);
    }
    return nok(code2);
  }
}
function spaceThenNonSpace(effects, ok2, nok) {
  return factorySpace(effects, after, "whitespace");
  function after(code2) {
    return code2 === null ? nok(code2) : ok2(code2);
  }
}
function gfm(options) {
  return combineExtensions([
    gfmAutolinkLiteral(),
    gfmFootnote(),
    gfmStrikethrough(options),
    gfmTable(),
    gfmTaskListItem()
  ]);
}
const emptyOptions = {};
function remarkGfm(options) {
  const self2 = (
    /** @type {Processor<Root>} */
    this
  );
  const settings = options || emptyOptions;
  const data = self2.data();
  const micromarkExtensions = data.micromarkExtensions || (data.micromarkExtensions = []);
  const fromMarkdownExtensions = data.fromMarkdownExtensions || (data.fromMarkdownExtensions = []);
  const toMarkdownExtensions = data.toMarkdownExtensions || (data.toMarkdownExtensions = []);
  micromarkExtensions.push(gfm(settings));
  fromMarkdownExtensions.push(gfmFromMarkdown());
  toMarkdownExtensions.push(gfmToMarkdown(settings));
}
function JsonBlock$1({ value, maxHeight = 160 }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "pre",
    {
      style: {
        margin: 0,
        padding: 8,
        background: "var(--hv-bg, #0b0d11)",
        border: "1px solid var(--hv-border)",
        borderRadius: "var(--hv-radius-sm, 4px)",
        fontSize: 11,
        lineHeight: 1.5,
        whiteSpace: "pre-wrap",
        wordBreak: "break-word",
        maxHeight,
        overflow: "auto"
      },
      children: JSON.stringify(value, null, 2)
    }
  );
}
function severityColor$1(sev) {
  const s = typeof sev === "string" ? sev.toLowerCase() : "";
  if (s === "high" || s === "critical") return "var(--hv-danger, #ef5b5b)";
  if (s === "med" || s === "medium") return "var(--hv-warn, #facc15)";
  return "var(--hv-fg-muted, #8c93a3)";
}
function weaknessSummary(w) {
  if (typeof w === "string") return w;
  if (w && typeof w === "object") {
    const obj = w;
    if (typeof obj.issue === "string") return obj.issue;
    if (typeof obj.id === "string") return obj.id;
  }
  return "item";
}
function WeaknessItem({
  weakness,
  index: index2,
  annLookup,
  actions,
  hideExtras,
  headerBadge
}) {
  if (typeof weakness === "string") {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("li", { style: { fontSize: "var(--hv-fs-xs, 12px)" }, children: [
      headerBadge && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { marginRight: 6 }, children: headerBadge }),
      weakness,
      actions
    ] });
  }
  if (weakness === null || typeof weakness !== "object") {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("li", { style: { fontSize: "var(--hv-fs-xs, 12px)" }, children: [
      headerBadge && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { marginBottom: 4 }, children: headerBadge }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(JsonBlock$1, { value: weakness, maxHeight: 120 }),
      actions
    ] });
  }
  const obj = weakness;
  const id = typeof obj.id === "string" ? obj.id : void 0;
  const ann = typeof obj.ann === "string" ? obj.ann : void 0;
  const severity = obj.severity;
  const issue = typeof obj.issue === "string" ? obj.issue : void 0;
  const fix = typeof obj.fix === "string" ? obj.fix : void 0;
  const known = /* @__PURE__ */ new Set(["id", "ann", "severity", "issue", "fix"]);
  const extras = {};
  for (const k of Object.keys(obj)) if (!known.has(k)) extras[k] = obj[k];
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("li", { style: { fontSize: "var(--hv-fs-xs, 12px)", lineHeight: 1.55 }, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        style: {
          display: "flex",
          gap: 6,
          alignItems: "baseline",
          flexWrap: "wrap"
        },
        children: [
          id && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", style: { color: "var(--hv-fg-muted)", fontWeight: 600 }, children: id }),
          severity != null && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { color: severityColor$1(severity), fontWeight: 600 }, children: [
            "[",
            String(severity),
            "]"
          ] }),
          ann && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "span",
            {
              className: "mono",
              style: { fontSize: "var(--hv-fs-kicker, 11px)", color: "var(--hv-fg-muted)" },
              title: annLookup == null ? void 0 : annLookup.get(ann),
              children: [
                "→ ",
                ann,
                (annLookup == null ? void 0 : annLookup.has(ann)) ? `: ${truncate(annLookup.get(ann), 60)}` : ""
              ]
            }
          ),
          headerBadge && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { marginLeft: "auto" }, children: headerBadge })
        ]
      }
    ),
    issue && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { marginTop: 4 }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Issue:" }),
      " ",
      issue
    ] }),
    fix && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { marginTop: 2, color: "var(--hv-fg-subtle)" }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Suggested fix:" }),
      " ",
      fix
    ] }),
    !hideExtras && Object.keys(extras).length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { style: { marginTop: 6 }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "summary",
        {
          style: {
            cursor: "pointer",
            fontSize: "var(--hv-fs-kicker, 11px)",
            color: "var(--hv-fg-muted)"
          },
          children: [
            "Other fields (",
            Object.keys(extras).length,
            ")"
          ]
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(JsonBlock$1, { value: extras, maxHeight: 160 })
    ] }),
    actions
  ] }, id ?? index2);
}
function truncate(s, n) {
  return s.length > n ? `${s.slice(0, n - 1)}…` : s;
}
const MARKDOWN_KEYS = ["markdown", "report", "notes", "text"];
/* @__PURE__ */ new Set([
  "verdict",
  "weaknesses",
  "plan",
  "summary",
  "groups",
  "files_changed",
  "commit_sha",
  ...MARKDOWN_KEYS
]);
function severityColor(sev) {
  const s = typeof sev === "string" ? sev.toLowerCase() : "";
  if (s === "high" || s === "critical") return "var(--hv-danger, #ef5b5b)";
  if (s === "med" || s === "medium") return "var(--hv-warn, #facc15)";
  return "var(--hv-fg-muted, #8c93a3)";
}
function VerdictPill({ verdict }) {
  const isRevise = verdict.toUpperCase() === "REVISE";
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "span",
    {
      style: {
        padding: "1px 6px",
        borderRadius: 3,
        background: isRevise ? "rgba(239,91,91,0.15)" : "rgba(74,222,128,0.15)",
        color: isRevise ? "var(--hv-danger, #ef5b5b)" : "var(--hv-success, #4ade80)",
        fontWeight: 700,
        fontSize: 11
      },
      children: verdict
    }
  );
}
function Markdown({ text: text2 }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-md", style: { fontSize: 12, lineHeight: 1.55 }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Markdown$1, { remarkPlugins: [remarkGfm], children: text2 }) });
}
function JsonBlock({ value, maxHeight = 320 }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "pre",
    {
      style: {
        margin: 0,
        padding: 8,
        background: "var(--hv-bg, #0b0d11)",
        border: "1px solid var(--hv-border)",
        borderRadius: 4,
        fontSize: 11,
        lineHeight: 1.5,
        whiteSpace: "pre-wrap",
        wordBreak: "break-word",
        maxHeight,
        overflow: "auto"
      },
      children: JSON.stringify(value, null, 2)
    }
  );
}
function WeaknessList({
  weaknesses,
  annLookup
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "ol",
    {
      style: {
        margin: 0,
        paddingLeft: 20,
        display: "flex",
        flexDirection: "column",
        gap: "var(--hv-space-sm, 8px)"
      },
      children: weaknesses.map((w, i) => /* @__PURE__ */ jsxRuntimeExports.jsx(WeaknessItem, { weakness: w, index: i, annLookup }, i))
    }
  );
}
function GroupsList({ groups }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("ol", { style: { margin: 0, paddingLeft: 20, display: "flex", flexDirection: "column", gap: 6 }, children: groups.map((g, i) => {
    if (g === null || typeof g !== "object") {
      return /* @__PURE__ */ jsxRuntimeExports.jsx("li", { children: /* @__PURE__ */ jsxRuntimeExports.jsx(JsonBlock, { value: g, maxHeight: 120 }) }, i);
    }
    const obj = g;
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("li", { style: { fontSize: 12 }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", gap: 6, flexWrap: "wrap", alignItems: "baseline" }, children: [
        typeof obj.id === "string" && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", style: { color: "var(--hv-fg-muted)" }, children: obj.id }),
        obj.severity != null && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { color: severityColor(obj.severity), fontWeight: 600 }, children: [
          "[",
          String(obj.severity),
          "]"
        ] }),
        typeof obj.area === "string" && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: obj.area })
      ] }),
      Array.isArray(obj.annotation_ids) && obj.annotation_ids.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { fontSize: 11, color: "var(--hv-fg-muted)" }, children: [
        "annotations: ",
        obj.annotation_ids.map(String).join(", ")
      ] }),
      Array.isArray(obj.files) && obj.files.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { fontSize: 11, color: "var(--hv-fg-muted)" }, children: [
        "files: ",
        obj.files.map(String).join(", ")
      ] })
    ] }, obj.id ?? i);
  }) });
}
function ResultPayloadView({ payload, annLookup }) {
  if (!payload) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { fontSize: 12, color: "var(--hv-fg-muted)" }, children: "No payload." });
  }
  const renderedKeys = /* @__PURE__ */ new Set();
  const sections = [];
  const verdict = payload.verdict;
  if (typeof verdict === "string" && verdict.length > 0) {
    renderedKeys.add("verdict");
    sections.push(
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", gap: 6, alignItems: "baseline" }, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { fontSize: 11, color: "var(--hv-fg-muted)" }, children: "verdict" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(VerdictPill, { verdict })
      ] }, "verdict")
    );
  }
  for (const key of MARKDOWN_KEYS) {
    const v = payload[key];
    if (typeof v === "string" && v.trim().length > 0) {
      renderedKeys.add(key);
      sections.push(
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { fontSize: 11, color: "var(--hv-fg-muted)", marginBottom: 4 }, children: key }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Markdown, { text: v })
        ] }, `md-${key}`)
      );
    }
  }
  const summary = payload.summary;
  if (typeof summary === "string" && summary.trim()) {
    renderedKeys.add("summary");
    sections.push(
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { fontSize: 12, whiteSpace: "pre-wrap" }, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Summary:" }),
        " ",
        summary
      ] }, "summary")
    );
  }
  const weaknesses = payload.weaknesses;
  if (Array.isArray(weaknesses) && weaknesses.length > 0) {
    renderedKeys.add("weaknesses");
    sections.push(
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { fontSize: 11, color: "var(--hv-fg-muted)", marginBottom: 4 }, children: [
          "weaknesses (",
          weaknesses.length,
          ")"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(WeaknessList, { weaknesses, annLookup })
      ] }, "weaknesses")
    );
  }
  const groups = payload.groups;
  if (Array.isArray(groups) && groups.length > 0) {
    renderedKeys.add("groups");
    sections.push(
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { fontSize: 11, color: "var(--hv-fg-muted)", marginBottom: 4 }, children: [
          "groups (",
          groups.length,
          ")"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(GroupsList, { groups })
      ] }, "groups")
    );
  }
  const filesChanged = payload.files_changed;
  if (Array.isArray(filesChanged) && filesChanged.length > 0) {
    renderedKeys.add("files_changed");
    sections.push(
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { fontSize: 12 }, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { fontSize: 11, color: "var(--hv-fg-muted)", marginBottom: 4 }, children: [
          "files_changed (",
          filesChanged.length,
          ")"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("ul", { className: "mono", style: { margin: 0, paddingLeft: 20, fontSize: 11 }, children: filesChanged.map((f, i) => /* @__PURE__ */ jsxRuntimeExports.jsx("li", { children: typeof f === "string" ? f : JSON.stringify(f) }, i)) })
      ] }, "files_changed")
    );
  }
  const commitSha = payload.commit_sha;
  if (typeof commitSha === "string" && commitSha.length > 0) {
    renderedKeys.add("commit_sha");
    sections.push(
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { fontSize: 12 }, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { fontSize: 11, color: "var(--hv-fg-muted)" }, children: "commit " }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", children: commitSha })
      ] }, "commit_sha")
    );
  }
  const plan = payload.plan;
  if (plan != null) {
    renderedKeys.add("plan");
    sections.push(
      /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { style: { fontSize: 12 }, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("summary", { style: { cursor: "pointer", fontSize: 11, color: "var(--hv-fg-muted)" }, children: [
          "plan ",
          typeof plan === "string" ? `(${plan.length} chars)` : ""
        ] }),
        typeof plan === "string" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Markdown, { text: plan }) : /* @__PURE__ */ jsxRuntimeExports.jsx(JsonBlock, { value: plan })
      ] }, "plan")
    );
  }
  const otherKeys = Object.keys(payload).filter((k) => !renderedKeys.has(k));
  if (otherKeys.length > 0) {
    const other = {};
    for (const k of otherKeys) other[k] = payload[k];
    sections.push(
      /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("summary", { style: { cursor: "pointer", fontSize: 11, color: "var(--hv-fg-muted)" }, children: [
          "Other fields (",
          otherKeys.length,
          "): ",
          otherKeys.join(", ")
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(JsonBlock, { value: other })
      ] }, "other")
    );
  }
  sections.push(
    /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("summary", { style: { cursor: "pointer", fontSize: 11, color: "var(--hv-fg-muted)" }, children: "Raw JSON" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(JsonBlock, { value: payload })
    ] }, "raw")
  );
  const nothingRecognized = renderedKeys.size === 0 || renderedKeys.size === 1 && renderedKeys.has("verdict") && otherKeys.length > 0;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      "data-testid": nothingRecognized ? "payload-unknown-shape" : "payload-rendered",
      style: { display: "flex", flexDirection: "column", gap: 10 },
      children: sections
    }
  );
}
function isLikelyMarkdown(path2) {
  return /\.(md|markdown)$/i.test(path2);
}
function isLikelyJson(path2, content2) {
  if (/\.(json|jsonl)$/i.test(path2)) return true;
  const t = content2.trimStart();
  return t.startsWith("{") || t.startsWith("[");
}
function ArtifactDrawer({ target, onClose }) {
  const [fileContent, setFileContent] = reactExports.useState(null);
  const [loading, setLoading] = reactExports.useState(false);
  const [error, setError] = reactExports.useState(null);
  const drawerRef = reactExports.useRef(null);
  const closeBtnRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    if (!target || target.kind !== "file_event") {
      setFileContent(null);
      setError(null);
      return;
    }
    if (target.event.id == null) {
      setError("No event ID — cannot fetch content.");
      return;
    }
    let cancelled = false;
    setLoading(true);
    setError(null);
    setFileContent(null);
    void (async () => {
      try {
        const result = await api.tasks.getFileEventContent(
          target.taskId,
          target.event.id
        );
        if (!cancelled) setFileContent(result);
      } catch (err) {
        if (!cancelled) setError(err instanceof Error ? err.message : "Failed to load file.");
      } finally {
        if (!cancelled) setLoading(false);
      }
    })();
    return () => {
      cancelled = true;
    };
  }, [target]);
  reactExports.useEffect(() => {
    var _a;
    if (!target) return;
    (_a = closeBtnRef.current) == null ? void 0 : _a.focus();
    const handler = (e) => {
      if (e.key === "Escape") {
        onClose();
        return;
      }
      if (e.key === "Tab" && drawerRef.current) {
        const focusable = drawerRef.current.querySelectorAll(
          'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
        );
        if (focusable.length === 0) return;
        const first = focusable[0];
        const last = focusable[focusable.length - 1];
        if (e.shiftKey && document.activeElement === first) {
          e.preventDefault();
          last.focus();
        } else if (!e.shiftKey && document.activeElement === last) {
          e.preventDefault();
          first.focus();
        }
      }
    };
    document.addEventListener("keydown", handler);
    const prev = document.body.style.overflow;
    document.body.style.overflow = "hidden";
    return () => {
      document.removeEventListener("keydown", handler);
      document.body.style.overflow = prev;
    };
  }, [target, onClose]);
  if (!target) return null;
  const title = target.kind === "file_event" ? target.event.path : target.kind === "text" ? target.title : `Result ${target.result.id}`;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      "data-testid": "artifact-drawer-backdrop",
      role: "presentation",
      onClick: (e) => {
        if (e.target === e.currentTarget) onClose();
      },
      style: {
        position: "fixed",
        inset: 0,
        background: "rgba(0,0,0,0.45)",
        zIndex: 1e3
      },
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            ref: drawerRef,
            "data-testid": "artifact-drawer",
            role: "dialog",
            "aria-modal": "true",
            "aria-label": title,
            style: {
              position: "fixed",
              top: 0,
              right: 0,
              bottom: 0,
              width: "min(860px, 92vw)",
              background: "var(--hv-surface, #14171c)",
              borderLeft: "1px solid var(--hv-border)",
              boxShadow: "-8px 0 24px rgba(0,0,0,0.5)",
              display: "flex",
              flexDirection: "column",
              animation: "hv-drawer-in 160ms ease-out"
            },
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "div",
                {
                  style: {
                    padding: "10px 14px",
                    borderBottom: "1px solid var(--hv-border)",
                    display: "flex",
                    alignItems: "center",
                    gap: 8,
                    flexShrink: 0
                  },
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "span",
                      {
                        className: "mono",
                        style: { fontSize: 12, flex: 1, wordBreak: "break-all" },
                        children: title
                      }
                    ),
                    target.kind === "text" && target.size != null && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { fontSize: 11, color: "var(--hv-muted)", whiteSpace: "nowrap" }, children: [
                      target.size.toLocaleString(),
                      " bytes",
                      target.truncated && " (truncated)"
                    ] }),
                    target.kind === "file_event" && fileContent && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { fontSize: 11, color: "var(--hv-muted)", whiteSpace: "nowrap" }, children: [
                      fileContent.size.toLocaleString(),
                      " bytes",
                      fileContent.truncated && " (truncated)"
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "button",
                      {
                        ref: closeBtnRef,
                        onClick: onClose,
                        "aria-label": "Close artifact drawer",
                        "data-testid": "artifact-drawer-close",
                        style: {
                          background: "none",
                          border: "none",
                          cursor: "pointer",
                          color: "var(--hv-muted)",
                          padding: "2px 8px",
                          fontSize: 16
                        },
                        children: "✕"
                      }
                    )
                  ]
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { overflow: "auto", flex: 1, padding: "12px 14px" }, children: [
                target.kind === "file_event" && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                  loading && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { color: "var(--hv-muted)", fontSize: 12 }, children: "Loading…" }),
                  error && !loading && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { color: "var(--hv-danger, #e55)", fontSize: 12 }, children: error }),
                  fileContent && !loading && /* @__PURE__ */ jsxRuntimeExports.jsx(FileContentRender, { content: fileContent })
                ] }),
                target.kind === "result" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                  ResultPayloadView,
                  {
                    payload: target.result.payload,
                    annLookup: target.annLookup
                  }
                ),
                target.kind === "text" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "pre",
                  {
                    className: target.language === "diff" ? "language-diff" : void 0,
                    style: {
                      margin: 0,
                      fontFamily: "var(--hv-font-mono, ui-monospace, SFMono-Regular, Menlo, monospace)",
                      fontSize: 12,
                      lineHeight: 1.55,
                      whiteSpace: "pre-wrap",
                      wordBreak: "break-word"
                    },
                    children: target.content
                  }
                )
              ] })
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx("style", { children: `
        @keyframes hv-drawer-in {
          from { transform: translateX(20px); opacity: 0.6; }
          to   { transform: translateX(0);    opacity: 1; }
        }
      ` })
      ]
    }
  );
}
function FileContentRender({ content: content2 }) {
  const md = isLikelyMarkdown(content2.path);
  const json = !md && isLikelyJson(content2.path, content2.content);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    content2.truncated && /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        style: {
          fontSize: 11,
          color: "var(--hv-warn, #b8860b)",
          marginBottom: 8,
          padding: "4px 8px",
          background: "var(--hv-warn-bg, rgba(220,160,30,0.12))",
          borderRadius: 4
        },
        children: "File exceeds 256 KiB — showing first 256 KiB only."
      }
    ),
    md ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-md", style: { fontSize: 13, lineHeight: 1.6 }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Markdown$1, { remarkPlugins: [remarkGfm], children: content2.content }) }) : json ? /* @__PURE__ */ jsxRuntimeExports.jsx(
      "pre",
      {
        style: {
          margin: 0,
          fontFamily: "var(--hv-font-mono, monospace)",
          fontSize: 12,
          lineHeight: 1.5,
          whiteSpace: "pre-wrap",
          wordBreak: "break-word"
        },
        children: prettyJson(content2.content)
      }
    ) : /* @__PURE__ */ jsxRuntimeExports.jsx(
      "pre",
      {
        style: {
          margin: 0,
          fontFamily: "var(--hv-font-mono, monospace)",
          fontSize: 12,
          lineHeight: 1.5,
          whiteSpace: "pre-wrap",
          wordBreak: "break-all"
        },
        children: content2.content
      }
    )
  ] });
}
function prettyJson(s) {
  try {
    return JSON.stringify(JSON.parse(s), null, 2);
  } catch {
    return s;
  }
}
async function jsonOrThrow(res) {
  if (!res.ok) {
    const text2 = await res.text().catch(() => "");
    const err = new Error(`API ${res.status}: ${text2}`);
    err.status = res.status;
    throw err;
  }
  if (res.status === 204) return void 0;
  return await res.json();
}
const annotationsApi = {
  async list() {
    const res = await fetch("/api/annotations", { credentials: "same-origin" });
    const body = await jsonOrThrow(res);
    return body.annotations;
  },
  async create(input) {
    const res = await fetch("/api/annotations", {
      method: "POST",
      credentials: "same-origin",
      headers: { "content-type": "application/json" },
      body: JSON.stringify(input)
    });
    return jsonOrThrow(res);
  },
  async remove(id) {
    const res = await fetch(`/api/annotations/${encodeURIComponent(id)}`, {
      method: "DELETE",
      credentials: "same-origin"
    });
    if (!res.ok) {
      const text2 = await res.text().catch(() => "");
      throw new Error(`API ${res.status}: ${text2}`);
    }
  },
  async startMission(opts) {
    const dispatch = (opts == null ? void 0 : opts.dispatch) ?? "spawn";
    const res = await fetch("/api/annotations/missions", {
      method: "POST",
      credentials: "same-origin",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({ dispatch })
    });
    return jsonOrThrow(res);
  },
  async archive(taskId) {
    const res = await fetch(
      `/api/tasks/${encodeURIComponent(taskId)}/annotations`,
      { credentials: "same-origin" }
    );
    const body = await jsonOrThrow(res);
    return body.archive;
  },
  async resumeWithFeedback(taskId, body) {
    const res = await fetch(
      `/api/tasks/${encodeURIComponent(taskId)}/resume?role=conductor`,
      {
        method: "POST",
        credentials: "same-origin",
        headers: { "content-type": "application/json" },
        body: JSON.stringify(body)
      }
    );
    return jsonOrThrow(res);
  }
};
const annotations = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  annotationsApi
}, Symbol.toStringTag, { value: "Module" }));
function djb2(s) {
  let h = 5381;
  for (let i = 0; i < s.length; i++) h = (h << 5) + h ^ s.charCodeAt(i);
  return h >>> 0;
}
function weaknessKey(w, i) {
  if (w && typeof w === "object" && "id" in w && typeof w.id === "string") {
    return w.id;
  }
  const summary = weaknessSummary(w).slice(0, 120);
  return `idx-${i}-${djb2(summary).toString(16)}`;
}
const STANCE_OPTIONS = [
  { value: "accept", label: "Accept", tone: "var(--hv-success, #4ade80)" },
  { value: "override", label: "Override", tone: "var(--hv-danger, #ef5b5b)" },
  { value: "note", label: "Note", tone: "var(--hv-honey-5, #ffb84d)" }
];
function hasDraftContent(d) {
  if (!d) return false;
  return d.status !== "note" || d.note.trim() !== "";
}
function renderStanceBadge(draft, hasContent) {
  const stance = (draft == null ? void 0 : draft.status) ?? "note";
  let label = "Pending";
  let symbol = "○";
  let bg = "rgba(110,118,129,0.18)";
  let fg = "var(--hv-fg-muted, #8c93a3)";
  if (hasContent) {
    if (stance === "accept") {
      label = "Pass";
      symbol = "✓";
      bg = "rgba(74,222,128,0.18)";
      fg = "var(--hv-success, #4ade80)";
    } else if (stance === "override") {
      label = "Override";
      symbol = "✗";
      bg = "rgba(239,91,91,0.18)";
      fg = "var(--hv-danger, #ef5b5b)";
    } else {
      label = "Note";
      symbol = "●";
      bg = "rgba(255,184,77,0.18)";
      fg = "var(--hv-honey-5, #ffb84d)";
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "span",
    {
      "data-testid": `weakness-stance-badge-${label.toLowerCase()}`,
      style: {
        display: "inline-flex",
        alignItems: "center",
        gap: 4,
        padding: "1px 8px",
        borderRadius: "var(--hv-radius-full, 999px)",
        background: bg,
        color: fg,
        fontSize: "var(--hv-fs-kicker, 11px)",
        fontWeight: 700,
        letterSpacing: 0.3,
        border: "1px solid currentColor"
      },
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { "aria-hidden": true, style: { fontSize: 10 }, children: symbol }),
        label
      ]
    }
  );
}
function RowControls({
  weaknessId,
  draft,
  busy,
  autoFocus,
  onChange,
  onCollapse
}) {
  const radioName = `stance-${weaknessId}`;
  const taRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    if (autoFocus && taRef.current) {
      taRef.current.focus();
    }
  }, [autoFocus]);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { marginTop: "var(--hv-space-sm, 8px)" }, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        role: "radiogroup",
        "aria-label": `Stance for ${weaknessId}`,
        className: "hv-chips",
        style: { marginBottom: 6 },
        children: STANCE_OPTIONS.map((opt) => {
          const checked = draft.status === opt.value;
          const inputId = `${radioName}-${opt.value}`;
          return /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "label",
            {
              htmlFor: inputId,
              className: `hv-chip${checked ? " on" : ""}`,
              "data-testid": `feedback-stance-${weaknessId}-${opt.value}`,
              style: {
                cursor: busy ? "not-allowed" : "pointer",
                opacity: busy ? 0.6 : 1
              },
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "input",
                  {
                    id: inputId,
                    type: "radio",
                    name: radioName,
                    value: opt.value,
                    checked,
                    disabled: busy,
                    onChange: () => onChange({ status: opt.value }),
                    style: {
                      position: "absolute",
                      width: 1,
                      height: 1,
                      padding: 0,
                      margin: -1,
                      overflow: "hidden",
                      clip: "rect(0,0,0,0)",
                      whiteSpace: "nowrap",
                      border: 0
                    }
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { "aria-hidden": true, style: { color: opt.tone, marginRight: 4 }, children: "●" }),
                opt.label
              ]
            },
            opt.value
          );
        })
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "textarea",
      {
        ref: taRef,
        className: "hv-inp",
        rows: 2,
        placeholder: "Optional note…",
        value: draft.note,
        disabled: busy,
        onChange: (e) => onChange({ note: e.target.value }),
        "data-testid": `feedback-note-${weaknessId}`,
        style: { minHeight: 56, fontSize: "var(--hv-fs-xs, 12px)" },
        "aria-label": `Note for ${weaknessId}`
      }
    ),
    onCollapse && !hasDraftContent(draft) && /* @__PURE__ */ jsxRuntimeExports.jsx(
      "button",
      {
        type: "button",
        onClick: onCollapse,
        disabled: busy,
        className: "hv-btn hv-btn-subtle hv-btn-sm",
        "data-testid": `feedback-collapse-${weaknessId}`,
        style: {
          marginTop: 6,
          fontSize: "var(--hv-fs-kicker, 11px)",
          color: "var(--hv-fg-muted)",
          background: "transparent",
          border: "none",
          cursor: busy ? "not-allowed" : "pointer",
          padding: "2px 0"
        },
        children: "Hide controls"
      }
    )
  ] });
}
function ReviewFeedbackPanel({
  taskId,
  result,
  weaknesses,
  annLookup,
  interactive,
  draftsRef,
  attemptsThisCycle,
  attemptsTotal,
  maxAttempts = 7,
  onSubmittingChange,
  onSubmitted
}) {
  var _a;
  const [drafts, setDrafts] = reactExports.useState(
    () => new Map(draftsRef.current)
  );
  reactExports.useEffect(() => {
    draftsRef.current = drafts;
  }, [drafts, draftsRef]);
  const [submitting, setSubmitting] = reactExports.useState(null);
  const [error, setError] = reactExports.useState(null);
  const [pending, setPending] = reactExports.useState(null);
  const [tick, setTick] = reactExports.useState(0);
  const [activity, setActivity] = reactExports.useState({ count: 0, lastMsg: null, lastStage: null, lastTs: null });
  const [showRaw, setShowRaw] = reactExports.useState(false);
  const [forceExpand, setForceExpand] = reactExports.useState(
    interactive || weaknesses.length <= 3
  );
  const [expandedRows, setExpandedRows] = reactExports.useState(/* @__PURE__ */ new Set());
  const [justExpanded, setJustExpanded] = reactExports.useState(null);
  reactExports.useEffect(() => {
    setForceExpand(interactive || weaknesses.length <= 3);
    setError(null);
    setExpandedRows(/* @__PURE__ */ new Set());
    setJustExpanded(null);
  }, [result.id, weaknesses.length, interactive]);
  reactExports.useEffect(() => {
    if (pending && result.id !== pending.priorResultId) {
      setPending(null);
    }
  }, [result.id, pending]);
  reactExports.useEffect(() => {
    if (!pending) return;
    const id = window.setInterval(() => setTick((t) => t + 1), 1e3);
    return () => window.clearInterval(id);
  }, [pending]);
  reactExports.useEffect(() => {
    if (!pending) {
      setActivity({ count: 0, lastMsg: null, lastStage: null, lastTs: null });
      return;
    }
    setActivity({ count: 0, lastMsg: null, lastStage: null, lastTs: null });
    const handler = (data) => {
      const d = data ?? {};
      const tid = d.task_id;
      if (typeof tid === "string" && tid !== taskId) return;
      const msg = typeof d.message === "string" ? d.message : null;
      const stage = typeof d.stage === "string" ? d.stage : null;
      setActivity((prev) => ({
        count: prev.count + 1,
        lastMsg: msg ?? prev.lastMsg,
        lastStage: stage ?? prev.lastStage,
        lastTs: Date.now()
      }));
    };
    const unsub = eventBus.subscribe("worker.step", handler);
    return unsub;
  }, [pending, taskId]);
  const pushToast = useToastStore((s) => s.push);
  const keys2 = reactExports.useMemo(
    () => weaknesses.map((w, i) => weaknessKey(w, i)),
    [weaknesses]
  );
  const getDraft = reactExports.useCallback(
    (key) => drafts.get(key) ?? { status: "note", note: "" },
    [drafts]
  );
  const setDraft = reactExports.useCallback((key, patch2) => {
    setDrafts((prev) => {
      const next = new Map(prev);
      const cur = next.get(key) ?? { status: "note", note: "" };
      next.set(key, { ...cur, ...patch2 });
      return next;
    });
  }, []);
  const builtPayload = reactExports.useMemo(
    () => {
      const out = [];
      weaknesses.forEach((w, i) => {
        const key = keys2[i];
        const ns = drafts.get(key);
        if (!ns) return;
        if (ns.note.trim() === "" && ns.status === "note") return;
        const id = w && typeof w === "object" && typeof w.id === "string" ? w.id : void 0;
        out.push({ index: i, id, status: ns.status, note: ns.note.trim() });
      });
      return out;
    },
    [weaknesses, keys2, drafts]
  );
  const noteCount = builtPayload.length;
  const busy = submitting !== null;
  const allRowsExpanded = interactive && weaknesses.length > 0 && keys2.every((k) => expandedRows.has(k) || hasDraftContent(drafts.get(k)));
  const expandAllFeedback = reactExports.useCallback(() => {
    setExpandedRows(new Set(keys2));
  }, [keys2]);
  const collapseAllFeedback = reactExports.useCallback(() => {
    setExpandedRows(/* @__PURE__ */ new Set());
    setJustExpanded(null);
  }, []);
  const expandRow = reactExports.useCallback((key) => {
    setExpandedRows((prev) => {
      const next = new Set(prev);
      next.add(key);
      return next;
    });
    setJustExpanded(key);
  }, []);
  const collapseRow = reactExports.useCallback((key) => {
    setExpandedRows((prev) => {
      const next = new Set(prev);
      next.delete(key);
      return next;
    });
    setJustExpanded((cur) => cur === key ? null : cur);
  }, []);
  const submit = async (action) => {
    if (busy) return;
    if (action === "force" && !window.confirm("Skip the review gate and advance past this stage? Reviewer feedback will be ignored.")) {
      return;
    }
    if (action === "retry" && noteCount > 0 && !window.confirm(`Discard your ${noteCount} typed note${noteCount === 1 ? "" : "s"} and just loop again without operator guidance?`)) {
      return;
    }
    setSubmitting(action);
    setError(null);
    onSubmittingChange == null ? void 0 : onSubmittingChange(true);
    const ctrl = new AbortController();
    const timer = window.setTimeout(() => ctrl.abort("timeout"), 3e4);
    try {
      const payload = action === "retry" ? [] : builtPayload;
      await annotationsApi.resumeWithFeedback(taskId, {
        force_advance: action === "force" ? true : void 0,
        operator_weakness_notes: payload
      });
      setDrafts(/* @__PURE__ */ new Map());
      const titleByAction = {
        force: "Force-advanced past review gate",
        loop: "Sent for another loop",
        retry: "Re-running review loop"
      };
      pushToast({
        type: "info",
        title: titleByAction[action],
        message: action === "retry" ? "No operator notes — reviewer cycle restarts" : payload.length === 1 ? "1 note" : `${payload.length} notes`
      });
      setPending({ action, submittedAt: Date.now(), priorResultId: result.id });
      onSubmitted == null ? void 0 : onSubmitted();
    } catch (err) {
      const msg = err instanceof Error ? err.name === "AbortError" ? "Request timed out after 30s" : err.message : String(err);
      setError(msg);
      pushToast({ type: "error", title: "Resume failed", message: msg });
    } finally {
      window.clearTimeout(timer);
      setSubmitting(null);
      onSubmittingChange == null ? void 0 : onSubmittingChange(false);
    }
  };
  const verdict = (_a = result.payload) == null ? void 0 : _a.verdict;
  const verdictStr = typeof verdict === "string" ? verdict : null;
  const showActions = interactive && weaknesses.length > 0;
  const renderRow = forceExpand;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", "data-testid": "review-feedback-panel", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Reviewer feedback" }),
      interactive && weaknesses.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "span",
        {
          "data-testid": "needs-decision-indicator",
          role: "status",
          "aria-label": "Operator decision required",
          style: {
            marginLeft: 8,
            padding: "2px 8px",
            borderRadius: "var(--hv-radius-full, 999px)",
            background: "rgba(220,160,30,0.18)",
            border: "1px solid var(--hv-warn, #facc15)",
            color: "var(--hv-warn, #facc15)",
            fontSize: "var(--hv-fs-kicker, 11px)",
            fontWeight: 700,
            textTransform: "uppercase",
            letterSpacing: 0.4,
            display: "inline-flex",
            alignItems: "center",
            gap: 4
          },
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { "aria-hidden": true, style: { fontSize: 10 }, children: "●" }),
            "Needs operator decision"
          ]
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-panel-sub", style: { marginLeft: 8 }, children: [
        result.id,
        " ·",
        " ",
        result.created_at ? formatLocalDateTime(result.created_at) : "—",
        " · ",
        weaknesses.length,
        " issue",
        weaknesses.length === 1 ? "" : "s"
      ] }),
      typeof attemptsThisCycle === "number" && /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "span",
        {
          "data-testid": "review-loop-count",
          title: typeof attemptsTotal === "number" && attemptsTotal !== attemptsThisCycle ? `${attemptsThisCycle} attempts since last resume · ${attemptsTotal} total across mission · cap = ${maxAttempts}` : `Review attempts in this cycle. Cap = ${maxAttempts}. "Just loop more" resumes with ${maxAttempts} fresh attempts.`,
          style: {
            marginLeft: 8,
            padding: "2px 8px",
            borderRadius: "var(--hv-radius-full, 999px)",
            fontSize: "var(--hv-fs-kicker, 11px)",
            fontWeight: 700,
            background: attemptsThisCycle >= maxAttempts ? "rgba(239,91,91,0.18)" : attemptsThisCycle >= Math.max(1, maxAttempts - 2) ? "rgba(220,160,30,0.18)" : "rgba(110,118,129,0.18)",
            color: attemptsThisCycle >= maxAttempts ? "var(--hv-danger, #ef5b5b)" : attemptsThisCycle >= Math.max(1, maxAttempts - 2) ? "var(--hv-warn, #facc15)" : "var(--hv-fg-muted, #8c93a3)",
            border: "1px solid currentColor"
          },
          children: [
            "loop ",
            attemptsThisCycle,
            "/",
            maxAttempts,
            typeof attemptsTotal === "number" && attemptsTotal !== attemptsThisCycle && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { opacity: 0.7, fontWeight: 500 }, children: [
              " ",
              "(",
              attemptsTotal,
              " total)"
            ] })
          ]
        }
      ),
      verdictStr && /* @__PURE__ */ jsxRuntimeExports.jsx(
        "span",
        {
          style: {
            marginLeft: 8,
            padding: "1px 6px",
            borderRadius: "var(--hv-radius-sm, 4px)",
            fontSize: "var(--hv-fs-kicker, 11px)",
            fontWeight: 700,
            background: verdictStr.toUpperCase() === "REVISE" ? "rgba(239,91,91,0.15)" : "rgba(74,222,128,0.15)",
            color: verdictStr.toUpperCase() === "REVISE" ? "var(--hv-danger, #ef5b5b)" : "var(--hv-success, #4ade80)"
          },
          children: verdictStr
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-actions", style: { marginLeft: "auto" }, children: [
        interactive && weaknesses.length > 1 && /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            variant: "subtle",
            size: "sm",
            onClick: () => allRowsExpanded ? collapseAllFeedback() : expandAllFeedback(),
            "data-testid": "feedback-toggle-expand-all",
            title: "Show or hide stance + note controls for every weakness.",
            children: allRowsExpanded ? "Collapse feedback" : "Expand feedback"
          }
        ),
        weaknesses.length > 3 && /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            variant: "subtle",
            size: "sm",
            onClick: () => setForceExpand((v) => !v),
            "data-testid": "feedback-toggle-expand",
            children: forceExpand ? "Collapse all" : `Expand all (${weaknesses.length})`
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            variant: "subtle",
            size: "sm",
            onClick: () => setShowRaw((v) => !v),
            "data-testid": "feedback-toggle-raw",
            children: showRaw ? "Hide raw" : "Show raw"
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-body", children: [
      renderRow ? /* @__PURE__ */ jsxRuntimeExports.jsx(
        "ol",
        {
          style: {
            margin: 0,
            paddingLeft: 20,
            display: "flex",
            flexDirection: "column",
            gap: "var(--hv-space-md, 12px)"
          },
          children: weaknesses.map((w, i) => {
            const key = keys2[i];
            const draft = getDraft(key);
            const isOpen = expandedRows.has(key) || hasDraftContent(draft);
            const stanceBadge = interactive ? renderStanceBadge(draft, hasDraftContent(draft)) : null;
            return /* @__PURE__ */ jsxRuntimeExports.jsx(
              WeaknessItem,
              {
                weakness: w,
                index: i,
                annLookup,
                hideExtras: interactive,
                headerBadge: stanceBadge,
                actions: interactive ? isOpen ? /* @__PURE__ */ jsxRuntimeExports.jsx(
                  RowControls,
                  {
                    weaknessId: key,
                    draft,
                    busy,
                    autoFocus: justExpanded === key,
                    onChange: (patch2) => setDraft(key, patch2),
                    onCollapse: () => collapseRow(key)
                  }
                ) : /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    type: "button",
                    onClick: () => expandRow(key),
                    disabled: busy,
                    "data-testid": `feedback-add-${key}`,
                    style: {
                      marginTop: "var(--hv-space-sm, 8px)",
                      padding: "4px 10px",
                      fontSize: "var(--hv-fs-kicker, 11px)",
                      color: "var(--hv-fg-muted)",
                      background: "transparent",
                      border: "1px dashed var(--hv-border)",
                      borderRadius: "var(--hv-radius-sm, 4px)",
                      cursor: busy ? "not-allowed" : "pointer",
                      fontWeight: 600,
                      letterSpacing: 0.3
                    },
                    title: "Open stance + note controls for this weakness",
                    children: "+ Add feedback"
                  }
                ) : null
              },
              key
            );
          })
        }
      ) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { fontSize: "var(--hv-fs-xs, 12px)", color: "var(--hv-fg-muted)" }, children: [
        weaknesses.length,
        " issue",
        weaknesses.length === 1 ? "" : "s",
        " —",
        " ",
        "click ",
        /* @__PURE__ */ jsxRuntimeExports.jsx("em", { children: "Expand all" }),
        " to read the reviewer's notes."
      ] }),
      showRaw && /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { open: true, style: { marginTop: "var(--hv-space-md, 12px)" }, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "summary",
          {
            style: {
              cursor: "pointer",
              fontSize: "var(--hv-fs-kicker, 11px)",
              color: "var(--hv-fg-muted)"
            },
            children: "Raw payload"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "pre",
          {
            style: {
              margin: "6px 0 0",
              padding: 8,
              background: "var(--hv-bg)",
              border: "1px solid var(--hv-border)",
              borderRadius: "var(--hv-radius-sm, 4px)",
              fontSize: 11,
              lineHeight: 1.5,
              whiteSpace: "pre-wrap",
              wordBreak: "break-word",
              maxHeight: 320,
              overflow: "auto"
            },
            children: JSON.stringify(result.payload, null, 2)
          }
        )
      ] })
    ] }),
    pending && (() => {
      const elapsed = Math.max(0, Math.floor((Date.now() - pending.submittedAt) / 1e3));
      const labelByAction = {
        loop: "Feedback sent — review loop running",
        retry: "Re-running review loop",
        force: "Force-advance accepted — moving on"
      };
      return /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          "data-testid": "review-loop-inflight-banner",
          role: "status",
          "aria-live": "polite",
          style: {
            display: "flex",
            alignItems: "center",
            gap: 10,
            padding: "10px 14px",
            borderTop: "1px solid var(--hv-border)",
            background: "rgba(74,158,255,0.10)",
            color: "var(--hv-info, #4a9eff)",
            fontSize: "var(--hv-fs-xs, 12px)",
            fontWeight: 600
          },
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-workbeat hv-workbeat-inline", "aria-hidden": "true", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", {}),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", {})
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { flex: 1, minWidth: 0, display: "flex", flexDirection: "column", gap: 2 }, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                labelByAction[pending.action],
                " · waiting for next reviewer result (",
                elapsed,
                "s)"
              ] }),
              activity.count > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "span",
                {
                  "data-testid": "review-loop-activity-line",
                  style: {
                    fontSize: 11,
                    color: "var(--hv-fg-muted, #8c93a3)",
                    fontFamily: "var(--hv-font-mono, ui-monospace, monospace)",
                    overflow: "hidden",
                    textOverflow: "ellipsis",
                    whiteSpace: "nowrap"
                  },
                  children: [
                    activity.count,
                    " worker step",
                    activity.count === 1 ? "" : "s",
                    activity.lastStage ? ` · ${activity.lastStage}` : "",
                    activity.lastTs ? ` · ${Math.max(0, Math.floor((Date.now() - activity.lastTs) / 1e3))}s ago` : "",
                    activity.lastMsg ? ` · ${activity.lastMsg.slice(0, 80)}` : ""
                  ]
                }
              ) : /* @__PURE__ */ jsxRuntimeExports.jsx(
                "span",
                {
                  style: {
                    fontSize: 11,
                    color: "var(--hv-fg-muted, #8c93a3)",
                    fontStyle: "italic"
                  },
                  children: "no worker steps yet — sub-agent spawning…"
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "span",
              {
                style: {
                  fontFamily: "var(--hv-font-mono, ui-monospace, monospace)",
                  fontSize: 11,
                  opacity: 0.7
                },
                children: [
                  "prev ",
                  pending.priorResultId.slice(0, 8)
                ]
              }
            )
          ]
        }
      );
    })(),
    showActions && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        style: {
          display: "flex",
          alignItems: "center",
          gap: "var(--hv-space-sm, 8px)",
          padding: "10px 14px",
          borderTop: "1px solid var(--hv-border)",
          flexWrap: "wrap"
        },
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Btn,
            {
              variant: "primary",
              size: "sm",
              onClick: () => {
                void submit("loop");
              },
              disabled: busy || noteCount === 0,
              "data-testid": "feedback-loop-btn",
              children: submitting === "loop" ? "Sending…" : error ? `Retry (${noteCount})` : noteCount > 0 ? `Send feedback (${noteCount}) & loop` : "Send feedback & loop"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Btn,
            {
              variant: "default",
              size: "sm",
              onClick: () => {
                void submit("retry");
              },
              disabled: busy,
              "data-testid": "feedback-retry-btn",
              title: "Restart a full review cycle without operator notes (up to 7 attempts).",
              children: submitting === "retry" ? "Re-running…" : "Just loop more"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Btn,
            {
              variant: "subtle",
              size: "sm",
              onClick: () => {
                void submit("force");
              },
              disabled: busy,
              "data-testid": "feedback-force-advance-btn",
              children: submitting === "force" ? "Advancing…" : "Force advance (skip review)"
            }
          ),
          error && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "span",
            {
              role: "alert",
              style: {
                marginLeft: "auto",
                fontSize: "var(--hv-fs-xs, 12px)",
                color: "var(--hv-danger, #ef5b5b)"
              },
              children: error
            }
          ),
          !error && noteCount === 0 && !busy && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "span",
            {
              style: {
                marginLeft: "auto",
                fontSize: "var(--hv-fs-kicker, 11px)",
                color: "var(--hv-fg-muted)"
              },
              children: [
                "Click ",
                /* @__PURE__ */ jsxRuntimeExports.jsx("em", { children: "+ Add feedback" }),
                " on any item to guide the next loop, or skip the gate."
              ]
            }
          )
        ]
      }
    )
  ] });
}
function cleanStageNote(note) {
  if (!note) return null;
  return note.replace(/<\/?user_content>/g, "").trim();
}
function StageGlyph({ kind, status }) {
  const glyph = STAGE_GLYPH[kind] ?? "·";
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `sp-glyph sp-glyph-${kind} st-${status}`, "aria-hidden": "true", children: glyph });
}
function StageTile({ stage, focused, onClick }) {
  var _a, _b, _c, _d, _e, _f, _g, _h;
  const tone = STATUS_TONE[stage.status] ?? "neutral";
  const note = cleanStageNote((_a = stage.meta) == null ? void 0 : _a.note);
  const cls = [
    "sp-stage",
    `sp-${tone}`,
    `sp-kind-${stage.kind}`,
    stage.status === "running" ? "sp-running" : "",
    stage.addedAt === "runtime" ? "sp-runtime-added" : "",
    focused ? "sp-focused" : ""
  ].filter(Boolean).join(" ");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "button",
    {
      type: "button",
      className: cls,
      onClick,
      title: note ?? stageLabel(stage.name),
      "data-stage-id": stage.id,
      "data-stage-name": stage.name,
      "data-stage-status": stage.status,
      "data-stage-kind": stage.kind,
      "data-added-at": stage.addedAt,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "sp-stage-top", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(StageGlyph, { kind: stage.kind, status: stage.status }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-stage-name", children: stageLabel(stage.name) }),
          stage.addedAt === "runtime" && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-runtime-pill", title: "Added at runtime", children: "⊕" }),
          (((_b = stage.meta) == null ? void 0 : _b.runCount) ?? 0) > 1 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-loop-pill", title: `Run ${((_c = stage.meta) == null ? void 0 : _c.currentRun) ?? ((_d = stage.meta) == null ? void 0 : _d.runCount)} of ${(_e = stage.meta) == null ? void 0 : _e.runCount}`, children: [
            "loop ",
            ((_f = stage.meta) == null ? void 0 : _f.currentRun) ?? ((_g = stage.meta) == null ? void 0 : _g.runCount),
            "/",
            (_h = stage.meta) == null ? void 0 : _h.runCount
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "sp-stage-bot", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: `sp-status sp-status-${tone}`, children: [
            stage.status === "running" && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-pulse" }),
            stage.status
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-dur mono", children: fmtDur$1(stage.durationMs) })
        ] }),
        note && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "sp-stage-note", children: note })
      ]
    }
  );
}
function Sparkbar({ ticks, onPickIdx, focusedId }) {
  const [hover, setHover] = reactExports.useState(null);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "sp-spark", onMouseLeave: () => setHover(null), "data-testid": "sp-spark", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "sp-spark-row", children: ticks.map((t, i) => {
      const tone = STATUS_TONE[t.status] ?? "neutral";
      const cls = [
        "sp-tick",
        `sp-${tone}`,
        focusedId === t.id ? "on" : "",
        t.status === "running" ? "run" : ""
      ].filter(Boolean).join(" ");
      return /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "button",
          className: cls,
          onMouseEnter: () => setHover({ i, t }),
          onClick: () => onPickIdx(i),
          "aria-label": `Iteration ${t.iter ?? i + 1}: ${t.status}`,
          "data-tick-id": t.id
        },
        t.id
      );
    }) }),
    hover && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "sp-spark-pop",
        style: {
          left: `calc(${hover.i / Math.max(ticks.length - 1, 1) * 100}% )`
        },
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono", children: [
            "#",
            hover.t.iter ?? hover.i + 1
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: hover.t.status }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono sp-pop-dur", children: fmtDur$1(hover.t.durationMs) })
        ]
      }
    )
  ] });
}
function LoopBlock({ loop, expanded, onToggle, focusedId, onPickIter }) {
  const { iterations, ticks, doneCount, warnCount, failedCount, running, name: name2, status } = loop;
  const total = iterations.length;
  const tone = status === "failed" ? "danger" : status === "running" ? "brand" : "success";
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: `sp-loop sp-${tone}${expanded ? " open" : ""}`,
      "data-loop-id": loop.id,
      "data-iter-count": total,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { type: "button", className: "sp-loop-head", onClick: onToggle, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-loop-icon", "aria-hidden": "true", children: "⟳" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "sp-loop-text", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "sp-loop-title", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-stage-name", children: stageLabel(name2) }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-loop-x mono", children: [
                "×",
                total
              ] }),
              running && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-pulse" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "sp-loop-sub", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono sp-ok", children: [
                doneCount,
                "✓"
              ] }),
              warnCount > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono sp-wn", children: [
                "·  ",
                warnCount,
                "⚠"
              ] }),
              failedCount > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono sp-fl", children: [
                "·  ",
                failedCount,
                "✗"
              ] })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-loop-chev", children: expanded ? "−" : "+" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Sparkbar,
          {
            ticks,
            focusedId,
            onPickIdx: (idx) => onPickIter(iterations[idx])
          }
        ),
        expanded && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "sp-loop-body", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "sp-loop-list", children: iterations.map((it) => {
          var _a;
          return /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              type: "button",
              className: `sp-iter sp-${STATUS_TONE[it.status] ?? "neutral"} ${focusedId === it.id ? "sp-focused" : ""}`,
              onClick: () => onPickIter(it),
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-iter-n mono", children: [
                  "#",
                  it.iteration ?? ""
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", {}),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-iter-name", children: it.status }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono sp-iter-dur", children: fmtDur$1(it.durationMs) }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-iter-note", children: ((_a = it.meta) == null ? void 0 : _a.note) ?? "" })
              ]
            },
            it.id
          );
        }) }) })
      ]
    }
  );
}
function PlannerPlaceholder() {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "sp-placeholder", "data-testid": "sp-placeholder", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "sp-placeholder-bar", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", {}),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", {}),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", {})
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "sp-placeholder-label", children: "stages emerge after planning" })
  ] });
}
function StagePipeline({
  pipeline,
  focusedId,
  onPick,
  view: viewProp,
  onChangeView,
  onTracerEmit,
  className,
  style
}) {
  var _a;
  const [internalView, setInternalView] = reactExports.useState("horizontal");
  const view = viewProp ?? internalView;
  const setView = (v) => {
    if (onChangeView) onChangeView(v);
    else setInternalView(v);
  };
  const items = reactExports.useMemo(() => collapseStages(pipeline.stages), [pipeline.stages]);
  const planning = ((_a = pipeline.planner) == null ? void 0 : _a.status) === "running";
  const totals = reactExports.useMemo(() => {
    const total = pipeline.stages.length || 1;
    return {
      total,
      done: pipeline.stages.filter((s) => s.status === "done").length,
      running: pipeline.stages.filter((s) => s.status === "running").length,
      failed: pipeline.stages.filter((s) => s.status === "failed").length,
      pending: pipeline.stages.filter((s) => s.status === "pending").length,
      awaitingApproval: pipeline.stages.filter((s) => s.status === "awaiting-approval").length
    };
  }, [pipeline.stages]);
  const [openLoops, setOpenLoops] = reactExports.useState({});
  const lastEmittedTaskIdRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    if (pipeline.stages.length === 0) return;
    if (lastEmittedTaskIdRef.current === pipeline.task_id) return;
    lastEmittedTaskIdRef.current = pipeline.task_id;
    tracer("CAP-29", `ok task=${pipeline.task_id} stages=${pipeline.stages.length}`);
    onTracerEmit == null ? void 0 : onTracerEmit();
  }, [pipeline.stages.length, pipeline.task_id, onTracerEmit]);
  const lastPlannedTaskIdRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    if (!pipeline.planned || pipeline.planned_pipeline_id == null) return;
    if (lastPlannedTaskIdRef.current === pipeline.task_id) return;
    lastPlannedTaskIdRef.current = pipeline.task_id;
    tracer("CAP-36", `ok pipeline=${pipeline.planned_pipeline_id} task=${pipeline.task_id}`);
  }, [pipeline.planned, pipeline.planned_pipeline_id, pipeline.task_id]);
  if (pipeline.stages.length === 0) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: `sp-wrap ${className ?? ""}`,
        style,
        "data-testid": "stage-pipeline",
        "data-empty": "true",
        "data-task-id": pipeline.task_id,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "sp-empty", children: "No pipeline data yet — task has not emitted structured steps." })
      }
    );
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: `sp-wrap ${className ?? ""}`,
      style,
      "data-testid": "stage-pipeline",
      "data-task-id": pipeline.task_id,
      "data-view": view,
      "data-stage-count": pipeline.stages.length,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "sp-toolbar", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "sp-toolbar-l", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-tot mono", "data-testid": "sp-tot", children: [
              totals.done,
              "/",
              totals.total
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-tot-bar", "aria-hidden": "true", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "span",
                {
                  className: "sp-tot-done",
                  style: { width: `${totals.done / totals.total * 100}%` }
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "span",
                {
                  className: "sp-tot-run",
                  style: { width: `${totals.running / totals.total * 100}%` }
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "span",
                {
                  className: "sp-tot-fail",
                  style: { width: `${totals.failed / totals.total * 100}%` }
                }
              )
            ] }),
            totals.running > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-status sp-status-brand", "data-testid": "sp-badge-running", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-pulse" }),
              " running"
            ] }),
            totals.failed > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-status sp-status-danger", "data-testid": "sp-badge-failed", children: [
              totals.failed,
              " failed"
            ] }),
            totals.awaitingApproval > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-status sp-status-warn", "data-testid": "sp-badge-awaiting", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-pulse" }),
              " awaiting approval"
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "sp-toolbar-r", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "sp-view-toggle", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                type: "button",
                className: view === "horizontal" ? "on" : "",
                onClick: () => setView("horizontal"),
                children: "━ flow"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                type: "button",
                className: view === "vertical" ? "on" : "",
                onClick: () => setView("vertical"),
                children: "┃ timeline"
              }
            )
          ] }) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "sp-flow", "data-testid": "sp-flow", children: items.map((item, idx) => {
          if (item.type === "loop") {
            const isLast2 = idx === items.length - 1;
            return /* @__PURE__ */ jsxRuntimeExports.jsxs(FragmentJoin, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                LoopBlock,
                {
                  loop: item,
                  expanded: !!openLoops[item.id],
                  onToggle: () => setOpenLoops((o) => ({ ...o, [item.id]: !o[item.id] })),
                  focusedId,
                  onPickIter: (s2) => onPick == null ? void 0 : onPick(s2)
                }
              ),
              !isLast2 && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-conn" })
            ] }, item.id);
          }
          const s = item.stage;
          const isLast = idx === items.length - 1;
          const showPlaceholder = planning && s.kind === "plan" && isLast;
          return /* @__PURE__ */ jsxRuntimeExports.jsxs(FragmentJoin, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              StageTile,
              {
                stage: s,
                focused: focusedId === s.id,
                onClick: () => onPick == null ? void 0 : onPick(s)
              }
            ),
            showPlaceholder && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-conn dashed" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(PlannerPlaceholder, {})
            ] }),
            !showPlaceholder && !isLast && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "sp-conn" })
          ] }, s.id);
        }) })
      ]
    }
  );
}
function FragmentJoin({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(jsxRuntimeExports.Fragment, { children });
}
function TaskWarningsBadges({ warnings }) {
  reactExports.useEffect(() => {
    for (const w of warnings) {
      tracer("ANN-22-UI", `ok mounted warning=${w}`);
    }
  }, [warnings.join("|")]);
  if (!warnings || warnings.length === 0) return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      style: { display: "flex", gap: 6, padding: "6px 12px", flexWrap: "wrap" },
      "data-testid": "task-warnings",
      children: warnings.map((w) => /* @__PURE__ */ jsxRuntimeExports.jsx(
        "span",
        {
          "data-testid": `task-warning-${w}`,
          style: {
            background: "var(--hv-danger-bg, #5a1a1a)",
            color: "var(--hv-danger-fg, #ffd0d0)",
            border: "1px solid var(--hv-danger-border, #a33)",
            borderRadius: 4,
            padding: "2px 8px",
            fontSize: 11,
            textTransform: "uppercase",
            letterSpacing: 0.5
          },
          children: w.replace(/_/g, " ")
        },
        w
      ))
    }
  );
}
const DEFAULT_STAGE_ORDER = [
  "triage",
  "plan",
  "review-plan",
  "impl",
  "review-impl",
  "commit",
  "push_gate"
];
const STAGE_TRACER = {
  triage: "ANN-09 spawn",
  plan: "ANN-10 advance",
  "review-plan": "ANN-10 advance",
  impl: "ANN-09 spawn",
  "review-impl": "ANN-10 advance",
  commit: "ANN-14 commit",
  push_gate: "ANN-10 advance"
};
function statusTone(status) {
  switch (status) {
    case "done":
      return "success";
    case "running":
    case "active":
      return "brand";
    case "failed":
    case "error":
      return "danger";
    case "parked":
    case "awaiting-approval":
    case "warn":
      return "warn";
    case "pending":
    case void 0:
    case "":
    default:
      return "neutral";
  }
}
function toneColor(tone) {
  switch (tone) {
    case "success":
      return "var(--hv-success, #4ade80)";
    case "brand":
      return "var(--hv-honey-5, #ffb84d)";
    case "danger":
      return "var(--hv-danger, #ef5b5b)";
    case "warn":
      return "var(--hv-warn, #facc15)";
    case "neutral":
    default:
      return "var(--hv-fg-muted, #8c93a3)";
  }
}
function readPipeline(task) {
  const payload = task.payload ?? {};
  const pipeline = payload.pipeline ?? {};
  const stages = Array.isArray(pipeline.stages) ? pipeline.stages : [];
  return { stages, currentStage: pipeline.current_stage ?? null };
}
function fmtTime(iso) {
  if (!iso) return "—";
  try {
    return formatLocalTime(iso);
  } catch {
    return "—";
  }
}
function fmtDur(startIso, endIso) {
  if (!startIso) return "";
  const start = Date.parse(startIso);
  if (Number.isNaN(start)) return "";
  const end = endIso ? Date.parse(endIso) : Date.now();
  if (Number.isNaN(end)) return "";
  const ms = Math.max(0, end - start);
  if (ms < 1e3) return `${ms}ms`;
  if (ms < 6e4) return `${(ms / 1e3).toFixed(1)}s`;
  const m = Math.floor(ms / 6e4);
  const s = Math.floor(ms % 6e4 / 1e3);
  return `${m}m ${String(s).padStart(2, "0")}s`;
}
function excerpt(text2, n = 120) {
  if (!text2) return "";
  const t = text2.trim();
  return t.length > n ? `${t.slice(0, n - 1)}…` : t;
}
function buildStageVms(stages, currentStage, steps, taskId) {
  var _a;
  const padded = stages.length > 0 ? stages : DEFAULT_STAGE_ORDER.map((n) => ({ name: n, status: "pending" }));
  const stepsByStage = {};
  for (const step of steps) {
    if (step.task_id && step.task_id !== taskId) continue;
    (stepsByStage[_a = step.stage] ?? (stepsByStage[_a] = [])).push(step);
  }
  return padded.map((s) => {
    const isCurrent = s.name === currentStage;
    const rawStatus = s.status ?? "pending";
    const effectiveStatus = (rawStatus === "running" || rawStatus === "active") && !isCurrent ? "completed" : rawStatus;
    const tone = statusTone(effectiveStatus);
    const stageSteps = stepsByStage[s.name] ?? [];
    const digestRow = [...stageSteps].reverse().find(
      (r) => r.metadata && r.metadata.watcher_digest === true
    ) ?? stageSteps[stageSteps.length - 1];
    const spawnRow = [...stageSteps].reverse().find(
      (r) => r.metadata && typeof r.metadata.sub_worker_id === "string"
    );
    const subWorker = (spawnRow == null ? void 0 : spawnRow.metadata) ? spawnRow.metadata.sub_worker_id : void 0;
    const subSession = (spawnRow == null ? void 0 : spawnRow.metadata) ? spawnRow.metadata.session : void 0;
    return {
      name: s.name,
      status: effectiveStatus,
      tone,
      startedAt: s.started_at,
      endedAt: s.ended_at,
      durationLabel: s.started_at && s.ended_at ? fmtDur(s.started_at, s.ended_at) : s.started_at && effectiveStatus === "running" && isCurrent ? fmtDur(s.started_at, void 0) : "",
      isCurrent,
      digest: digestRow ? excerpt(digestRow.message) : void 0,
      subWorker,
      subSession,
      tracer: STAGE_TRACER[s.name] ?? ""
    };
  });
}
function StageCard({ vm }) {
  const accent = toneColor(vm.tone);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      "data-testid": `ann-stage-card-${vm.name}`,
      "data-stage-name": vm.name,
      "data-stage-status": vm.status,
      "data-stage-current": vm.isCurrent ? "true" : "false",
      style: {
        flex: "1 1 220px",
        minWidth: 220,
        maxWidth: 320,
        background: "var(--hv-bg-raised, #1a1d24)",
        border: vm.isCurrent ? `1px solid ${accent}` : "1px solid var(--hv-border, #2a2f3a)",
        borderLeft: `3px solid ${accent}`,
        borderRadius: 6,
        padding: "10px 12px",
        display: "flex",
        flexDirection: "column",
        gap: 6,
        boxShadow: vm.isCurrent ? `0 0 0 1px ${accent} inset` : "none"
      },
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            style: {
              display: "flex",
              alignItems: "center",
              justifyContent: "space-between",
              gap: 8
            },
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "span",
                {
                  className: "mono",
                  style: {
                    fontSize: 12,
                    fontWeight: 600,
                    color: "var(--hv-fg, #e8eaf0)",
                    letterSpacing: 0.2
                  },
                  children: vm.name
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "span",
                {
                  "data-testid": `ann-stage-status-${vm.name}`,
                  style: {
                    fontSize: 10,
                    fontFamily: "var(--hv-font-mono, ui-monospace, monospace)",
                    textTransform: "uppercase",
                    letterSpacing: 0.5,
                    padding: "1px 6px",
                    borderRadius: 10,
                    border: `1px solid ${accent}`,
                    color: accent,
                    background: "transparent",
                    whiteSpace: "nowrap"
                  },
                  children: vm.status
                }
              )
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            style: {
              display: "flex",
              gap: 8,
              fontSize: 11,
              color: "var(--hv-fg-muted, #8c93a3)",
              fontFamily: "var(--hv-font-mono, ui-monospace, monospace)"
            },
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { title: "Started at", children: fmtTime(vm.startedAt) }),
              vm.durationLabel && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { color: "var(--hv-fg-subtle, #6b7280)" }, children: [
                "· ",
                vm.durationLabel
              ] })
            ]
          }
        ),
        (vm.subWorker || vm.subSession) && /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            style: {
              fontSize: 11,
              color: "var(--hv-fg-muted, #8c93a3)",
              fontFamily: "var(--hv-font-mono, ui-monospace, monospace)",
              display: "flex",
              gap: 6,
              flexWrap: "wrap"
            },
            children: [
              vm.subWorker && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { "data-sub-worker": vm.subWorker, children: [
                "w:",
                vm.subWorker
              ] }),
              vm.subSession && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { "data-sub-session": vm.subSession, title: vm.subSession, children: [
                "⌥ ",
                vm.subSession
              ] })
            ]
          }
        ),
        vm.digest && /* @__PURE__ */ jsxRuntimeExports.jsx(
          "div",
          {
            "data-testid": `ann-stage-digest-${vm.name}`,
            style: {
              fontSize: 11,
              lineHeight: 1.4,
              color: "var(--hv-fg-subtle, #b0b6c2)",
              background: "var(--hv-bg, #0f1115)",
              border: "1px solid var(--hv-border, #2a2f3a)",
              borderRadius: 4,
              padding: "4px 6px",
              fontFamily: "var(--hv-font-mono, ui-monospace, monospace)",
              wordBreak: "break-word"
            },
            children: vm.digest
          }
        ),
        vm.tracer && /* @__PURE__ */ jsxRuntimeExports.jsx(
          "div",
          {
            style: {
              fontSize: 9,
              color: "var(--hv-fg-subtle, #6b7280)",
              fontFamily: "var(--hv-font-mono, ui-monospace, monospace)",
              textTransform: "uppercase",
              letterSpacing: 0.5,
              marginTop: "auto"
            },
            children: vm.tracer
          }
        )
      ]
    }
  );
}
function AnnotationStagePanel({ task }) {
  const [archive, setArchive] = reactExports.useState(null);
  const [steps, setSteps] = reactExports.useState([]);
  const [error, setError] = reactExports.useState(null);
  const [pushGateLoading, setPushGateLoading] = reactExports.useState(false);
  reactExports.useEffect(() => {
    let cancelled = false;
    annotationsApi.archive(task.id).then((rows) => {
      if (cancelled) return;
      setArchive(rows);
      tracer("ANN-07", `ok archive task=${task.id} n=${rows.length}`);
    }).catch((err) => {
      if (cancelled) return;
      setError(err instanceof Error ? err.message : "archive load failed");
    });
    return () => {
      cancelled = true;
    };
  }, [task.id]);
  reactExports.useEffect(() => {
    if (archive && archive.length > 0) {
      tracer("ANN-16", `ok panel task=${task.id} rows=${archive.length}`);
    }
  }, [archive, task.id]);
  const workerId = task.worker_id ?? null;
  const refetchStepsTimerRef = reactExports.useRef(null);
  const refetchStepsCancelledRef = reactExports.useRef(false);
  const refetchSteps = reactExports.useCallback(async () => {
    if (!workerId) return;
    try {
      const res = await fetch(
        `/api/workers/${encodeURIComponent(workerId)}/steps`,
        { credentials: "same-origin", headers: _authHeaders() }
      );
      if (res.status === 401) {
        await _handle401();
        return;
      }
      if (!res.ok) return;
      const body = await res.json();
      const rows = Array.isArray(body) ? body : body.steps ?? [];
      if (!refetchStepsCancelledRef.current) setSteps(rows);
    } catch {
    }
  }, [workerId]);
  const scheduleRefetchSteps = reactExports.useCallback(() => {
    if (refetchStepsTimerRef.current) clearTimeout(refetchStepsTimerRef.current);
    refetchStepsTimerRef.current = setTimeout(() => {
      void refetchSteps();
    }, 250);
  }, [refetchSteps]);
  reactExports.useEffect(() => {
    if (!workerId) {
      setSteps([]);
      return;
    }
    refetchStepsCancelledRef.current = false;
    void refetchSteps();
    const handler = (data) => {
      var _a;
      const tid = (_a = data ?? void 0) == null ? void 0 : _a.task_id;
      if (tid && tid !== task.id) return;
      scheduleRefetchSteps();
    };
    const unsubStep = eventBus.subscribe("worker.step", handler);
    return () => {
      refetchStepsCancelledRef.current = true;
      if (refetchStepsTimerRef.current) clearTimeout(refetchStepsTimerRef.current);
      unsubStep();
    };
  }, [workerId, task.id, refetchSteps, scheduleRefetchSteps]);
  reactExports.useEffect(() => {
    const handler = (data) => {
      var _a, _b;
      const tid = (_a = data ?? void 0) == null ? void 0 : _a.task_id;
      const id = (_b = data ?? void 0) == null ? void 0 : _b.id;
      if (tid !== task.id && id !== task.id) return;
      scheduleRefetchSteps();
      tracer("ANN-49", `ok stagepanel_status_refresh task=${task.id}`);
    };
    const unsubA = eventBus.subscribe("task.status", handler);
    const unsubB = eventBus.subscribe("pipeline.stage_changed", handler);
    return () => {
      unsubA();
      unsubB();
    };
  }, [task.id, scheduleRefetchSteps]);
  reactExports.useEffect(() => {
    const status = task.status;
    if (status === "done" || status === "failed" || status === "cancelled") {
      tracer("ANN-50", `ok stagepanel_terminal task=${task.id} status=${status}`);
    }
  }, [task]);
  const pushToast = useToastStore((s) => s.push);
  const handleApprovePushGate = reactExports.useCallback(async () => {
    if (pushGateLoading) return;
    setPushGateLoading(true);
    try {
      const res = await authedFetch(
        `/api/tasks/${task.id}/resume?role=conductor`,
        { method: "POST" }
      );
      if (!res.ok) {
        const body = await res.text().catch(() => res.statusText);
        pushToast({ type: "error", title: "push_gate approval failed", message: body });
        return;
      }
      api.tasks.get(task.id).then((fresh) => {
        useTaskStore.getState().setTask(task.id, fresh);
      }).catch(() => {
      });
      tracer("ANN-push-gate", `ok approved task=${task.id}`);
    } catch (err) {
      pushToast({
        type: "error",
        title: "push_gate approval failed",
        message: err instanceof Error ? err.message : String(err)
      });
    } finally {
      setPushGateLoading(false);
    }
  }, [task.id, pushGateLoading, pushToast]);
  const { stages, currentStage } = reactExports.useMemo(() => readPipeline(task), [task]);
  const stageVms = reactExports.useMemo(
    () => buildStageVms(stages, currentStage, steps, task.id),
    [stages, currentStage, steps, task.id]
  );
  const visibleVms = reactExports.useMemo(() => {
    if (stageVms.length === 0) return stageVms;
    const byCurrent = currentStage ? stageVms.find((vm) => vm.name === currentStage) : void 0;
    const fallback = stageVms.find((vm) => vm.status === "running" || vm.status === "active") ?? stageVms.find((vm) => vm.status === "pending") ?? stageVms[stageVms.length - 1];
    const pick = byCurrent ?? fallback;
    return pick ? [pick] : [];
  }, [stageVms, currentStage]);
  const ann35FiredRef = reactExports.useRef(false);
  reactExports.useEffect(() => {
    if (!ann35FiredRef.current && archive && archive.length > 0 && currentStage) {
      ann35FiredRef.current = true;
      tracer("ANN-35", `ok pipeline_tab task=${task.id} stage=${currentStage} rows=${archive.length}`);
    }
  }, [archive, currentStage, task.id]);
  const ann38FiredRef = reactExports.useRef(false);
  reactExports.useEffect(() => {
    if (!ann38FiredRef.current && stageVms.length > 0) {
      ann38FiredRef.current = true;
      tracer("ANN-38", `ok cards task=${task.id} n=${stageVms.length}`);
    }
  }, [stageVms.length, task.id]);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "ann-mission-panel",
      "data-testid": "annotation-stage-panel",
      "data-task-type": "annotation_mission",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ann-mission-head", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ann-mission-title", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ann-mission-kicker", children: "Annotation mission" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: currentStage ? currentStage.replace(/[_-]+/g, " ") : "Preparing stage" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ann-mission-meta", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
              "Stage ",
              visibleVms[0] ? stageVms.findIndex((s) => s.name === visibleVms[0].name) + 1 : 0,
              "/",
              stageVms.length
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: archive ? `${archive.length} annotations` : "loading annotations" })
          ] })
        ] }),
        error && /* @__PURE__ */ jsxRuntimeExports.jsx(
          "div",
          {
            style: {
              color: "var(--hv-danger-fg, #ffd0d0)",
              fontSize: 12,
              marginBottom: 8
            },
            children: error
          }
        ),
        (() => {
          const p = task.payload ?? {};
          const blockedOn = p.blocked_on;
          const commitSha = typeof p.commit_sha === "string" ? p.commit_sha : null;
          if (blockedOn !== "push_gate") return null;
          return /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              "data-testid": "push-gate-approval-banner",
              role: "region",
              "aria-label": "push_gate approval required",
              style: {
                display: "flex",
                alignItems: "center",
                gap: 12,
                padding: "10px 12px",
                marginBottom: 10,
                background: "var(--hv-warn-bg, rgba(250,204,21,0.08))",
                border: "1px solid var(--hv-warn, #facc15)",
                borderRadius: 6,
                fontSize: 12,
                color: "var(--hv-warn, #facc15)",
                flexWrap: "wrap"
              },
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { flex: 1, minWidth: 0 }, children: [
                  "Mission parked at ",
                  /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "push_gate" }),
                  " — awaiting operator approval before push.",
                  commitSha && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                    " ",
                    "Commit:",
                    " ",
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "code",
                      {
                        "data-testid": "push-gate-commit-sha",
                        style: {
                          fontFamily: "var(--hv-font-mono, ui-monospace, monospace)",
                          fontSize: 11,
                          color: "var(--hv-fg, #e8eaf0)"
                        },
                        children: commitSha
                      }
                    )
                  ] })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs(
                  "button",
                  {
                    "data-testid": "push-gate-approve-btn",
                    disabled: pushGateLoading,
                    "aria-disabled": pushGateLoading,
                    onClick: () => {
                      void handleApprovePushGate();
                    },
                    style: {
                      display: "inline-flex",
                      alignItems: "center",
                      gap: 6,
                      padding: "5px 14px",
                      background: pushGateLoading ? "var(--hv-border, #2a2f3a)" : "var(--hv-warn, #facc15)",
                      color: pushGateLoading ? "var(--hv-fg-muted, #8c93a3)" : "var(--hv-bg, #0f1115)",
                      border: "none",
                      borderRadius: 4,
                      fontSize: 12,
                      fontWeight: 600,
                      cursor: pushGateLoading ? "not-allowed" : "pointer",
                      whiteSpace: "nowrap",
                      opacity: pushGateLoading ? 0.6 : 1
                    },
                    children: [
                      pushGateLoading && /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "span",
                        {
                          "aria-label": "loading",
                          style: {
                            display: "inline-block",
                            width: 10,
                            height: 10,
                            border: "2px solid currentColor",
                            borderTopColor: "transparent",
                            borderRadius: "50%",
                            animation: "spin 0.6s linear infinite"
                          }
                        }
                      ),
                      "Approve & continue (push_gate)"
                    ]
                  }
                )
              ]
            }
          );
        })(),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "div",
          {
            className: "ann-stage-cards",
            "data-testid": "ann-stage-cards",
            children: visibleVms.map((vm) => /* @__PURE__ */ jsxRuntimeExports.jsx(StageCard, { vm }, vm.name))
          }
        ),
        archive && archive.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "summary",
            {
              style: {
                cursor: "pointer",
                fontSize: 11,
                color: "var(--hv-fg-muted, #8c93a3)",
                padding: "4px 0"
              },
              children: [
                "Snapshot · ",
                archive.length,
                " annotations"
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              style: {
                display: "flex",
                flexDirection: "column",
                gap: 4,
                marginTop: 6
              },
              children: archive.map((row) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "div",
                {
                  "data-ann-id": row.id,
                  style: {
                    fontSize: 12,
                    fontFamily: "var(--hv-font-mono, ui-monospace, monospace)",
                    padding: "4px 6px",
                    borderBottom: "1px solid var(--hv-border, #2a2f3a)"
                  },
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "span",
                      {
                        style: {
                          marginRight: 6,
                          fontSize: 10,
                          color: row.severity === "bug" ? "var(--hv-danger-fg, #ffd0d0)" : "var(--hv-fg-muted, #8c93a3)"
                        },
                        children: [
                          "[",
                          row.severity,
                          "]"
                        ]
                      }
                    ),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: row.body }),
                    row.target_ref && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "span",
                      {
                        style: {
                          marginLeft: 8,
                          color: "var(--hv-fg-subtle, #6b7280)"
                        },
                        children: [
                          "→ ",
                          row.target_ref,
                          row.target_line ? `:${row.target_line}` : ""
                        ]
                      }
                    )
                  ]
                },
                row.id
              ))
            }
          )
        ] })
      ]
    }
  );
}
const VALID_TABS = ["review", "pipeline", "artifacts", "acceptance", "deps"];
function readTabFromHash() {
  if (typeof window === "undefined") return null;
  const m = window.location.hash.match(/[#&]tab=([a-z]+)/);
  if (!m) return null;
  const t = m[1];
  return VALID_TABS.includes(t) ? t : null;
}
function SkeletonRow() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      style: {
        height: 16,
        background: "var(--hv-border)",
        borderRadius: 4,
        marginBottom: 8,
        opacity: 0.5
      }
    }
  );
}
function resultSummary(result) {
  const payload = result.payload;
  if (payload && typeof payload.verdict === "string") {
    const verdict = payload.verdict.toUpperCase();
    const weaknesses = Array.isArray(payload.weaknesses) ? payload.weaknesses : [];
    const groups = Array.isArray(payload.groups) ? payload.groups : [];
    const plan = payload.plan && typeof payload.plan === "object" ? Object.keys(payload.plan).length : 0;
    if (weaknesses.length > 0) {
      const first = weaknesses[0];
      const issue = first && typeof first === "object" ? first.issue ?? first.fix : null;
      return `${verdict}: ${weaknesses.length} issue${weaknesses.length === 1 ? "" : "s"}${typeof issue === "string" ? ` - ${issue}` : ""}`;
    }
    if (groups.length > 0) {
      return `${verdict}: ${groups.length} fix group${groups.length === 1 ? "" : "s"}`;
    }
    if (plan > 0) {
      return `${verdict}: plan covers ${plan} annotation${plan === 1 ? "" : "s"}`;
    }
    return `${verdict}: reviewer result`;
  }
  const direct = (payload == null ? void 0 : payload.result) ?? (payload == null ? void 0 : payload.summary) ?? (payload == null ? void 0 : payload.message);
  if (typeof direct === "string" && direct.trim()) return direct;
  if (typeof (payload == null ? void 0 : payload.text) === "string" && payload.text.trim()) return payload.text;
  if (payload) return JSON.stringify(payload, null, 2);
  return "No result payload.";
}
function formatOutputKind(kind) {
  const normalized = kind.trim().toLowerCase().replace(/[-\s]+/g, "_");
  if (normalized === "result") return "RESULT";
  if (normalized === "stage_report" || normalized === "stage") return "STAGE REPORT";
  if (normalized === "file" || normalized === "file_event") return "FILE";
  if (normalized === "artifact" || normalized.endsWith("_produced")) return "ARTIFACT";
  return normalized.replace(/_/g, " ").toUpperCase();
}
function prettyStage(stage) {
  return stageLabel(stage);
}
function relativeActivityTime(ts, now) {
  if (!ts) return "no activity yet";
  const sec = Math.max(0, Math.floor((now - ts) / 1e3));
  if (sec < 60) return `${sec}s ago`;
  const min2 = Math.floor(sec / 60);
  const remSec = sec % 60;
  if (min2 < 60) return `${min2}m ${remSec}s ago`;
  const hr = Math.floor(min2 / 60);
  const remMin = min2 % 60;
  return `${hr}h ${remMin}m ${remSec}s ago`;
}
function parseHiveTimestamp(value) {
  if (!value) return null;
  const normalized = value.includes("T") ? value : `${value.replace(" ", "T")}Z`;
  const ms = Date.parse(normalized);
  return Number.isFinite(ms) ? ms : null;
}
function logLevelClass(level) {
  const l = (level ?? "info").toLowerCase();
  if (l === "warning") return "warn";
  if (l === "critical") return "error";
  if (["tool", "warn", "error", "debug", "info"].includes(l)) return l;
  return "info";
}
function upsertById(prev, next, getId) {
  const nextId = getId(next);
  if (nextId == null) return [next, ...prev ?? []];
  const existing = prev ?? [];
  return [next, ...existing.filter((item) => getId(item) !== nextId)];
}
function buildStageRuns(steps) {
  const byStage = /* @__PURE__ */ new Map();
  const sorted = [...steps ?? []].sort((a, b) => a.seq - b.seq);
  let current = null;
  const flush = () => {
    if (!current) return;
    const arr = byStage.get(current.stage) ?? [];
    arr.push({ ...current, id: "", index: 0, total: 0 });
    byStage.set(current.stage, arr);
    current = null;
  };
  for (const step of sorted) {
    if (!step.stage) continue;
    if (!current || current.stage !== step.stage) {
      flush();
      const ts = parseHiveTimestamp(step.ts);
      current = {
        stage: step.stage,
        startSeq: step.seq,
        endSeq: step.seq,
        startMs: ts,
        endMs: ts,
        status: step.status,
        steps: [step]
      };
    } else {
      current.endSeq = step.seq;
      current.endMs = parseHiveTimestamp(step.ts) ?? current.endMs;
      current.status = step.status ?? current.status;
      current.steps.push(step);
    }
  }
  flush();
  for (const [stage, runs] of byStage) {
    const total = runs.length;
    byStage.set(stage, runs.map((run, i) => ({
      ...run,
      id: `${stage}-${run.startSeq}-${run.endSeq}`,
      index: i + 1,
      total
    })));
  }
  return byStage;
}
function payloadStage(payload) {
  if (!payload) return null;
  for (const key of ["stage", "current_stage", "review_stage"]) {
    const value = payload[key];
    if (typeof value === "string") return value;
  }
  const nested = payload.pipeline;
  if (nested && typeof nested === "object") {
    const value = nested.current_stage;
    if (typeof value === "string") return value;
  }
  return null;
}
function resultLooksLikeStage(result, stage) {
  const payload = result.payload;
  const explicit = payloadStage(payload);
  if (explicit === stage) return true;
  if (!payload) return false;
  if (stage === "plan_tracers") return Boolean(payload.plan || payload.groups);
  if (stage === "review_plan" || stage === "review_impl") {
    return Array.isArray(payload.weaknesses) || typeof payload.verdict === "string";
  }
  return false;
}
function withinRun(ts, run) {
  if (!run) return true;
  const ms = parseHiveTimestamp(ts);
  if (ms == null || run.startMs == null) return true;
  if (ms < run.startMs - 1e3) return false;
  if (run.endMs != null && ms > run.endMs + 1e3) return false;
  return true;
}
function splitEvidenceMessage(message) {
  const parts = message.split(" - ");
  if (parts.length < 2) return { title: message, detail: null };
  return { title: parts[0], detail: parts.slice(1).join(" - ") };
}
function readableStageNote(note) {
  return note.replace(/<\/?user_content>/g, "").replace(/\\n/g, "\n").trim();
}
function TaskDetail({ task, worker, onBack: _onBack }) {
  var _a, _b, _c, _d, _e, _f, _g, _h;
  const [tab2, setTabRaw] = reactExports.useState(() => readTabFromHash() ?? "review");
  const userPickedTabRef = reactExports.useRef(readTabFromHash() !== null);
  const setTab = reactExports.useCallback((next) => {
    userPickedTabRef.current = true;
    setTabRaw(next);
  }, []);
  reactExports.useEffect(() => {
    if (typeof window === "undefined") return;
    const url = new URL(window.location.href);
    const hash = url.hash.replace(/^#/, "");
    const params = new URLSearchParams(hash);
    params.set("tab", tab2);
    url.hash = params.toString();
    window.history.replaceState({}, "", url.toString());
  }, [tab2]);
  const liveTask = useTaskStore(
    (s) => s.tasks[task.id]
  ) ?? task;
  const [acceptance, setAcceptance] = reactExports.useState(null);
  const [deps, setDeps] = reactExports.useState(null);
  const [pipeline, setPipeline] = reactExports.useState(null);
  const [artifacts, setArtifacts] = reactExports.useState(null);
  const [results, setResults] = reactExports.useState(null);
  const [logs, setLogs] = reactExports.useState(null);
  const [steps, setSteps] = reactExports.useState(null);
  const [runtimeWorkers, setRuntimeWorkers] = reactExports.useState([]);
  const [runtimeWorkersLoaded, setRuntimeWorkersLoaded] = reactExports.useState(false);
  const [drawerTarget, setDrawerTarget] = reactExports.useState(null);
  const closeDrawer = reactExports.useCallback(() => setDrawerTarget(null), []);
  const [showAllFiles, setShowAllFiles] = reactExports.useState(false);
  const [showAllResults, setShowAllResults] = reactExports.useState(false);
  const [archive, setArchive] = reactExports.useState([]);
  const [feedbackInFlight, setFeedbackInFlight] = reactExports.useState(false);
  const [reviewLoopPendingUntil, setReviewLoopPendingUntil] = reactExports.useState(0);
  const draftsRef = reactExports.useRef(/* @__PURE__ */ new Map());
  const [pipelineData, setPipelineData] = reactExports.useState(null);
  const [focusedStageId, setFocusedStageId] = reactExports.useState(() => {
    if (typeof window === "undefined") return null;
    return new URLSearchParams(window.location.search).get("stage");
  });
  const [selectedRunId, setSelectedRunId] = reactExports.useState("latest");
  const [resumeBusy, setResumeBusy] = reactExports.useState(false);
  const [cancelBusy, setCancelBusy] = reactExports.useState(false);
  const [diffBusy, setDiffBusy] = reactExports.useState(false);
  const [nowMs, setNowMs] = reactExports.useState(() => Date.now());
  const [archiveBusy, setArchiveBusy] = reactExports.useState(false);
  const pushToast = useToastStore((s) => s.push);
  const [loading, setLoading] = reactExports.useState(true);
  reactExports.useEffect(() => {
    let cancelled = false;
    setLoading(true);
    setAcceptance(null);
    setDeps(null);
    setPipeline(null);
    setArtifacts(null);
    setResults(null);
    setLogs(null);
    setSteps(null);
    setRuntimeWorkers([]);
    setRuntimeWorkersLoaded(false);
    setPipelineData(null);
    setArchive([]);
    void annotationsApi.archive(task.id).then((arc) => {
      if (!cancelled) setArchive(arc);
    }).catch(() => {
    });
    void (async () => {
      try {
        const [taskData, eventsData, fileEventsData, resultsData, logsData, stepsData, pipelineResp] = await Promise.all([
          api.tasks.get(task.id),
          api.tasks.events(task.id),
          api.tasks.getFileEvents(task.id),
          api.tasks.getResults(task.id),
          api.tasks.getLogs(task.id),
          api.tasks.getSteps(task.id).catch(() => []),
          // CAP-29: best-effort fetch — tolerate failure so pre-CAP-29 tasks
          // (no worker_steps rows) still render the rest of the page.
          api.tasks.pipeline(task.id).catch((err) => {
            console.warn("[CAP-29] pipeline fetch failed (non-fatal):", err);
            return null;
          })
        ]);
        if (cancelled) return;
        useTaskStore.getState().setTask(task.id, taskData);
        const payload2 = taskData.payload;
        const dod = typeof (payload2 == null ? void 0 : payload2.dod) === "string" ? payload2.dod : "";
        setAcceptance(dod);
        setDeps(taskData.depends_on ?? []);
        setPipeline(eventsData);
        setArtifacts(fileEventsData);
        setResults(resultsData);
        setLogs(logsData);
        setSteps(stepsData);
        setPipelineData(pipelineResp);
        tracer("CAP-14", "ok");
      } catch (err) {
        if (cancelled) return;
        console.error("[CAP-14] Failed to load task detail data:", err);
        setAcceptance("");
        setDeps([]);
        setPipeline([]);
        setArtifacts([]);
        setResults([]);
        setLogs([]);
        setSteps([]);
        setPipelineData(null);
      } finally {
        if (!cancelled) setLoading(false);
      }
    })();
    return () => {
      cancelled = true;
    };
  }, [task.id]);
  const mountedRef = reactExports.useRef(true);
  reactExports.useEffect(() => {
    mountedRef.current = true;
    return () => {
      mountedRef.current = false;
    };
  }, []);
  const evidenceRefreshInFlightRef = reactExports.useRef(false);
  const refreshEvidence = reactExports.useCallback(async () => {
    if (evidenceRefreshInFlightRef.current) return;
    evidenceRefreshInFlightRef.current = true;
    try {
      const [eventsData, logsData, stepsData, resultsData, fileEventsData, pipelineResp] = await Promise.all([
        api.tasks.events(task.id),
        api.tasks.getLogs(task.id),
        api.tasks.getSteps(task.id).catch(() => []),
        api.tasks.getResults(task.id),
        api.tasks.getFileEvents(task.id),
        api.tasks.pipeline(task.id).catch((err) => {
          console.warn("[CAP-29] pipeline refresh failed (non-fatal):", err);
          return null;
        })
      ]);
      if (!mountedRef.current) return;
      setPipeline(eventsData);
      setLogs(logsData);
      setSteps(stepsData);
      setResults(resultsData);
      setArtifacts(fileEventsData);
      if (pipelineResp) setPipelineData(pipelineResp);
    } catch (err) {
      console.warn("[CAP-29] task evidence refresh failed:", err);
    } finally {
      evidenceRefreshInFlightRef.current = false;
    }
  }, [task.id]);
  const refetchTimerRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    const scheduleRefresh = () => {
      if (refetchTimerRef.current) clearTimeout(refetchTimerRef.current);
      refetchTimerRef.current = setTimeout(() => {
        void refreshEvidence();
      }, 250);
    };
    const handleStep = (data) => {
      scheduleRefresh();
    };
    const handleTaskScopedEvent = (data) => {
      const tid = data.task_id ?? data.id;
      if (tid === task.id) scheduleRefresh();
    };
    const handleWorkerEvent = (data) => {
      const attachedWorkerId = liveTask.worker_id ?? task.worker ?? null;
      const tid = data.task_id;
      const wid = data.worker_id ?? data.id;
      if (tid === task.id || attachedWorkerId && wid === attachedWorkerId) scheduleRefresh();
    };
    const unsub = eventBus.subscribe("worker.step", handleStep);
    const unsubTask = eventBus.subscribe("task.status", handleTaskScopedEvent);
    const unsubStage = eventBus.subscribe("pipeline.stage_changed", handleTaskScopedEvent);
    const unsubWorker = eventBus.subscribe("worker.status", handleWorkerEvent);
    const unsubWorkerState = eventBus.subscribe("worker_state_change", handleWorkerEvent);
    return () => {
      if (refetchTimerRef.current) clearTimeout(refetchTimerRef.current);
      unsub();
      unsubTask();
      unsubStage();
      unsubWorker();
      unsubWorkerState();
    };
  }, [liveTask, refreshEvidence, task.id, task.worker]);
  reactExports.useEffect(() => {
    const handleResultCreated = (data) => {
      if (data.task_id !== task.id || typeof data.id !== "string") return;
      setResults(
        (prev) => upsertById(prev, data, (r) => r.id)
      );
      if (refetchTimerRef.current) clearTimeout(refetchTimerRef.current);
      refetchTimerRef.current = setTimeout(() => {
        void refreshEvidence();
      }, 400);
    };
    const handleFileEventCreated = (data) => {
      if (data.task_id !== task.id || typeof data.id !== "number") return;
      setArtifacts(
        (prev) => upsertById(prev, data, (f) => f.id)
      );
      if (refetchTimerRef.current) clearTimeout(refetchTimerRef.current);
      refetchTimerRef.current = setTimeout(() => {
        void refreshEvidence();
      }, 400);
    };
    const unsubResult = eventBus.subscribe("task.result.created", handleResultCreated);
    const unsubFile = eventBus.subscribe("task.file_event.created", handleFileEventCreated);
    return () => {
      unsubResult();
      unsubFile();
    };
  }, [refreshEvidence, task.id]);
  reactExports.useEffect(() => {
    const unsub = eventBus.subscribe("log", (data) => {
      const payload2 = data;
      if (payload2.task_id !== task.id) return;
      const createdAt = typeof payload2.timestamp === "number" ? new Date(payload2.timestamp * 1e3).toISOString() : (/* @__PURE__ */ new Date()).toISOString();
      const line = {
        id: typeof payload2.seq === "number" ? payload2.seq : null,
        worker_id: typeof payload2.worker_id === "string" ? payload2.worker_id : null,
        task_id: task.id,
        level: typeof payload2.level === "string" ? payload2.level : "INFO",
        message: typeof payload2.message === "string" ? payload2.message : "",
        metadata: null,
        created_at: createdAt
      };
      setLogs((prev) => [line, ...prev ?? []].slice(0, 200));
      if (refetchTimerRef.current) clearTimeout(refetchTimerRef.current);
      refetchTimerRef.current = setTimeout(() => {
        void refreshEvidence();
      }, 400);
    });
    return () => unsub();
  }, [refreshEvidence, task.id]);
  const liveRefreshTimerRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    const refresh = () => {
      api.tasks.get(task.id).then((fresh) => {
        useTaskStore.getState().setTask(task.id, fresh);
        tracer("ANN-36", `ok live_refresh task=${task.id}`);
      }).catch(() => {
      });
    };
    const debounced = (data) => {
      const tid = (data && data.task_id) ?? (data && data.id);
      if (tid !== task.id) return;
      if (liveRefreshTimerRef.current) {
        clearTimeout(liveRefreshTimerRef.current);
      }
      liveRefreshTimerRef.current = setTimeout(refresh, 250);
    };
    const unsubA = eventBus.subscribe("task.status", debounced);
    const unsubB = eventBus.subscribe("pipeline.stage_changed", debounced);
    return () => {
      if (liveRefreshTimerRef.current) {
        clearTimeout(liveRefreshTimerRef.current);
      }
      unsubA();
      unsubB();
    };
  }, [task.id]);
  reactExports.useEffect(() => {
    if (typeof window === "undefined") return;
    const url = new URL(window.location.href);
    if (focusedStageId) url.searchParams.set("stage", focusedStageId);
    else url.searchParams.delete("stage");
    window.history.replaceState({}, "", url.toString());
  }, [focusedStageId]);
  const handlePickStage = (s) => {
    setFocusedStageId((prev) => prev === s.id ? null : s.id);
  };
  const payload = liveTask.payload ?? {};
  const blockedOn = typeof payload.blocked_on === "string" ? payload.blocked_on : null;
  const allWarnings = Array.isArray(payload.warnings) ? payload.warnings.filter((w) => typeof w === "string") : [];
  const PARK_STATE_CHIPS = /* @__PURE__ */ new Set([
    "needs_human_review",
    "review_loops_exceeded",
    "sub_agent_stalled"
  ]);
  const warnings = blockedOn === "human_review" ? allWarnings : allWarnings.filter((w) => !PARK_STATE_CHIPS.has(w));
  const reviewOutcomes = Array.isArray(payload.review_outcomes) ? payload.review_outcomes.filter((o) => typeof o === "string") : [];
  const pipelinePayload = payload.pipeline;
  const currentStage = (pipelinePayload == null ? void 0 : pipelinePayload.current_stage) ?? null;
  const liveStatus = liveTask.status ?? task.status;
  const isPushGateBlocked = blockedOn === "push_gate";
  const isMergeConflictBlocked = blockedOn === "merge_conflict";
  const pushGateCommitSha = typeof payload.commit_sha === "string" ? payload.commit_sha : null;
  const mergeBlockedReason = typeof payload.blocked_reason === "string" ? payload.blocked_reason : null;
  const mergeWorktreePath = typeof payload.merge_worktree_path === "string" ? payload.merge_worktree_path : null;
  const mergeConflictFiles = Array.isArray(payload.merge_conflict_files) ? payload.merge_conflict_files.filter((f) => typeof f === "string") : [];
  const isFakeDispatchMission = payload.dispatch === "fake";
  const canViewPushGateDiff = !diffBusy && !isFakeDispatchMission;
  reactExports.useEffect(() => {
    if (tab2 !== "review") return;
    if (liveStatus !== "running" && !feedbackInFlight && !resumeBusy) return;
    void refreshEvidence();
  }, [feedbackInFlight, liveStatus, refreshEvidence, resumeBusy, tab2]);
  reactExports.useEffect(() => {
    if (tab2 !== "review" && liveStatus !== "running") return;
    let cancelled = false;
    const load = () => {
      api.workers.list().then((next) => {
        if (!cancelled) {
          setRuntimeWorkers(next);
          setRuntimeWorkersLoaded(true);
        }
      }).catch(() => {
        if (!cancelled) setRuntimeWorkersLoaded(true);
      });
    };
    load();
    const id = window.setInterval(load, 5e3);
    return () => {
      cancelled = true;
      window.clearInterval(id);
    };
  }, [tab2, liveStatus]);
  reactExports.useEffect(() => {
    if (tab2 !== "review") return;
    const id = window.setInterval(() => setNowMs(Date.now()), 1e3);
    return () => window.clearInterval(id);
  }, [tab2]);
  const showReviewerFeedback = reactExports.useCallback(() => {
    setTab("review");
  }, [setTab]);
  const prevBlockedOnRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    const prev = prevBlockedOnRef.current;
    if (prev !== "human_review" && blockedOn === "human_review" && !userPickedTabRef.current) {
      setTabRaw("review");
    }
    if (prev === "human_review" && blockedOn !== "human_review" && !feedbackInFlight) {
      draftsRef.current.clear();
    }
    prevBlockedOnRef.current = blockedOn;
  }, [blockedOn]);
  const handleResumeHumanReview = reactExports.useCallback(async () => {
    if (resumeBusy) return;
    setResumeBusy(true);
    try {
      const res = await authedFetch(
        `/api/tasks/${task.id}/resume?role=conductor`,
        { method: "POST" }
      );
      if (!res.ok) {
        const body = await res.text().catch(() => res.statusText);
        tracer("HRV-04", `ERROR resume_failed task=${task.id} msg=${body}`);
        pushToast({ type: "error", title: "human_review resume failed", message: body });
        return;
      }
      api.tasks.get(task.id).then((fresh) => {
        useTaskStore.getState().setTask(task.id, fresh);
      }).catch(() => {
      });
      tracer("HRV-02", `ok resume task=${task.id}`);
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      tracer("HRV-04", `ERROR resume_failed task=${task.id} msg=${msg}`);
      pushToast({ type: "error", title: "human_review resume failed", message: msg });
    } finally {
      setResumeBusy(false);
    }
  }, [task.id, resumeBusy, pushToast]);
  const handleCancelHumanReview = reactExports.useCallback(async () => {
    if (!window.confirm("Cancel mission permanently?")) return;
    if (cancelBusy) return;
    setCancelBusy(true);
    try {
      await api.tasks.update(task.id, {
        status: "cancelled",
        operator_override: true,
        override_reason: "human_review_cancel"
      });
      api.tasks.get(task.id).then((fresh) => {
        useTaskStore.getState().setTask(task.id, fresh);
      }).catch(() => {
      });
      tracer("HRV-03", `ok cancel task=${task.id}`);
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      tracer("HRV-04", `ERROR cancel_failed task=${task.id} msg=${msg}`);
      pushToast({ type: "error", title: "human_review cancel failed", message: msg });
    } finally {
      setCancelBusy(false);
    }
  }, [task.id, cancelBusy, pushToast]);
  const handleApprovePushGate = reactExports.useCallback(async () => {
    if (resumeBusy) return;
    setResumeBusy(true);
    try {
      const res = await authedFetch(
        `/api/tasks/${task.id}/resume?role=conductor`,
        { method: "POST" }
      );
      if (!res.ok) {
        const body = await res.text().catch(() => res.statusText);
        tracer("ANN-push-gate", `ERROR approve_failed task=${task.id} msg=${body}`);
        pushToast({ type: "error", title: "push_gate approval failed", message: body });
        return;
      }
      api.tasks.get(task.id).then((fresh) => {
        useTaskStore.getState().setTask(task.id, fresh);
      }).catch(() => {
      });
      tracer("ANN-push-gate", `ok approved task=${task.id}`);
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      tracer("ANN-push-gate", `ERROR approve_failed task=${task.id} msg=${msg}`);
      pushToast({ type: "error", title: "push_gate approval failed", message: msg });
    } finally {
      setResumeBusy(false);
    }
  }, [task.id, resumeBusy, pushToast]);
  const handleViewPushGateDiff = reactExports.useCallback(async () => {
    if (diffBusy) return;
    setDiffBusy(true);
    try {
      const diff = await api.tasks.getCommitDiff(task.id);
      setDrawerTarget({
        kind: "text",
        title: diff.path,
        content: diff.content,
        size: diff.size,
        truncated: diff.truncated,
        language: "diff"
      });
    } catch (err) {
      pushToast({
        type: "error",
        title: "Diff unavailable",
        message: err instanceof Error ? err.message : String(err)
      });
    } finally {
      setDiffBusy(false);
    }
  }, [diffBusy, task.id, pushToast]);
  const handleArchiveToggle = reactExports.useCallback(async () => {
    var _a2;
    if (archiveBusy) return;
    setArchiveBusy(true);
    const isCurrentlyArchived = Boolean(
      (_a2 = liveTask.payload) == null ? void 0 : _a2.archived
    );
    if (isCurrentlyArchived) {
      try {
        await api.tasks.unarchive(task.id);
        pushToast({ type: "info", title: "Mission unarchived" });
        api.tasks.get(task.id).then((fresh) => {
          useTaskStore.getState().setTask(task.id, fresh);
        }).catch(() => {
        });
      } catch (err) {
        const msg = err instanceof Error ? err.message : String(err);
        pushToast({ type: "error", title: "Unarchive failed", message: msg });
      } finally {
        setArchiveBusy(false);
      }
      return;
    }
    try {
      await api.tasks.archive(task.id);
      api.tasks.get(task.id).then((fresh) => {
        useTaskStore.getState().setTask(task.id, fresh);
      }).catch(() => {
      });
      pushToast({
        type: "info",
        title: "Mission archived",
        message: "Hidden from active views; history preserved.",
        action: {
          label: "Undo",
          onClick: () => {
            api.tasks.unarchive(task.id).then(() => api.tasks.get(task.id)).then((fresh) => useTaskStore.getState().setTask(task.id, fresh)).catch(() => {
            });
          }
        }
      });
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      pushToast({ type: "error", title: "Archive failed", message: msg });
    } finally {
      setArchiveBusy(false);
    }
  }, [task.id, archiveBusy, liveTask, pushToast]);
  const stageRunsByName = reactExports.useMemo(() => buildStageRuns(steps), [steps]);
  const pipelineForView = reactExports.useMemo(() => {
    var _a2;
    if (!pipelineData) return null;
    const payload2 = liveTask.payload;
    const payloadPipeline = payload2 == null ? void 0 : payload2.pipeline;
    const seeded = payloadPipeline == null ? void 0 : payloadPipeline.stages;
    if (!Array.isArray(seeded) || seeded.length === 0) return pipelineData;
    const observed = new Map(pipelineData.stages.map((s) => [s.name, s]));
    const order2 = seeded.map((s) => s.name).filter((n) => typeof n === "string");
    const STATUS_MAP2 = {
      completed: "done",
      done: "done",
      running: "running",
      active: "running",
      failed: "failed",
      error: "failed",
      pending: "pending",
      skipped: "skipped",
      "awaiting-approval": "awaiting-approval",
      warn: "warn"
    };
    const currentStage2 = (payloadPipeline == null ? void 0 : payloadPipeline.current_stage) ?? null;
    const loopPartner = {
      plan_tracers: "review_plan",
      review_plan: "plan_tracers",
      implement: "review_impl",
      review_impl: "implement"
    };
    const currentStageRunCount = currentStage2 ? (stageRunsByName.get(currentStage2) ?? []).length : 0;
    const merged = order2.map((name2, i) => {
      var _a3;
      const existing = observed.get(name2);
      const seedRow = seeded[i];
      const runs = stageRunsByName.get(name2) ?? [];
      const partnerRuns = stageRunsByName.get(loopPartner[name2] ?? "") ?? [];
      const loopRunCount = Math.max(runs.length, partnerRuns.length);
      const currentRun = runs.length > 0 ? name2 === currentStage2 ? ((_a3 = runs.find((run) => run.steps.some((step) => step.status === "running"))) == null ? void 0 : _a3.index) ?? runs.length : runs.length : void 0;
      const rawStatus = seedRow.status ?? "pending";
      const isStalePartnerInActiveLoop = currentStage2 != null && loopPartner[name2] === currentStage2 && currentStageRunCount > runs.length;
      const effectiveStatus = isStalePartnerInActiveLoop ? "pending" : (rawStatus === "running" || rawStatus === "active") && name2 !== currentStage2 ? "completed" : rawStatus;
      const mappedStatus = (existing == null ? void 0 : existing.status) === "awaiting-approval" ? "awaiting-approval" : STATUS_MAP2[effectiveStatus] ?? "pending";
      const reviewLoopSentBack = mappedStatus === "done" && reviewOutcomes.length > 0 && (name2 === "review_plan" || name2 === "review_impl") && currentStage2 === loopPartner[name2];
      const preferObservedTiming = (existing == null ? void 0 : existing.status) === "awaiting-approval" || (existing == null ? void 0 : existing.status) === "running" || name2 === currentStage2;
      return {
        id: (existing == null ? void 0 : existing.id) ?? `seed-${name2}`,
        name: name2,
        kind: (existing == null ? void 0 : existing.kind) ?? "work",
        status: mappedStatus,
        startedAt: preferObservedTiming ? (existing == null ? void 0 : existing.startedAt) ?? parseHiveTimestamp(seedRow.started_at) ?? null : parseHiveTimestamp(seedRow.started_at) ?? (existing == null ? void 0 : existing.startedAt) ?? null,
        endedAt: preferObservedTiming ? (existing == null ? void 0 : existing.endedAt) ?? parseHiveTimestamp(seedRow.ended_at) ?? null : parseHiveTimestamp(seedRow.ended_at) ?? (existing == null ? void 0 : existing.endedAt) ?? null,
        durationMs: seedRow.started_at && seedRow.ended_at ? Math.max(0, (parseHiveTimestamp(seedRow.ended_at) ?? 0) - (parseHiveTimestamp(seedRow.started_at) ?? 0)) : mappedStatus === "done" ? (existing == null ? void 0 : existing.durationMs) ?? null : null,
        addedAt: "plan",
        parentLoopId: (existing == null ? void 0 : existing.parentLoopId) ?? null,
        iteration: (existing == null ? void 0 : existing.iteration) ?? null,
        meta: {
          ...(existing == null ? void 0 : existing.meta) ?? {},
          ...loopRunCount > 1 ? { runCount: loopRunCount, currentRun: currentRun ?? 0 } : {},
          ...reviewLoopSentBack ? {
            revisionCount: reviewOutcomes.length,
            statusLabel: "sent back"
          } : {}
        }
      };
    });
    for (const s of pipelineData.stages) {
      if (!order2.includes(s.name)) merged.push(s);
    }
    const total = merged.length;
    const done = merged.filter((s) => s.status === "done").length;
    const runningStageId = ((_a2 = merged.find((s) => s.status === "running")) == null ? void 0 : _a2.id) ?? null;
    return { ...pipelineData, stages: merged, totalStages: total, doneStages: done, runningStageId };
  }, [pipelineData, liveTask, reviewOutcomes.length, stageRunsByName]);
  const selectedStage = reactExports.useMemo(() => {
    if (!(pipelineForView == null ? void 0 : pipelineForView.stages.length)) return null;
    return pipelineForView.stages.find((s) => s.id === focusedStageId) ?? pipelineForView.stages.find((s) => s.id === pipelineForView.runningStageId) ?? pipelineForView.stages.find((s) => s.name === currentStage) ?? pipelineForView.stages[0];
  }, [currentStage, focusedStageId, pipelineForView]);
  const selectedStageRuns = reactExports.useMemo(() => {
    if (!selectedStage) return [];
    return [...stageRunsByName.get(selectedStage.name) ?? []].sort((a, b) => b.index - a.index);
  }, [selectedStage, stageRunsByName]);
  const selectedRun = reactExports.useMemo(() => {
    if (selectedStageRuns.length === 0) return null;
    if (selectedRunId === "latest") return selectedStageRuns[0];
    return selectedStageRuns.find((run) => run.id === selectedRunId) ?? selectedStageRuns[0];
  }, [selectedRunId, selectedStageRuns]);
  reactExports.useEffect(() => {
    setSelectedRunId("latest");
  }, [selectedStage == null ? void 0 : selectedStage.id]);
  const selectedStageEvidence = reactExports.useMemo(() => {
    var _a2;
    if (!selectedStage) return [];
    const stage = selectedStage.name;
    const runLabel = selectedRun ? `run ${selectedRun.index}` : "run 1";
    const stageNote = ((_a2 = selectedStage.meta) == null ? void 0 : _a2.note) ? [{
      id: `stage-note-${selectedStage.id}`,
      createdAt: selectedStage.endedAt || selectedStage.startedAt ? new Date(selectedStage.endedAt ?? selectedStage.startedAt).toISOString() : null,
      kind: "summary",
      label: null,
      runLabel: null,
      message: readableStageNote(selectedStage.meta.note),
      onOpen: void 0
    }] : [];
    const stepLines = ((selectedRun == null ? void 0 : selectedRun.steps) ?? (steps ?? []).filter((step) => step.stage === stage)).map((step) => ({
      id: `step-${step.worker_id}-${step.seq}`,
      createdAt: step.ts,
      kind: "step",
      label: step.status ?? "STEP",
      runLabel,
      message: step.message ?? `${step.stage ?? "stage"} update`,
      onOpen: void 0
    }));
    const logLines = (logs ?? []).filter((line) => {
      const metaStage = payloadStage(line.metadata);
      return withinRun(line.created_at, selectedRun) && (metaStage === stage || line.message.includes(stage));
    }).map((line) => ({
      id: `log-${line.id ?? line.created_at ?? line.message}`,
      createdAt: line.created_at,
      kind: "log",
      label: line.level ?? "LOG",
      runLabel,
      message: line.message,
      onOpen: void 0
    }));
    const resultLines = (results ?? []).filter((result) => withinRun(result.created_at, selectedRun) && resultLooksLikeStage(result, stage)).map((result) => ({
      id: `result-${result.id}`,
      createdAt: result.created_at,
      kind: "result",
      label: result.status ?? "RESULT",
      runLabel,
      message: resultSummary(result),
      onOpen: void 0
    }));
    const eventLines = (pipeline ?? []).filter((ev) => {
      const evStage = payloadStage(ev.payload);
      return withinRun(ev.created_at, selectedRun) && (evStage === stage || ev.type.includes(stage));
    }).map((ev, i) => ({
      id: `event-${ev.id ?? i}`,
      createdAt: ev.created_at,
      kind: "event",
      label: ev.type,
      runLabel,
      message: ev.worker_id ?? "system",
      onOpen: void 0
    }));
    return [...stageNote, ...stepLines, ...logLines, ...resultLines, ...eventLines].sort((a, b) => {
      const ta = parseHiveTimestamp(a.createdAt) ?? 0;
      const tb = parseHiveTimestamp(b.createdAt) ?? 0;
      return tb - ta;
    }).slice(0, 50);
  }, [logs, pipeline, results, selectedRun, selectedStage, steps]);
  const sortedArtifacts = reactExports.useMemo(() => {
    if (!artifacts) return [];
    return [...artifacts].sort((a, b) => {
      const ta = parseHiveTimestamp(a.created_at) ?? 0;
      const tb = parseHiveTimestamp(b.created_at) ?? 0;
      return tb - ta;
    });
  }, [artifacts]);
  const artifactFileGroups = reactExports.useMemo(() => {
    const byPath = /* @__PURE__ */ new Map();
    for (const event of sortedArtifacts) {
      const existing = byPath.get(event.path);
      if (existing) {
        existing.eventCount += 1;
      } else {
        byPath.set(event.path, {
          path: event.path,
          latest: event,
          eventCount: 1
        });
      }
    }
    return Array.from(byPath.values());
  }, [sortedArtifacts]);
  const totalArtifactEvents = sortedArtifacts.length;
  const visibleArtifacts = showAllFiles ? artifactFileGroups : artifactFileGroups.slice(0, 8);
  const hiddenArtifactCount = artifactFileGroups.length - visibleArtifacts.length;
  const sortedResults = reactExports.useMemo(() => {
    if (!results) return [];
    return [...results].sort((a, b) => {
      const ta = parseHiveTimestamp(a.created_at) ?? 0;
      const tb = parseHiveTimestamp(b.created_at) ?? 0;
      return tb - ta;
    });
  }, [results]);
  const runtimeProof = reactExports.useMemo(() => {
    const attachedWorkerId = liveTask.worker_id ?? task.worker ?? null;
    const relevant = runtimeWorkers.filter((w) => {
      const spawnReason = w.spawn_reason ?? "";
      return w.id === attachedWorkerId || w.task_id === task.id || spawnReason.includes(task.id);
    });
    const active = relevant.filter(
      (w) => !["stopped", "stopping", "dead", "killed", "timeout", "error"].includes(w.status)
    );
    const orchestrator = active.find((w) => w.id === attachedWorkerId) ?? active.find((w) => w.role === "annotation-orchestrator") ?? null;
    const subagent = active.find((w) => w.role === "annotation-subagent") ?? active.find((w) => w.role !== "annotation-orchestrator") ?? null;
    const newestWorkerTs = relevant.flatMap((w) => [parseHiveTimestamp(w.last_heartbeat), parseHiveTimestamp(w.updated_at)]).filter((v) => v !== null).sort((a, b) => b - a)[0] ?? null;
    const newestResult = sortedResults[0] ?? null;
    return {
      orchestrator,
      subagent,
      activeCount: active.length,
      totalCount: relevant.length,
      newestWorkerTs,
      newestResult
    };
  }, [liveTask, runtimeWorkers, sortedResults, task.id, task.worker]);
  const workerIsActive = worker != null && !["stopped", "stopping", "dead", "killed", "timeout", "error"].includes(worker.status) || runtimeProof.activeCount > 0;
  const visibleResults = showAllResults ? sortedResults : sortedResults.slice(0, 8);
  const hiddenResultCount = sortedResults.length - visibleResults.length;
  const latestEvent = reactExports.useMemo(() => {
    if (!pipeline || pipeline.length === 0) return null;
    return [...pipeline].sort((a, b) => {
      const ta = parseHiveTimestamp(a.created_at) ?? 0;
      const tb = parseHiveTimestamp(b.created_at) ?? 0;
      return tb - ta;
    })[0] ?? null;
  }, [pipeline]);
  const latestEventMs = parseHiveTimestamp(latestEvent == null ? void 0 : latestEvent.created_at);
  const latestResultMs = parseHiveTimestamp((_a = sortedResults[0]) == null ? void 0 : _a.created_at);
  const latestStageMs = reactExports.useMemo(() => {
    const times = ((pipelineForView == null ? void 0 : pipelineForView.stages) ?? []).flatMap((s) => [s.startedAt, s.endedAt]).filter((t) => typeof t === "number" && Number.isFinite(t));
    return times.length > 0 ? Math.max(...times) : null;
  }, [pipelineForView]);
  const sortedLogs = reactExports.useMemo(() => {
    if (!logs) return [];
    return [...logs].sort((a, b) => {
      const ta = parseHiveTimestamp(a.created_at) ?? 0;
      const tb = parseHiveTimestamp(b.created_at) ?? 0;
      return tb - ta;
    });
  }, [logs]);
  const evidenceLines = reactExports.useMemo(() => {
    const stagePriority = (line) => {
      if (line.source !== "stage") return 0;
      if (line.message.includes("awaiting-approval")) return 3;
      if (line.message.includes("running")) return 2;
      if (line.message.includes("failed")) return 1;
      return 0;
    };
    const logLines = sortedLogs.map((line) => ({
      id: `log-${line.id ?? line.created_at ?? line.message}`,
      createdAt: line.created_at,
      level: line.level ?? "INFO",
      message: line.message,
      source: "log"
    }));
    const stepLines = (steps ?? []).map((step) => ({
      id: `step-${step.worker_id}-${step.seq}`,
      createdAt: step.ts,
      level: step.status ?? "STEP",
      message: `${step.stage ?? "step"}${step.message ? ` · ${step.message}` : ""}`,
      source: "step"
    }));
    const eventLines = (pipeline ?? []).map((ev, i) => ({
      id: `event-${ev.id ?? i}`,
      createdAt: ev.created_at,
      level: "EVENT",
      message: `${ev.type}${ev.worker_id ? ` · ${ev.worker_id}` : ""}`,
      source: "event"
    }));
    const stageLines = ((pipelineForView == null ? void 0 : pipelineForView.stages) ?? []).filter((s) => s.startedAt || s.endedAt).map((s) => ({
      id: `stage-${s.id}-${s.status}`,
      createdAt: new Date(s.endedAt ?? s.startedAt).toISOString(),
      level: "STAGE",
      message: `${s.name} · ${s.status}`,
      source: "stage"
    }));
    return [...logLines, ...stepLines, ...eventLines, ...stageLines].sort((a, b) => {
      const ta = parseHiveTimestamp(a.createdAt) ?? 0;
      const tb = parseHiveTimestamp(b.createdAt) ?? 0;
      if (tb !== ta) return tb - ta;
      return stagePriority(b) - stagePriority(a);
    }).slice(0, 100);
  }, [pipeline, pipelineForView, sortedLogs, steps]);
  const latestLogMs = parseHiveTimestamp((_b = sortedLogs[0]) == null ? void 0 : _b.created_at);
  const latestActivityMs = Math.max(
    latestEventMs ?? 0,
    latestResultMs ?? 0,
    latestLogMs ?? 0,
    latestStageMs ?? 0
  ) || null;
  const latestActivityAgeMs = latestActivityMs ? Math.max(0, nowMs - latestActivityMs) : null;
  const hasFreshProof = latestActivityAgeMs !== null && latestActivityAgeMs <= 12e4;
  const isAnnotationMission = liveTask.type === "annotation_mission" || task.rawType === "annotation_mission";
  const taskStartMs = parseHiveTimestamp(liveTask.started_at) ?? parseHiveTimestamp(liveTask.created_at);
  const taskStartAgeMs = taskStartMs ? Math.max(0, nowMs - taskStartMs) : null;
  const reviewLoopPending = (feedbackInFlight || nowMs < reviewLoopPendingUntil) && !blockedOn && !(workerIsActive && hasFreshProof);
  const initialWorkerAttachPending = liveStatus === "running" && isAnnotationMission && !workerIsActive && !hasFreshProof && !blockedOn && (taskStartAgeMs === null || taskStartAgeMs <= 12e4 || loading || !runtimeWorkersLoaded);
  const noActiveWorker = liveStatus === "running" && !workerIsActive && !reviewLoopPending && !initialWorkerAttachPending && !hasFreshProof && !loading && runtimeWorkersLoaded && !blockedOn;
  const staleRunning = liveStatus === "running" && workerIsActive && !hasFreshProof && !reviewLoopPending;
  const reviewActivityLive = liveStatus === "running" && (workerIsActive || hasFreshProof) || feedbackInFlight || resumeBusy || cancelBusy;
  const runSignalTitle = reviewLoopPending ? "Review loop starting" : initialWorkerAttachPending ? "Worker starting" : noActiveWorker ? "No active worker" : staleRunning ? "No fresh proof" : reviewActivityLive ? "Mission is running" : "Waiting on human review";
  const runSignalSub = reviewLoopPending ? "Resume accepted; waiting for the orchestrator and reviewer worker to attach." : initialWorkerAttachPending ? "Mission was just queued; waiting for the annotation orchestrator to attach." : noActiveWorker ? "Task status says running, but no worker is attached. Resume should spawn an orchestrator." : staleRunning ? `${prettyStage(currentStage)} has no output in ${relativeActivityTime(latestActivityMs, nowMs)}.` : reviewActivityLive ? `${prettyStage(currentStage)} is active; watching worker output in real time.` : "Worker loop is parked until review feedback is submitted.";
  const latestActivityKind = latestActivityMs && latestActivityMs === latestLogMs ? "console output" : latestActivityMs && latestActivityMs === latestStageMs ? "stage activity" : latestActivityMs && latestActivityMs === latestResultMs ? "worker result" : (latestEvent == null ? void 0 : latestEvent.type) ?? "worker event";
  const annLookup = reactExports.useMemo(() => {
    const m = /* @__PURE__ */ new Map();
    for (const a of archive) m.set(a.id, a.body);
    return m;
  }, [archive]);
  const producedWork = reactExports.useMemo(() => {
    return [
      ...sortedResults.slice(0, 3).map((r) => ({
        id: r.id,
        kind: "result",
        label: resultSummary(r).replace(/\s+/g, " ").slice(0, 120),
        createdAt: r.created_at,
        onOpen: () => setDrawerTarget({ kind: "result", result: r, annLookup })
      })),
      ...artifactFileGroups.slice(0, 3).map((group, i) => ({
        id: String(group.latest.id ?? `${group.path}-${i}`),
        kind: group.latest.event_type || "file",
        label: group.path,
        createdAt: group.latest.created_at,
        onOpen: () => {
          if (isRenderableFile(group.path)) {
            setDrawerTarget({ kind: "file_event", taskId: task.id, event: group.latest });
          }
        }
      }))
    ].sort((a, b) => {
      const ta = parseHiveTimestamp(a.createdAt) ?? 0;
      const tb = parseHiveTimestamp(b.createdAt) ?? 0;
      return tb - ta;
    }).slice(0, 5);
  }, [annLookup, artifactFileGroups, sortedResults, task.id]);
  const latestPlanResult = reactExports.useMemo(
    () => sortedResults.find((r) => {
      const p = r.payload;
      return p && p.plan != null;
    }) ?? null,
    [sortedResults]
  );
  const latestReviewResult = reactExports.useMemo(
    () => sortedResults.find((r) => {
      const p = r.payload;
      return p && Array.isArray(p.weaknesses) && p.weaknesses.length > 0;
    }) ?? null,
    [sortedResults]
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "hv-view nopad",
      "data-testid": "task-detail",
      style: { display: "block" },
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          HumanReviewBanner,
          {
            taskId: task.id,
            blockedOn,
            warnings,
            reviewOutcomes,
            currentStage,
            resumeBusy,
            cancelBusy,
            onResume: () => {
              void handleResumeHumanReview();
            },
            onCancel: () => {
              void handleCancelHumanReview();
            },
            onShowFeedback: showReviewerFeedback
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(TaskWarningsBadges, { warnings }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-td-head", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-td-breadcrumbs", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Kanban" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.chevR, { size: 11 }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: task.status }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.chevR, { size: 11 }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", children: task.id })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-td-title", style: { display: "flex", alignItems: "center", justifyContent: "space-between", flexWrap: "wrap", gap: 8 }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", alignItems: "center", gap: 8, flex: 1, minWidth: 0 }, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: task.title }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(StatusBadge, { status: task.status }),
              task.priority && /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "neutral", children: task.priority }),
              (task.status === "queued" || task.status === "blocked") && task.targetRole && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "span",
                {
                  "data-testid": "task-detail-routing",
                  "data-routing-role": task.targetRole,
                  title: task.routingReason ?? `Waiting for ${task.targetRole}`,
                  style: {
                    display: "inline-flex",
                    alignItems: "center",
                    gap: 4,
                    minWidth: 0,
                    maxWidth: 260,
                    padding: "2px 7px",
                    border: "1px solid rgba(79,224,216,0.34)",
                    borderRadius: 999,
                    background: "rgba(79,224,216,0.10)",
                    color: "var(--hv-info, #4fe0d8)",
                    fontSize: 11,
                    fontWeight: 650,
                    lineHeight: 1.2,
                    whiteSpace: "nowrap"
                  },
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.link, { size: 10 }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { overflow: "hidden", textOverflow: "ellipsis" }, children: [
                      "waiting: ",
                      task.targetRole,
                      task.targetEngine ? ` · ${task.targetEngine}` : ""
                    ] })
                  ]
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                "data-testid": ((_c = liveTask.payload) == null ? void 0 : _c.archived) ? "task-detail-unarchive-btn" : "task-detail-archive-btn",
                disabled: archiveBusy,
                onClick: () => {
                  void handleArchiveToggle();
                },
                style: {
                  flexShrink: 0,
                  padding: "4px 10px",
                  fontSize: 12,
                  fontWeight: 500,
                  cursor: archiveBusy ? "not-allowed" : "pointer",
                  background: "none",
                  border: "1px solid var(--hv-border)",
                  borderRadius: 4,
                  color: ((_d = liveTask.payload) == null ? void 0 : _d.archived) ? "var(--hv-fg)" : "var(--hv-fg-subtle)",
                  opacity: archiveBusy ? 0.6 : 1
                },
                children: archiveBusy ? "…" : ((_e = liveTask.payload) == null ? void 0 : _e.archived) ? "Unarchive" : "Archive"
              }
            )
          ] }),
          Boolean((_f = liveTask.payload) == null ? void 0 : _f.archived) && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              "data-testid": "task-detail-archived-banner",
              style: {
                marginTop: 8,
                marginBottom: 12,
                padding: "8px 12px",
                background: "var(--hv-surface-2, #1e2130)",
                border: "1px solid var(--hv-warn, #e8932c)",
                borderLeft: "4px solid var(--hv-warn, #e8932c)",
                borderRadius: 4,
                color: "var(--hv-fg-subtle)",
                fontSize: 12,
                lineHeight: 1.5
              },
              children: [
                "This mission has been archived. Hidden from active views; history preserved. Unarchiving restores visibility. Status remains",
                " ",
                /* @__PURE__ */ jsxRuntimeExports.jsx("code", { children: liveTask.status ?? task.status }),
                (liveTask.status ?? task.status) === "cancelled" ? " — re-queue manually to resume." : null
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-td-desc", children: task.description }),
          task.rawType !== "annotation_mission" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-td-row", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "f", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("label", { children: "Worker" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { display: "flex", alignItems: "center", gap: 5, fontSize: 13, fontWeight: 500 }, children: worker ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(HexMark, { size: 14, name: worker.name }),
                worker.name
              ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "muted", children: "unassigned" }) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "f", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("label", { children: "Type" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { fontSize: 13, fontWeight: 500 }, children: task.type })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "f", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("label", { children: "Created" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { fontSize: 13, fontWeight: 500 }, children: [
                task.createdAgo,
                " ago"
              ] })
            ] }),
            task.leaseRemainingSec > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "f", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("label", { children: "Lease" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { fontSize: 13, fontWeight: 500, color: "var(--hv-warn)", display: "flex", alignItems: "center", gap: 3 }, children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.timer, { size: 11 }),
                Math.floor(task.leaseRemainingSec / 60),
                "m left"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "f", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("label", { children: "Tokens" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { fontSize: 13, fontWeight: 500 }, children: [
                (task.tokens / 1e3).toFixed(1),
                "k"
              ] })
            ] })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-tabs", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              className: `hv-tab${tab2 === "review" ? " active" : ""}`,
              "data-testid": "task-detail-tab-review",
              onClick: () => setTab("review"),
              style: blockedOn === "human_review" || isPushGateBlocked ? { color: "var(--hv-warn, #facc15)" } : void 0,
              children: [
                "Review",
                (blockedOn === "human_review" || isPushGateBlocked) && /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "span",
                  {
                    "aria-label": isPushGateBlocked ? "approval required" : "action required",
                    style: {
                      marginLeft: 6,
                      display: "inline-block",
                      width: 6,
                      height: 6,
                      borderRadius: "50%",
                      background: "var(--hv-warn, #facc15)"
                    }
                  }
                )
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              className: `hv-tab${tab2 === "pipeline" ? " active" : ""}`,
              "data-testid": "task-detail-tab-pipeline",
              onClick: () => setTab("pipeline"),
              style: isPushGateBlocked ? { color: "var(--hv-warn, #facc15)" } : void 0,
              children: [
                "Pipeline",
                isPushGateBlocked && /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "span",
                  {
                    "aria-label": "approval required",
                    style: {
                      marginLeft: 6,
                      display: "inline-block",
                      width: 6,
                      height: 6,
                      borderRadius: "50%",
                      background: "var(--hv-warn, #facc15)"
                    }
                  }
                )
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              className: `hv-tab${tab2 === "artifacts" ? " active" : ""}`,
              "data-testid": "task-detail-tab-artifacts",
              onClick: () => setTab("artifacts"),
              children: "Artifacts"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              className: `hv-tab${tab2 === "acceptance" ? " active" : ""}`,
              "data-testid": "task-detail-tab-acceptance",
              onClick: () => setTab("acceptance"),
              children: "Acceptance"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              className: `hv-tab${tab2 === "deps" ? " active" : ""}`,
              "data-testid": "task-detail-tab-deps",
              onClick: () => setTab("deps"),
              disabled: !loading && Array.isArray(deps) && deps.length === 0,
              title: !loading && Array.isArray(deps) && deps.length === 0 ? "No dependencies" : void 0,
              children: "Dependencies"
            }
          )
        ] }),
        tab2 === "acceptance" && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: 20 }, children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-head", children: /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Acceptance criteria" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              className: "hv-panel-body",
              "data-testid": "task-detail-acceptance-content",
              style: { fontSize: 13, lineHeight: 1.6 },
              children: loading ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonRow, {}),
                /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonRow, {}),
                /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonRow, {})
              ] }) : acceptance ? /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { style: { whiteSpace: "pre-wrap", margin: 0, fontFamily: "inherit" }, children: acceptance }) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "muted", children: "No acceptance criteria defined." })
            }
          )
        ] }) }),
        tab2 === "deps" && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: 20 }, children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-head", children: /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Dependencies" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              className: "hv-panel-body",
              style: { display: "flex", flexDirection: "column", gap: 6, fontFamily: "var(--hv-font-mono)", fontSize: 12 },
              children: loading ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonRow, {}),
                /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonRow, {})
              ] }) : deps && deps.length > 0 ? deps.map((depId) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.link, { size: 10 }),
                " ",
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", children: depId })
              ] }, depId)) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "muted", children: "No dependencies." })
            }
          )
        ] }) }),
        tab2 === "pipeline" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", flexDirection: "column" }, children: [
          loading && !pipelineForView ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { padding: 20 }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonRow, {}),
            /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonRow, {}),
            /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonRow, {})
          ] }) : pipelineForView ? /* @__PURE__ */ jsxRuntimeExports.jsx(
            StagePipeline,
            {
              pipeline: pipelineForView,
              focusedId: focusedStageId,
              onPick: handlePickStage
            }
          ) : liveTask.type === "annotation_mission" || task.rawType === "annotation_mission" ? /* @__PURE__ */ jsxRuntimeExports.jsx(AnnotationStagePanel, { task: liveTask }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: 24, fontSize: 12, color: "var(--hv-fg-muted)" }, children: "No pipeline data yet — task has not emitted structured steps." }),
          isPushGateBlocked && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              "data-testid": "pipeline-push-gate-approval-banner",
              role: "region",
              "aria-label": "push_gate approval required",
              style: {
                display: "flex",
                alignItems: "center",
                gap: 16,
                padding: "16px 18px",
                margin: "6px 20px 0",
                background: "var(--hv-warn-bg, rgba(250,204,21,0.10))",
                border: "1px solid var(--hv-warn, #facc15)",
                borderRadius: 6,
                fontSize: 12,
                color: "var(--hv-fg, #e8eaf0)",
                flexWrap: "wrap"
              },
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { flex: "1 1 360px", minWidth: 0, lineHeight: 1.6 }, children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { color: "var(--hv-warn, #facc15)", fontWeight: 700, marginBottom: 4 }, children: "Operator approval required" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                    "Mission parked at ",
                    /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "push_gate" }),
                    " before push.",
                    pushGateCommitSha && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                      " ",
                      "Commit:",
                      " ",
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "code",
                        {
                          "data-testid": "pipeline-push-gate-commit-sha",
                          style: {
                            fontFamily: "var(--hv-font-mono, ui-monospace, monospace)",
                            fontSize: 11,
                            color: "var(--hv-fg, #e8eaf0)",
                            overflowWrap: "anywhere"
                          },
                          children: pushGateCommitSha
                        }
                      )
                    ] })
                  ] })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", gap: 8, flexWrap: "wrap", marginLeft: "auto" }, children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "button",
                    {
                      "data-testid": "pipeline-push-gate-view-diff-btn",
                      disabled: !canViewPushGateDiff,
                      "aria-disabled": !canViewPushGateDiff,
                      title: isFakeDispatchMission ? "Fake-dispatch missions do not create a real git commit." : void 0,
                      onClick: () => {
                        void handleViewPushGateDiff();
                      },
                      style: {
                        display: "inline-flex",
                        alignItems: "center",
                        gap: 6,
                        padding: "7px 12px",
                        background: "transparent",
                        color: "var(--hv-warn, #facc15)",
                        border: "1px solid var(--hv-warn, #facc15)",
                        borderRadius: 4,
                        fontSize: 12,
                        fontWeight: 600,
                        opacity: canViewPushGateDiff ? 1 : 0.6,
                        cursor: canViewPushGateDiff ? "pointer" : "not-allowed",
                        whiteSpace: "nowrap"
                      },
                      children: diffBusy ? "Loading diff…" : "View diff"
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "button",
                    {
                      "data-testid": "pipeline-push-gate-approve-btn",
                      disabled: resumeBusy,
                      "aria-disabled": resumeBusy,
                      onClick: () => {
                        void handleApprovePushGate();
                      },
                      style: {
                        display: "inline-flex",
                        alignItems: "center",
                        gap: 6,
                        padding: "7px 14px",
                        background: resumeBusy ? "var(--hv-border, #2a2f3a)" : "var(--hv-warn, #facc15)",
                        color: resumeBusy ? "var(--hv-fg-muted, #8c93a3)" : "var(--hv-bg, #0f1115)",
                        border: "none",
                        borderRadius: 4,
                        fontSize: 12,
                        fontWeight: 700,
                        cursor: resumeBusy ? "not-allowed" : "pointer",
                        whiteSpace: "nowrap"
                      },
                      children: resumeBusy ? "Approving…" : "Approve & continue"
                    }
                  )
                ] })
              ]
            }
          ),
          selectedStage && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel pipeline-stage-details", style: { margin: "12px 20px 0" }, "data-testid": "pipeline-stage-details", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: prettyStage(selectedStage.name) }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-panel-sub", style: { marginLeft: 8, fontSize: 11 }, children: [
                selectedStage.status,
                selectedStageRuns.length > 1 ? ` · loop ${(selectedRun == null ? void 0 : selectedRun.index) ?? ((_g = selectedStageRuns[0]) == null ? void 0 : _g.index)}/${selectedStageRuns.length}` : ""
              ] }),
              selectedStageRuns.length > 1 && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "select",
                {
                  value: selectedRunId,
                  onChange: (event) => setSelectedRunId(event.target.value),
                  style: {
                    marginLeft: "auto",
                    height: 28,
                    border: "1px solid var(--hv-border)",
                    borderRadius: 4,
                    background: "var(--hv-bg)",
                    color: "var(--hv-fg)",
                    fontSize: 12
                  },
                  "aria-label": "Pipeline stage run",
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "latest", children: "Latest run" }),
                    selectedStageRuns.map((run) => /* @__PURE__ */ jsxRuntimeExports.jsxs("option", { value: run.id, children: [
                      "Run ",
                      run.index,
                      " · seq ",
                      run.startSeq,
                      "-",
                      run.endSeq
                    ] }, run.id))
                  ]
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-body", style: { padding: 0 }, children: selectedStageEvidence.length > 0 ? (() => {
              const latest = selectedStageEvidence.slice(0, 8);
              const earlier = selectedStageEvidence.slice(8);
              const renderLine = (line) => {
                const message = splitEvidenceMessage(line.message);
                const row = /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-feed-row pipeline-stage-line", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-feed-t", children: line.createdAt ? formatLocalTime(line.createdAt) : "—" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-feed-src", children: line.kind }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "pipeline-stage-line-body", children: [
                    (line.runLabel || line.label) && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "pipeline-stage-line-meta mono", children: [
                      line.runLabel && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: line.runLabel }),
                      line.label && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: line.label })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "pipeline-stage-line-title", children: message.title }),
                    message.detail && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "pipeline-stage-line-detail", children: message.detail })
                  ] })
                ] });
                return line.onOpen ? /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    type: "button",
                    onClick: line.onOpen,
                    className: "pipeline-stage-line-button",
                    children: row
                  },
                  line.id
                ) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: row }, line.id);
              };
              return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "pipeline-stage-compact-head", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Latest output" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono", children: [
                    latest.length,
                    "/",
                    selectedStageEvidence.length
                  ] })
                ] }),
                latest.map(renderLine),
                earlier.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { className: "pipeline-stage-earlier", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("summary", { children: [
                    "Earlier output (",
                    earlier.length,
                    ")"
                  ] }),
                  earlier.map(renderLine)
                ] })
              ] });
            })() : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: 12, fontSize: 12, color: "var(--hv-fg-muted)" }, children: "No logs or step output recorded for this stage yet." }) })
          ] }),
          pipeline && pipeline.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { style: { margin: "12px 20px 20px" }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "summary",
              {
                style: {
                  cursor: "pointer",
                  fontSize: 12,
                  color: "var(--hv-fg-muted)",
                  padding: "4px 0"
                },
                children: [
                  "Raw event log (",
                  pipeline.length,
                  ")"
                ]
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel", style: { marginTop: 8 }, children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: pipeline.map((ev, i) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "div",
              {
                className: "hv-feed-row",
                style: { gridTemplateColumns: "76px 12px 1fr auto" },
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-feed-t", children: ev.created_at ? formatLocalTime(ev.created_at) : "—" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-feed-dot", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { background: "var(--hv-info)" } }) }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-feed-body mono", style: { fontSize: 12 }, children: ev.worker_id ?? "system" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-feed-src", children: ev.type })
                ]
              },
              ev.id ?? i
            )) }) })
          ] })
        ] }),
        tab2 === "review" && /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            "data-testid": "review-tab",
            style: { padding: 20, display: "flex", flexDirection: "column", gap: 12 },
            children: [
              isPushGateBlocked && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "div",
                {
                  className: "hv-push-gate-cta",
                  "data-testid": "review-push-gate-approval-cta",
                  role: "region",
                  "aria-label": "Operator approval required",
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-push-gate-icon", "aria-hidden": "true", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.approval, { size: 22 }) }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-push-gate-copy", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-push-gate-kicker", children: "Awaiting operator action" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Approve the push to continue" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: "Adversarial review is complete. This mission is parked at push_gate until an operator approves the recorded commit." }),
                      pushGateCommitSha && /* @__PURE__ */ jsxRuntimeExports.jsx("code", { "data-testid": "review-push-gate-commit-sha", children: pushGateCommitSha })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-push-gate-actions", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs(
                        "button",
                        {
                          className: "hv-push-gate-primary",
                          "data-testid": "review-push-gate-approve-btn",
                          disabled: resumeBusy,
                          "aria-disabled": resumeBusy,
                          onClick: () => {
                            void handleApprovePushGate();
                          },
                          children: [
                            /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.check, { size: 14 }),
                            resumeBusy ? "Approving..." : "Approve & continue"
                          ]
                        }
                      ),
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "button",
                        {
                          className: "hv-push-gate-secondary",
                          "data-testid": "review-push-gate-view-diff-btn",
                          disabled: !canViewPushGateDiff,
                          "aria-disabled": !canViewPushGateDiff,
                          title: isFakeDispatchMission ? "Fake-dispatch missions do not create a real git commit." : void 0,
                          onClick: () => {
                            void handleViewPushGateDiff();
                          },
                          children: diffBusy ? "Loading diff..." : "View diff"
                        }
                      )
                    ] })
                  ]
                }
              ),
              isMergeConflictBlocked && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "div",
                {
                  "data-testid": "review-merge-conflict-banner",
                  role: "region",
                  "aria-label": "Merge conflict — operator action required",
                  style: {
                    display: "flex",
                    flexDirection: "column",
                    gap: 10,
                    padding: "16px 18px",
                    background: "var(--hv-error-bg, rgba(239,68,68,0.10))",
                    border: "1px solid var(--hv-error, #ef4444)",
                    borderRadius: 6,
                    fontSize: 12,
                    color: "var(--hv-fg, #e8eaf0)"
                  },
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { color: "var(--hv-error, #ef4444)", fontWeight: 700, fontSize: 13 }, children: "Merge conflict — manual resolution required" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { lineHeight: 1.6 }, children: [
                      mergeBlockedReason === "push_race" ? "Two concurrent merges raced and could not be reconciled automatically. " : "The mission branch could not be rebased onto origin/main automatically. ",
                      "Resolve the conflict in the worktree, then click ",
                      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Retry merge" }),
                      "."
                    ] }),
                    mergeWorktreePath && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { color: "var(--hv-fg-muted, #8c93a3)" }, children: "Worktree: " }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "code",
                        {
                          "data-testid": "review-merge-conflict-worktree-path",
                          style: {
                            fontFamily: "var(--hv-font-mono, ui-monospace, monospace)",
                            fontSize: 11,
                            color: "var(--hv-fg, #e8eaf0)",
                            overflowWrap: "anywhere"
                          },
                          children: mergeWorktreePath
                        }
                      )
                    ] }),
                    mergeConflictFiles.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { color: "var(--hv-fg-muted, #8c93a3)", marginBottom: 4 }, children: [
                        "Conflicted files (",
                        mergeConflictFiles.length,
                        "):"
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("ul", { style: { margin: 0, paddingLeft: 16 }, children: [
                        mergeConflictFiles.slice(0, 10).map((f) => /* @__PURE__ */ jsxRuntimeExports.jsx("li", { children: /* @__PURE__ */ jsxRuntimeExports.jsx("code", { style: { fontSize: 11 }, children: f }) }, f)),
                        mergeConflictFiles.length > 10 && /* @__PURE__ */ jsxRuntimeExports.jsxs("li", { style: { color: "var(--hv-fg-muted, #8c93a3)" }, children: [
                          "… and ",
                          mergeConflictFiles.length - 10,
                          " more"
                        ] })
                      ] })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "button",
                      {
                        "data-testid": "review-merge-conflict-retry-btn",
                        disabled: resumeBusy,
                        "aria-disabled": resumeBusy,
                        onClick: () => {
                          void handleApprovePushGate();
                        },
                        style: {
                          display: "inline-flex",
                          alignItems: "center",
                          gap: 6,
                          padding: "7px 14px",
                          background: resumeBusy ? "var(--hv-border, #2a2f3a)" : "var(--hv-error, #ef4444)",
                          color: resumeBusy ? "var(--hv-fg-muted, #8c93a3)" : "#fff",
                          border: "none",
                          borderRadius: 4,
                          fontSize: 12,
                          fontWeight: 700,
                          cursor: resumeBusy ? "not-allowed" : "pointer",
                          whiteSpace: "nowrap"
                        },
                        children: resumeBusy ? "Retrying…" : "Retry merge"
                      }
                    ) })
                  ]
                }
              ),
              !isPushGateBlocked && !isMergeConflictBlocked && (reviewActivityLive || blockedOn === "human_review" || liveStatus === "running") && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "div",
                {
                  className: `hv-run-signal${reviewActivityLive ? " is-running" : noActiveWorker || staleRunning ? " is-stale" : " is-paused"}`,
                  "data-testid": "review-run-signal",
                  role: "status",
                  "aria-live": "polite",
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-workbeat", "aria-hidden": "true", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", {}),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", {}),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", {}),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", {})
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-run-main", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-run-title", children: runSignalTitle }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-run-sub", children: runSignalSub })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-run-meta", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Stage" }),
                        prettyStage(currentStage)
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Worker" }),
                        worker ? `${worker.name} · ${reviewActivityLive ? "attached" : worker.status}` : "none attached"
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Last activity" }),
                        latestActivityKind,
                        " · ",
                        relativeActivityTime(latestActivityMs, nowMs)
                      ] })
                    ] })
                  ]
                }
              ),
              !isPushGateBlocked && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-runtime-proof", "data-testid": "review-runtime-proof", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-proof-item", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Orchestrator" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: runtimeProof.orchestrator ? `${runtimeProof.orchestrator.designation ?? runtimeProof.orchestrator.id} · ${runtimeProof.orchestrator.status}` : "none active" })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-proof-item", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Heartbeat" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: ((_h = runtimeProof.orchestrator) == null ? void 0 : _h.last_heartbeat) ? relativeActivityTime(parseHiveTimestamp(runtimeProof.orchestrator.last_heartbeat), nowMs) : "no heartbeat" })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-proof-item", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Active subagent" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: runtimeProof.subagent ? `${runtimeProof.subagent.designation ?? runtimeProof.subagent.id} · ${runtimeProof.subagent.status}` : "none active" })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-proof-item", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Newest result" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: runtimeProof.newestResult ? `${runtimeProof.newestResult.worker_id ?? "worker"} · ${relativeActivityTime(parseHiveTimestamp(runtimeProof.newestResult.created_at), nowMs)}` : "none yet" })
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-evidence-panel", "data-testid": "review-evidence-panel", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-evidence-head", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Live work output" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "task-scoped console lines, results, and file artifacts" })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-evidence-count", children: [
                    evidenceLines.length,
                    " lines · ",
                    producedWork.length,
                    " outputs"
                  ] })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-evidence-grid", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-terminal-card", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-terminal-top", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "dot pulse" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "terminal tail" })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-terminal-lines", role: "log", "aria-label": "Mission console output", children: loading && evidenceLines.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-terminal-empty", children: "loading task output..." }) : evidenceLines.length > 0 ? evidenceLines.map((line, i) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "div",
                      {
                        className: `hv-terminal-line ${logLevelClass(line.level)}`,
                        "data-testid": "review-terminal-line",
                        "data-source": line.source,
                        children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-terminal-time", children: relativeActivityTime(parseHiveTimestamp(line.createdAt), nowMs) }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-terminal-level", children: line.level }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-terminal-msg", children: line.message })
                        ]
                      },
                      `${line.id}-${line.createdAt ?? i}`
                    )) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-terminal-empty", children: "no task logs or events yet; waiting for worker output..." }) })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-output-card", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-terminal-top", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.fileAdd, { size: 12 }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "produced work" })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-output-list", children: producedWork.length > 0 ? producedWork.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "button",
                      {
                        className: "hv-output-row",
                        onClick: item.onOpen,
                        "data-testid": "review-produced-work",
                        children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-output-kind", children: formatOutputKind(item.kind) }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-output-label", children: item.label }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-output-time", children: relativeActivityTime(parseHiveTimestamp(item.createdAt), nowMs) })
                        ]
                      },
                      `${item.kind}-${item.id}`
                    )) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-terminal-empty", children: "no results or file artifacts produced yet" }) })
                  ] })
                ] })
              ] }),
              archive.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Annotations being addressed" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-panel-sub", style: { marginLeft: 8, fontSize: 11 }, children: archive.length })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-body", style: { fontSize: 12, lineHeight: 1.55 }, children: /* @__PURE__ */ jsxRuntimeExports.jsx("ul", { style: { margin: 0, paddingLeft: 18 }, children: archive.map((a) => /* @__PURE__ */ jsxRuntimeExports.jsxs("li", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", style: { color: "var(--hv-fg-muted)" }, children: a.id }),
                  " ",
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { style: { fontWeight: 600 }, children: [
                    "[",
                    a.severity,
                    "]"
                  ] }),
                  " ",
                  a.body
                ] }, a.id)) }) })
              ] }),
              latestReviewResult && (() => {
                const reviewPayload = latestReviewResult.payload;
                const reviewWeaknesses = Array.isArray(reviewPayload == null ? void 0 : reviewPayload.weaknesses) ? reviewPayload.weaknesses : [];
                const interactiveFeedback = (blockedOn === "human_review" || feedbackInFlight) && reviewWeaknesses.length > 0;
                const reviewAttemptsTotal = sortedResults.filter((r) => {
                  const p = r.payload;
                  return p && Array.isArray(p.weaknesses);
                }).length;
                const hasReviewOutcomesArr = Array.isArray(payload.review_outcomes);
                const reviewOutcomesArr = hasReviewOutcomesArr ? payload.review_outcomes : [];
                const attemptsThisCycle = hasReviewOutcomesArr ? reviewOutcomesArr.length : reviewAttemptsTotal;
                return /* @__PURE__ */ jsxRuntimeExports.jsx(
                  ReviewFeedbackPanel,
                  {
                    taskId: task.id,
                    result: latestReviewResult,
                    weaknesses: reviewWeaknesses,
                    annLookup,
                    interactive: interactiveFeedback,
                    draftsRef,
                    attemptsThisCycle,
                    attemptsTotal: reviewAttemptsTotal,
                    onSubmittingChange: setFeedbackInFlight,
                    onSubmitted: () => {
                      setReviewLoopPendingUntil(Date.now() + 15e3);
                      const cur = useTaskStore.getState().tasks[task.id] ?? liveTask;
                      if (!cur) return;
                      const curPayload = cur.payload ?? {};
                      const nextPayload = { ...curPayload };
                      delete nextPayload.blocked_on;
                      if (Array.isArray(nextPayload.warnings)) {
                        nextPayload.warnings = nextPayload.warnings.filter((w) => w !== "review_loops_exceeded" && w !== "needs_human_review");
                      }
                      useTaskStore.getState().setTask(task.id, {
                        ...cur,
                        status: cur.status === "blocked" ? "running" : cur.status,
                        payload: nextPayload
                      });
                      window.setTimeout(() => {
                        api.tasks.get(task.id).then((fresh) => {
                          useTaskStore.getState().setTask(task.id, fresh);
                        }).catch(() => {
                        });
                      }, 600);
                    }
                  }
                );
              })(),
              latestPlanResult && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", "data-testid": "review-tab-plan", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Latest plan attempt" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-panel-sub", style: { marginLeft: 8, fontSize: 11 }, children: [
                    latestPlanResult.id,
                    " ·",
                    " ",
                    latestPlanResult.created_at ? formatLocalDateTime(latestPlanResult.created_at) : "—"
                  ] })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-body", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                  ResultPayloadView,
                  {
                    payload: latestPlanResult.payload,
                    annLookup
                  }
                ) })
              ] }),
              !latestPlanResult && !latestReviewResult && archive.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { fontSize: 12, color: "var(--hv-fg-muted)" }, children: "No review context available yet." }),
              (() => {
                const excludedIds = new Set(
                  [latestPlanResult == null ? void 0 : latestPlanResult.id, latestReviewResult == null ? void 0 : latestReviewResult.id].filter(
                    (x) => typeof x === "string"
                  )
                );
                const earlier = sortedResults.filter((r) => !excludedIds.has(r.id));
                if (earlier.length === 0) return null;
                return /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs(
                    "summary",
                    {
                      style: { cursor: "pointer", fontSize: 12, color: "var(--hv-fg-muted)", padding: "4px 0" },
                      children: [
                        "Earlier review iterations (",
                        earlier.length,
                        ")"
                      ]
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { display: "flex", flexDirection: "column", gap: 6, marginTop: 6 }, children: earlier.map((r) => {
                    var _a2;
                    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "button",
                      {
                        onClick: () => setDrawerTarget({ kind: "result", result: r, annLookup }),
                        style: {
                          textAlign: "left",
                          padding: "6px 10px",
                          border: "1px solid var(--hv-border)",
                          borderRadius: 4,
                          background: "none",
                          color: "inherit",
                          cursor: "pointer",
                          fontSize: 12
                        },
                        children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", style: { color: "var(--hv-fg-muted)" }, children: r.id }),
                          " · ",
                          ((_a2 = r.payload) == null ? void 0 : _a2.verdict) ? String(r.payload.verdict) : "no verdict",
                          " · ",
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { color: "var(--hv-fg-muted)" }, children: r.created_at ? formatLocalTime(r.created_at) : "—" })
                        ]
                      },
                      r.id
                    );
                  }) })
                ] });
              })()
            ]
          }
        ),
        tab2 === "artifacts" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { padding: 20, display: "flex", flexDirection: "column", gap: 12 }, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Results" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-panel-sub", style: { marginLeft: 8, fontSize: 11 }, children: [
                sortedResults.length,
                " worker report",
                sortedResults.length === 1 ? "" : "s",
                " — click to view"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              loading ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { padding: "8px 14px" }, children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonRow, {}),
                /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonRow, {})
              ] }) : visibleResults.length > 0 ? visibleResults.map((result) => {
                const payload2 = result.payload;
                const verdict = payload2 && typeof payload2.verdict === "string" ? payload2.verdict : null;
                const summary = resultSummary(result).slice(0, 140);
                return /* @__PURE__ */ jsxRuntimeExports.jsxs(
                  "button",
                  {
                    onClick: () => setDrawerTarget({ kind: "result", result, annLookup }),
                    "aria-label": `Open result ${result.id}`,
                    "data-testid": `result-row-${result.id}`,
                    style: {
                      display: "grid",
                      gridTemplateColumns: "120px 64px 1fr 80px",
                      gap: 10,
                      padding: "8px 14px",
                      width: "100%",
                      background: "none",
                      border: "none",
                      borderBottom: "1px solid var(--hv-border)",
                      textAlign: "left",
                      cursor: "pointer",
                      color: "inherit",
                      alignItems: "center"
                    },
                    onMouseEnter: (e) => e.currentTarget.style.background = "var(--hv-hover, rgba(255,255,255,0.04))",
                    onMouseLeave: (e) => e.currentTarget.style.background = "none",
                    children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", style: { fontSize: 12 }, children: result.id }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { fontSize: 11 }, children: verdict ? /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "span",
                        {
                          style: {
                            padding: "1px 6px",
                            borderRadius: 3,
                            background: verdict.toUpperCase() === "REVISE" ? "rgba(239,91,91,0.15)" : "rgba(74,222,128,0.15)",
                            color: verdict.toUpperCase() === "REVISE" ? "var(--hv-danger, #ef5b5b)" : "var(--hv-success, #4ade80)",
                            fontWeight: 700
                          },
                          children: verdict
                        }
                      ) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { color: "var(--hv-fg-muted)" }, children: "—" }) }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { fontSize: 12, color: "var(--hv-fg-subtle)", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }, children: summary }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", style: { fontSize: 11, color: "var(--hv-muted)" }, children: result.created_at ? formatLocalTime(result.created_at) : "—" })
                    ]
                  },
                  result.id
                );
              }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: "8px 14px", fontSize: 12, color: "var(--hv-muted)" }, children: "No worker results yet." }),
              hiddenResultCount > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "button",
                {
                  onClick: () => setShowAllResults(true),
                  "data-testid": "results-show-more",
                  style: {
                    width: "100%",
                    padding: "8px 14px",
                    background: "none",
                    border: "none",
                    borderTop: "1px solid var(--hv-border)",
                    color: "var(--hv-info, #4aa3ff)",
                    cursor: "pointer",
                    fontSize: 12,
                    textAlign: "center"
                  },
                  children: [
                    "Show ",
                    hiddenResultCount,
                    " more"
                  ]
                }
              )
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Files" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-panel-sub", style: { marginLeft: 8, fontSize: 11 }, children: [
                artifactFileGroups.length,
                " file",
                artifactFileGroups.length === 1 ? "" : "s",
                totalArtifactEvents !== artifactFileGroups.length ? ` · ${totalArtifactEvents} write event${totalArtifactEvents === 1 ? "" : "s"}` : "",
                " — click to view"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              loading ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { padding: "8px 14px" }, children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonRow, {}),
                /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonRow, {})
              ] }) : visibleArtifacts.length > 0 ? visibleArtifacts.map((group, i) => {
                const fe = group.latest;
                const renderable = isRenderableFile(fe.path);
                return /* @__PURE__ */ jsxRuntimeExports.jsxs(
                  "button",
                  {
                    onClick: () => {
                      if (renderable) {
                        setDrawerTarget({ kind: "file_event", taskId: task.id, event: fe });
                      }
                    },
                    disabled: !renderable,
                    title: renderable ? `Preview ${fe.path}` : "Binary file — open path manually",
                    "aria-label": renderable ? `Preview file: ${fe.path}` : `Binary file: ${fe.path}`,
                    style: {
                      display: "grid",
                      gridTemplateColumns: "1fr 80px 80px",
                      padding: "8px 14px",
                      width: "100%",
                      background: "none",
                      border: "none",
                      borderBottom: "1px solid var(--hv-border)",
                      textAlign: "left",
                      cursor: renderable ? "pointer" : "not-allowed",
                      color: renderable ? "inherit" : "var(--hv-fg-muted)",
                      opacity: renderable ? 1 : 0.6
                    },
                    onMouseEnter: (e) => {
                      if (renderable) {
                        e.currentTarget.style.background = "var(--hv-hover, rgba(255,255,255,0.04))";
                      }
                    },
                    onMouseLeave: (e) => e.currentTarget.style.background = "none",
                    children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono", style: { fontSize: 12 }, children: [
                        fe.path,
                        !renderable && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { marginLeft: 6, fontSize: 10, color: "var(--hv-fg-muted)" }, children: "(binary)" })
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", style: { fontSize: 11, color: "var(--hv-muted)" }, children: group.eventCount === 1 ? fe.event_type : `${group.eventCount} writes` }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", style: { fontSize: 11, color: "var(--hv-muted)" }, children: fe.created_at ? formatLocalTime(fe.created_at) : "—" })
                    ]
                  },
                  fe.id ?? i
                );
              }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: "8px 14px", fontSize: 12, color: "var(--hv-muted)" }, children: "No artifacts yet." }),
              hiddenArtifactCount > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "button",
                {
                  onClick: () => setShowAllFiles(true),
                  "data-testid": "files-show-more",
                  style: {
                    width: "100%",
                    padding: "8px 14px",
                    background: "none",
                    border: "none",
                    borderTop: "1px solid var(--hv-border)",
                    color: "var(--hv-info, #4aa3ff)",
                    cursor: "pointer",
                    fontSize: 12,
                    textAlign: "center"
                  },
                  children: [
                    "Show ",
                    hiddenArtifactCount,
                    " more"
                  ]
                }
              )
            ] })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ArtifactDrawer, { target: drawerTarget, onClose: closeDrawer })
      ]
    }
  );
}
const NON_RENDERABLE_EXT = /* @__PURE__ */ new Set([
  "pkl",
  "parquet",
  "arrow",
  "h5",
  "pt",
  "safetensors",
  "onnx",
  "bin",
  "png",
  "jpg",
  "jpeg",
  "gif",
  "webp",
  "pdf",
  "mp4",
  "mov",
  "wav",
  "mp3",
  "zip",
  "gz",
  "tar",
  "tgz",
  "whl"
]);
function isRenderableFile(path2) {
  const m = path2.match(/\.([a-zA-Z0-9]+)$/);
  if (!m) return true;
  return !NON_RENDERABLE_EXT.has(m[1].toLowerCase());
}
function Worktrees() {
  const params = useParams();
  const [worktrees, setWorktrees] = reactExports.useState([]);
  const [loadingList, setLoadingList] = reactExports.useState(true);
  const [listError, setListError] = reactExports.useState(null);
  const [selectedId, setSelectedId] = reactExports.useState(null);
  const [diffFiles, setDiffFiles] = reactExports.useState([]);
  const [loadingDiff, setLoadingDiff] = reactExports.useState(false);
  const [mergePending, setMergePending] = reactExports.useState(false);
  const [abortPending, setAbortPending] = reactExports.useState(false);
  const [rebasePending, setRebasePending] = reactExports.useState(false);
  const [confirmMerge, setConfirmMerge] = reactExports.useState(false);
  const pushToast = useToastStore((s) => s.push);
  reactExports.useEffect(() => {
    const controller = new AbortController();
    async function fetchWorktrees() {
      setLoadingList(true);
      setListError(null);
      try {
        const body = await api.worktrees.list();
        setWorktrees(body.worktrees ?? []);
      } catch (err) {
        if (err instanceof Error && err.name === "AbortError") return;
        const msg = err instanceof Error ? err.message : String(err);
        setListError(msg);
      } finally {
        setLoadingList(false);
      }
    }
    void fetchWorktrees();
    return () => controller.abort();
  }, []);
  reactExports.useEffect(() => {
    if (params.worktreeId && worktrees.length > 0) {
      const found = worktrees.find((w) => w.id === params.worktreeId);
      if (found && selectedId !== found.id) {
        setSelectedId(found.id);
      } else if (!found && selectedId === null) {
        setSelectedId(worktrees[0].id);
      }
    } else if (!params.worktreeId && worktrees.length > 0 && selectedId === null) {
      setSelectedId(worktrees[0].id);
    }
  }, [params.worktreeId, worktrees, selectedId]);
  reactExports.useEffect(() => {
    if (!selectedId) return;
    const controller = new AbortController();
    async function fetchDiff() {
      setLoadingDiff(true);
      setDiffFiles([]);
      try {
        const body = await api.worktrees.diff(selectedId);
        setDiffFiles(body.files ?? []);
        tracer("CAP-13", "ok");
      } catch (err) {
        if (err instanceof Error && err.name === "AbortError") return;
        const msg = err instanceof Error ? err.message : String(err);
        pushToast({ type: "error", title: "Diff load failed", message: msg });
      } finally {
        setLoadingDiff(false);
      }
    }
    void fetchDiff();
    return () => controller.abort();
  }, [selectedId, pushToast]);
  async function runAction(action, setPending) {
    if (!selectedId) return;
    setPending(true);
    try {
      const body = await api.worktrees.action(selectedId, action);
      if (body.ok === false) {
        const errMsg = body.error ?? `${action} failed`;
        pushToast({
          type: "error",
          title: `${action.charAt(0).toUpperCase() + action.slice(1)} failed`,
          message: errMsg
        });
      }
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      pushToast({
        type: "error",
        title: `${action.charAt(0).toUpperCase() + action.slice(1)} error`,
        message: msg
      });
    } finally {
      setPending(false);
    }
  }
  function handleMergeConfirm() {
    setConfirmMerge(false);
    void runAction("merge", setMergePending);
  }
  function handleAbort() {
    void runAction("abort", setAbortPending);
  }
  function handleRebase() {
    void runAction("rebase", setRebasePending);
  }
  const selectedWorktree = worktrees.find((w) => w.id === selectedId) ?? null;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view nopad", style: { display: "flex", flexDirection: "column" }, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { fontSize: 20, fontWeight: 600 }, children: "Worktrees" }),
        selectedWorktree && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { fontSize: 12.5, color: "var(--hv-fg-muted)", fontFamily: "var(--hv-font-mono)" }, children: [
          selectedWorktree.branch,
          " · ",
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", children: selectedWorktree.commit }),
          selectedWorktree.ahead > 0 && ` · ${selectedWorktree.ahead} ahead`,
          selectedWorktree.behind > 0 && ` · ${selectedWorktree.behind} behind`
        ] })
      ] }),
      selectedWorktree && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            size: "sm",
            onClick: handleAbort,
            disabled: abortPending || mergePending || rebasePending,
            "data-testid": "worktree-abort-btn",
            children: abortPending ? "Aborting…" : "Abort"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            size: "sm",
            leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.refresh, { size: 12 }),
            onClick: handleRebase,
            disabled: rebasePending || mergePending || abortPending,
            "data-testid": "worktree-rebase-btn",
            children: rebasePending ? "Rebasing…" : "Rebase"
          }
        ),
        confirmMerge ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { fontSize: 12, color: "var(--hv-fg-muted)", alignSelf: "center" }, children: "Merge into main?" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Btn,
            {
              size: "sm",
              variant: "subtle",
              onClick: () => setConfirmMerge(false),
              disabled: mergePending,
              children: "Cancel"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Btn,
            {
              size: "sm",
              variant: "primary",
              leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.check, { size: 12 }),
              onClick: handleMergeConfirm,
              disabled: mergePending,
              "data-testid": "worktree-merge-btn",
              children: mergePending ? "Merging…" : "Confirm merge"
            }
          )
        ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(
          Btn,
          {
            size: "sm",
            variant: "primary",
            leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.check, { size: 12 }),
            onClick: () => setConfirmMerge(true),
            disabled: mergePending || abortPending || rebasePending,
            "data-testid": "worktree-merge-btn",
            children: "Merge"
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-wt", style: { margin: "0 20px 20px", flex: 1 }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-wt-tree", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-wt-tree-head", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.worktree, { size: 12 }),
          loadingList ? "Loading…" : `${worktrees.length} worktree${worktrees.length !== 1 ? "s" : ""}`
        ] }),
        listError && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: "8px 12px", color: "var(--hv-danger)", fontSize: 12 }, children: listError }),
        !loadingList && worktrees.length === 0 && !listError && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: "8px 12px", color: "var(--hv-fg-muted)", fontSize: 12 }, children: "No worktrees found." }),
        worktrees.map((wt) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            className: `hv-wt-tree-item${selectedId === wt.id ? " sel" : ""}`,
            onClick: () => {
              setSelectedId(wt.id);
              setConfirmMerge(false);
            },
            role: "button",
            tabIndex: 0,
            "aria-current": selectedId === wt.id ? "true" : void 0,
            "data-testid": `worktree-item-${wt.id}`,
            onKeyDown: (e) => {
              if (e.key === "Enter" || e.key === " ") {
                setSelectedId(wt.id);
                setConfirmMerge(false);
              }
            },
            children: [
              wt.status === "conflict" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.alert, { size: 11, style: { color: "var(--hv-warn)", flexShrink: 0 } }) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { width: 11, display: "inline-block" } }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { style: { overflow: "hidden", textOverflow: "ellipsis", flex: 1 }, children: wt.branch }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "span",
                {
                  className: `hv-wt-tag ${wt.status === "conflict" ? "M" : wt.status === "modified" ? "M" : ""}`,
                  style: { fontSize: 10 },
                  children: wt.status
                }
              )
            ]
          },
          wt.id
        ))
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-wt-diff", "data-testid": "worktree-diff-panel", children: [
        loadingDiff && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: 16, color: "var(--hv-fg-muted)", fontSize: 12 }, children: "Loading diff…" }),
        !loadingDiff && !selectedWorktree && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: 16, color: "var(--hv-fg-muted)", fontSize: 12 }, children: "Select a worktree to view its diff." }),
        !loadingDiff && selectedWorktree && diffFiles.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-wt-diff-head", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.check, { size: 13, style: { color: "var(--hv-success)" } }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
            "No changed files — worktree is ",
            selectedWorktree.status,
            "."
          ] })
        ] }),
        !loadingDiff && diffFiles.map((f, i) => /* @__PURE__ */ jsxRuntimeExports.jsx(DiffFileRow, { file: f }, `${f.path}-${i}`))
      ] })
    ] })
  ] });
}
function DiffFileRow({ file }) {
  const tagClass = file.tag === "A" ? "A" : file.tag === "D" ? "D" : "M";
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-wt-diff-head", style: { borderBottom: "1px solid var(--hv-border)" }, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.fileAdd, { size: 13 }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mono", children: file.path }),
    file.conflict && /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "warn", dot: true, children: "conflict" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `hv-wt-tag ${tagClass}`, style: { marginLeft: 4 }, children: file.tag }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mono muted", style: { marginLeft: "auto", fontSize: 11 }, children: [
      file.additions > 0 && `+${file.additions}`,
      file.additions > 0 && file.deletions > 0 && " / ",
      file.deletions > 0 && `−${file.deletions}`
    ] })
  ] });
}
const ENGINES_FALLBACK = ["claude", "codex", "gemini"];
const MAX_MISSION_TITLE_LEN = 200;
let _cap41Emitted = false;
let _cap48Emitted = false;
const _testDispatchEnabled = false;
const DISPATCH_MODES = ["spawn", "none"];
const MAX_STANDING_ORDERS_LEN = 2e3;
const POLL_INTERVAL_MS$1 = 5e3;
function formatUptime(seconds) {
  const s = Math.floor(seconds);
  if (s < 60) return `${s}s`;
  const m = Math.floor(s / 60);
  if (s < 3600) return `${m}m ${s % 60}s`;
  const h = Math.floor(m / 60);
  return `${h}h ${m % 60}m`;
}
function formatBytes(bytes) {
  if (bytes >= 1073741824) return `${(bytes / 1073741824).toFixed(1)} GB`;
  if (bytes >= 1048576) return `${(bytes / 1048576).toFixed(1)} MB`;
  return `${(bytes / 1024).toFixed(1)} KB`;
}
function HeartbeatTimeline() {
  const workersMap = useWorkerStore((s) => s.workers);
  const now = Date.now();
  const rows = Object.values(workersMap).slice().sort((a, b) => {
    const ta = a.last_heartbeat ? parseUtc(a.last_heartbeat).getTime() : 0;
    const tb = b.last_heartbeat ? parseUtc(b.last_heartbeat).getTime() : 0;
    return tb - ta;
  }).slice(0, 10).map((w) => {
    const hbMs = w.last_heartbeat ? parseUtc(w.last_heartbeat).getTime() : 0;
    const ageSec = hbMs > 0 ? Math.floor((now - hbMs) / 1e3) : null;
    const agoStr = ageSec === null ? "—" : ageSec < 60 ? `${ageSec}s` : `${Math.floor(ageSec / 60)}m ${ageSec % 60}s`;
    const hbStatus = w.status === "dead" ? "dead" : ageSec !== null && ageSec > 60 ? "stale" : "ok";
    return { id: w.id, name: w.id, role: w.role, status: hbStatus, ago: agoStr, seed: w.id.charCodeAt(0) };
  });
  function pattern(status, seed) {
    return Array.from({ length: 60 }, (_, i) => {
      const r = seed * (i + 1) * 1103515245 + 12345 & 2147483647;
      if (status === "dead") return i < 48 ? "ok" : "miss";
      if (status === "stale") {
        if (i < 18) return "ok";
        if (i < 22) return "warn";
        return r % 7 === 0 ? "warn" : "ok";
      }
      return r % 53 === 0 ? "warn" : "ok";
    });
  }
  if (rows.length === 0) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-hb-timeline", style: { padding: "12px 0", color: "var(--hv-muted)", fontSize: 12 }, children: "No workers registered" });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-hb-timeline", children: rows.map((w, idx) => {
    const beats = pattern(w.status, idx + 7);
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-hbt-row", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-hbt-name", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `hv-hbt-dot${w.status === "stale" ? " warn" : w.status === "dead" ? " fail" : ""}` }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: w.name }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "subtle mono", style: { fontSize: 10, marginLeft: 4 }, children: w.role })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-hbt-track", children: beats.map((b, i) => /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `hv-hbt-beat${b === "ok" ? "" : ` ${b}`}` }, i)) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `hv-hbt-ago${w.status === "stale" ? " stale" : w.status === "dead" ? " dead" : ""}`, children: w.ago })
    ] }, w.id);
  }) });
}
function System() {
  const health = useSystemStore((s) => s.health);
  const stats = useSystemStore((s) => s.stats);
  const activity = useSystemStore((s) => s.activity);
  const startPolling = useSystemStore((s) => s.startPolling);
  const pollHealth = useSystemStore((s) => s.pollHealth);
  const pollStats = useSystemStore((s) => s.pollStats);
  const fetchActivity = useSystemStore((s) => s.fetchActivity);
  reactExports.useEffect(() => {
    const cleanup = startPolling(POLL_INTERVAL_MS$1);
    return cleanup;
  }, [startPolling]);
  function refreshSystem() {
    void pollHealth();
    void pollStats();
    void fetchActivity();
  }
  const uptimeDisplay = health ? formatUptime(health.uptime_s) : "—";
  const workerCount = stats ? stats.workers.total : health ? health.worker_count : 0;
  const dbSizeDisplay = stats ? formatBytes(stats.db_size_bytes) : "—";
  const costDisplay = stats ? `$${stats.cost_today_usd.toFixed(3)}` : "—";
  const apiStatus = health ? health.status : "unknown";
  const apiCls = apiStatus === "ok" ? "ok" : "bad";
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-view-summary", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-item", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "server health · heartbeats · activity" }) }) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-view-actions", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Btn,
        {
          size: "sm",
          leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.refresh, { size: 12 }),
          onClick: refreshSystem,
          "data-testid": "system-refresh-btn",
          children: "Refresh"
        }
      ) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sys-grid", style: { marginBottom: 16 }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `hv-sys-pill ${apiCls}`, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "l", children: "API" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "v", children: apiStatus })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sys-pill", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "l", children: "Uptime" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "v", "data-testid": "system-uptime", children: uptimeDisplay })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sys-pill", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "l", children: "Workers" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "v", "data-testid": "system-worker-count", children: workerCount })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sys-pill", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "l", children: "DB size" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "v", children: dbSizeDisplay })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sys-pill", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "l", children: "Cost today" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "v", children: costDisplay })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-sys-pill", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "l", children: "Event rate" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "v", children: stats ? `${stats.event_rate_per_sec.toFixed(1)}/s` : "—" })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16, flex: 1 }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Worker heartbeats" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-panel-sub", children: "top 10 · sorted by recency" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(HeartbeatTimeline, {}),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-hbt-legend", children: [["var(--hv-success)", "ok"], ["var(--hv-warn)", "late"], ["var(--hv-danger)", "failed"], ["var(--hv-border)", "missed"]].map(([c, l]) => /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-hbt-legend-item", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-hbt-legend-swatch", style: { background: c } }),
            l
          ] }, l)) })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Activity feed" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-panel-sub", children: "events · workers · tasks" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { "data-testid": "system-activity-feed", style: { maxHeight: 400, overflow: "auto" }, children: activity.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-feed-row", style: { color: "var(--hv-muted)", fontSize: 12, padding: "8px 0" }, children: "No recent activity" }) : activity.map((e, i) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-feed-row", style: { gridTemplateColumns: "50px 12px 1fr auto" }, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-feed-t", children: e.created_at ? formatLocalTime(e.created_at) : "—" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-feed-dot", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", {}) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-feed-body mono", style: { fontSize: 11.5 }, children: e.worker_id ?? "system" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-feed-src", children: e.type })
        ] }, e.id ?? i)) })
      ] })
    ] })
  ] });
}
function Onboard({ onSpawn }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onboard", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-onb-card", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onb-mark", "aria-hidden": "true" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onb-head", children: "Welcome to hive" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-onb-sub", children: [
      "Run a swarm of AI workers. Watch what they do. Steer when it matters.",
      /* @__PURE__ */ jsxRuntimeExports.jsx("br", {}),
      "You have zero workers online. Let's start your first one."
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-onb-steps", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-onb-step done", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onb-num", children: "1" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onb-step-title", children: "Server reachable" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onb-step-sub", children: "localhost:4848 · schema v41 · healthy" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.check, { size: 14, style: { color: "var(--hv-success)" } })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-onb-step", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onb-num", children: "2" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onb-step-title", children: "Spawn your first worker" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onb-step-sub", children: "Pick an engine and a role. Takes about 8 seconds." })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { variant: "primary", size: "sm", onClick: onSpawn, children: "Spawn" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-onb-step", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onb-num", children: "3" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onb-step-title", children: "Create a task" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onb-step-sub", children: "Describe the work. Hive will route it to the right cell." })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", disabled: true, children: "Locked" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-onb-step", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onb-num", children: "4" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onb-step-title", children: "Watch it run" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-onb-step-sub", children: "Logs stream live. Approvals surface in the rail." })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", disabled: true, children: "Locked" })
      ] })
    ] })
  ] }) });
}
function SpawnModal({ onClose }) {
  const [designation, setDesignation] = reactExports.useState("");
  const [standingOrders, setStandingOrders] = reactExports.useState("");
  const [dispatch, setDispatch] = reactExports.useState("spawn");
  const engines = useEngines();
  const [engine, setEngine] = reactExports.useState(engines[0] ?? ENGINES_FALLBACK[0]);
  reactExports.useEffect(() => {
    if (engines.length && !engines.includes(engine)) {
      setEngine(engines[0]);
    }
  }, [engines, engine]);
  const [loading, setLoading] = reactExports.useState(false);
  const [error, setError] = reactExports.useState(null);
  const hydrateWorkers = useWorkerStore((s) => s.hydrate);
  async function handleSubmit() {
    if (!designation.trim()) {
      setError("Designation is required");
      return;
    }
    if (standingOrders.length > MAX_STANDING_ORDERS_LEN) {
      setError(`Standing orders must be ${MAX_STANDING_ORDERS_LEN} characters or fewer`);
      return;
    }
    setLoading(true);
    setError(null);
    try {
      const taskBody = await api.tasks.create({
        type: "spawn-worker",
        payload: {
          designation: designation.trim(),
          standing_orders: standingOrders
        },
        // CAP-48 (audit-live F3): top-level engine field is what the backend
        // honours. Mirror MissionModal so the chosen engine actually routes
        // to _auto_spawn_for_task.
        engine,
        dispatch
      });
      onClose();
      await hydrateWorkers();
      const spawnMeta = taskBody.spawn;
      tracer("CAP-03", "ok", (spawnMeta == null ? void 0 : spawnMeta.worker_id) ?? "no-worker-id");
      if (!_cap48Emitted) {
        _cap48Emitted = true;
        tracer("CAP-48", "ok spawn-modal-engine-real engine=" + engine);
      }
    } catch (e) {
      setError(e instanceof Error ? e.message : "Failed to spawn worker");
    } finally {
      setLoading(false);
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className: "hv-scrim",
      onClick: onClose,
      role: "dialog",
      "aria-modal": true,
      "aria-labelledby": "spawn-title",
      "data-testid": "spawn-modal",
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-modal", onClick: (e) => e.stopPropagation(), children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-modal-head", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-modal-title", id: "spawn-title", children: "Spawn worker" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "muted", style: { fontSize: 12, marginTop: 2 }, children: "Starts a new session and registers it with the conductor." })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(IconBtn, { icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.close, { size: 14 }), onClick: onClose, label: "Close" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-modal-body", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("label", { htmlFor: "spawn-designation", children: "Designation" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                id: "spawn-designation",
                className: "hv-inp",
                type: "text",
                placeholder: "e.g. ops-researcher-01",
                value: designation,
                onChange: (e) => setDesignation(e.target.value),
                "data-testid": "spawn-designation-input",
                "aria-required": "true"
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("label", { children: "Engine" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-chips", children: engines.map((e) => /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                className: `hv-chip${engine === e ? " on" : ""}`,
                onClick: () => setEngine(e),
                children: e
              },
              e
            )) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("label", { htmlFor: "spawn-standing-orders", children: "Standing orders (optional)" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "textarea",
              {
                id: "spawn-standing-orders",
                className: "hv-inp",
                rows: 3,
                placeholder: "Extra context the worker should always carry. Leave empty to use the role default.",
                value: standingOrders,
                onChange: (e) => setStandingOrders(e.target.value),
                maxLength: MAX_STANDING_ORDERS_LEN
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("label", { htmlFor: "spawn-dispatch", children: "Dispatch mode" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "select",
              {
                id: "spawn-dispatch",
                className: "hv-inp",
                value: dispatch,
                onChange: (e) => setDispatch(e.target.value),
                "data-testid": "spawn-dispatch-select",
                children: DISPATCH_MODES.map((m) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: m, children: m === "spawn" ? "spawn — real subprocess (production)" : m === "fake" ? "fake — DB row only, no process (testing)" : "none — task row only, no worker" }, m))
              }
            )
          ] }),
          error && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              style: { color: "var(--hv-danger)", fontSize: 12, padding: "6px 0" },
              role: "alert",
              children: error
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-modal-foot", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-ft-meta", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.terminal, { size: 12 }),
            " ",
            "hive spawn --engine ",
            engine,
            " --dispatch ",
            dispatch
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", gap: 8 }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { variant: "subtle", onClick: onClose, disabled: loading, children: "Cancel" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Btn,
              {
                variant: "primary",
                onClick: () => {
                  void handleSubmit();
                },
                leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.zap, { size: 12 }),
                disabled: loading,
                "data-testid": "spawn-modal-submit",
                children: loading ? "Spawning…" : "Spawn"
              }
            )
          ] })
        ] })
      ] })
    }
  );
}
const TASK_ENGINES_FALLBACK = ["claude", "codex", "gemini"];
const TASK_ROLES_FALLBACK = ["conductor", "worker", "review", "codex", "gpu", "custom"];
let _cap45Emitted = false;
function useEngines() {
  const [engines, setEngines] = reactExports.useState([...TASK_ENGINES_FALLBACK]);
  reactExports.useEffect(() => {
    let cancelled = false;
    api.meta.engines().then((data) => {
      if (cancelled) return;
      const names = data.engines.map((e) => e.name);
      if (names.length > 0) {
        setEngines(names);
        if (!_cap45Emitted) {
          _cap45Emitted = true;
          tracer("CAP-45", "ok dynamic-engine-options count=" + names.length);
        }
      }
    }).catch(() => {
    });
    return () => {
      cancelled = true;
    };
  }, []);
  return engines;
}
function useRoles() {
  const [roles, setRoles] = reactExports.useState([...TASK_ROLES_FALLBACK]);
  reactExports.useEffect(() => {
    let cancelled = false;
    api.meta.roles().then((data) => {
      if (cancelled) return;
      const names = data.roles.map((r) => r.name);
      if (names.length > 0) {
        setRoles(names);
      }
    }).catch(() => {
    });
    return () => {
      cancelled = true;
    };
  }, []);
  return roles;
}
function MissionModal({ onClose, workers: _workers }) {
  const availableEngines = useEngines();
  const availableRoles = useRoles();
  const [title, setTitle] = reactExports.useState("");
  const [description, setDescription] = reactExports.useState("");
  const [priority, setPriority] = reactExports.useState(0);
  const [dispatch, setDispatch] = reactExports.useState("none");
  const [targetRole, setTargetRole] = reactExports.useState("");
  const [engine, setEngine] = reactExports.useState("claude");
  const [workerId, setWorkerId] = reactExports.useState("");
  const [isolation, setIsolation] = reactExports.useState("");
  const [baseRef, setBaseRef] = reactExports.useState("");
  const [maxBudgetUsd, setMaxBudgetUsd] = reactExports.useState("");
  const [timeoutS, setTimeoutS] = reactExports.useState("");
  const [loading, setLoading] = reactExports.useState(false);
  const [error, setError] = reactExports.useState(null);
  const hydrateTasks = useTaskStore((s) => s.hydrate);
  const showEngine = dispatch === "spawn" || dispatch === "queue" || dispatch === "assign";
  const showWorkerId = dispatch === "assign";
  const showIsolation = dispatch === "spawn";
  async function handleSubmit() {
    if (!title.trim()) {
      setError("Title is required");
      return;
    }
    setLoading(true);
    setError(null);
    try {
      const createBody = {
        type: title.trim(),
        description: description.trim() || void 0,
        priority,
        dispatch
      };
      if (targetRole) createBody.target_role = targetRole;
      if (showEngine && engine !== "claude") createBody.engine = engine;
      if (showWorkerId && workerId.trim()) createBody.worker_id = workerId.trim();
      if (showIsolation && isolation) createBody.isolation = isolation;
      if (showIsolation && baseRef.trim()) createBody.base_ref = baseRef.trim();
      const budgetNum = maxBudgetUsd.trim() ? parseFloat(maxBudgetUsd) : NaN;
      if (!isNaN(budgetNum) && budgetNum > 0) createBody.max_budget_usd = budgetNum;
      const timeoutNum = timeoutS.trim() ? parseInt(timeoutS, 10) : NaN;
      if (!isNaN(timeoutNum) && timeoutNum > 0) createBody.timeout_s = timeoutNum;
      const created = await api.tasks.create(createBody);
      onClose();
      await hydrateTasks();
      tracer("CAP-04", "ok", created.id);
      if (engine !== "claude" || targetRole) {
        tracer("CAP-35", "ok", created.id, "engine=" + engine, "role=" + targetRole);
      }
      if (dispatch === "spawn" && targetRole && targetRole !== "worker") {
        tracer("CAP-39", "ok", created.id, "role=" + targetRole);
      }
      if (!_cap41Emitted) {
        _cap41Emitted = true;
        tracer("CAP-41", "ok modal-api-client");
      }
    } catch (e) {
      setError(e instanceof Error ? e.message : "Failed to create mission");
    } finally {
      setLoading(false);
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      className: "hv-scrim",
      onClick: onClose,
      role: "dialog",
      "aria-modal": true,
      "aria-labelledby": "mission-title",
      "data-testid": "mission-modal",
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-modal modal-lg", onClick: (e) => e.stopPropagation(), children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-modal-head", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-modal-title", id: "mission-title", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.zap, { size: 14 }),
              " New mission"
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "muted", style: { fontSize: 12, marginTop: 2 }, children: [
              "One-off task. Runs once, then reports. For recurring work, create a ",
              /* @__PURE__ */ jsxRuntimeExports.jsx("em", { children: "duty" }),
              " instead."
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(IconBtn, { icon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.close, { size: 14 }), onClick: onClose, label: "Close" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-modal-body", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { htmlFor: "mission-title-input", children: [
              "Title",
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "muted", style: { fontSize: 10, marginLeft: 6 }, children: [
                "(stored as task type — max ",
                MAX_MISSION_TITLE_LEN,
                " chars)"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                id: "mission-title-input",
                className: "hv-inp",
                type: "text",
                placeholder: "e.g. Investigate heartbeat regression on ops-runner-03",
                value: title,
                onChange: (e) => setTitle(e.target.value),
                maxLength: MAX_MISSION_TITLE_LEN,
                "data-testid": "mission-title-input",
                "aria-required": "true"
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("label", { htmlFor: "mission-brief", children: "Brief" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "textarea",
              {
                id: "mission-brief",
                className: "hv-inp",
                rows: 4,
                placeholder: "What should the worker accomplish? Include context, success criteria, and any files or branches to reference.",
                value: description,
                onChange: (e) => setDescription(e.target.value)
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "help", children: "Markdown supported. Worker will receive this as its initial user message." })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("label", { children: "Priority" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-chips", children: [["low", -1], ["normal", 0], ["high", 1], ["urgent", 2]].map(([label, val]) => /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                className: `hv-chip${priority === val ? " on" : ""}`,
                onClick: () => setPriority(val),
                children: label
              },
              label
            )) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("label", { htmlFor: "mission-dispatch", children: "Dispatch mode" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "select",
              {
                id: "mission-dispatch",
                className: "hv-inp",
                value: dispatch,
                onChange: (e) => setDispatch(e.target.value),
                "data-testid": "mission-dispatch-select",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "none", children: "none — task row only, no worker" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "spawn", children: "spawn — start new worker subprocess" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "assign", children: "assign — route to existing worker by role or ID" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "queue", children: "queue — enqueue with role/engine affinity" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "conductor", children: "conductor — notify conductor inbox" }),
                  _testDispatchEnabled
                ]
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("label", { htmlFor: "mission-target-role", children: "Target role" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "select",
              {
                id: "mission-target-role",
                className: "hv-inp",
                value: targetRole,
                onChange: (e) => setTargetRole(e.target.value),
                "data-testid": "mission-target-role-select",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "— any role —" }),
                  availableRoles.map((r) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: r, children: r }, r))
                ]
              }
            )
          ] }),
          showEngine && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("label", { children: "Engine" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-chips", children: availableEngines.map((e) => /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                className: `hv-chip${engine === e ? " on" : ""}`,
                onClick: () => setEngine(e),
                "data-testid": `mission-engine-chip-${e}`,
                children: e
              },
              e
            )) })
          ] }),
          showWorkerId && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { htmlFor: "mission-worker-id", children: [
              "Worker ID",
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "muted", style: { fontSize: 10, marginLeft: 6 }, children: "(leave empty to use target role LRU routing)" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                id: "mission-worker-id",
                className: "hv-inp",
                type: "text",
                placeholder: "e.g. w-abc123 (optional)",
                value: workerId,
                onChange: (e) => setWorkerId(e.target.value),
                "data-testid": "mission-worker-id-input"
              }
            )
          ] }),
          showIsolation && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("label", { htmlFor: "mission-isolation", children: "Isolation" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "select",
                {
                  id: "mission-isolation",
                  className: "hv-inp",
                  value: isolation,
                  onChange: (e) => setIsolation(e.target.value),
                  "data-testid": "mission-isolation-select",
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "— none —" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "worktree", children: "worktree — git worktree per task" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "inplace", children: "inplace — work in repo root" })
                  ]
                }
              )
            ] }),
            isolation === "worktree" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("label", { htmlFor: "mission-base-ref", children: "Base ref" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "input",
                {
                  id: "mission-base-ref",
                  className: "hv-inp",
                  type: "text",
                  placeholder: "e.g. origin/main (default)",
                  value: baseRef,
                  onChange: (e) => setBaseRef(e.target.value),
                  "data-testid": "mission-base-ref-input"
                }
              )
            ] })
          ] }),
          (dispatch === "spawn" || dispatch === "assign") && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8 }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("label", { htmlFor: "mission-budget", children: "Max budget (USD)" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "input",
                {
                  id: "mission-budget",
                  className: "hv-inp",
                  type: "number",
                  min: "0",
                  step: "0.5",
                  placeholder: "2.00",
                  value: maxBudgetUsd,
                  onChange: (e) => setMaxBudgetUsd(e.target.value),
                  "data-testid": "mission-budget-input"
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-field", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("label", { htmlFor: "mission-timeout", children: "Timeout (s)" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "input",
                {
                  id: "mission-timeout",
                  className: "hv-inp",
                  type: "number",
                  min: "1",
                  step: "60",
                  placeholder: "600",
                  value: timeoutS,
                  onChange: (e) => setTimeoutS(e.target.value),
                  "data-testid": "mission-timeout-input"
                }
              )
            ] })
          ] }),
          error && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "div",
            {
              style: { color: "var(--hv-danger)", fontSize: 12, padding: "6px 0" },
              role: "alert",
              children: error
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-modal-foot", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-ft-meta mono", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.terminal, { size: 12 }),
            " hive mission create --dispatch ",
            dispatch,
            targetRole ? ` --role ${targetRole}` : "",
            showEngine && engine !== "claude" ? ` --engine ${engine}` : ""
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", gap: 8 }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { variant: "subtle", onClick: onClose, disabled: loading, children: "Cancel" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Btn,
              {
                variant: "primary",
                onClick: () => {
                  void handleSubmit();
                },
                leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.zap, { size: 12 }),
                disabled: loading,
                "data-testid": "mission-modal-submit",
                children: loading ? "Creating…" : "Create mission"
              }
            )
          ] })
        ] })
      ] })
    }
  );
}
function clamp$1(value, [min2, max2]) {
  return Math.min(max2, Math.max(min2, value));
}
function composeEventHandlers(originalEventHandler, ourEventHandler, { checkForDefaultPrevented = true } = {}) {
  return function handleEvent(event) {
    originalEventHandler == null ? void 0 : originalEventHandler(event);
    if (checkForDefaultPrevented === false || !event.defaultPrevented) {
      return ourEventHandler == null ? void 0 : ourEventHandler(event);
    }
  };
}
function createContextScope(scopeName, createContextScopeDeps = []) {
  let defaultContexts = [];
  function createContext3(rootComponentName, defaultContext) {
    const BaseContext = reactExports.createContext(defaultContext);
    const index2 = defaultContexts.length;
    defaultContexts = [...defaultContexts, defaultContext];
    const Provider = (props) => {
      var _a;
      const { scope, children, ...context } = props;
      const Context = ((_a = scope == null ? void 0 : scope[scopeName]) == null ? void 0 : _a[index2]) || BaseContext;
      const value = reactExports.useMemo(() => context, Object.values(context));
      return /* @__PURE__ */ jsxRuntimeExports.jsx(Context.Provider, { value, children });
    };
    Provider.displayName = rootComponentName + "Provider";
    function useContext2(consumerName, scope) {
      var _a;
      const Context = ((_a = scope == null ? void 0 : scope[scopeName]) == null ? void 0 : _a[index2]) || BaseContext;
      const context = reactExports.useContext(Context);
      if (context) return context;
      if (defaultContext !== void 0) return defaultContext;
      throw new Error(`\`${consumerName}\` must be used within \`${rootComponentName}\``);
    }
    return [Provider, useContext2];
  }
  const createScope = () => {
    const scopeContexts = defaultContexts.map((defaultContext) => {
      return reactExports.createContext(defaultContext);
    });
    return function useScope(scope) {
      const contexts = (scope == null ? void 0 : scope[scopeName]) || scopeContexts;
      return reactExports.useMemo(
        () => ({ [`__scope${scopeName}`]: { ...scope, [scopeName]: contexts } }),
        [scope, contexts]
      );
    };
  };
  createScope.scopeName = scopeName;
  return [createContext3, composeContextScopes(createScope, ...createContextScopeDeps)];
}
function composeContextScopes(...scopes) {
  const baseScope = scopes[0];
  if (scopes.length === 1) return baseScope;
  const createScope = () => {
    const scopeHooks = scopes.map((createScope2) => ({
      useScope: createScope2(),
      scopeName: createScope2.scopeName
    }));
    return function useComposedScopes(overrideScopes) {
      const nextScopes = scopeHooks.reduce((nextScopes2, { useScope, scopeName }) => {
        const scopeProps = useScope(overrideScopes);
        const currentScope = scopeProps[`__scope${scopeName}`];
        return { ...nextScopes2, ...currentScope };
      }, {});
      return reactExports.useMemo(() => ({ [`__scope${baseScope.scopeName}`]: nextScopes }), [nextScopes]);
    };
  };
  createScope.scopeName = baseScope.scopeName;
  return createScope;
}
function setRef(ref, value) {
  if (typeof ref === "function") {
    return ref(value);
  } else if (ref !== null && ref !== void 0) {
    ref.current = value;
  }
}
function composeRefs(...refs) {
  return (node2) => {
    let hasCleanup = false;
    const cleanups = refs.map((ref) => {
      const cleanup = setRef(ref, node2);
      if (!hasCleanup && typeof cleanup == "function") {
        hasCleanup = true;
      }
      return cleanup;
    });
    if (hasCleanup) {
      return () => {
        for (let i = 0; i < cleanups.length; i++) {
          const cleanup = cleanups[i];
          if (typeof cleanup == "function") {
            cleanup();
          } else {
            setRef(refs[i], null);
          }
        }
      };
    }
  };
}
function useComposedRefs(...refs) {
  return reactExports.useCallback(composeRefs(...refs), refs);
}
// @__NO_SIDE_EFFECTS__
function createSlot$2(ownerName) {
  const SlotClone = /* @__PURE__ */ createSlotClone$2(ownerName);
  const Slot2 = reactExports.forwardRef((props, forwardedRef) => {
    const { children, ...slotProps } = props;
    const childrenArray = reactExports.Children.toArray(children);
    const slottable = childrenArray.find(isSlottable$2);
    if (slottable) {
      const newElement = slottable.props.children;
      const newChildren = childrenArray.map((child) => {
        if (child === slottable) {
          if (reactExports.Children.count(newElement) > 1) return reactExports.Children.only(null);
          return reactExports.isValidElement(newElement) ? newElement.props.children : null;
        } else {
          return child;
        }
      });
      return /* @__PURE__ */ jsxRuntimeExports.jsx(SlotClone, { ...slotProps, ref: forwardedRef, children: reactExports.isValidElement(newElement) ? reactExports.cloneElement(newElement, void 0, newChildren) : null });
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(SlotClone, { ...slotProps, ref: forwardedRef, children });
  });
  Slot2.displayName = `${ownerName}.Slot`;
  return Slot2;
}
// @__NO_SIDE_EFFECTS__
function createSlotClone$2(ownerName) {
  const SlotClone = reactExports.forwardRef((props, forwardedRef) => {
    const { children, ...slotProps } = props;
    if (reactExports.isValidElement(children)) {
      const childrenRef = getElementRef$2(children);
      const props2 = mergeProps$2(slotProps, children.props);
      if (children.type !== reactExports.Fragment) {
        props2.ref = forwardedRef ? composeRefs(forwardedRef, childrenRef) : childrenRef;
      }
      return reactExports.cloneElement(children, props2);
    }
    return reactExports.Children.count(children) > 1 ? reactExports.Children.only(null) : null;
  });
  SlotClone.displayName = `${ownerName}.SlotClone`;
  return SlotClone;
}
var SLOTTABLE_IDENTIFIER$2 = Symbol("radix.slottable");
function isSlottable$2(child) {
  return reactExports.isValidElement(child) && typeof child.type === "function" && "__radixId" in child.type && child.type.__radixId === SLOTTABLE_IDENTIFIER$2;
}
function mergeProps$2(slotProps, childProps) {
  const overrideProps = { ...childProps };
  for (const propName in childProps) {
    const slotPropValue = slotProps[propName];
    const childPropValue = childProps[propName];
    const isHandler = /^on[A-Z]/.test(propName);
    if (isHandler) {
      if (slotPropValue && childPropValue) {
        overrideProps[propName] = (...args) => {
          const result = childPropValue(...args);
          slotPropValue(...args);
          return result;
        };
      } else if (slotPropValue) {
        overrideProps[propName] = slotPropValue;
      }
    } else if (propName === "style") {
      overrideProps[propName] = { ...slotPropValue, ...childPropValue };
    } else if (propName === "className") {
      overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
    }
  }
  return { ...slotProps, ...overrideProps };
}
function getElementRef$2(element2) {
  var _a, _b;
  let getter = (_a = Object.getOwnPropertyDescriptor(element2.props, "ref")) == null ? void 0 : _a.get;
  let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
  if (mayWarn) {
    return element2.ref;
  }
  getter = (_b = Object.getOwnPropertyDescriptor(element2, "ref")) == null ? void 0 : _b.get;
  mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
  if (mayWarn) {
    return element2.props.ref;
  }
  return element2.props.ref || element2.ref;
}
function createCollection(name2) {
  const PROVIDER_NAME = name2 + "CollectionProvider";
  const [createCollectionContext, createCollectionScope2] = createContextScope(PROVIDER_NAME);
  const [CollectionProviderImpl, useCollectionContext] = createCollectionContext(
    PROVIDER_NAME,
    { collectionRef: { current: null }, itemMap: /* @__PURE__ */ new Map() }
  );
  const CollectionProvider = (props) => {
    const { scope, children } = props;
    const ref = React2.useRef(null);
    const itemMap = React2.useRef(/* @__PURE__ */ new Map()).current;
    return /* @__PURE__ */ jsxRuntimeExports.jsx(CollectionProviderImpl, { scope, itemMap, collectionRef: ref, children });
  };
  CollectionProvider.displayName = PROVIDER_NAME;
  const COLLECTION_SLOT_NAME = name2 + "CollectionSlot";
  const CollectionSlotImpl = /* @__PURE__ */ createSlot$2(COLLECTION_SLOT_NAME);
  const CollectionSlot = React2.forwardRef(
    (props, forwardedRef) => {
      const { scope, children } = props;
      const context = useCollectionContext(COLLECTION_SLOT_NAME, scope);
      const composedRefs = useComposedRefs(forwardedRef, context.collectionRef);
      return /* @__PURE__ */ jsxRuntimeExports.jsx(CollectionSlotImpl, { ref: composedRefs, children });
    }
  );
  CollectionSlot.displayName = COLLECTION_SLOT_NAME;
  const ITEM_SLOT_NAME = name2 + "CollectionItemSlot";
  const ITEM_DATA_ATTR = "data-radix-collection-item";
  const CollectionItemSlotImpl = /* @__PURE__ */ createSlot$2(ITEM_SLOT_NAME);
  const CollectionItemSlot = React2.forwardRef(
    (props, forwardedRef) => {
      const { scope, children, ...itemData } = props;
      const ref = React2.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, ref);
      const context = useCollectionContext(ITEM_SLOT_NAME, scope);
      React2.useEffect(() => {
        context.itemMap.set(ref, { ref, ...itemData });
        return () => void context.itemMap.delete(ref);
      });
      return /* @__PURE__ */ jsxRuntimeExports.jsx(CollectionItemSlotImpl, { ...{ [ITEM_DATA_ATTR]: "" }, ref: composedRefs, children });
    }
  );
  CollectionItemSlot.displayName = ITEM_SLOT_NAME;
  function useCollection2(scope) {
    const context = useCollectionContext(name2 + "CollectionConsumer", scope);
    const getItems = React2.useCallback(() => {
      const collectionNode = context.collectionRef.current;
      if (!collectionNode) return [];
      const orderedNodes = Array.from(collectionNode.querySelectorAll(`[${ITEM_DATA_ATTR}]`));
      const items = Array.from(context.itemMap.values());
      const orderedItems = items.sort(
        (a, b) => orderedNodes.indexOf(a.ref.current) - orderedNodes.indexOf(b.ref.current)
      );
      return orderedItems;
    }, [context.collectionRef, context.itemMap]);
    return getItems;
  }
  return [
    { Provider: CollectionProvider, Slot: CollectionSlot, ItemSlot: CollectionItemSlot },
    useCollection2,
    createCollectionScope2
  ];
}
var DirectionContext = reactExports.createContext(void 0);
function useDirection(localDir) {
  const globalDir = reactExports.useContext(DirectionContext);
  return localDir || globalDir || "ltr";
}
// @__NO_SIDE_EFFECTS__
function createSlot$1(ownerName) {
  const SlotClone = /* @__PURE__ */ createSlotClone$1(ownerName);
  const Slot2 = reactExports.forwardRef((props, forwardedRef) => {
    const { children, ...slotProps } = props;
    const childrenArray = reactExports.Children.toArray(children);
    const slottable = childrenArray.find(isSlottable$1);
    if (slottable) {
      const newElement = slottable.props.children;
      const newChildren = childrenArray.map((child) => {
        if (child === slottable) {
          if (reactExports.Children.count(newElement) > 1) return reactExports.Children.only(null);
          return reactExports.isValidElement(newElement) ? newElement.props.children : null;
        } else {
          return child;
        }
      });
      return /* @__PURE__ */ jsxRuntimeExports.jsx(SlotClone, { ...slotProps, ref: forwardedRef, children: reactExports.isValidElement(newElement) ? reactExports.cloneElement(newElement, void 0, newChildren) : null });
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(SlotClone, { ...slotProps, ref: forwardedRef, children });
  });
  Slot2.displayName = `${ownerName}.Slot`;
  return Slot2;
}
// @__NO_SIDE_EFFECTS__
function createSlotClone$1(ownerName) {
  const SlotClone = reactExports.forwardRef((props, forwardedRef) => {
    const { children, ...slotProps } = props;
    if (reactExports.isValidElement(children)) {
      const childrenRef = getElementRef$1(children);
      const props2 = mergeProps$1(slotProps, children.props);
      if (children.type !== reactExports.Fragment) {
        props2.ref = forwardedRef ? composeRefs(forwardedRef, childrenRef) : childrenRef;
      }
      return reactExports.cloneElement(children, props2);
    }
    return reactExports.Children.count(children) > 1 ? reactExports.Children.only(null) : null;
  });
  SlotClone.displayName = `${ownerName}.SlotClone`;
  return SlotClone;
}
var SLOTTABLE_IDENTIFIER$1 = Symbol("radix.slottable");
function isSlottable$1(child) {
  return reactExports.isValidElement(child) && typeof child.type === "function" && "__radixId" in child.type && child.type.__radixId === SLOTTABLE_IDENTIFIER$1;
}
function mergeProps$1(slotProps, childProps) {
  const overrideProps = { ...childProps };
  for (const propName in childProps) {
    const slotPropValue = slotProps[propName];
    const childPropValue = childProps[propName];
    const isHandler = /^on[A-Z]/.test(propName);
    if (isHandler) {
      if (slotPropValue && childPropValue) {
        overrideProps[propName] = (...args) => {
          const result = childPropValue(...args);
          slotPropValue(...args);
          return result;
        };
      } else if (slotPropValue) {
        overrideProps[propName] = slotPropValue;
      }
    } else if (propName === "style") {
      overrideProps[propName] = { ...slotPropValue, ...childPropValue };
    } else if (propName === "className") {
      overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
    }
  }
  return { ...slotProps, ...overrideProps };
}
function getElementRef$1(element2) {
  var _a, _b;
  let getter = (_a = Object.getOwnPropertyDescriptor(element2.props, "ref")) == null ? void 0 : _a.get;
  let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
  if (mayWarn) {
    return element2.ref;
  }
  getter = (_b = Object.getOwnPropertyDescriptor(element2, "ref")) == null ? void 0 : _b.get;
  mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
  if (mayWarn) {
    return element2.props.ref;
  }
  return element2.props.ref || element2.ref;
}
var NODES = [
  "a",
  "button",
  "div",
  "form",
  "h2",
  "h3",
  "img",
  "input",
  "label",
  "li",
  "nav",
  "ol",
  "p",
  "select",
  "span",
  "svg",
  "ul"
];
var Primitive = NODES.reduce((primitive, node2) => {
  const Slot2 = /* @__PURE__ */ createSlot$1(`Primitive.${node2}`);
  const Node2 = reactExports.forwardRef((props, forwardedRef) => {
    const { asChild, ...primitiveProps } = props;
    const Comp = asChild ? Slot2 : node2;
    if (typeof window !== "undefined") {
      window[Symbol.for("radix-ui")] = true;
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Comp, { ...primitiveProps, ref: forwardedRef });
  });
  Node2.displayName = `Primitive.${node2}`;
  return { ...primitive, [node2]: Node2 };
}, {});
function dispatchDiscreteCustomEvent(target, event) {
  if (target) reactDomExports.flushSync(() => target.dispatchEvent(event));
}
function useCallbackRef$1(callback) {
  const callbackRef = reactExports.useRef(callback);
  reactExports.useEffect(() => {
    callbackRef.current = callback;
  });
  return reactExports.useMemo(() => (...args) => {
    var _a;
    return (_a = callbackRef.current) == null ? void 0 : _a.call(callbackRef, ...args);
  }, []);
}
function useEscapeKeydown(onEscapeKeyDownProp, ownerDocument = globalThis == null ? void 0 : globalThis.document) {
  const onEscapeKeyDown = useCallbackRef$1(onEscapeKeyDownProp);
  reactExports.useEffect(() => {
    const handleKeyDown = (event) => {
      if (event.key === "Escape") {
        onEscapeKeyDown(event);
      }
    };
    ownerDocument.addEventListener("keydown", handleKeyDown, { capture: true });
    return () => ownerDocument.removeEventListener("keydown", handleKeyDown, { capture: true });
  }, [onEscapeKeyDown, ownerDocument]);
}
var DISMISSABLE_LAYER_NAME = "DismissableLayer";
var CONTEXT_UPDATE = "dismissableLayer.update";
var POINTER_DOWN_OUTSIDE = "dismissableLayer.pointerDownOutside";
var FOCUS_OUTSIDE = "dismissableLayer.focusOutside";
var originalBodyPointerEvents;
var DismissableLayerContext = reactExports.createContext({
  layers: /* @__PURE__ */ new Set(),
  layersWithOutsidePointerEventsDisabled: /* @__PURE__ */ new Set(),
  branches: /* @__PURE__ */ new Set()
});
var DismissableLayer = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      disableOutsidePointerEvents = false,
      onEscapeKeyDown,
      onPointerDownOutside,
      onFocusOutside,
      onInteractOutside,
      onDismiss,
      ...layerProps
    } = props;
    const context = reactExports.useContext(DismissableLayerContext);
    const [node2, setNode] = reactExports.useState(null);
    const ownerDocument = (node2 == null ? void 0 : node2.ownerDocument) ?? (globalThis == null ? void 0 : globalThis.document);
    const [, force] = reactExports.useState({});
    const composedRefs = useComposedRefs(forwardedRef, (node22) => setNode(node22));
    const layers = Array.from(context.layers);
    const [highestLayerWithOutsidePointerEventsDisabled] = [...context.layersWithOutsidePointerEventsDisabled].slice(-1);
    const highestLayerWithOutsidePointerEventsDisabledIndex = layers.indexOf(highestLayerWithOutsidePointerEventsDisabled);
    const index2 = node2 ? layers.indexOf(node2) : -1;
    const isBodyPointerEventsDisabled = context.layersWithOutsidePointerEventsDisabled.size > 0;
    const isPointerEventsEnabled = index2 >= highestLayerWithOutsidePointerEventsDisabledIndex;
    const pointerDownOutside = usePointerDownOutside((event) => {
      const target = event.target;
      const isPointerDownOnBranch = [...context.branches].some((branch) => branch.contains(target));
      if (!isPointerEventsEnabled || isPointerDownOnBranch) return;
      onPointerDownOutside == null ? void 0 : onPointerDownOutside(event);
      onInteractOutside == null ? void 0 : onInteractOutside(event);
      if (!event.defaultPrevented) onDismiss == null ? void 0 : onDismiss();
    }, ownerDocument);
    const focusOutside = useFocusOutside((event) => {
      const target = event.target;
      const isFocusInBranch = [...context.branches].some((branch) => branch.contains(target));
      if (isFocusInBranch) return;
      onFocusOutside == null ? void 0 : onFocusOutside(event);
      onInteractOutside == null ? void 0 : onInteractOutside(event);
      if (!event.defaultPrevented) onDismiss == null ? void 0 : onDismiss();
    }, ownerDocument);
    useEscapeKeydown((event) => {
      const isHighestLayer = index2 === context.layers.size - 1;
      if (!isHighestLayer) return;
      onEscapeKeyDown == null ? void 0 : onEscapeKeyDown(event);
      if (!event.defaultPrevented && onDismiss) {
        event.preventDefault();
        onDismiss();
      }
    }, ownerDocument);
    reactExports.useEffect(() => {
      if (!node2) return;
      if (disableOutsidePointerEvents) {
        if (context.layersWithOutsidePointerEventsDisabled.size === 0) {
          originalBodyPointerEvents = ownerDocument.body.style.pointerEvents;
          ownerDocument.body.style.pointerEvents = "none";
        }
        context.layersWithOutsidePointerEventsDisabled.add(node2);
      }
      context.layers.add(node2);
      dispatchUpdate();
      return () => {
        if (disableOutsidePointerEvents && context.layersWithOutsidePointerEventsDisabled.size === 1) {
          ownerDocument.body.style.pointerEvents = originalBodyPointerEvents;
        }
      };
    }, [node2, ownerDocument, disableOutsidePointerEvents, context]);
    reactExports.useEffect(() => {
      return () => {
        if (!node2) return;
        context.layers.delete(node2);
        context.layersWithOutsidePointerEventsDisabled.delete(node2);
        dispatchUpdate();
      };
    }, [node2, context]);
    reactExports.useEffect(() => {
      const handleUpdate = () => force({});
      document.addEventListener(CONTEXT_UPDATE, handleUpdate);
      return () => document.removeEventListener(CONTEXT_UPDATE, handleUpdate);
    }, []);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.div,
      {
        ...layerProps,
        ref: composedRefs,
        style: {
          pointerEvents: isBodyPointerEventsDisabled ? isPointerEventsEnabled ? "auto" : "none" : void 0,
          ...props.style
        },
        onFocusCapture: composeEventHandlers(props.onFocusCapture, focusOutside.onFocusCapture),
        onBlurCapture: composeEventHandlers(props.onBlurCapture, focusOutside.onBlurCapture),
        onPointerDownCapture: composeEventHandlers(
          props.onPointerDownCapture,
          pointerDownOutside.onPointerDownCapture
        )
      }
    );
  }
);
DismissableLayer.displayName = DISMISSABLE_LAYER_NAME;
var BRANCH_NAME = "DismissableLayerBranch";
var DismissableLayerBranch = reactExports.forwardRef((props, forwardedRef) => {
  const context = reactExports.useContext(DismissableLayerContext);
  const ref = reactExports.useRef(null);
  const composedRefs = useComposedRefs(forwardedRef, ref);
  reactExports.useEffect(() => {
    const node2 = ref.current;
    if (node2) {
      context.branches.add(node2);
      return () => {
        context.branches.delete(node2);
      };
    }
  }, [context.branches]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Primitive.div, { ...props, ref: composedRefs });
});
DismissableLayerBranch.displayName = BRANCH_NAME;
function usePointerDownOutside(onPointerDownOutside, ownerDocument = globalThis == null ? void 0 : globalThis.document) {
  const handlePointerDownOutside = useCallbackRef$1(onPointerDownOutside);
  const isPointerInsideReactTreeRef = reactExports.useRef(false);
  const handleClickRef = reactExports.useRef(() => {
  });
  reactExports.useEffect(() => {
    const handlePointerDown = (event) => {
      if (event.target && !isPointerInsideReactTreeRef.current) {
        let handleAndDispatchPointerDownOutsideEvent2 = function() {
          handleAndDispatchCustomEvent(
            POINTER_DOWN_OUTSIDE,
            handlePointerDownOutside,
            eventDetail,
            { discrete: true }
          );
        };
        const eventDetail = { originalEvent: event };
        if (event.pointerType === "touch") {
          ownerDocument.removeEventListener("click", handleClickRef.current);
          handleClickRef.current = handleAndDispatchPointerDownOutsideEvent2;
          ownerDocument.addEventListener("click", handleClickRef.current, { once: true });
        } else {
          handleAndDispatchPointerDownOutsideEvent2();
        }
      } else {
        ownerDocument.removeEventListener("click", handleClickRef.current);
      }
      isPointerInsideReactTreeRef.current = false;
    };
    const timerId = window.setTimeout(() => {
      ownerDocument.addEventListener("pointerdown", handlePointerDown);
    }, 0);
    return () => {
      window.clearTimeout(timerId);
      ownerDocument.removeEventListener("pointerdown", handlePointerDown);
      ownerDocument.removeEventListener("click", handleClickRef.current);
    };
  }, [ownerDocument, handlePointerDownOutside]);
  return {
    // ensures we check React component tree (not just DOM tree)
    onPointerDownCapture: () => isPointerInsideReactTreeRef.current = true
  };
}
function useFocusOutside(onFocusOutside, ownerDocument = globalThis == null ? void 0 : globalThis.document) {
  const handleFocusOutside = useCallbackRef$1(onFocusOutside);
  const isFocusInsideReactTreeRef = reactExports.useRef(false);
  reactExports.useEffect(() => {
    const handleFocus = (event) => {
      if (event.target && !isFocusInsideReactTreeRef.current) {
        const eventDetail = { originalEvent: event };
        handleAndDispatchCustomEvent(FOCUS_OUTSIDE, handleFocusOutside, eventDetail, {
          discrete: false
        });
      }
    };
    ownerDocument.addEventListener("focusin", handleFocus);
    return () => ownerDocument.removeEventListener("focusin", handleFocus);
  }, [ownerDocument, handleFocusOutside]);
  return {
    onFocusCapture: () => isFocusInsideReactTreeRef.current = true,
    onBlurCapture: () => isFocusInsideReactTreeRef.current = false
  };
}
function dispatchUpdate() {
  const event = new CustomEvent(CONTEXT_UPDATE);
  document.dispatchEvent(event);
}
function handleAndDispatchCustomEvent(name2, handler, detail, { discrete }) {
  const target = detail.originalEvent.target;
  const event = new CustomEvent(name2, { bubbles: false, cancelable: true, detail });
  if (handler) target.addEventListener(name2, handler, { once: true });
  if (discrete) {
    dispatchDiscreteCustomEvent(target, event);
  } else {
    target.dispatchEvent(event);
  }
}
var count$1 = 0;
function useFocusGuards() {
  reactExports.useEffect(() => {
    const edgeGuards = document.querySelectorAll("[data-radix-focus-guard]");
    document.body.insertAdjacentElement("afterbegin", edgeGuards[0] ?? createFocusGuard());
    document.body.insertAdjacentElement("beforeend", edgeGuards[1] ?? createFocusGuard());
    count$1++;
    return () => {
      if (count$1 === 1) {
        document.querySelectorAll("[data-radix-focus-guard]").forEach((node2) => node2.remove());
      }
      count$1--;
    };
  }, []);
}
function createFocusGuard() {
  const element2 = document.createElement("span");
  element2.setAttribute("data-radix-focus-guard", "");
  element2.tabIndex = 0;
  element2.style.outline = "none";
  element2.style.opacity = "0";
  element2.style.position = "fixed";
  element2.style.pointerEvents = "none";
  return element2;
}
var AUTOFOCUS_ON_MOUNT = "focusScope.autoFocusOnMount";
var AUTOFOCUS_ON_UNMOUNT = "focusScope.autoFocusOnUnmount";
var EVENT_OPTIONS = { bubbles: false, cancelable: true };
var FOCUS_SCOPE_NAME = "FocusScope";
var FocusScope = reactExports.forwardRef((props, forwardedRef) => {
  const {
    loop = false,
    trapped = false,
    onMountAutoFocus: onMountAutoFocusProp,
    onUnmountAutoFocus: onUnmountAutoFocusProp,
    ...scopeProps
  } = props;
  const [container, setContainer] = reactExports.useState(null);
  const onMountAutoFocus = useCallbackRef$1(onMountAutoFocusProp);
  const onUnmountAutoFocus = useCallbackRef$1(onUnmountAutoFocusProp);
  const lastFocusedElementRef = reactExports.useRef(null);
  const composedRefs = useComposedRefs(forwardedRef, (node2) => setContainer(node2));
  const focusScope = reactExports.useRef({
    paused: false,
    pause() {
      this.paused = true;
    },
    resume() {
      this.paused = false;
    }
  }).current;
  reactExports.useEffect(() => {
    if (trapped) {
      let handleFocusIn2 = function(event) {
        if (focusScope.paused || !container) return;
        const target = event.target;
        if (container.contains(target)) {
          lastFocusedElementRef.current = target;
        } else {
          focus(lastFocusedElementRef.current, { select: true });
        }
      }, handleFocusOut2 = function(event) {
        if (focusScope.paused || !container) return;
        const relatedTarget = event.relatedTarget;
        if (relatedTarget === null) return;
        if (!container.contains(relatedTarget)) {
          focus(lastFocusedElementRef.current, { select: true });
        }
      }, handleMutations2 = function(mutations) {
        const focusedElement = document.activeElement;
        if (focusedElement !== document.body) return;
        for (const mutation of mutations) {
          if (mutation.removedNodes.length > 0) focus(container);
        }
      };
      document.addEventListener("focusin", handleFocusIn2);
      document.addEventListener("focusout", handleFocusOut2);
      const mutationObserver = new MutationObserver(handleMutations2);
      if (container) mutationObserver.observe(container, { childList: true, subtree: true });
      return () => {
        document.removeEventListener("focusin", handleFocusIn2);
        document.removeEventListener("focusout", handleFocusOut2);
        mutationObserver.disconnect();
      };
    }
  }, [trapped, container, focusScope.paused]);
  reactExports.useEffect(() => {
    if (container) {
      focusScopesStack.add(focusScope);
      const previouslyFocusedElement = document.activeElement;
      const hasFocusedCandidate = container.contains(previouslyFocusedElement);
      if (!hasFocusedCandidate) {
        const mountEvent = new CustomEvent(AUTOFOCUS_ON_MOUNT, EVENT_OPTIONS);
        container.addEventListener(AUTOFOCUS_ON_MOUNT, onMountAutoFocus);
        container.dispatchEvent(mountEvent);
        if (!mountEvent.defaultPrevented) {
          focusFirst(removeLinks(getTabbableCandidates(container)), { select: true });
          if (document.activeElement === previouslyFocusedElement) {
            focus(container);
          }
        }
      }
      return () => {
        container.removeEventListener(AUTOFOCUS_ON_MOUNT, onMountAutoFocus);
        setTimeout(() => {
          const unmountEvent = new CustomEvent(AUTOFOCUS_ON_UNMOUNT, EVENT_OPTIONS);
          container.addEventListener(AUTOFOCUS_ON_UNMOUNT, onUnmountAutoFocus);
          container.dispatchEvent(unmountEvent);
          if (!unmountEvent.defaultPrevented) {
            focus(previouslyFocusedElement ?? document.body, { select: true });
          }
          container.removeEventListener(AUTOFOCUS_ON_UNMOUNT, onUnmountAutoFocus);
          focusScopesStack.remove(focusScope);
        }, 0);
      };
    }
  }, [container, onMountAutoFocus, onUnmountAutoFocus, focusScope]);
  const handleKeyDown = reactExports.useCallback(
    (event) => {
      if (!loop && !trapped) return;
      if (focusScope.paused) return;
      const isTabKey = event.key === "Tab" && !event.altKey && !event.ctrlKey && !event.metaKey;
      const focusedElement = document.activeElement;
      if (isTabKey && focusedElement) {
        const container2 = event.currentTarget;
        const [first, last] = getTabbableEdges(container2);
        const hasTabbableElementsInside = first && last;
        if (!hasTabbableElementsInside) {
          if (focusedElement === container2) event.preventDefault();
        } else {
          if (!event.shiftKey && focusedElement === last) {
            event.preventDefault();
            if (loop) focus(first, { select: true });
          } else if (event.shiftKey && focusedElement === first) {
            event.preventDefault();
            if (loop) focus(last, { select: true });
          }
        }
      }
    },
    [loop, trapped, focusScope.paused]
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Primitive.div, { tabIndex: -1, ...scopeProps, ref: composedRefs, onKeyDown: handleKeyDown });
});
FocusScope.displayName = FOCUS_SCOPE_NAME;
function focusFirst(candidates, { select = false } = {}) {
  const previouslyFocusedElement = document.activeElement;
  for (const candidate of candidates) {
    focus(candidate, { select });
    if (document.activeElement !== previouslyFocusedElement) return;
  }
}
function getTabbableEdges(container) {
  const candidates = getTabbableCandidates(container);
  const first = findVisible(candidates, container);
  const last = findVisible(candidates.reverse(), container);
  return [first, last];
}
function getTabbableCandidates(container) {
  const nodes = [];
  const walker = document.createTreeWalker(container, NodeFilter.SHOW_ELEMENT, {
    acceptNode: (node2) => {
      const isHiddenInput = node2.tagName === "INPUT" && node2.type === "hidden";
      if (node2.disabled || node2.hidden || isHiddenInput) return NodeFilter.FILTER_SKIP;
      return node2.tabIndex >= 0 ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP;
    }
  });
  while (walker.nextNode()) nodes.push(walker.currentNode);
  return nodes;
}
function findVisible(elements, container) {
  for (const element2 of elements) {
    if (!isHidden(element2, { upTo: container })) return element2;
  }
}
function isHidden(node2, { upTo }) {
  if (getComputedStyle(node2).visibility === "hidden") return true;
  while (node2) {
    if (upTo !== void 0 && node2 === upTo) return false;
    if (getComputedStyle(node2).display === "none") return true;
    node2 = node2.parentElement;
  }
  return false;
}
function isSelectableInput(element2) {
  return element2 instanceof HTMLInputElement && "select" in element2;
}
function focus(element2, { select = false } = {}) {
  if (element2 && element2.focus) {
    const previouslyFocusedElement = document.activeElement;
    element2.focus({ preventScroll: true });
    if (element2 !== previouslyFocusedElement && isSelectableInput(element2) && select)
      element2.select();
  }
}
var focusScopesStack = createFocusScopesStack();
function createFocusScopesStack() {
  let stack = [];
  return {
    add(focusScope) {
      const activeFocusScope = stack[0];
      if (focusScope !== activeFocusScope) {
        activeFocusScope == null ? void 0 : activeFocusScope.pause();
      }
      stack = arrayRemove(stack, focusScope);
      stack.unshift(focusScope);
    },
    remove(focusScope) {
      var _a;
      stack = arrayRemove(stack, focusScope);
      (_a = stack[0]) == null ? void 0 : _a.resume();
    }
  };
}
function arrayRemove(array, item) {
  const updatedArray = [...array];
  const index2 = updatedArray.indexOf(item);
  if (index2 !== -1) {
    updatedArray.splice(index2, 1);
  }
  return updatedArray;
}
function removeLinks(items) {
  return items.filter((item) => item.tagName !== "A");
}
var useLayoutEffect2 = (globalThis == null ? void 0 : globalThis.document) ? reactExports.useLayoutEffect : () => {
};
var useReactId = React[" useId ".trim().toString()] || (() => void 0);
var count = 0;
function useId(deterministicId) {
  const [id, setId] = reactExports.useState(useReactId());
  useLayoutEffect2(() => {
    setId((reactId) => reactId ?? String(count++));
  }, [deterministicId]);
  return deterministicId || (id ? `radix-${id}` : "");
}
const sides = ["top", "right", "bottom", "left"];
const min = Math.min;
const max = Math.max;
const round = Math.round;
const floor = Math.floor;
const createCoords = (v) => ({
  x: v,
  y: v
});
const oppositeSideMap = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom"
};
function clamp(start, value, end) {
  return max(start, min(value, end));
}
function evaluate(value, param) {
  return typeof value === "function" ? value(param) : value;
}
function getSide(placement) {
  return placement.split("-")[0];
}
function getAlignment(placement) {
  return placement.split("-")[1];
}
function getOppositeAxis(axis) {
  return axis === "x" ? "y" : "x";
}
function getAxisLength(axis) {
  return axis === "y" ? "height" : "width";
}
function getSideAxis(placement) {
  const firstChar = placement[0];
  return firstChar === "t" || firstChar === "b" ? "y" : "x";
}
function getAlignmentAxis(placement) {
  return getOppositeAxis(getSideAxis(placement));
}
function getAlignmentSides(placement, rects, rtl) {
  if (rtl === void 0) {
    rtl = false;
  }
  const alignment = getAlignment(placement);
  const alignmentAxis = getAlignmentAxis(placement);
  const length = getAxisLength(alignmentAxis);
  let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
  if (rects.reference[length] > rects.floating[length]) {
    mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
  }
  return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
}
function getExpandedPlacements(placement) {
  const oppositePlacement = getOppositePlacement(placement);
  return [getOppositeAlignmentPlacement(placement), oppositePlacement, getOppositeAlignmentPlacement(oppositePlacement)];
}
function getOppositeAlignmentPlacement(placement) {
  return placement.includes("start") ? placement.replace("start", "end") : placement.replace("end", "start");
}
const lrPlacement = ["left", "right"];
const rlPlacement = ["right", "left"];
const tbPlacement = ["top", "bottom"];
const btPlacement = ["bottom", "top"];
function getSideList(side, isStart, rtl) {
  switch (side) {
    case "top":
    case "bottom":
      if (rtl) return isStart ? rlPlacement : lrPlacement;
      return isStart ? lrPlacement : rlPlacement;
    case "left":
    case "right":
      return isStart ? tbPlacement : btPlacement;
    default:
      return [];
  }
}
function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
  const alignment = getAlignment(placement);
  let list2 = getSideList(getSide(placement), direction === "start", rtl);
  if (alignment) {
    list2 = list2.map((side) => side + "-" + alignment);
    if (flipAlignment) {
      list2 = list2.concat(list2.map(getOppositeAlignmentPlacement));
    }
  }
  return list2;
}
function getOppositePlacement(placement) {
  const side = getSide(placement);
  return oppositeSideMap[side] + placement.slice(side.length);
}
function expandPaddingObject(padding) {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    ...padding
  };
}
function getPaddingObject(padding) {
  return typeof padding !== "number" ? expandPaddingObject(padding) : {
    top: padding,
    right: padding,
    bottom: padding,
    left: padding
  };
}
function rectToClientRect(rect) {
  const {
    x,
    y,
    width,
    height
  } = rect;
  return {
    width,
    height,
    top: y,
    left: x,
    right: x + width,
    bottom: y + height,
    x,
    y
  };
}
function computeCoordsFromPlacement(_ref, placement, rtl) {
  let {
    reference,
    floating
  } = _ref;
  const sideAxis = getSideAxis(placement);
  const alignmentAxis = getAlignmentAxis(placement);
  const alignLength = getAxisLength(alignmentAxis);
  const side = getSide(placement);
  const isVertical = sideAxis === "y";
  const commonX = reference.x + reference.width / 2 - floating.width / 2;
  const commonY = reference.y + reference.height / 2 - floating.height / 2;
  const commonAlign = reference[alignLength] / 2 - floating[alignLength] / 2;
  let coords;
  switch (side) {
    case "top":
      coords = {
        x: commonX,
        y: reference.y - floating.height
      };
      break;
    case "bottom":
      coords = {
        x: commonX,
        y: reference.y + reference.height
      };
      break;
    case "right":
      coords = {
        x: reference.x + reference.width,
        y: commonY
      };
      break;
    case "left":
      coords = {
        x: reference.x - floating.width,
        y: commonY
      };
      break;
    default:
      coords = {
        x: reference.x,
        y: reference.y
      };
  }
  switch (getAlignment(placement)) {
    case "start":
      coords[alignmentAxis] -= commonAlign * (rtl && isVertical ? -1 : 1);
      break;
    case "end":
      coords[alignmentAxis] += commonAlign * (rtl && isVertical ? -1 : 1);
      break;
  }
  return coords;
}
async function detectOverflow(state, options) {
  var _await$platform$isEle;
  if (options === void 0) {
    options = {};
  }
  const {
    x,
    y,
    platform: platform2,
    rects,
    elements,
    strategy
  } = state;
  const {
    boundary = "clippingAncestors",
    rootBoundary = "viewport",
    elementContext = "floating",
    altBoundary = false,
    padding = 0
  } = evaluate(options, state);
  const paddingObject = getPaddingObject(padding);
  const altContext = elementContext === "floating" ? "reference" : "floating";
  const element2 = elements[altBoundary ? altContext : elementContext];
  const clippingClientRect = rectToClientRect(await platform2.getClippingRect({
    element: ((_await$platform$isEle = await (platform2.isElement == null ? void 0 : platform2.isElement(element2))) != null ? _await$platform$isEle : true) ? element2 : element2.contextElement || await (platform2.getDocumentElement == null ? void 0 : platform2.getDocumentElement(elements.floating)),
    boundary,
    rootBoundary,
    strategy
  }));
  const rect = elementContext === "floating" ? {
    x,
    y,
    width: rects.floating.width,
    height: rects.floating.height
  } : rects.reference;
  const offsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(elements.floating));
  const offsetScale = await (platform2.isElement == null ? void 0 : platform2.isElement(offsetParent)) ? await (platform2.getScale == null ? void 0 : platform2.getScale(offsetParent)) || {
    x: 1,
    y: 1
  } : {
    x: 1,
    y: 1
  };
  const elementClientRect = rectToClientRect(platform2.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform2.convertOffsetParentRelativeRectToViewportRelativeRect({
    elements,
    rect,
    offsetParent,
    strategy
  }) : rect);
  return {
    top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
    bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
    left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
    right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
  };
}
const MAX_RESET_COUNT = 50;
const computePosition$1 = async (reference, floating, config) => {
  const {
    placement = "bottom",
    strategy = "absolute",
    middleware = [],
    platform: platform2
  } = config;
  const platformWithDetectOverflow = platform2.detectOverflow ? platform2 : {
    ...platform2,
    detectOverflow
  };
  const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(floating));
  let rects = await platform2.getElementRects({
    reference,
    floating,
    strategy
  });
  let {
    x,
    y
  } = computeCoordsFromPlacement(rects, placement, rtl);
  let statefulPlacement = placement;
  let resetCount = 0;
  const middlewareData = {};
  for (let i = 0; i < middleware.length; i++) {
    const currentMiddleware = middleware[i];
    if (!currentMiddleware) {
      continue;
    }
    const {
      name: name2,
      fn
    } = currentMiddleware;
    const {
      x: nextX,
      y: nextY,
      data,
      reset
    } = await fn({
      x,
      y,
      initialPlacement: placement,
      placement: statefulPlacement,
      strategy,
      middlewareData,
      rects,
      platform: platformWithDetectOverflow,
      elements: {
        reference,
        floating
      }
    });
    x = nextX != null ? nextX : x;
    y = nextY != null ? nextY : y;
    middlewareData[name2] = {
      ...middlewareData[name2],
      ...data
    };
    if (reset && resetCount < MAX_RESET_COUNT) {
      resetCount++;
      if (typeof reset === "object") {
        if (reset.placement) {
          statefulPlacement = reset.placement;
        }
        if (reset.rects) {
          rects = reset.rects === true ? await platform2.getElementRects({
            reference,
            floating,
            strategy
          }) : reset.rects;
        }
        ({
          x,
          y
        } = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
      }
      i = -1;
    }
  }
  return {
    x,
    y,
    placement: statefulPlacement,
    strategy,
    middlewareData
  };
};
const arrow$3 = (options) => ({
  name: "arrow",
  options,
  async fn(state) {
    const {
      x,
      y,
      placement,
      rects,
      platform: platform2,
      elements,
      middlewareData
    } = state;
    const {
      element: element2,
      padding = 0
    } = evaluate(options, state) || {};
    if (element2 == null) {
      return {};
    }
    const paddingObject = getPaddingObject(padding);
    const coords = {
      x,
      y
    };
    const axis = getAlignmentAxis(placement);
    const length = getAxisLength(axis);
    const arrowDimensions = await platform2.getDimensions(element2);
    const isYAxis = axis === "y";
    const minProp = isYAxis ? "top" : "left";
    const maxProp = isYAxis ? "bottom" : "right";
    const clientProp = isYAxis ? "clientHeight" : "clientWidth";
    const endDiff = rects.reference[length] + rects.reference[axis] - coords[axis] - rects.floating[length];
    const startDiff = coords[axis] - rects.reference[axis];
    const arrowOffsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(element2));
    let clientSize = arrowOffsetParent ? arrowOffsetParent[clientProp] : 0;
    if (!clientSize || !await (platform2.isElement == null ? void 0 : platform2.isElement(arrowOffsetParent))) {
      clientSize = elements.floating[clientProp] || rects.floating[length];
    }
    const centerToReference = endDiff / 2 - startDiff / 2;
    const largestPossiblePadding = clientSize / 2 - arrowDimensions[length] / 2 - 1;
    const minPadding = min(paddingObject[minProp], largestPossiblePadding);
    const maxPadding = min(paddingObject[maxProp], largestPossiblePadding);
    const min$1 = minPadding;
    const max2 = clientSize - arrowDimensions[length] - maxPadding;
    const center = clientSize / 2 - arrowDimensions[length] / 2 + centerToReference;
    const offset2 = clamp(min$1, center, max2);
    const shouldAddOffset = !middlewareData.arrow && getAlignment(placement) != null && center !== offset2 && rects.reference[length] / 2 - (center < min$1 ? minPadding : maxPadding) - arrowDimensions[length] / 2 < 0;
    const alignmentOffset = shouldAddOffset ? center < min$1 ? center - min$1 : center - max2 : 0;
    return {
      [axis]: coords[axis] + alignmentOffset,
      data: {
        [axis]: offset2,
        centerOffset: center - offset2 - alignmentOffset,
        ...shouldAddOffset && {
          alignmentOffset
        }
      },
      reset: shouldAddOffset
    };
  }
});
const flip$2 = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "flip",
    options,
    async fn(state) {
      var _middlewareData$arrow, _middlewareData$flip;
      const {
        placement,
        middlewareData,
        rects,
        initialPlacement,
        platform: platform2,
        elements
      } = state;
      const {
        mainAxis: checkMainAxis = true,
        crossAxis: checkCrossAxis = true,
        fallbackPlacements: specifiedFallbackPlacements,
        fallbackStrategy = "bestFit",
        fallbackAxisSideDirection = "none",
        flipAlignment = true,
        ...detectOverflowOptions
      } = evaluate(options, state);
      if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
        return {};
      }
      const side = getSide(placement);
      const initialSideAxis = getSideAxis(initialPlacement);
      const isBasePlacement = getSide(initialPlacement) === initialPlacement;
      const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
      const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
      const hasFallbackAxisSideDirection = fallbackAxisSideDirection !== "none";
      if (!specifiedFallbackPlacements && hasFallbackAxisSideDirection) {
        fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
      }
      const placements = [initialPlacement, ...fallbackPlacements];
      const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
      const overflows = [];
      let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
      if (checkMainAxis) {
        overflows.push(overflow[side]);
      }
      if (checkCrossAxis) {
        const sides2 = getAlignmentSides(placement, rects, rtl);
        overflows.push(overflow[sides2[0]], overflow[sides2[1]]);
      }
      overflowsData = [...overflowsData, {
        placement,
        overflows
      }];
      if (!overflows.every((side2) => side2 <= 0)) {
        var _middlewareData$flip2, _overflowsData$filter;
        const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
        const nextPlacement = placements[nextIndex];
        if (nextPlacement) {
          const ignoreCrossAxisOverflow = checkCrossAxis === "alignment" ? initialSideAxis !== getSideAxis(nextPlacement) : false;
          if (!ignoreCrossAxisOverflow || // We leave the current main axis only if every placement on that axis
          // overflows the main axis.
          overflowsData.every((d) => getSideAxis(d.placement) === initialSideAxis ? d.overflows[0] > 0 : true)) {
            return {
              data: {
                index: nextIndex,
                overflows: overflowsData
              },
              reset: {
                placement: nextPlacement
              }
            };
          }
        }
        let resetPlacement = (_overflowsData$filter = overflowsData.filter((d) => d.overflows[0] <= 0).sort((a, b) => a.overflows[1] - b.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
        if (!resetPlacement) {
          switch (fallbackStrategy) {
            case "bestFit": {
              var _overflowsData$filter2;
              const placement2 = (_overflowsData$filter2 = overflowsData.filter((d) => {
                if (hasFallbackAxisSideDirection) {
                  const currentSideAxis = getSideAxis(d.placement);
                  return currentSideAxis === initialSideAxis || // Create a bias to the `y` side axis due to horizontal
                  // reading directions favoring greater width.
                  currentSideAxis === "y";
                }
                return true;
              }).map((d) => [d.placement, d.overflows.filter((overflow2) => overflow2 > 0).reduce((acc, overflow2) => acc + overflow2, 0)]).sort((a, b) => a[1] - b[1])[0]) == null ? void 0 : _overflowsData$filter2[0];
              if (placement2) {
                resetPlacement = placement2;
              }
              break;
            }
            case "initialPlacement":
              resetPlacement = initialPlacement;
              break;
          }
        }
        if (placement !== resetPlacement) {
          return {
            reset: {
              placement: resetPlacement
            }
          };
        }
      }
      return {};
    }
  };
};
function getSideOffsets(overflow, rect) {
  return {
    top: overflow.top - rect.height,
    right: overflow.right - rect.width,
    bottom: overflow.bottom - rect.height,
    left: overflow.left - rect.width
  };
}
function isAnySideFullyClipped(overflow) {
  return sides.some((side) => overflow[side] >= 0);
}
const hide$2 = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "hide",
    options,
    async fn(state) {
      const {
        rects,
        platform: platform2
      } = state;
      const {
        strategy = "referenceHidden",
        ...detectOverflowOptions
      } = evaluate(options, state);
      switch (strategy) {
        case "referenceHidden": {
          const overflow = await platform2.detectOverflow(state, {
            ...detectOverflowOptions,
            elementContext: "reference"
          });
          const offsets = getSideOffsets(overflow, rects.reference);
          return {
            data: {
              referenceHiddenOffsets: offsets,
              referenceHidden: isAnySideFullyClipped(offsets)
            }
          };
        }
        case "escaped": {
          const overflow = await platform2.detectOverflow(state, {
            ...detectOverflowOptions,
            altBoundary: true
          });
          const offsets = getSideOffsets(overflow, rects.floating);
          return {
            data: {
              escapedOffsets: offsets,
              escaped: isAnySideFullyClipped(offsets)
            }
          };
        }
        default: {
          return {};
        }
      }
    }
  };
};
const originSides = /* @__PURE__ */ new Set(["left", "top"]);
async function convertValueToCoords(state, options) {
  const {
    placement,
    platform: platform2,
    elements
  } = state;
  const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
  const side = getSide(placement);
  const alignment = getAlignment(placement);
  const isVertical = getSideAxis(placement) === "y";
  const mainAxisMulti = originSides.has(side) ? -1 : 1;
  const crossAxisMulti = rtl && isVertical ? -1 : 1;
  const rawValue = evaluate(options, state);
  let {
    mainAxis,
    crossAxis,
    alignmentAxis
  } = typeof rawValue === "number" ? {
    mainAxis: rawValue,
    crossAxis: 0,
    alignmentAxis: null
  } : {
    mainAxis: rawValue.mainAxis || 0,
    crossAxis: rawValue.crossAxis || 0,
    alignmentAxis: rawValue.alignmentAxis
  };
  if (alignment && typeof alignmentAxis === "number") {
    crossAxis = alignment === "end" ? alignmentAxis * -1 : alignmentAxis;
  }
  return isVertical ? {
    x: crossAxis * crossAxisMulti,
    y: mainAxis * mainAxisMulti
  } : {
    x: mainAxis * mainAxisMulti,
    y: crossAxis * crossAxisMulti
  };
}
const offset$2 = function(options) {
  if (options === void 0) {
    options = 0;
  }
  return {
    name: "offset",
    options,
    async fn(state) {
      var _middlewareData$offse, _middlewareData$arrow;
      const {
        x,
        y,
        placement,
        middlewareData
      } = state;
      const diffCoords = await convertValueToCoords(state, options);
      if (placement === ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse.placement) && (_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
        return {};
      }
      return {
        x: x + diffCoords.x,
        y: y + diffCoords.y,
        data: {
          ...diffCoords,
          placement
        }
      };
    }
  };
};
const shift$2 = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "shift",
    options,
    async fn(state) {
      const {
        x,
        y,
        placement,
        platform: platform2
      } = state;
      const {
        mainAxis: checkMainAxis = true,
        crossAxis: checkCrossAxis = false,
        limiter = {
          fn: (_ref) => {
            let {
              x: x2,
              y: y2
            } = _ref;
            return {
              x: x2,
              y: y2
            };
          }
        },
        ...detectOverflowOptions
      } = evaluate(options, state);
      const coords = {
        x,
        y
      };
      const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
      const crossAxis = getSideAxis(getSide(placement));
      const mainAxis = getOppositeAxis(crossAxis);
      let mainAxisCoord = coords[mainAxis];
      let crossAxisCoord = coords[crossAxis];
      if (checkMainAxis) {
        const minSide = mainAxis === "y" ? "top" : "left";
        const maxSide = mainAxis === "y" ? "bottom" : "right";
        const min2 = mainAxisCoord + overflow[minSide];
        const max2 = mainAxisCoord - overflow[maxSide];
        mainAxisCoord = clamp(min2, mainAxisCoord, max2);
      }
      if (checkCrossAxis) {
        const minSide = crossAxis === "y" ? "top" : "left";
        const maxSide = crossAxis === "y" ? "bottom" : "right";
        const min2 = crossAxisCoord + overflow[minSide];
        const max2 = crossAxisCoord - overflow[maxSide];
        crossAxisCoord = clamp(min2, crossAxisCoord, max2);
      }
      const limitedCoords = limiter.fn({
        ...state,
        [mainAxis]: mainAxisCoord,
        [crossAxis]: crossAxisCoord
      });
      return {
        ...limitedCoords,
        data: {
          x: limitedCoords.x - x,
          y: limitedCoords.y - y,
          enabled: {
            [mainAxis]: checkMainAxis,
            [crossAxis]: checkCrossAxis
          }
        }
      };
    }
  };
};
const limitShift$2 = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    options,
    fn(state) {
      const {
        x,
        y,
        placement,
        rects,
        middlewareData
      } = state;
      const {
        offset: offset2 = 0,
        mainAxis: checkMainAxis = true,
        crossAxis: checkCrossAxis = true
      } = evaluate(options, state);
      const coords = {
        x,
        y
      };
      const crossAxis = getSideAxis(placement);
      const mainAxis = getOppositeAxis(crossAxis);
      let mainAxisCoord = coords[mainAxis];
      let crossAxisCoord = coords[crossAxis];
      const rawOffset = evaluate(offset2, state);
      const computedOffset = typeof rawOffset === "number" ? {
        mainAxis: rawOffset,
        crossAxis: 0
      } : {
        mainAxis: 0,
        crossAxis: 0,
        ...rawOffset
      };
      if (checkMainAxis) {
        const len = mainAxis === "y" ? "height" : "width";
        const limitMin = rects.reference[mainAxis] - rects.floating[len] + computedOffset.mainAxis;
        const limitMax = rects.reference[mainAxis] + rects.reference[len] - computedOffset.mainAxis;
        if (mainAxisCoord < limitMin) {
          mainAxisCoord = limitMin;
        } else if (mainAxisCoord > limitMax) {
          mainAxisCoord = limitMax;
        }
      }
      if (checkCrossAxis) {
        var _middlewareData$offse, _middlewareData$offse2;
        const len = mainAxis === "y" ? "width" : "height";
        const isOriginSide = originSides.has(getSide(placement));
        const limitMin = rects.reference[crossAxis] - rects.floating[len] + (isOriginSide ? ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse[crossAxis]) || 0 : 0) + (isOriginSide ? 0 : computedOffset.crossAxis);
        const limitMax = rects.reference[crossAxis] + rects.reference[len] + (isOriginSide ? 0 : ((_middlewareData$offse2 = middlewareData.offset) == null ? void 0 : _middlewareData$offse2[crossAxis]) || 0) - (isOriginSide ? computedOffset.crossAxis : 0);
        if (crossAxisCoord < limitMin) {
          crossAxisCoord = limitMin;
        } else if (crossAxisCoord > limitMax) {
          crossAxisCoord = limitMax;
        }
      }
      return {
        [mainAxis]: mainAxisCoord,
        [crossAxis]: crossAxisCoord
      };
    }
  };
};
const size$2 = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "size",
    options,
    async fn(state) {
      var _state$middlewareData, _state$middlewareData2;
      const {
        placement,
        rects,
        platform: platform2,
        elements
      } = state;
      const {
        apply = () => {
        },
        ...detectOverflowOptions
      } = evaluate(options, state);
      const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
      const side = getSide(placement);
      const alignment = getAlignment(placement);
      const isYAxis = getSideAxis(placement) === "y";
      const {
        width,
        height
      } = rects.floating;
      let heightSide;
      let widthSide;
      if (side === "top" || side === "bottom") {
        heightSide = side;
        widthSide = alignment === (await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating)) ? "start" : "end") ? "left" : "right";
      } else {
        widthSide = side;
        heightSide = alignment === "end" ? "top" : "bottom";
      }
      const maximumClippingHeight = height - overflow.top - overflow.bottom;
      const maximumClippingWidth = width - overflow.left - overflow.right;
      const overflowAvailableHeight = min(height - overflow[heightSide], maximumClippingHeight);
      const overflowAvailableWidth = min(width - overflow[widthSide], maximumClippingWidth);
      const noShift = !state.middlewareData.shift;
      let availableHeight = overflowAvailableHeight;
      let availableWidth = overflowAvailableWidth;
      if ((_state$middlewareData = state.middlewareData.shift) != null && _state$middlewareData.enabled.x) {
        availableWidth = maximumClippingWidth;
      }
      if ((_state$middlewareData2 = state.middlewareData.shift) != null && _state$middlewareData2.enabled.y) {
        availableHeight = maximumClippingHeight;
      }
      if (noShift && !alignment) {
        const xMin = max(overflow.left, 0);
        const xMax = max(overflow.right, 0);
        const yMin = max(overflow.top, 0);
        const yMax = max(overflow.bottom, 0);
        if (isYAxis) {
          availableWidth = width - 2 * (xMin !== 0 || xMax !== 0 ? xMin + xMax : max(overflow.left, overflow.right));
        } else {
          availableHeight = height - 2 * (yMin !== 0 || yMax !== 0 ? yMin + yMax : max(overflow.top, overflow.bottom));
        }
      }
      await apply({
        ...state,
        availableWidth,
        availableHeight
      });
      const nextDimensions = await platform2.getDimensions(elements.floating);
      if (width !== nextDimensions.width || height !== nextDimensions.height) {
        return {
          reset: {
            rects: true
          }
        };
      }
      return {};
    }
  };
};
function hasWindow() {
  return typeof window !== "undefined";
}
function getNodeName(node2) {
  if (isNode(node2)) {
    return (node2.nodeName || "").toLowerCase();
  }
  return "#document";
}
function getWindow(node2) {
  var _node$ownerDocument;
  return (node2 == null || (_node$ownerDocument = node2.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
}
function getDocumentElement(node2) {
  var _ref;
  return (_ref = (isNode(node2) ? node2.ownerDocument : node2.document) || window.document) == null ? void 0 : _ref.documentElement;
}
function isNode(value) {
  if (!hasWindow()) {
    return false;
  }
  return value instanceof Node || value instanceof getWindow(value).Node;
}
function isElement(value) {
  if (!hasWindow()) {
    return false;
  }
  return value instanceof Element || value instanceof getWindow(value).Element;
}
function isHTMLElement(value) {
  if (!hasWindow()) {
    return false;
  }
  return value instanceof HTMLElement || value instanceof getWindow(value).HTMLElement;
}
function isShadowRoot(value) {
  if (!hasWindow() || typeof ShadowRoot === "undefined") {
    return false;
  }
  return value instanceof ShadowRoot || value instanceof getWindow(value).ShadowRoot;
}
function isOverflowElement(element2) {
  const {
    overflow,
    overflowX,
    overflowY,
    display
  } = getComputedStyle$1(element2);
  return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && display !== "inline" && display !== "contents";
}
function isTableElement(element2) {
  return /^(table|td|th)$/.test(getNodeName(element2));
}
function isTopLayer(element2) {
  try {
    if (element2.matches(":popover-open")) {
      return true;
    }
  } catch (_e) {
  }
  try {
    return element2.matches(":modal");
  } catch (_e) {
    return false;
  }
}
const willChangeRe = /transform|translate|scale|rotate|perspective|filter/;
const containRe = /paint|layout|strict|content/;
const isNotNone = (value) => !!value && value !== "none";
let isWebKitValue;
function isContainingBlock(elementOrCss) {
  const css = isElement(elementOrCss) ? getComputedStyle$1(elementOrCss) : elementOrCss;
  return isNotNone(css.transform) || isNotNone(css.translate) || isNotNone(css.scale) || isNotNone(css.rotate) || isNotNone(css.perspective) || !isWebKit() && (isNotNone(css.backdropFilter) || isNotNone(css.filter)) || willChangeRe.test(css.willChange || "") || containRe.test(css.contain || "");
}
function getContainingBlock(element2) {
  let currentNode = getParentNode(element2);
  while (isHTMLElement(currentNode) && !isLastTraversableNode(currentNode)) {
    if (isContainingBlock(currentNode)) {
      return currentNode;
    } else if (isTopLayer(currentNode)) {
      return null;
    }
    currentNode = getParentNode(currentNode);
  }
  return null;
}
function isWebKit() {
  if (isWebKitValue == null) {
    isWebKitValue = typeof CSS !== "undefined" && CSS.supports && CSS.supports("-webkit-backdrop-filter", "none");
  }
  return isWebKitValue;
}
function isLastTraversableNode(node2) {
  return /^(html|body|#document)$/.test(getNodeName(node2));
}
function getComputedStyle$1(element2) {
  return getWindow(element2).getComputedStyle(element2);
}
function getNodeScroll(element2) {
  if (isElement(element2)) {
    return {
      scrollLeft: element2.scrollLeft,
      scrollTop: element2.scrollTop
    };
  }
  return {
    scrollLeft: element2.scrollX,
    scrollTop: element2.scrollY
  };
}
function getParentNode(node2) {
  if (getNodeName(node2) === "html") {
    return node2;
  }
  const result = (
    // Step into the shadow DOM of the parent of a slotted node.
    node2.assignedSlot || // DOM Element detected.
    node2.parentNode || // ShadowRoot detected.
    isShadowRoot(node2) && node2.host || // Fallback.
    getDocumentElement(node2)
  );
  return isShadowRoot(result) ? result.host : result;
}
function getNearestOverflowAncestor(node2) {
  const parentNode = getParentNode(node2);
  if (isLastTraversableNode(parentNode)) {
    return node2.ownerDocument ? node2.ownerDocument.body : node2.body;
  }
  if (isHTMLElement(parentNode) && isOverflowElement(parentNode)) {
    return parentNode;
  }
  return getNearestOverflowAncestor(parentNode);
}
function getOverflowAncestors(node2, list2, traverseIframes) {
  var _node$ownerDocument2;
  if (list2 === void 0) {
    list2 = [];
  }
  if (traverseIframes === void 0) {
    traverseIframes = true;
  }
  const scrollableAncestor = getNearestOverflowAncestor(node2);
  const isBody = scrollableAncestor === ((_node$ownerDocument2 = node2.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
  const win = getWindow(scrollableAncestor);
  if (isBody) {
    const frameElement = getFrameElement(win);
    return list2.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], frameElement && traverseIframes ? getOverflowAncestors(frameElement) : []);
  } else {
    return list2.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
  }
}
function getFrameElement(win) {
  return win.parent && Object.getPrototypeOf(win.parent) ? win.frameElement : null;
}
function getCssDimensions(element2) {
  const css = getComputedStyle$1(element2);
  let width = parseFloat(css.width) || 0;
  let height = parseFloat(css.height) || 0;
  const hasOffset = isHTMLElement(element2);
  const offsetWidth = hasOffset ? element2.offsetWidth : width;
  const offsetHeight = hasOffset ? element2.offsetHeight : height;
  const shouldFallback = round(width) !== offsetWidth || round(height) !== offsetHeight;
  if (shouldFallback) {
    width = offsetWidth;
    height = offsetHeight;
  }
  return {
    width,
    height,
    $: shouldFallback
  };
}
function unwrapElement(element2) {
  return !isElement(element2) ? element2.contextElement : element2;
}
function getScale(element2) {
  const domElement = unwrapElement(element2);
  if (!isHTMLElement(domElement)) {
    return createCoords(1);
  }
  const rect = domElement.getBoundingClientRect();
  const {
    width,
    height,
    $
  } = getCssDimensions(domElement);
  let x = ($ ? round(rect.width) : rect.width) / width;
  let y = ($ ? round(rect.height) : rect.height) / height;
  if (!x || !Number.isFinite(x)) {
    x = 1;
  }
  if (!y || !Number.isFinite(y)) {
    y = 1;
  }
  return {
    x,
    y
  };
}
const noOffsets = /* @__PURE__ */ createCoords(0);
function getVisualOffsets(element2) {
  const win = getWindow(element2);
  if (!isWebKit() || !win.visualViewport) {
    return noOffsets;
  }
  return {
    x: win.visualViewport.offsetLeft,
    y: win.visualViewport.offsetTop
  };
}
function shouldAddVisualOffsets(element2, isFixed, floatingOffsetParent) {
  if (isFixed === void 0) {
    isFixed = false;
  }
  if (!floatingOffsetParent || isFixed && floatingOffsetParent !== getWindow(element2)) {
    return false;
  }
  return isFixed;
}
function getBoundingClientRect(element2, includeScale, isFixedStrategy, offsetParent) {
  if (includeScale === void 0) {
    includeScale = false;
  }
  if (isFixedStrategy === void 0) {
    isFixedStrategy = false;
  }
  const clientRect = element2.getBoundingClientRect();
  const domElement = unwrapElement(element2);
  let scale = createCoords(1);
  if (includeScale) {
    if (offsetParent) {
      if (isElement(offsetParent)) {
        scale = getScale(offsetParent);
      }
    } else {
      scale = getScale(element2);
    }
  }
  const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
  let x = (clientRect.left + visualOffsets.x) / scale.x;
  let y = (clientRect.top + visualOffsets.y) / scale.y;
  let width = clientRect.width / scale.x;
  let height = clientRect.height / scale.y;
  if (domElement) {
    const win = getWindow(domElement);
    const offsetWin = offsetParent && isElement(offsetParent) ? getWindow(offsetParent) : offsetParent;
    let currentWin = win;
    let currentIFrame = getFrameElement(currentWin);
    while (currentIFrame && offsetParent && offsetWin !== currentWin) {
      const iframeScale = getScale(currentIFrame);
      const iframeRect = currentIFrame.getBoundingClientRect();
      const css = getComputedStyle$1(currentIFrame);
      const left = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css.paddingLeft)) * iframeScale.x;
      const top = iframeRect.top + (currentIFrame.clientTop + parseFloat(css.paddingTop)) * iframeScale.y;
      x *= iframeScale.x;
      y *= iframeScale.y;
      width *= iframeScale.x;
      height *= iframeScale.y;
      x += left;
      y += top;
      currentWin = getWindow(currentIFrame);
      currentIFrame = getFrameElement(currentWin);
    }
  }
  return rectToClientRect({
    width,
    height,
    x,
    y
  });
}
function getWindowScrollBarX(element2, rect) {
  const leftScroll = getNodeScroll(element2).scrollLeft;
  if (!rect) {
    return getBoundingClientRect(getDocumentElement(element2)).left + leftScroll;
  }
  return rect.left + leftScroll;
}
function getHTMLOffset(documentElement, scroll) {
  const htmlRect = documentElement.getBoundingClientRect();
  const x = htmlRect.left + scroll.scrollLeft - getWindowScrollBarX(documentElement, htmlRect);
  const y = htmlRect.top + scroll.scrollTop;
  return {
    x,
    y
  };
}
function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
  let {
    elements,
    rect,
    offsetParent,
    strategy
  } = _ref;
  const isFixed = strategy === "fixed";
  const documentElement = getDocumentElement(offsetParent);
  const topLayer = elements ? isTopLayer(elements.floating) : false;
  if (offsetParent === documentElement || topLayer && isFixed) {
    return rect;
  }
  let scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  let scale = createCoords(1);
  const offsets = createCoords(0);
  const isOffsetParentAnElement = isHTMLElement(offsetParent);
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isOffsetParentAnElement) {
      const offsetRect = getBoundingClientRect(offsetParent);
      scale = getScale(offsetParent);
      offsets.x = offsetRect.x + offsetParent.clientLeft;
      offsets.y = offsetRect.y + offsetParent.clientTop;
    }
  }
  const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
  return {
    width: rect.width * scale.x,
    height: rect.height * scale.y,
    x: rect.x * scale.x - scroll.scrollLeft * scale.x + offsets.x + htmlOffset.x,
    y: rect.y * scale.y - scroll.scrollTop * scale.y + offsets.y + htmlOffset.y
  };
}
function getClientRects(element2) {
  return Array.from(element2.getClientRects());
}
function getDocumentRect(element2) {
  const html2 = getDocumentElement(element2);
  const scroll = getNodeScroll(element2);
  const body = element2.ownerDocument.body;
  const width = max(html2.scrollWidth, html2.clientWidth, body.scrollWidth, body.clientWidth);
  const height = max(html2.scrollHeight, html2.clientHeight, body.scrollHeight, body.clientHeight);
  let x = -scroll.scrollLeft + getWindowScrollBarX(element2);
  const y = -scroll.scrollTop;
  if (getComputedStyle$1(body).direction === "rtl") {
    x += max(html2.clientWidth, body.clientWidth) - width;
  }
  return {
    width,
    height,
    x,
    y
  };
}
const SCROLLBAR_MAX = 25;
function getViewportRect(element2, strategy) {
  const win = getWindow(element2);
  const html2 = getDocumentElement(element2);
  const visualViewport = win.visualViewport;
  let width = html2.clientWidth;
  let height = html2.clientHeight;
  let x = 0;
  let y = 0;
  if (visualViewport) {
    width = visualViewport.width;
    height = visualViewport.height;
    const visualViewportBased = isWebKit();
    if (!visualViewportBased || visualViewportBased && strategy === "fixed") {
      x = visualViewport.offsetLeft;
      y = visualViewport.offsetTop;
    }
  }
  const windowScrollbarX = getWindowScrollBarX(html2);
  if (windowScrollbarX <= 0) {
    const doc = html2.ownerDocument;
    const body = doc.body;
    const bodyStyles = getComputedStyle(body);
    const bodyMarginInline = doc.compatMode === "CSS1Compat" ? parseFloat(bodyStyles.marginLeft) + parseFloat(bodyStyles.marginRight) || 0 : 0;
    const clippingStableScrollbarWidth = Math.abs(html2.clientWidth - body.clientWidth - bodyMarginInline);
    if (clippingStableScrollbarWidth <= SCROLLBAR_MAX) {
      width -= clippingStableScrollbarWidth;
    }
  } else if (windowScrollbarX <= SCROLLBAR_MAX) {
    width += windowScrollbarX;
  }
  return {
    width,
    height,
    x,
    y
  };
}
function getInnerBoundingClientRect(element2, strategy) {
  const clientRect = getBoundingClientRect(element2, true, strategy === "fixed");
  const top = clientRect.top + element2.clientTop;
  const left = clientRect.left + element2.clientLeft;
  const scale = isHTMLElement(element2) ? getScale(element2) : createCoords(1);
  const width = element2.clientWidth * scale.x;
  const height = element2.clientHeight * scale.y;
  const x = left * scale.x;
  const y = top * scale.y;
  return {
    width,
    height,
    x,
    y
  };
}
function getClientRectFromClippingAncestor(element2, clippingAncestor, strategy) {
  let rect;
  if (clippingAncestor === "viewport") {
    rect = getViewportRect(element2, strategy);
  } else if (clippingAncestor === "document") {
    rect = getDocumentRect(getDocumentElement(element2));
  } else if (isElement(clippingAncestor)) {
    rect = getInnerBoundingClientRect(clippingAncestor, strategy);
  } else {
    const visualOffsets = getVisualOffsets(element2);
    rect = {
      x: clippingAncestor.x - visualOffsets.x,
      y: clippingAncestor.y - visualOffsets.y,
      width: clippingAncestor.width,
      height: clippingAncestor.height
    };
  }
  return rectToClientRect(rect);
}
function hasFixedPositionAncestor(element2, stopNode) {
  const parentNode = getParentNode(element2);
  if (parentNode === stopNode || !isElement(parentNode) || isLastTraversableNode(parentNode)) {
    return false;
  }
  return getComputedStyle$1(parentNode).position === "fixed" || hasFixedPositionAncestor(parentNode, stopNode);
}
function getClippingElementAncestors(element2, cache) {
  const cachedResult = cache.get(element2);
  if (cachedResult) {
    return cachedResult;
  }
  let result = getOverflowAncestors(element2, [], false).filter((el) => isElement(el) && getNodeName(el) !== "body");
  let currentContainingBlockComputedStyle = null;
  const elementIsFixed = getComputedStyle$1(element2).position === "fixed";
  let currentNode = elementIsFixed ? getParentNode(element2) : element2;
  while (isElement(currentNode) && !isLastTraversableNode(currentNode)) {
    const computedStyle = getComputedStyle$1(currentNode);
    const currentNodeIsContaining = isContainingBlock(currentNode);
    if (!currentNodeIsContaining && computedStyle.position === "fixed") {
      currentContainingBlockComputedStyle = null;
    }
    const shouldDropCurrentNode = elementIsFixed ? !currentNodeIsContaining && !currentContainingBlockComputedStyle : !currentNodeIsContaining && computedStyle.position === "static" && !!currentContainingBlockComputedStyle && (currentContainingBlockComputedStyle.position === "absolute" || currentContainingBlockComputedStyle.position === "fixed") || isOverflowElement(currentNode) && !currentNodeIsContaining && hasFixedPositionAncestor(element2, currentNode);
    if (shouldDropCurrentNode) {
      result = result.filter((ancestor) => ancestor !== currentNode);
    } else {
      currentContainingBlockComputedStyle = computedStyle;
    }
    currentNode = getParentNode(currentNode);
  }
  cache.set(element2, result);
  return result;
}
function getClippingRect(_ref) {
  let {
    element: element2,
    boundary,
    rootBoundary,
    strategy
  } = _ref;
  const elementClippingAncestors = boundary === "clippingAncestors" ? isTopLayer(element2) ? [] : getClippingElementAncestors(element2, this._c) : [].concat(boundary);
  const clippingAncestors = [...elementClippingAncestors, rootBoundary];
  const firstRect = getClientRectFromClippingAncestor(element2, clippingAncestors[0], strategy);
  let top = firstRect.top;
  let right = firstRect.right;
  let bottom = firstRect.bottom;
  let left = firstRect.left;
  for (let i = 1; i < clippingAncestors.length; i++) {
    const rect = getClientRectFromClippingAncestor(element2, clippingAncestors[i], strategy);
    top = max(rect.top, top);
    right = min(rect.right, right);
    bottom = min(rect.bottom, bottom);
    left = max(rect.left, left);
  }
  return {
    width: right - left,
    height: bottom - top,
    x: left,
    y: top
  };
}
function getDimensions(element2) {
  const {
    width,
    height
  } = getCssDimensions(element2);
  return {
    width,
    height
  };
}
function getRectRelativeToOffsetParent(element2, offsetParent, strategy) {
  const isOffsetParentAnElement = isHTMLElement(offsetParent);
  const documentElement = getDocumentElement(offsetParent);
  const isFixed = strategy === "fixed";
  const rect = getBoundingClientRect(element2, true, isFixed, offsetParent);
  let scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  const offsets = createCoords(0);
  function setLeftRTLScrollbarOffset() {
    offsets.x = getWindowScrollBarX(documentElement);
  }
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isOffsetParentAnElement) {
      const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
      offsets.x = offsetRect.x + offsetParent.clientLeft;
      offsets.y = offsetRect.y + offsetParent.clientTop;
    } else if (documentElement) {
      setLeftRTLScrollbarOffset();
    }
  }
  if (isFixed && !isOffsetParentAnElement && documentElement) {
    setLeftRTLScrollbarOffset();
  }
  const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
  const x = rect.left + scroll.scrollLeft - offsets.x - htmlOffset.x;
  const y = rect.top + scroll.scrollTop - offsets.y - htmlOffset.y;
  return {
    x,
    y,
    width: rect.width,
    height: rect.height
  };
}
function isStaticPositioned(element2) {
  return getComputedStyle$1(element2).position === "static";
}
function getTrueOffsetParent(element2, polyfill) {
  if (!isHTMLElement(element2) || getComputedStyle$1(element2).position === "fixed") {
    return null;
  }
  if (polyfill) {
    return polyfill(element2);
  }
  let rawOffsetParent = element2.offsetParent;
  if (getDocumentElement(element2) === rawOffsetParent) {
    rawOffsetParent = rawOffsetParent.ownerDocument.body;
  }
  return rawOffsetParent;
}
function getOffsetParent(element2, polyfill) {
  const win = getWindow(element2);
  if (isTopLayer(element2)) {
    return win;
  }
  if (!isHTMLElement(element2)) {
    let svgOffsetParent = getParentNode(element2);
    while (svgOffsetParent && !isLastTraversableNode(svgOffsetParent)) {
      if (isElement(svgOffsetParent) && !isStaticPositioned(svgOffsetParent)) {
        return svgOffsetParent;
      }
      svgOffsetParent = getParentNode(svgOffsetParent);
    }
    return win;
  }
  let offsetParent = getTrueOffsetParent(element2, polyfill);
  while (offsetParent && isTableElement(offsetParent) && isStaticPositioned(offsetParent)) {
    offsetParent = getTrueOffsetParent(offsetParent, polyfill);
  }
  if (offsetParent && isLastTraversableNode(offsetParent) && isStaticPositioned(offsetParent) && !isContainingBlock(offsetParent)) {
    return win;
  }
  return offsetParent || getContainingBlock(element2) || win;
}
const getElementRects = async function(data) {
  const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
  const getDimensionsFn = this.getDimensions;
  const floatingDimensions = await getDimensionsFn(data.floating);
  return {
    reference: getRectRelativeToOffsetParent(data.reference, await getOffsetParentFn(data.floating), data.strategy),
    floating: {
      x: 0,
      y: 0,
      width: floatingDimensions.width,
      height: floatingDimensions.height
    }
  };
};
function isRTL(element2) {
  return getComputedStyle$1(element2).direction === "rtl";
}
const platform = {
  convertOffsetParentRelativeRectToViewportRelativeRect,
  getDocumentElement,
  getClippingRect,
  getOffsetParent,
  getElementRects,
  getClientRects,
  getDimensions,
  getScale,
  isElement,
  isRTL
};
function rectsAreEqual(a, b) {
  return a.x === b.x && a.y === b.y && a.width === b.width && a.height === b.height;
}
function observeMove(element2, onMove) {
  let io = null;
  let timeoutId;
  const root2 = getDocumentElement(element2);
  function cleanup() {
    var _io;
    clearTimeout(timeoutId);
    (_io = io) == null || _io.disconnect();
    io = null;
  }
  function refresh(skip, threshold) {
    if (skip === void 0) {
      skip = false;
    }
    if (threshold === void 0) {
      threshold = 1;
    }
    cleanup();
    const elementRectForRootMargin = element2.getBoundingClientRect();
    const {
      left,
      top,
      width,
      height
    } = elementRectForRootMargin;
    if (!skip) {
      onMove();
    }
    if (!width || !height) {
      return;
    }
    const insetTop = floor(top);
    const insetRight = floor(root2.clientWidth - (left + width));
    const insetBottom = floor(root2.clientHeight - (top + height));
    const insetLeft = floor(left);
    const rootMargin = -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px";
    const options = {
      rootMargin,
      threshold: max(0, min(1, threshold)) || 1
    };
    let isFirstUpdate = true;
    function handleObserve(entries) {
      const ratio = entries[0].intersectionRatio;
      if (ratio !== threshold) {
        if (!isFirstUpdate) {
          return refresh();
        }
        if (!ratio) {
          timeoutId = setTimeout(() => {
            refresh(false, 1e-7);
          }, 1e3);
        } else {
          refresh(false, ratio);
        }
      }
      if (ratio === 1 && !rectsAreEqual(elementRectForRootMargin, element2.getBoundingClientRect())) {
        refresh();
      }
      isFirstUpdate = false;
    }
    try {
      io = new IntersectionObserver(handleObserve, {
        ...options,
        // Handle <iframe>s
        root: root2.ownerDocument
      });
    } catch (_e) {
      io = new IntersectionObserver(handleObserve, options);
    }
    io.observe(element2);
  }
  refresh(true);
  return cleanup;
}
function autoUpdate(reference, floating, update, options) {
  if (options === void 0) {
    options = {};
  }
  const {
    ancestorScroll = true,
    ancestorResize = true,
    elementResize = typeof ResizeObserver === "function",
    layoutShift = typeof IntersectionObserver === "function",
    animationFrame = false
  } = options;
  const referenceEl = unwrapElement(reference);
  const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...floating ? getOverflowAncestors(floating) : []] : [];
  ancestors.forEach((ancestor) => {
    ancestorScroll && ancestor.addEventListener("scroll", update, {
      passive: true
    });
    ancestorResize && ancestor.addEventListener("resize", update);
  });
  const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update) : null;
  let reobserveFrame = -1;
  let resizeObserver = null;
  if (elementResize) {
    resizeObserver = new ResizeObserver((_ref) => {
      let [firstEntry] = _ref;
      if (firstEntry && firstEntry.target === referenceEl && resizeObserver && floating) {
        resizeObserver.unobserve(floating);
        cancelAnimationFrame(reobserveFrame);
        reobserveFrame = requestAnimationFrame(() => {
          var _resizeObserver;
          (_resizeObserver = resizeObserver) == null || _resizeObserver.observe(floating);
        });
      }
      update();
    });
    if (referenceEl && !animationFrame) {
      resizeObserver.observe(referenceEl);
    }
    if (floating) {
      resizeObserver.observe(floating);
    }
  }
  let frameId;
  let prevRefRect = animationFrame ? getBoundingClientRect(reference) : null;
  if (animationFrame) {
    frameLoop();
  }
  function frameLoop() {
    const nextRefRect = getBoundingClientRect(reference);
    if (prevRefRect && !rectsAreEqual(prevRefRect, nextRefRect)) {
      update();
    }
    prevRefRect = nextRefRect;
    frameId = requestAnimationFrame(frameLoop);
  }
  update();
  return () => {
    var _resizeObserver2;
    ancestors.forEach((ancestor) => {
      ancestorScroll && ancestor.removeEventListener("scroll", update);
      ancestorResize && ancestor.removeEventListener("resize", update);
    });
    cleanupIo == null || cleanupIo();
    (_resizeObserver2 = resizeObserver) == null || _resizeObserver2.disconnect();
    resizeObserver = null;
    if (animationFrame) {
      cancelAnimationFrame(frameId);
    }
  };
}
const offset$1 = offset$2;
const shift$1 = shift$2;
const flip$1 = flip$2;
const size$1 = size$2;
const hide$1 = hide$2;
const arrow$2 = arrow$3;
const limitShift$1 = limitShift$2;
const computePosition = (reference, floating, options) => {
  const cache = /* @__PURE__ */ new Map();
  const mergedOptions = {
    platform,
    ...options
  };
  const platformWithCache = {
    ...mergedOptions.platform,
    _c: cache
  };
  return computePosition$1(reference, floating, {
    ...mergedOptions,
    platform: platformWithCache
  });
};
var isClient = typeof document !== "undefined";
var noop = function noop2() {
};
var index = isClient ? reactExports.useLayoutEffect : noop;
function deepEqual(a, b) {
  if (a === b) {
    return true;
  }
  if (typeof a !== typeof b) {
    return false;
  }
  if (typeof a === "function" && a.toString() === b.toString()) {
    return true;
  }
  let length;
  let i;
  let keys2;
  if (a && b && typeof a === "object") {
    if (Array.isArray(a)) {
      length = a.length;
      if (length !== b.length) return false;
      for (i = length; i-- !== 0; ) {
        if (!deepEqual(a[i], b[i])) {
          return false;
        }
      }
      return true;
    }
    keys2 = Object.keys(a);
    length = keys2.length;
    if (length !== Object.keys(b).length) {
      return false;
    }
    for (i = length; i-- !== 0; ) {
      if (!{}.hasOwnProperty.call(b, keys2[i])) {
        return false;
      }
    }
    for (i = length; i-- !== 0; ) {
      const key = keys2[i];
      if (key === "_owner" && a.$$typeof) {
        continue;
      }
      if (!deepEqual(a[key], b[key])) {
        return false;
      }
    }
    return true;
  }
  return a !== a && b !== b;
}
function getDPR(element2) {
  if (typeof window === "undefined") {
    return 1;
  }
  const win = element2.ownerDocument.defaultView || window;
  return win.devicePixelRatio || 1;
}
function roundByDPR(element2, value) {
  const dpr = getDPR(element2);
  return Math.round(value * dpr) / dpr;
}
function useLatestRef(value) {
  const ref = reactExports.useRef(value);
  index(() => {
    ref.current = value;
  });
  return ref;
}
function useFloating(options) {
  if (options === void 0) {
    options = {};
  }
  const {
    placement = "bottom",
    strategy = "absolute",
    middleware = [],
    platform: platform2,
    elements: {
      reference: externalReference,
      floating: externalFloating
    } = {},
    transform = true,
    whileElementsMounted,
    open
  } = options;
  const [data, setData] = reactExports.useState({
    x: 0,
    y: 0,
    strategy,
    placement,
    middlewareData: {},
    isPositioned: false
  });
  const [latestMiddleware, setLatestMiddleware] = reactExports.useState(middleware);
  if (!deepEqual(latestMiddleware, middleware)) {
    setLatestMiddleware(middleware);
  }
  const [_reference, _setReference] = reactExports.useState(null);
  const [_floating, _setFloating] = reactExports.useState(null);
  const setReference = reactExports.useCallback((node2) => {
    if (node2 !== referenceRef.current) {
      referenceRef.current = node2;
      _setReference(node2);
    }
  }, []);
  const setFloating = reactExports.useCallback((node2) => {
    if (node2 !== floatingRef.current) {
      floatingRef.current = node2;
      _setFloating(node2);
    }
  }, []);
  const referenceEl = externalReference || _reference;
  const floatingEl = externalFloating || _floating;
  const referenceRef = reactExports.useRef(null);
  const floatingRef = reactExports.useRef(null);
  const dataRef = reactExports.useRef(data);
  const hasWhileElementsMounted = whileElementsMounted != null;
  const whileElementsMountedRef = useLatestRef(whileElementsMounted);
  const platformRef = useLatestRef(platform2);
  const openRef = useLatestRef(open);
  const update = reactExports.useCallback(() => {
    if (!referenceRef.current || !floatingRef.current) {
      return;
    }
    const config = {
      placement,
      strategy,
      middleware: latestMiddleware
    };
    if (platformRef.current) {
      config.platform = platformRef.current;
    }
    computePosition(referenceRef.current, floatingRef.current, config).then((data2) => {
      const fullData = {
        ...data2,
        // The floating element's position may be recomputed while it's closed
        // but still mounted (such as when transitioning out). To ensure
        // `isPositioned` will be `false` initially on the next open, avoid
        // setting it to `true` when `open === false` (must be specified).
        isPositioned: openRef.current !== false
      };
      if (isMountedRef.current && !deepEqual(dataRef.current, fullData)) {
        dataRef.current = fullData;
        reactDomExports.flushSync(() => {
          setData(fullData);
        });
      }
    });
  }, [latestMiddleware, placement, strategy, platformRef, openRef]);
  index(() => {
    if (open === false && dataRef.current.isPositioned) {
      dataRef.current.isPositioned = false;
      setData((data2) => ({
        ...data2,
        isPositioned: false
      }));
    }
  }, [open]);
  const isMountedRef = reactExports.useRef(false);
  index(() => {
    isMountedRef.current = true;
    return () => {
      isMountedRef.current = false;
    };
  }, []);
  index(() => {
    if (referenceEl) referenceRef.current = referenceEl;
    if (floatingEl) floatingRef.current = floatingEl;
    if (referenceEl && floatingEl) {
      if (whileElementsMountedRef.current) {
        return whileElementsMountedRef.current(referenceEl, floatingEl, update);
      }
      update();
    }
  }, [referenceEl, floatingEl, update, whileElementsMountedRef, hasWhileElementsMounted]);
  const refs = reactExports.useMemo(() => ({
    reference: referenceRef,
    floating: floatingRef,
    setReference,
    setFloating
  }), [setReference, setFloating]);
  const elements = reactExports.useMemo(() => ({
    reference: referenceEl,
    floating: floatingEl
  }), [referenceEl, floatingEl]);
  const floatingStyles = reactExports.useMemo(() => {
    const initialStyles = {
      position: strategy,
      left: 0,
      top: 0
    };
    if (!elements.floating) {
      return initialStyles;
    }
    const x = roundByDPR(elements.floating, data.x);
    const y = roundByDPR(elements.floating, data.y);
    if (transform) {
      return {
        ...initialStyles,
        transform: "translate(" + x + "px, " + y + "px)",
        ...getDPR(elements.floating) >= 1.5 && {
          willChange: "transform"
        }
      };
    }
    return {
      position: strategy,
      left: x,
      top: y
    };
  }, [strategy, transform, elements.floating, data.x, data.y]);
  return reactExports.useMemo(() => ({
    ...data,
    update,
    refs,
    elements,
    floatingStyles
  }), [data, update, refs, elements, floatingStyles]);
}
const arrow$1 = (options) => {
  function isRef(value) {
    return {}.hasOwnProperty.call(value, "current");
  }
  return {
    name: "arrow",
    options,
    fn(state) {
      const {
        element: element2,
        padding
      } = typeof options === "function" ? options(state) : options;
      if (element2 && isRef(element2)) {
        if (element2.current != null) {
          return arrow$2({
            element: element2.current,
            padding
          }).fn(state);
        }
        return {};
      }
      if (element2) {
        return arrow$2({
          element: element2,
          padding
        }).fn(state);
      }
      return {};
    }
  };
};
const offset = (options, deps) => {
  const result = offset$1(options);
  return {
    name: result.name,
    fn: result.fn,
    options: [options, deps]
  };
};
const shift = (options, deps) => {
  const result = shift$1(options);
  return {
    name: result.name,
    fn: result.fn,
    options: [options, deps]
  };
};
const limitShift = (options, deps) => {
  const result = limitShift$1(options);
  return {
    fn: result.fn,
    options: [options, deps]
  };
};
const flip = (options, deps) => {
  const result = flip$1(options);
  return {
    name: result.name,
    fn: result.fn,
    options: [options, deps]
  };
};
const size = (options, deps) => {
  const result = size$1(options);
  return {
    name: result.name,
    fn: result.fn,
    options: [options, deps]
  };
};
const hide = (options, deps) => {
  const result = hide$1(options);
  return {
    name: result.name,
    fn: result.fn,
    options: [options, deps]
  };
};
const arrow = (options, deps) => {
  const result = arrow$1(options);
  return {
    name: result.name,
    fn: result.fn,
    options: [options, deps]
  };
};
var NAME$1 = "Arrow";
var Arrow$1 = reactExports.forwardRef((props, forwardedRef) => {
  const { children, width = 10, height = 5, ...arrowProps } = props;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Primitive.svg,
    {
      ...arrowProps,
      ref: forwardedRef,
      width,
      height,
      viewBox: "0 0 30 10",
      preserveAspectRatio: "none",
      children: props.asChild ? children : /* @__PURE__ */ jsxRuntimeExports.jsx("polygon", { points: "0,0 30,0 15,10" })
    }
  );
});
Arrow$1.displayName = NAME$1;
var Root = Arrow$1;
function useSize(element2) {
  const [size2, setSize] = reactExports.useState(void 0);
  useLayoutEffect2(() => {
    if (element2) {
      setSize({ width: element2.offsetWidth, height: element2.offsetHeight });
      const resizeObserver = new ResizeObserver((entries) => {
        if (!Array.isArray(entries)) {
          return;
        }
        if (!entries.length) {
          return;
        }
        const entry = entries[0];
        let width;
        let height;
        if ("borderBoxSize" in entry) {
          const borderSizeEntry = entry["borderBoxSize"];
          const borderSize = Array.isArray(borderSizeEntry) ? borderSizeEntry[0] : borderSizeEntry;
          width = borderSize["inlineSize"];
          height = borderSize["blockSize"];
        } else {
          width = element2.offsetWidth;
          height = element2.offsetHeight;
        }
        setSize({ width, height });
      });
      resizeObserver.observe(element2, { box: "border-box" });
      return () => resizeObserver.unobserve(element2);
    } else {
      setSize(void 0);
    }
  }, [element2]);
  return size2;
}
var POPPER_NAME = "Popper";
var [createPopperContext, createPopperScope] = createContextScope(POPPER_NAME);
var [PopperProvider, usePopperContext] = createPopperContext(POPPER_NAME);
var Popper = (props) => {
  const { __scopePopper, children } = props;
  const [anchor, setAnchor] = reactExports.useState(null);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(PopperProvider, { scope: __scopePopper, anchor, onAnchorChange: setAnchor, children });
};
Popper.displayName = POPPER_NAME;
var ANCHOR_NAME = "PopperAnchor";
var PopperAnchor = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopePopper, virtualRef, ...anchorProps } = props;
    const context = usePopperContext(ANCHOR_NAME, __scopePopper);
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref);
    const anchorRef = reactExports.useRef(null);
    reactExports.useEffect(() => {
      const previousAnchor = anchorRef.current;
      anchorRef.current = (virtualRef == null ? void 0 : virtualRef.current) || ref.current;
      if (previousAnchor !== anchorRef.current) {
        context.onAnchorChange(anchorRef.current);
      }
    });
    return virtualRef ? null : /* @__PURE__ */ jsxRuntimeExports.jsx(Primitive.div, { ...anchorProps, ref: composedRefs });
  }
);
PopperAnchor.displayName = ANCHOR_NAME;
var CONTENT_NAME$1 = "PopperContent";
var [PopperContentProvider, useContentContext] = createPopperContext(CONTENT_NAME$1);
var PopperContent = reactExports.forwardRef(
  (props, forwardedRef) => {
    var _a, _b, _c, _d, _e, _f;
    const {
      __scopePopper,
      side = "bottom",
      sideOffset = 0,
      align = "center",
      alignOffset = 0,
      arrowPadding = 0,
      avoidCollisions = true,
      collisionBoundary = [],
      collisionPadding: collisionPaddingProp = 0,
      sticky = "partial",
      hideWhenDetached = false,
      updatePositionStrategy = "optimized",
      onPlaced,
      ...contentProps
    } = props;
    const context = usePopperContext(CONTENT_NAME$1, __scopePopper);
    const [content2, setContent] = reactExports.useState(null);
    const composedRefs = useComposedRefs(forwardedRef, (node2) => setContent(node2));
    const [arrow$12, setArrow] = reactExports.useState(null);
    const arrowSize = useSize(arrow$12);
    const arrowWidth = (arrowSize == null ? void 0 : arrowSize.width) ?? 0;
    const arrowHeight = (arrowSize == null ? void 0 : arrowSize.height) ?? 0;
    const desiredPlacement = side + (align !== "center" ? "-" + align : "");
    const collisionPadding = typeof collisionPaddingProp === "number" ? collisionPaddingProp : { top: 0, right: 0, bottom: 0, left: 0, ...collisionPaddingProp };
    const boundary = Array.isArray(collisionBoundary) ? collisionBoundary : [collisionBoundary];
    const hasExplicitBoundaries = boundary.length > 0;
    const detectOverflowOptions = {
      padding: collisionPadding,
      boundary: boundary.filter(isNotNull),
      // with `strategy: 'fixed'`, this is the only way to get it to respect boundaries
      altBoundary: hasExplicitBoundaries
    };
    const { refs, floatingStyles, placement, isPositioned, middlewareData } = useFloating({
      // default to `fixed` strategy so users don't have to pick and we also avoid focus scroll issues
      strategy: "fixed",
      placement: desiredPlacement,
      whileElementsMounted: (...args) => {
        const cleanup = autoUpdate(...args, {
          animationFrame: updatePositionStrategy === "always"
        });
        return cleanup;
      },
      elements: {
        reference: context.anchor
      },
      middleware: [
        offset({ mainAxis: sideOffset + arrowHeight, alignmentAxis: alignOffset }),
        avoidCollisions && shift({
          mainAxis: true,
          crossAxis: false,
          limiter: sticky === "partial" ? limitShift() : void 0,
          ...detectOverflowOptions
        }),
        avoidCollisions && flip({ ...detectOverflowOptions }),
        size({
          ...detectOverflowOptions,
          apply: ({ elements, rects, availableWidth, availableHeight }) => {
            const { width: anchorWidth, height: anchorHeight } = rects.reference;
            const contentStyle = elements.floating.style;
            contentStyle.setProperty("--radix-popper-available-width", `${availableWidth}px`);
            contentStyle.setProperty("--radix-popper-available-height", `${availableHeight}px`);
            contentStyle.setProperty("--radix-popper-anchor-width", `${anchorWidth}px`);
            contentStyle.setProperty("--radix-popper-anchor-height", `${anchorHeight}px`);
          }
        }),
        arrow$12 && arrow({ element: arrow$12, padding: arrowPadding }),
        transformOrigin({ arrowWidth, arrowHeight }),
        hideWhenDetached && hide({ strategy: "referenceHidden", ...detectOverflowOptions })
      ]
    });
    const [placedSide, placedAlign] = getSideAndAlignFromPlacement(placement);
    const handlePlaced = useCallbackRef$1(onPlaced);
    useLayoutEffect2(() => {
      if (isPositioned) {
        handlePlaced == null ? void 0 : handlePlaced();
      }
    }, [isPositioned, handlePlaced]);
    const arrowX = (_a = middlewareData.arrow) == null ? void 0 : _a.x;
    const arrowY = (_b = middlewareData.arrow) == null ? void 0 : _b.y;
    const cannotCenterArrow = ((_c = middlewareData.arrow) == null ? void 0 : _c.centerOffset) !== 0;
    const [contentZIndex, setContentZIndex] = reactExports.useState();
    useLayoutEffect2(() => {
      if (content2) setContentZIndex(window.getComputedStyle(content2).zIndex);
    }, [content2]);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        ref: refs.setFloating,
        "data-radix-popper-content-wrapper": "",
        style: {
          ...floatingStyles,
          transform: isPositioned ? floatingStyles.transform : "translate(0, -200%)",
          // keep off the page when measuring
          minWidth: "max-content",
          zIndex: contentZIndex,
          ["--radix-popper-transform-origin"]: [
            (_d = middlewareData.transformOrigin) == null ? void 0 : _d.x,
            (_e = middlewareData.transformOrigin) == null ? void 0 : _e.y
          ].join(" "),
          // hide the content if using the hide middleware and should be hidden
          // set visibility to hidden and disable pointer events so the UI behaves
          // as if the PopperContent isn't there at all
          ...((_f = middlewareData.hide) == null ? void 0 : _f.referenceHidden) && {
            visibility: "hidden",
            pointerEvents: "none"
          }
        },
        dir: props.dir,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          PopperContentProvider,
          {
            scope: __scopePopper,
            placedSide,
            onArrowChange: setArrow,
            arrowX,
            arrowY,
            shouldHideArrow: cannotCenterArrow,
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              Primitive.div,
              {
                "data-side": placedSide,
                "data-align": placedAlign,
                ...contentProps,
                ref: composedRefs,
                style: {
                  ...contentProps.style,
                  // if the PopperContent hasn't been placed yet (not all measurements done)
                  // we prevent animations so that users's animation don't kick in too early referring wrong sides
                  animation: !isPositioned ? "none" : void 0
                }
              }
            )
          }
        )
      }
    );
  }
);
PopperContent.displayName = CONTENT_NAME$1;
var ARROW_NAME$1 = "PopperArrow";
var OPPOSITE_SIDE = {
  top: "bottom",
  right: "left",
  bottom: "top",
  left: "right"
};
var PopperArrow = reactExports.forwardRef(function PopperArrow2(props, forwardedRef) {
  const { __scopePopper, ...arrowProps } = props;
  const contentContext = useContentContext(ARROW_NAME$1, __scopePopper);
  const baseSide = OPPOSITE_SIDE[contentContext.placedSide];
  return (
    // we have to use an extra wrapper because `ResizeObserver` (used by `useSize`)
    // doesn't report size as we'd expect on SVG elements.
    // it reports their bounding box which is effectively the largest path inside the SVG.
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "span",
      {
        ref: contentContext.onArrowChange,
        style: {
          position: "absolute",
          left: contentContext.arrowX,
          top: contentContext.arrowY,
          [baseSide]: 0,
          transformOrigin: {
            top: "",
            right: "0 0",
            bottom: "center 0",
            left: "100% 0"
          }[contentContext.placedSide],
          transform: {
            top: "translateY(100%)",
            right: "translateY(50%) rotate(90deg) translateX(-50%)",
            bottom: `rotate(180deg)`,
            left: "translateY(50%) rotate(-90deg) translateX(50%)"
          }[contentContext.placedSide],
          visibility: contentContext.shouldHideArrow ? "hidden" : void 0
        },
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Root,
          {
            ...arrowProps,
            ref: forwardedRef,
            style: {
              ...arrowProps.style,
              // ensures the element can be measured correctly (mostly for if SVG)
              display: "block"
            }
          }
        )
      }
    )
  );
});
PopperArrow.displayName = ARROW_NAME$1;
function isNotNull(value) {
  return value !== null;
}
var transformOrigin = (options) => ({
  name: "transformOrigin",
  options,
  fn(data) {
    var _a, _b, _c;
    const { placement, rects, middlewareData } = data;
    const cannotCenterArrow = ((_a = middlewareData.arrow) == null ? void 0 : _a.centerOffset) !== 0;
    const isArrowHidden = cannotCenterArrow;
    const arrowWidth = isArrowHidden ? 0 : options.arrowWidth;
    const arrowHeight = isArrowHidden ? 0 : options.arrowHeight;
    const [placedSide, placedAlign] = getSideAndAlignFromPlacement(placement);
    const noArrowAlign = { start: "0%", center: "50%", end: "100%" }[placedAlign];
    const arrowXCenter = (((_b = middlewareData.arrow) == null ? void 0 : _b.x) ?? 0) + arrowWidth / 2;
    const arrowYCenter = (((_c = middlewareData.arrow) == null ? void 0 : _c.y) ?? 0) + arrowHeight / 2;
    let x = "";
    let y = "";
    if (placedSide === "bottom") {
      x = isArrowHidden ? noArrowAlign : `${arrowXCenter}px`;
      y = `${-arrowHeight}px`;
    } else if (placedSide === "top") {
      x = isArrowHidden ? noArrowAlign : `${arrowXCenter}px`;
      y = `${rects.floating.height + arrowHeight}px`;
    } else if (placedSide === "right") {
      x = `${-arrowHeight}px`;
      y = isArrowHidden ? noArrowAlign : `${arrowYCenter}px`;
    } else if (placedSide === "left") {
      x = `${rects.floating.width + arrowHeight}px`;
      y = isArrowHidden ? noArrowAlign : `${arrowYCenter}px`;
    }
    return { data: { x, y } };
  }
});
function getSideAndAlignFromPlacement(placement) {
  const [side, align = "center"] = placement.split("-");
  return [side, align];
}
var Root2$1 = Popper;
var Anchor = PopperAnchor;
var Content = PopperContent;
var Arrow = PopperArrow;
var PORTAL_NAME$1 = "Portal";
var Portal$1 = reactExports.forwardRef((props, forwardedRef) => {
  var _a;
  const { container: containerProp, ...portalProps } = props;
  const [mounted, setMounted] = reactExports.useState(false);
  useLayoutEffect2(() => setMounted(true), []);
  const container = containerProp || mounted && ((_a = globalThis == null ? void 0 : globalThis.document) == null ? void 0 : _a.body);
  return container ? ReactDOM.createPortal(/* @__PURE__ */ jsxRuntimeExports.jsx(Primitive.div, { ...portalProps, ref: forwardedRef }), container) : null;
});
Portal$1.displayName = PORTAL_NAME$1;
// @__NO_SIDE_EFFECTS__
function createSlot(ownerName) {
  const SlotClone = /* @__PURE__ */ createSlotClone(ownerName);
  const Slot2 = reactExports.forwardRef((props, forwardedRef) => {
    const { children, ...slotProps } = props;
    const childrenArray = reactExports.Children.toArray(children);
    const slottable = childrenArray.find(isSlottable);
    if (slottable) {
      const newElement = slottable.props.children;
      const newChildren = childrenArray.map((child) => {
        if (child === slottable) {
          if (reactExports.Children.count(newElement) > 1) return reactExports.Children.only(null);
          return reactExports.isValidElement(newElement) ? newElement.props.children : null;
        } else {
          return child;
        }
      });
      return /* @__PURE__ */ jsxRuntimeExports.jsx(SlotClone, { ...slotProps, ref: forwardedRef, children: reactExports.isValidElement(newElement) ? reactExports.cloneElement(newElement, void 0, newChildren) : null });
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(SlotClone, { ...slotProps, ref: forwardedRef, children });
  });
  Slot2.displayName = `${ownerName}.Slot`;
  return Slot2;
}
// @__NO_SIDE_EFFECTS__
function createSlotClone(ownerName) {
  const SlotClone = reactExports.forwardRef((props, forwardedRef) => {
    const { children, ...slotProps } = props;
    if (reactExports.isValidElement(children)) {
      const childrenRef = getElementRef(children);
      const props2 = mergeProps(slotProps, children.props);
      if (children.type !== reactExports.Fragment) {
        props2.ref = forwardedRef ? composeRefs(forwardedRef, childrenRef) : childrenRef;
      }
      return reactExports.cloneElement(children, props2);
    }
    return reactExports.Children.count(children) > 1 ? reactExports.Children.only(null) : null;
  });
  SlotClone.displayName = `${ownerName}.SlotClone`;
  return SlotClone;
}
var SLOTTABLE_IDENTIFIER = Symbol("radix.slottable");
function isSlottable(child) {
  return reactExports.isValidElement(child) && typeof child.type === "function" && "__radixId" in child.type && child.type.__radixId === SLOTTABLE_IDENTIFIER;
}
function mergeProps(slotProps, childProps) {
  const overrideProps = { ...childProps };
  for (const propName in childProps) {
    const slotPropValue = slotProps[propName];
    const childPropValue = childProps[propName];
    const isHandler = /^on[A-Z]/.test(propName);
    if (isHandler) {
      if (slotPropValue && childPropValue) {
        overrideProps[propName] = (...args) => {
          const result = childPropValue(...args);
          slotPropValue(...args);
          return result;
        };
      } else if (slotPropValue) {
        overrideProps[propName] = slotPropValue;
      }
    } else if (propName === "style") {
      overrideProps[propName] = { ...slotPropValue, ...childPropValue };
    } else if (propName === "className") {
      overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
    }
  }
  return { ...slotProps, ...overrideProps };
}
function getElementRef(element2) {
  var _a, _b;
  let getter = (_a = Object.getOwnPropertyDescriptor(element2.props, "ref")) == null ? void 0 : _a.get;
  let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
  if (mayWarn) {
    return element2.ref;
  }
  getter = (_b = Object.getOwnPropertyDescriptor(element2, "ref")) == null ? void 0 : _b.get;
  mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
  if (mayWarn) {
    return element2.props.ref;
  }
  return element2.props.ref || element2.ref;
}
var useInsertionEffect = React[" useInsertionEffect ".trim().toString()] || useLayoutEffect2;
function useControllableState({
  prop,
  defaultProp,
  onChange = () => {
  },
  caller
}) {
  const [uncontrolledProp, setUncontrolledProp, onChangeRef] = useUncontrolledState({
    defaultProp,
    onChange
  });
  const isControlled = prop !== void 0;
  const value = isControlled ? prop : uncontrolledProp;
  {
    const isControlledRef = reactExports.useRef(prop !== void 0);
    reactExports.useEffect(() => {
      const wasControlled = isControlledRef.current;
      if (wasControlled !== isControlled) {
        const from = wasControlled ? "controlled" : "uncontrolled";
        const to = isControlled ? "controlled" : "uncontrolled";
        console.warn(
          `${caller} is changing from ${from} to ${to}. Components should not switch from controlled to uncontrolled (or vice versa). Decide between using a controlled or uncontrolled value for the lifetime of the component.`
        );
      }
      isControlledRef.current = isControlled;
    }, [isControlled, caller]);
  }
  const setValue = reactExports.useCallback(
    (nextValue) => {
      var _a;
      if (isControlled) {
        const value2 = isFunction(nextValue) ? nextValue(prop) : nextValue;
        if (value2 !== prop) {
          (_a = onChangeRef.current) == null ? void 0 : _a.call(onChangeRef, value2);
        }
      } else {
        setUncontrolledProp(nextValue);
      }
    },
    [isControlled, prop, setUncontrolledProp, onChangeRef]
  );
  return [value, setValue];
}
function useUncontrolledState({
  defaultProp,
  onChange
}) {
  const [value, setValue] = reactExports.useState(defaultProp);
  const prevValueRef = reactExports.useRef(value);
  const onChangeRef = reactExports.useRef(onChange);
  useInsertionEffect(() => {
    onChangeRef.current = onChange;
  }, [onChange]);
  reactExports.useEffect(() => {
    var _a;
    if (prevValueRef.current !== value) {
      (_a = onChangeRef.current) == null ? void 0 : _a.call(onChangeRef, value);
      prevValueRef.current = value;
    }
  }, [value, prevValueRef]);
  return [value, setValue, onChangeRef];
}
function isFunction(value) {
  return typeof value === "function";
}
function usePrevious(value) {
  const ref = reactExports.useRef({ value, previous: value });
  return reactExports.useMemo(() => {
    if (ref.current.value !== value) {
      ref.current.previous = ref.current.value;
      ref.current.value = value;
    }
    return ref.current.previous;
  }, [value]);
}
var VISUALLY_HIDDEN_STYLES = Object.freeze({
  // See: https://github.com/twbs/bootstrap/blob/main/scss/mixins/_visually-hidden.scss
  position: "absolute",
  border: 0,
  width: 1,
  height: 1,
  padding: 0,
  margin: -1,
  overflow: "hidden",
  clip: "rect(0, 0, 0, 0)",
  whiteSpace: "nowrap",
  wordWrap: "normal"
});
var NAME = "VisuallyHidden";
var VisuallyHidden = reactExports.forwardRef(
  (props, forwardedRef) => {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.span,
      {
        ...props,
        ref: forwardedRef,
        style: { ...VISUALLY_HIDDEN_STYLES, ...props.style }
      }
    );
  }
);
VisuallyHidden.displayName = NAME;
var getDefaultParent = function(originalTarget) {
  if (typeof document === "undefined") {
    return null;
  }
  var sampleTarget = Array.isArray(originalTarget) ? originalTarget[0] : originalTarget;
  return sampleTarget.ownerDocument.body;
};
var counterMap = /* @__PURE__ */ new WeakMap();
var uncontrolledNodes = /* @__PURE__ */ new WeakMap();
var markerMap = {};
var lockCount = 0;
var unwrapHost = function(node2) {
  return node2 && (node2.host || unwrapHost(node2.parentNode));
};
var correctTargets = function(parent, targets) {
  return targets.map(function(target) {
    if (parent.contains(target)) {
      return target;
    }
    var correctedTarget = unwrapHost(target);
    if (correctedTarget && parent.contains(correctedTarget)) {
      return correctedTarget;
    }
    console.error("aria-hidden", target, "in not contained inside", parent, ". Doing nothing");
    return null;
  }).filter(function(x) {
    return Boolean(x);
  });
};
var applyAttributeToOthers = function(originalTarget, parentNode, markerName, controlAttribute) {
  var targets = correctTargets(parentNode, Array.isArray(originalTarget) ? originalTarget : [originalTarget]);
  if (!markerMap[markerName]) {
    markerMap[markerName] = /* @__PURE__ */ new WeakMap();
  }
  var markerCounter = markerMap[markerName];
  var hiddenNodes = [];
  var elementsToKeep = /* @__PURE__ */ new Set();
  var elementsToStop = new Set(targets);
  var keep = function(el) {
    if (!el || elementsToKeep.has(el)) {
      return;
    }
    elementsToKeep.add(el);
    keep(el.parentNode);
  };
  targets.forEach(keep);
  var deep = function(parent) {
    if (!parent || elementsToStop.has(parent)) {
      return;
    }
    Array.prototype.forEach.call(parent.children, function(node2) {
      if (elementsToKeep.has(node2)) {
        deep(node2);
      } else {
        try {
          var attr = node2.getAttribute(controlAttribute);
          var alreadyHidden = attr !== null && attr !== "false";
          var counterValue = (counterMap.get(node2) || 0) + 1;
          var markerValue = (markerCounter.get(node2) || 0) + 1;
          counterMap.set(node2, counterValue);
          markerCounter.set(node2, markerValue);
          hiddenNodes.push(node2);
          if (counterValue === 1 && alreadyHidden) {
            uncontrolledNodes.set(node2, true);
          }
          if (markerValue === 1) {
            node2.setAttribute(markerName, "true");
          }
          if (!alreadyHidden) {
            node2.setAttribute(controlAttribute, "true");
          }
        } catch (e) {
          console.error("aria-hidden: cannot operate on ", node2, e);
        }
      }
    });
  };
  deep(parentNode);
  elementsToKeep.clear();
  lockCount++;
  return function() {
    hiddenNodes.forEach(function(node2) {
      var counterValue = counterMap.get(node2) - 1;
      var markerValue = markerCounter.get(node2) - 1;
      counterMap.set(node2, counterValue);
      markerCounter.set(node2, markerValue);
      if (!counterValue) {
        if (!uncontrolledNodes.has(node2)) {
          node2.removeAttribute(controlAttribute);
        }
        uncontrolledNodes.delete(node2);
      }
      if (!markerValue) {
        node2.removeAttribute(markerName);
      }
    });
    lockCount--;
    if (!lockCount) {
      counterMap = /* @__PURE__ */ new WeakMap();
      counterMap = /* @__PURE__ */ new WeakMap();
      uncontrolledNodes = /* @__PURE__ */ new WeakMap();
      markerMap = {};
    }
  };
};
var hideOthers = function(originalTarget, parentNode, markerName) {
  if (markerName === void 0) {
    markerName = "data-aria-hidden";
  }
  var targets = Array.from(Array.isArray(originalTarget) ? originalTarget : [originalTarget]);
  var activeParentNode = getDefaultParent(originalTarget);
  if (!activeParentNode) {
    return function() {
      return null;
    };
  }
  targets.push.apply(targets, Array.from(activeParentNode.querySelectorAll("[aria-live], script")));
  return applyAttributeToOthers(targets, activeParentNode, markerName, "aria-hidden");
};
var __assign = function() {
  __assign = Object.assign || function __assign2(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
    }
    return t;
  };
  return __assign.apply(this, arguments);
};
function __rest(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
}
function __spreadArray(to, from, pack) {
  if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
    if (ar || !(i in from)) {
      if (!ar) ar = Array.prototype.slice.call(from, 0, i);
      ar[i] = from[i];
    }
  }
  return to.concat(ar || Array.prototype.slice.call(from));
}
typeof SuppressedError === "function" ? SuppressedError : function(error, suppressed, message) {
  var e = new Error(message);
  return e.name = "SuppressedError", e.error = error, e.suppressed = suppressed, e;
};
var zeroRightClassName = "right-scroll-bar-position";
var fullWidthClassName = "width-before-scroll-bar";
var noScrollbarsClassName = "with-scroll-bars-hidden";
var removedBarSizeVariable = "--removed-body-scroll-bar-size";
function assignRef(ref, value) {
  if (typeof ref === "function") {
    ref(value);
  } else if (ref) {
    ref.current = value;
  }
  return ref;
}
function useCallbackRef(initialValue, callback) {
  var ref = reactExports.useState(function() {
    return {
      // value
      value: initialValue,
      // last callback
      callback,
      // "memoized" public interface
      facade: {
        get current() {
          return ref.value;
        },
        set current(value) {
          var last = ref.value;
          if (last !== value) {
            ref.value = value;
            ref.callback(value, last);
          }
        }
      }
    };
  })[0];
  ref.callback = callback;
  return ref.facade;
}
var useIsomorphicLayoutEffect = typeof window !== "undefined" ? reactExports.useLayoutEffect : reactExports.useEffect;
var currentValues = /* @__PURE__ */ new WeakMap();
function useMergeRefs(refs, defaultValue) {
  var callbackRef = useCallbackRef(null, function(newValue) {
    return refs.forEach(function(ref) {
      return assignRef(ref, newValue);
    });
  });
  useIsomorphicLayoutEffect(function() {
    var oldValue = currentValues.get(callbackRef);
    if (oldValue) {
      var prevRefs_1 = new Set(oldValue);
      var nextRefs_1 = new Set(refs);
      var current_1 = callbackRef.current;
      prevRefs_1.forEach(function(ref) {
        if (!nextRefs_1.has(ref)) {
          assignRef(ref, null);
        }
      });
      nextRefs_1.forEach(function(ref) {
        if (!prevRefs_1.has(ref)) {
          assignRef(ref, current_1);
        }
      });
    }
    currentValues.set(callbackRef, refs);
  }, [refs]);
  return callbackRef;
}
function ItoI(a) {
  return a;
}
function innerCreateMedium(defaults, middleware) {
  if (middleware === void 0) {
    middleware = ItoI;
  }
  var buffer = [];
  var assigned = false;
  var medium = {
    read: function() {
      if (assigned) {
        throw new Error("Sidecar: could not `read` from an `assigned` medium. `read` could be used only with `useMedium`.");
      }
      if (buffer.length) {
        return buffer[buffer.length - 1];
      }
      return defaults;
    },
    useMedium: function(data) {
      var item = middleware(data, assigned);
      buffer.push(item);
      return function() {
        buffer = buffer.filter(function(x) {
          return x !== item;
        });
      };
    },
    assignSyncMedium: function(cb) {
      assigned = true;
      while (buffer.length) {
        var cbs = buffer;
        buffer = [];
        cbs.forEach(cb);
      }
      buffer = {
        push: function(x) {
          return cb(x);
        },
        filter: function() {
          return buffer;
        }
      };
    },
    assignMedium: function(cb) {
      assigned = true;
      var pendingQueue = [];
      if (buffer.length) {
        var cbs = buffer;
        buffer = [];
        cbs.forEach(cb);
        pendingQueue = buffer;
      }
      var executeQueue = function() {
        var cbs2 = pendingQueue;
        pendingQueue = [];
        cbs2.forEach(cb);
      };
      var cycle = function() {
        return Promise.resolve().then(executeQueue);
      };
      cycle();
      buffer = {
        push: function(x) {
          pendingQueue.push(x);
          cycle();
        },
        filter: function(filter) {
          pendingQueue = pendingQueue.filter(filter);
          return buffer;
        }
      };
    }
  };
  return medium;
}
function createSidecarMedium(options) {
  if (options === void 0) {
    options = {};
  }
  var medium = innerCreateMedium(null);
  medium.options = __assign({ async: true, ssr: false }, options);
  return medium;
}
var SideCar$1 = function(_a) {
  var sideCar = _a.sideCar, rest = __rest(_a, ["sideCar"]);
  if (!sideCar) {
    throw new Error("Sidecar: please provide `sideCar` property to import the right car");
  }
  var Target = sideCar.read();
  if (!Target) {
    throw new Error("Sidecar medium not found");
  }
  return reactExports.createElement(Target, __assign({}, rest));
};
SideCar$1.isSideCarExport = true;
function exportSidecar(medium, exported) {
  medium.useMedium(exported);
  return SideCar$1;
}
var effectCar = createSidecarMedium();
var nothing = function() {
  return;
};
var RemoveScroll = reactExports.forwardRef(function(props, parentRef) {
  var ref = reactExports.useRef(null);
  var _a = reactExports.useState({
    onScrollCapture: nothing,
    onWheelCapture: nothing,
    onTouchMoveCapture: nothing
  }), callbacks = _a[0], setCallbacks = _a[1];
  var forwardProps = props.forwardProps, children = props.children, className = props.className, removeScrollBar = props.removeScrollBar, enabled = props.enabled, shards = props.shards, sideCar = props.sideCar, noRelative = props.noRelative, noIsolation = props.noIsolation, inert = props.inert, allowPinchZoom = props.allowPinchZoom, _b = props.as, Container = _b === void 0 ? "div" : _b, gapMode = props.gapMode, rest = __rest(props, ["forwardProps", "children", "className", "removeScrollBar", "enabled", "shards", "sideCar", "noRelative", "noIsolation", "inert", "allowPinchZoom", "as", "gapMode"]);
  var SideCar2 = sideCar;
  var containerRef = useMergeRefs([ref, parentRef]);
  var containerProps = __assign(__assign({}, rest), callbacks);
  return reactExports.createElement(
    reactExports.Fragment,
    null,
    enabled && reactExports.createElement(SideCar2, { sideCar: effectCar, removeScrollBar, shards, noRelative, noIsolation, inert, setCallbacks, allowPinchZoom: !!allowPinchZoom, lockRef: ref, gapMode }),
    forwardProps ? reactExports.cloneElement(reactExports.Children.only(children), __assign(__assign({}, containerProps), { ref: containerRef })) : reactExports.createElement(Container, __assign({}, containerProps, { className, ref: containerRef }), children)
  );
});
RemoveScroll.defaultProps = {
  enabled: true,
  removeScrollBar: true,
  inert: false
};
RemoveScroll.classNames = {
  fullWidth: fullWidthClassName,
  zeroRight: zeroRightClassName
};
var getNonce = function() {
  if (typeof __webpack_nonce__ !== "undefined") {
    return __webpack_nonce__;
  }
  return void 0;
};
function makeStyleTag() {
  if (!document)
    return null;
  var tag = document.createElement("style");
  tag.type = "text/css";
  var nonce = getNonce();
  if (nonce) {
    tag.setAttribute("nonce", nonce);
  }
  return tag;
}
function injectStyles(tag, css) {
  if (tag.styleSheet) {
    tag.styleSheet.cssText = css;
  } else {
    tag.appendChild(document.createTextNode(css));
  }
}
function insertStyleTag(tag) {
  var head = document.head || document.getElementsByTagName("head")[0];
  head.appendChild(tag);
}
var stylesheetSingleton = function() {
  var counter = 0;
  var stylesheet = null;
  return {
    add: function(style) {
      if (counter == 0) {
        if (stylesheet = makeStyleTag()) {
          injectStyles(stylesheet, style);
          insertStyleTag(stylesheet);
        }
      }
      counter++;
    },
    remove: function() {
      counter--;
      if (!counter && stylesheet) {
        stylesheet.parentNode && stylesheet.parentNode.removeChild(stylesheet);
        stylesheet = null;
      }
    }
  };
};
var styleHookSingleton = function() {
  var sheet = stylesheetSingleton();
  return function(styles, isDynamic) {
    reactExports.useEffect(function() {
      sheet.add(styles);
      return function() {
        sheet.remove();
      };
    }, [styles && isDynamic]);
  };
};
var styleSingleton = function() {
  var useStyle = styleHookSingleton();
  var Sheet = function(_a) {
    var styles = _a.styles, dynamic = _a.dynamic;
    useStyle(styles, dynamic);
    return null;
  };
  return Sheet;
};
var zeroGap = {
  left: 0,
  top: 0,
  right: 0,
  gap: 0
};
var parse = function(x) {
  return parseInt(x || "", 10) || 0;
};
var getOffset = function(gapMode) {
  var cs = window.getComputedStyle(document.body);
  var left = cs[gapMode === "padding" ? "paddingLeft" : "marginLeft"];
  var top = cs[gapMode === "padding" ? "paddingTop" : "marginTop"];
  var right = cs[gapMode === "padding" ? "paddingRight" : "marginRight"];
  return [parse(left), parse(top), parse(right)];
};
var getGapWidth = function(gapMode) {
  if (gapMode === void 0) {
    gapMode = "margin";
  }
  if (typeof window === "undefined") {
    return zeroGap;
  }
  var offsets = getOffset(gapMode);
  var documentWidth = document.documentElement.clientWidth;
  var windowWidth = window.innerWidth;
  return {
    left: offsets[0],
    top: offsets[1],
    right: offsets[2],
    gap: Math.max(0, windowWidth - documentWidth + offsets[2] - offsets[0])
  };
};
var Style = styleSingleton();
var lockAttribute = "data-scroll-locked";
var getStyles = function(_a, allowRelative, gapMode, important) {
  var left = _a.left, top = _a.top, right = _a.right, gap = _a.gap;
  if (gapMode === void 0) {
    gapMode = "margin";
  }
  return "\n  .".concat(noScrollbarsClassName, " {\n   overflow: hidden ").concat(important, ";\n   padding-right: ").concat(gap, "px ").concat(important, ";\n  }\n  body[").concat(lockAttribute, "] {\n    overflow: hidden ").concat(important, ";\n    overscroll-behavior: contain;\n    ").concat([
    allowRelative && "position: relative ".concat(important, ";"),
    gapMode === "margin" && "\n    padding-left: ".concat(left, "px;\n    padding-top: ").concat(top, "px;\n    padding-right: ").concat(right, "px;\n    margin-left:0;\n    margin-top:0;\n    margin-right: ").concat(gap, "px ").concat(important, ";\n    "),
    gapMode === "padding" && "padding-right: ".concat(gap, "px ").concat(important, ";")
  ].filter(Boolean).join(""), "\n  }\n  \n  .").concat(zeroRightClassName, " {\n    right: ").concat(gap, "px ").concat(important, ";\n  }\n  \n  .").concat(fullWidthClassName, " {\n    margin-right: ").concat(gap, "px ").concat(important, ";\n  }\n  \n  .").concat(zeroRightClassName, " .").concat(zeroRightClassName, " {\n    right: 0 ").concat(important, ";\n  }\n  \n  .").concat(fullWidthClassName, " .").concat(fullWidthClassName, " {\n    margin-right: 0 ").concat(important, ";\n  }\n  \n  body[").concat(lockAttribute, "] {\n    ").concat(removedBarSizeVariable, ": ").concat(gap, "px;\n  }\n");
};
var getCurrentUseCounter = function() {
  var counter = parseInt(document.body.getAttribute(lockAttribute) || "0", 10);
  return isFinite(counter) ? counter : 0;
};
var useLockAttribute = function() {
  reactExports.useEffect(function() {
    document.body.setAttribute(lockAttribute, (getCurrentUseCounter() + 1).toString());
    return function() {
      var newCounter = getCurrentUseCounter() - 1;
      if (newCounter <= 0) {
        document.body.removeAttribute(lockAttribute);
      } else {
        document.body.setAttribute(lockAttribute, newCounter.toString());
      }
    };
  }, []);
};
var RemoveScrollBar = function(_a) {
  var noRelative = _a.noRelative, noImportant = _a.noImportant, _b = _a.gapMode, gapMode = _b === void 0 ? "margin" : _b;
  useLockAttribute();
  var gap = reactExports.useMemo(function() {
    return getGapWidth(gapMode);
  }, [gapMode]);
  return reactExports.createElement(Style, { styles: getStyles(gap, !noRelative, gapMode, !noImportant ? "!important" : "") });
};
var passiveSupported = false;
if (typeof window !== "undefined") {
  try {
    var options = Object.defineProperty({}, "passive", {
      get: function() {
        passiveSupported = true;
        return true;
      }
    });
    window.addEventListener("test", options, options);
    window.removeEventListener("test", options, options);
  } catch (err) {
    passiveSupported = false;
  }
}
var nonPassive = passiveSupported ? { passive: false } : false;
var alwaysContainsScroll = function(node2) {
  return node2.tagName === "TEXTAREA";
};
var elementCanBeScrolled = function(node2, overflow) {
  if (!(node2 instanceof Element)) {
    return false;
  }
  var styles = window.getComputedStyle(node2);
  return (
    // not-not-scrollable
    styles[overflow] !== "hidden" && // contains scroll inside self
    !(styles.overflowY === styles.overflowX && !alwaysContainsScroll(node2) && styles[overflow] === "visible")
  );
};
var elementCouldBeVScrolled = function(node2) {
  return elementCanBeScrolled(node2, "overflowY");
};
var elementCouldBeHScrolled = function(node2) {
  return elementCanBeScrolled(node2, "overflowX");
};
var locationCouldBeScrolled = function(axis, node2) {
  var ownerDocument = node2.ownerDocument;
  var current = node2;
  do {
    if (typeof ShadowRoot !== "undefined" && current instanceof ShadowRoot) {
      current = current.host;
    }
    var isScrollable = elementCouldBeScrolled(axis, current);
    if (isScrollable) {
      var _a = getScrollVariables(axis, current), scrollHeight = _a[1], clientHeight = _a[2];
      if (scrollHeight > clientHeight) {
        return true;
      }
    }
    current = current.parentNode;
  } while (current && current !== ownerDocument.body);
  return false;
};
var getVScrollVariables = function(_a) {
  var scrollTop = _a.scrollTop, scrollHeight = _a.scrollHeight, clientHeight = _a.clientHeight;
  return [
    scrollTop,
    scrollHeight,
    clientHeight
  ];
};
var getHScrollVariables = function(_a) {
  var scrollLeft = _a.scrollLeft, scrollWidth = _a.scrollWidth, clientWidth = _a.clientWidth;
  return [
    scrollLeft,
    scrollWidth,
    clientWidth
  ];
};
var elementCouldBeScrolled = function(axis, node2) {
  return axis === "v" ? elementCouldBeVScrolled(node2) : elementCouldBeHScrolled(node2);
};
var getScrollVariables = function(axis, node2) {
  return axis === "v" ? getVScrollVariables(node2) : getHScrollVariables(node2);
};
var getDirectionFactor = function(axis, direction) {
  return axis === "h" && direction === "rtl" ? -1 : 1;
};
var handleScroll = function(axis, endTarget, event, sourceDelta, noOverscroll) {
  var directionFactor = getDirectionFactor(axis, window.getComputedStyle(endTarget).direction);
  var delta = directionFactor * sourceDelta;
  var target = event.target;
  var targetInLock = endTarget.contains(target);
  var shouldCancelScroll = false;
  var isDeltaPositive = delta > 0;
  var availableScroll = 0;
  var availableScrollTop = 0;
  do {
    if (!target) {
      break;
    }
    var _a = getScrollVariables(axis, target), position2 = _a[0], scroll_1 = _a[1], capacity = _a[2];
    var elementScroll = scroll_1 - capacity - directionFactor * position2;
    if (position2 || elementScroll) {
      if (elementCouldBeScrolled(axis, target)) {
        availableScroll += elementScroll;
        availableScrollTop += position2;
      }
    }
    var parent_1 = target.parentNode;
    target = parent_1 && parent_1.nodeType === Node.DOCUMENT_FRAGMENT_NODE ? parent_1.host : parent_1;
  } while (
    // portaled content
    !targetInLock && target !== document.body || // self content
    targetInLock && (endTarget.contains(target) || endTarget === target)
  );
  if (isDeltaPositive && (Math.abs(availableScroll) < 1 || false)) {
    shouldCancelScroll = true;
  } else if (!isDeltaPositive && (Math.abs(availableScrollTop) < 1 || false)) {
    shouldCancelScroll = true;
  }
  return shouldCancelScroll;
};
var getTouchXY = function(event) {
  return "changedTouches" in event ? [event.changedTouches[0].clientX, event.changedTouches[0].clientY] : [0, 0];
};
var getDeltaXY = function(event) {
  return [event.deltaX, event.deltaY];
};
var extractRef = function(ref) {
  return ref && "current" in ref ? ref.current : ref;
};
var deltaCompare = function(x, y) {
  return x[0] === y[0] && x[1] === y[1];
};
var generateStyle = function(id) {
  return "\n  .block-interactivity-".concat(id, " {pointer-events: none;}\n  .allow-interactivity-").concat(id, " {pointer-events: all;}\n");
};
var idCounter = 0;
var lockStack = [];
function RemoveScrollSideCar(props) {
  var shouldPreventQueue = reactExports.useRef([]);
  var touchStartRef = reactExports.useRef([0, 0]);
  var activeAxis = reactExports.useRef();
  var id = reactExports.useState(idCounter++)[0];
  var Style2 = reactExports.useState(styleSingleton)[0];
  var lastProps = reactExports.useRef(props);
  reactExports.useEffect(function() {
    lastProps.current = props;
  }, [props]);
  reactExports.useEffect(function() {
    if (props.inert) {
      document.body.classList.add("block-interactivity-".concat(id));
      var allow_1 = __spreadArray([props.lockRef.current], (props.shards || []).map(extractRef), true).filter(Boolean);
      allow_1.forEach(function(el) {
        return el.classList.add("allow-interactivity-".concat(id));
      });
      return function() {
        document.body.classList.remove("block-interactivity-".concat(id));
        allow_1.forEach(function(el) {
          return el.classList.remove("allow-interactivity-".concat(id));
        });
      };
    }
    return;
  }, [props.inert, props.lockRef.current, props.shards]);
  var shouldCancelEvent = reactExports.useCallback(function(event, parent) {
    if ("touches" in event && event.touches.length === 2 || event.type === "wheel" && event.ctrlKey) {
      return !lastProps.current.allowPinchZoom;
    }
    var touch = getTouchXY(event);
    var touchStart = touchStartRef.current;
    var deltaX = "deltaX" in event ? event.deltaX : touchStart[0] - touch[0];
    var deltaY = "deltaY" in event ? event.deltaY : touchStart[1] - touch[1];
    var currentAxis;
    var target = event.target;
    var moveDirection = Math.abs(deltaX) > Math.abs(deltaY) ? "h" : "v";
    if ("touches" in event && moveDirection === "h" && target.type === "range") {
      return false;
    }
    var selection = window.getSelection();
    var anchorNode = selection && selection.anchorNode;
    var isTouchingSelection = anchorNode ? anchorNode === target || anchorNode.contains(target) : false;
    if (isTouchingSelection) {
      return false;
    }
    var canBeScrolledInMainDirection = locationCouldBeScrolled(moveDirection, target);
    if (!canBeScrolledInMainDirection) {
      return true;
    }
    if (canBeScrolledInMainDirection) {
      currentAxis = moveDirection;
    } else {
      currentAxis = moveDirection === "v" ? "h" : "v";
      canBeScrolledInMainDirection = locationCouldBeScrolled(moveDirection, target);
    }
    if (!canBeScrolledInMainDirection) {
      return false;
    }
    if (!activeAxis.current && "changedTouches" in event && (deltaX || deltaY)) {
      activeAxis.current = currentAxis;
    }
    if (!currentAxis) {
      return true;
    }
    var cancelingAxis = activeAxis.current || currentAxis;
    return handleScroll(cancelingAxis, parent, event, cancelingAxis === "h" ? deltaX : deltaY);
  }, []);
  var shouldPrevent = reactExports.useCallback(function(_event) {
    var event = _event;
    if (!lockStack.length || lockStack[lockStack.length - 1] !== Style2) {
      return;
    }
    var delta = "deltaY" in event ? getDeltaXY(event) : getTouchXY(event);
    var sourceEvent = shouldPreventQueue.current.filter(function(e) {
      return e.name === event.type && (e.target === event.target || event.target === e.shadowParent) && deltaCompare(e.delta, delta);
    })[0];
    if (sourceEvent && sourceEvent.should) {
      if (event.cancelable) {
        event.preventDefault();
      }
      return;
    }
    if (!sourceEvent) {
      var shardNodes = (lastProps.current.shards || []).map(extractRef).filter(Boolean).filter(function(node2) {
        return node2.contains(event.target);
      });
      var shouldStop = shardNodes.length > 0 ? shouldCancelEvent(event, shardNodes[0]) : !lastProps.current.noIsolation;
      if (shouldStop) {
        if (event.cancelable) {
          event.preventDefault();
        }
      }
    }
  }, []);
  var shouldCancel = reactExports.useCallback(function(name2, delta, target, should) {
    var event = { name: name2, delta, target, should, shadowParent: getOutermostShadowParent(target) };
    shouldPreventQueue.current.push(event);
    setTimeout(function() {
      shouldPreventQueue.current = shouldPreventQueue.current.filter(function(e) {
        return e !== event;
      });
    }, 1);
  }, []);
  var scrollTouchStart = reactExports.useCallback(function(event) {
    touchStartRef.current = getTouchXY(event);
    activeAxis.current = void 0;
  }, []);
  var scrollWheel = reactExports.useCallback(function(event) {
    shouldCancel(event.type, getDeltaXY(event), event.target, shouldCancelEvent(event, props.lockRef.current));
  }, []);
  var scrollTouchMove = reactExports.useCallback(function(event) {
    shouldCancel(event.type, getTouchXY(event), event.target, shouldCancelEvent(event, props.lockRef.current));
  }, []);
  reactExports.useEffect(function() {
    lockStack.push(Style2);
    props.setCallbacks({
      onScrollCapture: scrollWheel,
      onWheelCapture: scrollWheel,
      onTouchMoveCapture: scrollTouchMove
    });
    document.addEventListener("wheel", shouldPrevent, nonPassive);
    document.addEventListener("touchmove", shouldPrevent, nonPassive);
    document.addEventListener("touchstart", scrollTouchStart, nonPassive);
    return function() {
      lockStack = lockStack.filter(function(inst) {
        return inst !== Style2;
      });
      document.removeEventListener("wheel", shouldPrevent, nonPassive);
      document.removeEventListener("touchmove", shouldPrevent, nonPassive);
      document.removeEventListener("touchstart", scrollTouchStart, nonPassive);
    };
  }, []);
  var removeScrollBar = props.removeScrollBar, inert = props.inert;
  return reactExports.createElement(
    reactExports.Fragment,
    null,
    inert ? reactExports.createElement(Style2, { styles: generateStyle(id) }) : null,
    removeScrollBar ? reactExports.createElement(RemoveScrollBar, { noRelative: props.noRelative, gapMode: props.gapMode }) : null
  );
}
function getOutermostShadowParent(node2) {
  var shadowParent = null;
  while (node2 !== null) {
    if (node2 instanceof ShadowRoot) {
      shadowParent = node2.host;
      node2 = node2.host;
    }
    node2 = node2.parentNode;
  }
  return shadowParent;
}
const SideCar = exportSidecar(effectCar, RemoveScrollSideCar);
var ReactRemoveScroll = reactExports.forwardRef(function(props, ref) {
  return reactExports.createElement(RemoveScroll, __assign({}, props, { ref, sideCar: SideCar }));
});
ReactRemoveScroll.classNames = RemoveScroll.classNames;
var OPEN_KEYS = [" ", "Enter", "ArrowUp", "ArrowDown"];
var SELECTION_KEYS = [" ", "Enter"];
var SELECT_NAME = "Select";
var [Collection, useCollection, createCollectionScope] = createCollection(SELECT_NAME);
var [createSelectContext] = createContextScope(SELECT_NAME, [
  createCollectionScope,
  createPopperScope
]);
var usePopperScope = createPopperScope();
var [SelectProvider, useSelectContext] = createSelectContext(SELECT_NAME);
var [SelectNativeOptionsProvider, useSelectNativeOptionsContext] = createSelectContext(SELECT_NAME);
var Select = (props) => {
  const {
    __scopeSelect,
    children,
    open: openProp,
    defaultOpen,
    onOpenChange,
    value: valueProp,
    defaultValue,
    onValueChange,
    dir,
    name: name2,
    autoComplete,
    disabled,
    required,
    form
  } = props;
  const popperScope = usePopperScope(__scopeSelect);
  const [trigger, setTrigger] = reactExports.useState(null);
  const [valueNode, setValueNode] = reactExports.useState(null);
  const [valueNodeHasChildren, setValueNodeHasChildren] = reactExports.useState(false);
  const direction = useDirection(dir);
  const [open, setOpen] = useControllableState({
    prop: openProp,
    defaultProp: defaultOpen ?? false,
    onChange: onOpenChange,
    caller: SELECT_NAME
  });
  const [value, setValue] = useControllableState({
    prop: valueProp,
    defaultProp: defaultValue,
    onChange: onValueChange,
    caller: SELECT_NAME
  });
  const triggerPointerDownPosRef = reactExports.useRef(null);
  const isFormControl = trigger ? form || !!trigger.closest("form") : true;
  const [nativeOptionsSet, setNativeOptionsSet] = reactExports.useState(/* @__PURE__ */ new Set());
  const nativeSelectKey = Array.from(nativeOptionsSet).map((option) => option.props.value).join(";");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Root2$1, { ...popperScope, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
    SelectProvider,
    {
      required,
      scope: __scopeSelect,
      trigger,
      onTriggerChange: setTrigger,
      valueNode,
      onValueNodeChange: setValueNode,
      valueNodeHasChildren,
      onValueNodeHasChildrenChange: setValueNodeHasChildren,
      contentId: useId(),
      value,
      onValueChange: setValue,
      open,
      onOpenChange: setOpen,
      dir: direction,
      triggerPointerDownPosRef,
      disabled,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Collection.Provider, { scope: __scopeSelect, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          SelectNativeOptionsProvider,
          {
            scope: props.__scopeSelect,
            onNativeOptionAdd: reactExports.useCallback((option) => {
              setNativeOptionsSet((prev) => new Set(prev).add(option));
            }, []),
            onNativeOptionRemove: reactExports.useCallback((option) => {
              setNativeOptionsSet((prev) => {
                const optionsSet = new Set(prev);
                optionsSet.delete(option);
                return optionsSet;
              });
            }, []),
            children
          }
        ) }),
        isFormControl ? /* @__PURE__ */ jsxRuntimeExports.jsxs(
          SelectBubbleInput,
          {
            "aria-hidden": true,
            required,
            tabIndex: -1,
            name: name2,
            autoComplete,
            value,
            onChange: (event) => setValue(event.target.value),
            disabled,
            form,
            children: [
              value === void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "" }) : null,
              Array.from(nativeOptionsSet)
            ]
          },
          nativeSelectKey
        ) : null
      ]
    }
  ) });
};
Select.displayName = SELECT_NAME;
var TRIGGER_NAME = "SelectTrigger";
var SelectTrigger = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSelect, disabled = false, ...triggerProps } = props;
    const popperScope = usePopperScope(__scopeSelect);
    const context = useSelectContext(TRIGGER_NAME, __scopeSelect);
    const isDisabled = context.disabled || disabled;
    const composedRefs = useComposedRefs(forwardedRef, context.onTriggerChange);
    const getItems = useCollection(__scopeSelect);
    const pointerTypeRef = reactExports.useRef("touch");
    const [searchRef, handleTypeaheadSearch, resetTypeahead] = useTypeaheadSearch((search2) => {
      const enabledItems = getItems().filter((item) => !item.disabled);
      const currentItem = enabledItems.find((item) => item.value === context.value);
      const nextItem = findNextItem(enabledItems, search2, currentItem);
      if (nextItem !== void 0) {
        context.onValueChange(nextItem.value);
      }
    });
    const handleOpen = (pointerEvent) => {
      if (!isDisabled) {
        context.onOpenChange(true);
        resetTypeahead();
      }
      if (pointerEvent) {
        context.triggerPointerDownPosRef.current = {
          x: Math.round(pointerEvent.pageX),
          y: Math.round(pointerEvent.pageY)
        };
      }
    };
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Anchor, { asChild: true, ...popperScope, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.button,
      {
        type: "button",
        role: "combobox",
        "aria-controls": context.contentId,
        "aria-expanded": context.open,
        "aria-required": context.required,
        "aria-autocomplete": "none",
        dir: context.dir,
        "data-state": context.open ? "open" : "closed",
        disabled: isDisabled,
        "data-disabled": isDisabled ? "" : void 0,
        "data-placeholder": shouldShowPlaceholder(context.value) ? "" : void 0,
        ...triggerProps,
        ref: composedRefs,
        onClick: composeEventHandlers(triggerProps.onClick, (event) => {
          event.currentTarget.focus();
          if (pointerTypeRef.current !== "mouse") {
            handleOpen(event);
          }
        }),
        onPointerDown: composeEventHandlers(triggerProps.onPointerDown, (event) => {
          pointerTypeRef.current = event.pointerType;
          const target = event.target;
          if (target.hasPointerCapture(event.pointerId)) {
            target.releasePointerCapture(event.pointerId);
          }
          if (event.button === 0 && event.ctrlKey === false && event.pointerType === "mouse") {
            handleOpen(event);
            event.preventDefault();
          }
        }),
        onKeyDown: composeEventHandlers(triggerProps.onKeyDown, (event) => {
          const isTypingAhead = searchRef.current !== "";
          const isModifierKey = event.ctrlKey || event.altKey || event.metaKey;
          if (!isModifierKey && event.key.length === 1) handleTypeaheadSearch(event.key);
          if (isTypingAhead && event.key === " ") return;
          if (OPEN_KEYS.includes(event.key)) {
            handleOpen();
            event.preventDefault();
          }
        })
      }
    ) });
  }
);
SelectTrigger.displayName = TRIGGER_NAME;
var VALUE_NAME = "SelectValue";
var SelectValue = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSelect, className, style, children, placeholder = "", ...valueProps } = props;
    const context = useSelectContext(VALUE_NAME, __scopeSelect);
    const { onValueNodeHasChildrenChange } = context;
    const hasChildren = children !== void 0;
    const composedRefs = useComposedRefs(forwardedRef, context.onValueNodeChange);
    useLayoutEffect2(() => {
      onValueNodeHasChildrenChange(hasChildren);
    }, [onValueNodeHasChildrenChange, hasChildren]);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.span,
      {
        ...valueProps,
        ref: composedRefs,
        style: { pointerEvents: "none" },
        children: shouldShowPlaceholder(context.value) ? /* @__PURE__ */ jsxRuntimeExports.jsx(jsxRuntimeExports.Fragment, { children: placeholder }) : children
      }
    );
  }
);
SelectValue.displayName = VALUE_NAME;
var ICON_NAME = "SelectIcon";
var SelectIcon = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSelect, children, ...iconProps } = props;
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Primitive.span, { "aria-hidden": true, ...iconProps, ref: forwardedRef, children: children || "▼" });
  }
);
SelectIcon.displayName = ICON_NAME;
var PORTAL_NAME = "SelectPortal";
var SelectPortal = (props) => {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Portal$1, { asChild: true, ...props });
};
SelectPortal.displayName = PORTAL_NAME;
var CONTENT_NAME = "SelectContent";
var SelectContent = reactExports.forwardRef(
  (props, forwardedRef) => {
    const context = useSelectContext(CONTENT_NAME, props.__scopeSelect);
    const [fragment, setFragment] = reactExports.useState();
    useLayoutEffect2(() => {
      setFragment(new DocumentFragment());
    }, []);
    if (!context.open) {
      const frag = fragment;
      return frag ? reactDomExports.createPortal(
        /* @__PURE__ */ jsxRuntimeExports.jsx(SelectContentProvider, { scope: props.__scopeSelect, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Collection.Slot, { scope: props.__scopeSelect, children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: props.children }) }) }),
        frag
      ) : null;
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(SelectContentImpl, { ...props, ref: forwardedRef });
  }
);
SelectContent.displayName = CONTENT_NAME;
var CONTENT_MARGIN = 10;
var [SelectContentProvider, useSelectContentContext] = createSelectContext(CONTENT_NAME);
var CONTENT_IMPL_NAME = "SelectContentImpl";
var Slot = /* @__PURE__ */ createSlot("SelectContent.RemoveScroll");
var SelectContentImpl = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeSelect,
      position: position2 = "item-aligned",
      onCloseAutoFocus,
      onEscapeKeyDown,
      onPointerDownOutside,
      //
      // PopperContent props
      side,
      sideOffset,
      align,
      alignOffset,
      arrowPadding,
      collisionBoundary,
      collisionPadding,
      sticky,
      hideWhenDetached,
      avoidCollisions,
      //
      ...contentProps
    } = props;
    const context = useSelectContext(CONTENT_NAME, __scopeSelect);
    const [content2, setContent] = reactExports.useState(null);
    const [viewport, setViewport] = reactExports.useState(null);
    const composedRefs = useComposedRefs(forwardedRef, (node2) => setContent(node2));
    const [selectedItem, setSelectedItem] = reactExports.useState(null);
    const [selectedItemText, setSelectedItemText] = reactExports.useState(
      null
    );
    const getItems = useCollection(__scopeSelect);
    const [isPositioned, setIsPositioned] = reactExports.useState(false);
    const firstValidItemFoundRef = reactExports.useRef(false);
    reactExports.useEffect(() => {
      if (content2) return hideOthers(content2);
    }, [content2]);
    useFocusGuards();
    const focusFirst2 = reactExports.useCallback(
      (candidates) => {
        const [firstItem, ...restItems] = getItems().map((item) => item.ref.current);
        const [lastItem] = restItems.slice(-1);
        const PREVIOUSLY_FOCUSED_ELEMENT = document.activeElement;
        for (const candidate of candidates) {
          if (candidate === PREVIOUSLY_FOCUSED_ELEMENT) return;
          candidate == null ? void 0 : candidate.scrollIntoView({ block: "nearest" });
          if (candidate === firstItem && viewport) viewport.scrollTop = 0;
          if (candidate === lastItem && viewport) viewport.scrollTop = viewport.scrollHeight;
          candidate == null ? void 0 : candidate.focus();
          if (document.activeElement !== PREVIOUSLY_FOCUSED_ELEMENT) return;
        }
      },
      [getItems, viewport]
    );
    const focusSelectedItem = reactExports.useCallback(
      () => focusFirst2([selectedItem, content2]),
      [focusFirst2, selectedItem, content2]
    );
    reactExports.useEffect(() => {
      if (isPositioned) {
        focusSelectedItem();
      }
    }, [isPositioned, focusSelectedItem]);
    const { onOpenChange, triggerPointerDownPosRef } = context;
    reactExports.useEffect(() => {
      if (content2) {
        let pointerMoveDelta = { x: 0, y: 0 };
        const handlePointerMove = (event) => {
          var _a, _b;
          pointerMoveDelta = {
            x: Math.abs(Math.round(event.pageX) - (((_a = triggerPointerDownPosRef.current) == null ? void 0 : _a.x) ?? 0)),
            y: Math.abs(Math.round(event.pageY) - (((_b = triggerPointerDownPosRef.current) == null ? void 0 : _b.y) ?? 0))
          };
        };
        const handlePointerUp = (event) => {
          if (pointerMoveDelta.x <= 10 && pointerMoveDelta.y <= 10) {
            event.preventDefault();
          } else {
            if (!content2.contains(event.target)) {
              onOpenChange(false);
            }
          }
          document.removeEventListener("pointermove", handlePointerMove);
          triggerPointerDownPosRef.current = null;
        };
        if (triggerPointerDownPosRef.current !== null) {
          document.addEventListener("pointermove", handlePointerMove);
          document.addEventListener("pointerup", handlePointerUp, { capture: true, once: true });
        }
        return () => {
          document.removeEventListener("pointermove", handlePointerMove);
          document.removeEventListener("pointerup", handlePointerUp, { capture: true });
        };
      }
    }, [content2, onOpenChange, triggerPointerDownPosRef]);
    reactExports.useEffect(() => {
      const close = () => onOpenChange(false);
      window.addEventListener("blur", close);
      window.addEventListener("resize", close);
      return () => {
        window.removeEventListener("blur", close);
        window.removeEventListener("resize", close);
      };
    }, [onOpenChange]);
    const [searchRef, handleTypeaheadSearch] = useTypeaheadSearch((search2) => {
      const enabledItems = getItems().filter((item) => !item.disabled);
      const currentItem = enabledItems.find((item) => item.ref.current === document.activeElement);
      const nextItem = findNextItem(enabledItems, search2, currentItem);
      if (nextItem) {
        setTimeout(() => nextItem.ref.current.focus());
      }
    });
    const itemRefCallback = reactExports.useCallback(
      (node2, value, disabled) => {
        const isFirstValidItem = !firstValidItemFoundRef.current && !disabled;
        const isSelectedItem = context.value !== void 0 && context.value === value;
        if (isSelectedItem || isFirstValidItem) {
          setSelectedItem(node2);
          if (isFirstValidItem) firstValidItemFoundRef.current = true;
        }
      },
      [context.value]
    );
    const handleItemLeave = reactExports.useCallback(() => content2 == null ? void 0 : content2.focus(), [content2]);
    const itemTextRefCallback = reactExports.useCallback(
      (node2, value, disabled) => {
        const isFirstValidItem = !firstValidItemFoundRef.current && !disabled;
        const isSelectedItem = context.value !== void 0 && context.value === value;
        if (isSelectedItem || isFirstValidItem) {
          setSelectedItemText(node2);
        }
      },
      [context.value]
    );
    const SelectPosition = position2 === "popper" ? SelectPopperPosition : SelectItemAlignedPosition;
    const popperContentProps = SelectPosition === SelectPopperPosition ? {
      side,
      sideOffset,
      align,
      alignOffset,
      arrowPadding,
      collisionBoundary,
      collisionPadding,
      sticky,
      hideWhenDetached,
      avoidCollisions
    } : {};
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SelectContentProvider,
      {
        scope: __scopeSelect,
        content: content2,
        viewport,
        onViewportChange: setViewport,
        itemRefCallback,
        selectedItem,
        onItemLeave: handleItemLeave,
        itemTextRefCallback,
        focusSelectedItem,
        selectedItemText,
        position: position2,
        isPositioned,
        searchRef,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(ReactRemoveScroll, { as: Slot, allowPinchZoom: true, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          FocusScope,
          {
            asChild: true,
            trapped: context.open,
            onMountAutoFocus: (event) => {
              event.preventDefault();
            },
            onUnmountAutoFocus: composeEventHandlers(onCloseAutoFocus, (event) => {
              var _a;
              (_a = context.trigger) == null ? void 0 : _a.focus({ preventScroll: true });
              event.preventDefault();
            }),
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              DismissableLayer,
              {
                asChild: true,
                disableOutsidePointerEvents: true,
                onEscapeKeyDown,
                onPointerDownOutside,
                onFocusOutside: (event) => event.preventDefault(),
                onDismiss: () => context.onOpenChange(false),
                children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                  SelectPosition,
                  {
                    role: "listbox",
                    id: context.contentId,
                    "data-state": context.open ? "open" : "closed",
                    dir: context.dir,
                    onContextMenu: (event) => event.preventDefault(),
                    ...contentProps,
                    ...popperContentProps,
                    onPlaced: () => setIsPositioned(true),
                    ref: composedRefs,
                    style: {
                      // flex layout so we can place the scroll buttons properly
                      display: "flex",
                      flexDirection: "column",
                      // reset the outline by default as the content MAY get focused
                      outline: "none",
                      ...contentProps.style
                    },
                    onKeyDown: composeEventHandlers(contentProps.onKeyDown, (event) => {
                      const isModifierKey = event.ctrlKey || event.altKey || event.metaKey;
                      if (event.key === "Tab") event.preventDefault();
                      if (!isModifierKey && event.key.length === 1) handleTypeaheadSearch(event.key);
                      if (["ArrowUp", "ArrowDown", "Home", "End"].includes(event.key)) {
                        const items = getItems().filter((item) => !item.disabled);
                        let candidateNodes = items.map((item) => item.ref.current);
                        if (["ArrowUp", "End"].includes(event.key)) {
                          candidateNodes = candidateNodes.slice().reverse();
                        }
                        if (["ArrowUp", "ArrowDown"].includes(event.key)) {
                          const currentElement = event.target;
                          const currentIndex = candidateNodes.indexOf(currentElement);
                          candidateNodes = candidateNodes.slice(currentIndex + 1);
                        }
                        setTimeout(() => focusFirst2(candidateNodes));
                        event.preventDefault();
                      }
                    })
                  }
                )
              }
            )
          }
        ) })
      }
    );
  }
);
SelectContentImpl.displayName = CONTENT_IMPL_NAME;
var ITEM_ALIGNED_POSITION_NAME = "SelectItemAlignedPosition";
var SelectItemAlignedPosition = reactExports.forwardRef((props, forwardedRef) => {
  const { __scopeSelect, onPlaced, ...popperProps } = props;
  const context = useSelectContext(CONTENT_NAME, __scopeSelect);
  const contentContext = useSelectContentContext(CONTENT_NAME, __scopeSelect);
  const [contentWrapper, setContentWrapper] = reactExports.useState(null);
  const [content2, setContent] = reactExports.useState(null);
  const composedRefs = useComposedRefs(forwardedRef, (node2) => setContent(node2));
  const getItems = useCollection(__scopeSelect);
  const shouldExpandOnScrollRef = reactExports.useRef(false);
  const shouldRepositionRef = reactExports.useRef(true);
  const { viewport, selectedItem, selectedItemText, focusSelectedItem } = contentContext;
  const position2 = reactExports.useCallback(() => {
    if (context.trigger && context.valueNode && contentWrapper && content2 && viewport && selectedItem && selectedItemText) {
      const triggerRect = context.trigger.getBoundingClientRect();
      const contentRect = content2.getBoundingClientRect();
      const valueNodeRect = context.valueNode.getBoundingClientRect();
      const itemTextRect = selectedItemText.getBoundingClientRect();
      if (context.dir !== "rtl") {
        const itemTextOffset = itemTextRect.left - contentRect.left;
        const left = valueNodeRect.left - itemTextOffset;
        const leftDelta = triggerRect.left - left;
        const minContentWidth = triggerRect.width + leftDelta;
        const contentWidth = Math.max(minContentWidth, contentRect.width);
        const rightEdge = window.innerWidth - CONTENT_MARGIN;
        const clampedLeft = clamp$1(left, [
          CONTENT_MARGIN,
          // Prevents the content from going off the starting edge of the
          // viewport. It may still go off the ending edge, but this can be
          // controlled by the user since they may want to manage overflow in a
          // specific way.
          // https://github.com/radix-ui/primitives/issues/2049
          Math.max(CONTENT_MARGIN, rightEdge - contentWidth)
        ]);
        contentWrapper.style.minWidth = minContentWidth + "px";
        contentWrapper.style.left = clampedLeft + "px";
      } else {
        const itemTextOffset = contentRect.right - itemTextRect.right;
        const right = window.innerWidth - valueNodeRect.right - itemTextOffset;
        const rightDelta = window.innerWidth - triggerRect.right - right;
        const minContentWidth = triggerRect.width + rightDelta;
        const contentWidth = Math.max(minContentWidth, contentRect.width);
        const leftEdge = window.innerWidth - CONTENT_MARGIN;
        const clampedRight = clamp$1(right, [
          CONTENT_MARGIN,
          Math.max(CONTENT_MARGIN, leftEdge - contentWidth)
        ]);
        contentWrapper.style.minWidth = minContentWidth + "px";
        contentWrapper.style.right = clampedRight + "px";
      }
      const items = getItems();
      const availableHeight = window.innerHeight - CONTENT_MARGIN * 2;
      const itemsHeight = viewport.scrollHeight;
      const contentStyles = window.getComputedStyle(content2);
      const contentBorderTopWidth = parseInt(contentStyles.borderTopWidth, 10);
      const contentPaddingTop = parseInt(contentStyles.paddingTop, 10);
      const contentBorderBottomWidth = parseInt(contentStyles.borderBottomWidth, 10);
      const contentPaddingBottom = parseInt(contentStyles.paddingBottom, 10);
      const fullContentHeight = contentBorderTopWidth + contentPaddingTop + itemsHeight + contentPaddingBottom + contentBorderBottomWidth;
      const minContentHeight = Math.min(selectedItem.offsetHeight * 5, fullContentHeight);
      const viewportStyles = window.getComputedStyle(viewport);
      const viewportPaddingTop = parseInt(viewportStyles.paddingTop, 10);
      const viewportPaddingBottom = parseInt(viewportStyles.paddingBottom, 10);
      const topEdgeToTriggerMiddle = triggerRect.top + triggerRect.height / 2 - CONTENT_MARGIN;
      const triggerMiddleToBottomEdge = availableHeight - topEdgeToTriggerMiddle;
      const selectedItemHalfHeight = selectedItem.offsetHeight / 2;
      const itemOffsetMiddle = selectedItem.offsetTop + selectedItemHalfHeight;
      const contentTopToItemMiddle = contentBorderTopWidth + contentPaddingTop + itemOffsetMiddle;
      const itemMiddleToContentBottom = fullContentHeight - contentTopToItemMiddle;
      const willAlignWithoutTopOverflow = contentTopToItemMiddle <= topEdgeToTriggerMiddle;
      if (willAlignWithoutTopOverflow) {
        const isLastItem = items.length > 0 && selectedItem === items[items.length - 1].ref.current;
        contentWrapper.style.bottom = "0px";
        const viewportOffsetBottom = content2.clientHeight - viewport.offsetTop - viewport.offsetHeight;
        const clampedTriggerMiddleToBottomEdge = Math.max(
          triggerMiddleToBottomEdge,
          selectedItemHalfHeight + // viewport might have padding bottom, include it to avoid a scrollable viewport
          (isLastItem ? viewportPaddingBottom : 0) + viewportOffsetBottom + contentBorderBottomWidth
        );
        const height = contentTopToItemMiddle + clampedTriggerMiddleToBottomEdge;
        contentWrapper.style.height = height + "px";
      } else {
        const isFirstItem = items.length > 0 && selectedItem === items[0].ref.current;
        contentWrapper.style.top = "0px";
        const clampedTopEdgeToTriggerMiddle = Math.max(
          topEdgeToTriggerMiddle,
          contentBorderTopWidth + viewport.offsetTop + // viewport might have padding top, include it to avoid a scrollable viewport
          (isFirstItem ? viewportPaddingTop : 0) + selectedItemHalfHeight
        );
        const height = clampedTopEdgeToTriggerMiddle + itemMiddleToContentBottom;
        contentWrapper.style.height = height + "px";
        viewport.scrollTop = contentTopToItemMiddle - topEdgeToTriggerMiddle + viewport.offsetTop;
      }
      contentWrapper.style.margin = `${CONTENT_MARGIN}px 0`;
      contentWrapper.style.minHeight = minContentHeight + "px";
      contentWrapper.style.maxHeight = availableHeight + "px";
      onPlaced == null ? void 0 : onPlaced();
      requestAnimationFrame(() => shouldExpandOnScrollRef.current = true);
    }
  }, [
    getItems,
    context.trigger,
    context.valueNode,
    contentWrapper,
    content2,
    viewport,
    selectedItem,
    selectedItemText,
    context.dir,
    onPlaced
  ]);
  useLayoutEffect2(() => position2(), [position2]);
  const [contentZIndex, setContentZIndex] = reactExports.useState();
  useLayoutEffect2(() => {
    if (content2) setContentZIndex(window.getComputedStyle(content2).zIndex);
  }, [content2]);
  const handleScrollButtonChange = reactExports.useCallback(
    (node2) => {
      if (node2 && shouldRepositionRef.current === true) {
        position2();
        focusSelectedItem == null ? void 0 : focusSelectedItem();
        shouldRepositionRef.current = false;
      }
    },
    [position2, focusSelectedItem]
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    SelectViewportProvider,
    {
      scope: __scopeSelect,
      contentWrapper,
      shouldExpandOnScrollRef,
      onScrollButtonChange: handleScrollButtonChange,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "div",
        {
          ref: setContentWrapper,
          style: {
            display: "flex",
            flexDirection: "column",
            position: "fixed",
            zIndex: contentZIndex
          },
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            Primitive.div,
            {
              ...popperProps,
              ref: composedRefs,
              style: {
                // When we get the height of the content, it includes borders. If we were to set
                // the height without having `boxSizing: 'border-box'` it would be too big.
                boxSizing: "border-box",
                // We need to ensure the content doesn't get taller than the wrapper
                maxHeight: "100%",
                ...popperProps.style
              }
            }
          )
        }
      )
    }
  );
});
SelectItemAlignedPosition.displayName = ITEM_ALIGNED_POSITION_NAME;
var POPPER_POSITION_NAME = "SelectPopperPosition";
var SelectPopperPosition = reactExports.forwardRef((props, forwardedRef) => {
  const {
    __scopeSelect,
    align = "start",
    collisionPadding = CONTENT_MARGIN,
    ...popperProps
  } = props;
  const popperScope = usePopperScope(__scopeSelect);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Content,
    {
      ...popperScope,
      ...popperProps,
      ref: forwardedRef,
      align,
      collisionPadding,
      style: {
        // Ensure border-box for floating-ui calculations
        boxSizing: "border-box",
        ...popperProps.style,
        // re-namespace exposed content custom properties
        ...{
          "--radix-select-content-transform-origin": "var(--radix-popper-transform-origin)",
          "--radix-select-content-available-width": "var(--radix-popper-available-width)",
          "--radix-select-content-available-height": "var(--radix-popper-available-height)",
          "--radix-select-trigger-width": "var(--radix-popper-anchor-width)",
          "--radix-select-trigger-height": "var(--radix-popper-anchor-height)"
        }
      }
    }
  );
});
SelectPopperPosition.displayName = POPPER_POSITION_NAME;
var [SelectViewportProvider, useSelectViewportContext] = createSelectContext(CONTENT_NAME, {});
var VIEWPORT_NAME = "SelectViewport";
var SelectViewport = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSelect, nonce, ...viewportProps } = props;
    const contentContext = useSelectContentContext(VIEWPORT_NAME, __scopeSelect);
    const viewportContext = useSelectViewportContext(VIEWPORT_NAME, __scopeSelect);
    const composedRefs = useComposedRefs(forwardedRef, contentContext.onViewportChange);
    const prevScrollTopRef = reactExports.useRef(0);
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "style",
        {
          dangerouslySetInnerHTML: {
            __html: `[data-radix-select-viewport]{scrollbar-width:none;-ms-overflow-style:none;-webkit-overflow-scrolling:touch;}[data-radix-select-viewport]::-webkit-scrollbar{display:none}`
          },
          nonce
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Collection.Slot, { scope: __scopeSelect, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive.div,
        {
          "data-radix-select-viewport": "",
          role: "presentation",
          ...viewportProps,
          ref: composedRefs,
          style: {
            // we use position: 'relative' here on the `viewport` so that when we call
            // `selectedItem.offsetTop` in calculations, the offset is relative to the viewport
            // (independent of the scrollUpButton).
            position: "relative",
            flex: 1,
            // Viewport should only be scrollable in the vertical direction.
            // This won't work in vertical writing modes, so we'll need to
            // revisit this if/when that is supported
            // https://developer.chrome.com/blog/vertical-form-controls
            overflow: "hidden auto",
            ...viewportProps.style
          },
          onScroll: composeEventHandlers(viewportProps.onScroll, (event) => {
            const viewport = event.currentTarget;
            const { contentWrapper, shouldExpandOnScrollRef } = viewportContext;
            if ((shouldExpandOnScrollRef == null ? void 0 : shouldExpandOnScrollRef.current) && contentWrapper) {
              const scrolledBy = Math.abs(prevScrollTopRef.current - viewport.scrollTop);
              if (scrolledBy > 0) {
                const availableHeight = window.innerHeight - CONTENT_MARGIN * 2;
                const cssMinHeight = parseFloat(contentWrapper.style.minHeight);
                const cssHeight = parseFloat(contentWrapper.style.height);
                const prevHeight = Math.max(cssMinHeight, cssHeight);
                if (prevHeight < availableHeight) {
                  const nextHeight = prevHeight + scrolledBy;
                  const clampedNextHeight = Math.min(availableHeight, nextHeight);
                  const heightDiff = nextHeight - clampedNextHeight;
                  contentWrapper.style.height = clampedNextHeight + "px";
                  if (contentWrapper.style.bottom === "0px") {
                    viewport.scrollTop = heightDiff > 0 ? heightDiff : 0;
                    contentWrapper.style.justifyContent = "flex-end";
                  }
                }
              }
            }
            prevScrollTopRef.current = viewport.scrollTop;
          })
        }
      ) })
    ] });
  }
);
SelectViewport.displayName = VIEWPORT_NAME;
var GROUP_NAME = "SelectGroup";
var [SelectGroupContextProvider, useSelectGroupContext] = createSelectContext(GROUP_NAME);
var SelectGroup = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSelect, ...groupProps } = props;
    const groupId = useId();
    return /* @__PURE__ */ jsxRuntimeExports.jsx(SelectGroupContextProvider, { scope: __scopeSelect, id: groupId, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Primitive.div, { role: "group", "aria-labelledby": groupId, ...groupProps, ref: forwardedRef }) });
  }
);
SelectGroup.displayName = GROUP_NAME;
var LABEL_NAME = "SelectLabel";
var SelectLabel = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSelect, ...labelProps } = props;
    const groupContext = useSelectGroupContext(LABEL_NAME, __scopeSelect);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Primitive.div, { id: groupContext.id, ...labelProps, ref: forwardedRef });
  }
);
SelectLabel.displayName = LABEL_NAME;
var ITEM_NAME = "SelectItem";
var [SelectItemContextProvider, useSelectItemContext] = createSelectContext(ITEM_NAME);
var SelectItem = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeSelect,
      value,
      disabled = false,
      textValue: textValueProp,
      ...itemProps
    } = props;
    const context = useSelectContext(ITEM_NAME, __scopeSelect);
    const contentContext = useSelectContentContext(ITEM_NAME, __scopeSelect);
    const isSelected = context.value === value;
    const [textValue, setTextValue] = reactExports.useState(textValueProp ?? "");
    const [isFocused, setIsFocused] = reactExports.useState(false);
    const composedRefs = useComposedRefs(
      forwardedRef,
      (node2) => {
        var _a;
        return (_a = contentContext.itemRefCallback) == null ? void 0 : _a.call(contentContext, node2, value, disabled);
      }
    );
    const textId = useId();
    const pointerTypeRef = reactExports.useRef("touch");
    const handleSelect = () => {
      if (!disabled) {
        context.onValueChange(value);
        context.onOpenChange(false);
      }
    };
    if (value === "") {
      throw new Error(
        "A <Select.Item /> must have a value prop that is not an empty string. This is because the Select value can be set to an empty string to clear the selection and show the placeholder."
      );
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SelectItemContextProvider,
      {
        scope: __scopeSelect,
        value,
        disabled,
        textId,
        isSelected,
        onItemTextChange: reactExports.useCallback((node2) => {
          setTextValue((prevTextValue) => prevTextValue || ((node2 == null ? void 0 : node2.textContent) ?? "").trim());
        }, []),
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Collection.ItemSlot,
          {
            scope: __scopeSelect,
            value,
            disabled,
            textValue,
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              Primitive.div,
              {
                role: "option",
                "aria-labelledby": textId,
                "data-highlighted": isFocused ? "" : void 0,
                "aria-selected": isSelected && isFocused,
                "data-state": isSelected ? "checked" : "unchecked",
                "aria-disabled": disabled || void 0,
                "data-disabled": disabled ? "" : void 0,
                tabIndex: disabled ? void 0 : -1,
                ...itemProps,
                ref: composedRefs,
                onFocus: composeEventHandlers(itemProps.onFocus, () => setIsFocused(true)),
                onBlur: composeEventHandlers(itemProps.onBlur, () => setIsFocused(false)),
                onClick: composeEventHandlers(itemProps.onClick, () => {
                  if (pointerTypeRef.current !== "mouse") handleSelect();
                }),
                onPointerUp: composeEventHandlers(itemProps.onPointerUp, () => {
                  if (pointerTypeRef.current === "mouse") handleSelect();
                }),
                onPointerDown: composeEventHandlers(itemProps.onPointerDown, (event) => {
                  pointerTypeRef.current = event.pointerType;
                }),
                onPointerMove: composeEventHandlers(itemProps.onPointerMove, (event) => {
                  var _a;
                  pointerTypeRef.current = event.pointerType;
                  if (disabled) {
                    (_a = contentContext.onItemLeave) == null ? void 0 : _a.call(contentContext);
                  } else if (pointerTypeRef.current === "mouse") {
                    event.currentTarget.focus({ preventScroll: true });
                  }
                }),
                onPointerLeave: composeEventHandlers(itemProps.onPointerLeave, (event) => {
                  var _a;
                  if (event.currentTarget === document.activeElement) {
                    (_a = contentContext.onItemLeave) == null ? void 0 : _a.call(contentContext);
                  }
                }),
                onKeyDown: composeEventHandlers(itemProps.onKeyDown, (event) => {
                  var _a;
                  const isTypingAhead = ((_a = contentContext.searchRef) == null ? void 0 : _a.current) !== "";
                  if (isTypingAhead && event.key === " ") return;
                  if (SELECTION_KEYS.includes(event.key)) handleSelect();
                  if (event.key === " ") event.preventDefault();
                })
              }
            )
          }
        )
      }
    );
  }
);
SelectItem.displayName = ITEM_NAME;
var ITEM_TEXT_NAME = "SelectItemText";
var SelectItemText = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSelect, className, style, ...itemTextProps } = props;
    const context = useSelectContext(ITEM_TEXT_NAME, __scopeSelect);
    const contentContext = useSelectContentContext(ITEM_TEXT_NAME, __scopeSelect);
    const itemContext = useSelectItemContext(ITEM_TEXT_NAME, __scopeSelect);
    const nativeOptionsContext = useSelectNativeOptionsContext(ITEM_TEXT_NAME, __scopeSelect);
    const [itemTextNode, setItemTextNode] = reactExports.useState(null);
    const composedRefs = useComposedRefs(
      forwardedRef,
      (node2) => setItemTextNode(node2),
      itemContext.onItemTextChange,
      (node2) => {
        var _a;
        return (_a = contentContext.itemTextRefCallback) == null ? void 0 : _a.call(contentContext, node2, itemContext.value, itemContext.disabled);
      }
    );
    const textContent = itemTextNode == null ? void 0 : itemTextNode.textContent;
    const nativeOption = reactExports.useMemo(
      () => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: itemContext.value, disabled: itemContext.disabled, children: textContent }, itemContext.value),
      [itemContext.disabled, itemContext.value, textContent]
    );
    const { onNativeOptionAdd, onNativeOptionRemove } = nativeOptionsContext;
    useLayoutEffect2(() => {
      onNativeOptionAdd(nativeOption);
      return () => onNativeOptionRemove(nativeOption);
    }, [onNativeOptionAdd, onNativeOptionRemove, nativeOption]);
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Primitive.span, { id: itemContext.textId, ...itemTextProps, ref: composedRefs }),
      itemContext.isSelected && context.valueNode && !context.valueNodeHasChildren ? reactDomExports.createPortal(itemTextProps.children, context.valueNode) : null
    ] });
  }
);
SelectItemText.displayName = ITEM_TEXT_NAME;
var ITEM_INDICATOR_NAME = "SelectItemIndicator";
var SelectItemIndicator = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSelect, ...itemIndicatorProps } = props;
    const itemContext = useSelectItemContext(ITEM_INDICATOR_NAME, __scopeSelect);
    return itemContext.isSelected ? /* @__PURE__ */ jsxRuntimeExports.jsx(Primitive.span, { "aria-hidden": true, ...itemIndicatorProps, ref: forwardedRef }) : null;
  }
);
SelectItemIndicator.displayName = ITEM_INDICATOR_NAME;
var SCROLL_UP_BUTTON_NAME = "SelectScrollUpButton";
var SelectScrollUpButton = reactExports.forwardRef((props, forwardedRef) => {
  const contentContext = useSelectContentContext(SCROLL_UP_BUTTON_NAME, props.__scopeSelect);
  const viewportContext = useSelectViewportContext(SCROLL_UP_BUTTON_NAME, props.__scopeSelect);
  const [canScrollUp, setCanScrollUp] = reactExports.useState(false);
  const composedRefs = useComposedRefs(forwardedRef, viewportContext.onScrollButtonChange);
  useLayoutEffect2(() => {
    if (contentContext.viewport && contentContext.isPositioned) {
      let handleScroll2 = function() {
        const canScrollUp2 = viewport.scrollTop > 0;
        setCanScrollUp(canScrollUp2);
      };
      const viewport = contentContext.viewport;
      handleScroll2();
      viewport.addEventListener("scroll", handleScroll2);
      return () => viewport.removeEventListener("scroll", handleScroll2);
    }
  }, [contentContext.viewport, contentContext.isPositioned]);
  return canScrollUp ? /* @__PURE__ */ jsxRuntimeExports.jsx(
    SelectScrollButtonImpl,
    {
      ...props,
      ref: composedRefs,
      onAutoScroll: () => {
        const { viewport, selectedItem } = contentContext;
        if (viewport && selectedItem) {
          viewport.scrollTop = viewport.scrollTop - selectedItem.offsetHeight;
        }
      }
    }
  ) : null;
});
SelectScrollUpButton.displayName = SCROLL_UP_BUTTON_NAME;
var SCROLL_DOWN_BUTTON_NAME = "SelectScrollDownButton";
var SelectScrollDownButton = reactExports.forwardRef((props, forwardedRef) => {
  const contentContext = useSelectContentContext(SCROLL_DOWN_BUTTON_NAME, props.__scopeSelect);
  const viewportContext = useSelectViewportContext(SCROLL_DOWN_BUTTON_NAME, props.__scopeSelect);
  const [canScrollDown, setCanScrollDown] = reactExports.useState(false);
  const composedRefs = useComposedRefs(forwardedRef, viewportContext.onScrollButtonChange);
  useLayoutEffect2(() => {
    if (contentContext.viewport && contentContext.isPositioned) {
      let handleScroll2 = function() {
        const maxScroll = viewport.scrollHeight - viewport.clientHeight;
        const canScrollDown2 = Math.ceil(viewport.scrollTop) < maxScroll;
        setCanScrollDown(canScrollDown2);
      };
      const viewport = contentContext.viewport;
      handleScroll2();
      viewport.addEventListener("scroll", handleScroll2);
      return () => viewport.removeEventListener("scroll", handleScroll2);
    }
  }, [contentContext.viewport, contentContext.isPositioned]);
  return canScrollDown ? /* @__PURE__ */ jsxRuntimeExports.jsx(
    SelectScrollButtonImpl,
    {
      ...props,
      ref: composedRefs,
      onAutoScroll: () => {
        const { viewport, selectedItem } = contentContext;
        if (viewport && selectedItem) {
          viewport.scrollTop = viewport.scrollTop + selectedItem.offsetHeight;
        }
      }
    }
  ) : null;
});
SelectScrollDownButton.displayName = SCROLL_DOWN_BUTTON_NAME;
var SelectScrollButtonImpl = reactExports.forwardRef((props, forwardedRef) => {
  const { __scopeSelect, onAutoScroll, ...scrollIndicatorProps } = props;
  const contentContext = useSelectContentContext("SelectScrollButton", __scopeSelect);
  const autoScrollTimerRef = reactExports.useRef(null);
  const getItems = useCollection(__scopeSelect);
  const clearAutoScrollTimer = reactExports.useCallback(() => {
    if (autoScrollTimerRef.current !== null) {
      window.clearInterval(autoScrollTimerRef.current);
      autoScrollTimerRef.current = null;
    }
  }, []);
  reactExports.useEffect(() => {
    return () => clearAutoScrollTimer();
  }, [clearAutoScrollTimer]);
  useLayoutEffect2(() => {
    var _a;
    const activeItem = getItems().find((item) => item.ref.current === document.activeElement);
    (_a = activeItem == null ? void 0 : activeItem.ref.current) == null ? void 0 : _a.scrollIntoView({ block: "nearest" });
  }, [getItems]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Primitive.div,
    {
      "aria-hidden": true,
      ...scrollIndicatorProps,
      ref: forwardedRef,
      style: { flexShrink: 0, ...scrollIndicatorProps.style },
      onPointerDown: composeEventHandlers(scrollIndicatorProps.onPointerDown, () => {
        if (autoScrollTimerRef.current === null) {
          autoScrollTimerRef.current = window.setInterval(onAutoScroll, 50);
        }
      }),
      onPointerMove: composeEventHandlers(scrollIndicatorProps.onPointerMove, () => {
        var _a;
        (_a = contentContext.onItemLeave) == null ? void 0 : _a.call(contentContext);
        if (autoScrollTimerRef.current === null) {
          autoScrollTimerRef.current = window.setInterval(onAutoScroll, 50);
        }
      }),
      onPointerLeave: composeEventHandlers(scrollIndicatorProps.onPointerLeave, () => {
        clearAutoScrollTimer();
      })
    }
  );
});
var SEPARATOR_NAME = "SelectSeparator";
var SelectSeparator = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSelect, ...separatorProps } = props;
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Primitive.div, { "aria-hidden": true, ...separatorProps, ref: forwardedRef });
  }
);
SelectSeparator.displayName = SEPARATOR_NAME;
var ARROW_NAME = "SelectArrow";
var SelectArrow = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSelect, ...arrowProps } = props;
    const popperScope = usePopperScope(__scopeSelect);
    const context = useSelectContext(ARROW_NAME, __scopeSelect);
    const contentContext = useSelectContentContext(ARROW_NAME, __scopeSelect);
    return context.open && contentContext.position === "popper" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Arrow, { ...popperScope, ...arrowProps, ref: forwardedRef }) : null;
  }
);
SelectArrow.displayName = ARROW_NAME;
var BUBBLE_INPUT_NAME = "SelectBubbleInput";
var SelectBubbleInput = reactExports.forwardRef(
  ({ __scopeSelect, value, ...props }, forwardedRef) => {
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref);
    const prevValue = usePrevious(value);
    reactExports.useEffect(() => {
      const select = ref.current;
      if (!select) return;
      const selectProto = window.HTMLSelectElement.prototype;
      const descriptor = Object.getOwnPropertyDescriptor(
        selectProto,
        "value"
      );
      const setValue = descriptor.set;
      if (prevValue !== value && setValue) {
        const event = new Event("change", { bubbles: true });
        setValue.call(select, value);
        select.dispatchEvent(event);
      }
    }, [prevValue, value]);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.select,
      {
        ...props,
        style: { ...VISUALLY_HIDDEN_STYLES, ...props.style },
        ref: composedRefs,
        defaultValue: value
      }
    );
  }
);
SelectBubbleInput.displayName = BUBBLE_INPUT_NAME;
function shouldShowPlaceholder(value) {
  return value === "" || value === void 0;
}
function useTypeaheadSearch(onSearchChange) {
  const handleSearchChange = useCallbackRef$1(onSearchChange);
  const searchRef = reactExports.useRef("");
  const timerRef = reactExports.useRef(0);
  const handleTypeaheadSearch = reactExports.useCallback(
    (key) => {
      const search2 = searchRef.current + key;
      handleSearchChange(search2);
      (function updateSearch(value) {
        searchRef.current = value;
        window.clearTimeout(timerRef.current);
        if (value !== "") timerRef.current = window.setTimeout(() => updateSearch(""), 1e3);
      })(search2);
    },
    [handleSearchChange]
  );
  const resetTypeahead = reactExports.useCallback(() => {
    searchRef.current = "";
    window.clearTimeout(timerRef.current);
  }, []);
  reactExports.useEffect(() => {
    return () => window.clearTimeout(timerRef.current);
  }, []);
  return [searchRef, handleTypeaheadSearch, resetTypeahead];
}
function findNextItem(items, search2, currentItem) {
  const isRepeated = search2.length > 1 && Array.from(search2).every((char) => char === search2[0]);
  const normalizedSearch = isRepeated ? search2[0] : search2;
  const currentItemIndex = currentItem ? items.indexOf(currentItem) : -1;
  let wrappedItems = wrapArray(items, Math.max(currentItemIndex, 0));
  const excludeCurrentItem = normalizedSearch.length === 1;
  if (excludeCurrentItem) wrappedItems = wrappedItems.filter((v) => v !== currentItem);
  const nextItem = wrappedItems.find(
    (item) => item.textValue.toLowerCase().startsWith(normalizedSearch.toLowerCase())
  );
  return nextItem !== currentItem ? nextItem : void 0;
}
function wrapArray(array, startIndex) {
  return array.map((_, index2) => array[(startIndex + index2) % array.length]);
}
var Root2 = Select;
var Trigger = SelectTrigger;
var Value = SelectValue;
var Icon = SelectIcon;
var Portal = SelectPortal;
var Content2 = SelectContent;
var Viewport = SelectViewport;
var Item = SelectItem;
var ItemText = SelectItemText;
const useAnnotationStore = create$1((set, get) => ({
  annotations: [],
  loading: false,
  error: null,
  hydrate: async () => {
    set({ loading: true, error: null });
    try {
      const rows = await annotationsApi.list();
      set({ annotations: rows, loading: false });
    } catch (err) {
      set({
        loading: false,
        error: err instanceof Error ? err.message : "hydrate failed"
      });
    }
  },
  add: async (input) => {
    const created = await annotationsApi.create(input);
    set({ annotations: [...get().annotations, created] });
    return created;
  },
  remove: async (id) => {
    await annotationsApi.remove(id);
    set({ annotations: get().annotations.filter((a) => a.id !== id) });
  },
  setAll: (rows) => set({ annotations: rows })
}));
const __vite_import_meta_env__ = { "BASE_URL": "/hive-public/", "DEV": false, "MODE": "production", "PROD": true, "SSR": false, "VITE_BASE_PATH": "/hive-public/", "VITE_MOCK_BACKEND": "1" };
const SEVERITY_OPTIONS = ["note", "bug", "polish"];
const SEVERITY_TONE = {
  note: "neutral",
  bug: "danger",
  polish: "warn"
};
const SEVERITY_ORDER = ["bug", "polish", "note"];
const LIST_PANEL_MIN_HEIGHT = 240;
function buildSelector(el) {
  const parts = [];
  let node2 = el;
  let depth = 0;
  while (node2 && node2.nodeType === 1 && depth < 5 && node2.tagName !== "BODY") {
    const testId = node2.getAttribute("data-testid");
    if (testId) {
      parts.unshift(`[data-testid="${testId}"]`);
      break;
    }
    if (node2.id) {
      parts.unshift(`#${node2.id}`);
      break;
    }
    let part = node2.tagName.toLowerCase();
    const parent = node2.parentElement;
    if (parent) {
      const sameTag = Array.from(parent.children).filter((c) => c.tagName === node2.tagName);
      if (sameTag.length > 1) {
        const idx = sameTag.indexOf(node2) + 1;
        part += `:nth-of-type(${idx})`;
      }
    }
    parts.unshift(part);
    node2 = node2.parentElement;
    depth++;
  }
  return parts.join(" > ");
}
function buildLabel(el) {
  const tag = el.tagName.toLowerCase();
  const text2 = (el.textContent ?? "").trim().slice(0, 40);
  if (text2) return `<${tag}> ${text2}`;
  const aria2 = el.getAttribute("aria-label");
  if (aria2) return `<${tag}> ${aria2}`;
  return `<${tag}>`;
}
function startElementPicker(onPick, onCancel) {
  let activeOutline = null;
  const clearOutline = () => {
    if (activeOutline) {
      activeOutline.el.style.outline = activeOutline.prev;
      activeOutline = null;
    }
  };
  const onMove = (e) => {
    const el = e.target;
    if (!el) return;
    if (el.closest("[data-annotation-picker-ui]")) return;
    clearOutline();
    activeOutline = { el, prev: el.style.outline };
    el.style.outline = "2px solid var(--hv-honey-5, #ffb84d)";
  };
  const onClick = (e) => {
    var _a;
    const el = e.target;
    if (!el) return;
    if ((_a = el.closest) == null ? void 0 : _a.call(el, "[data-annotation-picker-ui]")) return;
    e.preventDefault();
    e.stopPropagation();
    clearOutline();
    teardown();
    onPick({ selector: buildSelector(el), label: buildLabel(el) });
  };
  const onKey = (e) => {
    if (e.key === "Escape") {
      clearOutline();
      teardown();
      onCancel();
    }
  };
  const teardown = () => {
    document.removeEventListener("mousemove", onMove, true);
    document.removeEventListener("click", onClick, true);
    document.removeEventListener("keydown", onKey, true);
    document.body.style.cursor = "";
  };
  document.addEventListener("mousemove", onMove, true);
  document.addEventListener("click", onClick, true);
  document.addEventListener("keydown", onKey, true);
  document.body.style.cursor = "crosshair";
  return teardown;
}
function SeveritySelect({ value, onChange, testId }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Root2, { value, onValueChange: (v) => onChange(v), children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      Trigger,
      {
        id: "annotation-severity",
        className: "hv-inp hv-select-trigger",
        "data-testid": testId,
        "aria-label": "Severity",
        title: "Severity",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Value, {}),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Icon, { className: "hv-select-icon", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.chevD, { size: 12 }) })
        ]
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Portal, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      Content2,
      {
        className: "hv-select-content",
        position: "popper",
        sideOffset: 4,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(Viewport, { children: SEVERITY_OPTIONS.map((s) => /* @__PURE__ */ jsxRuntimeExports.jsx(Item, { value: s, className: "hv-select-item", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ItemText, { children: s }) }, s)) })
      }
    ) })
  ] });
}
function TargetPicker({ value, label, onChange }) {
  const [picking, setPicking] = reactExports.useState(false);
  const [rawMode, setRawMode] = reactExports.useState(false);
  const teardownRef = reactExports.useRef(null);
  const startPick = reactExports.useCallback(() => {
    setPicking(true);
    teardownRef.current = startElementPicker(
      ({ selector, label: lbl }) => {
        onChange(selector, lbl);
        setPicking(false);
      },
      () => setPicking(false)
    );
  }, [onChange]);
  reactExports.useEffect(() => () => {
    var _a;
    (_a = teardownRef.current) == null ? void 0 : _a.call(teardownRef);
  }, []);
  const hasValue = value.trim().length > 0;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-ann-target", "data-annotation-picker-ui": true, children: [
    !rawMode && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "button",
        {
          type: "button",
          className: "hv-btn hv-btn-default hv-btn-sm",
          onClick: startPick,
          "data-testid": "annotation-target-pick",
          disabled: picking,
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.link, { size: 12 }),
            " ",
            picking ? "Click an element…" : hasValue ? "Re-pick element" : "Pick element"
          ]
        }
      ),
      hasValue && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-ann-target-chip", title: value, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-ann-target-label", children: label ?? value }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            type: "button",
            className: "hv-ann-target-clear",
            "aria-label": "Clear target",
            onClick: () => onChange("", null),
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.close, { size: 10 })
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "button",
          className: "hv-panel-link hv-ann-target-raw",
          onClick: () => setRawMode(true),
          children: "edit raw"
        }
      )
    ] }),
    rawMode && /* @__PURE__ */ jsxRuntimeExports.jsx(
      "input",
      {
        value,
        onChange: (e) => onChange(e.target.value, null),
        onBlur: () => setRawMode(false),
        placeholder: "CSS selector or path:line (optional)",
        "data-testid": "annotation-target-input",
        className: "hv-inp hv-ann-ref",
        "aria-label": "Target reference (raw)",
        autoFocus: true
      }
    ),
    !rawMode && /* @__PURE__ */ jsxRuntimeExports.jsx(
      "input",
      {
        type: "hidden",
        value,
        "data-testid": "annotation-target-input",
        className: "hv-inp",
        readOnly: true,
        "aria-hidden": "true"
      }
    )
  ] });
}
function AnnotationsView() {
  const navigate = useNavigate();
  const annotations$1 = useAnnotationStore((s) => s.annotations);
  const loading = useAnnotationStore((s) => s.loading);
  const error = useAnnotationStore((s) => s.error);
  const hydrate = useAnnotationStore((s) => s.hydrate);
  const add = useAnnotationStore((s) => s.add);
  const remove = useAnnotationStore((s) => s.remove);
  const [body, setBody] = reactExports.useState("");
  const [targetRef, setTargetRef] = reactExports.useState("");
  const [targetLabel, setTargetLabel] = reactExports.useState(null);
  const [severity, setSeverity] = reactExports.useState("note");
  const [submitting, setSubmitting] = reactExports.useState(false);
  const [missionStarting, setMissionStarting] = reactExports.useState(false);
  const [missionError, setMissionError] = reactExports.useState(null);
  const [editingId, setEditingId] = reactExports.useState(null);
  const [editBody, setEditBody] = reactExports.useState("");
  const [editSeverity, setEditSeverity] = reactExports.useState("note");
  reactExports.useEffect(() => {
    void hydrate();
  }, [hydrate]);
  const grouped = reactExports.useMemo(() => {
    const out = {};
    for (const a of annotations$1) {
      const key = a.severity || "note";
      out[key] = out[key] ?? [];
      out[key].push(a);
    }
    return out;
  }, [annotations$1]);
  async function handleAdd(e) {
    e.preventDefault();
    if (!body.trim()) return;
    setSubmitting(true);
    try {
      await add({
        body: body.trim(),
        target_ref: targetRef.trim() || null,
        severity
      });
      setBody("");
      setTargetRef("");
      setTargetLabel(null);
    } finally {
      setSubmitting(false);
    }
  }
  async function handleFixAll() {
    setMissionStarting(true);
    setMissionError(null);
    try {
      const w = window;
      const enabled = (__vite_import_meta_env__ == null ? void 0 : __vite_import_meta_env__.VITE_HIVE_ENABLE_TEST_DISPATCH) === "1";
      const dispatch = enabled && w.__HIVE_DISPATCH_OVERRIDE__ ? w.__HIVE_DISPATCH_OVERRIDE__ : "spawn";
      const { annotationsApi: annotationsApi2 } = await __vitePreload(async () => {
        const { annotationsApi: annotationsApi3 } = await Promise.resolve().then(() => annotations);
        return { annotationsApi: annotationsApi3 };
      }, true ? void 0 : void 0);
      const res = await annotationsApi2.startMission({ dispatch });
      tracer("ANN-03", `ok mission=${res.id}`);
      await Promise.all([
        useTaskStore.getState().hydrate(),
        useAnnotationStore.getState().hydrate()
      ]);
      navigate("/console/kanban");
    } catch (err) {
      setMissionError(err instanceof Error ? err.message : "mission start failed");
    } finally {
      setMissionStarting(false);
    }
  }
  function startEdit(id, currentBody, currentSev) {
    setEditingId(id);
    setEditBody(currentBody);
    setEditSeverity(SEVERITY_OPTIONS.includes(currentSev) ? currentSev : "note");
  }
  function cancelEdit() {
    setEditingId(null);
    setEditBody("");
  }
  async function saveEdit(id, original) {
    if (!editBody.trim()) return;
    const newBody = editBody.trim();
    const newSev = editSeverity;
    cancelEdit();
    await remove(id);
    await add({
      body: newBody,
      target_ref: original.target_ref ?? null,
      target_line: original.target_line ?? null,
      severity: newSev
    });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view", "data-testid": "view-annotations", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-view-head", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-view-summary", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-vs-item", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-num", children: annotations$1.length }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-vs-lbl", children: "open annotations" })
      ] }) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-view-actions", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Btn,
        {
          variant: "primary",
          "data-testid": "fix-all-annotations",
          onClick: handleFixAll,
          disabled: missionStarting || annotations$1.length === 0,
          leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.zap, { size: 13 }),
          children: missionStarting ? "Starting…" : "Fix All Annotations"
        }
      ) })
    ] }),
    error && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { role: "alert", className: "hv-ann-alert", children: error }),
    missionError && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { role: "alert", "data-testid": "mission-error", className: "hv-ann-alert", children: missionError }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", style: { marginBottom: 16 }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.fileAdd, { size: 13 }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Add annotation" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-panel-body", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "form",
        {
          onSubmit: handleAdd,
          "data-testid": "annotation-add-form",
          className: "hv-ann-form",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-ann-body", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              "textarea",
              {
                value: body,
                onChange: (e) => setBody(e.target.value),
                placeholder: "Describe an issue or polish item…",
                "data-testid": "annotation-body-input",
                rows: 2,
                className: "hv-inp",
                "aria-label": "Annotation body"
              }
            ) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-ann-form-row2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                TargetPicker,
                {
                  value: targetRef,
                  label: targetLabel,
                  onChange: (sel, lbl) => {
                    setTargetRef(sel);
                    setTargetLabel(lbl);
                  }
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-ann-sev", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("label", { htmlFor: "annotation-severity", className: "sr-only", children: "Severity" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  SeveritySelect,
                  {
                    value: severity,
                    onChange: setSeverity,
                    testId: "annotation-severity-select"
                  }
                )
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Btn,
                {
                  type: "submit",
                  disabled: submitting || !body.trim(),
                  "data-testid": "annotation-add-btn",
                  leftIcon: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.plus, { size: 13 }),
                  children: submitting ? "…" : "Add"
                }
              )
            ] })
          ]
        }
      ) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-head", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.alert, { size: 13 }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Open annotations" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "hv-panel-sub", children: [
          annotations$1.length,
          " item",
          annotations$1.length !== 1 ? "s" : ""
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-ann-list", style: { minHeight: LIST_PANEL_MIN_HEIGHT }, children: [
        loading && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { padding: "8px 14px" }, children: Array.from({ length: SKELETON_COUNT_DEFAULT }, (_, i) => /* @__PURE__ */ jsxRuntimeExports.jsx(SkeletonRow$1, { cols: 3 }, i)) }),
        !loading && annotations$1.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-panel-empty", "data-testid": "annotation-empty", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { marginBottom: 6, color: "var(--hv-fg-subtle)" }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.fileAdd, { size: 28 }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { fontSize: 14, fontWeight: 500, color: "var(--hv-fg-muted)", marginBottom: 4 }, children: "No annotations yet" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { fontSize: 12, color: "var(--hv-fg-subtle)", maxWidth: 260, textAlign: "center" }, children: "Add one above to seed a Fix All mission." })
        ] }),
        !loading && SEVERITY_ORDER.filter((sev) => {
          var _a;
          return ((_a = grouped[sev]) == null ? void 0 : _a.length) > 0;
        }).map((sev) => {
          const rows = grouped[sev];
          const tone = SEVERITY_TONE[sev] ?? "neutral";
          return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-ann-group", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-ann-group-header", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone, children: sev }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hv-ann-group-count", children: rows.length })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-ann-group-rows", children: rows.map((a) => {
              const isEditing = editingId === a.id;
              return /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "div",
                {
                  "data-ann-id": a.id,
                  "data-testid": `annotation-row-${a.id}`,
                  className: `hv-ann-row${isEditing ? " editing" : ""}`,
                  role: isEditing ? void 0 : "button",
                  tabIndex: isEditing ? -1 : 0,
                  onClick: () => {
                    if (!isEditing) startEdit(a.id, a.body, a.severity || "note");
                  },
                  onKeyDown: (e) => {
                    if (!isEditing && (e.key === "Enter" || e.key === " ")) {
                      e.preventDefault();
                      startEdit(a.id, a.body, a.severity || "note");
                    }
                  },
                  children: [
                    !isEditing && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-ann-body-text", children: a.body }),
                        a.target_ref && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-ann-ref-text", children: [
                          "→ ",
                          a.target_ref,
                          a.target_line ? `:${a.target_line}` : ""
                        ] })
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        Btn,
                        {
                          size: "sm",
                          variant: "subtle",
                          "data-testid": `annotation-delete-${a.id}`,
                          onClick: (e) => {
                            e.stopPropagation();
                            void remove(a.id);
                          },
                          "aria-label": `Delete annotation: ${a.body.slice(0, 40)}`,
                          children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon$1.close, { size: 12 })
                        }
                      )
                    ] }),
                    isEditing && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "div",
                      {
                        className: "hv-ann-edit",
                        onClick: (e) => e.stopPropagation(),
                        "data-testid": `annotation-edit-${a.id}`,
                        children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx(
                            "textarea",
                            {
                              className: "hv-inp",
                              rows: 2,
                              value: editBody,
                              onChange: (e) => setEditBody(e.target.value),
                              autoFocus: true,
                              "aria-label": "Edit annotation body"
                            }
                          ),
                          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-ann-edit-row", children: [
                            /* @__PURE__ */ jsxRuntimeExports.jsx(
                              SeveritySelect,
                              {
                                value: editSeverity,
                                onChange: setEditSeverity,
                                testId: `annotation-edit-severity-${a.id}`
                              }
                            ),
                            /* @__PURE__ */ jsxRuntimeExports.jsx(
                              Btn,
                              {
                                size: "sm",
                                variant: "primary",
                                onClick: () => void saveEdit(a.id, { target_ref: a.target_ref, target_line: a.target_line }),
                                "data-testid": `annotation-edit-save-${a.id}`,
                                disabled: !editBody.trim(),
                                children: "Save"
                              }
                            ),
                            /* @__PURE__ */ jsxRuntimeExports.jsx(Btn, { size: "sm", variant: "subtle", onClick: cancelEdit, children: "Cancel" })
                          ] })
                        ]
                      }
                    )
                  ]
                },
                a.id
              );
            }) })
          ] }, sev);
        })
      ] })
    ] })
  ] });
}
const AUTO_DISMISS_MS = 5e3;
const TYPE_COLORS = {
  error: "var(--hv-danger, #e84040)",
  warning: "var(--hv-warn, #e8932c)",
  info: "var(--hv-info, #3b82f6)"
};
const TYPE_ICONS = {
  error: "✕",
  warning: "⚠",
  info: "ℹ"
};
function ToastItem({ toast }) {
  const dismiss = useToastStore((s) => s.dismiss);
  const timeoutRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    timeoutRef.current = setTimeout(() => {
      dismiss(toast.id);
    }, AUTO_DISMISS_MS);
    return () => {
      if (timeoutRef.current !== null) {
        clearTimeout(timeoutRef.current);
      }
    };
  }, [toast.id, dismiss]);
  const accentColor = TYPE_COLORS[toast.type];
  const icon = TYPE_ICONS[toast.type];
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      "data-testid": `toast-item-${toast.id}`,
      role: "alert",
      style: {
        display: "flex",
        alignItems: "flex-start",
        gap: "10px",
        background: "var(--hv-surface-2, #1e2130)",
        border: `1px solid ${accentColor}`,
        borderLeft: `4px solid ${accentColor}`,
        borderRadius: "6px",
        padding: "10px 12px",
        minWidth: "280px",
        maxWidth: "380px",
        boxShadow: "0 4px 12px rgba(0,0,0,0.4)",
        color: "var(--hv-fg, #e8eaf0)",
        fontSize: "13px",
        lineHeight: "1.4",
        position: "relative"
      },
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "span",
          {
            "aria-hidden": "true",
            style: { color: accentColor, fontWeight: 700, flexShrink: 0, fontSize: "14px", marginTop: "1px" },
            children: icon
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { flex: 1, minWidth: 0 }, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { fontWeight: 600, marginBottom: toast.message ? "3px" : 0 }, children: toast.title }),
          toast.message && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { opacity: 0.75, wordBreak: "break-word" }, children: toast.message }),
          toast.action && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              onClick: () => {
                toast.action.onClick();
                dismiss(toast.id);
              },
              style: {
                marginTop: "6px",
                background: "none",
                border: `1px solid ${accentColor}`,
                borderRadius: "3px",
                cursor: "pointer",
                color: accentColor,
                fontSize: "11px",
                fontWeight: 600,
                padding: "2px 8px",
                lineHeight: 1.5
              },
              children: toast.action.label
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            "aria-label": "Dismiss notification",
            onClick: () => dismiss(toast.id),
            style: {
              background: "none",
              border: "none",
              cursor: "pointer",
              color: "var(--hv-fg-subtle, #8891b0)",
              padding: "0 2px",
              fontSize: "14px",
              lineHeight: 1,
              flexShrink: 0,
              marginTop: "1px"
            },
            children: "✕"
          }
        )
      ]
    }
  );
}
function ToastContainer() {
  const toasts = useToastStore((s) => s.toasts);
  const tracerEmitted = reactExports.useRef(false);
  reactExports.useEffect(() => {
    if (toasts.length > 0 && !tracerEmitted.current) {
      tracerEmitted.current = true;
      tracer("CAP-19", "ok");
    }
  }, [toasts.length]);
  const visible = toasts.slice(-5);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      "data-testid": "toast-container",
      "aria-live": "polite",
      "aria-atomic": "false",
      style: {
        position: "fixed",
        top: "16px",
        right: "16px",
        zIndex: 2e3,
        display: "flex",
        flexDirection: "column",
        gap: "8px",
        pointerEvents: visible.length > 0 ? "auto" : "none"
      },
      children: visible.map((toast) => /* @__PURE__ */ jsxRuntimeExports.jsx(ToastItem, { toast }, toast.id))
    }
  );
}
const VERSION = "v0.41.2";
const BUDGET_HARD_USD = 21;
const BUDGET_WARN_RATIO = 0.8;
const POLL_INTERVAL_MS = 5e3;
const TERMINAL_WORKER_GRACE_MS = 6e4;
const TERMINAL_WORKER_STATUSES = /* @__PURE__ */ new Set([
  "stopped",
  "stopping",
  "killed",
  "timeout",
  "dead",
  "error"
]);
function parseWorkerTimestamp(value) {
  if (!value) return null;
  const normalized = value.includes("T") ? value : value.replace(" ", "T") + "Z";
  const ms = Date.parse(normalized);
  return Number.isFinite(ms) ? ms : null;
}
function isFleetVisibleWorker(worker) {
  if (!TERMINAL_WORKER_STATUSES.has(worker.status)) return true;
  const changedAt = parseWorkerTimestamp(worker.updated_at) ?? parseWorkerTimestamp(worker.last_heartbeat) ?? parseWorkerTimestamp(worker.created_at);
  return changedAt !== null && Date.now() - changedAt < TERMINAL_WORKER_GRACE_MS;
}
const ROUTE_PATHS = {
  dash: "/console",
  kanban: "/console/kanban",
  annotations: "/console/annotations",
  fleet: "/console/fleet",
  duties: "/console/duties",
  chat: "/console/channels",
  approvals: "/console/approvals",
  worktrees: "/console/worktrees",
  alerts: "/console/alerts",
  system: "/console/system",
  costs: "/console/costs"
};
function surfaceFromPath(pathname) {
  if (pathname.startsWith("/console/kanban") || pathname.startsWith("/console/missions")) return "kanban";
  if (pathname.startsWith("/console/annotations")) return "annotations";
  if (pathname.startsWith("/console/fleet") || pathname.startsWith("/console/workers")) return "fleet";
  if (pathname.startsWith("/console/duties")) return "duties";
  if (pathname.startsWith("/console/channels")) return "chat";
  if (pathname.startsWith("/console/approvals")) return "approvals";
  if (pathname.startsWith("/console/alerts")) return "alerts";
  if (pathname.startsWith("/console/worktrees")) return "worktrees";
  if (pathname.startsWith("/console/costs")) return "costs";
  if (pathname.startsWith("/console/system")) return "system";
  return "dash";
}
const CRUMBS_MAP = {
  dash: [{ label: "Workspace" }, { label: "ops-prod" }, { label: "Dashboard" }],
  kanban: [{ label: "Workspace" }, { label: "ops-prod" }, { label: "Missions" }],
  duties: [{ label: "Workspace" }, { label: "ops-prod" }, { label: "Duties" }],
  fleet: [{ label: "Workspace" }, { label: "ops-prod" }, { label: "Fleet" }],
  chat: [{ label: "Workspace" }, { label: "ops-prod" }, { label: "Chat" }, { label: "#kappa" }],
  approvals: [{ label: "Workspace" }, { label: "ops-prod" }, { label: "Approvals" }],
  worktrees: [{ label: "Workspace" }, { label: "ops-prod" }, { label: "Worktrees" }],
  alerts: [{ label: "Workspace" }, { label: "ops-prod" }, { label: "Alerts" }],
  system: [{ label: "Workspace" }, { label: "ops-prod" }, { label: "System" }],
  costs: [{ label: "Workspace" }, { label: "ops-prod" }, { label: "Costs" }],
  annotations: [{ label: "Workspace" }, { label: "ops-prod" }, { label: "Annotations" }]
};
function ConsoleLayout() {
  var _a;
  const location = useLocation();
  const navigate = useNavigate();
  const params = useParams();
  const surface = surfaceFromPath(location.pathname);
  const [spawnOpen, setSpawnOpen] = reactExports.useState(false);
  const [missionOpen, setMissionOpen] = reactExports.useState(false);
  const [palette, setPalette] = reactExports.useState(false);
  const [banner, setBanner] = reactExports.useState("off");
  const [sseConnected, setSSEConnected] = reactExports.useState(false);
  const [showArchived, setShowArchived] = reactExports.useState(false);
  const [lastUpdated, setLastUpdated] = reactExports.useState(null);
  const [dataStale, setDataStale] = reactExports.useState(false);
  const [showReconnecting, setShowReconnecting] = reactExports.useState(false);
  const storeWorkers = useWorkerStore((s) => s.workers);
  const hydrateWorkers = useWorkerStore((s) => s.hydrate);
  const killWorker = useWorkerStore((s) => s.killWorker);
  const restartWorker = useWorkerStore((s) => s.restartWorker);
  const stopWorker = useWorkerStore((s) => s.stopWorker);
  const storeTasks = useTaskStore((s) => s.tasks);
  const hydrateTasks = useTaskStore((s) => s.hydrate);
  const setTask = useTaskStore((s) => s.setTask);
  const setTasks = useTaskStore((s) => s.setTasks);
  const pendingApprovals = useApprovalStore(
    (s) => Object.values(s.approvals).filter((i) => i.metadata.status === "pending").length
  );
  const hydrateApprovals = useApprovalStore((s) => s.hydrate);
  const activeDuties = useDutyStore(
    (s) => Object.values(s.duties).filter((d) => d.status === "active").length
  );
  const hydrateDuties = useDutyStore((s) => s.hydrate);
  const alertCount = useAlertStore(
    (s) => s.counts.critical + s.counts.warning + s.counts.info
  );
  const pendingAnnotations = useAnnotationStore((s) => s.annotations.length);
  const hydrateAnnotations = useAnnotationStore((s) => s.hydrate);
  const fetchAlerts = useAlertStore((s) => s.fetchAlerts);
  const pollHealth = useSystemStore((s) => s.pollHealth);
  const pollStats = useSystemStore((s) => s.pollStats);
  const [worktreeCount, setWorktreeCount] = reactExports.useState(0);
  const [costTotal, setCostTotal] = reactExports.useState(0);
  const tracerEmitted = reactExports.useRef(false);
  const cap16TracerEmitted = reactExports.useRef(false);
  reactExports.useEffect(() => {
    const controller = new AbortController();
    authedFetch("/api/worktrees", { signal: controller.signal }).then((r) => r.ok ? r.json() : Promise.reject(r.status)).then((d) => setWorktreeCount(d.worktrees.length)).catch(() => {
    });
    return () => controller.abort();
  }, []);
  reactExports.useEffect(() => {
    const fetchCost = () => api.costs.today().then((d) => setCostTotal(d.total_usd ?? 0)).catch(() => {
    });
    void fetchCost();
    const id = setInterval(fetchCost, 1e4);
    return () => clearInterval(id);
  }, []);
  const probeAndHydrate = reactExports.useCallback(() => {
    authedFetch("/api/workers").then((r) => {
      if (!r.ok) throw new Error(`HTTP ${r.status}`);
      setLastUpdated(Date.now());
      setDataStale(false);
      void useWorkerStore.getState().hydrate();
      void useTaskStore.getState().hydrate();
    }).catch(() => {
      setDataStale(true);
      tracer("CAP-25", "ok");
    });
  }, []);
  const handleRefresh = reactExports.useCallback(() => {
    probeAndHydrate();
  }, [probeAndHydrate]);
  reactExports.useEffect(() => {
    const unsubConnection = eventBus.onConnectionChange(setSSEConnected);
    const cleanupEvents = initEvents();
    probeAndHydrate();
    void pollHealth();
    void pollStats();
    void hydrateApprovals();
    void hydrateDuties();
    void fetchAlerts();
    void hydrateAnnotations();
    const timer = setInterval(() => {
      probeAndHydrate();
      void pollHealth();
      void pollStats();
      void hydrateApprovals();
      void hydrateDuties();
      void fetchAlerts();
      void hydrateAnnotations();
    }, POLL_INTERVAL_MS);
    return () => {
      clearInterval(timer);
      cleanupEvents();
      unsubConnection();
    };
  }, [probeAndHydrate, pollHealth, pollStats, hydrateApprovals, hydrateDuties, fetchAlerts, hydrateAnnotations]);
  reactExports.useEffect(() => {
    void hydrateWorkers();
    void hydrateTasks();
  }, [hydrateWorkers, hydrateTasks]);
  reactExports.useEffect(() => {
    let timer;
    const unsubscribe = eventBus.onConnectionChange((connected) => {
      if (!connected) {
        timer = setTimeout(() => setShowReconnecting(true), 1e4);
      } else {
        clearTimeout(timer);
        setShowReconnecting(false);
      }
    });
    return () => {
      clearTimeout(timer);
      unsubscribe();
    };
  }, []);
  const workers = reactExports.useMemo(
    () => workersToUI(Object.values(storeWorkers).filter(isFleetVisibleWorker)),
    [storeWorkers]
  );
  const tasks = reactExports.useMemo(
    () => tasksToUI(Object.values(storeTasks)),
    [storeTasks]
  );
  reactExports.useEffect(() => {
    if (!tracerEmitted.current && workers.length > 0) {
      tracerEmitted.current = true;
      emitTracerMarker();
    }
  }, [workers]);
  reactExports.useEffect(() => {
    if (pendingApprovals > 0) {
      tracer("CAP-17", "ok");
    }
  }, [pendingApprovals]);
  reactExports.useEffect(() => {
    tracer("UI-F", "ok");
  }, []);
  reactExports.useEffect(() => {
    api.tasks.list({ includeArchived: showArchived }).then((tasks2) => setTasks(tasks2)).catch(() => {
    });
  }, [showArchived, setTasks]);
  reactExports.useEffect(() => {
    if (params.taskId && !cap16TracerEmitted.current) {
      cap16TracerEmitted.current = true;
      tracer("CAP-16", "ok");
    }
  }, [params.taskId]);
  reactExports.useEffect(() => {
    if (costTotal >= BUDGET_WARN_RATIO * BUDGET_HARD_USD) {
      tracer("CAP-18", "ok");
      setBanner("budget");
    } else {
      setBanner("off");
    }
  }, [costTotal]);
  const liveAlerts = useAlertStore((s) => s.alerts);
  const liveAttention = reactExports.useMemo(() => {
    const out = [];
    const sevMap = {
      critical: "danger",
      warning: "warn",
      info: "info"
    };
    for (const a of liveAlerts.filter((a2) => !a2.acknowledged).slice(0, 6)) {
      out.push({
        id: `alert:${a.alert_id}`,
        severity: sevMap[a.severity] ?? "info",
        title: a.title,
        body: a.body,
        action: "Inspect",
        workerId: a.source_worker_id ?? null,
        taskId: a.source_task_id ?? null,
        alertId: a.alert_id
      });
    }
    const blocked = Object.values(storeTasks).filter(
      (t) => t.status === "blocked" || t.status === "conflict"
    );
    for (const t of blocked.slice(0, 4)) {
      out.push({
        id: `task:${t.id}:${t.status}`,
        severity: t.status === "conflict" ? "danger" : "warn",
        title: `Task ${t.id} — ${t.status}`,
        body: t.type || `Task is ${t.status}.`,
        action: "Open task",
        workerId: t.worker_id ?? null,
        taskId: t.id
      });
    }
    const ATTENTION_BACKEND_STATUSES = /* @__PURE__ */ new Set([
      "warn",
      "error",
      "timeout",
      "crashed",
      "interrupted",
      "blocked",
      "degraded",
      "dead",
      "draining",
      "paused"
    ]);
    const attentionWorkers = Object.values(storeWorkers).filter(
      (w) => ATTENTION_BACKEND_STATUSES.has(w.status)
    );
    for (const w of attentionWorkers.slice(0, 3)) {
      const isFatal = w.status === "dead" || w.status === "error" || w.status === "crashed";
      out.push({
        id: `worker:${w.id}:${w.status}`,
        severity: isFatal ? "danger" : "warn",
        title: `Worker ${w.id} — ${w.status}`,
        body: w.error ? w.error : `Worker is in ${w.status} state. Inspect heartbeat / task lease.`,
        action: "Inspect",
        workerId: w.id
      });
    }
    if (attentionWorkers.length > 0) {
      tracer("CAP-53", "ok dashboard-worker-health-live count=" + attentionWorkers.length);
    }
    if (pendingApprovals > 0) {
      out.push({
        id: `approvals:${pendingApprovals}`,
        severity: "info",
        title: pendingApprovals === 1 ? "1 approval waiting" : `${pendingApprovals} approvals waiting`,
        body: "Review pending approvals.",
        action: "Review"
      });
    }
    if (costTotal > 0 && costTotal >= BUDGET_HARD_USD * BUDGET_WARN_RATIO) {
      const pct = Math.round(costTotal / BUDGET_HARD_USD * 100);
      const bucket = Math.floor(pct / 10) * 10;
      out.push({
        id: `budget:${bucket}`,
        severity: pct >= 100 ? "danger" : "warn",
        title: `Budget: ${pct}% of daily cap`,
        body: `Daily burn at $${costTotal.toFixed(2)} / $${BUDGET_HARD_USD.toFixed(2)}.`,
        action: "Open costs"
      });
    }
    if (out.length > 0) {
      tracer("CAP-50", "ok dashboard-attention-live count=" + out.length);
    }
    return out;
  }, [liveAlerts, pendingApprovals, storeTasks, storeWorkers, costTotal]);
  const counts = reactExports.useMemo(() => ({
    workers: workers.length,
    running: workers.filter((w) => w.status === "running").length,
    tasks: tasks.filter((t) => ["queued", "assigned", "running", "blocked", "conflict"].includes(t.status)).length,
    duties: activeDuties,
    approvals: pendingApprovals,
    worktrees: worktreeCount,
    alerts: alertCount,
    annotations: pendingAnnotations
  }), [workers, tasks, activeDuties, pendingApprovals, worktreeCount, alertCount, pendingAnnotations]);
  const selectedWorker = params.workerId ? workers.find((w) => w.id === params.workerId) ?? null : null;
  const liveSelectedTask = params.taskId ? tasks.find((t) => t.id === params.taskId) ?? null : null;
  const stickyTaskRef = reactExports.useRef(null);
  if (liveSelectedTask && liveSelectedTask.id === params.taskId) {
    stickyTaskRef.current = liveSelectedTask;
  } else if (params.taskId && ((_a = stickyTaskRef.current) == null ? void 0 : _a.id) !== params.taskId) {
    stickyTaskRef.current = null;
  } else if (!params.taskId) {
    stickyTaskRef.current = null;
  }
  const selectedTask = liveSelectedTask ?? stickyTaskRef.current;
  const moveTask = async (id, status) => {
    const previous2 = storeTasks[id];
    const TERMINALS = ["done", "failed", "cancelled"];
    const isTerminal = TERMINALS.includes(status);
    let overrideReason = null;
    if (isTerminal && previous2 && !TERMINALS.includes(previous2.status)) {
      const reason = window.prompt(
        `Manually mark task ${id} as "${status}"?

This bypasses the worker completion path. Enter an audit reason:`
      );
      if (!reason || !reason.trim()) {
        throw new Error("terminal_override_cancelled");
      }
      overrideReason = reason.trim();
    }
    if (previous2) {
      setTask(id, { ...previous2, status });
    }
    try {
      const body = overrideReason ? { status, operator_override: true, override_reason: overrideReason } : { status };
      const updated = await api.tasks.update(id, body);
      setTask(id, updated);
    } catch (err) {
      if (previous2) {
        setTask(id, previous2);
      }
      throw err;
    }
  };
  const handleArchive = reactExports.useCallback(async (taskId, currentlyArchived) => {
    try {
      if (currentlyArchived) {
        await api.tasks.unarchive(taskId);
      } else {
        await api.tasks.archive(taskId);
      }
      void useTaskStore.getState().hydrate();
    } catch (err) {
      useToastStore.getState().push({
        type: "error",
        title: currentlyArchived ? "Unarchive failed" : "Archive failed",
        message: err instanceof Error ? err.message : "Unknown error"
      });
    }
  }, []);
  const pickWorker = (w) => navigate(`/console/workers/${w.id}`);
  const navigateTo = (r) => navigate(ROUTE_PATHS[r]);
  reactExports.useEffect(() => {
    const kb = (e) => {
      if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === "k") {
        e.preventDefault();
        setPalette(true);
      } else if (e.key === "Escape") {
        setPalette(false);
        setSpawnOpen(false);
        setMissionOpen(false);
      }
    };
    window.addEventListener("keydown", kb);
    return () => window.removeEventListener("keydown", kb);
  }, []);
  const crumbs = reactExports.useMemo(() => {
    if (selectedWorker) {
      return [
        { label: "Workspace" },
        { label: "ops-prod" },
        { label: "Fleet", to: "/console/fleet" },
        { label: selectedWorker.name }
      ];
    }
    if (selectedTask) {
      return [
        { label: "Workspace" },
        { label: "ops-prod" },
        { label: "Missions", to: "/console/kanban" },
        { label: selectedTask.title || selectedTask.id }
      ];
    }
    if (params.workerId) {
      return [
        { label: "Workspace" },
        { label: "ops-prod" },
        { label: "Fleet", to: "/console/fleet" },
        { label: params.workerId }
      ];
    }
    if (params.taskId) {
      return [
        { label: "Workspace" },
        { label: "ops-prod" },
        { label: "Missions", to: "/console/kanban" },
        { label: params.taskId }
      ];
    }
    return CRUMBS_MAP[surface] ?? [{ label: "Workspace" }];
  }, [selectedWorker, selectedTask, params.workerId, params.taskId, surface]);
  let view;
  if (workers.length === 0 && tasks.length === 0 && surface === "dash") {
    view = /* @__PURE__ */ jsxRuntimeExports.jsx(Onboard, { onSpawn: () => setSpawnOpen(true) });
  } else if (selectedWorker) {
    view = /* @__PURE__ */ jsxRuntimeExports.jsx(
      WorkerDetail,
      {
        worker: selectedWorker,
        onBack: () => navigate("/console/fleet"),
        onKill: async (id) => {
          await killWorker(id);
          navigate("/console/fleet");
        },
        onRestart: async (id) => {
          await restartWorker(id);
          navigate("/console/fleet");
        },
        onStop: async (id) => {
          await stopWorker(id, "graceful");
        }
      }
    );
  } else if (params.taskId) {
    view = selectedTask ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { "data-task-id": params.taskId, style: { display: "flex", flexDirection: "column", flex: 1, minHeight: 0 }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      TaskDetail,
      {
        task: selectedTask,
        worker: workers.find((w) => w.id === selectedTask.worker),
        onBack: () => navigate("/console/kanban")
      }
    ) }) : (
      // Skeleton shown while store hydrates. Provides data-testid so tracer can
      // poll before TaskDetail mounts. Replaced by TaskDetail once task loads.
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { "data-testid": "task-detail", "data-task-id": params.taskId, style: { padding: 24, color: "var(--hv-fg-subtle)" }, children: "Loading task…" })
    );
  } else if (surface === "dash") {
    view = /* @__PURE__ */ jsxRuntimeExports.jsx(
      Dashboard,
      {
        workers,
        tasks,
        attention: liveAttention,
        sseConnected,
        onPickWorker: pickWorker,
        onOpenKanban: () => navigateTo("kanban"),
        onOpenFleet: () => navigateTo("fleet")
      }
    );
  } else if (surface === "kanban") {
    view = /* @__PURE__ */ jsxRuntimeExports.jsx(
      Kanban,
      {
        tasks,
        onMoveTask: moveTask,
        onOpen: (t) => navigate(`/console/missions/${t.id}`),
        onNewMission: () => setMissionOpen(true),
        onArchive: handleArchive,
        showArchived,
        onToggleArchived: () => setShowArchived((v) => !v)
      }
    );
  } else if (surface === "duties") {
    view = /* @__PURE__ */ jsxRuntimeExports.jsx(DutiesView, {});
  } else if (surface === "fleet") {
    view = /* @__PURE__ */ jsxRuntimeExports.jsx(
      FleetView,
      {
        workers,
        onPickWorker: pickWorker,
        selected: selectedWorker
      }
    );
  } else if (surface === "chat") {
    view = /* @__PURE__ */ jsxRuntimeExports.jsx(Chat, {});
  } else if (surface === "approvals") {
    view = /* @__PURE__ */ jsxRuntimeExports.jsx(Approvals, {});
  } else if (surface === "alerts") {
    view = /* @__PURE__ */ jsxRuntimeExports.jsx(Alerts, {});
  } else if (surface === "worktrees") {
    view = /* @__PURE__ */ jsxRuntimeExports.jsx(Worktrees, {});
  } else if (surface === "costs") {
    view = /* @__PURE__ */ jsxRuntimeExports.jsx(Costs, { workers });
  } else if (surface === "system") {
    view = /* @__PURE__ */ jsxRuntimeExports.jsx(System, {});
  } else if (surface === "annotations") {
    view = /* @__PURE__ */ jsxRuntimeExports.jsx(AnnotationsView, {});
  } else {
    view = /* @__PURE__ */ jsxRuntimeExports.jsx(
      Dashboard,
      {
        workers,
        tasks,
        attention: liveAttention,
        sseConnected,
        onPickWorker: pickWorker,
        onOpenKanban: () => navigateTo("kanban"),
        onOpenFleet: () => navigateTo("fleet")
      }
    );
  }
  return (
    // data-testid="console-root" preserved per Wave-0 task I contract.
    // CAP-25b: data-stale applied here (ConsolePage owns this element, not Shell.tsx).
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "console-root",
        "data-testid": "console-root",
        "data-stale": dataStale ? "true" : void 0,
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Sidebar,
            {
              surface,
              counts,
              version: VERSION
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hv-main", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Topbar,
              {
                crumbs,
                onNewMission: () => setMissionOpen(true),
                onNewDuty: () => {
                  navigate(`${ROUTE_PATHS.duties}?new=1`);
                  tracer("CAP-49", "ok global-new-duty source=topbar");
                },
                onSpawn: () => setSpawnOpen(true),
                onOpenPalette: () => setPalette(true),
                lastUpdated,
                onRefresh: handleRefresh,
                showReconnecting
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Banner, { state: banner, onDismiss: () => setBanner("off") }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "hv-content", children: view })
          ] }),
          spawnOpen && /* @__PURE__ */ jsxRuntimeExports.jsx(SpawnModal, { onClose: () => setSpawnOpen(false) }),
          missionOpen && /* @__PURE__ */ jsxRuntimeExports.jsx(MissionModal, { onClose: () => setMissionOpen(false), workers }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            CmdPalette,
            {
              open: palette,
              onClose: () => setPalette(false),
              onNav: (r) => {
                if (r === "spawn") {
                  setSpawnOpen(true);
                  return;
                }
                if (r === "new-mission") {
                  setMissionOpen(true);
                  return;
                }
                if (r === "new-duty") {
                  navigate(`${ROUTE_PATHS.duties}?new=1`);
                  tracer("CAP-49", "ok global-new-duty source=palette");
                  return;
                }
                navigateTo(r);
              }
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(ToastContainer, {}),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { "aria-hidden": "true", style: { display: "none" }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { "data-testid": "fleet-worker-count", children: workers.length }),
            workers.map((w) => (
              // CAP-05: onClick added so Playwright can navigate to WorkerDetail
              // by dispatching a synthetic click via page.evaluate (display:none
              // prevents real pointer events; JS dispatchEvent bypasses that).
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "span",
                {
                  "data-worker-id": w.id,
                  onClick: () => navigate(`/console/workers/${w.id}`)
                },
                w.id
              )
            ))
          ] })
        ]
      }
    )
  );
}
function ConsolePage() {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Routes, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "missions/:taskId", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "workers/:workerId", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "worktrees/:worktreeId", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "channels/:channelId", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "kanban", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "fleet", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "duties", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "approvals", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "alerts", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "worktrees", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "costs", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "system", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "channels", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "annotations", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "*", element: /* @__PURE__ */ jsxRuntimeExports.jsx(ConsoleLayout, {}) })
  ] });
}
export {
  ConsolePage
};
